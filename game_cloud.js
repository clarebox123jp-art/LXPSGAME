/* ════════════════════════════════════════════════════════════════════
 *  game_cloud.js — 小英雄大對抗主程式模組
 *  ★ v5.235.0(2026-09-25)模組化拆分第一階段:本檔內容是從 index.html 的 inline <script>
 *    「逐位元組原封不動」搬出來的,執行位置與順序與拆分前完全相同(index.html 在原處用
 *    window._lxpsModLoad('game_cloud.js') 載入)。
 *  內容:Firebase 登入／雲端存檔／Firestore 串接(原 index.html 第 386 行起的 <script type="module">,ES module)
 *  ⚠ 改本檔後:_LXPS_FILE_VERSIONS['game_cloud.js'] 必須改成本輪版號,並重新產生 version.json;
 *    上傳順序一律「本檔 → index.html → version.json」(先模組、後主檔),
 *    否則新 index.html 會用新版號去抓到舊內容,並被瀏覽器以新版號快取住。
 *  ⚠ 檔尾最後一行是載入完成記號,不可刪、不可在它後面加程式。
 * ════════════════════════════════════════════════════════════════════ */

  import { initializeApp } from "https://www.gstatic.com/firebasejs/10.12.0/firebase-app.js";
  import { getAuth, GoogleAuthProvider, signInWithPopup, signInWithRedirect, getRedirectResult, signOut, onAuthStateChanged, signInWithCredential }   /* ★ v5.148.0 加 signInWithCredential（直連 OAuth） */
    from "https://www.gstatic.com/firebasejs/10.12.0/firebase-auth.js";
  import { getFirestore, doc, setDoc, getDoc, getDocFromServer, getDocs, collection, query, where, orderBy, limit, updateDoc, arrayUnion, arrayRemove, onSnapshot, increment, deleteDoc, deleteField, runTransaction, getCountFromServer }   /* ★ v5.235.0 加 getCountFromServer(目前在線人數改用計數查詢,1 次查詢只算 1 次讀取) */
    from "https://www.gstatic.com/firebasejs/10.12.0/firebase-firestore.js";

  const firebaseConfig = {
    apiKey: "AIzaSyDTsWozvDMoJyDhPmiYi1QZ31rlyy7fxZc",
    authDomain: "lxpsgame.firebaseapp.com",
    projectId: "lxpsgame",
    storageBucket: "lxpsgame.firebasestorage.app",
    messagingSenderId: "887017862215",
    appId: "1:887017862215:web:7f6248ec2e5684cf63ed62"
  };

  const _fbApp  = initializeApp(firebaseConfig);
  const _fbAuth = getAuth(_fbApp);
  const _fbDb   = getFirestore(_fbApp);
  const _fbProvider = new GoogleAuthProvider();

  // ★ v3.11.29 — 把 Firestore 核心物件掛到 window,供動態載入的外部模組使用。
  //   根因:admin_panel.js(動態載入)與 index.html 內的 _adminCleanMyOwnWeeklyQuiz
  //   都讀 window._fbDb,但 _fbDb/_fbAuth 原本是模組內 const、從未掛 window,
  //   導致「刪除小博士排名」報「window._fbDb 不可用」。一併掛 _fbApp 與常用
  //   Firestore 函式(doc / runTransaction / setDoc / getDoc),讓外部模組免再動態 import。
  try{
    window._fbApp  = _fbApp;
    window._fbAuth = _fbAuth;
    window._fbDb   = _fbDb;
    window._fbFns  = { doc, setDoc, getDoc, getDocFromServer, getDocs, collection,
      query, where, orderBy, limit, updateDoc, arrayUnion, arrayRemove,
      onSnapshot, increment, deleteDoc, deleteField, runTransaction };
  }catch(_eExpose){ console.warn('[v3.11.29] 暴露 Firestore 物件到 window 失敗', _eExpose); }


  // ── D. 可見診斷面板(iPad 拍照用) ─────────────────────────────────
  window._fbDiagLogs = [];
  // ★ v3.3.7 — 記錄頁面載入時刻,給 onAuthStateChanged race 保護使用
  //   不能等 onAuthStateChanged 才設(那時就太晚了,race 視窗已經過了)
  if(!window._pageLoadedAt) window._pageLoadedAt = Date.now();
  window._fbDiag = function(msg, level){
    try{
      const ts = new Date();
      const hh = String(ts.getHours()).padStart(2,'0');
      const mm = String(ts.getMinutes()).padStart(2,'0');
      const ss = String(ts.getSeconds()).padStart(2,'0');
      const ms = String(ts.getMilliseconds()).padStart(3,'0');
      const line = `[${hh}:${mm}:${ss}.${ms}] ${msg}`;
      window._fbDiagLogs.push({ line, level: level || 'info' });
      // 同步輸出到 console
      const fn = (level === 'error') ? console.error
              : (level === 'warn')  ? console.warn  : console.log;
      fn('[FBDiag]', line);
      // 自動寫到面板(若已建立)
      _fbDiagPaint();
    }catch(_){}
  };
  function _fbDiagPaint(){
    try{
      const el = document.getElementById('_fb-diag-panel');
      if(!el) return;
      const body = el.querySelector('._fb-diag-body');
      if(!body) return;
      body.innerHTML = window._fbDiagLogs.map(o => {
        const c = (o.level === 'error') ? '#ff8866'
                : (o.level === 'warn')  ? '#ffcc44' : '#88dd88';
        return `<div style="color:${c};font-family:monospace;font-size:11px;line-height:1.4;border-bottom:1px solid #333;padding:2px 0;word-break:break-all;">${o.line.replace(/[<>&]/g, c=>({ '<':'&lt;','>':'&gt;','&':'&amp;' }[c]))}</div>`;
      }).join('');
      // 自動捲到最底
      body.scrollTop = body.scrollHeight;
    }catch(_){}
  }
  window._fbShowDiag = function(){
    try{
      let el = document.getElementById('_fb-diag-panel');
      if(!el){
        el = document.createElement('div');
        el.id = '_fb-diag-panel';
        el.style.cssText = 'position:fixed;right:8px;bottom:8px;width:min(420px,92vw);max-height:60vh;'
          + 'background:rgba(0,0,0,0.92);border:2px solid #4488ff;border-radius:10px;z-index:99999;'
          + 'box-shadow:0 4px 24px rgba(0,0,0,0.6);display:flex;flex-direction:column;overflow:hidden;';
        el.innerHTML = `
          <div style="display:flex;align-items:center;gap:6px;padding:6px 10px;background:#1a3a6b;color:#cfe7ff;font-weight:700;font-size:13px;">
            🔍 登入診斷面板
            <span style="flex:1"></span>
            <button id="_fb-diag-copy" style="background:#2a5aa0;color:#fff;border:0;border-radius:4px;padding:3px 8px;font-size:12px;cursor:pointer;">複製</button>
            <button id="_fb-diag-close" style="background:#6a2a2a;color:#fff;border:0;border-radius:4px;padding:3px 8px;font-size:12px;cursor:pointer;">關閉</button>
          </div>
          <div class="_fb-diag-body" style="flex:1;overflow-y:auto;padding:6px 8px;background:#0a0a0a;"></div>`;
        document.body.appendChild(el);
        el.querySelector('#_fb-diag-close').onclick = () => el.remove();
        el.querySelector('#_fb-diag-copy').onclick = () => {
          const txt = window._fbDiagLogs.map(o => o.line).join('\n');
          try{
            if(navigator.clipboard && navigator.clipboard.writeText){
              navigator.clipboard.writeText(txt).then(
                () => { el.querySelector('#_fb-diag-copy').textContent = '✓ 已複製'; },
                () => { _fbDiagFallbackCopy(txt, el); }
              );
            }else{ _fbDiagFallbackCopy(txt, el); }
          }catch(_){ _fbDiagFallbackCopy(txt, el); }
        };
      }
      _fbDiagPaint();
    }catch(_){}
  };
  function _fbDiagFallbackCopy(txt, el){
    try{
      const ta = document.createElement('textarea');
      ta.value = txt;
      ta.style.cssText = 'position:fixed;top:-1000px;left:0;';
      document.body.appendChild(ta);
      ta.select();
      document.execCommand('copy');
      ta.remove();
      if(el) el.querySelector('#_fb-diag-copy').textContent = '✓ 已複製';
    }catch(_){}
  }

  _fbDiag('boot:_fbApp 已建立, UA=' + (navigator.userAgent || '').slice(0, 80));
  // ★ FIX 20260517(gg) — 印 URL/origin/authDomain,協助診斷 redirect 回不來
  try{
    const _loc = window.location;
    _fbDiag('boot:origin=' + _loc.origin);
    _fbDiag('boot:href=' + _loc.href.slice(0, 200));
    _fbDiag('boot:hash長度=' + (_loc.hash || '').length + (_loc.hash ? ' (有 hash,可能含 OAuth credential)' : ''));
    _fbDiag('boot:search=' + (_loc.search || '(無)').slice(0, 120));
    _fbDiag('boot:authDomain=' + (firebaseConfig && firebaseConfig.authDomain));
    // 是否同 origin
    try{
      const _authOrigin = 'https://' + (firebaseConfig && firebaseConfig.authDomain || '');
      _fbDiag('boot:authOrigin=' + _authOrigin + ' (同 origin? ' + (_loc.origin === _authOrigin ? '是' : '否 — 跨域') + ')');
    }catch(_){}
    // referrer(可看是否從 accounts.google.com 跳回)
    try{
      _fbDiag('boot:referrer=' + ((document.referrer || '(無)').slice(0, 120)));
    }catch(_){}
    // sessionStorage 裡 firebase 的 pendingRedirect key(直接看 Firebase 內部有沒有)
    try{
      const _ssKeys = Object.keys(sessionStorage || {});
      const _fbKeys = _ssKeys.filter(k => k.indexOf('firebase') >= 0);
      _fbDiag('boot:sessionStorage 內 firebase keys=' + (_fbKeys.length ? _fbKeys.join(',').slice(0, 160) : '(無)'));
    }catch(_){}
  }catch(_){}

  // ── A. 不再呼叫 setPersistence ───────────────────────────────────
  // ★ FIX 20260517(ff) — iPad Chrome 登入回不來的真正根因(用診斷面板抓到)!
  //
  // 從 iPad 端診斷面板 log 看到:
  //   [07:07:50.117] persistence:✅ indexedDBLocalPersistence OK (耗時 759ms)
  //   [07:07:50.119] redirect:getRedirectResult 完成 (耗時 0ms)
  //   [07:07:50.119] redirect:⚠ 有 pending 旗標但 getRedirectResult 回 null
  //   [07:07:50.121] redirect:currentUser=null
  //
  // getRedirectResult 在 0ms 內回 null + currentUser=null
  // → Firebase 內部 OAuth credential 完全沒處理到。
  //
  // 對照 Firebase issue #6827 + 官方文件:
  //   1. getAuth() 觸發內部 initializeCurrentUser 異步任務,該任務會去
  //      解析 URL hash 裡的 OAuth credential、寫 sessionStorage key
  //      `firebase:pendingRedirect:...`。
  //   2. setPersistence() 切換 persistence 時會清掉舊 persistence 上的
  //      暫存資料 → 在 initializeCurrentUser 還沒完成時就被清。
  //   3. Firebase 官方文件:「當 signInWithRedirect 被呼叫,目前的 persistence
  //      會被保留並套用到回來時新登入的 user」→ 發起跟回來時的 persistence
  //      必須一致;setPersistence 切換會破壞這個機制。
  //
  // 比對「三天前的版本」(用戶上傳到 /mnt/user-data/uploads/index.html):
  //   - 三天前完全沒呼叫 setPersistence,也沒 import 那幾個 persistence 模式
  //   - 為了世界 BOSS 連線新功能改 auth 區塊時誤加 setPersistence
  //   - 在 iPad Chrome 慢 IndexedDB 環境下觸發 race condition → 登入回不來
  //
  // 修法:回到三天前的做法,完全不呼叫 setPersistence。Firebase 預設
  //       persistence 就是 browserLocalPersistence(有 IndexedDB 自動用),
  //       對我們完全夠用。連線世界 BOSS 跟 auth persistence 沒任何關聯,
  //       那段 v10 (t) 的修補是誤判導致的迴歸。
  //
  // _fbAuthReady 保留為立即就緒 Promise,讓其他地方的 `await _fbAuthReady`
  // 不會出錯(向後相容,不需要全檔改)。
  // ────────────────────────────────────────────────────────────────
  window._fbAuthReady = Promise.resolve('default');
  _fbDiag('persistence:跳過 setPersistence(FIX ff),沿用 Firebase 預設 — 避免跟 redirect 流程競態');


  // 顯示登入錯誤的友善訊息(被擋 / 網路 / 無權限等)
  function _showLoginErrorTip(title, hint){
    try {
      // 避免重複疊加
      const old = document.getElementById('_login-error-tip');
      if(old) old.remove();
      const pop = document.createElement('div');
      pop.id = '_login-error-tip';
      pop.style.cssText =
        'position:fixed;inset:0;z-index:99998;background:rgba(0,0,0,0.85);' +
        'display:flex;align-items:center;justify-content:center;' +
        'font-family:"M PLUS Rounded 1c","Nunito",sans-serif;';
      const card = document.createElement('div');
      card.style.cssText =
        'background:linear-gradient(135deg,#1a0820,#0d0515);' +
        'border:3px solid #ff6688;border-radius:24px;' +
        'padding:36px 40px;max-width:min(92vw,520px);text-align:center;' +
        'box-shadow:0 0 50px rgba(255,80,120,0.4);';
      card.innerHTML =
        '<div style="font-size:54px;margin-bottom:14px;">⚠️</div>' +
        '<div style="font-size:24px;font-weight:900;color:#ff88aa;margin-bottom:10px;">' + title + '</div>' +
        '<div style="font-size:16px;color:#ddd;line-height:1.7;margin-bottom:22px;text-align:left;">' + hint + '</div>' +
        '<button id="_login-error-close" style="padding:12px 32px;font-size:18px;font-weight:900;' +
          'background:linear-gradient(135deg,rgba(80,150,255,0.3),rgba(40,100,200,0.3));' +
          'border:2px solid #6688ff;color:#aaccff;border-radius:12px;cursor:pointer;">我知道了</button>';
      pop.appendChild(card);
      document.body.appendChild(pop);
      pop.addEventListener('click', function(e){ if(e.target===pop) pop.remove(); });
      document.getElementById('_login-error-close').onclick = function(){ pop.remove(); };
    } catch(_) { /* 連 DOM 都不行就 alert */
      try { alert(title + '\n\n' + hint.replace(/<[^>]+>/g,'')); } catch(__) {}
    }
  }

  // ★ FIX 20260517(gg) — 三天前能用、現在壞掉的真正根因
  //
  // 從 iPad 端診斷面板 log 看到:
  //   boot:authOrigin=https://lxpsgame.firebaseapp.com (同 origin? 否 — 跨域)
  //   boot:referrer=(無)
  //   boot:sessionStorage 內 firebase keys=firebase:pendingRedirect:xxx ← 進去有
  //   redirect:getRedirectResult 完成 (耗時 535ms)
  //   redirect:result 物件=null
  //   redirect:sessionStorage firebase keys(after)=(無)  ← 處理完被清掉
  //   redirect:現在 URL hash 長度=0 search=(無)  ← URL 沒帶 OAuth credential
  //
  // 對照 Firebase 官方 issue #8329 + #9366(2025年11月仍在燒) + #9108:
  //   「Starting June 24, 2024, if you're using Firebase Auth signInWithRedirect
  //    without implementing one of the options in best practices guide, end users
  //    will not be able to authenticate when using browsers that block third-party
  //    cookies. This is already required on Chrome M115+, Firefox 109+, Safari 16.1+」
  //
  // 真正根因:
  //   1. 我們的網頁載入 origin 不是 firebaseapp.com(用了非 Firebase Hosting,
  //      像 GitHub Pages),signInWithRedirect 需要跨域 iframe 跟 firebaseapp.com
  //      傳遞 credential,iPad Chrome 第三方 cookie 被擋 → 整個傳遞鏈失效。
  //   2. 三天前能用是巧合 — Chrome iOS 最近幾版加強了第三方 cookie 攔截,
  //      跟我們改 setPersistence 是巧合,不是根因。
  //
  // 修法:改成「所有環境都先試 popup,popup 失敗才 fallback redirect」。
  //   - popup 不靠第三方 cookie,iPad Chrome 上可用
  //   - 若 popup 被瀏覽器擋,使用者會看到瀏覽器自帶的「允許彈出視窗」提示,
  //     fallback redirect 雖然壞但至少有路徑
  //   - 三天前 _shouldPreferRedirect 對 iPad 一律走 redirect 是過時的策略
  //
  // 已不再需要 _shouldPreferRedirect,但保留函式防外部誤呼叫
  function _shouldPreferRedirect(){
    // ★ FIX 20260517(gg) — 永遠回傳 false:都先試 popup
    return false;
  }

  // ════════════════════════════════════════════════════════════════════
  // ★★ v5.148.0（2026-09-07・老師回報「iPad 學生登入困難／登入失敗一直要重複登入」根治）★★
  //   【病因（實查 @firebase/auth 1.7.4 原始碼）】iOS 主畫面安裝版（navigator.standalone===true）
  //   呼叫 signInWithPopup 時，SDK 的 _open() 走 openAsNewWindowIOS：用假的 <a target> 點擊把
  //   Google 頁面開到系統瀏覽器，回傳 AuthPopup(null) ⇒ 沒視窗可輪詢關閉、沒 window.opener
  //   送回結果 ⇒ promise 永遠不 resolve 也不 reject。⇒ _signInBusy 永久 true，之後每次按登入都
  //   被「忽略重複觸發」吃掉，學生只看到按了沒反應 → 關 App 重開 → 再來一次。
  //   而 signInWithRedirect 也救不了：authDomain 是跨域的 firebaseapp.com，Safari 16.1+ 的
  //   第三方儲存分區已讓 getRedirectResult 恆為 null（FIX gg 的 iPad 診斷 log 早已實證）。
  //   ⇒ iPad 安裝版在本輪之前**沒有任何可用的登入路徑**（過去靠裝置信任自動登入撐著）。
  //   【修法＝Firebase 官方最佳實務 Option 5】不用 Firebase 的 popup/redirect，整頁導向
  //   accounts.google.com（response_type=id_token）拿 ID Token，Google 回跳到**我們自己的網址**
  //   #id_token=…，開機時驗 state/nonce/aud 後 signInWithCredential ⇒ 全程第一方，不經
  //   firebaseapp.com iframe，安裝版與 Safari 都能完成。
  //   ⚠ 需老師在 Google Cloud Console 的 Web client 加「已授權重新導向 URI」＝本站
  //     https://clarebox123jp-art.github.io/LXPSGAME/ 與 …/LXPSGAME/index.html。
  //   ⚠ 共用平板防線一條都沒少：prompt=select_account 照帶、login_hint 只對準；回程以
  //     window._lxpsRedirectSignInThisPageload runtime 旗標放行（與 v5.136.0 同口徑，reload 即歸零）。
  // ════════════════════════════════════════════════════════════════════
  // 老師可填 Web client ID（Firebase 主控台 → Authentication → Google → Web SDK 設定）；
  // 留空 ⇒ 執行期用 identitytoolkit createAuthUri 自動探索並快取到 localStorage。
  window._LXPS_GOOGLE_WEB_CLIENT_ID = '887017862215-p3c8qeupiksengt4cbkmgu1gas23qse6.apps.googleusercontent.com';   // ★ v5.148.1 老師提供（Google Cloud Console 已加好重新導向 URI），免執行期 createAuthUri 探索，對校網更穩
  // 直連 OAuth 適用範圍：'off'=不用 / 'standalone'=只有 iOS 主畫面安裝版（預設・零回歸）/
  //   'ios'=所有 iPhone/iPad（含 Safari 分頁）/ 'all'=所有裝置。
  //   ★ 不論何種模式，popup 被擋／不支援／web-storage-unsupported 時一律改走直連（取代已壞的 signInWithRedirect）。
  window._LXPS_OAUTH_DIRECT_MODE = 'standalone';
  const _LXPS_OAUTH_DIRECT_KEY = 'lxps_oauth_direct_pending';
  const _LXPS_OAUTH_CID_KEY    = 'lxps_google_web_client_id';
  function _lxpsIsIOS(){
    try{
      const ua = navigator.userAgent || '';
      if(/iPad|iPhone|iPod/.test(ua)) return true;
      return (navigator.platform === 'MacIntel' && (navigator.maxTouchPoints || 0) > 1);   // iPadOS 桌面版 UA
    }catch(_){ return false; }
  }
  function _lxpsIsIOSStandalone(){
    try{ return _lxpsIsIOS() && (window.navigator.standalone === true); }catch(_){ return false; }   // 與 SDK _isIOSStandalone 同條件
  }
  function _lxpsOAuthDirectWanted(){
    const m = String(window._LXPS_OAUTH_DIRECT_MODE || 'standalone');
    if(m === 'off') return false;
    if(m === 'all') return true;
    if(m === 'ios') return _lxpsIsIOS();
    return _lxpsIsIOSStandalone();
  }
  function _lxpsRandHex(n){
    try{
      const a = new Uint8Array(n); crypto.getRandomValues(a);
      return Array.prototype.map.call(a, function(b){ return ('0' + b.toString(16)).slice(-2); }).join('');
    }catch(_){
      let s = ''; for(let i = 0; i < n * 2; i++){ s += Math.floor(Math.random() * 16).toString(16); } return s;
    }
  }
  function _lxpsB64UrlDecode(s){
    try{
      s = String(s || '').replace(/-/g, '+').replace(/_/g, '/');
      while(s.length % 4){ s += '='; }
      return decodeURIComponent(Array.prototype.map.call(atob(s), function(c){
        return '%' + ('00' + c.charCodeAt(0).toString(16)).slice(-2);
      }).join(''));
    }catch(_){ return ''; }
  }
  // 取得 Web client ID：常數 → 本機快取 → createAuthUri 自動探索
  window._lxpsOAuthDirectClientId = async function(){
    try{
      const c = String(window._LXPS_GOOGLE_WEB_CLIENT_ID || '').trim();
      if(c) return c;
    }catch(_){}
    try{
      const cached = localStorage.getItem(_LXPS_OAUTH_CID_KEY);
      if(cached && /apps\.googleusercontent\.com$/.test(cached)) return cached;
    }catch(_){}
    try{
      const _ctl = (typeof AbortController === 'function') ? new AbortController() : null;
      const _tm = setTimeout(function(){ try{ if(_ctl) _ctl.abort(); }catch(_){} }, 8000);
      const r = await fetch('https://identitytoolkit.googleapis.com/v1/accounts/createAuthUri?key=' + encodeURIComponent(firebaseConfig.apiKey), {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ providerId: 'google.com', continueUri: 'https://' + firebaseConfig.authDomain + '/__/auth/handler', oauthScope: 'openid email profile' }),
        signal: _ctl ? _ctl.signal : undefined
      });
      clearTimeout(_tm);
      const j = await r.json();
      const m = /[?&]client_id=([^&]+)/.exec(String(j && j.authUri || ''));
      const cid = m ? decodeURIComponent(m[1]) : '';
      if(cid){
        try{ localStorage.setItem(_LXPS_OAUTH_CID_KEY, cid); }catch(_){}
        _fbDiag('oauthDirect:自動探索到 Web client ID=' + cid + '（老師可貼進 _LXPS_GOOGLE_WEB_CLIENT_ID）');
        return cid;
      }
      _fbDiag('oauthDirect:createAuthUri 回應無 client_id ' + JSON.stringify(j).slice(0, 160), 'warn');
    }catch(e){
      _fbDiag('oauthDirect:探索 client ID 失敗 ' + (e && (e.code || e.message || e)), 'warn');
    }
    return '';
  };
  // 發起直連登入：回傳 true=已整頁導向 Google（本頁即將卸載）；false=無法使用（呼叫端退回 popup）
  window._lxpsOAuthDirectStart = async function(opts){
    opts = opts || {};
    const cid = await window._lxpsOAuthDirectClientId();
    if(!cid){ _fbDiag('oauthDirect:無 client ID，無法直連', 'warn'); return false; }
    try{
      const state = _lxpsRandHex(16), nonce = _lxpsRandHex(16);
      const redirect = window.location.origin + window.location.pathname;
      let _thenAdv = 0;
      try{ _thenAdv = sessionStorage.getItem('lxps_login_then_adventure') ? 1 : 0; }catch(_){}
      const rec = { state: state, nonce: nonce, ts: Date.now(), hint: String(opts.loginHint || ''), thenAdv: _thenAdv, redirect: redirect, cid: cid };
      localStorage.setItem(_LXPS_OAUTH_DIRECT_KEY, JSON.stringify(rec));
      const p = new URLSearchParams();
      p.set('client_id', cid);
      p.set('redirect_uri', redirect);
      p.set('response_type', 'id_token');
      p.set('scope', 'openid email profile');
      p.set('nonce', nonce);
      p.set('state', state);
      p.set('prompt', String(opts.prompt || 'select_account'));   // ★ v5.136.0 共用平板防線：一律問
      if(opts.loginHint) p.set('login_hint', String(opts.loginHint));
      const url = 'https://accounts.google.com/o/oauth2/v2/auth?' + p.toString();
      _fbDiag('oauthDirect:整頁導向 Google 登入 redirect=' + redirect + (opts.loginHint ? ' hint=' + opts.loginHint : ''));
      window.location.assign(url);
      return true;
    }catch(e){
      _fbDiag('oauthDirect:發起失敗 ' + (e && (e.code || e.message || e)), 'error');
      try{ localStorage.removeItem(_LXPS_OAUTH_DIRECT_KEY); }catch(_){}
      return false;
    }
  };
  // 回程處理：URL hash 有 id_token → 驗 state/nonce/aud → signInWithCredential。回傳 user 或 null。
  window._lxpsOAuthDirectResume = async function(){
    const h = String(window.location.hash || '');
    if(h.indexOf('id_token=') < 0 && h.indexOf('error=') < 0) return null;
    const params = new URLSearchParams(h.replace(/^#/, ''));
    const idTok = params.get('id_token'), st = params.get('state'), err = params.get('error');
    // 立刻把 token 從網址與歷史紀錄拿掉（重整不會再處理一次、也不留在 history）
    try{ history.replaceState(null, '', window.location.pathname + window.location.search); }catch(_){}
    let rec = null;
    try{ rec = JSON.parse(localStorage.getItem(_LXPS_OAUTH_DIRECT_KEY) || 'null'); }catch(_){ rec = null; }
    try{ localStorage.removeItem(_LXPS_OAUTH_DIRECT_KEY); }catch(_){}
    if(err){
      _fbDiag('oauthDirect:Google 回傳 error=' + err, 'warn');
      if(err !== 'access_denied'){
        _showLoginErrorTip('Google 登入未完成', '<b>錯誤代碼:</b> <code style="color:#ffaa66;">' + String(err).replace(/[<>]/g, '') + '</code><br><br>請重新點登入再試一次；重複失敗請把代碼告訴老師。');
      }
      return null;
    }
    if(!idTok) return null;
    if(!rec || !st || rec.state !== st){
      _fbDiag('oauthDirect:state 對不上（rec=' + (rec ? 'Y' : 'N') + '）', 'warn');
      _showLoginErrorTip('登入驗證資料對不上', '為了安全，這次登入結果不予採用。<br>請重新點「開始遊戲」再登入一次。');
      return null;
    }
    if(Date.now() - (rec.ts || 0) > 15 * 60 * 1000){
      _fbDiag('oauthDirect:回程逾時（>15 分鐘）', 'warn');
      _showLoginErrorTip('登入逾時', '從按下登入到回來已超過 15 分鐘。<br>請重新點「開始遊戲」再登入一次。');
      return null;
    }
    let payload = null;
    try{ payload = JSON.parse(_lxpsB64UrlDecode(idTok.split('.')[1] || '')); }catch(_){ payload = null; }
    if(!payload || payload.nonce !== rec.nonce || (rec.cid && payload.aud !== rec.cid)){
      _fbDiag('oauthDirect:nonce/aud 驗證失敗', 'warn');
      _showLoginErrorTip('登入驗證資料對不上', '為了安全，這次登入結果不予採用。<br>請重新點「開始遊戲」再登入一次。');
      return null;
    }
    // 放行本次合法登入（共用平板攔截器 v3.10.1／v5.136.0 認這個 runtime 旗標；reload 即歸零）
    window._lxpsRedirectSignInThisPageload = true;
    if(rec.thenAdv){ try{ sessionStorage.setItem('lxps_login_then_adventure', '1'); }catch(_){} }
    try{
      const cred = GoogleAuthProvider.credential(idTok);
      const res = await signInWithCredential(_fbAuth, cred);
      const u = res && res.user;
      if(u){
        window._lxpsOAuthDirectResumedUid = u.uid;
        _fbDiag('oauthDirect:✅ signInWithCredential 成功 user=' + (u.email || u.uid));
        try{ if(typeof window._addRecentAccount === 'function') window._addRecentAccount(u); }catch(_){}
        return u;
      }
      _fbDiag('oauthDirect:signInWithCredential 無 user', 'warn');
      return null;
    }catch(e){
      const code = e && e.code;
      _fbDiag('oauthDirect:signInWithCredential 失敗 code=' + (code || 'unknown'), 'error');
      window._lxpsRedirectSignInThisPageload = false;
      _showLoginErrorTip('登入時發生錯誤', '<b>錯誤代碼:</b> <code style="color:#ffaa66;">' + (code || 'unknown') + '</code><br><br>請重新點「開始遊戲」再登入一次。<br>如果重複失敗，請把這個錯誤代碼告訴老師。');
      return null;
    }
  };

  // ★ v3.13.53 — 登入併發鎖:防止連點登入鈕觸發多次 signInWithPopup(彼此取消 → auth/cancelled-popup-request)
  let _signInBusy = false;
  let _signInBusyAt = 0;   // ★ v5.148.0 — 鎖的時間戳（安全閥：卡超過 90 秒視為死鎖自動放行）
  // 核心:先試 popup,popup 失敗 fallback redirect,redirect 也失敗才彈錯誤
  async function _doSignInFlow(provider, opts){
    opts = opts || {};
    const _ua = (navigator.userAgent || '').toLowerCase();
    _fbDiag('signIn:開始 (UA=' + _ua.slice(0, 60) + ')');
    // ★ v3.13.53 — 已有登入進行中 → 忽略此次重複觸發(共用 iPad 學生常連點登入鈕)
    // ★ v5.148.0 — 安全閥：iOS 安裝版的 popup 永不 resolve（見上方病因），舊碼會讓這把鎖永久卡死、
    //   之後每次按登入都被靜默吃掉。鎖超過 90 秒一律視為死鎖放行（SDK 本身會取消前一個 popup 作業）。
    if(_signInBusy && (Date.now() - _signInBusyAt) < 90000){
      _fbDiag('signIn:已有登入進行中 → 忽略重複觸發(避免 auth/cancelled-popup-request)', 'warn');
      return null;
    }
    if(_signInBusy){ _fbDiag('signIn:登入鎖已卡超過 90 秒 → 視為死鎖，放行本次登入', 'warn'); }
    _signInBusy = true;
    _signInBusyAt = Date.now();   // ★ v5.148.0
    try {

    // ★ v3.10.1(2026-05-26) — 標記「玩家主動發起登入」,給 onAuth 區分自動 vs 主動
    //   原問題:共用 iPad 上一個學生沒登出 → 下個學生打開遊戲 → Firebase 從 IndexedDB
    //          自動還原 token → onAuthStateChanged 觸發 → 直接進到別人帳號
    //   修法:每次主動點登入時設 window runtime 變數(reload 後一定清空),
    //         onAuth 觸發時若沒這個變數 → 代表是「自動還原」→ 強制 signOut 並停在登入頁
    //   為什麼用 window 變數而非 sessionStorage?
    //     - sessionStorage 在同分頁 reload 後仍保留 → 學生 A 沒關 Safari,
    //       學生 B 拿過去 F5 → flag 還在 → 仍會誤進 A 帳號
    //     - window 變數隨 JS runtime 一起重置:reload / 關閉 / 新分頁都清空 → 100% 安全
    //   配套:redirect 流程仰賴 sessionStorage('lxps_pending_redirect_session') 旗標,
    //         那是「跨 redirect 跳轉」用的(由 _doSignInFlow 的 redirect 分支自己寫),
    //         onAuth 端會把它跟 window 變數一起當「合法登入跡象」
    window._lxpsActiveSignInThisPageload = true;

    // ★ v5.148.0 — 直連 Google OAuth（見上方模組註解）。從 provider 取 prompt / login_hint 一併帶過去。
    let _dpPrompt = 'select_account', _dpHint = '';
    try{
      const _cp = (provider && typeof provider.getCustomParameters === 'function') ? provider.getCustomParameters() : null;
      if(_cp && _cp.prompt) _dpPrompt = String(_cp.prompt);
      if(_cp && _cp.login_hint) _dpHint = String(_cp.login_hint);
    }catch(_){}
    if(_lxpsOAuthDirectWanted()){
      _fbDiag('signIn:適用直連 OAuth（mode=' + window._LXPS_OAUTH_DIRECT_MODE + ' standalone=' + _lxpsIsIOSStandalone() + '）');
      const _went = await window._lxpsOAuthDirectStart({ prompt: _dpPrompt, loginHint: _dpHint });
      if(_went) return null;   // 本頁即將卸載；回程由 _lxpsOAuthDirectResume 接手
      if(_lxpsIsIOSStandalone()){
        // 安裝版 popup 必卡死（SDK 走 openAsNewWindowIOS 永不回傳）⇒ 不再嘗試，明講原因
        _showLoginErrorTip(
          '安裝版暫時無法登入',
          '這台裝置的「主畫面安裝版」需要老師完成一次 Google 登入設定才能使用。<br><br>' +
          '<b>先這樣做：</b><br>' +
          '・改用 <b>Safari</b> 直接開啟遊戲網址登入（功能完全相同）<br>' +
          '・或告訴老師：「安裝版登入需要設定 Web client ID / 重新導向網址」'
        );
        return null;
      }
      _fbDiag('signIn:直連不可用 → 退回 popup', 'warn');
    }

    // ── 先試 popup(所有環境一致)──────────────────────────────
    try {
      _fbDiag('signIn:嘗試 signInWithPopup');
      const result = await signInWithPopup(_fbAuth, provider);
      _fbDiag('signIn:✅ signInWithPopup 成功 user=' + (result && result.user && result.user.email));
      // ★ v3.16.43 — PC 安裝版切換帳號清單只剩一個根治:登入成功的最早一刻(profile 最完整)就記錄到「最近帳號」,
      //   不再單靠 onAuth 深層那一處(避免 PC popup 流程下任何 race/早退漏記)。result.user 為 Google 帳號必有 email。
      try{ if(result && result.user && typeof window._addRecentAccount === 'function') window._addRecentAccount(result.user); }catch(_){}
      return result;
    } catch(e){
      const code = e && e.code;
      _fbDiag('signIn:popup 失敗 code=' + (code || 'unknown'), 'warn');

      // ★ v3.13.53 — 重複觸發 popup(連點登入鈕)→ 前一個請求被取消。良性,安靜略過(併發鎖已防大部分,此為保險)
      if(code === 'auth/cancelled-popup-request'){
        _fbDiag('signIn:重複 popup 請求被取消(auth/cancelled-popup-request)→ 安靜略過,請完成已開啟的登入視窗');
        return null;
      }

      // 使用者主動取消 → 完全安靜(不算錯誤)
      if(code === 'auth/cancelled-by-user' || code === 'auth/popup-closed-by-user'){
        _fbDiag('signIn:使用者取消(popup-closed-by-user)');
        return null;
      }

      // popup 被擋 / 環境不支援 → 自動 fallback 到 redirect
      // ★ v5.148.0 — 一律先改走直連 OAuth（signInWithRedirect 在跨域 authDomain＋Safari 16.1+ 已壞，僅留作最後備援）；
      //   web-storage-unsupported（SDK 判 iframe 存不了東西）也納入同一條。
      if(code === 'auth/popup-blocked' || code === 'auth/operation-not-supported-in-this-environment' || code === 'auth/popup-closed-by-user' || code === 'auth/web-storage-unsupported'){
        _fbDiag('signIn:popup 被擋(' + code + ') → 先試直連 OAuth', 'warn');
        try{
          const _went2 = await window._lxpsOAuthDirectStart({ prompt: _dpPrompt, loginHint: _dpHint });
          if(_went2) return null;
        }catch(_eD2){ _fbDiag('signIn:直連 OAuth 備援例外 ' + (_eD2 && (_eD2.code || _eD2.message)), 'warn'); }
        _fbDiag('signIn:popup 被擋,fallback signInWithRedirect', 'warn');
        try {
          try{ localStorage.setItem('lxps_pending_redirect_signin', String(Date.now())); }catch(_){}
          try{ sessionStorage.setItem('lxps_pending_redirect_session', String(Date.now())); }catch(_){}
          await signInWithRedirect(_fbAuth, provider);
          return null;
        } catch(e2){
          try{ localStorage.removeItem('lxps_pending_redirect_signin'); }catch(_){}
          try{ sessionStorage.removeItem('lxps_pending_redirect_session'); }catch(_){}
          _fbDiag('signIn:redirect fallback 也失敗 code=' + (e2 && e2.code), 'error');
          _showLoginErrorTip(
            '瀏覽器擋住了 Google 登入',
            '<b>請先允許彈出視窗:</b><br>' +
            '・iPad Chrome:網址列點圖示 → 「永遠允許彈出視窗」<br>' +
            '・iPad Safari:設定 → Safari → 關閉「阻擋彈出式視窗」<br>' +
            '・桌機 Chrome:網址列右側 🚫 → 「永遠允許」<br><br>' +
            '允許後請重新整理頁面再點登入。'
          );
          return null;
        }
      }

      // 網路錯誤 → 友善訊息
      if(code === 'auth/network-request-failed'){
        _showLoginErrorTip(
          '網路連線有問題',
          '無法連到 Google 登入伺服器。<br><br>' +
          '<b>請確認:</b><br>' +
          '① Wi-Fi / 行動網路有連上<br>' +
          '② 過 10 秒後再點一次登入<br>' +
          '③ 如果重複失敗,請告訴老師'
        );
        return null;
      }

      // 未授權網域(理論上不會發生,但保險)
      if(code === 'auth/unauthorized-domain'){
        _showLoginErrorTip(
          '網域未授權',
          '目前的網址未授權使用 Google 登入。<br><br>' +
          '請告訴老師,將網址加入 Firebase 授權清單。'
        );
        return null;
      }

      // 其他未知錯誤 → 顯示原始錯誤碼,方便回報
      console.error('[登入] 未預期錯誤', code, e);
      _showLoginErrorTip(
        '登入時發生未預期錯誤',
        '<b>錯誤代碼:</b> <code style="color:#ffaa66;">' + (code || 'unknown') + '</code><br><br>' +
        '請重新整理頁面再試一次。<br>如果重複失敗,請把這個錯誤代碼告訴老師。'
      );
      return null;
    }
    } finally {
      _signInBusy = false;   // ★ v3.13.53 登入流程結束(成功/取消/錯誤)→ 解鎖,允許下次登入
    }
  }

  // ════════════════════════════════════════════════════════════════════
  // ★★ v5.136.0(2026-09-03・老師裁定①丙)— 共用平板:一律強制顯示 Google 帳號選擇器 ★★
  // ─────────────────────────────────────────────────────────────────────
  //   老師實錘:「首頁左上按了登出後再點登入 Google 帳號,又自動登入上一個同學的帳號」。
  //   根因(實查):本函式用的是裸的 new GoogleAuthProvider(),**沒有帶 prompt**。
  //     Firebase 的 signOut() 只清得掉 Firebase 自己的狀態,
  //     **清不掉 accounts.google.com 的 SSO cookie** —— 那是 Google 網域的東西,
  //     我們的程式(含 IndexedDB 清理)碰不到也刪不掉。
  //     於是 Google 端看到「這台裝置只有一個已登入帳號」就直接靜默回傳,
  //     連帳號選擇器都不顯示 ⇒ 秒進上一位同學的帳號。這不是 iPad 的毛病,
  //     是 OAuth 的預期行為,唯一可靠解就是每次都叫 Google 問一次。
  //
  //   ⚠ 這是「不可拿掉」的共用平板防線:900 名學生共用 iPad,
  //     少了它,前一位沒登出 = 下一位直接繼承別人的帳號與存檔。
  //   ⚠ 副作用(已向老師報備):所有學生每次登入都會多看到一次帳號選擇畫面。
  //     這是必要代價,不是 BUG。
  //   ★ 全檔已有一支寫對的 _fbSignInPickAccount(下方),但它只掛在「加入其他帳號」;
  //     v5.136.0 起首頁登入鈕隱藏、登入入口收斂成「開始遊戲 → login-gate-modal」單一路徑,
  //     而那條路徑走的正是本函式 ⇒ 改這裡即 100% 涵蓋,不可能有漏網入口。
  // ════════════════════════════════════════════════════════════════════
  window._fbSignIn = async function() {
    const provider = new GoogleAuthProvider();
    provider.setCustomParameters({ prompt: 'select_account' });   // ★ v5.136.0（舊:無 prompt）
    return await _doSignInFlow(provider);
  };

  // 暴露給全域:用 email 提示登入(快速切換用)
  // ★ v5.136.0 — 舊註解寫「Google 通常會自動跳過帳號選擇器,直接以該 email 完成登入」,
  //   那個「自動跳過」正是共用平板的破口(不必再認證一次就換人)。
  //   老師需求:「必須登入(或選擇已在此裝置登入過的)帳號並輸入密碼」⇒
  //   改成 prompt:'select_account' + login_hint 併用:
  //     - select_account 保證 Google 一定會問(該輸密碼就輸密碼)
  //     - login_hint 只負責讓選擇器預先對準這個帳號,省得小朋友在一堆帳號裡找
  window._fbSignInWithHint = async function(email){
    const provider = new GoogleAuthProvider();
    if(email && typeof email === 'string'){
      provider.setCustomParameters({ prompt: 'select_account', login_hint: email });   // ★ v5.136.0（舊:只有 login_hint）
    } else {
      provider.setCustomParameters({ prompt: 'select_account' });
    }
    return await _doSignInFlow(provider);
  };

  // 暴露給全域:強制顯示帳號選擇器(用於「加入其他帳號」)
  window._fbSignInPickAccount = async function(){
    const provider = new GoogleAuthProvider();
    provider.setCustomParameters({ prompt: 'select_account' });
    return await _doSignInFlow(provider);
  };

  window._fbSignOut = async function() {
    // ★ v3.5.65(2026-05-23) — 登出前 flush 小博士本地累計到雲端(老師需求:三層保險之一)
    try{
      if(window._weeklyQuiz && typeof window._weeklyQuiz.flushNow === 'function'){
        await window._weeklyQuiz.flushNow();
      }
    }catch(_e){ console.warn('[登出 flush 小博士] 失敗(不阻擋登出)', _e); }
    // ★ v3.10.1(2026-05-26) — 清掉「本次主動登入」runtime 旗標,確保下次完全重新登入
    try { window._lxpsActiveSignInThisPageload = false; } catch(_){}
    // ════════════════════════════════════════════════════════════════════
    // ★ v3.11.4(2026-05-27) — iPad 共用平板登出強化(BUG #8)
    //   老師回報:有同學在 iPad 上「無法登出別人帳號」— 點登出按鈕沒效果,
    //             或登出後重整再看到舊帳號殘留。
    //   根因推測:iPad Safari 對 Firebase Auth IndexedDB(`firebaseLocalStorageDb`)
    //             有特殊持久化行為,signOut() 雖然清掉 _fbAuth.currentUser,但
    //             IndexedDB 的 firebase token 沒被立刻 flush,下次 onAuthStateChanged
    //             仍會從 IndexedDB 自動還原(雖然 v3.10.1 的「禁止自動登入」攔截已存在,
    //             但若兩個 runtime flag 同時殘留,還是會還原)。
    //   修法:登出時主動刪除 Firebase Auth 相關的 IndexedDB(firebaseLocalStorageDb),
    //         並清 lxps_recent_accounts、lxps_last_active_uid 等本機 key,
    //         保證下一輪 onAuthStateChanged 看到的是「乾淨狀態」。
    // ════════════════════════════════════════════════════════════════════
    await signOut(_fbAuth);
    // 主動清 Firebase Auth IndexedDB(iPad Safari 特殊處理)
    // ★ v3.11.4 修正:只刪 Auth 的 DB(firebaseLocalStorageDb),
    //                 不要碰 firebase-installations-database / firebase-heartbeat-database
    //                 — 那兩個跟 Auth 無關,刪了會影響 Cloud Messaging / Analytics 等次要服務
    try{
      if(typeof indexedDB !== 'undefined' && indexedDB.deleteDatabase){
        try{
          const _req = indexedDB.deleteDatabase('firebaseLocalStorageDb');
          _req.onsuccess = () => console.log('[v3.11.4 登出強化] ✅ IndexedDB firebaseLocalStorageDb 已刪除');
          _req.onerror = (e) => console.warn('[v3.11.4 登出強化] IndexedDB firebaseLocalStorageDb 刪除失敗(可忽略)', e);
          _req.onblocked = () => console.warn('[v3.11.4 登出強化] IndexedDB firebaseLocalStorageDb blocked(另一個分頁鎖住,可忽略)');
        }catch(_eDel){ console.warn('[v3.11.4 登出強化] deleteDatabase 例外', _eDel); }
      }
    }catch(_eIDB){ console.warn('[v3.11.4 登出強化] IndexedDB 清理整體失敗', _eIDB); }
    // 清相關 localStorage key(保留 lxps_recent_accounts 因為這是「最近登入過」列表,有用)
    // ★ v3.11.4 修正:_lxpsActiveSignInThisPageload 是 runtime variable 不是 localStorage,
    //                 line 503 已經清掉了,這裡不重複(不會錯,只是冗餘)
    try{
      ['lxps_last_active_uid', 'lxps_session_id'].forEach(k => {
        try{ localStorage.removeItem(k); sessionStorage.removeItem(k); }catch(_){}
      });
    }catch(_eLS){ console.warn('[v3.11.4 登出強化] localStorage 清理失敗', _eLS); }
    // 給玩家明確 UI 回饋
    try{
      const _toast = document.createElement('div');
      _toast.style.cssText = 'position:fixed;top:80px;left:50%;transform:translateX(-50%);' +
        'z-index:99999;background:rgba(20,30,55,0.95);border:2px solid #66ccff;color:#cce4ff;' +
        'font-size:16px;font-weight:700;padding:14px 22px;border-radius:12px;' +
        'box-shadow:0 4px 24px rgba(0,0,0,0.6);max-width:90vw;text-align:center;line-height:1.55;' +
        'font-family:"M PLUS Rounded 1c","Nunito",sans-serif;';
      _toast.innerHTML = '✅ 已登出。<br><span style="font-size:14px;color:#99bbdd;">' +
        '如果還看到舊帳號殘留,請重新整理頁面(下拉刷新)。</span>';
      document.body.appendChild(_toast);
      setTimeout(() => { if(_toast.parentNode) _toast.remove(); }, 5000);
    }catch(_eUI){ console.warn('[v3.11.4 登出強化] toast 顯示失敗', _eUI); }
    console.log('[v3.11.4 登出強化] ✅ signOut 完成(含 IndexedDB 清理)');
  };

  window._fbSwitchAccount = async function(email) {
    // 先記下要切到哪個 email(供登出後重新登入用)
    const _hint = email || null;
    // ★ v3.5.65 — 切帳號前 flush 小博士本地累計(避免換帳號後本地資料殘留錯人)
    try{
      if(window._weeklyQuiz && typeof window._weeklyQuiz.flushNow === 'function'){
        await window._weeklyQuiz.flushNow();
      }
    }catch(_e){ console.warn('[切帳號 flush 小博士] 失敗(不阻擋切換)', _e); }
    try {
      // 1) 立刻登出當前帳號,讓 onAuthStateChanged 跑完 else 分支
      //    (會清掉 session、停心跳、停閒置計時、清本機殘留)
      await signOut(_fbAuth);
      // 2) 等一拍讓 onAuthStateChanged 完成清理
      await new Promise(r => setTimeout(r, 300));
    } catch(e){
      console.warn('[切換帳號] 先登出失敗(繼續嘗試新登入)', e);
    }
    // 3) 再開新登入(若有 hint 就帶,讓 Google 直接認得)
    const provider = new GoogleAuthProvider();
    // ★ v5.136.0 — hint 分支原本只帶 login_hint(Google 會自動跳過選擇器直接換人 = 共用平板破口),
    //   改為與 _fbSignInWithHint 同口徑:一律 select_account,login_hint 只負責預先對準。
    if(_hint){
      provider.setCustomParameters({ prompt: 'select_account', login_hint: _hint });   // ★ v5.136.0（舊:只有 login_hint）
    } else {
      provider.setCustomParameters({ prompt: 'select_account' });
    }
    return await _doSignInFlow(provider);
  };

  // ════════════════════════════════════════════════════════════════
  // ★ 最近登入過的帳號清單(只存本機 localStorage,每台平板各自記憶)
  //   結構:[{ uid, email, displayName, photoURL, lastLoginAt }]
  //   上限 6 個,LRU 排序(最近登入的在最前面)
  // ════════════════════════════════════════════════════════════════
  const RECENT_ACCOUNTS_KEY = 'lxps_recent_accounts';
  const RECENT_ACCOUNTS_MAX = 6;

  // ════════════════════════════════════════════════════════════════
  // ★★ v5.138.0 — 一次性遷移:把舊的 lxps_recent_accounts@@<uid> 合併回全域 ★★
  //   v5.138.0 之前這個 key 被分帳號存放(見檔頭 _EXACT_GLOBAL 的說明),
  //   本輪改為裝置層全域後,舊資料會全部變成讀不到的孤兒。
  //   若不遷移,每一台 iPad 的「登入過的帳號」清單都要從零重新累積 ——
  //   900 名學生的環境下,等於這個功能要好幾週才會真正可用。
  //   ⇒ 開機時掃一次,把所有 @@<uid> 分身合併進全域,依 lastLoginAt 由新到舊取前 6 筆。
  //   ⚠ key 含 '@@' 時 _isGlobal 直接回 true ⇒ 這裡的讀取拿到的是真正的原始 key,
  //     不會被 wrapper 再包一層(localStorage.key()/length 本來就沒被包裝)。
  //   ⚠ **只讀不刪**:舊 key 留著當孤兒,萬一合併邏輯有誤還能回頭撈(誤刪是大忌)。
  //   ⚠ 用 _migrated 旗標(裝置層)確保只跑一次,避免每次開機都做全表掃描。
  // ════════════════════════════════════════════════════════════════
  (function _migrateRecentAccounts(){
    try{
      if(localStorage.getItem('lxps_recent_accounts_migrated') === '1') return;
      const _merged = {};
      // 先放全域現有的(若有)
      const _seed = [];
      try{
        const _g = localStorage.getItem(RECENT_ACCOUNTS_KEY);
        if(_g){ const _p = JSON.parse(_g); if(Array.isArray(_p)) _seed.push.apply(_seed, _p); }
      }catch(_){}
      // 再掃所有分帳號分身
      const _keys = [];
      for(let i = 0; i < localStorage.length; i++){
        const _k = localStorage.key(i);
        if(_k && _k.indexOf(RECENT_ACCOUNTS_KEY + '@@') === 0) _keys.push(_k);
      }
      _keys.forEach(function(_k){
        try{
          const _p = JSON.parse(localStorage.getItem(_k) || '[]');
          if(Array.isArray(_p)) _seed.push.apply(_seed, _p);
        }catch(_){}
      });
      if(_seed.length === 0){
        localStorage.setItem('lxps_recent_accounts_migrated', '1');
        return;
      }
      // 依 uid 去重,保留 lastLoginAt 較新的那筆,並補齊空欄位
      _seed.forEach(function(a){
        if(!a || !a.uid) return;
        const _old = _merged[a.uid];
        if(!_old){ _merged[a.uid] = a; return; }
        const _newer = (a.lastLoginAt || 0) >= (_old.lastLoginAt || 0) ? a : _old;
        const _older = _newer === a ? _old : a;
        _merged[a.uid] = {
          uid: a.uid,
          email: _newer.email || _older.email || '',
          displayName: _newer.displayName || _older.displayName || _newer.email || '玩家',
          photoURL: _newer.photoURL || _older.photoURL || '',
          lastLoginAt: Math.max(_newer.lastLoginAt || 0, _older.lastLoginAt || 0),
        };
      });
      const _list = Object.keys(_merged).map(function(u){ return _merged[u]; })
        .sort(function(x, y){ return (y.lastLoginAt || 0) - (x.lastLoginAt || 0); })
        .slice(0, RECENT_ACCOUNTS_MAX);
      localStorage.setItem(RECENT_ACCOUNTS_KEY, JSON.stringify(_list));
      localStorage.setItem('lxps_recent_accounts_migrated', '1');
      console.log('[最近帳號 v5.138.0] 已把 ' + _keys.length + ' 份舊的分帳號清單合併回裝置層,共 '
        + _list.length + ' 個帳號(舊 key 保留不刪)');
    }catch(e){ console.warn('[最近帳號 v5.138.0] 遷移失敗(不影響功能,清單會從本次登入重新累積)', e); }
  })();

  window._getRecentAccounts = function(){
    try {
      const raw = localStorage.getItem(RECENT_ACCOUNTS_KEY);
      if(!raw) return [];
      const list = JSON.parse(raw);
      if(!Array.isArray(list)) return [];
      // ★ v3.16.43 — 只要有 uid 就保留(原本要求 a.uid && a.email,會把「暫時缺 email」的帳號整筆丟掉,
      //   是 PC 切換清單只剩一個的可能成因之一)。email 缺時下次登入由 _addRecentAccount 合併補回;
      //   切換列在 email 為空時本就會安全 no-op,寧可顯示帳號也不要把它藏起來。
      return list.filter(a => a && a.uid);
    } catch(e) {
      console.warn('[最近帳號] 讀取失敗', e);
      return [];
    }
  };

  window._addRecentAccount = function(user){
    if(!user || !user.uid) return;
    try {
      let list = window._getRecentAccounts();
      // ★ v3.16.43 — 找出既有同 uid 的舊紀錄,做「欄位增補合併」:
      //   若本次 user 某欄位為空(email/displayName/photoURL 偶爾未即時載入),保留舊紀錄的非空值,
      //   絕不用空值覆蓋已知資訊 → 避免帳號因暫時缺 email 被後續 filter 丟掉(PC 切換清單只剩一個的關鍵防護)。
      const _prev = list.filter(a => a && a.uid === user.uid)[0] || null;
      // 移除既有同 uid(等下放回最前面)
      list = list.filter(a => a.uid !== user.uid);
      const _newEmail = (user.email || '').toLowerCase();
      const _entry = {
        uid: user.uid,
        email: _newEmail || (_prev && _prev.email) || '',
        displayName: user.displayName || (_prev && _prev.displayName) || user.email || (_prev && _prev.email) || '玩家',
        photoURL: user.photoURL || (_prev && _prev.photoURL) || '',
        lastLoginAt: Date.now(),
      };
      // 把(合併後的)新紀錄放最前面
      list.unshift(_entry);
      // 限制長度
      if(list.length > RECENT_ACCOUNTS_MAX) list = list.slice(0, RECENT_ACCOUNTS_MAX);
      localStorage.setItem(RECENT_ACCOUNTS_KEY, JSON.stringify(list));
    } catch(e) {
      console.warn('[最近帳號] 寫入失敗', e);
    }
  };

  window._removeRecentAccount = function(uid){
    if(!uid) return;
    try {
      const list = window._getRecentAccounts().filter(a => a.uid !== uid);
      localStorage.setItem(RECENT_ACCOUNTS_KEY, JSON.stringify(list));
    } catch(e) { console.warn('[最近帳號] 移除失敗', e); }
  };

  // ══ Session 管理 API ══

  // 建立/更新 session（登入時呼叫）
  // ★ v3.14.4(2026-06-10)— 誤踢根治配套:claim「確認送達 server」旗標。
  //   課堂 Wi-Fi 壅塞時 updateDoc 可能排隊數秒~數十秒才真正送達;期間若 watcher 的
  //   grace 後複查 getDoc 讀到 server 舊值(前一位學生的 session)→ 誤判被踢 →
  //   學生剛登入幾秒就彈「帳號已在其他裝置登入」,以為登入失敗 → 重登 → 無限循環。
  //   修法:寫入 promise resolve(= 已送達 server)才設 _sessionClaimConfirmed=true;
  //   watcher 在 confirmed 前一律不判定被踢(寧可延後偵測,絕不誤踢)。
  window._fbCreateSession = async function(uid, sessionId) {
    window._sessionClaimConfirmed = false;
    try {
      await updateDoc(doc(_fbDb, 'players', uid), {
        activeSession: sessionId,
        sessionAt: Date.now()
      });
      window._sessionClaimConfirmed = true;
      window._sessionClaimConfirmedAt = Date.now();
    } catch(e) {
      // 文件不存在時用 setDoc
      try {
        await setDoc(doc(_fbDb, 'players', uid), { activeSession: sessionId, sessionAt: Date.now() }, { merge: true });
        window._sessionClaimConfirmed = true;
        window._sessionClaimConfirmedAt = Date.now();
      }
      catch(e2) { console.warn('[Session] 建立失敗', e2); }
    }
  };

  // 心跳更新（保持 session 活躍）
  window._fbHeartbeat = async function(uid, sessionId) {
    try {
      const snap = await getDoc(doc(_fbDb, 'players', uid));
      if(!snap.exists()) return 'invalid';
      const d = snap.data();
      if(d.activeSession !== sessionId) return 'kicked'; // 其他裝置登入了
      await updateDoc(doc(_fbDb, 'players', uid), { sessionAt: Date.now() });
      return 'ok';
    } catch(e) { return 'error'; }
  };

  // ════════════════════════════════════════════════════════════════
  // ★ v3.13.97(2026-06-09)— Session 監聽改用 onSnapshot 取代 5 秒輪詢
  //   背景：原本每 5 秒對 players/{uid} 做 getDoc(讀整份存檔)+updateDoc(寫 sessionAt)，
  //         每位上線學生 ~720 讀+720 寫/小時，且每次都下載整份存檔(吃讀取配額+頻寬)。
  //         sessionAt 經全專案 grep 確認「只寫不讀」(無時效比對、後台不依賴)，隨輪詢一併移除。
  //   改法：改掛 onSnapshot 監聽 players/{uid}，只在 activeSession 變成別的 session 時觸發被踢處理。
  //         偵測被踢更即時(瞬間，非最多 5 秒)；平時近乎零讀取、零頻寬。
  //   競態防護：剛登入/剛取回 session 時 claim 寫入可能尚未傳播 → GRACE 緩衝期內先忽略，
  //            緩衝後再做「單次 getDoc 複查」補捉緩衝期內被搶走的情況(等同舊版 5 秒首輪偵測力)。
  //   進度保護不變：進度不遺失靠「每回合快照 + 離場三層事件 + gameCloudSave + 5 分鐘 safe 槽」，
  //               與本監聽無關；被踢當下仍會 _forceSaveBattleSnapshotAndSync 強制存快照。
  window._installSessionWatcher = function(){
    // 清掉舊監聽 / 舊輪詢 / 舊複查計時器(相容舊流程)
    try{ if(window._sessionListenerUnsub){ window._sessionListenerUnsub(); } }catch(_){}
    window._sessionListenerUnsub = null;
    try{ if(window._sessionHeartbeat){ clearInterval(window._sessionHeartbeat); } }catch(_){}
    window._sessionHeartbeat = null;
    try{ if(window._sessionGraceTimer){ clearTimeout(window._sessionGraceTimer); } }catch(_){}
    window._sessionGraceTimer = null;

    if(!window._gUserId || !window._mySessionId) return;
    if(typeof _fbDb === 'undefined' || !_fbDb) return;
    if(typeof onSnapshot !== 'function' || typeof doc !== 'function') return;

    const _installedAt = Date.now();
    const GRACE_MS = 4000;  // session 建立寫入傳播緩衝，避免剛登入/剛取回就誤判被踢

    // 被踢處理(原 _checkHeartbeat 'kicked' 分支，集中一處)
    const _onKicked = function(){
      if(window._kickedFromSession) return;
      try{ if(typeof window._forceSaveBattleSnapshotAndSync === 'function') window._forceSaveBattleSnapshotAndSync('about_to_be_kicked'); }catch(_){}
      try{ if(typeof window._trySaveSafeIfHealthier === 'function') window._trySaveSafeIfHealthier('about_to_be_kicked'); }catch(_){}
      window._kickedFromSession = true;
      console.warn('⛔ [Session] 已被其他裝置踢出，後續存檔將被拒絕');
      try{ if(window._sessionListenerUnsub){ window._sessionListenerUnsub(); } }catch(_){}
      window._sessionListenerUnsub = null;
      try{ if(window._sessionGraceTimer){ clearTimeout(window._sessionGraceTimer); } }catch(_){}
      window._sessionGraceTimer = null;
      if(window._safeSlotInterval){ clearInterval(window._safeSlotInterval); window._safeSlotInterval = null; }
      try{ if(typeof window._showKickedNotice === 'function') window._showKickedNotice(); }catch(_){}
    };

    // ★ v3.16.5 — 偵測到 GM 權威操作(補償/還原/重建)後的處置:鎖存(_authoritativeReloadTriggered → gameCloudSave 拒存)+ 重新載入,避免「補償前就已載入的過時 session」一存檔就蓋掉老師的操作(110082 補了又消失根治)
    const _onAuthoritativeRestore = function(ts){
      console.warn('🛡️ [權威保護 v3.16.5] 偵測到老師對此帳號的補償/還原(restoreAt=' + ts + '),鎖存並重新載入最新進度,避免過時資料覆蓋');
      // 立即停止本機所有寫雲端動作(unsub listener + 清安全槽定時存檔)→ 即使下面延後重載,延後期間本機過時進度也不會反向覆蓋老師改動。
      //   gameCloudSave 另有 _authoritativeReloadTriggered 保護層(caller 已設 true)雙重把關。
      try{ if(window._sessionListenerUnsub){ window._sessionListenerUnsub(); } }catch(_){}
      window._sessionListenerUnsub = null;
      try{ if(window._safeSlotInterval){ clearInterval(window._safeSlotInterval); window._safeSlotInterval = null; } }catch(_){}
      // ★ v3.16.24 — 雙重打破無限重載迴圈:① 持久化「已處理 restoreAt」到 localStorage(綁 @@uid·跨重載/關閉皆存);② 分頁斷路器計數(綁 uid)。
      //   兩者都「先落地」:即使下面延後到打完戰鬥才重載,基線/計數也已寫入,迴圈保護語意不變。
      try{ localStorage.setItem('_lxpsAuthRestoreSeen', String(ts)); }catch(_ePersist){}
      try{ var _ck='_lxpsAuthReloadCnt_'+(window._gUserId||'nouid'); var _cc=(parseInt(sessionStorage.getItem(_ck)||'0',10)||0)+1; sessionStorage.setItem(_ck,String(_cc)); }catch(_eCnt){}
      // ★ v3.16.82 — 戰鬥中不硬重載(老師需求:學生練到一半的進度別被「老師更新資料」中斷而回溯)。
      //   比照既有「即時版本更新」延後機制(_isInBattleNow + _flushPendingUpdateIfAny):戰鬥中先提示「打完回關卡選擇自動套用」,
      //   輪詢等玩家回到非戰鬥畫面再重載;附 15 分鐘最長等待保險,偵測異常時超時也會重載(吃權威雲端,絕不永久卡在過時資料)。
      //   ★ 延後期間存檔已被擋(上面 unsub + 清定時 + _authoritativeReloadTriggered),故延後不會讓過時進度蓋掉老師改動。
      var _doAuthReload = function(){ try{ if(typeof window._lxpsReloadKeepLogin === 'function'){ window._lxpsReloadKeepLogin(); return; } }catch(_){} try{ location.reload(); }catch(_eRl){} };   // ★ v5.141.0 — 自家重載設放行旗標，不被攔截器當成自動還原踢出
      var _inBattleNow = false;
      try{ _inBattleNow = (typeof _isInBattleNow === 'function') && _isInBattleNow(); }catch(_eIB){}
      if(_inBattleNow){
        try{ if(typeof _showInGameToast === 'function') _showInGameToast('🎁 老師更新了你的帳號資料!打完本場戰鬥、回到關卡選擇後會自動套用最新進度,先別擔心~', '#88dd88', 4200); }catch(_){}
        var _arWaitStart = Date.now();
        var _AR_MAX_WAIT = 15 * 60 * 1000;
        var _arPoll = setInterval(function(){
          var _still = false;
          try{ _still = (typeof _isInBattleNow === 'function') && _isInBattleNow(); }catch(_ePoll){}
          if(!_still || (Date.now() - _arWaitStart) > _AR_MAX_WAIT){
            clearInterval(_arPoll);
            try{ if(typeof _showInGameToast === 'function') _showInGameToast('🔄 正在套用老師更新的最新進度…', '#88dd88', 1600); }catch(_){}
            setTimeout(_doAuthReload, 1200);
          }
        }, 1500);
        return;
      }
      // 非戰鬥:照舊立即重載(維持原行為)。
      try{ if(typeof _showInGameToast === 'function') _showInGameToast('🎁 老師更新了你的帳號資料,正在重新載入以套用最新進度…', '#88dd88', 2200); }catch(_){}
      setTimeout(_doAuthReload, 1800);
    };

    // 判斷某份快照是否代表被踢
    // ★ v3.14.4 — 誤踢根治:
    //   (1) 本機 claim 尚未確認送達 server(_sessionClaimConfirmed!==true)→ 一律不判被踢。
    //       課堂 Wi-Fi 壅塞時寫入可能排隊很久,期間任何 server 舊值(前一位學生 session)
    //       都不可信;claim 排隊中最終必會覆蓋 server 值,真被搶時對方寫得更晚才算。
    //   (2) hasPendingWrites=true 的快照(本機尚有未送達寫入的 latency-compensated 視圖)
    //       → 不判,等 server 確認快照。
    const _checkSnapData = function(d, withinGrace, hasPendingWrites){
      if(window._kickedFromSession) return;
      if(!d) return;
      if(hasPendingWrites === true) return;                 // ★ v3.14.4 (2)
      // ★ v3.16.5 — 權威 restore 偵測(放在 claim 守門「之前」:與 session 擁有權無關;GM 對在線/不在線帳號的補償/還原都要捕捉)
      //   _authoritativeRestoreAt 只由 GM 補償/還原/重建寫;雲端值比本 session 載入時新 → 本機記憶體已過時,
      //   一存檔就會把老師的操作蓋掉(110082 補了又消失)→ 鎖存 + 重載,改吃權威狀態。只看 server 確認快照(上面已擋 hasPendingWrites)。
      try{
        var _cloudRA = (typeof d._authoritativeRestoreAt === 'number') ? d._authoritativeRestoreAt : 0;
        var _seenRA = (typeof window._mySessionRestoreSeen === 'number') ? window._mySessionRestoreSeen : 0;
        // ★ v3.16.24 — 折入 localStorage 持久基線(綁 @@uid·跨重載):_applySafeData 未帶到 _authoritativeRestoreAt 時
        //   記憶體基線=0 會每次載入都重載 → 用上次「已處理 restoreAt」墊高基線,打破無限重載迴圈(管理員/特定帳號回報卡死根因)。
        try{ var _persRA = parseInt(localStorage.getItem('_lxpsAuthRestoreSeen') || '0', 10); if(!isNaN(_persRA) && _persRA > _seenRA) _seenRA = _persRA; }catch(_ePR){}
        // ★ v3.16.24 — 硬性斷路器:同一分頁同帳號權威重載達 3 次後不再重載(防任何殘留迴圈·寧可手動重整也不卡死)。
        var _arCnt = 0;
        try{ _arCnt = parseInt(sessionStorage.getItem('_lxpsAuthReloadCnt_' + (window._gUserId || 'nouid')) || '0', 10) || 0; }catch(_eAC){}
        if(_cloudRA > 0 && _cloudRA > _seenRA && !window._authoritativeReloadTriggered && _arCnt < 3){
          window._authoritativeReloadTriggered = true;
          _onAuthoritativeRestore(_cloudRA);
          return;
        }
      }catch(_eAR){}
      // ★ v3.32.0 — GM 即時訊息偵測(讀 _gmChat.at 比已讀基線新 → 彈窗+可回覆)
      //   放 claim 守門「之前」:純顯示 UI·與 session 擁有權無關;已讀基線 = 記憶體 + localStorage(綁 @@uid)雙保險防重複彈。
      try{
        var _gc = d._gmChat;
        if(_gc && typeof _gc.at === 'number' && _gc.at > 0 && _gc.msg){
          var _gseen = window._gmChatSeenAt || 0;
          try{ var _gp = parseInt(localStorage.getItem('_lxpsGmChatSeen') || '0', 10); if(!isNaN(_gp) && _gp > _gseen) _gseen = _gp; }catch(_eGS){}
          if(_gc.at > _gseen && typeof window._gmChatIncoming === 'function'){
            window._gmChatIncoming(_gc);
          }
        }
      }catch(_eGC){}
      if(window._sessionClaimConfirmed !== true) return;    // ★ v3.14.4 (1)
      // 只有「別的裝置帶真實 session 上線」才算被踢；activeSession 為空不踢(避免誤殺)
      if(d.activeSession && d.activeSession !== window._mySessionId){
        if(withinGrace) return;  // 緩衝期內先忽略(claim 寫入可能還沒到)
        _onKicked();
      }
    };

    // 即時監聽：之後任何 activeSession 變動都會觸發
    // ★ v3.14.4 — iPad PWA 背景恢復後 Firestore stream 常斷線;listener 死掉會漏偵測被踢,
    //   舊裝置繼續寫存檔可能覆蓋新裝置進度。error 時 8 秒後自動重裝(_installSessionWatcher 冪等)。
    try{
      window._sessionListenerUnsub = onSnapshot(
        doc(_fbDb, 'players', window._gUserId),
        function(snap){
          if(!snap || !snap.exists()) return;  // 文件暫時讀不到 → 不誤判
          const _hpw = !!(snap.metadata && snap.metadata.hasPendingWrites);
          _checkSnapData(snap.data() || {}, (Date.now() - _installedAt) < GRACE_MS, _hpw);
        },
        function(err){
          console.warn('[SessionWatcher] onSnapshot error,8 秒後自動重裝', err);
          try{ if(window._sessionListenerUnsub){ window._sessionListenerUnsub(); } }catch(_){}
          window._sessionListenerUnsub = null;
          setTimeout(function(){
            try{
              if(!window._kickedFromSession && window._gUserId && window._mySessionId
                 && !window._sessionListenerUnsub && typeof window._installSessionWatcher === 'function'){
                window._installSessionWatcher();
              }
            }catch(_){}
          }, 8000);
        }
      );
    }catch(e){
      console.warn('[SessionWatcher] 啟動失敗', e);
    }

    // 緩衝期後單次 getDoc 複查：補捉緩衝期內被別台搶走 session 的情況
    // ★ v3.14.4 — 複查必須等本機 claim 確認送達 server 後才有意義;
    //   未確認時讀到的 server 值是「claim 還沒寫上去的舊值」,複查會 100% 誤踢。
    //   改為:claim 未確認 → 每 3 秒重排,最多等 60 秒(期間 onSnapshot 持續守護真被搶情境)。
    const _graceRecheck = function(_retryUntil){
      window._sessionGraceTimer = null;
      if(window._kickedFromSession) return;
      if(!window._gUserId || !window._mySessionId) return;
      if(window._sessionClaimConfirmed !== true){
        if(Date.now() < _retryUntil){
          window._sessionGraceTimer = setTimeout(function(){ _graceRecheck(_retryUntil); }, 3000);
        } else {
          console.warn('[SessionWatcher v3.14.4] claim 60 秒仍未確認送達(網路極差),放棄複查(onSnapshot 續守)');
        }
        return;
      }
      try{
        getDoc(doc(_fbDb, 'players', window._gUserId)).then(function(snap){
          if(snap && snap.exists()) _checkSnapData(snap.data() || {}, false, false);
        }).catch(function(){});
      }catch(_){}
    };
    window._sessionGraceTimer = setTimeout(function(){
      _graceRecheck(Date.now() + 60000);
    }, GRACE_MS + 500);
  };

  // 取消 session 監聽(登出 / 切帳號用)
  window._teardownSessionWatcher = function(){
    try{ if(window._sessionListenerUnsub){ window._sessionListenerUnsub(); } }catch(_){}
    window._sessionListenerUnsub = null;
    try{ if(window._sessionHeartbeat){ clearInterval(window._sessionHeartbeat); } }catch(_){}
    window._sessionHeartbeat = null;
    try{ if(window._sessionGraceTimer){ clearTimeout(window._sessionGraceTimer); } }catch(_){}
    window._sessionGraceTimer = null;
  };

  // 清除 session（登出時）
  window._fbClearSession = async function(uid, sessionId) {
    try {
      const snap = await getDoc(doc(_fbDb, 'players', uid));
      if(snap.exists() && snap.data().activeSession === sessionId) {
        await updateDoc(doc(_fbDb, 'players', uid), { activeSession: null, sessionAt: null });
      }
    } catch(_){}
  };

  // ════════════════════════════════════════════════════════════════
  // ★ v3.13.47(2026-06-04) — 皮膚(英雄肖像)雲端「只增不減」保護
  //   背景:部分學生帳號被跨帳號汙染後,花錢買的皮膚消失。
  //   根因鏈:① _fbSave 寫主文件、_fbSaveLive 寫 live 槽都用 merge:true,
  //            但 heroPortraitUnlocked 是 {英雄:[皮膚id]} 的 map,merge:true 對「map 內的陣列」
  //            是「整段取代」而非聯集 → 本地殘缺(汙染)的英雄皮膚陣列會直接覆蓋掉雲端較完整的。
  //          ② _fbLoad 讀的是主文件,主文件被覆蓋後就回不來。
  //          ③ 健康度守門只看英雄數/最高等級,皮膚不在守門指標內 → 攔不住。
  //   修法(本 helper):寫入前把「要寫的皮膚」與「雲端既有皮膚」做 per-英雄 Set 聯集,
  //          確保每次寫入只會「新增」皮膚、永不「移除」。三個寫入點(主文件 / live / safe)都套。
  //   注意:管理員強制操作(救援/收回)走直接 setDoc,不經這條路 → 仍可由 GM 端覆蓋(見 admin 工具)。
  function _mergePortraitUnlockUnion(existing, incoming){
    const _ex = (existing && typeof existing === 'object') ? existing : {};
    const _in = (incoming && typeof incoming === 'object') ? incoming : null;
    // incoming 沒帶 portrait(部分欄位寫入,如只更新暱稱)→ 不動雲端既有
    if(!_in) return (existing && typeof existing === 'object') ? existing : undefined;
    const _out = {};
    Object.keys(_ex).forEach(h => {
      const a = Array.isArray(_ex[h]) ? _ex[h] : [];
      _out[h] = Array.from(new Set(a));
    });
    Object.keys(_in).forEach(h => {
      const a = Array.isArray(_in[h]) ? _in[h] : [];
      const set = new Set(_out[h] || []);
      a.forEach(id => { if(id) set.add(id); });
      _out[h] = Array.from(set);
    });
    return _out;
  }
  window._mergePortraitUnlockUnion = _mergePortraitUnlockUnion;

  // ★ v3.13.47 — 皮膚「選擇(目前使用哪個肖像)」保護:presence > absence
  //   規則:incoming 有「非空字串」值(含玩家明確選 'default' 想看原始 Q 版,需尊重)→ 覆蓋;
  //         incoming 缺/空 → 不可把雲端既有選擇清空(防汙染/殘缺本地把選擇洗掉)。
  //   注:選擇值最終應指向「已解鎖」的肖像;解鎖清單已由聯集保護,選擇只要不被清空即可。
  function _mergePortraitChoice(existing, incoming){
    const _ex = (existing && typeof existing === 'object') ? existing : {};
    const _in = (incoming && typeof incoming === 'object') ? incoming : null;
    if(!_in) return (existing && typeof existing === 'object') ? existing : undefined;
    const _out = Object.assign({}, _ex);
    Object.keys(_in).forEach(h => {
      const v = _in[h];
      if(v && typeof v === 'string') _out[h] = v;  // 非空字串才覆蓋
    });
    return _out;
  }
  window._mergePortraitChoice = _mergePortraitChoice;

  // ════════════════════════════════════════════════════════════════════════════
  // ★★ v5.111.0(2026-08-23・老師裁定甲)— Firestore「巢狀陣列」安全網 ★★
  //
  // 【為什麼要有這一層】
  //   Firestore 硬性禁止「陣列的元素本身又是陣列」(array-in-array),而且 **連空陣列也算**。
  //   一旦存檔 blob 內任何一處踩到,`setDoc` / `Transaction.set` 會**整份拋錯**,
  //   也就是說「一個欄位寫壞 = 整個學生的所有進度都存不進雲端」,而且畫面毫無異狀,
  //   只在 console 留一行紅字 ⇒ 學生照玩、progress 照跑,重載後全部消失。
  //   v5.107.0 的 `advPresetTeams.treasures` 就是這樣讓整個雲端存檔靜默全滅兩天。
  //
  // 【本層的定位】
  //   ★ 這是**兜底**,不是主修法。主修法是「不要產生巢狀陣列」(見 `_lxpsPresetTeamsForCloud`)。
  //   本層負責保證:日後任何人再寫出同型錯誤,**最多只犧牲那一個欄位,不會賠掉整份存檔**。
  //
  // 【處理方式:整個 top-level 欄位打包成 map】
  //   掃到有巢狀陣列的最外層欄位 → 整個換成 `{ __lxpsNestedJson: '<原值 JSON>' }`
  //   (map 是合法值、內含字串也合法 ⇒ 一定寫得進去,且**資料不遺失**,日後要救得回來)。
  //   ⚠ 刻意不做「就地把內層陣列轉字串」——那會讓欄位形狀看起來正常卻語意錯亂,
  //     載入端 `Array.isArray` 之類的守門會誤判成合法資料而拿去用,比整包標記更危險。
  //   ⚠ 舊版 client 讀到這個 map 會因為型別不符而略過該欄位 ⇒ 等同「這一欄沒資料」,
  //     這是刻意的:寧可少一欄,也絕不能讓整份存檔寫不進去。
  //   ★ 另寫 `_lxpsSanitized` 欄位列出被處理的 key ＋ console.error 大聲喊,
  //     讓 GM 稽核時看得到「哪一版哪一欄出過事」,不會再靜默兩天。
  // ════════════════════════════════════════════════════════════════════════════
  function _lxpsHasNestedArray(v, depth, inArray){
    if(depth > 12) return false;                      // 防環狀結構/超深物件把主執行緒卡死
    if(!v || typeof v !== 'object') return false;
    if(Array.isArray(v)){
      if(inArray) return true;                        // ← 陣列直接裝陣列 = Firestore 拒收
      for(var i = 0; i < v.length; i++){
        if(_lxpsHasNestedArray(v[i], depth + 1, true)) return true;
      }
      return false;
    }
    for(var k in v){
      if(!Object.prototype.hasOwnProperty.call(v, k)) continue;
      if(_lxpsHasNestedArray(v[k], depth + 1, false)) return true;   // map 內的陣列是合法的,重新起算
    }
    return false;
  }
  window._lxpsSanitizeForFirestore = function(data, label){
    try{
      if(!data || typeof data !== 'object' || Array.isArray(data)) return data;
      var _bad = null;
      for(var k in data){
        if(!Object.prototype.hasOwnProperty.call(data, k)) continue;
        if(_lxpsHasNestedArray(data[k], 0, false)){ (_bad = _bad || []).push(k); }
      }
      if(!_bad) return data;                           // 正常情況:零複製、零成本直接放行
      var _out = Object.assign({}, data);
      _bad.forEach(function(k){
        try{ _out[k] = { __lxpsNestedJson: JSON.stringify(data[k]) }; }
        catch(_eStr){ _out[k] = { __lxpsNestedJson: '', __lxpsStringifyFailed: true }; }
      });
      _out._lxpsSanitized = { at: Date.now(), keys: _bad.slice(0, 20), ver: (window._GAME_LOADED_VERSION || '') };
      console.error('🛡️ [Firestore 安全網 v5.111.0] 偵測到巢狀陣列,已打包以保住整份存檔 —— 欄位:'
        + _bad.join(', ') + '(來源 ' + (label || '?') + ')。這是程式缺陷,請回報給老師。');
      return _out;
    }catch(_e){ console.warn('[Firestore 安全網] 掃描失敗,原樣放行', _e); return data; }
  };

  window._fbSave = async function(uid, data) {
    if(!uid || typeof uid !== 'string'){
      console.warn('[_fbSave] 收到無效 uid,跳過儲存:', uid);
      return false;
    }
    // ★ v3.4.7 — SDK 已死則直接放棄
    if(window._firestoreSdkIsDead){
      console.warn('[_fbSave] Firestore SDK 已死亡,跳過儲存');
      return false;
    }
    // ★ v5.111.0 — 巢狀陣列安全網:置於所有守門與健康度比對「之前」,
    //   確保任何下游路徑(皮膚聯集 / 部分欄位 patch / 摘要計算)拿到的都是已消毒的同一份。
    data = window._lxpsSanitizeForFirestore(data, '_fbSave');
    try {
      // ★ v3.13.47 — 皮膚保護:此份 data 含 heroPortraitUnlocked(完整存檔)時,
      //   先讀主文件既有皮膚做聯集再寫,確保主文件皮膚「只增不減」(主文件是 _fbLoad 的讀取來源)。
      //   部分欄位寫入(無 heroPortraitUnlocked)不觸發額外讀取,維持原效能。
      let _writeData = data;
      if(data && (data.heroPortraitUnlocked || data.heroPortraitChoice)){
        try{
          const _exSnap = await getDoc(doc(_fbDb, 'players', uid));
          const _exD = _exSnap.exists() ? (_exSnap.data() || {}) : {};
          const _patch = {};
          if(data.heroPortraitUnlocked && typeof data.heroPortraitUnlocked === 'object'){
            const _mu = _mergePortraitUnlockUnion(_exD.heroPortraitUnlocked, data.heroPortraitUnlocked);
            if(_mu) _patch.heroPortraitUnlocked = _mu;
          }
          if(data.heroPortraitChoice && typeof data.heroPortraitChoice === 'object'){
            const _mc = _mergePortraitChoice(_exD.heroPortraitChoice, data.heroPortraitChoice);
            if(_mc) _patch.heroPortraitChoice = _mc;
          }
          if(Object.keys(_patch).length) _writeData = Object.assign({}, data, _patch);
        }catch(_ePortMerge){ console.warn('[_fbSave 皮膚聯集] 讀取既有皮膚失敗,改用原資料寫入', _ePortMerge); }
      }
      // ★ v3.13.88 / v3.13.90 — 主文件健康度守門(對齊 _fbSaveLive,堵住「預設池/LV1 覆蓋主文件」)
      //   v3.13.90 強化(老師裁示丙):
      //     乙-1 比對用 getDoc「讀取失敗(離線)」→ 不再保守放行,改保守「拒絕整份存檔」
      //          (防離線時把預設池薄資料寫回雲端;資料仍在雲端 + live 槽 + 本機備份,下次連線重試補上)。
      //     乙-3 unlockedHeroes 與雲端既有「聯集(只增不減)」→ 即使守門邊界放行,也絕不把英雄清單縮成預設
      //          (Firestore merge 對陣列是整包取代,故在此先 union 上雲端既有)。
      //   原則維持:只在「整份存檔」(帶 _dataSummary 或含 unlockedHeroes/heroLevels)時啟用;
      //            暱稱/好友/代表英雄/皮膚 等部分欄位寫入一律放行(不進此 if、不讀雲端、不受影響)。
      //            管理員強制(_adminForceRestore/_adminRescueSignal)一律放行縮減。
      try {
        const _hasSummary  = _writeData && _writeData._dataSummary && typeof _writeData._dataSummary === 'object';
        const _hasHeroData = _writeData && (Array.isArray(_writeData.unlockedHeroes)
                              || (_writeData.heroLevels && typeof _writeData.heroLevels === 'object'));
        const _adminForce = !!(_writeData && (_writeData._adminForceRestore || _writeData._adminRescueSignal));
        if(_hasSummary || _hasHeroData){
          // 讀一次既有主文件,供「倒退守門」+「unlockedHeroes 聯集」共用(避免重複 getDoc)
          let _exGate = null;
          try {
            const _exSnapGate = await getDoc(doc(_fbDb, 'players', uid));
            _exGate = _exSnapGate.exists() ? (_exSnapGate.data() || {}) : null;
          } catch(_eRead){
            // ★ v3.13.90(乙-1)— 比對用讀取失敗 → 無法確認是否倒退 → 保守拒絕整份存檔(非管理員救援)
            if(!_adminForce){
              console.error('🛡️ [_fbSave v3.13.90 主文件守門] 比對讀取失敗(離線?)→ 保守拒絕整份存檔,'
                + '防薄資料覆蓋(資料仍安全在雲端/live 槽/本機備份,下次連線重試會寫回)', _eRead);
              return false;
            }
            console.warn('[_fbSave v3.13.90] 比對讀取失敗但為管理員強制 → 放行', _eRead);
          }
          if(_exGate){
            // ★ v3.16.93 — 稽核感知倒退豁免:雲端/本地「最近一筆解鎖紀錄=admin_delete(GM 收回過度補償)
            //   或 audit_error_recovered(學生自助『不是我的』disown)」的英雄,視為「已合法移除」→
            //   不計入倒退比較的擁有數、也不被下方 union 復活 → 讓 GM 收回與學生自審刪除能正常從存檔減少,
            //   不再卡『資料倒退/缺英雄X隻』。latest-entry-wins:玩家若重新抽到/老師補發(更新紀錄)則自然恢復。
            var _gateRemovedSet = {};
            try{
              var _gateHist = [].concat(
                (_writeData && Array.isArray(_writeData._heroUnlockHistory)) ? _writeData._heroUnlockHistory : [],
                (_exGate && Array.isArray(_exGate._heroUnlockHistory)) ? _exGate._heroUnlockHistory : []
              );
              var _gateDel = _lxpsLatestDeletedMap(_gateHist, 'name');
              Object.keys(_gateDel).forEach(function(n){ if(_gateDel[n] && _gateDel[n].deleted) _gateRemovedSet[n] = 1; });
            }catch(_eGR){ _gateRemovedSet = {}; }
            const _calcSum = (d) => {
              if(d && d._dataSummary && typeof d._dataSummary === 'object'){
                return {
                  unlockedCount: d._dataSummary.unlockedCount || 0,
                  maxHeroLv:     d._dataSummary.maxHeroLv     || 0,
                  totalHeroLv:   d._dataSummary.totalHeroLv   || 0
                };
              }
              const _hl   = (d && d.heroLevels) || {};
              const _vals = Object.values(_hl).filter(v => typeof v === 'number');
              return {
                unlockedCount: Array.isArray(d && d.unlockedHeroes) ? d.unlockedHeroes.length : 0,
                maxHeroLv:     _vals.length ? Math.max(0, ..._vals) : 0,
                totalHeroLv:   _vals.reduce((a, b) => a + b, 0)
              };
            };
            const _newS = _calcSum(_writeData);
            const _exS  = _calcSum(_exGate);
            // ★ v3.15.41 丁:口徑一致 + 自動修復 — 兩邊的 unlockedCount/maxLv/totalLv 都改用
            //   「實際 unlockedHeroes/heroLevels 陣列 + 同一支 _PLAYER_HERO_NAMES 過濾」即時重算,
            //   不信舊版補償寫進雲端的「未過濾」_dataSummary。如此被舊膨脹 summary 卡住的帳號下次存檔
            //   會以「過濾後雲端(46) vs 過濾後本地(46)」通過;真正的英雄倒退仍擋得住(過濾後雲端 > 本地才算)。
            try{
              const _gIsPHN = function(nm){ try{ if(typeof _PLAYER_HERO_NAMES==='undefined'||!_PLAYER_HERO_NAMES) return true; return _PLAYER_HERO_NAMES.has(nm); }catch(_){ return true; } };
              const _recompS = function(d, s){
                // ★ v3.15.76 — 以 unlockedHeroes(陣列;set(merge:true) 對陣列是整包取代,不會累積污染)為「真正擁有」權威清單。
                const _unlSet = (d && Array.isArray(d.unlockedHeroes)) ? new Set(d.unlockedHeroes.filter(function(n){ return _gIsPHN(n) && !_gateRemovedSet[n]; })) : null;
                if(_unlSet) s.unlockedCount = _unlSet.size;
                else if(d && Array.isArray(d.unlockedHeroes)) s.unlockedCount = d.unlockedHeroes.filter(function(n){ return _gIsPHN(n) && !_gateRemovedSet[n]; }).length;
                if(d && d.heroLevels && typeof d.heroLevels === 'object'){
                  let _m=0,_t=0; Object.keys(d.heroLevels).forEach(function(n){
                    if(!_gIsPHN(n)) return;
                    // ★ v3.15.76 — 只計入「確實在 unlockedHeroes」的英雄等級;排除 heroLevels(map)因 set(merge:true)
                    //   深合併殘留、不在擁有清單裡的幻影條目(老師回報:GM 救援還原正確資料後,雲端 heroLevels
                    //   殘留把 maxLv 墊高,使正確的本地存檔被誤判「最高等級倒退」而拒絕同步)。
                    if(_unlSet && !_unlSet.has(n)) return;
                    const lv=d.heroLevels[n]||0; if(lv>_m)_m=lv; _t+=lv;
                  });
                  s.maxHeroLv=_m; s.totalHeroLv=_t;
                }
              };
              _recompS(_writeData, _newS); _recompS(_exGate, _exS);
              // ★ v3.15.76 診斷(老師核對 uid 實際資料用):印出雲端 heroLevels 是否有「不在 unlockedHeroes」的幻影殘留
              try{
                const _phantomOf = function(d){
                  if(!d || !d.heroLevels || typeof d.heroLevels!=='object' || !Array.isArray(d.unlockedHeroes)) return null;
                  const _us = new Set(d.unlockedHeroes.filter(_gIsPHN));
                  const _ph = Object.keys(d.heroLevels).filter(function(n){ return _gIsPHN(n) && !_us.has(n); });
                  let _pm=0; _ph.forEach(function(n){ const lv=d.heroLevels[n]||0; if(lv>_pm)_pm=lv; });
                  return { count:_ph.length, maxLv:_pm, sample:_ph.slice(0,15) };
                };
                const _exPh = _phantomOf(_exGate);
                if(_exPh && _exPh.count > 0){
                  console.warn('🔎 [_fbSave v3.15.76 倒退核對 uid=' + uid + '] 雲端 heroLevels 有 ' + _exPh.count
                    + ' 個「不在 unlockedHeroes」的幻影殘留(最高 Lv ' + _exPh.maxLv
                    + '),已從倒退比較排除 → 雲端真實 maxLv=' + _exS.maxHeroLv + '、本地 maxLv=' + _newS.maxHeroLv
                    + '、雲端擁有數=' + _exS.unlockedCount + '、本地擁有數=' + _newS.unlockedCount
                    + '。幻影樣本:' + _exPh.sample.join(','));
                }
              }catch(_eDiagPh){}
            }catch(_eDdMain){}
            const _dUnlocked = _newS.unlockedCount - _exS.unlockedCount;
            const _dMaxLv    = _newS.maxHeroLv     - _exS.maxHeroLv;
            const _dTotalLv  = _newS.totalHeroLv   - _exS.totalHeroLv;
            const _reverseBreakout = (_dTotalLv >= 15) && (_dUnlocked <= -3);   // 本地明顯更豐富 → 放行
            const _hardRegression  = (_dUnlocked <= -5) || (_dMaxLv <= -5);
            const _heroDoubleRegression = (_dUnlocked <= -2) && (_dMaxLv <= -3);  // ★ 對齊 _fbSaveLive:雙重倒退也擋
            if((_hardRegression || _heroDoubleRegression) && !_reverseBreakout && !_adminForce){
              console.error('🛡️ [_fbSave v3.13.88 主文件守門] 拒絕寫入主文件 — 偵測到大幅倒退(疑似預設池/別帳號殘留/LV1 覆蓋):',
                { unlocked: _newS.unlockedCount + ' vs ' + _exS.unlockedCount,
                  maxLv:    _newS.maxHeroLv     + ' vs ' + _exS.maxHeroLv,
                  totalLv:  _newS.totalHeroLv   + ' vs ' + _exS.totalHeroLv });
              return false;   // 不讓預設池/LV1 蓋掉主文件好資料(live 槽守門已擋,這裡補上主文件這一道)
            }
            // ★ v3.13.90(乙-3)— unlockedHeroes 不縮減:與雲端既有聯集(只增不減),非管理員強制時。
            //   防「守門邊界放行但英雄清單被縮成預設」。merge 對陣列整包取代,故先 union。
            if(!_adminForce && Array.isArray(_writeData.unlockedHeroes) && Array.isArray(_exGate.unlockedHeroes)){
              const _uSet = new Set(_writeData.unlockedHeroes);
              let _addedBack = 0;
              _exGate.unlockedHeroes.forEach(n => { if(n && !_uSet.has(n) && !_gateRemovedSet[n]){ _uSet.add(n); _addedBack++; } });
              if(_addedBack > 0){
                _writeData = Object.assign({}, _writeData, { unlockedHeroes: [..._uSet] });
                console.warn('🛡️ [_fbSave v3.13.90 聯集] 要寫的 unlockedHeroes 比雲端少 ' + _addedBack
                  + ' 隻 → 已聯集補回(只增不減,防英雄被縮成預設)');
              }
            }
            // ★ v3.13.93(2026-06-09)— Bug 1(c) 知識王累積天數倒退守門:
            //   防 stale / 跨帳號殘留把「持之以恆」達標天數寫低於雲端,造成「達標卻沒券」。
            //   只在「非剛領取」時生效:_kingClaimPersistReward 合法領取會把天數歸 0 並設 _kingStreakJustClaimed 時間戳,
            //   2 分鐘內(含雲端重試)允許下降;其餘一律取雲端較高值。admin 強制放行。
            try{
              if(!_adminForce && _writeData._kingChallenge && typeof _writeData._kingChallenge === 'object'
                 && _exGate._kingChallenge && typeof _exGate._kingChallenge === 'object'){
                const _justClaimed = (typeof window._kingStreakJustClaimed === 'number')
                  && (Date.now() - window._kingStreakJustClaimed < 120000);
                if(!_justClaimed){
                  const _wkc = _writeData._kingChallenge, _ekc = _exGate._kingChallenge;
                  const _wssr = (typeof _wkc._ssrStreakDays === 'number') ? _wkc._ssrStreakDays : 0;
                  const _essr = (typeof _ekc._ssrStreakDays === 'number') ? _ekc._ssrStreakDays : 0;
                  const _wsr  = (typeof _wkc._srStreakDays  === 'number') ? _wkc._srStreakDays  : 0;
                  const _esr  = (typeof _ekc._srStreakDays  === 'number') ? _ekc._srStreakDays  : 0;
                  if(_wssr < _essr || _wsr < _esr){
                    const _fixedKc = Object.assign({}, _wkc);
                    if(_wssr < _essr) _fixedKc._ssrStreakDays = _essr;
                    if(_wsr  < _esr)  _fixedKc._srStreakDays  = _esr;
                    _writeData = Object.assign({}, _writeData, { _kingChallenge: _fixedKc });
                    console.warn('🛡️ [_fbSave v3.13.93 知識王天數守門] 要寫的累積天數比雲端低且非剛領取 → 取雲端較高值'
                      + '(SSR ' + _wssr + '→' + _essr + ', SR ' + _wsr + '→' + _esr + ',防達標進度被覆蓋)');
                  }
                }
              }
            }catch(_eKingGate){ console.warn('[_fbSave 知識王天數守門] 例外,保守跳過', _eKingGate); }
          }
        }
      } catch(_eMainGate){ console.warn('[_fbSave 主文件守門] 非預期例外,保守放行', _eMainGate); }
      // ★ merge:true 保留 representativeHero / friends 等欄位不被覆蓋
      await setDoc(doc(_fbDb, 'players', uid), _writeData, { merge: true });
      return true;
    } catch(e) {
      console.error('雲端儲存失敗', e);
      // ★ v3.4.7 — 偵測是否為 SDK assertion 死亡
      if(typeof window._checkAndHandleFirestoreDeath === 'function'){
        window._checkAndHandleFirestoreDeath(e, '_fbSave');
      }
      return false;
    }
  };

  // ── 即時同步：監聽玩家 profile 變更（暱稱、代表英雄、好友名單）──
  window._fbWatchProfile = function(uid, onChange) {
    if(!uid) return () => {};
    const unsubscribe = onSnapshot(
      doc(_fbDb, 'players', uid),
      (snap) => {
        if(snap.exists()) onChange(snap.data());
      },
      (err) => console.warn('[即時同步] 監聽失敗', err)
    );
    return unsubscribe; // 呼叫可取消監聽
  };

  // ════════════════════════════════════════════════════════════════
  // ★ v3.5.18 — 換帳號保護:Pending Handoff API(共用平板情境)
  //
  // 情境:小明在 iPad-01 打 BOSS 沒登出,小華第 2 節用同台登入自己帳號 →
  //       小華這台會偵測到「上次是別人」,在清本機前先把小明的戰鬥快照
  //       「暫存到雲端」,等小明下次登入時自動讀出來補回他的雲端 advSnapshot
  //
  // 路徑設計:
  //   寫:小華的 auth.uid 寫到 pendingHandoffs/{小明 uid} 文件
  //   讀:小明登入後,自己讀 pendingHandoffs/{小明 uid}(他自己有讀權限)
  //   清:小明讀完並寫回自己的 advSnapshot 後,主動刪掉 handoff 文件
  //
  // Firestore Rules 建議(老師上 Firebase Console 設定一次):
  //   match /pendingHandoffs/{victimUid} {
  //     allow write: if request.auth != null && request.auth.uid != victimUid;
  //     allow read, delete: if request.auth != null && request.auth.uid == victimUid;
  //   }
  //
  // ⚠ 若 rules 還沒設,寫入會 permission-denied,但有 fallback:
  //   localStorage 'lxps_pending_handoff_{小明 uid}' 也會被寫入,小明回到同台就能讀
  // ════════════════════════════════════════════════════════════════
  window._fbSaveHandoff = async function(victimUid, payload){
    if(!victimUid || typeof victimUid !== 'string'){
      console.warn('[_fbSaveHandoff] 無效 victimUid,跳過');
      return false;
    }
    if(window._firestoreSdkIsDead){
      console.warn('[_fbSaveHandoff] Firestore SDK 已死亡,跳過');
      return false;
    }
    try{
      await setDoc(doc(_fbDb, 'pendingHandoffs', victimUid), payload, { merge: false });
      console.log('🤝 [_fbSaveHandoff] 已寫入 handoff for ' + victimUid.slice(0,8) + '...');
      return true;
    }catch(e){
      // permission-denied 是預期內(rules 未設定),不嚇人,只 warn
      const _code = e && e.code;
      if(_code === 'permission-denied'){
        console.warn('🤝 [_fbSaveHandoff] Firestore rules 未開放 pendingHandoffs 寫入,'
          + '改靠 localStorage fallback(小明只要回到原台就能救回)');
      } else {
        console.warn('🤝 [_fbSaveHandoff] 寫入失敗', e);
      }
      return false;
    }
  };

  window._fbConsumeHandoff = async function(uid){
    // 讀「等候我接收」的 handoff,讀到就回傳並刪掉
    if(!uid || typeof uid !== 'string') return null;
    if(window._firestoreSdkIsDead) return null;
    try{
      const _snap = await getDoc(doc(_fbDb, 'pendingHandoffs', uid));
      if(!_snap.exists()) return null;
      const _data = _snap.data();
      // 讀到就刪,避免重複讀
      try{
        await deleteDoc(doc(_fbDb, 'pendingHandoffs', uid));
      }catch(_eD){ console.warn('🤝 [_fbConsumeHandoff] 讀到 handoff 但刪除失敗(不影響資料還原)', _eD); }
      return _data;
    }catch(e){
      const _code = e && e.code;
      if(_code === 'permission-denied'){
        // rules 未設定 → 雲端路徑沒戲,只能靠 localStorage fallback
        console.log('🤝 [_fbConsumeHandoff] 雲端 handoff 路徑未開放,稍後用本機 fallback');
        return null;
      }
      console.warn('🤝 [_fbConsumeHandoff] 例外', e);
      return null;
    }
  };

  // ★ v3.15.44 — Firestore 讀取逾時保護(共用 iPad 全班同時登入,Firebase 限流/校園網路擁塞時防永久 hang)
  //   根因:全班同時開機 → 同一對外 IP 大量並發 → Firestore 連線被限流/半通不通,
  //         getDocFromServer(強制走網路、繞快取)在這種情況「既不成功也不快速失敗」→ 永久 pending →
  //         await 卡死整個 gameCloudLoad/登入流程 → 那 2~3 台玩家卡在首頁,要重開 30 分鐘才進得去。
  //   修:用 Promise.race 給每個雲端讀取套逾時。逾時 reject 後:
  //       - getDocFromServer 段 → 落入 _fbLoad/_fbLoadSlot 既有 catch → fallback getDoc(快取,本 uid 專屬,安全)
  //       - getDoc(快取)段也套較短逾時 → 快取有即回、快取無+網路 hang 則逾時拋出 →
  //         gameCloudLoad 既有「載入失敗視窗,請重整」路徑接手(不退回預設池、不空白覆蓋雲端)。
  //   設計:逾時絕不吃舊資料、不假裝新玩家 → 完全維持帳號資料保護;只把「無限等待」變成
  //         「有限等待(最壞約 server 9s + cache 4s)後給可重整的友善提示」。
  window._FB_GET_SERVER_TIMEOUT_MS = 9000;   // getDocFromServer(走網路)逾時
  window._FB_GET_CACHE_TIMEOUT_MS  = 4000;   // getDoc fallback(優先快取)逾時
  window._fbRaceTimeout = function(p, ms, label){
    var _ms = ms || 9000;
    return Promise.race([
      p,
      new Promise(function(_res, _rej){
        setTimeout(function(){
          _rej(new Error('[fb逾時] ' + (label || '讀取') + ' 超過 ' + _ms + 'ms(雲端忙線/網路擁塞)'));
        }, _ms);
      })
    ]);
  };

  window._fbLoad = async function(uid) {
    // ★ v1.0.20260514.6550 — 入口加 uid 守衛
    //   原問題:登入流程剛啟動時若 user.uid 還沒就緒就被呼叫,Firestore 的 doc() 會在
    //          ResourcePath.fromString 內讀 undefined.indexOf 拋 TypeError,
    //          gameCloudLoad 接到 throw 後誤判為雲端異常,觸發「⚠️ 雲端連線異常」紅框
    //   修法:直接擋在外面,回 null(視同新玩家無雲端文件),交由呼叫端正常流程處理
    if(!uid || typeof uid !== 'string'){
      console.warn('[_fbLoad] 收到無效 uid,跳過讀取(視同無雲端文件):', uid);
      return null;
    }
    try {
      // 優先用 getDocFromServer 強制走網路,確保拿到最新版本
      // ★ v3.15.44 — 套逾時,避免限流/網路擁塞時永久 pending 卡死登入
      const snap = await window._fbRaceTimeout(getDocFromServer(doc(_fbDb, 'players', uid)), window._FB_GET_SERVER_TIMEOUT_MS, '_fbLoad-server');
      console.log('[_fbLoad] 從伺服器強制取得最新資料');
      return snap.exists() ? snap.data() : null;  // null = 確認此玩家尚無文件
    } catch(e) {
      // ★ getDocFromServer 失敗(網路斷/逾時)時,回退到允許快取的 getDoc,但要記錄
      console.warn('[_fbLoad] 從伺服器讀取失敗,回退到允許快取的 getDoc', e);
      try {
        // ★ v3.15.44 — fallback 也套(較短)逾時:快取有即回,快取無+網路 hang 則逾時拋出
        const snap = await window._fbRaceTimeout(getDoc(doc(_fbDb, 'players', uid)), window._FB_GET_CACHE_TIMEOUT_MS, '_fbLoad-cache');
        const _fromCache = snap.metadata && snap.metadata.fromCache;
        if(_fromCache) console.warn('[_fbLoad] ⚠ 資料來自本地快取(可能不是最新)');
        return snap.exists() ? snap.data() : null;
      } catch(e2) {
        console.error('雲端讀取失敗,將往外拋以便呼叫端處理 fallback', e2);
        e2._fbLoadFailed = true;
        throw e2;
      }
    }
  };


  // 比較兩份摘要,判斷 A 是否「足夠健康可覆蓋 B」(A 在所有指標都不倒退)
  // 用於:safe 寫入決策(只有 live 健康才允許覆蓋 safe)
  // ★ v3.9.2(2026-05-26) — 補償消失 BUG 修補:移除流動性指標 coins / bagItems
  //   ROOT CAUSE(老師 2026/5/26 回報):補償工具寫了 safe 槽但沒生效。
  //   ────────
  //   舊版 _summaryIsHealthierOrEqual 要求所有指標都不能倒退,包含 coins 與 bagItems。
  //   但玩家正常遊玩會「消費」coins(買商品、做召喚)和 bagItems(用掉道具),
  //   這些指標自然會降低 — 不代表存檔不健康。
  //   ────────
  //   爆炸情境:
  //   1. 玩家 safe 槽備份時 coins=50000, bagItems=50
  //   2. 玩家正常消費到 coins=20000, bagItems=20
  //   3. 老師補償 +1 隻新英雄 → 補完後 _dataSummary 是 31隻/coins=20000/bagItems=20
  //   4. _fbWriteSafeIfHealthier 比對:coins 與 bagItems 都倒退 → 拒絕寫 safe
  //   5. 下次學生登入,_summaryRichnessScore 看到 safe 槽 coins=50000 比 live 高很多
  //      → 選 safe 槽 → 補償英雄消失
  //   ────────
  //   修補:健康度只看「非可逆指標」— 解鎖英雄數、最高等級、總等級(這些只會增加)。
  //   coins 與 bagItems 不再參與比對,改在 _summaryRichnessScore 也降權重(看下方)。
  function _summaryIsHealthierOrEqual(a, b) {
    if(!a || !b) return false;
    return (a.unlockedCount||0) >= (b.unlockedCount||0)
        && (a.maxHeroLv||0)    >= (b.maxHeroLv||0)
        && (a.totalHeroLv||0)  >= (b.totalHeroLv||0);
  }

  // 比較兩份摘要,判斷哪一份「整體更豐富」(用於登入時選 live vs safe)
  // 規則:逐項加權打分,unlockedCount 權重最高,因為這對玩家最有感
  // ★ v3.9.2(2026-05-26) — 補償消失 BUG 修補(配套):大幅降低 coins / bagItems 權重
  //   舊版 Math.min(coins, 100000) 讓 coins 權重 ≈ 100 隻英雄,比英雄總數還重要,
  //   導致「消費掉 coins 的 live 槽」分數低於「舊備份的 safe 槽」,選錯槽。
  //   新版:coins 權重壓到只貢獻分數的零頭,主要靠英雄類指標決定豐富度。
  function _summaryRichnessScore(s) {
    if(!s) return -1;
    return (s.unlockedCount||0) * 1000     // 解鎖英雄最重要(主要決定槽次)
         + (s.totalHeroLv||0) * 10
         + (s.maxHeroLv||0) * 5
         + (s.treasureCount||0) * 50       // ★ v3.14.17 — 至寶計分(50/件):領了新至寶的槽必勝舊快照,防覆蓋謀殺;權重低於英雄(1000/隻)不干擾主排序
         + (s.gearCount||0) * 30           // ★ v5.27.0 — 裝備計分(30/件):同款防覆蓋謀殺;權重低於至寶不干擾主排序
         + (s.ticketCount||0) * 40         // ★ v5.135.0 — 券實例計分(40/張):領了新券的槽必勝舊快照,防覆蓋謀殺;權重低於至寶(50)不干擾主排序
         + Math.min((s.coins||0) / 1000, 100)   // ★ v3.9.2 — coins 權重從上限 100000 → 100,只當小尾數
         + Math.min((s.bagItems||0), 100);      // ★ v3.9.2 — bagItems 移除 ×20 倍率
  }
  window._summaryIsHealthierOrEqual = _summaryIsHealthierOrEqual;
  window._summaryRichnessScore = _summaryRichnessScore;

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.15.40(2026-06-18)— 跨槽稽核感知合併(帳號資料保護「最高規格」核心)
  // ────────────────────────────────────────────────────────────────────
  // 老師需求:完全杜絕「抽到/補發的英雄又消失、出現別人角色、水晶/鬥技證歸零、
  //          日本關退條件、GM 刪不掉」。根因是 gameCloudLoad 舊版「三槽只挑一槽」
  //          (_summaryRichnessScore 選最豐富那份),導致:
  //            ① 落選槽獨有的資源(水晶/券/英雄)直接消失
  //            ② GM 刪角色寫的 _adminRescueSignal 只在主檔,若 live/safe 勝出讀不到 → 復活
  //            ③ 補償寫某幾槽,挑到沒補的槽就「補償失敗」
  // 治本:不再挑一槽,改「三槽合併成一份權威資料」:
  //   - 累積型(只增不減):unlockedHeroes / 六大養成表 / 台灣至寶 / 友情之心 /
  //     鬥技之證(持有+累積)/ 獎章 / 各類歷史帳本 → union 或逐鍵 max。
  //   - 消耗型(會花掉,必須尊重最新消費):知識幣 / 召喚水晶 / 背包 → 取「最新存檔槽」
  //     (savedAt 最大那槽)的值。三槽都是同帳號不同時間寫的,最新槽=最新真相,
  //     既不會把舊槽的高餘額退款(防刷),也不會被舊槽的低餘額蓋掉(防遺失)。
  //   - _adminRescueSignal:取三槽中 ts 最大者(GM 在任一槽下的救援/刪除信號都讀得到)。
  //   - 稽核感知:英雄/至寶 union 後,凡「最近一筆解鎖紀錄是 admin_delete」者一律排除
  //     → GM 刪除永久生效,不會被 union 從殘留槽復活;若之後玩家重新抽到(更新的正常
  //       解鎖紀錄)則自然恢復擁有(以最新紀錄為準)。
  // 安全:union/max 只增不減,結構上不可能造成英雄/累積資源遺失;消耗型取最新槽
  //       不退款也不遺失。對「已被舊版弄壞的帳號」,union 會自動把散在各槽的英雄全撿回。
  // ════════════════════════════════════════════════════════════════════
  function _lxpsArrFromSlot(s, key){
    try{
      if(Array.isArray(s[key])) return s[key];
      if(typeof s[key+'_s'] === 'string'){ var p = JSON.parse(s[key+'_s']); if(Array.isArray(p)) return p; }
    }catch(_){}
    return [];
  }
  // ★ v3.15.96 / v3.16.5 — 列入「優先採信字串版 _s」的養成表(免疫 Firebase merge:true 對 map 深合併殘留的幻影)。
  //   v3.16.5(piece1):heroStatInvested / heroSkillLevels 一併納入,與 _applySafeData(早已優先採信此二者的 _s,L≈90204/90207)對齊,
  //   消除「單檔載入採信 _s、三槽合併卻採信 map」的路徑不一致;且 v3.16.4 已讓 4 支 GM 清污染工具同步寫 _s,
  //   納入後不再有「GM 清乾淨 map 但 _s 殘髒」的回歸。fallback 安全:某槽無 _s → 退回該槽 map。
  //   ★ v3.16.33 — heroExp/heroStatPoints/heroBurstLevels/heroTraitLevel 四欄一併納入(本版已讓 _applySafeData 優先採信此四者 _s + 6 處 GM 清污染工具同步寫 _s,兩路徑對齊,不再有「採信 _s vs 採信 map」不一致)。heroCapsuleInvested 仍維持原行為(_applySafeData 走自有 _s 分支)。
  // ★ v3.37.0 — tamedPets(已馴養寵物)納入:union+逐鍵max 合併,_s 優先採信免疫 merge 幻影
  // ★ v5.35.0 — equipmentData 一併納入:玩家端賣出/送禮/GM 刪除都是 delete instances[eqUid],但存檔走 merge:true
  //   → 雲端 map 欄位保留已刪子鍵(playerBackpack 洗錢 BUG 同款病灶);_s 整包字串永遠正確。原本三槽合併
  //   讀 map 優先,幻影實例僅靠 300 筆帳本稽核感知攔截(帳本被擠爆後即失守=賣掉的裝備復活可再賣)。
  //   納入後合併一律優先採信 _s,與 _applySafeData 單檔路徑(_fromS 早已 _s 優先)對齊,消除路徑不一致
  //   (v3.16.5 heroStatInvested 同款對齊前例)。fallback 安全:某槽無 _s → 退回該槽 map。
  var _LXPS_PREFER_S = { heroLevels: 1, heroStatInvested: 1, heroSkillLevels: 1, heroExp: 1, heroStatPoints: 1, heroBurstLevels: 1, heroTraitLevel: 1, tamedPets: 1, equipmentData: 1, alchemyData: 1, ticketData: 1 };   /* ★ v5.135.0 補 ticketData（券實例：消費後 delete inst[tid]，map 深合併會讓已用券復活） */   // ★ v5.56.0 補 alchemyData(消耗型計數·賣出/煉丹後防 map 深合併復活)
  function _lxpsObjFromSlot(s, key){
    try{
      // heroLevels:先採信乾淨字串版,避免 map 深合併幻影被 max-merge 帶進來
      if(_LXPS_PREFER_S[key] && typeof s[key+'_s'] === 'string'){
        var ps = JSON.parse(s[key+'_s']); if(ps && typeof ps === 'object' && !Array.isArray(ps)) return ps;
      }
      if(s[key] && typeof s[key] === 'object' && !Array.isArray(s[key])) return s[key];
      if(typeof s[key+'_s'] === 'string'){ var p = JSON.parse(s[key+'_s']); if(p && typeof p === 'object' && !Array.isArray(p)) return p; }
    }catch(_){}
    return {};
  }
  // 合併多槽的「歷史帳本」陣列(以 keyFn 去重,依 at 排序,保留尾端 cap 筆)
  function _lxpsMergeHist(slots, key, keyFn, cap){
    var seen = {}; var out = [];
    slots.forEach(function(s){
      _lxpsArrFromSlot(s, key).forEach(function(e){
        if(!e || typeof e !== 'object') return;
        var k = keyFn(e); if(!k || seen[k]) return; seen[k] = true; out.push(e);
      });
    });
    out.sort(function(a,b){ return (a.at||0) - (b.at||0); });
    return cap && out.length > cap ? out.slice(-cap) : out;
  }
  // 由合併後的解鎖紀錄,算出每個 name/id「最近一筆動作是否為 admin_delete」
  //   normal/unlock/admin_grant/admin_restore → 視為擁有(deleted=false)
  //   admin_delete → deleted=true。以 at 最大者為準。
  function _lxpsLatestDeletedMap(hist, nameField){
    var m = {};
    hist.forEach(function(e){
      var nm = e && e[nameField]; if(!nm) return;
      var at = e.at || 0;
      var prev = m[nm];
      if(!prev || at >= prev.at){
        // ★ v3.16.87 — 稽核感知擴充:admin_delete(GM 刪)與 audit_error_recovered(學生自助「不是我的」disown)
        //   皆視為「已移除」。以「最近一筆紀錄」為準 → 之後若玩家重新抽到/老師補發(更新的 normal/admin_grant
        //   紀錄,at 更大)則自然恢復擁有。如此學生 disown 的污染英雄/至寶不會被殘槽 union 復活,
        //   且不影響合法再取得(latest-entry-wins)。
        // ★ v5.31.0 — 裝備 Phase 3/4:'sold'(玩家賣出)/'gift_sent'(送給好友)同列已移除
        //   (僅裝備帳本會寫這兩個 source;英雄/至寶帳本從不寫,零影響)
        m[nm] = { at: at, deleted: (e.source === 'admin_delete' || e.source === 'audit_error_recovered' || e.source === 'sold' || e.source === 'gift_sent') };
      }
    });
    return m;
  }
  try{ window._lxpsLatestDeletedMap = _lxpsLatestDeletedMap; }catch(_){}   // ★ v3.16.93 — 供 gameCloudSave 等其他 script block 取用(稽核感知倒退豁免)
  // ════════════════════════════════════════════════════════════════════
  // ★ v3.16.94 — 登入「權威下載」練英雄保底 helper(問題3:原本有練的英雄變低等且未收錄)
  // ────────────────────────────────────────────────────────────────────
  // 病灶:v3.16.87/90 權威下載在多數情況直接採主檔(不合併)→ 若某槽有「主檔沒有、但已練過」
  //   的英雄(或主檔該英雄等級偏低、槽裡等級高),會被靜默丟失/降等。
  // 中間地帶:主檔權威為底,只把「有投資證據(effort)且未被 admin_delete/audit_error_recovered、
  //   且來自 reset 之後(savedAt > cutoff)的槽」的英雄保底補回(union unlockedHeroes + 逐鍵 max 養成),
  //   同步重寫 _s 與 _dataSummary(unlockedCount/maxHeroLv)。coins/至寶/背包 一律仍採主檔。純加不減。
  //   ・effort 閘門 → Lv1 幻影不復活;・cutoff per-slot → reset 清掉的不復活;
  //   ・removal-aware → GM bulk-remove(admin_delete)/學生 disown(audit_error_recovered)不復活;
  //   ・全程 try-catch:任何錯誤 → 回傳原 main(只會改善或 no-op,絕不讓登入更糟);無合格槽英雄 → 自限 no-op。
  // ════════════════════════════════════════════════════════════════════
  window._lxpsHeroFloorOntoMain = function(main, rawSlots, cutoff){
    try{
      if(!main || typeof main !== 'object') return main;
      var _cut = (typeof cutoff === 'number') ? cutoff : 0;
      var _eligible = (Array.isArray(rawSlots) ? rawSlots : []).filter(function(s){
        return s && typeof s === 'object' && (typeof s.savedAt === 'number') && s.savedAt > _cut;
      });
      if(_eligible.length === 0) return main;   // 無合格槽(reset 後/無槽)→ no-op
      // 移除感知:union 主檔+合格槽 歷史 → admin_delete / audit_error_recovered 英雄不復活
      var _allHist = _lxpsMergeHist([main].concat(_eligible), '_heroUnlockHistory', function(e){ return (e.at||0)+'|'+(e.name||''); }, 400);
      var _delMap = _lxpsLatestDeletedMap(_allHist, 'name');
      // effort:任一合格槽對該英雄有投資證據(等級>1/素質/技能/天賦/爆發/膠囊/點數/經驗/裝至寶)
      var _slotEffort = function(s, n){
        try{
          var hl = _lxpsObjFromSlot(s, 'heroLevels'); var lv = (typeof hl[n] === 'number') ? hl[n] : 0; if(lv > 1) return true;
          var inv = _lxpsObjFromSlot(s, 'heroStatInvested'); if(inv[n] && typeof inv[n] === 'object'){ var _sm = 0; Object.keys(inv[n]).forEach(function(k){ var v = inv[n][k]; if(typeof v === 'number') _sm += v; }); if(_sm > 0) return true; }
          var sk = _lxpsObjFromSlot(s, 'heroSkillLevels'); if(sk[n] && typeof sk[n] === 'object'){ var _a = false; Object.keys(sk[n]).forEach(function(k){ if((sk[n][k] || 0) > 0) _a = true; }); if(_a) return true; }
          if((_lxpsObjFromSlot(s, 'heroTraitLevel')[n] || 0) > 0) return true;
          if((_lxpsObjFromSlot(s, 'heroBurstLevels')[n] || 0) > 0) return true;
          if((_lxpsObjFromSlot(s, 'heroCapsuleInvested')[n] || 0) > 0) return true;
          if((_lxpsObjFromSlot(s, 'heroStatPoints')[n] || 0) > 0) return true;
          if((_lxpsObjFromSlot(s, 'heroExp')[n] || 0) > 0) return true;
          var _td = _lxpsObjFromSlot(s, 'taiwanTreasureData'); var _eq = false; Object.keys(_td).forEach(function(id){ var t = _td[id]; if(t && t.equippedTo === n) _eq = true; }); if(_eq) return true;
        }catch(_){}
        return false;
      };
      // Q:合格補回英雄集(在合格槽 unlocked + 有 effort + 未被刪)
      var _Q = {};
      _eligible.forEach(function(s){
        _lxpsArrFromSlot(s, 'unlockedHeroes').forEach(function(n){
          if(!n || _Q[n]) return;
          if(_delMap[n] && _delMap[n].deleted) return;   // 被刪英雄不復活
          if(_slotEffort(s, n)) _Q[n] = 1;               // 有投資證據才補回(Lv1 幻影不復活)
        });
      });
      var _Qkeys = Object.keys(_Q);
      if(_Qkeys.length === 0) return main;   // 無合格補回英雄 → no-op(自限)
      // unlockedHeroes:主檔 ∪ Q(純加不減;主檔既有一律保留,Q 已排除被刪)
      var _uSet = {}; _lxpsArrFromSlot(main, 'unlockedHeroes').forEach(function(n){ if(n) _uSet[n] = 1; });
      _Qkeys.forEach(function(n){ _uSet[n] = 1; });
      var _flUnlocked = Object.keys(_uSet);
      // 八大養成表:主檔為底完整保留;僅對 Q 英雄逐鍵 max 從合格槽 floor-up
      var _SIX = ['heroLevels','heroExp','heroSkillLevels','heroStatPoints','heroStatInvested','heroCapsuleInvested','heroBurstLevels','heroTraitLevel'];
      var _outGrowth = {};
      _SIX.forEach(function(tk){
        var _mainMap = _lxpsObjFromSlot(main, tk);
        var acc = {}; Object.keys(_mainMap).forEach(function(k){ acc[k] = _mainMap[k]; });   // 主檔全保留
        _Qkeys.forEach(function(H){
          var base = (H in acc) ? acc[H] : undefined;
          _eligible.forEach(function(s){
            var v = _lxpsObjFromSlot(s, tk)[H];
            if(typeof v === 'number'){
              if(typeof base !== 'number' || v > base) base = v;
            } else if(v && typeof v === 'object'){
              var cur = (base && typeof base === 'object') ? base : {};
              var out2 = Object.assign({}, cur);
              Object.keys(v).forEach(function(sk){ var sv = v[sk]; if(typeof sv === 'number'){ if(!(sk in out2) || sv > out2[sk]) out2[sk] = sv; } });
              base = out2;
            } else if(base === undefined){ base = v; }
          });
          if(base !== undefined) acc[H] = base;
        });
        _outGrowth[tk] = acc;
      });
      // 組裝:主檔為底,只覆寫 unlocked + 八養成表(含 _s)+ _dataSummary;其餘(coins/至寶/背包…)沿用主檔
      var out = Object.assign({}, main);
      out.unlockedHeroes = _flUnlocked;
      try{ out.unlockedHeroes_s = JSON.stringify(_flUnlocked); }catch(_){}
      _SIX.forEach(function(tk){ out[tk] = _outGrowth[tk]; try{ out[tk+'_s'] = JSON.stringify(_outGrowth[tk]); }catch(_){} });
      var _maxLv = 0; var _hl2 = _outGrowth['heroLevels'] || {}; Object.keys(_hl2).forEach(function(k){ var v = +_hl2[k]; if(!isNaN(v) && v > _maxLv) _maxLv = v; });
      var _ds = (main._dataSummary && typeof main._dataSummary === 'object') ? Object.assign({}, main._dataSummary) : {};
      _ds.unlockedCount = _flUnlocked.length;
      _ds.maxHeroLv = Math.max((typeof _ds.maxHeroLv === 'number' ? _ds.maxHeroLv : 0), _maxLv);
      out._dataSummary = _ds;
      try{ console.log('[練英雄保底 v3.16.94] 補回 ' + _Qkeys.length + ' 隻 slot 已練英雄', { added:_Qkeys, cutoff:_cut, eligibleSlots:_eligible.length, unlockedCount:_flUnlocked.length, maxHeroLv:_ds.maxHeroLv }); }catch(_){}
      return out;
    }catch(_e){ try{ console.warn('[練英雄保底 v3.16.94] 例外,回傳原主檔(不影響登入)', _e && _e.message); }catch(_){} return main; }
  };
  window._lxpsMergeSlots = function(rawSlots){
    var slots = (Array.isArray(rawSlots) ? rawSlots : []).filter(function(s){ return s && typeof s === 'object'; });
    if(slots.length === 0) return null;
    if(slots.length === 1) return slots[0];
    // 最新槽(savedAt 最大)— 作為消耗型欄位 + 其他未明列欄位的基底
    var _newest = slots[0];
    slots.forEach(function(s){ if((s.savedAt||0) > (_newest.savedAt||0)) _newest = s; });
    // 從最新槽複製一份當基底(保留 medals/portrait/designerWelcome/各種旗標等未明列欄位)
    var merged = Object.assign({}, _newest);

    try{
      // ── 1) 歷史帳本:跨槽 union(這些是稽核與還原的依據,務必全留) ──
      var _heroHist = _lxpsMergeHist(slots, '_heroUnlockHistory', function(e){ return (e.at||0)+'|'+(e.name||''); }, 200);
      var _treHist  = _lxpsMergeHist(slots, '_treasureUnlockHistory', function(e){ return (e.at||0)+'|'+(e.id||''); }, 300);
      var _skinHist = _lxpsMergeHist(slots, '_skinUnlockHistory', function(e){ return (e.hero||'')+'|'+(e.id||''); }, 300);
      var _coinTx   = _lxpsMergeHist(slots, '_coinTransactions', function(e){ return e.id ? ('id:'+e.id) : ((e.at||0)+'|'+(e.amount||0)); }, 400);
      var _crysTx   = _lxpsMergeHist(slots, '_crystalTransactions', function(e){ return e.id ? ('id:'+e.id) : ((e.at||0)+'|'+(e.amount||0)); }, 400);
      var _fruitH   = _lxpsMergeHist(slots, '_fruitHistory', function(e){ return (e.at||0)+'|'+(e.source||''); }, 200);
      var _actH     = _lxpsMergeHist(slots, '_activityHistory', function(e){ return e.id ? ('id:'+e.id) : ((e.at||0)+'|'+(e.type||'')); }, 800);
      var _battleMap = {};
      slots.forEach(function(s){
        _lxpsArrFromSlot(s, '_battleHistory').forEach(function(e){
          if(!e || !e.battleId) return;
          var k = String(e.battleId)+'|'+(e.type||'boss_battle');
          var prev = _battleMap[k];
          if(!prev){ _battleMap[k] = e; }
          else {
            var r1 = prev.rewards||{}, r2 = e.rewards||{};
            var u = function(a,b){ return Array.from(new Set([].concat(a||[], b||[]))); };
            _battleMap[k] = { battleId:e.battleId, at:Math.min(prev.at||Date.now(), e.at||Date.now()),
              type:e.type||prev.type, bossName:e.bossName||prev.bossName||'',
              win:(e.win!==false)&&(prev.win!==false), grade:e.grade||prev.grade||'',
              rewards:{ coins:Math.max(r1.coins||0,r2.coins||0), crystals:Math.max(r1.crystals||0,r2.crystals||0),
                exp:Math.max(r1.exp||0,r2.exp||0), heroes:u(r1.heroes,r2.heroes), treasures:u(r1.treasures,r2.treasures) },
              teamHeroes:(e.teamHeroes&&e.teamHeroes.length)?e.teamHeroes:(prev.teamHeroes||[]), uid:e.uid||prev.uid };
          }
        });
      });
      var _battleH = Object.keys(_battleMap).map(function(k){ return _battleMap[k]; })
                       .sort(function(a,b){ return (a.at||0)-(b.at||0); }).slice(-100);
      // 補償史:取筆數最多那槽(已含完整 diff,不適合逐筆 union)
      var _compHist = [], _compCount = 0;
      slots.forEach(function(s){
        var h = _lxpsArrFromSlot(s, '_compensationHistory');
        if(h.length > _compHist.length) _compHist = h;
        if((s._compensationCount||0) > _compCount) _compCount = s._compensationCount||0;
      });

      // ── 2) 稽核感知:被 GM 刪除(最近一筆=admin_delete)的英雄/至寶要排除 ──
      var _heroDel = _lxpsLatestDeletedMap(_heroHist, 'name');
      var _treDel  = _lxpsLatestDeletedMap(_treHist, 'id');

      // ── 3) unlockedHeroes:跨槽 union → 扣掉被刪英雄 ──
      var _heroSet = {};
      slots.forEach(function(s){ _lxpsArrFromSlot(s, 'unlockedHeroes').forEach(function(n){ if(n) _heroSet[n] = 1; }); });
      Object.keys(_heroDel).forEach(function(n){ if(_heroDel[n].deleted) delete _heroSet[n]; });
      var _unlocked = Object.keys(_heroSet);

      // ── 4) 六大養成表:逐鍵 max;被刪英雄的養成資料一併清掉 ──
      var _SIX = ['heroLevels','heroExp','heroSkillLevels','heroStatPoints','heroStatInvested','heroCapsuleInvested','heroBurstLevels','heroTraitLevel'];
      var _mergedSix = {};
      _SIX.forEach(function(tk){
        var acc = {};
        slots.forEach(function(s){
          var o = _lxpsObjFromSlot(s, tk);
          Object.keys(o).forEach(function(nm){
            var v = o[nm];
            if(typeof v === 'number'){ if(!(nm in acc) || v > acc[nm]) acc[nm] = v; }
            else if(v && typeof v === 'object'){
              // heroStatInvested 之類巢狀 {hp,atk,sp,spd}:逐子鍵 max
              var cur = (acc[nm] && typeof acc[nm] === 'object') ? acc[nm] : {};
              var out = Object.assign({}, cur);
              Object.keys(v).forEach(function(sk){ var sv = v[sk]; if(typeof sv === 'number'){ if(!(sk in out) || sv > out[sk]) out[sk] = sv; } });
              acc[nm] = out;
            } else if(!(nm in acc)){ acc[nm] = v; }
          });
        });
        // 清掉被刪英雄
        Object.keys(_heroDel).forEach(function(nm){ if(_heroDel[nm].deleted) delete acc[nm]; });
        _mergedSix[tk] = acc;
      });

      // ── 4.9) ★ v3.37.0 寵物馴養:union(同名取 lv/exp 較高;followTo 取最新槽非空值)·純加不減 ──
      var _petAcc = {};
      slots.forEach(function(s){
        var po = _lxpsObjFromSlot(s, 'tamedPets');
        Object.keys(po).forEach(function(pn){
          var rec = po[pn]; if(!rec || typeof rec !== 'object') return;
          var prev = _petAcc[pn];
          if(!prev){
            _petAcc[pn] = { lv: Math.max(1, rec.lv|0 || 1), exp: Math.max(0, rec.exp|0 || 0), followTo: (typeof rec.followTo==='string'&&rec.followTo) ? rec.followTo : null, aff: Math.max(0, Math.min(100, rec.aff|0 || 0)) };
            // ★ v4.14.0 — 隨機素質 seed/retrainVer 載送(缺則不寫=legacy fallback hash(name))
            if(typeof rec.seed === 'number') _petAcc[pn].seed = rec.seed;
            _petAcc[pn].retrainVer = rec.retrainVer|0 || 0;
          }
          else {
            // 逐鍵 max:等級高者為準;同級取 exp 大;followTo 有值者優先(較新槽在後迭代 → 後者非空即覆蓋)
            if((rec.lv|0) > prev.lv){ prev.lv = rec.lv|0; prev.exp = Math.max(0, rec.exp|0 || 0); }
            else if((rec.lv|0) === prev.lv){ prev.exp = Math.max(prev.exp, rec.exp|0 || 0); }
            if(typeof rec.followTo==='string' && rec.followTo) prev.followTo = rec.followTo;
            // ★ v4.0.0 — 好感度(獨立累積)逐鍵取 max(誤刪是大忌·只增不減)
            prev.aff = Math.max(prev.aff|0 || 0, Math.min(100, rec.aff|0 || 0));
            // ★ v4.14.0 — seed:retrainVer 高者整包勝出(重擲=最新權威·非逐鍵max→杜絕洗素質);同版且 prev 無 seed 才補
            var _rvA = rec.retrainVer|0 || 0, _pvA = prev.retrainVer|0 || 0;
            if(_rvA > _pvA){ prev.retrainVer = _rvA; if(typeof rec.seed === 'number') prev.seed = rec.seed; }
            else if(_rvA === _pvA && typeof prev.seed !== 'number' && typeof rec.seed === 'number'){ prev.seed = rec.seed; }
          }
        });
      });
      // 寵物馴養帳本(只增不減·同款去重合併)
      var _petHist = _lxpsMergeHist(slots, '_petTameHistory', function(e){ return (e.at||0)+'|'+(e.pet||''); }, 200);

      // ── 5) 台灣至寶:union(同 id 取等級/exp 較高);扣掉被刪至寶 ──
      var _twAcc = {};
      slots.forEach(function(s){
        var o = _lxpsObjFromSlot(s, 'taiwanTreasureData');
        Object.keys(o).forEach(function(tid){
          var rec = o[tid]; if(!rec || typeof rec !== 'object') return;
          var prev = _twAcc[tid];
          if(!prev){ _twAcc[tid] = Object.assign({}, rec); }
          else {
            // ★ v3.16.67 — 修:原 else 只取 lv/exp/equippedTo → 丟失 invested(至寶投資點數)
            //   病灶:A 帳號投資至寶 → 切 B → 切回 A,該至寶在 live/safe/主檔多槽都有 → 走此 else →
            //         重建物件漏 invested → 至寶投資點數歸零(「變回尚未投資」)。
            //   修法:比照 _applySafeData 對 invested 逐項 max 保留,並以 Object.assign(prev,rec) 保住其他子欄位。
            var _pInv = (prev.invested && typeof prev.invested === 'object') ? prev.invested : {};
            var _rInv = (rec.invested  && typeof rec.invested  === 'object') ? rec.invested  : {};
            var _mInv = {};
            ['hp','atk','sp','spd'].forEach(function(_ik){ _mInv[_ik] = Math.max(Number(_pInv[_ik])||0, Number(_rInv[_ik])||0); });
            _twAcc[tid] = Object.assign({}, prev, rec, {
              lv: Math.max(prev.lv||1, rec.lv||1),
              exp: Math.max(prev.exp||0, rec.exp||0),
              equippedTo: (rec.equippedTo!=null ? rec.equippedTo : prev.equippedTo),
              invested: _mInv
            });
          }
        });
      });
      Object.keys(_treDel).forEach(function(tid){ if(_treDel[tid].deleted) delete _twAcc[tid]; });

      // ── 5b) ★ v5.27.0 裝備系統:帳本去重合併 + 稽核感知 + 以 eqUid 整顆實例 union ──
      //   鐵則③:整顆實例物件原樣搬移(itemId/affixes/gifted/equippedTo/src/at/at2/rr 一個欄位都不能漏,
      //   直接吸取 v3.16.67 至寶 union 漏搬 invested 的教訓);同 eqUid 多槽衝突 → at2(最後異動時間)高者整包勝出
      //   (裝/卸/GM 重Roll 都會更新 at2,newest-wins 語義正確);最近一筆帳本=admin_delete/audit_error_recovered
      //   的 eqUid 一律排除,防 GM 刪除後被殘槽復活(_lxpsLatestDeletedMap 英雄/至寶前例)。
      var _gearHist = _lxpsMergeHist(slots, '_equipUnlockHistory', function(e){ return (e.at||0)+'|'+(e.eqUid||''); }, 300);
      var _gearDel  = _lxpsLatestDeletedMap(_gearHist, 'eqUid');
      var _gearAcc = {};
      slots.forEach(function(s){
        var go = _lxpsObjFromSlot(s, 'equipmentData');
        var inst = (go && go.instances && typeof go.instances === 'object') ? go.instances : {};
        Object.keys(inst).forEach(function(uid){
          var rec = inst[uid];
          if(!rec || typeof rec !== 'object' || !rec.itemId) return;
          var prev = _gearAcc[uid];
          if(!prev || ((rec.at2||rec.at||0) > (prev.at2||prev.at||0))){
            try{ _gearAcc[uid] = JSON.parse(JSON.stringify(rec)); }catch(_eGc){ _gearAcc[uid] = rec; }
          }
        });
      });
      Object.keys(_gearDel).forEach(function(uid){ if(_gearDel[uid].deleted) delete _gearAcc[uid]; });

      // ── 5c) ★★ v5.135.0 召喚卷唯一 ID：券實例三槽合併（本系統的根治點）★★ ──
      //   ① inst：以 tid union（只增不減）—— 任一槽有的實例都撿回來，不會因為挑錯槽而遺失券
      //   ② used：已用 tid 跨槽 union（append-only）
      //   ③ un  ：歷來離開數逐鍵取 max（單調遞增・永不遞減 ＝ 對帳分母不會縮水）
      //   ④ ★最後一律 inst = inst \ used★ —— 殘槽把「已經用掉的券」union 回來時當場蒸發，
      //      這正是「扣券沒上雲 ⇒ 下次登入券完好如初 ⇒ 同一張再用一次」的根治點。
      //   ⚠ 順序不可調換：先 union 才剔除，否則 A 槽的 used 攔不住 B 槽的 inst。
      //   ⚠ un 若改成取最新槽而非 max，舊槽會把分母拉低 ⇒ 對帳器把幽靈券補成合法實例（設計反例，勿改）。
      var _tkInst = {}, _tkUsedSet = {}, _tkUn = {}, _tkBf = false;
      slots.forEach(function(s){
        var td = null;
        try{ if(typeof s.ticketData_s === 'string' && s.ticketData_s){ var _p = JSON.parse(s.ticketData_s); if(_p && typeof _p === 'object' && !Array.isArray(_p)) td = _p; } }catch(_eTdS){}
        if(!td) td = _lxpsObjFromSlot(s, 'ticketData');
        if(!td || typeof td !== 'object') return;
        if(td.inst && typeof td.inst === 'object' && !Array.isArray(td.inst)){
          Object.keys(td.inst).forEach(function(tid){
            var r = td.inst[tid];
            if(!r || typeof r !== 'object' || !r.it) return;
            if(!_tkInst[tid]){ try{ _tkInst[tid] = JSON.parse(JSON.stringify(r)); }catch(_eTi){ _tkInst[tid] = r; } }
          });
        }
        if(Array.isArray(td.used)) td.used.forEach(function(tid){ if(tid) _tkUsedSet[tid] = 1; });
        if(td.un && typeof td.un === 'object' && !Array.isArray(td.un)){
          Object.keys(td.un).forEach(function(k){
            var v = parseInt(td.un[k], 10) || 0;
            if(!(k in _tkUn) || v > _tkUn[k]) _tkUn[k] = v;
          });
        }
        if(td.bf) _tkBf = true;
      });
      Object.keys(_tkUsedSet).forEach(function(tid){ if(_tkInst[tid]) delete _tkInst[tid]; });   // ★ 已用券永不復活
      var _tkUsed = Object.keys(_tkUsedSet).slice(-2000);
      // 受贈抵銷帳(鐵則⑧):累積型,逐鍵取 max
      var _grlAcc = {};
      slots.forEach(function(s){
        var o = _lxpsObjFromSlot(s, 'giftReceivedLedger');
        Object.keys(o).forEach(function(k){ var v = o[k]; if(typeof v === 'number'){ if(!(k in _grlAcc) || v > _grlAcc[k]) _grlAcc[k] = v; } });
      });

      // ── 6) 獎章:union(任一槽有就保留)。★ 真正的已得獎章在 playerMedals(物件);
      //   medals 是「永遠空陣列的遺留欄位」,兩者都 union 但以 playerMedals 為主。 ──
      var _medalAcc = {};   // playerMedals
      var _legacyMedalAcc = {};
      slots.forEach(function(s){
        var o = _lxpsObjFromSlot(s, 'playerMedals');
        Object.keys(o).forEach(function(mid){ if(!(mid in _medalAcc) || (!_medalAcc[mid] && o[mid])) _medalAcc[mid] = o[mid]; });
        var lo = _lxpsObjFromSlot(s, 'medals');
        Object.keys(lo).forEach(function(mid){ if(!(mid in _legacyMedalAcc) || (!_legacyMedalAcc[mid] && lo[mid])) _legacyMedalAcc[mid] = lo[mid]; });
      });
      // medalStats:逐鍵 max(進度計數器,避免取最新槽時丟失別槽較高的進度)
      var _medalStatsAcc = {};
      slots.forEach(function(s){
        var o = _lxpsObjFromSlot(s, 'medalStats');
        Object.keys(o).forEach(function(k){ var v = o[k]; if(typeof v === 'number'){ if(!(k in _medalStatsAcc) || v > _medalStatsAcc[k]) _medalStatsAcc[k] = v; } else if(!(k in _medalStatsAcc)){ _medalStatsAcc[k] = v; } });
      });
      // ★ v5.68.0(老師需求③:煉丹連勝「有錯就清零」)— 歸零型計數器白名單:改「取最新槽(savedAt 最大)」。
      //   根因:上面逐鍵 max 對「累積型進度」正確,但對「會歸零的連勝」是致命的——
      //   仙品煉失敗歸 0 存進 A 槽,B 槽還留著舊值 5 → max(0,5)=5 → 連勝永遠清不掉,
      //   下一次成功直接跳 6,獎章會被誤發、「連續」語意整個失效。
      //   ⚠ 老師 2026-08-18 追加裁定:arenaWinStreak(鬥技場連勝)與 previewPerfectStreak(知識預習連續全對)
      //     為同一病灶,一併納入白名單:
      //     ·arenaWinStreak → 鬥技場「連勝 5 場」獎章 arena_streak_5 原會被別槽舊高值救回=平/敗中斷後仍誤發。
      //     ·previewPerfectStreak → 預習頁「連續全對」計數與顯示原會被救回=答錯後數字不歸零、顯示失真。
      //   ⚠ cnAlchImmortalMaxStreak / previewMaxStreak(歷史最佳·單調遞增)維持 max 合併不變。
      try{
        var _MEDALSTATS_LATEST_KEYS = ['cnAlchImmortalStreak', 'arenaWinStreak', 'previewPerfectStreak'];
        var _msNewest = _lxpsObjFromSlot(_newest, 'medalStats');
        _MEDALSTATS_LATEST_KEYS.forEach(function(k){
          if(_msNewest && typeof _msNewest[k] === 'number') _medalStatsAcc[k] = _msNewest[k];
          else if(k in _medalStatsAcc) delete _medalStatsAcc[k];
        });
      }catch(_eMsLatest){ console.warn('[medalStats 歸零型欄位取最新槽] 失敗', _eMsLatest); }
      // ── 6b) petsEverCollected:累積型(已相遇寵物·v4.9.0 正名),跨槽 union(_applySafeData 是直接覆蓋,不 union 會遺失) ──
      var _petsSet = {};
      slots.forEach(function(s){ var a = _lxpsArrFromSlot(s, 'petsEverCollected'); a.forEach(function(p){ if(p) _petsSet[p] = 1; }); });
      var _petsEver = Object.keys(_petsSet);

      // ── 7) 累積型純量:max ──
      var _maxNum = function(key){ var m = 0; slots.forEach(function(s){ var v = s[key]; if(typeof v === 'number' && v > m) m = v; }); return m; };
      var _friend = _maxNum('friendshipHeart');
      var _zHeld  = _maxNum('arenaZhengHeld');
      var _zLife  = Math.max(_maxNum('arenaZhengLifetime'), _zHeld);  // 終身鬥技之證(累積型,真實存檔欄位,跨槽 max)

      // ── 8) japanProgress:OR(任一 true/S 就採用,絕不退回) ──
      var _jp = { treasures:{tengu:false,shutendoji:false,tamamo:false}, maokongSFullClear:'', maokongLegacyConfirmed:'', maokongBestRank:'' };
      var _rankOrder = {'S':4,'A':3,'B':2,'C':1,'D':0,'':-1};
      slots.forEach(function(s){
        var j = s.japanProgress; if(!j || typeof j !== 'object') return;
        if(j.treasures){ _jp.treasures.tengu = _jp.treasures.tengu||!!j.treasures.tengu;
          _jp.treasures.shutendoji = _jp.treasures.shutendoji||!!j.treasures.shutendoji;
          _jp.treasures.tamamo = _jp.treasures.tamamo||!!j.treasures.tamamo; }
        if(j.maokongSFullClear === '1') _jp.maokongSFullClear = '1';
        if(j.maokongLegacyConfirmed === '1') _jp.maokongLegacyConfirmed = '1';
        if((_rankOrder[j.maokongBestRank]||-1) > (_rankOrder[_jp.maokongBestRank]||-1)) _jp.maokongBestRank = j.maokongBestRank||'';
      });

      // ── 8b) ★ v5.41.0 — chinaProgress:OR(任一 true 就採用,絕不退回;同 japanProgress 模式) ──
      var _cnp = { treasures:{fengshen:false,xiyou:false,sanguo:false}, treasuresGot:{fengshen:0,xiyou:0,sanguo:0}, treasuresUsed:0, lvbuCleared:false };   // ★ v5.101.0 計數欄位
      slots.forEach(function(s){
        var c = s.chinaProgress; if(!c || typeof c !== 'object') return;
        if(c.treasures){ _cnp.treasures.fengshen = _cnp.treasures.fengshen||!!c.treasures.fengshen;
          _cnp.treasures.xiyou = _cnp.treasures.xiyou||!!c.treasures.xiyou;
          _cnp.treasures.sanguo = _cnp.treasures.sanguo||!!c.treasures.sanguo; }
        // ★★ v5.101.0(老師裁定乙)— 逐件取得次數 / 消耗次數:三槽逐鍵取 max(單調遞增)★★
        //   ⚠ 舊版把「消耗」寫成布林 false 是本輪根治的病灶:OR 合併「絕不退回」會從殘槽
        //     撿回 true ⇒ 涿鹿永不重鎖。計數型天生免疫此問題(只增不減,但語意正確)。
        if(c.treasuresGot && typeof c.treasuresGot === 'object'){
          _cnp.treasuresGot.fengshen = Math.max(_cnp.treasuresGot.fengshen, (c.treasuresGot.fengshen || 0));
          _cnp.treasuresGot.xiyou    = Math.max(_cnp.treasuresGot.xiyou,    (c.treasuresGot.xiyou    || 0));
          _cnp.treasuresGot.sanguo   = Math.max(_cnp.treasuresGot.sanguo,   (c.treasuresGot.sanguo   || 0));
        }
        _cnp.treasuresUsed = Math.max(_cnp.treasuresUsed, (c.treasuresUsed || 0));
        if(c.lvbuCleared) _cnp.lvbuCleared = true;
      });

      // ── 8c) ★ v5.56.0 — alchemyData(神農鼎煉丹):消耗型主體取最新槽(_s 字串優先=playerBackpack 口徑,
      //   材料/丹藥計數尊重最新消費不復活);配方 r 跨槽 union 只增不減、intro 旗標 OR、
      //   採集時間戳 g 跨槽聯集後只留 70 分鐘內 ≤6 筆(防換槽重置刷採集額度) ──
      var _alc = null;
      try{
        if(typeof _newest.alchemyData_s === 'string' && _newest.alchemyData_s){
          var _alP = JSON.parse(_newest.alchemyData_s);
          if(_alP && typeof _alP === 'object' && !Array.isArray(_alP)) _alc = _alP;
        }
      }catch(_eAlS){ _alc = null; }
      if(!_alc) _alc = _lxpsObjFromSlot(_newest, 'alchemyData');
      if(!_alc || typeof _alc !== 'object') _alc = {};
      if(!_alc.m || typeof _alc.m !== 'object') _alc.m = {};
      if(!_alc.p || typeof _alc.p !== 'object') _alc.p = {};
      if(!_alc.r || typeof _alc.r !== 'object') _alc.r = {};
      var _alG = Array.isArray(_alc.g) ? _alc.g.slice() : [];
      slots.forEach(function(s){
        var o = _lxpsObjFromSlot(s, 'alchemyData');
        if(!o || typeof o !== 'object') return;
        if(o.r && typeof o.r === 'object'){ Object.keys(o.r).forEach(function(k){ if(o.r[k]) _alc.r[k] = 1; }); }
        if(o.intro) _alc.intro = 1;
        if(Array.isArray(o.g)){ o.g.forEach(function(t){ if(typeof t === 'number' && _alG.indexOf(t) < 0) _alG.push(t); }); }
      });
      var _alNow = Date.now();
      _alc.g = _alG.filter(function(t){ return typeof t === 'number' && (_alNow - t) < 4200000; })
                   .sort(function(a,b){ return b - a; }).slice(0, 6);
      _alc.intro = _alc.intro ? 1 : 0;

      // ── 9) _adminRescueSignal:取三槽 ts 最大者(任一槽的 GM 救援/刪除信號都要被看到) ──
      var _rescue = null;
      slots.forEach(function(s){
        var r = s._adminRescueSignal;
        if(r && r.ts && (!_rescue || r.ts > _rescue.ts)) _rescue = r;
      });

      // ── 10) 消耗型:取最新槽(savedAt 最大),尊重最新消費、不退款不遺失 ──
      var _coins = (typeof _newest.knowledgeCoins === 'number') ? _newest.knowledgeCoins : 0;
      // ★ v3.15.57(2026-06-20)— 洗錢 BUG 根治(鐵律 1.213):消費型背包改「優先採信字串版 playerBackpack_s」。
      //   血淚現象(老師回報):87 萬 → 賣垃圾得 7 萬 = 94 萬,垃圾消失;重新整理後知識幣仍 94 萬(正確),
      //                       但剛賣掉的垃圾又出現可再賣 → 無限洗錢。
      //   根因:backpackRemove 對數量歸 0 的道具 delete key;但賣出走 _fbSaveLive/_fbSave 的 set(merge:true),
      //         Firestore 對 map(物件)欄位是「深度合併」→ 新資料未提及的子鍵(賣掉的道具)不會被刪除 →
      //         雲端 map 欄位 playerBackpack 保留舊道具(復活);而純量字串 playerBackpack_s 會被整包覆蓋(永遠正確)。
      //         → 知識幣(純量)正確、背包(map)子鍵復活,正是「94 萬保留、垃圾復活」的不對稱現象。
      //   漏網:_applySafeData 早有同款「優先字串繞道」(見該函式註解),但本「多槽合併」路徑漏修 →
      //         有 live+safe+主檔三槽的玩家走 _lxpsMergeSlots,_lxpsObjFromSlot 優先讀被污染 map → 中招。
      //   修法:合併時 playerBackpack 一律優先解析字串版(賣後正確、免疫 merge 污染),map 欄位僅在無字串時 fallback。
      var _bag;
      try{
        if(typeof _newest.playerBackpack_s === 'string'){
          var _pbParsed = JSON.parse(_newest.playerBackpack_s);
          _bag = (_pbParsed && typeof _pbParsed === 'object' && !Array.isArray(_pbParsed))
                   ? _pbParsed : _lxpsObjFromSlot(_newest, 'playerBackpack');
        } else {
          _bag = _lxpsObjFromSlot(_newest, 'playerBackpack');
        }
      }catch(_eBagStr){ _bag = _lxpsObjFromSlot(_newest, 'playerBackpack'); }

      // ── 11) 組權威合併物件(以最新槽為基底覆蓋上去) ──
      merged.unlockedHeroes        = _unlocked;
      _SIX.forEach(function(tk){ merged[tk] = _mergedSix[tk]; merged[tk+'_s'] = JSON.stringify(_mergedSix[tk]); });
      merged.taiwanTreasureData    = _twAcc;  merged.taiwanTreasureData_s = JSON.stringify(_twAcc);
      merged.equipmentData         = { instances:_gearAcc };  merged.equipmentData_s = JSON.stringify({ instances:_gearAcc });   // ★ v5.27.0 裝備
      merged._equipUnlockHistory   = _gearHist;  merged._equipUnlockHistory_s = JSON.stringify(_gearHist);
      merged.ticketData            = { v:1, inst:_tkInst, used:_tkUsed, un:_tkUn, bf:_tkBf };   // ★ v5.135.0 召喚卷唯一 ID
      merged.ticketData_s          = JSON.stringify({ v:1, inst:_tkInst, used:_tkUsed, un:_tkUn, bf:_tkBf });
      merged.giftReceivedLedger    = _grlAcc;    merged.giftReceivedLedger_s = JSON.stringify(_grlAcc);
      merged.tamedPets             = _petAcc; merged.tamedPets_s = JSON.stringify(_petAcc);   // ★ v3.37.0 寵物馴養
      merged._petTameHistory       = _petHist; merged._petTameHistory_s = JSON.stringify(_petHist);
      merged.playerMedals          = _medalAcc;   merged.playerMedals_s = JSON.stringify(_medalAcc);
      merged.medals                = (Array.isArray(_newest.medals) && Object.keys(_legacyMedalAcc).length === 0) ? _newest.medals : _legacyMedalAcc;
      merged.medalStats            = _medalStatsAcc;  merged.medalStats_s = JSON.stringify(_medalStatsAcc);
      merged.petsEverCollected     = _petsEver;
      merged.friendshipHeart       = _friend;
      merged.arenaZhengHeld        = _zHeld;
      merged.arenaZhengLifetime    = _zLife;
      merged.japanProgress         = _jp;
      merged.chinaProgress         = _cnp;   // ★ v5.41.0 — 中國關三神物/官渡解鎖(OR 合併結果)
      merged.alchemyData           = _alc;   merged.alchemyData_s = JSON.stringify(_alc);   // ★ v5.56.0 神農鼎煉丹
      merged.knowledgeCoins        = _coins;
      merged.playerBackpack        = _bag;     merged.playerBackpack_s = JSON.stringify(_bag);
      merged._heroUnlockHistory    = _heroHist;     merged._heroUnlockHistory_s = JSON.stringify(_heroHist);
      merged._treasureUnlockHistory= _treHist;      merged._treasureUnlockHistory_s = JSON.stringify(_treHist);
      merged._skinUnlockHistory    = _skinHist;     merged._skinUnlockHistory_s = JSON.stringify(_skinHist);
      merged._coinTransactions     = _coinTx;       merged._coinTransactions_s = JSON.stringify(_coinTx);
      merged._crystalTransactions  = _crysTx;       merged._crystalTransactions_s = JSON.stringify(_crysTx);
      merged._fruitHistory         = _fruitH;
      merged._battleHistory        = _battleH;
      merged._activityHistory      = _actH;         merged._activityHistory_s = JSON.stringify(_actH);
      merged._compensationHistory  = _compHist;     merged._compensationHistory_s = JSON.stringify(_compHist);
      merged._compensationCount    = _compCount;
      if(_rescue) merged._adminRescueSignal = _rescue;
      merged.savedAt = _maxNum('savedAt') || _newest.savedAt || Date.now();
      merged._chosenSaveSlot = 'merged';

      // ── 12) 重算 _dataSummary(讓下游守門/挑槽口徑一致) ──
      // ★ v3.15.41 乙:summary 口徑對齊 client/補償 — 只算白名單英雄(非白名單殘留不灌數字)。
      //   注意:只過濾「summary 計數」,merged.unlockedHeroes 仍保留完整 union(不從清單刪英雄)。
      var _isPHN12 = function(nm){ try{ if(typeof _PLAYER_HERO_NAMES==='undefined'||!_PLAYER_HERO_NAMES) return true; return _PLAYER_HERO_NAMES.has(nm); }catch(_){ return true; } };
      var _maxLv = 0, _totalLv = 0;
      // ★ v3.15.76 — summary 的 maxLv/totalLv 只計入「確實在 unlockedHeroes」的英雄,排除 heroLevels 逐鍵 max 後
      //   殘留、不在擁有清單裡的幻影條目(對齊 _fbSave/_fbSaveLive 守門口徑,避免下游守門用膨脹 maxLv 誤判倒退)。
      var _unlSet12 = {}; _unlocked.forEach(function(n){ _unlSet12[n]=1; });
      Object.keys(_mergedSix.heroLevels||{}).forEach(function(n){ if(!_isPHN12(n)) return; if(!_unlSet12[n]) return; var lv = _mergedSix.heroLevels[n]||0; if(lv>_maxLv) _maxLv=lv; _totalLv+=lv; });
      var _bagItems = Object.keys(_bag).reduce(function(a,k){ return a + (typeof _bag[k]==='number' ? _bag[k] : 0); }, 0);
      merged._dataSummary = { unlockedCount:_unlocked.filter(_isPHN12).length, totalHeroLv:_totalLv, maxHeroLv:_maxLv,
        coins:_coins, bagItems:_bagItems, treasureCount:Object.keys(_twAcc).length, gearCount:Object.keys(_gearAcc).length, ticketCount:Object.keys(_tkInst).length, ts:Date.now(), _source:'_lxpsMergeSlots_v3.15.40' };

      console.log('[跨槽合併 v3.15.40] 槽數=' + slots.length + ' → 英雄=' + _unlocked.length
        + ' 最高Lv=' + _maxLv + ' 幣=' + _coins + ' 水晶=' + (_bag.summon_crystal||0)
        + ' 鬥技證=' + _zHeld + ' 至寶=' + Object.keys(_twAcc).length
        + (_rescue ? ' [含救援信號 ts=' + _rescue.ts + ']' : '')
        + (Object.keys(_heroDel).filter(function(n){return _heroDel[n].deleted;}).length ? ' [排除已刪英雄 ' + Object.keys(_heroDel).filter(function(n){return _heroDel[n].deleted;}).length + ' 隻]' : ''));
    }catch(_eMerge){
      console.error('[跨槽合併 v3.15.40] 合併例外,回退用最新槽', _eMerge);
      return _newest;
    }
    // ★★ v5.141.0（老師回報「舊帳號更新後又跳出已做完的寵物教學」）— 一次性教學旗標跨槽 OR ★★
    //   petTut1Done／petTut2Seen／petTut2Off／petHouseTutDone 由 _fbSetPetTutFlag **只 merge 進主檔**
    //   （不在 gameCloudSave 白名單，live／safe 槽永遠沒有）；而本函式的基底是「savedAt 最新的槽」
    //   ⇒ live 槽只要比主檔新（幾乎必然），旗標就整組消失 ⇒ _applySafeData 的 v5.125.0 掛旗標讀到 undefined
    //   ⇒ 新裝置（無 localStorage）每次都「第一次」⇒ 教學重跑、「你收到上線禮」文案重現。
    //   修法：這四個「一旦為 true 永不回頭」的旗標，任一槽為 true 即為 true（只增不減，不可能把已完成打回未完成）。
    try{
      ['petTut1Done','petTut2Seen','petTut2Off','petHouseTutDone'].forEach(function(k){
        if(merged[k]) return;
        for(var i = 0; i < slots.length; i++){ if(slots[i] && slots[i][k]){ merged[k] = slots[i][k]; break; } }
      });
    }catch(_ePt){}
    return merged;
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.4.7 ── Firestore SDK 死亡偵測 + 救援 UI ──
  // ────────────────────────────────────────────────────────────────────
  // 根因(老師 2026/5/20 截圖):Firestore JS SDK 10.12.0 內部 bug
  //   "FIRESTORE (10.12.0) INTERNAL ASSERTION FAILED: Unexpected state"
  //   觸發場景:多 tab 切換 / 網路重連 / IndexedDB 髒掉 / write stream 重連時佇列損毀
  //   災難:一旦觸發,此 tab 的 Firestore client 從此完全壞掉,所有後續
  //         setDoc/updateDoc 都會繼續拋同樣的 assertion。
  //   後果(在世界 BOSS 連線戰):client 玩家施技能 → updateDoc(pendingAction) 失敗
  //         → 「行動送出失敗」彈窗 → 點 BUG 回報也失敗(同樣是 setDoc)
  //
  // 修法策略:
  //   (a) 在每次寫入失敗的 catch 內呼叫 _checkAndHandleFirestoreDeath(e)
  //   (b) 偵測連續 ≥2 次的「INTERNAL ASSERTION FAILED」→ 標記 SDK 已死
  //   (c) SDK 已死後,在畫面右上角貼一個明顯的紅色 banner,引導玩家重新整理頁面
  //   (d) BUG 回報失敗時改存 localStorage,等下次能連線時補送
  window._firestoreSdkDeadCount = 0;        // 累計遇到 INTERNAL ASSERTION 的次數
  window._firestoreSdkIsDead = false;        // 標記 SDK 已死(true 後不再嘗試任何寫入)
  window._firestoreSdkLastErrorTs = 0;

  // 判斷錯誤是否屬於「SDK 內部 assertion 死亡」類型
  window._isFirestoreFatalError = function(e){
    if(!e) return false;
    const _msg = (e.message || e.toString() || '').toUpperCase();
    return _msg.indexOf('INTERNAL ASSERTION FAILED') >= 0
        || _msg.indexOf('UNEXPECTED STATE') >= 0;
  };

  // 寫入失敗時呼叫,若是 SDK 死亡型錯誤則累計次數;達 2 次標記為死亡並顯示救援 UI
  window._checkAndHandleFirestoreDeath = function(e, _source){
    if(!window._isFirestoreFatalError(e)) return false;
    window._firestoreSdkLastErrorTs = Date.now();
    window._firestoreSdkDeadCount = (window._firestoreSdkDeadCount || 0) + 1;
    console.warn('[Firestore SDK 死亡偵測] 累計 ' + window._firestoreSdkDeadCount + ' 次 INTERNAL ASSERTION FAILED'
                 + (_source ? ' (來源:' + _source + ')' : ''));
    if(window._firestoreSdkDeadCount >= 2 && !window._firestoreSdkIsDead){
      window._firestoreSdkIsDead = true;
      console.error('💀 [Firestore SDK 死亡] 已連續 2 次 INTERNAL ASSERTION FAILED,標記 SDK 為死亡狀態,後續寫入全部會失敗');
      try{ window._showFirestoreDeathBanner && window._showFirestoreDeathBanner(); }catch(_){}
    }
    return true;
  };

  // 顯示救援 banner(只顯示一次,點重新整理才會消失)
  window._showFirestoreDeathBanner = function(){
    if(document.getElementById('_firestore-death-banner')) return;
    const _banner = document.createElement('div');
    _banner.id = '_firestore-death-banner';
    _banner.style.cssText = [
      'position:fixed;top:14px;left:50%;transform:translateX(-50%);z-index:2147483647;',
      'background:linear-gradient(135deg,rgba(180,30,30,0.97),rgba(140,20,20,0.97));',
      'border:3px solid #ff6666;border-radius:12px;',
      'padding:14px 22px;color:#ffe0e0;font-size:15px;font-weight:700;',
      'font-family:"M PLUS Rounded 1c","Nunito",sans-serif;line-height:1.5;',
      'box-shadow:0 8px 32px rgba(0,0,0,0.7),0 0 24px rgba(255,80,80,0.5);',
      'max-width:min(96vw,580px);text-align:center;letter-spacing:1px;',
      'animation:_fsDeathPulse 1.5s ease-in-out infinite;',
    ].join('');
    _banner.innerHTML =
      '<style>@keyframes _fsDeathPulse{0%,100%{box-shadow:0 8px 32px rgba(0,0,0,0.7),0 0 24px rgba(255,80,80,0.5);}50%{box-shadow:0 8px 32px rgba(0,0,0,0.7),0 0 36px rgba(255,120,120,0.85);}}</style>'
      + '<div style="font-size:18px;margin-bottom:6px;">⚠ 雲端連線異常</div>'
      + '<div style="font-size:13px;color:#ffcccc;margin-bottom:10px;font-weight:500;">'
      + '伺服器連線發生內部錯誤,所有上傳功能(行動送出、雲端存檔、BUG 回報)<br>'
      + '在這個分頁都將失效。請按下方按鈕重新整理頁面恢復連線。'
      + '</div>'
      + '<button id="_firestore-death-reload" style="'
      + 'padding:10px 22px;font-size:15px;font-weight:800;cursor:pointer;letter-spacing:2px;'
      + 'background:linear-gradient(135deg,#ffdd66,#ffaa22);border:2px solid #fff;color:#5a2a00;'
      + 'border-radius:8px;font-family:inherit;box-shadow:0 0 12px rgba(255,200,80,0.6);">'
      + '🔄 立刻重新整理頁面'
      + '</button>'
      + '<button id="_firestore-death-dismiss" style="'
      + 'margin-left:10px;padding:10px 16px;font-size:13px;font-weight:600;cursor:pointer;'
      + 'background:rgba(60,30,30,0.7);border:1.5px solid rgba(255,180,180,0.5);color:#ffcccc;'
      + 'border-radius:8px;font-family:inherit;">'
      + '✕ 我知道了'
      + '</button>';
    document.body.appendChild(_banner);
    document.getElementById('_firestore-death-reload').onclick = () => {
      // ★ v5.148.0 — 登入中的自家重整改走保留登入的 helper（未登入時等同裸 reload）
      if(typeof window._lxpsReloadKeepLoginIfLoggedIn === 'function'){ window._lxpsReloadKeepLoginIfLoggedIn(); return; }
      try{ location.reload(); }catch(_){ window.location.href = window.location.href; }   // 舊碼備查
    };
    document.getElementById('_firestore-death-dismiss').onclick = () => {
      _banner.remove();
    };
  };

  // ★ v3.4.8 — 主動的「Firestore 連線預檢」工具
  //   背景:老師 2026/5/20 回報「組隊打世界 BOSS,第二位玩家施技能卡死」截圖中,
  //         SDK 已被 INTERNAL ASSERTION FAILED 摧毀,但因為玩家完全沒做任何寫入
  //         動作(只是在大廳組隊),SDK 死亡偵測一直沒被觸發,直到戰鬥開打才爆。
  //   策略:在玩家「即將進入世界 BOSS 戰流程」前(創房/加入房間/單人練習),
  //         主動發一個輕量的測試讀取 + 測試寫入,確認 SDK 還活著。
  //         不活 → 直接擋下並顯示明確錯誤,避免玩家組好隊伍才卡死被白扣次數。
  //   優點:讀寫都做(因為某些 INTERNAL ASSERTION 只在 write stream 才會跑出來),
  //         同時消耗最少配額(讀:玩家自己的 saves/live;寫:可選的 ping doc)。
  //
  // 回傳: { ok: boolean, reason: string, errorMessage: string }
  //   - ok=true   → SDK 健康,可繼續流程
  //   - ok=false  → 已自動顯示救援 banner,呼叫方應 return 不要往下走
  window._fbPreflightCheck = async function(){
    // (1) SDK 已被標記死亡 → 直接拒絕
    if(window._firestoreSdkIsDead){
      console.warn('[Preflight] SDK 已標記為死亡,直接拒絕');
      try{ window._showFirestoreDeathBanner(); }catch(_){}
      return { ok:false, reason:'sdk_dead', errorMessage:'Firestore SDK 已被標記為死亡' };
    }
    // (2) 未登入 → 後面不會有寫入,直接放行(避免阻擋訪客單人練習)
    if(!window._gUserId){
      return { ok:true, reason:'guest_no_check' };
    }
    // (3) 做一次輕量的讀取 + 寫入測試(timeout 5 秒)
    const _TIMEOUT_MS = 5000;
    const _withTimeout = (p, label) => Promise.race([
      p,
      new Promise((_, rej) => setTimeout(() => rej(new Error('[Preflight] ' + label + ' 逾時 ' + _TIMEOUT_MS + 'ms')), _TIMEOUT_MS))
    ]);
    try{
      // 讀:玩家自己的 saves/live(必定存在或不存在都可以,只在乎 SDK 能不能跑完一次 IO)
      await _withTimeout(
        getDoc(doc(_fbDb, 'players', window._gUserId, 'saves', 'live')),
        'read-saves-live'
      );
      // 寫:玩家自己 profile 加 ping 時戳(merge:true 不會破壞既有資料)
      //   merge:true + 只寫一個 ping 欄位,寫失敗最不影響玩家狀態
      await _withTimeout(
        setDoc(doc(_fbDb, 'players', window._gUserId),
          { _wbPreflightPing: Date.now() },
          { merge: true }),
        'write-ping'
      );
      console.log('[Preflight] ✅ Firestore SDK 健康檢查通過');
      return { ok:true, reason:'passed' };
    } catch(e){
      console.error('[Preflight] ❌ 健康檢查失敗', e);
      const _isFatal = (typeof window._isFirestoreFatalError === 'function') && window._isFirestoreFatalError(e);
      if(_isFatal){
        // 是 SDK 致命錯誤(INTERNAL ASSERTION FAILED),直接標記死亡
        if(typeof window._checkAndHandleFirestoreDeath === 'function'){
          // 直接讓累計計數一次跳 2,觸發 banner
          window._firestoreSdkDeadCount = (window._firestoreSdkDeadCount || 0) + 2;
          window._checkAndHandleFirestoreDeath(e, 'Preflight');
        } else {
          window._firestoreSdkIsDead = true;
          try{ window._showFirestoreDeathBanner(); }catch(_){}
        }
        return { ok:false, reason:'sdk_internal_assertion', errorMessage: e.message || String(e) };
      }
      // 不是 SDK 致命錯誤,可能是網路斷線/逾時 — 也擋下,但用較溫和的提示
      return { ok:false, reason:'network_or_timeout', errorMessage: e.message || String(e) };
    }
  };

  // 寫 live 槽(高頻,無保護,純覆蓋)
  // 維持與舊 _fbSave 同樣寫法:setDoc + merge,讓 Firebase merge 規則一致
  window._fbSaveLive = async function(uid, data) {
    // ★ v1.0.20260514.6550 — uid 守衛
    if(!uid || typeof uid !== 'string'){
      console.warn('[_fbSaveLive] 收到無效 uid,跳過儲存:', uid);
      return false;
    }
    // ★ v3.4.7 — SDK 已死則直接放棄,免得連續拋同樣 assertion 把 console 塞爆
    if(window._firestoreSdkIsDead){
      console.warn('[_fbSaveLive] Firestore SDK 已死亡,跳過儲存');
      return false;
    }
    // ★ v5.111.0 — 巢狀陣列安全網(同 _fbSave;live 槽走 Transaction.set,同樣會整份拋錯)
    data = window._lxpsSanitizeForFirestore(data, '_fbSaveLive');
    // ★ v3.5.37 — 健康度守門:寫入前比對雲端 live 槽,本地差太多就拒絕
    //   背景:老師回報「有學生帳號變回 Lv1」,根因是共用 iPad 上別人沒清乾淨
    //         的本地記憶體變數,在 _progressLoaded race condition 觸發 autosave,
    //         把對方雲端的豐富資料覆蓋成自己的低等級資料。
    //   舊版 gameCloudSave 的保護層 2 只擋「全空白」,但「Lv5 + 200 幣 + 18 隻英雄」
    //         不算全空白,所以擋不下來。
    //   修法:在這裡加 transaction read 比對,本地「解鎖英雄數」或「總等級」或
    //         「最高等級」明顯低於雲端就拒絕(差距閾值給留一些彈性,不誤殺正常存檔)
    //   閾值規則(任一成立即拒絕):
    //     - unlockedCount  少 5 隻以上(正常玩遊戲不會一次丟 5 隻)
    //     - maxHeroLv      少 5 級以上(正常玩遊戲不會等級倒退 5 級)
    //     - totalHeroLv    少 30 級以上(總等級倒退 30 級顯然是換成別人的資料)
    //     - coins          少 5000 以上(知識幣只會累積,大幅倒退不正常)
    //   注意:候選資料 _dataSummary 必須存在,不然不做比對(向後相容老存檔)
    try {
      const _newSummary = data && data._dataSummary;
      if(_newSummary){
        const _liveRef = doc(_fbDb, 'players', uid, 'saves', 'live');
        const _gateResult = await runTransaction(_fbDb, async (tx) => {
          const _liveSnap = await tx.get(_liveRef);
          if(!_liveSnap.exists()){
            // 雲端 live 還沒存在 → 第一次寫,直接通過
            tx.set(_liveRef, data, { merge: true });
            return { written: true, reason: 'first_write' };
          }
          const _existing = _liveSnap.data();
          // ★ v3.13.47 — 皮膚保護(live 槽):與雲端既有皮膚 + 選擇 聯集,只增不減
          let _liveWriteData = data;
          if(data && (data.heroPortraitUnlocked || data.heroPortraitChoice)){
            const _patch = {};
            if(data.heroPortraitUnlocked && typeof data.heroPortraitUnlocked === 'object'){
              const _mu = _mergePortraitUnlockUnion(_existing && _existing.heroPortraitUnlocked, data.heroPortraitUnlocked);
              if(_mu) _patch.heroPortraitUnlocked = _mu;
            }
            if(data.heroPortraitChoice && typeof data.heroPortraitChoice === 'object'){
              const _mc = _mergePortraitChoice(_existing && _existing.heroPortraitChoice, data.heroPortraitChoice);
              if(_mc) _patch.heroPortraitChoice = _mc;
            }
            if(Object.keys(_patch).length) _liveWriteData = Object.assign({}, data, _patch);
          }
          const _existingSummary = _existing && _existing._dataSummary;
          if(!_existingSummary){
            // 雲端 live 沒摘要(舊存檔) → 用「整體有資料就放行」的寬鬆模式
            tx.set(_liveRef, _liveWriteData, { merge: true });
            return { written: true, reason: 'no_existing_summary' };
          }
          // 兩邊都有摘要 → 做差距比對
          // ★ v3.15.41 丁:口徑一致 + 自動修復 — unlocked/maxLv/totalLv 改用「實際 unlockedHeroes/heroLevels
          //   陣列 + 同一支 _PLAYER_HERO_NAMES 過濾」即時重算,不信舊版補償寫進雲端 live 的未過濾 _dataSummary。
          //   → 被舊膨脹 summary 卡住的帳號下次存檔以「過濾後雲端 vs 過濾後本地」通過;真倒退仍擋(過濾後雲端 > 本地才算)。
          const _gIsPHN = function(nm){ try{ if(typeof _PLAYER_HERO_NAMES==='undefined'||!_PLAYER_HERO_NAMES) return true; return _PLAYER_HERO_NAMES.has(nm); }catch(_){ return true; } };
          // ★ v3.16.93 — 稽核感知倒退豁免(同 _fbSave 主守門):被 GM 收回過度補償(admin_delete)/
          //   學生自助 disown(audit_error_recovered)的英雄不計入倒退比較 → 不再卡『解鎖英雄倒退X隻』。
          var _gateRemovedSetL = {};
          try{
            var _gateHistL = [].concat(
              (data && Array.isArray(data._heroUnlockHistory)) ? data._heroUnlockHistory : [],
              (_existing && Array.isArray(_existing._heroUnlockHistory)) ? _existing._heroUnlockHistory : []
            );
            var _gateDelL = _lxpsLatestDeletedMap(_gateHistL, 'name');
            Object.keys(_gateDelL).forEach(function(n){ if(_gateDelL[n] && _gateDelL[n].deleted) _gateRemovedSetL[n] = 1; });
          }catch(_eGRL){ _gateRemovedSetL = {}; }
          const _effCnt = function(_doc, _sum){
            if(_doc && Array.isArray(_doc.unlockedHeroes)) return _doc.unlockedHeroes.filter(function(n){ return _gIsPHN(n) && !_gateRemovedSetL[n]; }).length;
            return (_sum && _sum.unlockedCount) || 0;
          };
          const _effLv = function(_doc, _sum){
            if(_doc && _doc.heroLevels && typeof _doc.heroLevels === 'object'){
              // ★ v3.15.76 — 只計入「確實在 unlockedHeroes」的英雄等級,排除 heroLevels(map)深合併殘留的幻影條目
              //   (對齊 _fbSave 主守門:防 GM 救援還原後雲端 heroLevels 殘留墊高 maxLv → 正確存檔被誤判倒退拒同步)
              const _unlSet = (_doc && Array.isArray(_doc.unlockedHeroes)) ? new Set(_doc.unlockedHeroes.filter(function(n){ return _gIsPHN(n) && !_gateRemovedSetL[n]; })) : null;
              let _m=0,_t=0; Object.keys(_doc.heroLevels).forEach(function(n){ if(!_gIsPHN(n)) return; if(_unlSet && !_unlSet.has(n)) return; const lv=_doc.heroLevels[n]||0; if(lv>_m)_m=lv; _t+=lv; });
              return { max:_m, total:_t };
            }
            return { max:(_sum&&_sum.maxHeroLv)||0, total:(_sum&&_sum.totalHeroLv)||0 };
          };
          const _newLvO = _effLv(data, _newSummary), _exLvO = _effLv(_existing, _existingSummary);
          const _diff = {
            unlockedDelta: _effCnt(data, _newSummary) - _effCnt(_existing, _existingSummary),
            maxLvDelta:    _newLvO.max   - _exLvO.max,
            totalLvDelta:  _newLvO.total - _exLvO.total,
            coinsDelta:    (_newSummary.coins || 0) - (_existingSummary.coins || 0),
          };
          // ★ v3.5.38 — 守門邏輯重寫:解決 v3.5.37 大量誤殺 + 6 分鐘 32 筆自動 BUG 通報問題
          //   v3.5.37 失敗案例(老師 2026-05-22 23:13~23:19 學生實況):
          //     差異: {unlockedDelta:+1, maxLvDelta:+1, totalLvDelta:+4, coinsDelta:-8293}
          //     ↑ 學生「英雄全部進步」,但因為買召喚水晶 coins -8293 → 被擋下
          //     → 6 分鐘內擋了 32 次、自動發 32 筆 BUG 回報,Firestore 配額爆炸
          //   v3.5.38 核心原則:
          //     【鐵則】英雄相關指標若「進步或持平」,coins 倒退就一定是正常扣款 → 通過
          //     【硬擋】任一英雄指標倒退,才需要看 coins 一併判斷
          //     【硬擋】單一指標倒退到極端值才當「災難」擋下
          //   ★ v3.5.46(2026-05-23) — 完全移除知識幣倒退檢測
          //     老師指示:「知識幣倒退檢核會造成雲端寫入失敗,跳過知識幣大幅倒退的檢測,
          //              大部分都是玩家自己的消費。」
          //     理由:知識幣倒退 99% 是玩家正常消費(升級至寶、買召喚水晶、買知識卡)
          //          即使「200,000 全敗光」也是玩家自己的選擇,不該被守門擋下而搞丟存檔
          //          英雄資料倒退才是真正的「帳號被覆蓋」訊號,coins 是干擾噪音
          //   ★ v3.6.1(2026-05-24) — 完全移除「總等級倒退」檢測 (鐵律 1.66)
          //     老師回報 2026-05-24 12:16 console 案例:
          //       差異: {unlockedDelta:0, maxLvDelta:0, totalLvDelta:-1154, coinsDelta:+78}
          //       ↑ unlocked 跟 maxLv 完全對得上(0/0),但 totalLv 少 1154 級被擋下
          //       → 戰鬥中每次行動都跳紅框錯誤、爆送 BUG 回報
          //     根因:totalHeroLv 是「累加所有英雄等級」的衍生指標,
          //          容易因 _heroLevels 時序載入不完整、合併路徑差異而少算,
          //          但實際英雄資料完全沒少。
          //     原則(鐵律 1.66):衍生指標 totalHeroLv 跟 coins 一樣不可靠,不參與守門。
          //     真正可信的災難訊號只有「不可逆」的兩項:
          //       - unlockedCount(英雄被刪)
          //       - maxHeroLv(最高等級倒退)
          //   閾值(v3.6.1):
          //     【硬擋指標】(任一觸發即拒絕)
          //       - unlockedCount 倒退 >= 5 隻
          //       - maxHeroLv     倒退 >= 5 級
          //       - (totalHeroLv 完全不擋 — v3.6.1 移除)
          //       - (coins       完全不擋 — v3.5.46 移除)
          //     【複合指標】(unlocked + maxLv 一起倒退才算)
          //       - unlocked -2 且 maxLv -3
          //     【關鍵保護】只要 unlocked 跟 maxLv 沒「災難級」倒退,一律通過
          const _rejectReasons = [];
          // 計算英雄相關有沒有任何一個倒退(v3.6.1 不再看 totalLv)
          const _anyHeroRegressed =
            (_diff.unlockedDelta < 0) || (_diff.maxLvDelta < 0);
          // ★ v3.6.1 — totalLvDelta 從硬擋移除
          const _hardRegression =
            (_diff.unlockedDelta <= -5) ||
            (_diff.maxLvDelta    <= -5);
          // 複合倒退(v3.6.1):僅 unlocked + maxLv 同時倒退才算
          const _heroDoubleRegression = _anyHeroRegressed && (
            (_diff.unlockedDelta <= -2 && _diff.maxLvDelta <= -3)
          );
          // ★ v3.5.72 — 反向破口逃生口(學生 24/5/24 9:52 案例)
          //   案例:學生本地 unlocked=7-11、totalLv=141,雲端 unlocked=19、totalLv=122
          //         本地 unlocked 比雲端少 8(看起來像倒退) → 但本地 totalLv 比雲端高 19
          //   邏輯:正常人「英雄少+總等級高」的組合可能嗎?
          //         → 不可能。如果是同一個人,unlocked 多時 totalLv 也多;少時也少。
         //         → 唯一解釋是「兩個帳號資料混在一起」,且雲端那一份不是這個學生的
         //   結論:本地 totalLv 比雲端 totalLv 多 15 級以上 → 視為「雲端不是這個學生的,本地才是真的」
        //         → 給綠燈,讓本地寫回雲端覆蓋掉別人留下的殘留
        //   風險評估:正常玩家「等級高 + 解鎖少」需湊滿(totalLv 比雲端多 15+ 級且 unlocked 比雲端少 5+)
        //             幾乎不可能在「同一個帳號正常累積」下發生,這是「不同帳號殘留」的指紋
          const _localTotalLvHigher = (_diff.totalLvDelta >= 15);
          const _localHasMoreEvidence = _localTotalLvHigher && (_diff.unlockedDelta <= -3);
          if(_localHasMoreEvidence){
            console.warn('🛡️ [_fbSaveLive v3.5.72] 偵測到「反向破口」— 本地 totalLv 顯著高於雲端,'
              + '判定雲端是別帳號殘留,放行本地寫入',
              { diff: _diff, new: _newSummary, existing: _existingSummary });
          } else if(_hardRegression || _heroDoubleRegression){
            if(_diff.unlockedDelta <= -5)    _rejectReasons.push('解鎖英雄倒退 ' + Math.abs(_diff.unlockedDelta) + ' 隻');
            if(_diff.maxLvDelta    <= -5)    _rejectReasons.push('最高等級倒退 ' + Math.abs(_diff.maxLvDelta) + ' 級');
            // ★ v3.6.1 — 「總等級倒退」push 已移除(鐵律 1.66:衍生指標不參與守門)
            // ★ v3.5.46 — 知識幣倒退不再列入拒絕原因
            if(_heroDoubleRegression && _rejectReasons.length === 0){
              _rejectReasons.push('英雄資料雙重倒退(疑似帳號覆蓋):'
                + 'unlocked ' + _diff.unlockedDelta + '、maxLv ' + _diff.maxLvDelta);
            }
          }
          // ★ v3.5.46 — 移除「通過但留 log」的雜訊
          //   既然完全不檢測 coins,每次正常扣款都印 log 反而干擾 console
          //   真要看歷史扣款請看 _fbDiagnoseAllSlots 工具
          if(_rejectReasons.length > 0){
            return {
              written: false, reason: 'health_regression',
              rejectReasons: _rejectReasons,
              newSummary: _newSummary, existingSummary: _existingSummary, diff: _diff
            };
          }
          // 通過 → 寫入(★ v3.13.47 用皮膚聯集後的 _liveWriteData,確保皮膚只增不減)
          tx.set(_liveRef, _liveWriteData, { merge: true });
          return { written: true, reason: 'health_check_passed' };
        });
        if(!_gateResult.written){
          console.error('🛡️ [_fbSaveLive v3.6.5 健康度守門] 拒絕寫入 — 偵測到資料倒退:',
            _gateResult.rejectReasons);
          console.error('    新資料摘要:', _gateResult.newSummary);
          console.error('    雲端摘要:',   _gateResult.existingSummary);
          console.error('    差異:',       _gateResult.diff);
          // ★ v3.6.5(2026-05-24) — 完全移除「自動發 BUG 回報」邏輯
          //   原因(老師明確指示):「不要再出現這個了會一直洗管理員錯誤回報區」
          //   背景:健康度守門的自動發報歷經 v3.5.38 cooldown + v3.5.72 24h 冷卻多次補救,
          //         仍會因為各種誤判類型(總等級倒退、解鎖倒退、最高等級倒退)持續送進老師後台,
          //         每天累積數十至上百筆,老師審查負擔過大。
          //   原則:擋下寫入的安全動作仍保留(玩家資料保護優先),
          //         紅框 UI 提示仍保留(讓玩家知道發生事情),
          //         console.error 完整訊息仍保留(老師若想查 console 仍能看到),
          //         但「自動寫進 bugReports 集合」邏輯完全廢除,不再洗版。
          //   若老師未來需要批次審查,可從 Firestore Console 直接查歷史 bugReports,
          //         或從學生回報 console 截圖判斷。
          // [此處原 v3.5.38 cooldown + v3.5.72 24h 冷卻 + window._fbSubmitBugReport 呼叫已全數移除]
          // 用 UI 告知玩家:很可能是別人的資料殘留(只有第一次擋下時才彈,後續安靜)
          if(!window._healthRegressionUiShown){
            window._healthRegressionUiShown = true;
            // ★ v3.6.5 — 不再自動 setTimeout 重置,改由玩家按按鈕關閉
            //   按【我覺得沒問題】或【有錯誤我要回報】或紅框存在 30 秒自動關 → 都會 reset flag
            try{
              const _t = document.createElement('div');
              _t.id = '_health_regression_banner';
              _t.style.cssText = 'position:fixed;top:60px;left:50%;transform:translateX(-50%);z-index:99999;' +
                'background:rgba(80,20,20,0.96);border:3px solid #ff4444;color:#ffdddd;' +
                'font-size:14px;padding:16px 24px;border-radius:14px;max-width:92vw;min-width:300px;' +
                'box-shadow:0 8px 32px rgba(0,0,0,0.6);text-align:center;line-height:1.6;' +
                'font-family:"M PLUS Rounded 1c","Nunito",sans-serif;';
              // 安全消毒 reject reasons(避免 XSS,雖然來源是內建文字)
              const _safeReason = String(_gateResult.rejectReasons.join('、')).replace(/[<>&]/g, '');
              _t.innerHTML =
                '<div style="font-size:18px;font-weight:900;margin-bottom:6px;">🛡️ 雲端寫入保護啟動</div>' +
                '<div style="font-size:13px;color:#ffaaaa;margin-bottom:4px;">' +
                  '偵測到要寫入的資料比雲端少很多,可能是別的帳號殘留<br>' +
                  '已自動取消寫入以保護你的進度' +
                '</div>' +
                '<div style="font-size:11px;color:#ffcc88;margin-bottom:12px;">原因:' + _safeReason + '</div>' +
                '<div style="display:flex;gap:10px;justify-content:center;flex-wrap:wrap;margin-top:8px;">' +
                  '<button id="_hrb_ok_btn" style="' +
                    'padding:9px 22px;font-size:14px;font-weight:700;' +
                    'background:rgba(120,120,120,0.4);border:2px solid #888;color:#ddd;' +
                    'border-radius:10px;cursor:pointer;' +
                    'font-family:inherit;' +
                    '">👍 我知道了</button>' +
                '</div>';
              document.body.appendChild(_t);
              // 按鈕事件 + 清理函式
              const _closeBanner = function(){
                try{
                  if(_t.parentNode){
                    _t.style.transition = 'opacity 0.4s';
                    _t.style.opacity = '0';
                    setTimeout(function(){ try{ _t.remove(); }catch(_){} }, 450);
                  }
                }catch(_){}
                window._healthRegressionUiShown = false;
              };
              try{
                const _okBtn = document.getElementById('_hrb_ok_btn');
                if(_okBtn){
                  _okBtn.onclick = function(){
                    try{ if(typeof playSfx === 'function') playSfx('sfx-cancel', 0.4); }catch(_){}
                    _closeBanner();
                  };
                }
                // ★ v3.7.10 — 移除「🐛 有錯誤我要回報」按鈕事件
                //   老師指示:英雄倒退這類觸發後不再送通知到管理員後台
                //   紅框 UI 保留(讓玩家知道保護動作),但不再提供發報入口
              }catch(_eBtn){ console.warn('[健康度守門紅框] 按鈕事件綁定例外', _eBtn); }
              // 30 秒後若玩家還沒按,自動淡出避免擋畫面(同時 reset flag)
              setTimeout(_closeBanner, 30000);
            }catch(_){}
          }
          return false;
        }
        return true;
      }
      // 候選沒摘要 → 走舊路徑(向後相容)
      // ★ v3.11.3(2026-05-27) — 無摘要也加最後一道保險:讀雲端 live 槽自算摘要對比
      //   背景:_buildSafeData 的 _dataSummary 是 try-catch,例外時回 null,
      //         舊版直接放行 setDoc → 雲端被汙染。
      //         玩家「⏸ 暫存後關閉再重開 → 8 隻 1 級」的可疑路徑之一就是這裡。
      //   修法:在無摘要情境下,從雲端 live 抓現有資料自算摘要,
      //         若偵測到大幅倒退(unlocked / maxLv 任一倒退 >= 5)就拒絕。
      // ★ v3.13.47 — 無摘要 fallback 也套皮膚/選擇聯集(只增不減)
      let _noSumWriteData = data;
      try {
        const _liveRef2 = doc(_fbDb, 'players', uid, 'saves', 'live');
        const _liveSnap2 = await getDoc(_liveRef2);
        if(_liveSnap2.exists()){
          const _existing2 = _liveSnap2.data() || {};
          // 皮膚/選擇聯集
          if(data && (data.heroPortraitUnlocked || data.heroPortraitChoice)){
            const _patch = {};
            if(data.heroPortraitUnlocked && typeof data.heroPortraitUnlocked === 'object'){
              const _mu = _mergePortraitUnlockUnion(_existing2.heroPortraitUnlocked, data.heroPortraitUnlocked);
              if(_mu) _patch.heroPortraitUnlocked = _mu;
            }
            if(data.heroPortraitChoice && typeof data.heroPortraitChoice === 'object'){
              const _mc = _mergePortraitChoice(_existing2.heroPortraitChoice, data.heroPortraitChoice);
              if(_mc) _patch.heroPortraitChoice = _mc;
            }
            if(Object.keys(_patch).length) _noSumWriteData = Object.assign({}, data, _patch);
          }
          const _existHL = _existing2.heroLevels || {};
          const _existUnlocked = Array.isArray(_existing2.unlockedHeroes) ? _existing2.unlockedHeroes.length : 0;
          const _existMaxLv = Math.max(0, ...Object.values(_existHL).filter(v => typeof v === 'number'));
          const _newHL = data.heroLevels || {};
          const _newUnlocked = Array.isArray(data.unlockedHeroes) ? data.unlockedHeroes.length : 0;
          const _newMaxLv = Math.max(0, ...Object.values(_newHL).filter(v => typeof v === 'number'));
          // 大幅倒退 → 拒絕
          if(_existUnlocked - _newUnlocked >= 5 || _existMaxLv - _newMaxLv >= 5){
            console.error('🛡️ [_fbSaveLive v3.11.3 無摘要保險] 拒絕寫入 — 偵測到大幅倒退',
              { unlocked: _newUnlocked + ' vs ' + _existUnlocked, maxLv: _newMaxLv + ' vs ' + _existMaxLv });
            return false;
          }
        }
      } catch(_eNoSum) { console.warn('[v3.11.3 無摘要保險] 雲端比對失敗,仍允許寫入', _eNoSum); }
      await setDoc(doc(_fbDb, 'players', uid, 'saves', 'live'), _noSumWriteData, { merge: true });
      return true;
    } catch(e) {
      console.error('[_fbSaveLive] 失敗', e);
      window._checkAndHandleFirestoreDeath(e, '_fbSaveLive');
      return false;
    }
  };

  // 讀 live / safe 任一槽(強制從伺服器抓最新版,網路斷時 fallback 到快取)
  window._fbLoadSlot = async function(uid, slot) {
    if(slot !== 'live' && slot !== 'safe') throw new Error('slot must be live or safe');
    // ★ v1.0.20260514.6550 — uid 守衛(同 _fbLoad)
    if(!uid || typeof uid !== 'string'){
      console.warn('[_fbLoadSlot] 收到無效 uid,跳過讀取(視同無雲端文件):', uid, 'slot:', slot);
      return null;
    }
    const _ref = doc(_fbDb, 'players', uid, 'saves', slot);
    try {
      // ★ v3.15.44 — 套逾時,避免限流/網路擁塞時永久 pending 卡死登入
      const snap = await window._fbRaceTimeout(getDocFromServer(_ref), window._FB_GET_SERVER_TIMEOUT_MS, '_fbLoadSlot-server-' + slot);
      return snap.exists() ? snap.data() : null;
    } catch(e) {
      console.warn('[_fbLoadSlot] 伺服器讀取失敗,回退到 getDoc:', slot, e);
      try {
        // ★ v3.15.44 — fallback 也套(較短)逾時
        const snap = await window._fbRaceTimeout(getDoc(_ref), window._FB_GET_CACHE_TIMEOUT_MS, '_fbLoadSlot-cache-' + slot);
        const _fromCache = snap.metadata && snap.metadata.fromCache;
        if(_fromCache) console.warn('[_fbLoadSlot] ⚠ 來自快取', slot);
        return snap.exists() ? snap.data() : null;
      } catch(e2) {
        console.error('[_fbLoadSlot] 完全失敗,將拋出', slot, e2);
        e2._fbLoadFailed = true;
        throw e2;
      }
    }
  };

  // 條件式寫 safe 槽:用 Firestore transaction 做 read-then-write,
  //   ① 讀當前雲端 safe 摘要
  //   ② 比對:候選 data 的摘要 vs 雲端 safe 摘要
  //   ③ 若候選 ≥ 雲端 safe(所有指標都不倒退) → 寫入
  //   ④ 否則跳過(別裝置剛寫了更豐富的 safe,我不該蓋掉)
  //
  // 回傳: { written: bool, reason: string, prevSummary, newSummary }
  window._fbWriteSafeIfHealthier = async function(uid, data) {
    if(!uid || !data) return { written: false, reason: 'invalid_args' };
    // ★ v5.111.0 — 巢狀陣列安全網(第三個吃完整 blob 的寫入者;safe 槽是最後的保命底稿,更不能寫不進去)
    data = window._lxpsSanitizeForFirestore(data, '_fbWriteSafeIfHealthier');
    try {
      const result = await runTransaction(_fbDb, async (tx) => {
        const _safeRef = doc(_fbDb, 'players', uid, 'saves', 'safe');
        const _safeSnap = await tx.get(_safeRef);
        const _existing = _safeSnap.exists() ? _safeSnap.data() : null;
        const _existingSummary = _existing ? _existing._dataSummary : null;
        const _newSummary = data._dataSummary;
        // ★ v3.13.47 — safe 槽皮膚/選擇聯集(只增不減);first_write 無 existing 不需聯集
        let _safeWriteData = data;
        if(_existing && data && (data.heroPortraitUnlocked || data.heroPortraitChoice)){
          const _patch = {};
          if(data.heroPortraitUnlocked && typeof data.heroPortraitUnlocked === 'object'){
            const _mu = _mergePortraitUnlockUnion(_existing.heroPortraitUnlocked, data.heroPortraitUnlocked);
            if(_mu) _patch.heroPortraitUnlocked = _mu;
          }
          if(data.heroPortraitChoice && typeof data.heroPortraitChoice === 'object'){
            const _mc = _mergePortraitChoice(_existing.heroPortraitChoice, data.heroPortraitChoice);
            if(_mc) _patch.heroPortraitChoice = _mc;
          }
          if(Object.keys(_patch).length) _safeWriteData = Object.assign({}, data, _patch);
        }

        // safe 槽不存在 → 第一次寫入,直接通過
        if(!_existing) {
          tx.set(_safeRef, data);
          return { written: true, reason: 'first_write', prevSummary: null, newSummary: _newSummary };
        }

        // 候選資料沒摘要 → 拒絕(無法做健康比對)
        if(!_newSummary) {
          return { written: false, reason: 'candidate_has_no_summary', prevSummary: _existingSummary, newSummary: null };
        }

        // 雲端 safe 沒摘要(舊版資料) → 視為「不可信」,候選只要有摘要就允許覆蓋
        if(!_existingSummary) {
          tx.set(_safeRef, _safeWriteData);
          return { written: true, reason: 'replacing_unsummarized_safe', prevSummary: null, newSummary: _newSummary };
        }

        // 兩邊都有摘要 → 比對
        if(_summaryIsHealthierOrEqual(_newSummary, _existingSummary)) {
          tx.set(_safeRef, _safeWriteData);
          return { written: true, reason: 'healthier_or_equal', prevSummary: _existingSummary, newSummary: _newSummary };
        } else {
          return { written: false, reason: 'cloud_safe_richer', prevSummary: _existingSummary, newSummary: _newSummary };
        }
      });
      return result;
    } catch(e) {
      console.error('[_fbWriteSafeIfHealthier] transaction 失敗', e);
      return { written: false, reason: 'transaction_failed', error: e && e.message };
    }
  };

  // ── 好友系統 Firebase API ──

  // 儲存代表英雄
  window._fbSetRepHero = async function(uid, heroData) {
    try {
      await updateDoc(doc(_fbDb, 'players', uid), { representativeHero: heroData });
      return true;
    } catch(e) {
      // 若文件不存在則用 setDoc 合併
      try { await setDoc(doc(_fbDb, 'players', uid), { representativeHero: heroData }, { merge: true }); return true; }
      catch(e2) { console.error('儲存代表英雄失敗', e2); return false; }
    }
  };

  // ★★ v4.90.0 主角技能繼承:BD 存 players/{uid} 主檔(比照 representativeHero/avatarCard 模式)
  //   ★ 純 self-write 於自己文件的新欄位 → firestore.rules 免改免部署
  //   ★ 只在「玩家按下選定/命名」時寫入=天然節流,不進 autosave 路徑不影響存檔合併
  window._fbSaveProtagBD = async function(bd) {
    try {
      var uid = '';
      try{ uid = (window._fbAuth && window._fbAuth.currentUser) ? window._fbAuth.currentUser.uid : ''; }catch(_){}
      if(!uid) return false;
      try { await updateDoc(doc(_fbDb, 'players', uid), { protagBD: bd }); return true; }
      catch(e) {
        try { await setDoc(doc(_fbDb, 'players', uid), { protagBD: bd }, { merge: true }); return true; }
        catch(e2) { console.warn('[v4.90.0 protagBD 寫雲端失敗]', e2); return false; }
      }
    } catch(e3) { console.warn('[v4.90.0 protagBD 寫入例外]', e3); return false; }
  };

  window._fbFindByEmail = async function(email) {
    const _emailNorm = (email || '').toLowerCase().trim();
    if(!_emailNorm) {
      console.warn('[_fbFindByEmail] email 為空');
      return null;
    }
    try {
      const q = query(collection(_fbDb, 'players'), where('email', '==', _emailNorm));
      const snap = await getDocs(q);
      if(snap.empty) {
        console.log('[_fbFindByEmail] 查詢成功但找不到 email:', _emailNorm);
        return null;
      }
      const d = snap.docs[0];
      console.log('[_fbFindByEmail] 找到玩家:', _emailNorm, '→ uid:', d.id);
      return { uid: d.id, ...d.data() };
    } catch(e) {
      console.error('[_fbFindByEmail] 查詢失敗:', _emailNorm, e);
      const _eMsg = e && (e.code || e.message) ? (e.code || e.message) : '網路或權限錯誤';
      const _newErr = new Error('查詢玩家失敗: ' + _eMsg);
      _newErr.code = e && e.code;
      _newErr.original = e;
      throw _newErr;
    }
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.11.19(2026-05-28) — 第二段密碼系統(Second Password)Firestore 層
  // ────────────────────────────────────────────────────────────────────
  // 老師需求:玩家登入後可「自由決定」設定第二段密碼(固定 8 位大寫英文+數字
  //   混合)。設密碼後,每次登入(雲端載入完成後)要先輸入正確密碼才能進遊戲。
  //   管理員可輸入玩家 email 強制重設 = 「清除」密碼,讓學生下次登入重新自設。
  //
  // 安全性說明(誠實告知限制):
  //   - 純前端遊戲無法做到伺服器端真正加密,但「明碼存 Firestore」風險更高。
  //   - 採 SHA-256(salt + 密碼) 雜湊後存,管理員也看不到原碼(故重設只能「清除」)。
  //   - 這是「防同學偷看/惡作劇」等級的保護,非銀行級安全,對國小情境足夠。
  //
  // 資料結構 players/{uid}.secondPassword = {
  //   hash, salt, setAt, failCount, lockedUntil, lastFailAt, failHistory:[{ts},...]
  // }
  // ════════════════════════════════════════════════════════════════════
  window._LXPS_PW_MAX_FAIL = 5;              // 連續錯 5 次鎖定
  window._LXPS_PW_LOCK_MS  = 10 * 60 * 1000; // 鎖 10 分鐘
  window._LXPS_PW_HIST_MAX = 30;             // 錯誤紀錄最多保留 30 筆

  window._lxpsValidatePwFormat = function(pw){
    const s = String(pw == null ? '' : pw);
    if(s.length !== 8) return { ok:false, reason:'密碼必須剛好 8 位' };
    if(!/^[A-Z0-9]+$/.test(s)) return { ok:false, reason:'只能用「大寫英文字母」與「數字」' };
    if(!/[A-Z]/.test(s) || !/[0-9]/.test(s)) return { ok:false, reason:'必須同時含「大寫英文」與「數字」(混合)' };
    return { ok:true };
  };

  window._lxpsHashPw = async function(salt, pw){
    const raw = String(salt || '') + '::' + String(pw || '');
    try{
      if(window.crypto && window.crypto.subtle){
        const enc = new TextEncoder().encode(raw);
        const buf = await window.crypto.subtle.digest('SHA-256', enc);
        return Array.from(new Uint8Array(buf)).map(b => b.toString(16).padStart(2,'0')).join('');
      }
    }catch(_){}
    let h = 0; for(let i=0;i<raw.length;i++){ h = ((h<<5)-h + raw.charCodeAt(i))|0; }
    return 'fb_' + (h >>> 0).toString(16);
  };

  window._lxpsGenSalt = function(){
    try{
      const a = new Uint8Array(8);
      if(window.crypto && window.crypto.getRandomValues) window.crypto.getRandomValues(a);
      return Array.from(a).map(b=>b.toString(16).padStart(2,'0')).join('');
    }catch(_){ return String(Date.now()) + Math.random().toString(16).slice(2); }
  };

  window._fbGetMyPwState = async function(){
    if(!window._gUserId) return null;
    try{
      const snap = await getDoc(doc(_fbDb, 'players', window._gUserId));
      if(!snap.exists()) return null;
      return snap.data().secondPassword || null;
    }catch(e){ console.warn('[第二密碼] 讀取狀態失敗', e); return null; }
  };

  window._fbSetMyPw = async function(pw){
    if(!window._gUserId) return { ok:false, reason:'尚未登入' };
    const v = window._lxpsValidatePwFormat(pw);
    if(!v.ok) return v;
    try{
      const salt = window._lxpsGenSalt();
      const hash = await window._lxpsHashPw(salt, pw);
      await setDoc(doc(_fbDb, 'players', window._gUserId), {
        secondPassword: { hash:hash, salt:salt, setAt:Date.now(), failCount:0, lockedUntil:0, lastFailAt:0, failHistory:[] }
      }, { merge: true });
      return { ok:true };
    }catch(e){
      console.error('[第二密碼] 設定失敗', e);
      return { ok:false, reason:(e && (e.code||e.message)) || '寫入失敗' };
    }
  };

  window._fbVerifyMyPw = async function(pw){
    if(!window._gUserId) return { ok:false, reason:'尚未登入' };
    try{
      const ref = doc(_fbDb, 'players', window._gUserId);
      const snap = await getDoc(ref);
      const sp = (snap.exists() && snap.data().secondPassword) || null;
      if(!sp || !sp.hash) return { ok:true, noPassword:true };
      const now = Date.now();
      const locked = sp.lockedUntil && now < sp.lockedUntil;
      const inputHash = await window._lxpsHashPw(sp.salt, pw);
      const match = (inputHash === sp.hash);
      if(match){
        await setDoc(ref, { secondPassword: { ...sp, failCount:0, lockedUntil:0 } }, { merge: true });
        return { ok:true, wasLocked: !!locked };
      }
      const newFail = (sp.failCount || 0) + 1;
      const hist = Array.isArray(sp.failHistory) ? sp.failHistory.slice() : [];
      hist.push({ ts: now });
      while(hist.length > window._LXPS_PW_HIST_MAX) hist.shift();
      let lockedUntil = sp.lockedUntil || 0;
      let justLocked = false;
      if(newFail >= window._LXPS_PW_MAX_FAIL){ lockedUntil = now + window._LXPS_PW_LOCK_MS; justLocked = true; }
      await setDoc(ref, { secondPassword: { ...sp, failCount:newFail, lastFailAt:now, lockedUntil:lockedUntil, failHistory:hist } }, { merge: true });
      const remaining = Math.max(0, window._LXPS_PW_MAX_FAIL - newFail);
      return { ok:false, match:false, failCount:newFail, remaining:remaining, justLocked:justLocked, lockedUntil:lockedUntil, wasLocked:!!locked };
    }catch(e){
      console.error('[第二密碼] 驗證失敗', e);
      return { ok:false, reason:(e && (e.code||e.message)) || '驗證失敗' };
    }
  };

  window._fbAdminPeekPwByEmail = async function(email){
    const p = await window._fbFindByEmail(email);
    if(!p || !p.uid) return null;
    const sp = p.secondPassword || null;
    return {
      uid: p.uid, email: p.email || email, displayName: p.displayName || '',
      hasPassword: !!(sp && sp.hash), setAt: (sp && sp.setAt) || 0,
      failCount: (sp && sp.failCount) || 0, lockedUntil: (sp && sp.lockedUntil) || 0,
      lastFailAt: (sp && sp.lastFailAt) || 0,
      failHistory: (sp && Array.isArray(sp.failHistory)) ? sp.failHistory : []
    };
  };

  window._fbAdminClearPwByEmail = async function(email){
    const p = await window._fbFindByEmail(email);
    if(!p || !p.uid) return { ok:false, reason:'找不到此 email 對應的玩家' };
    try{
      await setDoc(doc(_fbDb, 'players', p.uid), { secondPassword: deleteField() }, { merge: true });
      return { ok:true, uid: p.uid, displayName: p.displayName || '' };
    }catch(e){
      console.error('[第二密碼] 管理員清除失敗', e);
      return { ok:false, reason:(e && (e.code||e.message)) || '清除失敗' };
    }
  };

  window._fbAdminUnlockPwByEmail = async function(email){
    const p = await window._fbFindByEmail(email);
    if(!p || !p.uid) return { ok:false, reason:'找不到此 email 對應的玩家' };
    const sp = p.secondPassword || null;
    if(!sp || !sp.hash) return { ok:false, reason:'此玩家未設定密碼,無需解鎖' };
    try{
      await setDoc(doc(_fbDb, 'players', p.uid), { secondPassword: { ...sp, failCount:0, lockedUntil:0 } }, { merge: true });
      return { ok:true, uid: p.uid };
    }catch(e){ return { ok:false, reason:(e && (e.code||e.message)) || '解鎖失敗' }; }
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.10.13(2026-05-26) — 世界 BOSS 每日場次工具【新版】
  // ────────────────────────────────────────────────────────────────────
  // 改版背景:舊 v3.9.1 工具依賴 players/{uid}.wbDailyEntries 欄位,
  //   但 v3.10.13 起改用「讀排行榜 battleHistory」判定今日場次,
  //   這個欄位以後不再使用,所有 console API 重寫為新版邏輯。
  //
  // 設計:
  //   - 查詢:_adminPeekWbDailyByEmail(email) → 回傳今日已上榜場次 + 含哪些 teamKey
  //   - 重置:_adminResetWbDailyByEmail(email)
  //           → 找出所有「含該 uid 的 teamKey」整筆從 leaderboard 砍掉
  //             (連帶其他隊友的歷史也會清,因為老師選 A:整筆 teamKey 砍掉)
  //           → 同時砍掉「該 uid 的傷害紀錄」,讓他重新打更公平
  //   - 自救:_myResetWbDailyToday() → 自己重置自己(老師也能用)
  //
  // 注意:這些 API 不需「YES」確認字串,因為「重置今日次數」是低風險操作 —
  //   最差就是學生多打 2 場,不會傷到任何資料。
  // ════════════════════════════════════════════════════════════════════

  // ★ v3.12.9(2026-05-30) — 改用 players/{uid}.wbDailyCount 計次
  //   舊版 v3.10.13 是「讀排行榜 battleHistory」,但這有 race condition
  //   新版老師需求:玩家自己的計數器,進場 +1,結算才扣,排行榜獨立保留
  //
  // 取得指定 uid 今日世界 BOSS 場次
  window._fbGetWbDailyByUid = async function(uid){
    if(!uid){ console.warn('[_fbGetWbDailyByUid] uid 為空'); return null; }
    try{
      // 先讀玩家計數器
      const _pSnap = await getDoc(doc(_fbDb, 'players', uid));
      const _pData = _pSnap.exists() ? (_pSnap.data() || {}) : {};
      const _email = ((_pData.email || '') + '').toLowerCase();
      const _wbDc = _pData.wbDailyCount || {};
      const _todayStr = (window._wbDailyLimit && typeof window._wbDailyLimit._todayStr === 'function')
        ? window._wbDailyLimit._todayStr()
        : (function(){
            const _d = new Date();
            if(_d.getHours() < 8) _d.setDate(_d.getDate() - 1);
            return _d.getFullYear() + '-' + String(_d.getMonth()+1).padStart(2,'0') + '-' + String(_d.getDate()).padStart(2,'0');
          })();

      // 若 wbDailyCount.dateStr === today,該欄位有效;否則視為 0
      const _countToday = (_wbDc.dateStr === _todayStr) ? (Number(_wbDc.count) || 0) : 0;

      // 同時讀「今日 leaderboard 上榜紀錄」(輔助資訊,不影響計次)
      const _BOSS_ID = (typeof window._wbGetCurrentBossId === 'function') ? window._wbGetCurrentBossId() : 'vesuvius_fire_dragon';  // ★ v3.14.20 動態當前龍王
      let _todayBattles = [];
      let _todayDmgSum = 0;
      const _myTeamKeys = [];
      try{
        const _statsRef = doc(_fbDb, 'stats', 'global');
        const _statsSnap = await getDoc(_statsRef);
        const _statsData = _statsSnap.exists() ? (_statsSnap.data() || {}) : {};
        const _lbMap = _statsData.worldBossLeaderboard || {};
        const _list = Array.isArray(_lbMap[_BOSS_ID]) ? _lbMap[_BOSS_ID] : [];
        const _dateStrOf = (window._wbDailyLimit && typeof window._wbDailyLimit._dateStrOf === 'function')
          ? window._wbDailyLimit._dateStrOf
          : function(ts){
              if(!ts || typeof ts !== 'number') return '';
              const _d = new Date(ts);
              if(_d.getHours() < 8) _d.setDate(_d.getDate() - 1);
              return _d.getFullYear() + '-' + String(_d.getMonth()+1).padStart(2,'0') + '-' + String(_d.getDate()).padStart(2,'0');
            };
        for(const _entry of _list){
          if(!_entry || !_entry.teamKey) continue;
          const _uids = String(_entry.teamKey).split('|');
          if(!_uids.includes(uid)) continue;
          _myTeamKeys.push(_entry.teamKey);
          const _hist = Array.isArray(_entry.battleHistory) ? _entry.battleHistory : [];
          for(const _b of _hist){
            if(_b && _b.at && _dateStrOf(_b.at) === _todayStr){
              _todayBattles.push({ at: _b.at, dmg: _b.dmg || 0, teamKey: _entry.teamKey, mvp: _b.mvp || null });
              _todayDmgSum += (_b.dmg || 0);
            }
          }
        }
      }catch(_eRd){ console.warn('[_fbGetWbDailyByUid] 讀排行榜輔助資訊失敗', _eRd); }

      console.log('[_fbGetWbDailyByUid v3.12.9] uid=' + uid + ' email=' + _email +
                  ' wbDailyCount=' + _countToday + '/2 (' + (_wbDc.dateStr || 'N/A') + ')' +
                  ' 含我的隊伍 ' + _myTeamKeys.length + ' 支,leaderboard 今日上榜 ' + _todayBattles.length + ' 場,總傷 ' + _todayDmgSum);
      return {
        uid: uid,
        email: _email,
        todayStr: _todayStr,
        countToday: _countToday,                  // ★ v3.12.9 — 計次以這個為準(權威來源)
        wbDailyCount: _wbDc,                       // 原始計數器資料(debug 用)
        // 以下是輔助資訊(從排行榜算的,僅供老師參考)
        leaderboardTodayBattles: _todayBattles,
        leaderboardTodayDmgSum: _todayDmgSum,
        myTeamKeys: _myTeamKeys,
      };
    }catch(e){
      console.error('[_fbGetWbDailyByUid] 讀取失敗:', e);
      return { uid: uid, error: e && e.message };
    }
  };

  // 重置指定 uid 的今日世界 BOSS 計次
  //   ★ v3.12.9 改版:只清 players/{uid}.wbDailyCount,不動 leaderboard
  //   (老師需求:排行榜延續到 BOSS 倒下才重置,救援個別學生不該砍排行榜)
  window._fbResetWbDailyByUid = async function(uid){
    if(!uid){ console.warn('[_fbResetWbDailyByUid] uid 為空'); return false; }
    try{
      await setDoc(doc(_fbDb, 'players', uid), { wbDailyCount: deleteField() }, { merge: true });
      console.log('[_fbResetWbDailyByUid v3.12.9] ✅ 已清除 uid=' + uid + ' 的 wbDailyCount(排行榜保留)');
      return true;
    }catch(e){
      console.error('[_fbResetWbDailyByUid] 重置失敗:', e);
      return false;
    }
  };

  // ★ v3.12.10(2026-05-30) — GM 寫世界 BOSS 異常記錄補償通知到指定玩家
  //   觸發時機:GM 從後台「世界 BOSS 排行榜明細」勾選異常紀錄刪除時,對隊伍中
  //              每位玩家寫此欄位,玩家下次登入會看到「補償通知視窗」。
  //
  //   資料結構:players/{uid}.wbAbnormalRemoval = {
  //     reason: string,           // GM 填寫的原因(玩家看得到)
  //     boss: string,             // BOSS 名稱
  //     removedAt: number,        // GM 刪除時間
  //     acknowledgedAt?: number,  // 玩家按「我知道了」時間(由玩家自己寫)
  //   }
  //
  //   ★ 跨玩家寫入(GM 寫別人的 players/{uid})— Firestore Rules 需開放:
  //     - GM(_isAdmin)可以寫任何玩家
  //     - 一般玩家只能寫自己的 acknowledgedAt
  //   (參考 weeklyQuizPendingAward 的跨玩家寫入模式,鐵律 1.55)
  window._adminWriteWbCompensation = async function(uid, payload){
    if(!uid){ console.warn('[_adminWriteWbCompensation] uid 為空'); throw new Error('uid 為空'); }
    if(!payload){ payload = {}; }
    try{
      const _data = {
        reason:    String(payload.reason || '(GM 未填寫原因)').slice(0, 200),
        boss:      String(payload.boss || '火山炎龍王').slice(0, 60),
        removedAt: (typeof payload.removedAt === 'number') ? payload.removedAt : Date.now(),
        // ★ 注意:不寫 acknowledgedAt,由玩家自己按「我知道了」時寫
      };
      await setDoc(doc(_fbDb, 'players', uid), { wbAbnormalRemoval: _data }, { merge: true });
      console.log('[_adminWriteWbCompensation v3.12.10] ✅ uid=' + uid + ' 補償通知已寫入,reason=' + _data.reason);
      return true;
    }catch(e){
      console.error('[_adminWriteWbCompensation] 寫入失敗 uid=' + uid + ':', e);
      throw e;
    }
  };

  // ★ v3.12.10 — 玩家自己標記補償通知為「已讀」
  //   觸發時機:玩家點補償通知視窗的「我知道了」按鈕
  //   只寫 acknowledgedAt 一個欄位,Firestore Rules 限制玩家只能寫這個欄位
  window._wbConsumeAbnormalRemoval = async function(){
    const _uid = window._gUserId;
    if(!_uid){ console.warn('[_wbConsumeAbnormalRemoval] 未登入'); return false; }
    try{
      // 用 dot-notation 確保只動 acknowledgedAt 一個欄位(Rules 守門需要)
      await setDoc(doc(_fbDb, 'players', _uid), {
        wbAbnormalRemoval: { acknowledgedAt: Date.now() }
      }, { merge: true });
      console.log('[_wbConsumeAbnormalRemoval v3.12.10] ✅ 已標記為已讀');
      return true;
    }catch(e){
      console.error('[_wbConsumeAbnormalRemoval] 標記已讀失敗:', e);
      return false;
    }
  };

  // 用 email 直接查並重置(老師最常用)
  window._adminPeekWbDailyByEmail = async function(email){
    const _player = await window._fbFindByEmail(email);
    if(!_player){
      console.warn('⛔ 找不到 email=' + email);
      return null;
    }
    return await window._fbGetWbDailyByUid(_player.uid);
  };

  window._adminResetWbDailyByEmail = async function(email){
    const _player = await window._fbFindByEmail(email);
    if(!_player){
      console.warn('⛔ 找不到 email=' + email);
      return false;
    }
    const _ok = await window._fbResetWbDailyByUid(_player.uid);
    if(_ok){
      console.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
      console.log('✅ 已重置 ' + email + ' 的世界 BOSS 今日次數');
      console.log('   uid=' + _player.uid);
      console.log('   ⚠ 排行榜紀錄保留(老師需求:延續到 BOSS 倒下)');
      console.log('   學生請「重新整理」即可再開始打世界 BOSS');
      console.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
    }
    return _ok;
  };

  // 自己用版(老師自己 / 學生卡死時自救)
  window._myResetWbDailyToday = async function(){
    if(!window._gUserId){ console.warn('⛔ 尚未登入'); return false; }
    const _ok = await window._fbResetWbDailyByUid(window._gUserId);
    if(_ok){
      // 順手清掉 v3.9.1 殘留的舊 localStorage key(向後相容)
      try{ localStorage.removeItem('lxps_wb_daily_entries_v1'); }catch(_){}
      console.log('✅ 已重置「你自己」的世界 BOSS 今日次數,可以再打了');
    }
    return _ok;
  };

  // ★ v3.11.8(2026-05-28) — 管理員 API 使用說明:只在管理員身份登入後才印
  //   原問題:line 1538-1541 啟動時無條件 console.log 印出 4 條訊息暴露 _adminPeek*/_adminReset* API 名稱
  //          學生 F12 → Console 一打開就看到「管理員工具已掛載」+ 完整指令名稱
  //   修補:用 onAuth 完成後 + _isAdminUser() 守門,只給管理員看
  //   API 函式本身還是會掛 window(才能用),但 console 提示只給管理員
  function _printAdminWbDailyHelp(){
    try{
      if(typeof window._isAdminUser === 'function' && window._isAdminUser()){
        console.log('[v3.10.13] ✅ 世界 BOSS 次數查詢/重置工具已掛載(讀排行榜判定版):');
        console.log('   _adminPeekWbDailyByEmail("xxx@email")   — 查詢學生今日次數(不改)');
        console.log('   _adminResetWbDailyByEmail("xxx@email")  — 重置學生今日次數(砍 teamKey)');
        console.log('   _myResetWbDailyToday()                  — 自己重置(老師/學生自救)');
      }
    }catch(_){}
  }
  // 啟動時若管理員已登入,立即印;否則用 onAuth hook 在登入完成後印
  _printAdminWbDailyHelp();
  // 額外 hook:若 _fbUser 還沒就緒,等 1.5 秒後再試一次(onAuth 通常在 1 秒內完成)
  setTimeout(_printAdminWbDailyHelp, 1500);
  setTimeout(_printAdminWbDailyHelp, 5000);  // 慢網路保險


  // ★ v1.0.20260424.1150 — 邀請碼系統(取代 email 查詢)
  //   設計:每個玩家擁有 8 碼邀請碼,存在 inviteCodes/{code} → { uid:'xxx' }
  //   加好友流程:輸入邀請碼 → getDoc(inviteCodes/{code}).uid → getDoc(players/{uid})
  //   全程都是 getDoc,不會觸發 Firestore where 查詢的 permission-denied 問題

  // 產生 8 碼邀請碼(大寫英數字,排除易混淆字元 0/O/1/I/L)
  function _genInviteCode(){
    const chars = 'ABCDEFGHJKMNPQRSTUVWXYZ23456789';
    let code = '';
    for(let i=0; i<8; i++) code += chars.charAt(Math.floor(Math.random()*chars.length));
    return code;
  }

  // ════════════════════════════════════════════════════════════════════
  // ★★ v5.136.0(2026-09-03・老師回報「有人正確輸入朋友的好友代碼,但一直無法成功登錄」)★★
  //   實查出**兩個各自獨立**的根因,本輪一起根治。
  //
  //   【根因①・幽靈邀請碼 —— 主因】
  //     舊版三條錯誤路徑,全都是「寫入雲端失敗 → 照樣把代碼交給玩家顯示」:
  //       (a) 最外層 catch:開頭 getDoc(players/{uid}) 一拋錯就 `return _uidCode`,
  //           **一個字都沒寫進 inviteCodes 反查表**。
  //       (b) 中段 catch(eWrite):setDoc 失敗也照樣 return _uidCode。
  //       (c) 已有 myCode 的分支:補寫反查表包在 `catch(_){}` 裡靜默吞掉,沒補成也回傳。
  //     ⇒ 學生畫面上看到漂漂亮亮的 8 碼、抄給朋友,朋友輸入 → codeDoc.exists() 為 false
  //       → 「找不到邀請碼」,而且**永遠找不到**,直到那位學生自己某次登入剛好寫成功。
  //     觸發條件在本專案非常常見:校網壅塞、Firestore 配額耗盡(v5.11.0 就有 resource-exhausted 前科)。
  //     ★ 修法:**反查表確定寫進去了才把代碼交出去**;沒寫成功一律回 null,
  //       由 UI 顯示「代碼還沒同步,請重新整理」——寧可暫時沒代碼,也絕不能給一個查不到的假碼。
  //
  //   【根因②・0/O 與 1/I/L 抄錯】
  //     下方 _genInviteCode() 的字集刻意排除了 0/O/1/I/L,註解也寫明原因 ——
  //     **但那支函式全檔從未被呼叫**。實際發出去的是 _uidCode(Firebase uid 前 8 碼轉大寫),
  //     而 uid 是 [A-Za-z0-9] 任意組合 ⇒ 0、O、1、I、L(小寫 l 轉大寫)全都會出現。
  //     學生看螢幕抄給同學,把 O 抄成 0 → 查無此碼,而他本人確信自己抄對了。完全吻合老師的描述。
  //     ★ 修法:**不換掉任何人現有的代碼**(換了 = 已分享出去的全部失效),
  //       改為額外登記一份「正規化別名」文件:把 O→0、I→1、L→1 之後的字串也指向同一個 uid。
  //       查詢時先查原碼、查不到再查別名 ⇒ 抄錯這五個字也照樣找得到人。
  //     ⚠ 只正規化 0/O/1/I/L 這五個(= _genInviteCode 原始設計認定的易混淆集合),
  //       刻意不擴大到 S/5、B/8、Z/2 —— 每多一組,不同學生的別名相撞的機率就上升一階,
  //       而撞到就是「加到別人」這種比查不到嚴重得多的錯。
  // ════════════════════════════════════════════════════════════════════

  // 易混淆字元正規化(單一真相:產生端與查詢端共用,兩邊口徑不可能分岔)
  window._lxpsInviteNormalize = function(code){
    return (code || '').toUpperCase().trim()
      .replace(/O/g, '0')      // 英文 O → 數字 0
      .replace(/[IL]/g, '1');  // 英文 I / L → 數字 1
  };

  // 寫入(或補寫)一筆別名文件。
  // ⚠ 別名已存在且屬於別人時**絕不覆寫** —— 覆寫會把別人的代碼搶過來,
  //   後果是「所有輸入那個碼的人都加到我」,比查不到嚴重得多。查無此人時靜默略過即可。
  async function _inviteWriteAlias(uid, canonCode){
    const _alias = window._lxpsInviteNormalize(canonCode);
    if(!_alias || _alias === canonCode) return;   // 原碼裡沒有易混淆字元 → 不需要別名
    try{
      const _aDoc = await getDoc(doc(_fbDb, 'inviteCodes', _alias));
      if(_aDoc.exists()){
        const _owner = _aDoc.data() && _aDoc.data().uid;
        if(_owner && _owner !== uid){
          console.warn('[InviteCode] 別名 ' + _alias + ' 已屬於其他玩家,略過(不覆寫)');
          return;
        }
      }
      await setDoc(doc(_fbDb, 'inviteCodes', _alias),
        { uid: uid, ts: Date.now(), alias: true, of: canonCode }, { merge: true });
      console.log('[InviteCode] 已登記易混淆別名:', canonCode, '→', _alias);
    }catch(eA){ console.warn('[InviteCode] 別名寫入失敗(不影響原碼可用)', eA); }
  }

  // 確保自己有邀請碼:登入時呼叫。
  // ★ v5.136.0 回傳語義變更:**只有反查表確實存在時才回傳代碼**,否則回 null。
  //   呼叫端(好友面板)看到 null 就顯示「尚未同步」提示,不再把假碼給學生。
  window._fbEnsureInviteCode = async function(uid){
    if(!uid) return null;
    const _uidCode = (uid + '').replace(/[^a-zA-Z0-9]/g, '').toUpperCase().substring(0, 8).padEnd(8, 'X');
    try{
      // 1. 從自己的 player doc 讀取 inviteCode(舊玩家可能是更早年代產的隨機碼,一律沿用不換)
      const myDoc = await getDoc(doc(_fbDb, 'players', uid));
      let myCode = (myDoc.exists() && myDoc.data().inviteCode) ? myDoc.data().inviteCode : null;

      if(myCode){
        // 1a. 確保反查表存在(舊資料可能只寫在 player doc,或當年反查表那次寫失敗了)
        //     ★ v5.136.0:這裡的失敗**不再靜默吞掉** —— 補寫不成就回 null,
        //       因為此時這個代碼對朋友來說根本查不到,顯示出來只會害人白抄一場。
        try{
          const codeDoc = await getDoc(doc(_fbDb, 'inviteCodes', myCode));
          if(!codeDoc.exists()){
            await setDoc(doc(_fbDb, 'inviteCodes', myCode), { uid: uid, ts: Date.now() });
            console.log('[InviteCode] 反查表缺漏,已補寫:', myCode);
          }
        }catch(eFix){
          console.error('[InviteCode] 反查表補寫失敗 → 本次不提供代碼(避免給出查不到的幽靈碼)', eFix);
          return null;   // ★ v5.136.0（舊:catch(_){} 靜默吞掉後照樣 return myCode）
        }
        await _inviteWriteAlias(uid, myCode);   // ★ v5.136.0 別名(失敗不影響原碼)
        return myCode;
      }

      // 2. 還沒有邀請碼 → 用 uid 推導出確定性代碼並寫入
      //    ★ v5.136.0:兩個 setDoc 的失敗一律往外拋到下面的 catch,不再「寫失敗照樣回傳」。
      //    ★ 順序刻意是「先寫反查表、再寫 player doc」:
      //      反查表才是朋友查得到的關鍵;先寫它,萬一 player doc 那步失敗,
      //      下次登入會用同一組 _uidCode 重跑(uid 推導是確定性的,不會產生第二個碼)。
      await setDoc(doc(_fbDb, 'inviteCodes', _uidCode), { uid: uid, ts: Date.now() });
      await setDoc(doc(_fbDb, 'players', uid), { inviteCode: _uidCode }, { merge: true });
      console.log('[InviteCode] 已寫入新邀請碼(uid-based):', _uidCode);
      await _inviteWriteAlias(uid, _uidCode);
      return _uidCode;

    }catch(e){
      // ★★ v5.136.0 根因① 的修補點 ★★
      //   舊碼在這裡 `return _uidCode` —— 也就是「雲端一個字都沒寫成功,還是把代碼給玩家看」。
      //   那正是老師回報「正確輸入卻一直無法成功登錄」的來源。改為回 null,由 UI 誠實告知。
      console.error('[InviteCode] 取得/建立邀請碼失敗 → 本次不提供代碼(舊版會在這裡回傳查不到的幽靈碼)', e);
      return null;
    }
  };

  // 用邀請碼查找玩家(取代 _fbFindByEmail)
  window._fbFindByInviteCode = async function(code){
    const _codeNorm = (code || '').toUpperCase().trim();
    if(!_codeNorm){
      console.warn('[_fbFindByInviteCode] 邀請碼為空');
      return null;
    }
    try{
      // 1. 從反查表 inviteCodes/{code} 取得 uid
      let codeDoc = await getDoc(doc(_fbDb, 'inviteCodes', _codeNorm));

      // ★★ v5.136.0 — 原碼查不到時,改查「易混淆正規化別名」★★
      //   老師回報的第二個根因:代碼裡有 0/O/1/I/L,學生看螢幕手抄給同學就抄錯,
      //   本人卻確信自己抄對了。這裡把輸入也做同一套正規化再查一次別名文件
      //   (別名由 _fbEnsureInviteCode 在對方登入時登記),⇒ 抄錯這五個字仍找得到人。
      //   ⚠ 只多花一次 getDoc,而且只在第一次查失敗時才發生 ⇒ 對配額幾乎沒有影響。
      //   ⚠ 別名本身也可能剛好等於原碼(代碼裡沒有易混淆字元)→ 跳過不重查。
      if(!codeDoc.exists() && typeof window._lxpsInviteNormalize === 'function'){
        const _alias = window._lxpsInviteNormalize(_codeNorm);
        if(_alias && _alias !== _codeNorm){
          try{
            const _aDoc = await getDoc(doc(_fbDb, 'inviteCodes', _alias));
            if(_aDoc.exists()){
              console.log('[_fbFindByInviteCode] 原碼查無,改由易混淆別名命中:', _codeNorm, '→', _alias);
              codeDoc = _aDoc;
            }
          }catch(eAl){ console.warn('[_fbFindByInviteCode] 別名查詢失敗(不影響原碼結果)', eAl); }
        }
      }

      if(!codeDoc.exists()){
        console.log('[_fbFindByInviteCode] 找不到邀請碼:', _codeNorm);
        return null;
      }
      const data = codeDoc.data();
      const targetUid = data.uid;
      if(!targetUid){
        console.warn('[_fbFindByInviteCode] inviteCodes 文件缺 uid 欄位:', _codeNorm);
        return null;
      }
      // 2. 用 uid 讀取玩家資料
      const playerDoc = await getDoc(doc(_fbDb, 'players', targetUid));
      if(!playerDoc.exists()){
        console.warn('[_fbFindByInviteCode] 邀請碼對應的玩家不存在:', targetUid);
        return null;
      }
      console.log('[_fbFindByInviteCode] 找到玩家:', _codeNorm, '→ uid:', targetUid);
      return { uid: targetUid, ...playerDoc.data() };
    }catch(e){
      console.error('[_fbFindByInviteCode] 查詢失敗:', _codeNorm, e);
      const _eMsg = e && (e.code || e.message) ? (e.code || e.message) : '網路錯誤';
      const _newErr = new Error('查詢失敗: ' + _eMsg);
      _newErr.code = e && e.code;
      _newErr.original = e;
      throw _newErr;
    }
  };

  // 讀取好友完整資料
  window._fbLoadFriend = async function(uid) {
    try {
      const snap = await getDoc(doc(_fbDb, 'players', uid));
      return snap.exists() ? { uid: snap.id, ...snap.data() } : null;
    } catch(e) { console.error('讀取好友資料失敗', e); return null; }
  };

  // 加好友（存在自己的資料中）
  window._fbAddFriend = async function(myUid, friendInfo) {
    try {
      await setDoc(doc(_fbDb, 'players', myUid), {
        friends: arrayUnion({ uid: friendInfo.uid, email: friendInfo.email, name: friendInfo.name || friendInfo.email })
      }, { merge: true });
      return true;
    } catch(e) { console.error('加好友失敗', e); return false; }
  };

  // 刪好友
  window._fbRemoveFriend = async function(myUid, friendInfo) {
    try {
      await setDoc(doc(_fbDb, 'players', myUid), {
        friends: arrayRemove({ uid: friendInfo.uid, email: friendInfo.email, name: friendInfo.name || friendInfo.email })
      }, { merge: true });
      return true;
    } catch(e) { console.error('刪好友失敗', e); return false; }
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.12.0(2026-05-29)— 好友送禮 Firebase API
  //
  // 資料結構:players/{recipientUid}/pendingGifts/{auto-id}
  //   { from, fromName, itemId, deliver, ts }
  //
  // Firestore Rules 必須開放(老師到 Firebase Console 設定):
  //   match /players/{uid}/pendingGifts/{giftId} {
  //     // 任何登入者(非 uid 本人)可以「create」一筆 pendingGift,但只能寫單一禮物
  //     allow create: if request.auth != null
  //                   && request.auth.uid != uid
  //                   && request.resource.data.from == request.auth.uid
  //                   && request.resource.data.keys().hasOnly(['from','fromName','itemId','deliver','ts']);
  //     // 接收者本人可以讀/刪自己的 pendingGift
  //     allow read, delete: if request.auth != null && request.auth.uid == uid;
  //   }
  // ════════════════════════════════════════════════════════════════════

  // 寫一筆禮物到對方的 pendingGifts(addDoc 自動產生文件 id)
  window._fbSendGift = async function(recipientUid, giftPayload){
    if(!recipientUid || !giftPayload) return false;
    // ★ v3.12.0(2026-05-29)BUG #19 — 前置守門:from 必須是有效 uid(配 Rules)
    //   若 from 為空字串/null/undefined → Rules 會拒絕(因為 != auth.uid)
    //   →這時直接 return false 由送禮端走「自動退款」路徑,顯示明確錯誤
    if(!giftPayload.from || typeof giftPayload.from !== 'string' || giftPayload.from.length < 10){
      console.error('[v3.12.0 _fbSendGift] 拒絕:from 無效',
                    {from: giftPayload.from, recipientUid});
      return false;
    }
    try{
      const _giftsCol = collection(_fbDb, 'players', recipientUid, 'pendingGifts');
      // ★ v3.13.80 防重複送禮:改用「送禮者uid_日期」固定 docId(原本是隨機 id → 伺服器零去重)
      //   同一送禮者「同一天」送同一收禮者 → 同一 docId:第二次寫=update →
      //   Rules(allow update: if isAdmin)擋下 → setDoc 丟例外 → 回 false,由送禮端自動退款。
      //   跨天 dateKey 改變 → 不同 docId → 正常可再送(每日 8:00 重置)。
      //   (送禮者無法讀收禮者 pendingGifts,故不預查;直接靠 update 被拒達成去重。)
      let _newRef;
      try{
        const _dk = (typeof window._giftGetTodayKey === 'function') ? window._giftGetTodayKey() : '';
        _newRef = (_dk && giftPayload.from) ? doc(_giftsCol, giftPayload.from + '_' + _dk) : doc(_giftsCol);
      }catch(_eId){ _newRef = doc(_giftsCol); }
      // payload 要精準對應 Rules 守門(hasOnly 5 欄位)
      // ★ v3.12.2(2026-05-30) — deliver fallback 從 null 改 {},
      //   因為 Rules 守門要求 deliver is map(map 型別),null 會被擋
      const _doc = {
        from: giftPayload.from,  // ★ v3.12.0:守門已確認 from 有效,不再 fallback ''
        fromName: giftPayload.fromName || '',
        itemId: giftPayload.itemId || '',
        deliver: giftPayload.deliver || {},  // ★ v3.12.2:從 null 改 {}
        ts: giftPayload.ts || Date.now(),
      };
      // ★ v3.14.4 — 勉勵話索引(老師需求 3)。僅 0-4 有效時才帶(Rules 非必填守門,舊版相容)
      if(typeof giftPayload.msgIdx === 'number'
         && giftPayload.msgIdx >= 0 && giftPayload.msgIdx <= 4){
        _doc.msgIdx = Math.floor(giftPayload.msgIdx);
      }
      await setDoc(_newRef, _doc);
      console.log('[v3.12.0 _fbSendGift] ✅ 已送出禮物 to ' + recipientUid + ', itemId=' + _doc.itemId);
      return true;
    }catch(e){
      console.error('[v3.12.0 _fbSendGift] 失敗(可能重複送禮被擋,或 Rules 未配置):', e);
      return false;
    }
  };

  // 讀取自己的 pendingGifts(onAuth hook 觸發時呼叫)
  window._fbReadPendingGifts = async function(myUid){
    if(!myUid) return [];
    try{
      const _giftsCol = collection(_fbDb, 'players', myUid, 'pendingGifts');
      const _snap = await getDocs(_giftsCol);
      const _gifts = [];
      _snap.forEach(function(d){
        const _data = d.data();
        _gifts.push(Object.assign({ docId: d.id }, _data));
      });
      return _gifts;
    }catch(e){
      console.warn('[v3.12.0 _fbReadPendingGifts]', e);
      return [];
    }
  };

  // 刪掉已領取的 pendingGift
  window._fbDeletePendingGift = async function(myUid, docId){
    if(!myUid || !docId) return false;
    try{
      await deleteDoc(doc(_fbDb, 'players', myUid, 'pendingGifts', docId));
      return true;
    }catch(e){
      console.warn('[v3.12.0 _fbDeletePendingGift]', e);
      return false;
    }
  };


  // ══ 玩家人數統計系統（總玩家數 + 今日上線 + 本週新增玩家 + 累計冒險次數）══
  // 使用台灣時區（UTC+8）做日期 key
  // ★ v3.13.12(2026-06-01) 鐵律 1.142 — 全遊戲「每日重置」統一邏輯
  //   設計理念:學生「到學校開始玩就是新的一天」(早上 8:00 為跨日基準)。
  //   過往各系統各自實作(_getTodayKeyTW、_kingGetTodayStr、_previewTodayStr、
  //   _getShopDailyKey、_getTaiwanDateStr...)有些不同步、有些靠 UTC 湊巧對齊,
  //   現一律改用 _lxpsGetGameDayKey() 為單一真相來源。
  //   - 強制 Asia/Taipei 時區(避免裝置時區干擾跨時區同學)
  //   - 8:00 為跨日基準(< 08:00 視為昨天)
  //   - 回傳格式:'YYYY-MM-DD'
  function _lxpsGetGameDayKey(){
    try{
      // 用 toLocaleString 取出台灣時區的「年月日時」,避免裝置時區干擾
      const _now = new Date();
      // 用 en-CA(YYYY-MM-DD) 取台灣日期 + sv-SE(24h HH:MM:SS) 取台灣小時
      const _dateStr = _now.toLocaleDateString('en-CA', { timeZone: 'Asia/Taipei' });
      const _timeStr = _now.toLocaleTimeString('en-GB', { timeZone: 'Asia/Taipei', hour12: false });
      const _h = parseInt(_timeStr.split(':')[0], 10);
      if(!isFinite(_h)) return _dateStr;
      // < 8 算昨天
      if(_h < 8){
        // 把日期字串轉成 Date 再 -1 天,再轉回字串
        const [_y, _m, _d] = _dateStr.split('-').map(s => parseInt(s, 10));
        // 用 Date.UTC 避免本地時區干擾,只做純粹的日期計算
        const _dt = new Date(Date.UTC(_y, _m - 1, _d));
        _dt.setUTCDate(_dt.getUTCDate() - 1);
        const _yy = _dt.getUTCFullYear();
        const _mm = String(_dt.getUTCMonth() + 1).padStart(2, '0');
        const _dd = String(_dt.getUTCDate()).padStart(2, '0');
        return _yy + '-' + _mm + '-' + _dd;
      }
      return _dateStr;
    }catch(e){
      // fallback:用裸 Date(回到舊版湊巧的 UTC 邏輯)
      try{ return new Date().toISOString().slice(0,10); }catch(_){ return ''; }
    }
  }
  // 全域曝光,讓所有檔案/函式都能用
  try{ window._lxpsGetGameDayKey = _lxpsGetGameDayKey; }catch(_){}

  function _getTodayKeyTW() {
    // ★ v3.13.12 — 對齊鐵律 1.142,改用統一函式(原本是台灣 00:00,現改為 8:00)
    try{ return _lxpsGetGameDayKey(); }catch(_){}
    // fallback (舊邏輯)
    try {
      return new Date().toLocaleDateString('en-CA', { timeZone: 'Asia/Taipei' });
    } catch(e) {
      const d = new Date(Date.now() + 8*60*60*1000);
      return d.toISOString().slice(0, 10);
    }
  }

  // 取得台灣時區「本週週一」的日期字串（YYYY-MM-DD），作為本週 key
  function _getThisWeekMondayKeyTW() {
    // Date.now() 是 UTC 毫秒；+8h 後當作台灣時間來計算週一
    const twMs = Date.now() + 8*3600*1000;
    const tw = new Date(twMs);
    // day: 0=Sun, 1=Mon, ..., 6=Sat（getUTCDay 避開本機時區干擾）
    const day = tw.getUTCDay();
    const mondayOffset = (day === 0) ? -6 : (1 - day);
    const monday = new Date(twMs + mondayOffset * 86400000);
    return monday.toISOString().slice(0, 10);
  }

  // 登入時觸發：
  //  - 若玩家尚未被計入總玩家數（_countedInStats 為 false 或不存在）→ totalPlayers+1 並標記
  //  - 若為計數系統上線後的全新玩家 → 額外 newPlayers_本週+1
  //  - 今日首次上線 → active_今日+1
  // ★ 這種設計讓「新計數系統上線前就存在的舊玩家」在下次登入時自動被補計入，
  //   不需要管理員手動回填。
  window._fbTrackPlayerStats = async function(uid) {
    if(!uid) return;
    try {
      const todayKey = _getTodayKeyTW();
      const weekKey  = _getThisWeekMondayKeyTW();
      const playerRef = doc(_fbDb, 'players', uid);
      // ★ v5.152.0 — 登入流程已在閘門讀過同一份 players/uid（window._lxpsLoginPlayerDoc），60 秒內直接重用，
      //   省一次讀取；沒有快取或已過期才走原本的 getDoc。本函式現在在登入後延後幾秒才跑（不再 await），
      //   quota 與熱門文件 stats/global 的寫入壓力由呼叫端抖動錯開。
      let existed, data;
      const _lc = window._lxpsLoginPlayerDoc;
      if(_lc && _lc.uid === uid && (Date.now() - (_lc.at || 0)) < 60000){
        existed = !!_lc.existed;
        data = _lc.data || {};
      } else {
        const playerSnap = await getDoc(playerRef);
        existed = playerSnap.exists();
        data = existed ? (playerSnap.data() || {}) : {};
      }
      // 未被計入過的玩家（舊玩家首次回歸也會被標記）
      const needsCounting = !data._countedInStats;
      // 是否是真正的全新玩家（文件根本不存在）
      const isBrandNewPlayer = !existed;
      const lastActiveDate = data.lastActiveDate || '';
      const isFirstLoginToday = lastActiveDate !== todayKey;

      // ── 更新全域統計 ──
      const statsUpdates = {};
      if(needsCounting) statsUpdates.totalPlayers = increment(1);
      // newPlayers_週一 只算真正的全新玩家（避免舊玩家回歸也算新增）
      if(isBrandNewPlayer) statsUpdates['newPlayers_' + weekKey] = increment(1);
      if(isFirstLoginToday) statsUpdates['active_' + todayKey] = increment(1);
      if(Object.keys(statsUpdates).length > 0) {
        const statsRef = doc(_fbDb, 'stats', 'global');
        await setDoc(statsRef, statsUpdates, { merge: true });
      }

      // ── 更新玩家文件上的標記（確保同一玩家不會被重覆計算）──
      const playerUpdates = {};
      if(needsCounting) playerUpdates._countedInStats = true;
      if(isFirstLoginToday) playerUpdates.lastActiveDate = todayKey;
      if(Object.keys(playerUpdates).length > 0) {
        await setDoc(playerRef, playerUpdates, { merge: true });
      }
      // ★ v3.4.5 — 加 diagnostic log,讓老師從 console 可以直接看「這次登入有沒有 +1」
      //   (排除「同帳號當日重複登入確實不會 +1」的疑慮)
      console.info('📊 [人數統計] '
        + 'totalPlayers' + (needsCounting ? '+1' : '(同人,不+)') + ', '
        + 'newPlayers_' + weekKey + (isBrandNewPlayer ? '+1' : '(非新人)') + ', '
        + 'active_' + todayKey + (isFirstLoginToday ? '+1' : '(今日已上線過)'));
    } catch(e) {
      console.error('❌ [人數統計] 更新失敗 code=' + (e && e.code) + '（可能原因：Firestore 規則未允許寫入 stats 集合）', e);
    }
  };

  // 冒險次數:每次玩家按「整裝出發」啟動一次冒險時呼叫,全域計數 +1
  // ★ 同時在玩家文件累積個人冒險次數(給個人統計用)
  // ★ v3.5.53 — 每小時最多 +1 次(老師核定,防玩家「投降回學校→再進」spam Firebase 寫入)
  //   邏輯:用 localStorage 記錄上次寫雲端的時間戳,1 小時內跳過寫入,只 console.log
  //   原本一場冒險寫一次 → 玩家 1 分鐘可累積 20+ 次寫入,改為「每小時封頂 1 次」
  window._fbIncrementAdventureCount = async function() {
    try {
      // ★ v3.5.53 — 每小時冷卻保護(同帳號 1 小時內不重複寫雲端)
      try{
        const _COOL_MS = 60 * 60 * 1000; // 1 小時
        const _uid2 = window._gUserId || 'guest';
        const _coolKey = '_advCountCool_' + _uid2;
        const _lastTs = parseInt(localStorage.getItem(_coolKey) || '0', 10);
        const _elapsed = Date.now() - _lastTs;
        if(_lastTs > 0 && _elapsed < _COOL_MS){
          const _remainMin = Math.ceil((_COOL_MS - _elapsed) / 60000);
          console.info('📊 [人數統計] 冒險次數冷卻中(' + _remainMin + ' 分鐘後再寫),本次跳過寫雲端');
          return;
        }
        // 通過冷卻 → 記錄本次時間戳,接著寫雲端
        localStorage.setItem(_coolKey, String(Date.now()));
      }catch(_eCool){ console.warn('[v3.5.53 冒險次數冷卻] 例外', _eCool); }

      const uid = window._gUserId;
      const statsRef = doc(_fbDb, 'stats', 'global');
      await setDoc(statsRef, { totalAdventures: increment(1) }, { merge: true });
      if(uid) {
        const playerRef = doc(_fbDb, 'players', uid);
        await setDoc(playerRef, { adventureCount: increment(1) }, { merge: true });
      }
      // ★ v3.4.5 — 成功 log,讓老師確認真的有 +1
      console.info('📊 [人數統計] totalAdventures +1, adventureCount(' + (uid || 'guest').slice(0, 8) + '..) +1 (下次 1 小時後)');
    } catch(e) {
      console.error('❌ [人數統計] 冒險次數 +1 失敗 code=' + (e && e.code), e);
    }
  };

  // ★ 管理員工具：掃描 /players 集合計算歷史總玩家數，覆寫 stats/global.totalPlayers
  //   用於一次性回填舊資料（新計數系統上線前已存在的玩家）
  window._fbBackfillTotalPlayers = async function() {
    try {
      const snap = await getDocs(collection(_fbDb, 'players'));
      const count = snap.size;
      // 用 setDoc 直接覆寫成絕對值（不是 increment）
      const statsRef = doc(_fbDb, 'stats', 'global');
      await setDoc(statsRef, { totalPlayers: count, _backfillAt: Date.now() }, { merge: true });
      console.info('✅ [回填] totalPlayers 已設為 ' + count);
      return count;
    } catch(e) {
      console.error('❌ [回填] 失敗', e);
      throw e;
    }
  };

  // ★ 管理員工具：手動設定 totalAdventures 絕對值（用於回填歷史冒險次數）
  window._fbSetAdventureTotal = async function(newTotal) {
    try {
      const n = Math.max(0, parseInt(newTotal, 10) || 0);
      const statsRef = doc(_fbDb, 'stats', 'global');
      await setDoc(statsRef, { totalAdventures: n }, { merge: true });
      console.info('✅ [管理] totalAdventures 已設為 ' + n);
      return n;
    } catch(e) {
      console.error('❌ [管理] 設定失敗', e);
      throw e;
    }
  };


  // 讀取目前維修狀態
  window._fbGetMaintenance = async function() {
    try {
      const snap = await getDoc(doc(_fbDb, 'stats', 'global'));
      if(!snap.exists()) return { enabled: false };
      const m = snap.data().maintenance;
      return (m && typeof m === 'object') ? m : { enabled: false };
    } catch(e) {
      console.warn('[維修模式] 讀取失敗(網路問題 or 權限),視為關閉', e);
      return { enabled: false };  // 讀取失敗時保守視為關閉,避免全體鎖死
    }
  };

  // 設定維修狀態(管理員用)
  // ★ v1.1.0 — Firestore 配額爆掉時的緊急機制:
  //   1. 自動重試 5 次,每次間隔 5 秒(共 25 秒)
  //   2. 寫入失敗時也寫入 localStorage,確保管理員自己這台電腦至少能切換維修畫面
  //   3. 拋出明確錯誤訊息,引導管理員到 Firebase 控制台手動寫入
  window._fbSetMaintenance = async function(enabled, message, adminEmail) {
    const payload = {
      enabled: !!enabled,
      message: message || '',
      startedAt: Date.now(),
      startedBy: adminEmail || 'unknown'
    };
    // 同步寫入 localStorage,讓管理員的瀏覽器即使 Firestore 失敗也能看到維修畫面
    try {
      if(enabled){
        localStorage.setItem('lxps_maintenance_local', JSON.stringify(payload));
      } else {
        localStorage.removeItem('lxps_maintenance_local');
      }
    } catch(_){}

    // 自動重試 5 次,每次 5 秒(配額重置可能在數分鐘內 — 給足時間)
    let _lastErr = null;
    for(let _i = 0; _i < 5; _i++){
      try {
        await setDoc(doc(_fbDb, 'stats', 'global'), { maintenance: payload }, { merge: true });
        console.info('✅ [維修模式] 已設定為', payload, '(嘗試次數:' + (_i+1) + ')');
        return payload;
      } catch(e) {
        _lastErr = e;
        const _isQuota = e && (e.code === 'resource-exhausted' || (e.message || '').includes('Quota'));
        console.warn('[維修模式] 寫入失敗(第 ' + (_i+1) + ' 次)' + (_isQuota ? ' — Firestore 配額爆掉' : ''), e && e.message);
        if(_i < 4){
          // 等 5 秒再試
          await new Promise(r => setTimeout(r, 5000));
        }
      }
    }
    // 5 次都失敗 → 拋出附帶 Firebase 控制台指引的錯誤
    console.error('❌ [維修模式] 5 次重試都失敗,但 localStorage 已設定(這台電腦的管理員仍可看到維修畫面)');
    console.error('💡 緊急救援:請至 https://console.firebase.google.com/project/lxpsgame/firestore/data/~2Fstats~2Fglobal');
    console.error('💡 在文件 stats/global 中加入欄位 maintenance (map 類型),子欄位:');
    console.error('   enabled: true (boolean)');
    console.error('   message: "' + payload.message + '" (string)');
    console.error('   startedAt: ' + payload.startedAt + ' (number)');
    console.error('   startedBy: "' + payload.startedBy + '" (string)');
    const _quotaErr = new Error('Firestore 寫入失敗(可能是配額爆掉)。請至 Firebase 控制台手動寫入 stats/global 的 maintenance 欄位。詳細指引已寫入 console。');
    _quotaErr.code = (_lastErr && _lastErr.code) || 'unknown';
    _quotaErr.originalError = _lastErr;
    throw _quotaErr;
  };

  // ★ v3.13.60(2026-06-05)— 鬥技場 GM 開關:寫 stats/global 旗標(商店開放 / 排名發獎)
  //   鏡像維修模式的重試寫法;onSnapshot 會把旗標同步到所有客戶端的 _cachedGlobalStats。
  async function _fbSetGlobalFlag(field, value, label){
    let _lastErr = null;
    for(let _i = 0; _i < 5; _i++){
      try{
        const _patch = {}; _patch[field] = value;
        await setDoc(doc(_fbDb, 'stats', 'global'), _patch, { merge: true });
        console.info('✅ [' + label + '] 已設定為', value, '(嘗試次數:' + (_i+1) + ')');
        return value;
      }catch(e){
        _lastErr = e;
        console.warn('[' + label + '] 寫入失敗(第 ' + (_i+1) + ' 次)', e && e.message);
        if(_i < 4) await new Promise(r => setTimeout(r, 5000));
      }
    }
    const _err = new Error('[' + label + '] Firestore 寫入失敗,請至 Firebase 控制台手動寫 stats/global.' + field);
    _err.code = (_lastErr && _lastErr.code) || 'unknown';
    throw _err;
  }
  window._fbSetArenaShopOpen = async function(enabled){
    return _fbSetGlobalFlag('arenaShopOpen', !!enabled, '鬥技場商店開關');
  };
  window._fbSetArenaRankReward = async function(enabled){
    return _fbSetGlobalFlag('arenaRankRewardEnabled', !!enabled, '鬥技場排名發獎開關');
  };
  // GM 後台讀目前旗標值(無設定時:商店預設開、排名預設關)
  window._fbGetArenaFlags = async function(){
    try{
      const snap = await getDoc(doc(_fbDb, 'stats', 'global'));
      const d = snap.exists() ? (snap.data() || {}) : {};
      return {
        arenaShopOpen: (typeof d.arenaShopOpen === 'boolean') ? d.arenaShopOpen : true,
        arenaRankRewardEnabled: (typeof d.arenaRankRewardEnabled === 'boolean') ? d.arenaRankRewardEnabled : false,
      };
    }catch(e){ console.warn('[鬥技場旗標] 讀取失敗', e); return { arenaShopOpen:true, arenaRankRewardEnabled:false, _error:String(e&&e.message||e) }; }
  };

  // ★ v1.1.0 — 客戶端讀取維修狀態時,優先讀 localStorage
  //   這樣即使 Firestore 配額爆掉,管理員自己的瀏覽器也能看到維修畫面測試
  window._fbGetMaintenanceLocal = function(){
    try {
      const raw = localStorage.getItem('lxps_maintenance_local');
      if(!raw) return null;
      return JSON.parse(raw);
    } catch(_){ return null; }
  };

  // ══════════════════════════════════════════════════════════════════
  // ★ v3.15.9 — 伺服器休息 / 開機排程(每日自動休息時段)後端
  //   雲端:gameConfig/restSchedule { enabled, startHHMM, endHHMM, warnMin, restMessage, warnMessage, updatedAt, updatedBy }
  //   GM 後台寫(admin_panel.js,gameConfig = GM-only 寫 / 登入可讀);玩家端只讀,不需改 firestore.rules,無安全漏洞。
  //   讀失敗一律保守視為「停用」,避免全體被誤鎖在休息畫面。與「維修模式」(stats/global.maintenance)各自獨立。
  // ══════════════════════════════════════════════════════════════════
  window._fbGetRestSchedule = async function(){
    try{
      const snap = await getDoc(doc(_fbDb, 'gameConfig', 'restSchedule'));
      if(!snap.exists()) return { enabled: false };
      const d = snap.data();
      return (d && typeof d === 'object') ? d : { enabled: false };
    }catch(e){
      console.warn('[休息排程] 讀取失敗(網路/權限),視為停用', e);
      return { enabled: false };
    }
  };
  window._fbWatchRestSchedule = function(callback){
    try{
      return onSnapshot(doc(_fbDb, 'gameConfig', 'restSchedule'), (snap) => {
        if(!snap.exists()){ callback({ enabled: false }); return; }
        const d = snap.data();
        callback((d && typeof d === 'object') ? d : { enabled: false });
      }, (err) => {
        console.warn('[休息排程] 監聽錯誤', err);
        callback({ enabled: false });
      });
    }catch(e){
      console.error('[休息排程] 啟動監聽失敗', e);
      return function(){};
    }
  };

  // ══════════════════════════════════════════════════════════════════
  // ★ v3.29.0 — 🎓 課堂獎勵加成(GM 開關·gameConfig/classRewardBoost)後端讀取
  //   { enabled, subject, pct, expiresAt, recent[], updatedAt, updatedBy }
  //   GM 後台寫(gameConfig = GM-only 寫 / 登入可讀);玩家端只讀,不需改 firestore.rules。
  //   套用點:_gameBlessingMult 折入(本場冒險科目 _advPlayerSubject == subject 時
  //           結算 EXP / 知識幣 / 掉寶率 +pct%)。讀失敗一律視為 null(=停用)。
  // ══════════════════════════════════════════════════════════════════
  window._fbWatchClassBoost = function(callback){
    try{
      return onSnapshot(doc(_fbDb, 'gameConfig', 'classRewardBoost'), (snap) => {
        callback(snap.exists() ? (snap.data() || null) : null);
      }, (err) => {
        console.warn('[課堂獎勵加成] 監聽錯誤', err);
        callback(null);
      });
    }catch(e){
      console.error('[課堂獎勵加成] 啟動監聽失敗', e);
      return function(){};
    }
  };

  // ★ v3.30.0 — 課堂複習加成 GM 開關監聽(gameConfig/reviewBoost·同 classRewardBoost 規則:玩家讀/GM 寫·免改 firestore.rules)
  //   { baseEnabled:「🎁 課堂複習基礎 +25%(冒險+知識王)」, doubledEnabled:「⭐ 加強複習期間 再+25%」, updatedAt, updatedBy }
  //   讀不到/文件不存在 → callback(null) → 玩家端兩個閘門皆預設回 true = 維持既有行為(向下相容·保守)
  window._fbWatchReviewBoost = function(callback){
    try{
      return onSnapshot(doc(_fbDb, 'gameConfig', 'reviewBoost'), (snap) => {
        callback(snap.exists() ? (snap.data() || null) : null);
      }, (err) => {
        console.warn('[課堂複習加成開關] 監聽錯誤', err);
        callback(null);
      });
    }catch(e){
      console.error('[課堂複習加成開關] 啟動監聽失敗', e);
      return function(){};
    }
  };

  // ★★ v5.116.0(老師需求:「讓 GM 可以勾選是否顯示各單元題庫，或出現在知識王、競技場、龍王戰」)★★
  //   gameConfig/quizVisibility = { hidden:{ adv:[], king:[], arena:[], wb:[] }, updatedAt, updatedBy }
  //   ★ 只存「被關掉的」黑名單 ⇒ 日後新增任何題庫預設一律可見(符合直覺,不必每次上線都回後台補勾)。
  //   ★ docId 不帶 gm_ 前綴 ⇒ 沿用既有 match /gameConfig/{docId} 的「登入者可讀·isAdmin 可寫」條款,
  //     零 firestore.rules 改動(與 arenaSwitch / classRewardBoost / reviewBoost 同款)。
  //   讀不到/文件不存在 → callback(null) → 玩家端謂詞一律回 true = 全部可見 = 維持既有行為。
  window._fbWatchQuizVisibility = function(callback){
    try{
      return onSnapshot(doc(_fbDb, 'gameConfig', 'quizVisibility'), (snap) => {
        callback(snap.exists() ? (snap.data() || null) : null);
      }, (err) => {
        console.warn('[題庫可見性] 監聽錯誤', err);
        callback(null);
      });
    }catch(e){
      console.error('[題庫可見性] 啟動監聽失敗', e);
      return function(){};
    }
  };

  // ══════════════════════════════════════════════════════════════════
  // ★ v3.5.0 — 下載安裝授權管理(管理員批准制)
  // ──────────────────────────────────────────────────────────────────
  // 設計:
  //   1. 玩家「下載安裝完整遊戲」按鈕預設不顯示
  //   2. 玩家可透過「向管理員申請下載權限」按鈕送出申請(寫入 downloadPermissions/{uid})
  //   3. 管理員在後台批准 → status='approved' + grantedAt + expiresAt(24小時後)
  //   4. 玩家畫面 onSnapshot 監聽自己的權限文件,獲批後按鈕自動顯示
  //   5. 24小時後到期 → 自動失效(讀取時驗證 expiresAt)
  //
  // 資料結構: downloadPermissions/{uid}
  //   { uid, email, displayName, status, requestedAt, grantedAt, grantedBy,
  //     expiresAt, revokedAt, revokedBy, note }
  //
  // status 列舉:
  //   - 'pending'  : 玩家已申請,等待管理員批准
  //   - 'approved' : 管理員已批准,在 expiresAt 之前可下載
  //   - 'expired'  : 過期(此狀態由 client 端 lazy 判斷,不主動寫入)
  //   - 'revoked'  : 管理員手動撤銷
  // ══════════════════════════════════════════════════════════════════

  // ★★ v5.143.0（老師裁定 2026-09-05「下載安裝改成自動進行下載免申請」）— 免申請單一開關 ★★
  //   true ⇒ 任何已登入玩家一律視為「已核准、永不過期」：
  //     ・_fbGetDownloadPermission／_fbWatchDownloadPermission 直接回合成的核准物件（零 Firestore 讀取、零寫入）
  //     ・既有所有消費點（下載按鈕顯示／_tryStartFullDownload 守門／_showPwaGuide 守門／維修模式保留登入／
  //       bootPWA 放行三條件／登入後 3.5 秒自動 boot）全部**自動**走「已核准」分支，一行不必改
  //     ・「📩 申請下載權限」鈕因狀態恆為 approved 自然隱藏；到期提醒不排程
  //   false ⇒ 完整退回 v3.5.0 管理員批准制（後台「⬇️ 下載安裝權限」卡與後端函式全部保留，可隨時切回）。
  //   ⚠ 流量提醒：v3.5.17 批准制的初衷是「老師一台一台核准＝一台一台裝」避免 26 台同時撞校網 NAT；
  //     改自動後，同一節課同時首次登入的裝置會同時開始預載（各約 43MB）。
  window._LXPS_DL_PERM_FREE = true;
  // ★ v5.143.0 老師更正（2026-09-05）：免申請 ≠ 啟動時自動下載。
  //   true ⇒ 免申請合成的核准（_auto:true）**只讓「⬇️ 下載安裝」按鈕出現**，不觸發任何自動預載：
  //     ・bootPWA 不把 _auto 核准算進放行條件（沒裝過的裝置維持 sw-light 輕量模式）
  //     ・登入 3.5 秒自動 boot／權限監聽「剛核准就 boot」兩條自動路徑對 _auto 核准一律跳過
  //     ・只有玩家按下「⬇️ 下載安裝」→ _tryStartFullDownload 設 window._lxpsManualDownloadStart=true → 才 boot 完整 PWA 並開始預載
  //   已裝過的裝置（INSTALLED_KEY 有值）維持既有「背景補抓」行為不受影響。
  window._LXPS_DL_MANUAL_ONLY = true;
  window._lxpsManualDownloadStart = false;
  window._lxpsAutoDlPerm = function(uid){
    var _exp = Date.now() + 3650 * 86400000;   // 十年，實質永不過期
    return { uid: uid, status: 'approved', expiresAt: _exp, remainingMs: _exp - Date.now(), grantedBy: 'auto', _auto: true };
  };

  // 玩家送出下載權限申請
  window._fbRequestDownloadPermission = async function(uid, email, displayName){
    if(!uid){ console.warn('[下載授權] uid 為空,跳過'); return false; }
    try {
      const _now = Date.now();
      // 用 setDoc(merge) — 若已存在 pending/approved 也不破壞既有資料
      // 若先前是 revoked/expired,這次重新申請會把 status 改回 pending
      await setDoc(doc(_fbDb, 'downloadPermissions', uid), {
        uid: uid,
        email: (email || '').toLowerCase(),
        displayName: displayName || '',
        status: 'pending',
        requestedAt: _now,
        // 不動既有 grantedAt/expiresAt — 留給管理員批准時覆寫
      }, { merge: true });
      console.log('[下載授權] 已送出申請,uid=' + uid);
      return true;
    } catch(e){
      console.error('[下載授權] 申請寫入失敗', e);
      return false;
    }
  };

  // 取得單一玩家的下載權限狀態(含過期自動判定)
  // 回傳: { status, grantedAt, expiresAt, remainingMs, ... } 或 null(無申請紀錄)
  window._fbGetDownloadPermission = async function(uid){
    if(!uid) return null;
    if(window._LXPS_DL_PERM_FREE === true) return window._lxpsAutoDlPerm(uid);   // ★ v5.143.0 免申請
    try {
      const snap = await getDoc(doc(_fbDb, 'downloadPermissions', uid));
      if(!snap.exists()) return null;
      const d = snap.data();
      // 判斷是否過期(approved 但 expiresAt 已過)
      if(d.status === 'approved' && d.expiresAt && d.expiresAt < Date.now()){
        d.status = 'expired';
        d.remainingMs = 0;
      } else if(d.status === 'approved' && d.expiresAt){
        d.remainingMs = Math.max(0, d.expiresAt - Date.now());
      }
      return d;
    } catch(e){
      console.warn('[下載授權] 讀取失敗', e);
      return null;
    }
  };

  // 即時監聽單一玩家的下載權限變化(玩家自己用)
  // 回傳一個 unsubscribe 函式
  window._fbWatchDownloadPermission = function(uid, onChange){
    if(!uid) return () => {};
    if(window._LXPS_DL_PERM_FREE === true){   // ★ v5.143.0 免申請：不開 onSnapshot，直接回一次核准
      try{ setTimeout(function(){ try{ onChange(window._lxpsAutoDlPerm(uid)); }catch(_e){} }, 0); }catch(_){}
      return () => {};
    }
    return onSnapshot(
      doc(_fbDb, 'downloadPermissions', uid),
      (snap) => {
        if(!snap.exists()){ onChange(null); return; }
        const d = snap.data();
        if(d.status === 'approved' && d.expiresAt && d.expiresAt < Date.now()){
          d.status = 'expired';
          d.remainingMs = 0;
        } else if(d.status === 'approved' && d.expiresAt){
          d.remainingMs = Math.max(0, d.expiresAt - Date.now());
        }
        onChange(d);
      },
      (err) => { console.warn('[下載授權] 監聽失敗', err); }
    );
  };

  // 管理員批准下載權限(預設 24 小時有效)
  window._fbGrantDownloadPermission = async function(targetUid, adminEmail, hours, note){
    if(!targetUid){ throw new Error('targetUid 必填'); }
    const _hours = (typeof hours === 'number' && hours > 0) ? hours : 24;
    const _now = Date.now();
    const _expires = _now + _hours * 3600 * 1000;
    await setDoc(doc(_fbDb, 'downloadPermissions', targetUid), {
      status: 'approved',
      grantedAt: _now,
      grantedBy: adminEmail || 'unknown',
      expiresAt: _expires,
      note: note || '',
    }, { merge: true });
    console.log('[下載授權] 已批准,uid=' + targetUid + ',到期=' + new Date(_expires).toLocaleString());
    return _expires;
  };

  // 管理員撤銷下載權限
  window._fbRevokeDownloadPermission = async function(targetUid, adminEmail, reason){
    if(!targetUid){ throw new Error('targetUid 必填'); }
    await setDoc(doc(_fbDb, 'downloadPermissions', targetUid), {
      status: 'revoked',
      revokedAt: Date.now(),
      revokedBy: adminEmail || 'unknown',
      note: reason || '',
    }, { merge: true });
    console.log('[下載授權] 已撤銷,uid=' + targetUid);
    return true;
  };

  // 取得全部下載權限紀錄(管理員面板用)
  window._fbListDownloadPermissions = async function(){
    try {
      const snap = await getDocs(collection(_fbDb, 'downloadPermissions'));
      const _now = Date.now();
      const _list = [];
      snap.forEach(d => {
        const _data = d.data();
        // 自動標記過期(不寫回 Firestore,只在回傳時加註)
        if(_data.status === 'approved' && _data.expiresAt && _data.expiresAt < _now){
          _data._displayStatus = 'expired';
        } else {
          _data._displayStatus = _data.status;
        }
        _list.push(_data);
      });
      // 排序:pending 在最前,然後 approved,再然後其他;同類別按 requestedAt 倒序
      const _rank = { pending: 0, approved: 1, expired: 2, revoked: 3 };
      _list.sort((a, b) => {
        const _ra = _rank[a._displayStatus] ?? 9;
        const _rb = _rank[b._displayStatus] ?? 9;
        if(_ra !== _rb) return _ra - _rb;
        return (b.requestedAt || 0) - (a.requestedAt || 0);
      });
      return _list;
    } catch(e){
      console.error('[下載授權] 列表讀取失敗', e);
      throw e;
    }
  };

  // 即時監聽所有下載權限紀錄(管理員面板用)
  window._fbWatchAllDownloadPermissions = function(onChange){
    return onSnapshot(
      collection(_fbDb, 'downloadPermissions'),
      (snap) => {
        const _now = Date.now();
        const _list = [];
        snap.forEach(d => {
          const _data = d.data();
          if(_data.status === 'approved' && _data.expiresAt && _data.expiresAt < _now){
            _data._displayStatus = 'expired';
          } else {
            _data._displayStatus = _data.status;
          }
          _list.push(_data);
        });
        const _rank = { pending: 0, approved: 1, expired: 2, revoked: 3 };
        _list.sort((a, b) => {
          const _ra = _rank[a._displayStatus] ?? 9;
          const _rb = _rank[b._displayStatus] ?? 9;
          if(_ra !== _rb) return _ra - _rb;
          return (b.requestedAt || 0) - (a.requestedAt || 0);
        });
        onChange(_list);
      },
      (err) => console.warn('[下載授權] 列表監聽失敗', err)
    );
  };

  // ══════════════════════════════════════════════════════════════════
  // ★ v3.15.6(2026-06-14) — 畢業帳號資料轉移(實名制 GM 審核)後端 API
  // ──────────────────────────────────────────────────────────────────
  // 學生端:_fbSubmitAccountTransferRequest / _fbGetMyAccountTransferRequest
  // GM 端:_fbListAccountTransferRequests / _fbMigrateAccountData /
  //        _fbBackupNewAccountBeforeMigration / _fbRestoreNewAccountPreMigration /
  //        _fbAccountSnapshot / _fbResolveAccountTransfer
  // 安全核心(老師需求「回不去舊帳號」的後路):
  //   ① 遷移只「讀舊、寫新」,絕不改舊帳號 → 舊資料永遠是可重搬的「母本」
  //   ② 停權舊帳號是「最後一步」,且由 GM 端在複製驗證成功後才執行
  //   ③ 覆蓋新帳號前先備份(_premig_*),可一鍵還原
  // ══════════════════════════════════════════════════════════════════
  window._fbSubmitAccountTransferRequest = async function(newUid, newEmail, payload){
    if(!newUid){ console.warn('[帳號轉移] uid 為空,跳過'); return false; }
    payload = payload || {};
    try{
      await setDoc(doc(_fbDb, 'accountTransferRequests', newUid), {
        newUid: newUid,
        newEmail: (newEmail || '').toLowerCase(),
        realName: payload.realName || '',
        gradYear: payload.gradYear || '',
        classSeat: payload.classSeat || '',
        oldEmail: (payload.oldEmail || '').toLowerCase(),
        status: 'pending',
        createdAt: Date.now()
      }, { merge: true });
      console.log('[帳號轉移] 已送出申請,newUid=' + newUid);
      return true;
    }catch(e){ console.error('[帳號轉移] 申請寫入失敗', e); return false; }
  };

  window._fbGetMyAccountTransferRequest = async function(uid){
    if(!uid) return null;
    try{ const s = await getDoc(doc(_fbDb, 'accountTransferRequests', uid)); return s.exists() ? s.data() : null; }
    catch(e){ console.warn('[帳號轉移] 讀自己申請失敗', e); return null; }
  };

  window._fbListAccountTransferRequests = async function(){
    try{
      const snap = await getDocs(collection(_fbDb, 'accountTransferRequests'));
      const out = []; snap.forEach(d => out.push(Object.assign({ id: d.id }, d.data())));
      out.sort((a, b) => (b.createdAt || 0) - (a.createdAt || 0));
      return out;
    }catch(e){ console.error('[帳號轉移] 列表失敗', e); return []; }
  };

  // 讀某帳號關鍵指標(供 GM 比較新舊進度;對應覆蓋保護)
  window._fbAccountSnapshot = async function(uid){
    try{
      const p = await getDoc(doc(_fbDb, 'players', uid));
      if(!p.exists()) return { exists: false };
      const d = p.data();
      const _hl = d.heroLevels || {};
      const _unlocked = (Array.isArray(d.unlockedHeroes) && d.unlockedHeroes.length) || Object.keys(_hl).length || 0;
      let _maxLv = 0, _totalLv = 0;
      for(const k in _hl){ const v = _hl[k] || 0; _totalLv += v; if(v > _maxLv) _maxLv = v; }
      const _coins = (typeof d.knowledgeCoins === 'number') ? d.knowledgeCoins
                   : (typeof d.coins === 'number' ? d.coins : 0);
      return { exists: true, coins: _coins, unlocked: _unlocked, maxLv: _maxLv, totalLv: _totalLv,
               displayName: d.displayName || '', suspended: !!d._suspended };
    }catch(e){ return { exists: false, error: String(e) }; }
  };

  // 遷移前備份新帳號(players 主檔 + saves/live + saves/safe → _premig_* 備份槽)
  window._fbBackupNewAccountBeforeMigration = async function(newUid){
    if(!newUid) throw new Error('newUid 必填');
    try{ const p = await getDoc(doc(_fbDb, 'players', newUid));
         if(p.exists()) await setDoc(doc(_fbDb, 'players', newUid, 'saves', '_premig_main'), p.data()); }catch(_){}
    const _bk = async (slot, dest) => {
      try{ const s = await getDoc(doc(_fbDb, 'players', newUid, 'saves', slot));
           if(s.exists()) await setDoc(doc(_fbDb, 'players', newUid, 'saves', dest), s.data()); }catch(_){}
    };
    await _bk('live', '_premig_live');
    await _bk('safe', '_premig_safe');
    console.log('[帳號轉移] 已備份新帳號遷移前狀態,newUid=' + newUid);
    return true;
  };

  // 還原新帳號到遷移前(把 _premig_* 備份寫回 players 主檔 + saves)
  window._fbRestoreNewAccountPreMigration = async function(newUid){
    if(!newUid) throw new Error('newUid 必填');
    let _n = 0;
    try{ const m = await getDoc(doc(_fbDb, 'players', newUid, 'saves', '_premig_main'));
         if(m.exists()){ await setDoc(doc(_fbDb, 'players', newUid), m.data()); _n++; } }catch(_){}
    const _rs = async (src, slot) => {
      try{ const s = await getDoc(doc(_fbDb, 'players', newUid, 'saves', src));
           if(s.exists()){ await setDoc(doc(_fbDb, 'players', newUid, 'saves', slot), s.data()); _n++; } }catch(_){}
    };
    await _rs('_premig_live', 'live');
    await _rs('_premig_safe', 'safe');
    console.log('[帳號轉移] 已還原新帳號到遷移前,newUid=' + newUid + ',還原 ' + _n + ' 份');
    return _n;
  };

  // 核心遷移:複製舊 uid → 新 uid(全搬:players 主檔 + saves/live + saves/safe + arenaTeams + wbDamageDetail)
  //   ★ 只讀舊、寫新,絕不改舊帳號(母本永久保留)。回傳各項複製結果供 GM 驗證。
  window._fbMigrateAccountData = async function(oldUid, newUid){
    if(!oldUid || !newUid) throw new Error('oldUid / newUid 必填');
    if(oldUid === newUid) throw new Error('新舊 uid 相同,無需遷移');
    const _r = { playersDoc: false, saveLive: false, saveSafe: false, arenaTeams: false, wbDamage: false };
    // 1) players 主檔(整份複製到新 uid)
    const _oldP = await getDoc(doc(_fbDb, 'players', oldUid));
    if(!_oldP.exists()) throw new Error('舊帳號 players 主檔不存在(oldUid 可能有誤)');
    // ★ v3.15.18(2026-06-16) 修「新帳號無法登入」根因 ──────────────────────────
    //   主檔整份複製會把舊帳號的「帳號控管 / 連線 session」欄位也一起搬進新帳號。
    //   本核准流程「最後一步」會停權舊帳號;若舊帳號在「複製這一刻」就已是停權狀態
    //   (例:重跑遷移→第一次已把舊帳號停權;或畢業帳號早被停權),則 _suspended=true
    //   會被原封不動帶進新帳號 → 新帳號一誕生就被「登入停權閘門」(_suspended===true)
    //   擋下,顯示「🚫 帳號暫停存取」,學生回報「無法用新帳號登入」。
    //   修法:遷移只搬「遊戲進度」,主動剝除「停權 6 欄位 + 連線 session 2 欄位」。
    //   → 新帳號永遠以「未停權」誕生;session 由新帳號自己重建(_fbCreateSession 會覆寫)。
    const _srcData = _oldP.data();
    ['_suspended', '_suspendedAt', '_suspendedBy', '_suspendReason',
     '_unsuspendedAt', '_unsuspendedBy',
     'activeSession', 'sessionAt'].forEach(function(k){ delete _srcData[k]; });
    // ★ v3.16.61 — 修「轉移後新帳號無法登入」殘留根因:存檔內含 ownerUid(這份存檔屬於哪個 uid)。
    //   遷移整份複製會把舊帳號的 ownerUid 一起搬進新帳號 → 新帳號登入時 gameCloudLoad 的 Layer B
    //   發現 data.ownerUid(舊uid)≠ 現在登入 uid(新uid)→ 判定「外來資料」拒絕套用 → 卡「載入失敗」視窗。
    //   修法:把新帳號主檔 + 兩槽存檔的 ownerUid 一律改寫成新 uid(只讀舊寫新,不碰母本)。
    _srcData.ownerUid = newUid;
    await setDoc(doc(_fbDb, 'players', newUid), _srcData);
    _r.playersDoc = true;
    // 2) saves/live + saves/safe(同步把槽內 ownerUid 改成新 uid,否則三槽合併後 Layer B 仍會擋)
    const _copySlot = async (slot, key) => {
      const s = await getDoc(doc(_fbDb, 'players', oldUid, 'saves', slot));
      if(s.exists()){
        const _slotData = Object.assign({}, s.data(), { ownerUid: newUid });
        await setDoc(doc(_fbDb, 'players', newUid, 'saves', slot), _slotData); _r[key] = true;
      }
    };
    await _copySlot('live', 'saveLive');
    await _copySlot('safe', 'saveSafe');
    // 3) arenaTeams/{uid}(改 uid 欄為新 uid)
    try{ const a = await getDoc(doc(_fbDb, 'arenaTeams', oldUid));
         if(a.exists()){ const _d = Object.assign({}, a.data(), { uid: newUid }); await setDoc(doc(_fbDb, 'arenaTeams', newUid), _d); _r.arenaTeams = true; } }catch(_){}
    // 4) wbDamageDetail/{uid}(改 uid 欄為新 uid)
    try{ const w = await getDoc(doc(_fbDb, 'wbDamageDetail', oldUid));
         if(w.exists()){ const _d = Object.assign({}, w.data(), { uid: newUid }); await setDoc(doc(_fbDb, 'wbDamageDetail', newUid), _d); _r.wbDamage = true; } }catch(_){}
    console.log('[帳號轉移] 遷移完成 oldUid=' + oldUid + ' → newUid=' + newUid, _r);
    return _r;
  };

  // GM 標記轉移結果(approved/rejected + 中繼資料)
  window._fbResolveAccountTransfer = async function(newUid, status, extra){
    if(!newUid) throw new Error('newUid 必填');
    extra = extra || {};
    const _patch = { status: status, resolvedAt: Date.now(),
                     resolvedBy: (_fbAuth.currentUser && _fbAuth.currentUser.email) || 'unknown' };
    if(extra.oldUid) _patch.oldUid = extra.oldUid;
    if(extra.migratedAt) _patch.migratedAt = extra.migratedAt;
    if(typeof extra.gmNote === 'string') _patch.gmNote = extra.gmNote;
    await setDoc(doc(_fbDb, 'accountTransferRequests', newUid), _patch, { merge: true });
    return true;
  };

  window._fbDeleteAccountTransferRequest = async function(newUid){
    if(!newUid) throw new Error('newUid 必填');
    await deleteDoc(doc(_fbDb, 'accountTransferRequests', newUid));
    return true;
  };

  // ══════════════════════════════════════════════════════════════════
  // ★ v3.15.99(2026-06-24)— 小英雄會員資料(首次登入建立 + 可編輯同步雲端)
  // ──────────────────────────────────────────────────────────────────
  // 情境:
  //   玩家首次登入(memberProfileComplete 未設)→ 關卡頁自動彈「會員資料建立」,
  //   填妥 7 欄位送出 → 寫 players/{自己uid} 三個欄位:
  //     memberProfile(資料本體)、memberProfileComplete:true(關閉再彈)、
  //     memberProfileRewarded:true(一次性獎勵守門)。
  //   之後可在關卡頁「會員帳號與救援申請」→「編輯會員資料」隨時改(不再發獎)。
  //   GM 後台讀 players/{uid}.memberProfile 即可統計/辨識玩家身分。
  //
  // ★ 安全 / 規則:
  //   - 資料寫進玩家「自己的」players/{uid}(非新集合),走既有
  //     「玩家自己 + 不寫停權欄位」update 規則,memberProfile 三欄皆非停權欄位,
  //     → 自動放行,「無需新增 firestore.rules」。
  //   - 一次性獎勵(水晶/幣/SSR券)由 memberProfileRewarded 守門:送出前讀雲端
  //     現值,已 rewarded 則絕不重發(編輯既有資料也不會再發)。旗標先寫雲端、
  //     再本地發獎+存檔 → at-most-once(寧可極端網路失敗時少發、由 GM 補,
  //     也不重複發,呼應帳號救援「不採信前端、杜絕刷資源」精神)。
  //   - 會員暱稱(nickname)是「GM 專用識別欄位」,與遊戲內公開暱稱
  //     (window._playerNickname,排行榜會遮罩)無關,不對其他玩家顯示。
  // ══════════════════════════════════════════════════════════════════
  // 玩家身分(可複選)
  window._MEMBER_IDENTITY_OPTS = [
    '力行國小學生', '力行國小教師/職員', '力行國小學生家長', '力行國小志工',
    '力行國小畢業校友', '外校學生', '外校教師/職員', '外校學生家長',
    '教育界夥伴', '遊戲產業專家', '社會人士'
  ];
  // 在學就讀年級(選填;第一項空字串=未選/不適用)
  window._MEMBER_GRADE_OPTS = [
    '', '幼兒園',
    '國小一年級', '國小二年級', '國小三年級', '國小四年級', '國小五年級', '國小六年級',
    '國中一年級', '國中二年級', '國中三年級',
    '高中/職一年級', '高中/職二年級', '高中/職三年級',
    '大學一年級', '大學二年級', '大學三年級', '大學四年級',
    '研究所', '博士班'
  ];
  // 真實性別(必填)
  window._MEMBER_GENDER_OPTS = ['男', '女', '保密'];
  // 主要遊玩平台(可複選)
  window._MEMBER_PLATFORM_OPTS = ['iPad 平板', 'PC 桌上型電腦', 'NB 筆記型電腦', '手機'];

  // 讀玩家會員資料狀態(供首登判斷 + 編輯預填)
  window._fbGetMemberProfile = async function(uid){
    if(!uid || typeof _fbDb === 'undefined' || !_fbDb) return null;
    try{
      const s = await window._lxpsFbAwait(getDoc(doc(_fbDb, 'players', uid)), '會員資料·讀取', 10000);   /* ★ v5.212.0 讀取也加逾時 */
      const d = (s.exists() ? s.data() : null) || {};
      // ★ v5.212.0 主檔寫不進去時會改寫到 saves/memberProfile 小文件 ⇒ 兩邊合併(任一邊完成/已領就算)
      let sub = null;
      try{ const ss = await window._lxpsFbAwait(getDoc(doc(_fbDb, 'players', uid, 'saves', 'memberProfile')), '會員資料·讀取小文件', 8000); sub = ss.exists() ? (ss.data() || null) : null; }catch(_eSub){}
      const _pick = (sub && (sub.memberProfileUpdatedAt || 0) > (d.memberProfileUpdatedAt || 0)) ? sub : d;
      return {
        complete: !!(d.memberProfileComplete || (sub && sub.memberProfileComplete)),
        rewarded: !!(d.memberProfileRewarded || (sub && sub.memberProfileRewarded)),
        updatedAt: Math.max((typeof d.memberProfileUpdatedAt === 'number') ? d.memberProfileUpdatedAt : 0, (sub && typeof sub.memberProfileUpdatedAt === 'number') ? sub.memberProfileUpdatedAt : 0),
        profile: (_pick.memberProfile && typeof _pick.memberProfile === 'object') ? _pick.memberProfile : ((d.memberProfile && typeof d.memberProfile === 'object') ? d.memberProfile : null),
        inSubOnly: !!(sub && sub.memberProfileComplete && !d.memberProfileComplete),
        subData: sub
      };
    }catch(e){ console.warn('[會員資料] 讀取失敗', e); return null; }
  };

  // ════════════════════════════════════════════════════════════════════
  // ★★ v5.126.0【老師回報】Firestore 寫入逾時保護（治「按了送出沒反應，卡著」）★★
  // ────────────────────────────────────────────────────────────────────
  // 病灶：
  //   自我審查／會員資料等視窗，送出時每個 Firestore 寫入都是裸 `await`，**沒有任何逾時**。
  //   而 Firestore SDK 在寫入佇列塞滿時（就是學生回報過的
  //   `resource-exhausted: Write stream exhausted maximum allowed queued writes`）
  //   **不會 reject，它會一直排隊等**。
  //   ⇒ 按鈕已經 disabled + 顯示「處理中…」，然後永遠回不來，學生也沒辦法重按 ⇒ 整個卡死。
  //   ⚠ 這些視窗本身的失敗還原邏輯是好的（_disownOk/_submitOk 會把按鈕恢復），
  //     問題純粹出在 promise 永遠不 settle，那些還原邏輯根本沒機會執行。
  //
  // 修法：Promise.race 加一道逾時。逾時視同失敗 ⇒ 走既有的還原路徑 ⇒ 按鈕恢復、可重按。
  //   ⚠ 逾時**不代表寫入失敗**——Firestore 可能稍後才真的寫進去。
  //     所以所有呼叫端的動作都必須是「可重入」的（disown 是集合差集、會員資料是 merge 覆蓋，
  //     重按一次不會產生第二筆或雙倍效果）。⇒ 逾時後請學生重按是安全的。
  //   ⚠ 刻意不縮短到 5 秒：學校 iPad 網路慢，太短會把正常寫入誤判成失敗。
  // ════════════════════════════════════════════════════════════════════
  window._LXPS_FB_TIMEOUT_MS = 15000;
  window._lxpsFbAwait = function(p, label, ms){
    var _ms = (typeof ms === 'number' && ms > 0) ? ms : (window._LXPS_FB_TIMEOUT_MS || 15000);
    return new Promise(function(resolve, reject){
      var _done = false;
      var _t = setTimeout(function(){
        if(_done) return;
        _done = true;
        var _e = new Error('雲端回應逾時（' + (_ms/1000) + ' 秒）：' + String(label || ''));
        _e.code = 'lxps/timeout';
        _e.lxpsTimeout = true;
        try{ console.warn('[雲端逾時] ' + String(label || '') + ' 超過 ' + _ms + 'ms 未回應（可能是寫入佇列塞滿，稍後可能仍會寫入成功）'); }catch(_){}
        reject(_e);
      }, _ms);
      Promise.resolve(p).then(function(v){
        if(_done) return; _done = true; clearTimeout(_t); resolve(v);
      }, function(e){
        if(_done) return; _done = true; clearTimeout(_t); reject(e);
      });
    });
  };

  // ════════════════════════════════════════════════════════════════════
  // ★★ v5.212.0【老師回報：很多學生 iPad 填完會員資料「無法儲存、領不到獎勵」】REST 直寫備援 ★★
  //   v5.126.0/v5.147.0 已處理「憑證過期」「uid 不一致」,但 SDK 寫入仍會卡在:
  //     ① 上課 25 台 iPad 同時存檔 ⇒ SDK 寫入佇列塞滿(resource-exhausted)⇒ 永遠排隊 ⇒ 逾時
  //     ② iOS 從背景回來 WebChannel 連線半死不活 ⇒ setDoc 不 resolve 也不 reject
  //   ⇒ 改成:SDK 9 秒沒回應 → 用 fetch 打 Firestore REST(同一套安全規則,只寫 updateMask 指定的欄位)
  //      → 主檔還是寫不進去(例如主檔超過 1MB 上限回 400)→ 寫到自己的小文件 players/{uid}/saves/memberProfile
  //        (saves/{slot} 規則本來就允許本人讀寫,不必改 firestore.rules),下次登入自動搬回主檔。
  // ════════════════════════════════════════════════════════════════════
  window._lxpsFsEnc = function(v){
    if(v === null || v === undefined) return { nullValue: null };
    if(typeof v === 'boolean') return { booleanValue: v };
    if(typeof v === 'number') return (Math.floor(v) === v && Math.abs(v) < 9e15) ? { integerValue: String(v) } : { doubleValue: v };
    if(typeof v === 'string') return { stringValue: v };
    if(Array.isArray(v)) return { arrayValue: { values: v.map(window._lxpsFsEnc) } };
    var _f = {}; Object.keys(v).forEach(function(k){ _f[k] = window._lxpsFsEnc(v[k]); });
    return { mapValue: { fields: _f } };
  };
  window._lxpsFsRestPatch = async function(path, obj, token, ms){
    var _pid = 'lxpsgame';
    try{ if(typeof firebaseConfig !== 'undefined' && firebaseConfig.projectId) _pid = firebaseConfig.projectId; }catch(_){}
    var _qs = Object.keys(obj).map(function(k){ return 'updateMask.fieldPaths=' + encodeURIComponent(k); }).join('&');
    var _body = { fields: {} };
    Object.keys(obj).forEach(function(k){ _body.fields[k] = window._lxpsFsEnc(obj[k]); });
    var _ctl = (typeof AbortController === 'function') ? new AbortController() : null;
    var _tm = setTimeout(function(){ try{ if(_ctl) _ctl.abort(); }catch(_){} }, ms || 12000);
    try{
      var _r = await fetch('https://firestore.googleapis.com/v1/projects/' + encodeURIComponent(_pid) + '/databases/(default)/documents/' + path + '?' + _qs, {
        method: 'PATCH', headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer ' + token },
        body: JSON.stringify(_body), signal: _ctl ? _ctl.signal : undefined, cache: 'no-store'
      });
      if(!_r.ok){ var _t = ''; try{ _t = await _r.text(); }catch(_){} var _e = new Error('REST ' + _r.status + ' ' + String(_t).slice(0, 180)); _e.code = 'lxps/rest-' + _r.status; throw _e; }
      return true;
    } finally { clearTimeout(_tm); }
  };
  window._lxpsMpRestFallback = async function(uid, patch, user){
    var _u = user || null, _tk = '';
    try{ if(!_u) _u = (window._fbAuth && window._fbAuth.currentUser) || null; }catch(_){}
    if(!_u || _u.uid !== uid) return false;
    try{ _tk = await _u.getIdToken(false); }catch(_eT){ try{ _tk = await _u.getIdToken(true); }catch(_eT2){ return false; } }
    try{
      await window._lxpsFsRestPatch('players/' + encodeURIComponent(uid), patch, _tk, 12000);
      try{ console.info('[會員資料] ✅ SDK 卡住,已改用 REST 直寫主檔成功'); }catch(_){}
      return true;
    }catch(_eMain){
      try{ console.warn('[會員資料] REST 主檔寫入失敗', _eMain && (_eMain.code || _eMain.message)); }catch(_){}
      if(String(_eMain && _eMain.code).indexOf('401') >= 0){ try{ _tk = await _u.getIdToken(true); }catch(_){} }
    }
    try{
      await window._lxpsFsRestPatch('players/' + encodeURIComponent(uid) + '/saves/memberProfile', patch, _tk, 12000);
      try{ localStorage.setItem('lxps_member_profile_sub_' + uid, '1'); }catch(_){}
      try{ console.info('[會員資料] ✅ 主檔寫不進去,已改寫到 players/{uid}/saves/memberProfile(下次登入自動搬回主檔)'); }catch(_){}
      return true;
    }catch(_eSub){
      try{ console.error('[會員資料] ✗ REST 備援(主檔+小文件)全部失敗', _eSub && (_eSub.code || _eSub.message)); }catch(_){}
      return false;
    }
  };

  // 寫玩家會員資料(merge:true;grantReward=true 時一併設 rewarded 旗標)
  //   只寫 memberProfile / memberProfileComplete / memberProfileUpdatedAt
  //   (+ 視情況 memberProfileRewarded),不動其他玩家欄位。
  window._fbSaveMemberProfile = async function(uid, profile, grantReward){
    if(!uid || typeof _fbDb === 'undefined' || !_fbDb) throw new Error('uid/_fbDb 必要');
    const _patch = {
      memberProfile: profile,
      memberProfileComplete: true,
      memberProfileUpdatedAt: Date.now()
    };
    if(grantReward){
      _patch.memberProfileRewarded = true;
      _patch.memberProfileCreatedAt = Date.now();   // 首次建立時間(僅首次設定,之後編輯不覆蓋)
    }
    // ════════════════════════════════════════════════════════════════
    // ★★ v5.126.0【老師回報】「儲存被拒」診斷 + 憑證重取重試 ★★
    // ────────────────────────────────────────────────────────────────
    //   規則上學生寫自己的 players 主檔是允許的（`allow update: auth.uid == uid
    //   && !isWritingSuspendFields()`），而這裡只寫 memberProfile 四個欄位、不碰停權欄位
    //   ⇒ 正常情況不該 permission-denied。實際仍有學生被拒，最可能的兩個原因：
    //     ① Auth token 過期（iPad 長時間開著／PWA 從背景恢復）⇒ request.auth 變 null
    //     ② 共用 iPad 換帳號時 `window._gUserId` 與實際登入的 uid 不同步 ⇒ 變成寫別人的主檔
    //   ⇒ 先擋掉 ②（寫入前比對，不一致直接擋下並講清楚，**絕不寫進別人的主檔**），
    //     再對 ① 做「強制刷新憑證後重試一次」。
    //   ⚠ 診斷資訊一律寫 console，讓下一個卡住的學生能直接告訴我們原因（不靜默）。
    // ════════════════════════════════════════════════════════════════
    var _authUid = '';
    try{ _authUid = (window._fbUser && window._fbUser.uid) || ''; }catch(_){}
    // ★ v5.147.0(老師回報:iPad 填完送出跳錯、重新登入也存不了)— 以 SDK 目前真正的登入者為準
    //   (_fbUser 是 onAuth 快照,共用 iPad 換人/從背景恢復後可能落後;Firestore 實際用的是 _fbAuth.currentUser 的 token)
    var _curUser = null;
    try{ _curUser = (window._fbAuth && window._fbAuth.currentUser) || null; }catch(_){}
    if(_curUser && _curUser.uid){ _authUid = _curUser.uid; }
    if(!_curUser){
      try{ console.error('[會員資料] ✗ SDK 目前沒有登入者(currentUser=null)— token 可能已失效,需重新登入'); }catch(_){}
      var _eNo = new Error('目前未登入(憑證已失效)'); _eNo.code = 'lxps/no-auth'; throw _eNo;
    }
    if(_authUid && String(_authUid) !== String(uid)){
      try{ console.error('[會員資料] ✗ uid 不一致，已擋下寫入 — 目標=' + String(uid).slice(0,12)
        + ' / 實際登入=' + String(_authUid).slice(0,12) + '（共用裝置換帳號未同步）'); }catch(_){}
      var _eMis = new Error('登入帳號與資料對象不一致');
      _eMis.code = 'lxps/uid-mismatch';
      throw _eMis;
    }
    try{
      // ★ v5.212.0【老師回報：很多學生 iPad 填完會員資料存不進去、領不到獎勵】
      //   SDK 寫入改成 9 秒內沒回應就改走 REST 直寫(不經 SDK 的 WebChannel 寫入佇列;
      //   上課 25 台 iPad 同時存檔時 SDK 佇列塞滿 = 永遠排隊 = 學生看到「逾時／儲存失敗」)。
      await window._lxpsFbAwait(setDoc(doc(_fbDb, 'players', uid), _patch, { merge: true }), '會員資料·SDK 寫入', 9000);
      return true;
    }catch(_e1){
      var _c1 = String((_e1 && (_e1.code || _e1.message)) || _e1);
      try{ console.warn('[會員資料] 第一次寫入失敗 code=' + _c1
        + ' / uid=' + String(uid).slice(0,12) + ' / authUid=' + String(_authUid || '(無)').slice(0,12)); }catch(_){}
      // 只對 permission-denied 做「刷新憑證後重試」——其他錯誤(逾時/佇列塞滿/文件過大/網路)改走 REST 備援
      if(_c1.indexOf('permission') < 0 && _c1.indexOf('Missing or insufficient') < 0){
        if(await window._lxpsMpRestFallback(uid, _patch, _curUser)) return true;
        throw _e1;
      }
      try{
        // ★ v5.147.0 先 reload 使用者(同步伺服器端狀態)再強制換 token;用 currentUser 而不是快照
        if(_curUser && typeof _curUser.reload === 'function'){ try{ await _curUser.reload(); }catch(_eR){} }
        if(_curUser && typeof _curUser.getIdToken === 'function'){
          var _tk = await _curUser.getIdToken(true);           // true = 強制向伺服器換新 token
          try{ console.info('[會員資料] 已強制刷新憑證(長度 ' + String(_tk || '').length + '),重試一次…'); }catch(_){}
        }
      }catch(_eTok){ try{ console.warn('[會員資料] 憑證刷新失敗', _eTok); }catch(_){} }
      await new Promise(function(r){ setTimeout(r, 800); });   // ★ v5.147.0 給 SDK 一點時間把新 token 套到連線上
      try{
        await window._lxpsFbAwait(setDoc(doc(_fbDb, 'players', uid), _patch, { merge: true }), '會員資料·SDK 重試', 9000);
        try{ console.info('[會員資料] ✅ 刷新憑證後重試成功'); }catch(_){}
        return true;
      }catch(_e2){
        // ★ v5.212.0 SDK 仍失敗 ⇒ 用新 token 走 REST 直寫(SDK 連線可能還掛著舊 token)
        try{ if(await window._lxpsMpRestFallback(uid, _patch, _curUser)) return true; }catch(_eRf){}
        // ★ v5.147.0 仍被拒 ⇒ 把完整診斷丟到 console(規則版本/是否有 email_verified/doc 是否存在)供老師回報
        try{
          var _ex = 'unknown';
          try{ var _snapP = await getDoc(doc(_fbDb, 'players', uid)); _ex = _snapP.exists() ? 'exists' : 'missing'; }catch(_eG){ _ex = 'read-denied:' + String(_eG && _eG.code); }
          console.error('[會員資料] ✗ 刷新憑證後仍被拒 code=' + String(_e2 && (_e2.code || _e2.message))
            + ' | uid=' + String(uid) + ' | email=' + String(_curUser && _curUser.email) + ' | verified=' + String(_curUser && _curUser.emailVerified)
            + ' | provider=' + String(_curUser && _curUser.providerData && _curUser.providerData.length && _curUser.providerData[0].providerId)
            + ' | players doc=' + _ex + ' | patchKeys=' + Object.keys(_patch).join(','));
        }catch(_){}
        throw _e2;
      }
    }
  };

  // ★ v5.147.0 — 會員資料「本機暫存 + 自動重送」:雲端存不進去時先把表單存在本機(綁 uid),
  //   之後每次登入(8 秒後)與網路恢復時自動重送;成功才清掉。學生不用重填、也不會因為一時的憑證/網路問題丟資料。
  window._memberProfilePendingKey = function(uid){ return 'lxps_member_profile_pending_' + String(uid || ''); };
  window._memberProfileStash = function(uid, profile, grantReward){
    try{ localStorage.setItem(window._memberProfilePendingKey(uid), JSON.stringify({ profile: profile, grantReward: !!grantReward, at: Date.now() })); }catch(_){}
  };
  // ★ v5.212.0 — 獎勵「寫雲端旗標 → 本地發獎」之間的斷點記號(綁 uid)。
  //   舊版:寫入逾時(其實稍後有寫進去)⇒ 本地沒發獎;學生再按一次時雲端已是 rewarded ⇒ 永遠領不到。
  //   新版:寫入前先記 'w';只有「本地真的發過獎」才清掉。之後任何時候看到「雲端已 rewarded + 本機還有 'w'」
  //         就代表上次旗標寫進去了但獎勵沒發到 ⇒ 補發一次(記號綁這台裝置,不會跨裝置重複)。
  window._memberRewardMarkKey = function(uid){ return 'lxps_member_reward_w_' + String(uid || ''); };
  window._memberRewardGrantLocal = function(uid, source){
    try{
      if(typeof backpackAdd === 'function'){ backpackAdd('summon_crystal', 10); backpackAdd('summon_ticket_ssr', 1); }
      if(typeof addKnowledgeCoins === 'function'){ addKnowledgeCoins(20000); }
      else if(typeof _knowledgeCoins !== 'undefined'){ window._knowledgeCoins = (_knowledgeCoins || 0) + 20000; if(typeof _syncCoinsDisplay === 'function') _syncCoinsDisplay(); }
      if(typeof _syncSummonCrystalDisplay === 'function') _syncSummonCrystalDisplay();
      if(typeof _setNavDot === 'function') _setNavDot('背包', true);
      if(typeof window._logActivity === 'function') window._logActivity('reward', { source: source || 'member_profile_create', items:['summon_crystal x10','summon_ticket_ssr x1','coins x20000'] });
      if(typeof playSfx === 'function') playSfx('sfx-powerup', 0.9);
    }catch(_eRw){ console.warn('[會員資料發獎] 例外', _eRw); }
    try{ localStorage.removeItem(window._memberRewardMarkKey(uid)); }catch(_){}
    // 立刻強制存檔(不吃戰鬥節流);存檔保護層擋下時 5 秒後再試,最多 4 次
    var _n = 0;
    var _try = function(){
      _n++;
      try{ window._lxpsCloudSaveForceNext = true; }catch(_){}
      var _p = null; try{ if(typeof gameCloudSave === 'function') _p = gameCloudSave(); }catch(_eS){}
      Promise.resolve(_p).then(function(ok){ if(ok !== true && _n < 4) setTimeout(_try, 5000); }, function(){ if(_n < 4) setTimeout(_try, 5000); });
    };
    _try();
  };
  // ★ v5.212.0 — 發獎前一定要等「遊戲進度載入完成」:載入前發的獎勵會被隨後套用的雲端進度蓋掉,
  //   而且 gameCloudSave 在載入前一律拒存(保護層 1)⇒ 這就是「填好了但獎勵不見」的另一條路。
  window._memberWaitProgressLoaded = async function(ms){
    var _t0 = Date.now();
    while(!window._progressLoaded && Date.now() - _t0 < (ms || 25000)){ await new Promise(function(r){ setTimeout(r, 400); }); }
    return !!window._progressLoaded;
  };
  window._memberProfileRetryPending = async function(reason){
    try{
      var _uid = window._gUserId; if(!_uid) return false;
      // ★ v5.212.0 ① 補發斷點:雲端已 rewarded、本機還留著 'w' 記號 ⇒ 上次旗標有寫進去但獎勵沒發到
      var _mk0 = null; try{ _mk0 = localStorage.getItem(window._memberRewardMarkKey(_uid)); }catch(_){}
      var _hasSub0 = false; try{ _hasSub0 = !!localStorage.getItem('lxps_member_profile_sub_' + _uid); }catch(_){}
      if(_mk0 || _hasSub0){
        var _st0 = null; try{ _st0 = await window._fbGetMemberProfile(_uid); }catch(_){}
        if(_st0 && _st0.rewarded && _mk0){
          if(await window._memberWaitProgressLoaded(30000)){
            window._memberRewardGrantLocal(_uid, 'member_profile_recover');
            try{ localStorage.removeItem(window._memberProfilePendingKey(_uid)); }catch(_){}
            if(typeof _showSimpleToast === 'function') _showSimpleToast('🎉 會員資料獎勵補發完成!🔮×10 + 💰20,000 + 🌈SSR券×1', 'ok');
          }
        }
        // ② 上次只寫進 saves/memberProfile 小文件 ⇒ 試著搬回主檔(GM 名單讀主檔)
        if(_st0 && _st0.inSubOnly && _st0.subData && _st0.subData.memberProfile){
          try{
            await window._lxpsFbAwait(setDoc(doc(_fbDb, 'players', _uid), {
              memberProfile: _st0.subData.memberProfile, memberProfileComplete: true,
              memberProfileUpdatedAt: _st0.subData.memberProfileUpdatedAt || Date.now(),
              memberProfileRewarded: !!_st0.subData.memberProfileRewarded
            }, { merge: true }), '會員資料·搬回主檔', 12000);
            try{ localStorage.removeItem('lxps_member_profile_sub_' + _uid); }catch(_){}
          }catch(_eMv){ try{ console.warn('[會員資料] 小文件搬回主檔失敗(下次再試,資料不會遺失)', _eMv && (_eMv.code || _eMv.message)); }catch(_){} }
        } else if(_st0 && !_st0.inSubOnly){ try{ localStorage.removeItem('lxps_member_profile_sub_' + _uid); }catch(_){} }
      }
      var _raw = null; try{ _raw = localStorage.getItem(window._memberProfilePendingKey(_uid)); }catch(_){}
      if(!_raw) return false;
      var _p = null; try{ _p = JSON.parse(_raw); }catch(_){ _p = null; }
      if(!_p || !_p.profile){ try{ localStorage.removeItem(window._memberProfilePendingKey(_uid)); }catch(_){} return false; }
      // 已在雲端建立過就不再重送(避免覆蓋更新的資料)
      var _st = null; try{ _st = await window._fbGetMemberProfile(_uid); }catch(_){}
      if(_st && _st.complete && _st.updatedAt && _st.updatedAt >= (_p.profile.updatedAt || 0)){ try{ localStorage.removeItem(window._memberProfilePendingKey(_uid)); }catch(_){} return false; }
      var _mk1 = null; try{ _mk1 = localStorage.getItem(window._memberRewardMarkKey(_uid)); }catch(_){}
      var _grant = !!_p.grantReward && !(_st && _st.rewarded);
      var _recover = !!_p.grantReward && !!(_st && _st.rewarded) && !!_mk1;   /* ★ v5.212.0 旗標已上雲但獎勵沒發到 */
      if(_grant || _recover){ try{ localStorage.setItem(window._memberRewardMarkKey(_uid), 'w'); }catch(_){} }
      await window._lxpsFbAwait(window._fbSaveMemberProfile(_uid, _p.profile, _grant), '會員資料·自動重送(' + String(reason || '') + ')', 45000);
      try{ localStorage.removeItem(window._memberProfilePendingKey(_uid)); }catch(_){}
      try{ console.info('[會員資料] ✅ 本機暫存的會員資料已自動補送雲端(' + String(reason || '') + ')'); }catch(_){}
      if(_grant || _recover){
        if(await window._memberWaitProgressLoaded(30000)){
          window._memberRewardGrantLocal(_uid, 'member_profile_resend');
          if(typeof _showSimpleToast === 'function') _showSimpleToast('🎉 上次填的會員資料已成功送出!獎勵 🔮×10 + 💰20,000 + 🌈SSR券×1 已發放', 'ok');
        }
      }else{
        if(typeof _showSimpleToast === 'function') _showSimpleToast('✅ 上次填的會員資料已成功送出', 'ok');
      }
      return true;
    }catch(e){
      try{ console.warn('[會員資料] 自動重送仍失敗(保留本機暫存,下次再試)', e && (e.code || e.message)); }catch(_){}
      return false;
    }
  };
  try{ window.addEventListener('online', function(){ setTimeout(function(){ window._memberProfileRetryPending('online'); }, 3000); }); }catch(_){}

  // ★ v3.16.32 — 審查失誤「道歉 + 補償」一次性旗標(per-UID·UID 雲端同步·at-most-once)
  //   讀:從 server 讀 players/{uid}._auditApologyCompensatedV1(讀失敗 throw → 呼叫端跳過不發,避免不確定下重發)。
  //   寫:setDoc merge 寫旗標(鏡像 memberProfileRewarded 旗標先寫模式)。玩家自寫自己 doc·非停權欄位·走既有規則,無需改 rules。
  window._fbGetAuditApologyClaimed = async function(uid){
    if(!uid || typeof _fbDb === 'undefined' || !_fbDb) return false;
    try{
      var _snap = await getDocFromServer(doc(_fbDb, 'players', uid));
      if(_snap.exists()){ var _d = _snap.data() || {}; return _d._auditApologyCompensatedV1 === true; }
      return false;
    }catch(e){ console.warn('[審查道歉補償] 讀旗標失敗', e); throw e; }
  };
  window._fbSetAuditApologyClaimed = async function(uid){
    if(!uid || typeof _fbDb === 'undefined' || !_fbDb) throw new Error('uid/_fbDb 必要');
    await setDoc(doc(_fbDb, 'players', uid), { _auditApologyCompensatedV1: true, _auditApologyCompensatedAt: Date.now() }, { merge: true });
    return true;
  };

  // ══════════════════════════════════════════════════════════════════
  // ★ v3.15.90(2026-06-23)— 帳號救援申請(學生自助向老師申請補回遺失資料)
  // ──────────────────────────────────────────────────────────────────
  // 情境:
  //   學生在關卡頁底部工具列點「📨 帳號救援申請」→ 勾選遺失的項目
  //   (英雄/至寶/召喚水晶/召喚卷/知識幣/排名獎勵)→ 寫一筆到
  //   accountRescueRequests/{自己uid}(status='pending',每日上限 1)。
  //   GM 後台「📨 帳號救援申請審核」list 待處理 → 對每筆按
  //   「🔍 核對並準備救援」自動跑 _fbRebuildAccountFromLedgers(uid) 從雲端
  //   帳本「權威反推」應有資料,對照學生勾選逐項標 ✅符合/❌不符合/⏳待判斷,
  //   GM 確認後 _fbApplyAccountRebuild(只增不減)補回 + _fbResolveAccountRescueRequest。
  //
  // ★ 安全核心:
  //   - 學生寫的 claims / selfCheck 只是「給老師參考的勾選與初判」,GM 補償時
  //     完全不採信這些欄位 → 一律由 GM 端 _fbRebuildAccountFromLedgers 從雲端
  //     帳本重新權威反推「真正缺什麼」,杜絕學生靠勾選刷資源。
  //   - rules:create 限本人 pending + 白名單 8 欄 + 不可塞 GM 審核欄位;
  //     list 限 GM;get/update 限本人或 GM。
  // ══════════════════════════════════════════════════════════════════
  window._fbSubmitAccountRescueRequest = async function(uid, email, displayLabel, claims, selfCheck){
    if(!uid){ console.warn('[帳號救援] uid 為空,跳過'); return false; }
    try{
      await setDoc(doc(_fbDb, 'accountRescueRequests', uid), {
        uid: uid,
        email: (email || '').toLowerCase(),
        displayLabel: displayLabel || '',
        claims: (claims && typeof claims === 'object') ? claims : {},
        selfCheck: (selfCheck && typeof selfCheck === 'object') ? selfCheck : {},
        status: 'pending',
        requestDateStr: (typeof _getTodayKeyTW === 'function') ? _getTodayKeyTW() : '',
        createdAt: Date.now()
      }, { merge: true });
      console.log('[帳號救援] 已送出申請,uid=' + uid);
      return true;
    }catch(e){ console.error('[帳號救援] 申請寫入失敗', e); return false; }
  };

  window._fbGetMyAccountRescueRequest = async function(uid){
    if(!uid) return null;
    try{ const s = await getDoc(doc(_fbDb, 'accountRescueRequests', uid)); return s.exists() ? s.data() : null; }
    catch(e){ console.warn('[帳號救援] 讀自己申請失敗', e); return null; }
  };

  window._fbListAccountRescueRequests = async function(){
    try{
      const snap = await getDocs(collection(_fbDb, 'accountRescueRequests'));
      const out = []; snap.forEach(d => out.push(Object.assign({ id: d.id }, d.data())));
      out.sort((a, b) => (b.createdAt || 0) - (a.createdAt || 0));
      return out;
    }catch(e){ console.error('[帳號救援] 列表失敗', e); return []; }
  };

  // GM 標記救援結果(resolved/rejected/handled + 備註)
  window._fbResolveAccountRescueRequest = async function(uid, status, gmNote){
    if(!uid) throw new Error('uid 必填');
    const _patch = { status: status, resolvedAt: Date.now(),
                     resolvedBy: (_fbAuth.currentUser && _fbAuth.currentUser.email) || 'unknown' };
    if(typeof gmNote === 'string') _patch.gmNote = gmNote;
    await setDoc(doc(_fbDb, 'accountRescueRequests', uid), _patch, { merge: true });
    return true;
  };

  // ★ v3.16.78 — GM 標記「單一審查項目」已處理(不關閉整筆申請)
  //   讓 GM 能在同一筆救援申請內逐項完成英雄/至寶的審查/刪除/補償,
  //   且每筆紀錄持久化(寫雲端 _handledItems[key]),重新整理也看得到哪些已審查、哪些待確認。
  //   merge:true 對 nested map 的單一子鍵安全:寫 {_handledItems:{key:e}} 只會新增/覆寫該 key,
  //   不會洗掉 _handledItems 內其他已處理項目。整筆關閉仍走 _fbResolveAccountRescueRequest(由「✔標記已處理/✖駁回」觸發)。
  window._fbMarkRescueItemHandled = async function(uid, itemKey, note){
    if(!uid || !itemKey) throw new Error('uid / itemKey 必填');
    var _by = (_fbAuth.currentUser && _fbAuth.currentUser.email) || 'unknown';
    var _entry = { at: Date.now(), by: _by, note: (typeof note === 'string' ? note : '') };
    var _patch = { _handledItems: {} };
    _patch._handledItems[itemKey] = _entry;
    await setDoc(doc(_fbDb, 'accountRescueRequests', uid), _patch, { merge: true });
    return true;
  };

  // ════════════════════════════════════════════════════════════════
  // ★ v3.16.32 — GM 即時提醒:救援申請 + 錯誤回報(浮動徽章 + 悅耳音效)
  //   只在管理員登入時啟用(_lxpsRescueBadgeStart 由 onAuth 呼叫·登出 Stop)。
  //   監聽兩個集合:accountRescueRequests(待處理) + bugReports(未回覆)。
  //   有「新」項目進來(總數較上次增加)時播放清楚悅耳的提示音 + 徽章脈動;
  //   登入當下既有的待處理只顯示徽章不播音(避免每次登入被嚇到)。
  //   ⚠ 相依:accountRescueRequests / bugReports 的 firestore.rules 需已部署
  //          (GM list 權限·與既有「📨 帳號救援申請審核」「📥 接收錯誤回報」兩卡同相依)。
  // ════════════════════════════════════════════════════════════════
  // 監聽待處理救援申請(status 非 resolved/rejected)
  window._fbWatchAccountRescuePending = function(onChange){
    try{
      return onSnapshot(collection(_fbDb, 'accountRescueRequests'), function(snap){
        var _n = 0;
        snap.forEach(function(d){
          var _r = d.data() || {};
          if(_r.status !== 'resolved' && _r.status !== 'rejected') _n++;
        });
        try{ onChange(_n); }catch(_){}
      }, function(err){ console.warn('[救援即時提醒] 監聽失敗', err); try{ onChange(-1); }catch(_){} });
    }catch(e){ console.warn('[救援即時提醒] 啟動失敗', e); return null; }
  };
  // 監聽未回覆錯誤回報(沒有 adminReply = 老師還沒回)
  window._fbWatchBugReportsUnhandled = function(onChange){
    try{
      return onSnapshot(collection(_fbDb, 'bugReports'), function(snap){
        var _n = 0;
        snap.forEach(function(d){ var _r = d.data() || {}; if(!_r.adminReply) _n++; });
        try{ onChange(_n); }catch(_){}
      }, function(err){ console.warn('[錯誤回報即時提醒] 監聽失敗', err); try{ onChange(-1); }catch(_){} });
    }catch(e){ console.warn('[錯誤回報即時提醒] 啟動失敗', e); return null; }
  };
  // 清楚又悅耳的提示音(Web Audio 合成上行大三和弦琶音·無音檔相依·失敗不影響徽章)
  window._lxpsRescueChimeCtx = null;
  // ★ v3.16.33 — 改播老師指定 mp3(收到救援申請.mp3);失敗(自動播放被擋/載入失敗)fallback 回 Web Audio 合成琶音(雙保險·不影響徽章)
  window._lxpsPlayRescueChimeSynth = function(){
    try{
      var _AC = window.AudioContext || window.webkitAudioContext;
      if(!_AC) return;
      if(!window._lxpsRescueChimeCtx){ window._lxpsRescueChimeCtx = new _AC(); }
      var _ctx = window._lxpsRescueChimeCtx;
      if(_ctx.state === 'suspended'){ try{ _ctx.resume(); }catch(_){} }
      var _t0 = _ctx.currentTime;
      var _notes = [ {f:523.25, t:0.00}, {f:659.25, t:0.12}, {f:783.99, t:0.24} ]; // C5-E5-G5
      _notes.forEach(function(_n){
        var _osc = _ctx.createOscillator();
        var _g = _ctx.createGain();
        _osc.type = 'sine';
        _osc.frequency.value = _n.f;
        var _st = _t0 + _n.t;
        _g.gain.setValueAtTime(0.0001, _st);
        _g.gain.exponentialRampToValueAtTime(0.18, _st + 0.02);
        _g.gain.exponentialRampToValueAtTime(0.0001, _st + 0.55);
        _osc.connect(_g); _g.connect(_ctx.destination);
        _osc.start(_st); _osc.stop(_st + 0.62);
      });
    }catch(e){ /* 音效失敗不影響功能 */ }
  };
  window._lxpsPlayRescueChime = function(){
    try{
      var _el = document.getElementById('sfx-rescue-chime');
      if(_el && typeof _el.play === 'function'){
        try{ _el.pause(); }catch(_){}
        try{ _el.currentTime = 0; }catch(_){}
        var _p = _el.play();
        if(_p && typeof _p.then === 'function'){
          _p.catch(function(){ try{ window._lxpsPlayRescueChimeSynth(); }catch(_){} });  // 被擋/載入失敗 → 回合成音
        }
        return;
      }
    }catch(_e){ /* 落到合成音 */ }
    try{ window._lxpsPlayRescueChimeSynth(); }catch(_){}
  };
  // 徽章狀態 + 啟動/停止/渲染
  window._lxpsRescueBadgeUnsubA = null;     // 救援申請監聽
  window._lxpsRescueBadgeUnsubB = null;     // 錯誤回報監聽
  window._lxpsRescuePendingCount = 0;
  window._lxpsBugUnhandledCount  = 0;
  window._lxpsRescueBadgeTotalSeen = -1;    // -1 = 尚未取得第一次快照(首次不播音)
  window._lxpsRescueBadgeStart = function(){
    try{
      if(typeof window._isAdminUser !== 'function' || !window._isAdminUser()) return;   // 只 GM
      if(window._lxpsRescueBadgeUnsubA || window._lxpsRescueBadgeUnsubB) return;         // 已啟用
      window._lxpsRescueBadgeTotalSeen = -1;
      if(typeof window._fbWatchAccountRescuePending === 'function'){
        window._lxpsRescueBadgeUnsubA = window._fbWatchAccountRescuePending(function(_c){
          if(_c >= 0) window._lxpsRescuePendingCount = _c;
          window._lxpsRescueBadgeRender();
        });
      }
      if(typeof window._fbWatchBugReportsUnhandled === 'function'){
        window._lxpsRescueBadgeUnsubB = window._fbWatchBugReportsUnhandled(function(_c){
          if(_c >= 0) window._lxpsBugUnhandledCount = _c;
          window._lxpsRescueBadgeRender();
        });
      }
    }catch(e){ console.warn('[GM 即時提醒徽章] 啟動失敗', e); }
  };
  window._lxpsRescueBadgeStop = function(){
    try{ if(window._lxpsRescueBadgeUnsubA){ window._lxpsRescueBadgeUnsubA(); window._lxpsRescueBadgeUnsubA = null; } }catch(_){}
    try{ if(window._lxpsRescueBadgeUnsubB){ window._lxpsRescueBadgeUnsubB(); window._lxpsRescueBadgeUnsubB = null; } }catch(_){}
    try{ var _b = document.getElementById('_lxps-rescue-badge'); if(_b) _b.remove(); }catch(_){}
    window._lxpsRescuePendingCount = 0;
    window._lxpsBugUnhandledCount  = 0;
    window._lxpsRescueBadgeTotalSeen = -1;
  };
  window._lxpsRescueBadgeRender = function(){
    try{
      var _rescue = window._lxpsRescuePendingCount || 0;
      var _bug    = window._lxpsBugUnhandledCount || 0;
      var _total  = _rescue + _bug;
      // 新項目進來(總數較上次增加·且非首次快照)→ 播音 + 脈動
      var _isNew = (window._lxpsRescueBadgeTotalSeen >= 0 && _total > window._lxpsRescueBadgeTotalSeen);
      window._lxpsRescueBadgeTotalSeen = _total;
      var _b = document.getElementById('_lxps-rescue-badge');
      if(_total <= 0){ if(_b) _b.remove(); return; }
      if(!_b){
        _b = document.createElement('div');
        _b.id = '_lxps-rescue-badge';
        _b.style.cssText = 'position:fixed;right:14px;bottom:96px;z-index:2147483000;'
          + 'background:linear-gradient(135deg,#2a5a46,#143526);border:2px solid #8fe3b4;'
          + 'border-radius:14px;padding:10px 14px;cursor:pointer;'
          + 'box-shadow:0 6px 22px rgba(40,170,115,0.5);'
          + 'font-family:"M PLUS Rounded 1c","Nunito",sans-serif;color:#e6fff0;'
          + 'font-size:14px;font-weight:800;max-width:250px;line-height:1.4;'
          + 'display:flex;align-items:center;gap:10px;';
        _b.onclick = function(){
          var _sec = ((window._lxpsRescuePendingCount || 0) > 0) ? '_admin-rescue-req-section' : '_admin-bug-section';
          try{ if(typeof window._showAdminStatsPanel === 'function') window._showAdminStatsPanel(); }catch(_){}
          var _tries = 0;
          var _iv = setInterval(function(){
            _tries++;
            var _btn = document.querySelector('.admin-sidebar-item[data-sec="' + _sec + '"]');
            if(_btn){ try{ _btn.click(); }catch(_){} clearInterval(_iv); }
            else if(_tries > 40){ clearInterval(_iv); }
          }, 150);
        };
        document.body.appendChild(_b);
      }
      var _parts = [];
      if(_rescue > 0) _parts.push('救援申請 <b style="color:#fff;">' + _rescue + '</b>');
      if(_bug > 0)    _parts.push('錯誤回報 <b style="color:#fff;">' + _bug + '</b>');
      _b.innerHTML = '<span style="font-size:24px;flex-shrink:0;">🔔</span>'
        + '<span>有 <b style="color:#fff;font-size:16px;">' + _total + '</b> 筆待處理<br>'
        + '<span style="font-size:11.5px;color:#bdf0d4;">' + _parts.join(' ・ ') + '</span><br>'
        + '<span style="font-size:10.5px;color:#9adcbb;opacity:0.85;">點我打開後台處理</span></span>';
      if(_isNew){
        try{ window._lxpsPlayRescueChime(); }catch(_){}
        try{ _b.animate([{transform:'scale(1)'},{transform:'scale(1.13)'},{transform:'scale(1)'}], {duration:560, iterations:2}); }catch(_){}
      }
    }catch(e){ console.warn('[GM 即時提醒徽章] 渲染失敗', e); }
  };

  // ══════════════════════════════════════════════════════════════════
  // ★ v3.5.0 — 可疑帳號偵測(全玩家掃描 + 即時監聽)
  // ──────────────────────────────────────────────────────────────────
  // 偵測策略(老師選的):
  //   A. _dataSummary 在短時間內爆衝
  //      - 等級突然 +50 / 知識幣突然 +10萬 / 解鎖英雄 +20 等
  //      - 對比歷史 _dataSummary 序列(放在 stats/suspiciousHistory/{uid})
  //   B. 能力值/技能威力超過該英雄定義上限
  //      - 比對 HERO_DB 中各英雄的能力總和(必須=100)
  //      - 比對 heroStatInvested 是否超過該等級允許上限(等級-1 = 可分配點數)
  //   C. 擁有不該擁有的英雄(可選擴充)
  //
  // 觸發時機(老師選的):「獨立的可疑帳號面板,即時 onSnapshot 監聽」
  // 因此這裡只提供「掃描+評估」邏輯,UI 的 onSnapshot 在管理員面板開啟時才啟動
  // ══════════════════════════════════════════════════════════════════

  // 規則參數(可調)
  window._SUS_RULES = {
    burstCoinsDelta: 50000,     // 知識幣短時間 +5萬視為可疑(老師要求 +10萬,這裡先抓 5萬作為「警告」分級)
    burstCoinsAlert: 100000,    // +10萬視為「嚴重」
    burstLvDelta: 30,           // 等級短時間 +30 視為可疑
    burstLvAlert: 50,           // +50 視為嚴重
    burstUnlockDelta: 10,       // 解鎖英雄短時間 +10 視為可疑
    burstUnlockAlert: 20,       // +20 視為嚴重
    summaryWindowMs: 6 * 60 * 60 * 1000, // 比對「6小時內的變化量」(避免合理長期遊玩被誤判)
  };

  // 評估單一玩家資料是否可疑(輸入是 player document 完整內容)
  // 回傳: { suspicious: bool, severity: 'none'|'warn'|'alert', reasons: [string], details: {} }
  window._fbEvaluatePlayerSuspicious = function(playerData){
    const _result = { suspicious: false, severity: 'none', reasons: [], details: {} };
    if(!playerData || typeof playerData !== 'object') return _result;

    const _rules = window._SUS_RULES;
    const _summary = playerData._dataSummary;
    const _prevSummary = playerData._prevDataSummary;

    // ── 策略 A: 短時間爆衝偵測 ──
    if(_summary && _prevSummary && typeof _summary === 'object' && typeof _prevSummary === 'object'){
      const _dt = (_summary.ts || 0) - (_prevSummary.ts || 0);
      // 只在窗口時間內比對(超過 6 小時就不能算「爆衝」了)
      if(_dt > 0 && _dt < _rules.summaryWindowMs){
        const _dCoins = (_summary.coins || 0) - (_prevSummary.coins || 0);
        const _dMaxLv = (_summary.maxHeroLv || 0) - (_prevSummary.maxHeroLv || 0);
        const _dTotalLv = (_summary.totalHeroLv || 0) - (_prevSummary.totalHeroLv || 0);
        const _dUnlock = (_summary.unlockedCount || 0) - (_prevSummary.unlockedCount || 0);

        if(_dCoins >= _rules.burstCoinsAlert){
          _result.reasons.push('知識幣短時間爆增 +' + _dCoins.toLocaleString());
          _result.severity = 'alert';
        } else if(_dCoins >= _rules.burstCoinsDelta){
          _result.reasons.push('知識幣短時間明顯增加 +' + _dCoins.toLocaleString());
          if(_result.severity !== 'alert') _result.severity = 'warn';
        }
        if(_dMaxLv >= _rules.burstLvAlert){
          _result.reasons.push('最高英雄等級短時間爆增 +' + _dMaxLv);
          _result.severity = 'alert';
        } else if(_dMaxLv >= _rules.burstLvDelta){
          _result.reasons.push('最高英雄等級短時間增加 +' + _dMaxLv);
          if(_result.severity !== 'alert') _result.severity = 'warn';
        }
        if(_dTotalLv >= _rules.burstLvAlert * 4){
          _result.reasons.push('總英雄等級短時間爆增 +' + _dTotalLv);
          _result.severity = 'alert';
        }
        if(_dUnlock >= _rules.burstUnlockAlert){
          _result.reasons.push('解鎖英雄數短時間爆增 +' + _dUnlock);
          _result.severity = 'alert';
        } else if(_dUnlock >= _rules.burstUnlockDelta){
          _result.reasons.push('解鎖英雄數短時間明顯增加 +' + _dUnlock);
          if(_result.severity !== 'alert') _result.severity = 'warn';
        }

        _result.details.deltas = { coins: _dCoins, maxLv: _dMaxLv, totalLv: _dTotalLv, unlock: _dUnlock, windowMs: _dt };
      }
    }

    // ── 策略 B: 能力值/技能投資超過上限 ──
    // 規則:升 1 級得 1 點素質點,因此 lv-1 = 該英雄可分配總點數
    //       若 heroStatInvested[name] 的總和超過 (lv-1) + 寬容量(20),視為異常
    try {
      if(playerData.heroStatInvested && playerData.heroLevels){
        const _invested = playerData.heroStatInvested;
        const _levels = playerData.heroLevels;
        const _overflowList = [];
        Object.keys(_invested).forEach(name => {
          const _inv = _invested[name] || {};
          const _sum = (_inv.hp || 0) + (_inv.atk || 0) + (_inv.sp || 0) + (_inv.spd || 0);
          const _lv = _levels[name] || 1;
          const _allowed = Math.max(0, _lv - 1);
          // 寬容 20 點(避免規則更動造成誤判 — 老師若曾調整過點數規則的英雄)
          if(_sum > _allowed + 20){
            _overflowList.push(name + '(投資' + _sum + '點,等級' + _lv + '僅允許' + _allowed + '點)');
          }
        });
        if(_overflowList.length > 0){
          _result.reasons.push('英雄能力值投資超過上限:' + _overflowList.slice(0, 3).join('、') +
            (_overflowList.length > 3 ? ' 等 ' + _overflowList.length + ' 隻' : ''));
          _result.severity = 'alert';
          _result.details.overInvested = _overflowList;
        }
      }
    } catch(e){ console.warn('[可疑帳號] 能力值檢查例外', e); }

    // 整合結論
    _result.suspicious = (_result.severity !== 'none');
    return _result;
  };

  // 全玩家掃描(管理員按鈕用 — 一次性批次)
  // 注意:這會讀取所有 players 文件,配額消耗大,只在管理員主動點選時呼叫
  window._fbScanSuspiciousPlayers = async function(){
    try {
      const _snap = await getDocs(collection(_fbDb, 'players'));
      const _suspicious = [];
      _snap.forEach(d => {
        const _data = d.data();
        const _eval = window._fbEvaluatePlayerSuspicious(_data);
        if(_eval.suspicious){
          _suspicious.push({
            uid: d.id,
            email: _data.email || '',
            displayName: _data.displayName || '',
            severity: _eval.severity,
            reasons: _eval.reasons,
            details: _eval.details,
            _dataSummary: _data._dataSummary,
            _suspended: !!_data._suspended,
          });
        }
      });
      // 嚴重 → 警告 → 已停權順序
      const _rank = { alert: 0, warn: 1 };
      _suspicious.sort((a, b) => {
        if(a._suspended !== b._suspended) return a._suspended ? 1 : -1;
        const _ra = _rank[a.severity] ?? 9;
        const _rb = _rank[b.severity] ?? 9;
        return _ra - _rb;
      });
      return _suspicious;
    } catch(e){
      console.error('[可疑帳號] 全玩家掃描失敗', e);
      throw e;
    }
  };

  // 即時監聽所有玩家(管理員面板開啟時用)
  // ⚠ 配額警告:這會觸發 onSnapshot 監聽整個 players 集合,只在面板打開時啟用,關閉時務必 unsubscribe
  window._fbWatchAllPlayersForSuspicious = function(onChange){
    return onSnapshot(
      collection(_fbDb, 'players'),
      (snap) => {
        const _suspicious = [];
        snap.forEach(d => {
          const _data = d.data();
          const _eval = window._fbEvaluatePlayerSuspicious(_data);
          if(_eval.suspicious){
            _suspicious.push({
              uid: d.id,
              email: _data.email || '',
              displayName: _data.displayName || '',
              severity: _eval.severity,
              reasons: _eval.reasons,
              details: _eval.details,
              _dataSummary: _data._dataSummary,
              _suspended: !!_data._suspended,
            });
          }
        });
        const _rank = { alert: 0, warn: 1 };
        _suspicious.sort((a, b) => {
          if(a._suspended !== b._suspended) return a._suspended ? 1 : -1;
          const _ra = _rank[a.severity] ?? 9;
          const _rb = _rank[b.severity] ?? 9;
          return _ra - _rb;
        });
        onChange(_suspicious);
      },
      (err) => {
        console.warn('[可疑帳號] 即時監聽失敗', err);
        onChange([], err);
      }
    );
  };

  // ════════════════════════════════════════════════════════════════
  // ★ v3.16.76 — GM「英雄/至寶持有者審查」後端
  // ────────────────────────────────────────────────────────────────
  //   用途:老師勾選一個或多個英雄/至寶 → 掃描全體玩家,列出「誰持有 +
  //         獲得來源 + 時間標記」,協助審查是否為不正常取得 → 直接刪除/補償。
  //   ⚠ 配額:一次讀取所有 players 文件(資料量大),只在管理員主動點「查詢」時呼叫。
  //   來源/時間取自 players/{uid}._heroUnlockHistory[] / _treasureUnlockHistory[]
  //   (每名英雄/至寶取「最新一筆」帳本紀錄);無紀錄則來源空字串(=可疑·較早期/查無)。
  //   回傳:{ scanned, owners:[{uid,email,displayName,
  //            heroes:[{name,source,at,lv}], treasures:[{id,name,source,at,lv}]}] }
  window._fbAdminScanItemOwners = async function(heroNames, treasureIds){
    var _heroSet = {}; (heroNames || []).forEach(function(n){ if(n) _heroSet[n] = true; });
    var _treSet  = {}; (treasureIds || []).forEach(function(i){ if(i) _treSet[i] = true; });
    var _hasHero = Object.keys(_heroSet).length > 0;
    var _hasTre  = Object.keys(_treSet).length > 0;
    if(!_hasHero && !_hasTre) return { scanned:0, owners:[] };
    var _snap = await getDocs(collection(_fbDb, 'players'));
    var _owners = [];
    _snap.forEach(function(d){
      var _data = d.data() || {};
      // ── 英雄持有(unlockedHeroes 陣列;來源/時間 ← _heroUnlockHistory 最新一筆)──
      var _ownHeroes = [];
      if(_hasHero){
        var _uh = Array.isArray(_data.unlockedHeroes) ? _data.unlockedHeroes : [];
        var _hHist = Array.isArray(_data._heroUnlockHistory) ? _data._heroUnlockHistory : [];
        var _hLatest = {};
        _hHist.forEach(function(r){
          if(!r || !r.name) return;
          var _at = Number(r.at) || 0;
          if(!(r.name in _hLatest) || _at >= _hLatest[r.name].at) _hLatest[r.name] = { at:_at, source:(r.source || '') };
        });
        var _hLv = (_data.heroLevels && typeof _data.heroLevels === 'object') ? _data.heroLevels : {};
        for(var _hi = 0; _hi < _uh.length; _hi++){
          var _hn = _uh[_hi];
          if(_hn && _heroSet[_hn]){
            var _rec = _hLatest[_hn] || { at:0, source:'' };
            _ownHeroes.push({ name:_hn, source:_rec.source, at:_rec.at, lv:(Number(_hLv[_hn]) || 1) });
          }
        }
      }
      // ── 至寶持有(taiwanTreasureData map;來源/時間 ← _treasureUnlockHistory 最新一筆)──
      var _ownTre = [];
      if(_hasTre){
        var _tw = (_data.taiwanTreasureData && typeof _data.taiwanTreasureData === 'object') ? _data.taiwanTreasureData : {};
        var _tHist = Array.isArray(_data._treasureUnlockHistory) ? _data._treasureUnlockHistory : [];
        var _tLatest = {};
        _tHist.forEach(function(r){
          if(!r || !r.id) return;
          var _at = Number(r.at) || 0;
          if(!(r.id in _tLatest) || _at >= _tLatest[r.id].at) _tLatest[r.id] = { at:_at, source:(r.source || '') };
        });
        Object.keys(_treSet).forEach(function(_tid){
          var _tr = _tw[_tid];
          if(_tr){
            var _trec = _tLatest[_tid] || { at:0, source:'' };
            var _tname = (window.TAIWAN_TREASURES && window.TAIWAN_TREASURES[_tid] && window.TAIWAN_TREASURES[_tid].name) || _tid;
            _ownTre.push({ id:_tid, name:_tname, source:_trec.source, at:_trec.at, lv:(Number(_tr.lv) || 1) });
          }
        });
      }
      if(_ownHeroes.length || _ownTre.length){
        _owners.push({ uid:d.id, email:(_data.email || ''), displayName:(_data.displayName || ''), heroes:_ownHeroes, treasures:_ownTre });
      }
    });
    // 排序:有「來源空白 / admin_delete」(可疑)者優先,其次依玩家名稱
    function _suspScore(o){
      var _items = (o.heroes || []).concat(o.treasures || []);
      for(var i = 0; i < _items.length; i++){ var s = String(_items[i].source || ''); if(!s || s === 'admin_delete') return 0; }
      return 1;
    }
    _owners.sort(function(a, b){
      var sa = _suspScore(a), sb = _suspScore(b);
      if(sa !== sb) return sa - sb;
      return String(a.displayName || a.uid).localeCompare(String(b.displayName || b.uid));
    });
    return { scanned:_snap.size, owners:_owners };
  };

  // 管理員停權某帳號(寫入 players/{uid}._suspended = true)
  // 玩家下次登入會在 onAuthStateChanged 流程中被擋下(用既有維修模式類似流程)
  window._fbSuspendPlayer = async function(targetUid, adminEmail, reason){
    if(!targetUid) throw new Error('targetUid 必填');
    await setDoc(doc(_fbDb, 'players', targetUid), {
      _suspended: true,
      _suspendedAt: Date.now(),
      _suspendedBy: adminEmail || 'unknown',
      _suspendReason: reason || '',
    }, { merge: true });
    console.log('[可疑帳號] 已停權,uid=' + targetUid);
    return true;
  };

  // 管理員解除停權
  window._fbUnsuspendPlayer = async function(targetUid, adminEmail){
    if(!targetUid) throw new Error('targetUid 必填');
    await setDoc(doc(_fbDb, 'players', targetUid), {
      _suspended: false,
      _unsuspendedAt: Date.now(),
      _unsuspendedBy: adminEmail || 'unknown',
    }, { merge: true });
    console.log('[可疑帳號] 已解除停權,uid=' + targetUid);
    return true;
  };

  // 即時監聽維修狀態變更(若管理員中途開啟,要立刻踢出非管理員玩家)
  window._fbWatchMaintenance = function(callback) {
    try {
      return onSnapshot(doc(_fbDb, 'stats', 'global'), (snap) => {
        if(!snap.exists()) { callback({ enabled: false }); return; }
        const m = snap.data().maintenance;
        callback((m && typeof m === 'object') ? m : { enabled: false });
      }, (err) => {
        console.warn('[維修模式] 監聽錯誤', err);
        callback({ enabled: false });
      });
    } catch(e) {
      console.error('[維修模式] 啟動監聽失敗', e);
      return () => {};
    }
  };

  window._fbSubmitBugReport = async function(payload) {
    try {
      const _id = 'bug_' + Date.now() + '_' + Math.random().toString(36).slice(2, 8);
      const _email = (payload && payload.email) || (window._fbUser && window._fbUser.email) || '訪客';
      const _uid   = (payload && payload.uid)   || (window._fbUser && window._fbUser.uid)   || 'guest';

      // ★ v3.3.2 — 組裝完整 content:玩家文字 + 戰鬥快照 + console dump
      const _userText = ((payload && payload.content) || '').toString();
      const _snapshot = (function(){
        // 收集當下戰鬥/旗標狀態(若不在戰鬥中也回基礎環境資訊)
        try{
          const _G = (typeof G !== 'undefined') ? G : null;
          const lines = [];
          lines.push('## 🎮 環境資訊');
          lines.push('- URL: ' + (location && location.href || '?'));
          lines.push('- UA: ' + (navigator && navigator.userAgent || '?').slice(0, 200));
          lines.push('- 時間: ' + new Date().toLocaleString('zh-TW'));
          lines.push('- 版本: ' + (window._GAME_LOADED_VERSION || 'unknown'));
          lines.push('- 頁面: ' + ((payload && payload.page) || 'unknown'));
          if(typeof _adventureMode !== 'undefined' && _adventureMode){
            lines.push('');
            lines.push('## ⚔ 冒險狀態');
            lines.push('- _adventureStage: ' + (typeof _adventureStage !== 'undefined' ? _adventureStage : '?'));
            lines.push('- _advMiniBattleActive: ' + (typeof _advMiniBattleActive !== 'undefined' ? _advMiniBattleActive : '?'));
            lines.push('- _advQuizPhase: ' + (typeof _advQuizPhase !== 'undefined' ? _advQuizPhase : '?'));
            lines.push('- _advQuizResolveCb: ' + (typeof _advQuizResolveCb !== 'undefined' ? !!_advQuizResolveCb : '?'));
            lines.push('- _gamePaused: ' + (typeof _gamePaused !== 'undefined' ? _gamePaused : '?'));
            lines.push('- _bossQuizInFlight: ' + !!window._bossQuizInFlight);
            lines.push('- _advBossActionSkip: ' + (typeof _advBossActionSkip !== 'undefined' ? _advBossActionSkip : '?'));
            lines.push('- _advMiniResultShowing: ' + !!window._advMiniResultShowing);
            lines.push('- _wbInWorldBossMode: ' + !!window._wbInWorldBossMode);
            lines.push('- _wbSoloPracticeMode: ' + !!window._wbSoloPracticeMode);
          }
          if(_G){
            lines.push('');
            lines.push('## 🎯 戰場 G');
            lines.push('- round: ' + (_G.round || '?'));
            lines.push('- turn: ' + (_G.turn || '?'));
            lines.push('- activeChar: ' + (_G.activeChar && _G.activeChar.name || 'none')
              + ' (side=' + (_G.activeChar && _G.activeChar.side) + ', pos=' + (_G.activeChar && _G.activeChar.pos) + ')');
            if(_G.p1){
              lines.push('- p1: ' + _G.p1.map(h => h ?
                (h.name + '(HP ' + h.curHp + '/' + h.hp + ', acted=' + !!h.acted
                + (h.status && h.status.length ? ', status=[' + h.status.map(s=>s.type).join(',') + ']' : '')
                + ')') : 'null').join(' | '));
            }
            if(_G.p2){
              lines.push('- p2: ' + _G.p2.map(h => h ?
                (h.name + '(HP ' + h.curHp + '/' + h.hp + ', acted=' + !!h.acted
                + (h.status && h.status.length ? ', status=[' + h.status.map(s=>s.type).join(',') + ']' : '')
                + ')') : 'null').join(' | '));
            }
          }
          // modal 顯示狀態
          const _modalIds = ['adv-quiz-overlay','adv-reward-overlay','adv-result-overlay',
            'adv-boss-intro','adv-continue-overlay','adv-cutscene-overlay',
            'burst-priority-modal','action-panel','item-panel'];
          const _visibleModals = [];
          _modalIds.forEach(id => {
            const el = document.getElementById(id);
            if(!el) return;
            const cs = window.getComputedStyle(el);
            const hasShow = el.classList.contains('show');
            const visible = cs.display !== 'none' && cs.visibility !== 'hidden';
            if(hasShow || visible) _visibleModals.push(id + (hasShow ? '.show' : '') + (visible ? '(visible)' : ''));
          });
          if(_visibleModals.length){
            lines.push('');
            lines.push('## 📦 顯示中 modal/panel');
            lines.push('- ' + _visibleModals.join('\n- '));
          }
          return lines.join('\n');
        }catch(eS){ return '## ⚠ 快照失敗\n' + (eS && eS.message || eS); }
      })();
      const _consoleDump = (typeof window._consoleDumpForBugReport === 'function')
        ? window._consoleDumpForBugReport() : '(無 console buffer)';

      // 組裝 markdown 格式 content(管理員一鍵複製貼給 Claude 直接讀)
      const _fullContent = [
        '# 🐛 玩家 BUG 回報',
        '',
        '## 📝 玩家描述',
        _userText || '(玩家未填寫描述)',
        '',
        _snapshot,
        '',
        '## 🔍 Console 紀錄(最近 300 條)',
        '```',
        _consoleDump,
        '```',
      ].join('\n');

      // 上限 8000 字,超過從 console dump 頭部截掉(保留玩家描述 + 快照 + 最新 console)
      let _finalContent = _fullContent;
      if(_finalContent.length > 8000){
        const _overflow = _finalContent.length - 8000 + 50; // +50 給「(已截掉舊 log)」標籤
        // 找到 Console 區塊起點,從中間截
        const _consoleStart = _finalContent.indexOf('## 🔍 Console 紀錄');
        if(_consoleStart > 0 && _consoleStart < _finalContent.length - _overflow){
          // 在 console dump 內截(保留新的)
          const _codeStart = _finalContent.indexOf('```\n', _consoleStart);
          if(_codeStart > 0){
            _finalContent = _finalContent.slice(0, _codeStart + 4)
              + '(已截掉舊 log,僅保留最新)\n...\n'
              + _finalContent.slice(_codeStart + 4 + _overflow);
          } else {
            _finalContent = _finalContent.slice(0, 7950) + '\n...(截)';
          }
        } else {
          _finalContent = _finalContent.slice(0, 7950) + '\n...(截)';
        }
      }

      const reportObj = {
        id: _id,
        email: (_email || '').toString().slice(0, 200),
        uid: (_uid || '').toString().slice(0, 200),
        content: _finalContent,
        page: (payload && payload.page) || 'unknown',  // battle / stage_select / other
        version: (window._GAME_LOADED_VERSION || 'unknown'),
        timestamp: Date.now(),
        // 額外保留拆分後欄位,管理員 UI 若要分區顯示也可用
        _userText: _userText.slice(0, 500),
        _hasConsoleDump: true,
        // ★ v3.5.42 — 玩家勾選的 BUG 分類陣列(供老師後台篩選統計)
        //   合法值: dead_lock / skill_no_response / data_incorrect / quiz_error
        //          / crash_quit / battle_lost / other
        //   空陣列 = 沒勾任何分類(向後相容,舊版客戶端不會送這欄位)
        categories: (payload && Array.isArray(payload.categories))
          ? payload.categories.slice(0, 7) : [],
      };
      await setDoc(doc(_fbDb, 'bugReports', _id), reportObj);
      console.info('✅ [BUG 回報] 已送出', { id:_id, contentLen:_finalContent.length });
      return reportObj;
    } catch(e) {
      console.error('❌ [BUG 回報] 送出失敗', e);
      throw e;
    }
  };

  // 管理員查詢所有 BUG 回報(最新 200 筆,按時間倒序)
  window._fbListBugReports = async function() {
    try {
      const snap = await getDocs(collection(_fbDb, 'bugReports'));
      const list = [];
      snap.forEach(d => {
        const v = d.data() || {};
        list.push({
          id: v.id || d.id,
          email: v.email || '',
          uid: v.uid || '',
          content: v.content || '',
          page: v.page || '',
          version: v.version || '',
          timestamp: v.timestamp || 0,
          time: v.timestamp ? new Date(v.timestamp).toLocaleString('zh-TW') : '?',
          // ★ v3.4.6 — 管理員回覆欄位
          adminReply: v.adminReply || '',
          adminReplyTs: v.adminReplyTs || 0,
          adminReplyTime: v.adminReplyTs ? new Date(v.adminReplyTs).toLocaleString('zh-TW') : '',
        });
      });
      // 按時間倒序(最新在前)
      list.sort((a, b) => (b.timestamp || 0) - (a.timestamp || 0));
      return list.slice(0, 200);
    } catch(e) {
      console.error('[BUG 回報] 查詢失敗', e);
      return [];
    }
  };

  // 管理員刪除單筆 BUG 回報(處理完後可清除)
  window._fbDeleteBugReport = async function(id) {
    if(!id) return false;
    try {
      await deleteDoc(doc(_fbDb, 'bugReports', id));
      return true;
    } catch(e) {
      console.error('[BUG 回報] 刪除失敗', e);
      return false;
    }
  };

  // ★ v3.4.6 — 管理員寫入/更新某筆 BUG 回報的回覆 (adminReply)
  //   結構: { adminReply: string, adminReplyTs: number, adminReplyRead: false }
  //   寫入後玩家在「管理員回信」分頁可看到老師的處理情形回覆
  window._fbUpdateBugReportAdminReply = async function(id, replyText) {
    if(!id) return false;
    try {
      const _reply = (replyText || '').toString().slice(0, 4000);
      await updateDoc(doc(_fbDb, 'bugReports', id), {
        adminReply: _reply,
        adminReplyTs: Date.now(),
        adminReplyRead: false,
      });
      console.info('✅ [BUG 回報] 管理員回覆已儲存', { id, len: _reply.length });
      return true;
    } catch(e) {
      console.error('[BUG 回報] 更新管理員回覆失敗', e);
      return false;
    }
  };

  // ★ v3.4.13 — 玩家標記某筆管理員回信為「已讀」
  //   觸發時機:玩家打開「📬 管理員回信」分頁後,把當前清單中所有未讀的 reply 一次標記
  //   作用:讓置頂通知 + 按鈕發亮可以消失,下次登入不再重複提示
  window._fbMarkBugReplyRead = async function(id) {
    if(!id) return false;
    try {
      await updateDoc(doc(_fbDb, 'bugReports', id), {
        adminReplyRead: true,
        adminReplyReadTs: Date.now(),
      });
      return true;
    } catch(e) {
      console.warn('[BUG 回報] 標記已讀失敗', e);
      return false;
    }
  };

  // ★ v3.4.13 — 玩家查詢「我自己的未讀回信」筆數
  //   用於登入後判斷是否要彈出「老師已回覆」置頂通知 + 按鈕發亮
  //   回傳: { count: number, items: [{id, adminReplyTs, content摘要}, ...] }
  // ★ v3.4.16 — 改用 query+where 在 server 端過濾, 避免 rules 拒讀整個 collection
  //   原 bug: getDocs(collection) 撈全部 → rules 規定「玩家只能讀自己的回報」→
  //          其他人的文件 read 被拒 → 整個 query 拋 permission-denied →
  //          玩家完全看不到任何回信
  // ★ v3.12.5 — items 內加 adminReply(回信全文)與 contentExcerpt(原回報前 120 字摘要),
  //   讓 _showAdminReplyPopup 彈窗能直接顯示「老師回什麼 + 你原本回報什麼」,
  //   不用玩家自己再點按鈕進回信分頁找。
  window._fbCountMyUnreadBugReplies = async function(uid) {
    if(!uid) {
      uid = (window._fbUser && window._fbUser.uid) || null;
    }
    if(!uid) return { count: 0, items: [] };
    try {
      // ★ v3.4.16 — 加 where('uid','==',uid) 讓 server 只回符合 rules 的文件
      const _q = query(collection(_fbDb, 'bugReports'), where('uid', '==', uid));
      const snap = await getDocs(_q);
      const items = [];
      snap.forEach(d => {
        const v = d.data() || {};
        // 有 adminReply 但 adminReplyRead 為 false / 未設 → 算未讀
        const _hasReply = !!(v.adminReply && v.adminReply.toString().trim());
        const _isRead = (v.adminReplyRead === true);
        if(_hasReply && !_isRead){
          // ★ v3.12.5 — 原回報內容摘要(最多 120 字,給玩家看「我當時回什麼」的提示)
          let _excerpt = '';
          try{
            const _raw = (v.content || v.description || '').toString();
            _excerpt = _raw.length > 120 ? (_raw.slice(0, 120) + '…') : _raw;
          }catch(_){}
          items.push({
            id: v.id || d.id,
            adminReplyTs: v.adminReplyTs || 0,
            adminReplyTime: v.adminReplyTs ? new Date(v.adminReplyTs).toLocaleString('zh-TW') : '',
            adminReply: (v.adminReply || '').toString(),  // ★ v3.12.5
            contentExcerpt: _excerpt,                      // ★ v3.12.5
            reportedAt: v.ts ? new Date(v.ts).toLocaleString('zh-TW') : '',  // ★ v3.12.5
          });
        }
      });
      items.sort((a, b) => (b.adminReplyTs || 0) - (a.adminReplyTs || 0));
      return { count: items.length, items };
    } catch(e) {
      console.warn('[BUG 回報] 查詢未讀回信失敗', e);
      return { count: 0, items: [] };
    }
  };

  // ★ v3.4.6 — 玩家端:查詢「我自己」的 BUG 回報清單(只顯示我的 uid)
  //   用於玩家端「管理員回信」分頁:顯示自己送出過的回報,以及老師的回覆
  // ★ v3.4.16 — 改用 query+where 在 server 端過濾, 避免 rules 拒讀整個 collection
  //   (同 _fbCountMyUnreadBugReplies, 原本撈全部會被 rules 擋下)
  window._fbListBugReportsByUid = async function(uid) {
    if(!uid) {
      // 沒登入時改用 _fbUser
      uid = (window._fbUser && window._fbUser.uid) || null;
    }
    if(!uid) return [];
    try {
      // ★ v3.4.16 — server 端過濾 uid, 避開 rules 拒讀
      const _q = query(collection(_fbDb, 'bugReports'), where('uid', '==', uid));
      const snap = await getDocs(_q);
      const list = [];
      snap.forEach(d => {
        const v = d.data() || {};
        list.push({
          id: v.id || d.id,
          email: v.email || '',
          uid: v.uid || '',
          content: v.content || '',
          _userText: v._userText || '',  // ★ v3.4.6 — 玩家原始文字(短版),沒有就拿 content
          page: v.page || '',
          version: v.version || '',
          timestamp: v.timestamp || 0,
          time: v.timestamp ? new Date(v.timestamp).toLocaleString('zh-TW') : '?',
          adminReply: v.adminReply || '',
          adminReplyTs: v.adminReplyTs || 0,
          adminReplyTime: v.adminReplyTs ? new Date(v.adminReplyTs).toLocaleString('zh-TW') : '',
        });
      });
      // 最新在前
      list.sort((a, b) => (b.timestamp || 0) - (a.timestamp || 0));
      return list.slice(0, 50);  // 玩家只顯示自己最新 50 筆
    } catch(e) {
      console.error('[BUG 回報] 玩家查詢自己的清單失敗', e);
      return [];
    }
  };


  // ════════════════════════════════════════════════════════════════════
  // ★ v5.100.0（老師 2026-08-21 裁定甲）— 讀回「召喚卷確認」問卷的學生回覆
  // ────────────────────────────────────────────────────────────────────
  // 用途：資產對帳稽核卡要在**同一列**顯示「這位學生回答了什麼」，
  //   老師才不必在「🐛 錯誤回報」與「🔎 資產對帳稽核」兩張卡之間來回比對學號。
  //
  // ★ 走 server 端 where(page) 過濾：單一等值條件、無 orderBy ⇒ **不需要複合索引**，
  //   也不會把整個 bugReports 拉下來。
  // ★ 一位學生可能回覆多次（例如先申訴、後來又補一筆）→ 只留**最新一筆**，
  //   但把總筆數帶出來，老師看得到「他回了 2 次」。
  // ════════════════════════════════════════════════════════════════════
  window._fbAdminLoadAssetSelfCheckReplies = async function(){
    var _map = {};
    try{
      var _q = query(collection(_fbDb, 'bugReports'), where('page', '==', 'asset_self_check'));
      var _snap = await getDocs(_q);
      _snap.forEach(function(d){
        var v = d.data() || {};
        var _uid = String(v.uid || '');
        if(!_uid) return;
        var _sc = (v._assetSelfCheck && typeof v._assetSelfCheck === 'object') ? v._assetSelfCheck : {};
        var _rec = {
          id:        String(v.id || d.id || ''),
          at:        (parseInt(v.timestamp, 10) || 0),
          answers:   (Array.isArray(_sc.answers) ? _sc.answers : []),
          disputed:  (_sc.disputed ? 1 : 0),
          note:      String(_sc.note || v._userText || ''),
          replied:   !!(v.adminReply && String(v.adminReply).trim()),
          replyAt:   (parseInt(v.adminReplyTs, 10) || 0),
          n:         1
        };
        var _old = _map[_uid];
        if(!_old){ _map[_uid] = _rec; return; }
        _rec.n = (_old.n || 1) + 1;
        // 只留最新一筆（舊的那筆只貢獻計數）
        _map[_uid] = (_rec.at >= (_old.at || 0)) ? _rec : (function(){ _old.n = _rec.n; return _old; })();
      });
    }catch(e){
      console.warn('[召喚卷確認 回覆讀取失敗]', e);
      throw e;
    }
    return _map;
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v5.203.0（老師 2026-09-15 回報：「召喚券確認視窗輸入完也處理的學生，
  //   登入後還是會不斷重複出現此視窗，尤其是共用平板」）
  // ────────────────────────────────────────────────────────────────────
  // 【根因】「這位學生已經回答過了」這件事，過去只記在兩個都靠不住的地方：
  //   ① 雲端那份通知文件被**學生端自己**刪掉（deleteDoc）——刪除失敗（校網不穩／
  //      送出後馬上關掉 App／還沒按「關閉」就重整）就永遠留著；
  //   ② localStorage 的 lxps_notif_hide_<id>——**只活在那一台 iPad 的那個瀏覽器**，
  //      換一台平板就完全沒有這個記號。共用平板正是每天換機器的環境。
  //   再加上後台「📨 再寄一次（勾選者）」每按一次就寫一份**新的**通知文件（新 id），
  //   舊的本機旗標對新 id 一律無效 ⇒ 已經乖乖填完的學生被一問再問。
  //
  // 【修法】把「回答過」這件事改讀**學生自己的回覆本身**：問卷送出時寫進 bugReports
  //   （page='asset_self_check'），那是雲端、跟著帳號走、換幾台平板都在。
  //   ⚠ 查詢只用 uid 一個等值條件（與 _fbListBugReportsByUid 同款），page 在前端過濾：
  //     rules 要求讀自己的才放行，多加一個等值條件還可能需要複合索引，不值得。
  //   ⚠ 讀失敗一律回 false＝寧可多問一次，也不要漏問真的還沒回答的學生。
  // ════════════════════════════════════════════════════════════════════
  var _ascAnsweredCache = null;   // { uid, val, at }
  window._fbAssetSelfCheckAnswered = async function(uid){
    if(!uid) return false;
    if(_ascAnsweredCache && _ascAnsweredCache.uid === uid
       && (Date.now() - _ascAnsweredCache.at) < 600000) return _ascAnsweredCache.val;
    try{
      const _q = query(collection(_fbDb, 'bugReports'), where('uid', '==', uid));
      const _snap = await getDocs(_q);
      let _hit = false;
      _snap.forEach(function(d){
        const v = d.data() || {};
        if(String(v.page || '') === 'asset_self_check') _hit = true;
      });
      _ascAnsweredCache = { uid: uid, val: _hit, at: Date.now() };
      return _hit;
    }catch(e){
      console.warn('[召喚卷確認] 查詢是否已回答失敗（視為尚未回答）', e);
      return false;
    }
  };
  // 學生剛送出問卷時呼叫：立刻把快取標成「已回答」，同一次登入裡若還有第二、第三封
  // 重複通知，會被靜默消化掉、不會連跳三次。
  window._fbAssetSelfCheckMarkAnswered = function(uid){
    if(!uid) return;
    _ascAnsweredCache = { uid: uid, val: true, at: Date.now() };
    _ascPendingCache = null;   /* ★ v5.204.0 一併讓「待填問卷」快取失效，收件箱重繪就會解鎖領獎鈕 */
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v5.204.0（老師需求：「自選卷確認單的登入提示改成進入冒險模式後出現在 GM 獎勵箱，
  //   要勾選完送出才能繼續領取 GM 獎勵」）— 查「這位學生還有沒有一封沒填的召喚卷確認單」
  // ────────────────────────────────────────────────────────────────────
  //   回傳 { id, data } 或 null。先問 v5.203.0 的「已回答」憑據，已回答一律回 null。
  //   ⚠ 讀取失敗一律回 null（＝不擋領獎）：問卷再重要，也不該因為一次網路不順就把學生
  //     該領的獎勵鎖住；漏擋一次頂多是少問一次，鎖住獎勵卻會讓學生當場卡死。
  //   ⚠ 同時有多封（後台重寄過）時取**最早**那一封，內容一樣、填一封就全部解除。
  // ════════════════════════════════════════════════════════════════════
  var _ascPendingCache = null;   // { uid, val, at }
  window._fbAssetSelfCheckPending = async function(uid, force){
    if(!uid) return null;
    if(!force && _ascPendingCache && _ascPendingCache.uid === uid
       && (Date.now() - _ascPendingCache.at) < 60000) return _ascPendingCache.val;
    var _out = null;
    try{
      var _done = await window._fbAssetSelfCheckAnswered(uid);
      if(_done){ _ascPendingCache = { uid: uid, val: null, at: Date.now() }; return null; }
      const _snap = await getDocs(collection(_fbDb, 'pendingAdminNotifications', uid, 'items'));
      var _best = null;
      _snap.forEach(function(d){
        var v = d.data() || {};
        if(v.type !== 'assetSelfCheck' || v.read === true) return;
        var _at = parseInt(v.createdAt, 10) || 0;
        if(!_best || _at < _best.at) _best = { id: d.id, data: v, at: _at };
      });
      _out = _best ? { id: _best.id, data: _best.data } : null;
    }catch(e){
      console.warn('[召喚卷確認] 查待填問卷失敗（視為沒有，不擋領獎）', e);
      _out = null;
    }
    _ascPendingCache = { uid: uid, val: _out, at: Date.now() };
    return _out;
  };
  window._fbAssetSelfCheckClearCache = function(){ _ascPendingCache = null; _ascAnsweredCache = null; };

  // 發送 GM 公告
  window._fbSendGmBroadcast = async function(payload) {
    try {
      const broadcastObj = {
        id: 'gm_' + Date.now() + '_' + Math.random().toString(36).slice(2,8),
        text: (payload && payload.text) || '',
        color: (payload && payload.color) || '#ffcc44',
        type: (payload && payload.type) || 'marquee',
        durationMs: (payload && payload.durationMs) || 8000,
        sender: (payload && payload.sender) || 'admin',
        timestamp: Date.now(),
      };
      await setDoc(doc(_fbDb, 'stats', 'global'), { gmBroadcast: broadcastObj }, { merge: true });
      console.info('✅ [GM 公告] 已發送', broadcastObj);
      return broadcastObj;
    } catch(e) {
      console.error('❌ [GM 公告] 發送失敗', e);
      throw e;
    }
  };

  // 監聽 GM 公告變更
  window._fbWatchGmBroadcast = function(callback) {
    try {
      return onSnapshot(doc(_fbDb, 'stats', 'global'), (snap) => {
        if(!snap.exists()) return;
        const _b = snap.data().gmBroadcast;
        if(_b && _b.id) callback(_b);
      }, (err) => {
        console.warn('[GM 公告] 監聽錯誤', err);
      });
    } catch(e) {
      console.error('[GM 公告] 啟動監聽失敗', e);
      return () => {};
    }
  };

  // ★ v1.1.0 ── GM 公告「我知道了」回應系統 ──
  //   玩家點「我知道了」會寫入 stats/global.gmAck.{broadcastId}.{uid} = {name, timestamp}
  //   管理員可用 _fbGetGmAckList(broadcastId) 查詢回應名單
  window._fbAckGmBroadcast = async function(broadcastId, uid, name) {
    if(!broadcastId || !uid) return;
    try {
      const ackData = {};
      ackData['gmAck.' + broadcastId + '.' + uid] = {
        name: name || '匿名',
        timestamp: Date.now(),
      };
      await setDoc(doc(_fbDb, 'stats', 'global'), ackData, { merge: true });
      console.info('✅ [GM 公告回應] 已記錄 uid=' + uid + ' name=' + name);
    } catch(e) {
      console.warn('[GM 公告回應] 寫入失敗', e);
    }
  };

  // 管理員查詢 GM 公告回應名單
  window._fbGetGmAckList = async function(broadcastId) {
    try {
      const snap = await getDoc(doc(_fbDb, 'stats', 'global'));
      if(!snap.exists()) return { count: 0, list: [] };
      const ack = (snap.data().gmAck || {})[broadcastId] || {};
      const list = Object.entries(ack).map(([uid, info]) => ({
        uid,
        name: info.name || '匿名',
        timestamp: info.timestamp || 0,
        time: new Date(info.timestamp || 0).toLocaleString('zh-TW'),
      })).sort((a, b) => a.timestamp - b.timestamp);
      return { count: list.length, list };
    } catch(e) {
      console.error('[GM 公告回應] 查詢失敗', e);
      return { count: 0, list: [] };
    }
  };

  // ★ v1.1.0 ── 線上人數系統(Presence)──
  //   每位登入玩家在 presences/{uid} 寫入 {name, lastHeartbeat}
  //   每 30 秒更新 lastHeartbeat
  //   首頁讀取所有 presences,過濾 60 秒內活躍的計數
  //   登出 / 關閉頁面時刪除自己的 presence
  window._presenceHeartbeat = null;
  window._presenceUnsubscribe = null;

  // ★ v3.32.0 — 目前狀況判定(presence.scene·供 GM 線上玩家列表顯示)
  //   沿用 v3.17.1 _lxpsBusyNoReload 同款判定來源;純字串顯示用,判不到=「🏠 首頁/選單」
  window._presenceSceneNow = function(){
    try{
      if(window._wbInWorldBossMode) return '🐲 世界BOSS戰';
      if(window._wbSoloPracticeMode) return '🐲 龍王單人練習';
      if(typeof _isInBattleNow === 'function' && _isInBattleNow()) return '⚔️ 戰鬥中';
      if(document.getElementById('king-question-popup')
         || document.getElementById('king-start-popup')
         || document.getElementById('king-challenge-popup')) return '👑 知識王挑戰';
      const _sumEl = document.getElementById('summon-overlay');
      if(_sumEl && _sumEl.style.display !== 'none' && _sumEl.offsetParent !== null) return '✨ 召喚星空';
      const _advEl = document.getElementById('adventure-overlay');
      if(_advEl && _advEl.style.display !== 'none') return '🗺️ 關卡選擇頁';
    }catch(_){}
    return '🏠 首頁/選單';
  };

  // 啟動 presence(玩家登入後呼叫)
  window._fbStartPresence = async function(uid, name) {
    if(!uid) return;
    // ★ v1.1.0 — 緊急修復:Firestore 配額爆掉時,完全停用 presence 以保住主要存檔功能
    //   若管理員想暫時關閉 presence,可在 console 執行 window._presenceDisabled = true
    if(window._presenceDisabled){
      console.info('[Presence] 已被手動停用,跳過');
      return;
    }
    try {
      // 先寫入自己的 presence
      // ★ v3.32.0 — 加 loginAt(本次登入時間·僅啟動時寫一次)+ scene(目前狀況)供 GM 線上玩家列表
      await setDoc(doc(_fbDb, 'presences', uid), {
        name: name || '匿名',
        lastHeartbeat: Date.now(),
        loginAt: Date.now(),
        scene: (typeof window._presenceSceneNow === 'function') ? window._presenceSceneNow() : '',
      });
      console.info('✅ [Presence] 已啟動 uid=' + uid);
      // ★ v3.33.0 — 好友清單「最近登入時間 / 離線時間」用:把 lastLoginAt/lastSeenAt 寫進
      //   玩家自己的 players/{uid} 主檔(self-write 非停權欄位·免改 firestore.rules·非存檔白名單
      //   欄位不進載入路徑)。presence 於登出/關頁會刪除,offline 好友讀不到 presence,故改由好友
      //   讀取對方 players 主檔(_fbLoadFriend 已回傳整份)取得這兩個時間戳。
      try{
        const _tnow = Date.now();
        window._lxpsLastSeenMirrorAt = _tnow;
        setDoc(doc(_fbDb, 'players', uid), { lastLoginAt: _tnow, lastSeenAt: _tnow }, { merge: true }).catch(function(){});
      }catch(_){}
      // ★ v1.1.0 — 心跳間隔調整為 1 分鐘(在 200~300 學生規模下平衡即時性與配額)
      //   配合 _fbWatchOnlineCount 的活躍判定 = 1 分鐘
      if(window._presenceHeartbeat) clearInterval(window._presenceHeartbeat);
      window._presenceHeartbeat = setInterval(async () => {
        try {
          await setDoc(doc(_fbDb, 'presences', uid), {
            name: name || '匿名',
            lastHeartbeat: Date.now(),
            scene: (typeof window._presenceSceneNow === 'function') ? window._presenceSceneNow() : '',
          }, { merge: true });
          // ★ v3.33.0 — 把 lastSeenAt 鏡射進 players 主檔(節流:每 4 分鐘才寫一次·非每次心跳),
          //   讓好友清單即使對方是「直接關頁/當掉」也能顯示接近正確的離線時間(誤差 ≤4 分鐘)。
          try{
            const _n2 = Date.now();
            if(!window._lxpsLastSeenMirrorAt || (_n2 - window._lxpsLastSeenMirrorAt) > 240000){
              window._lxpsLastSeenMirrorAt = _n2;
              setDoc(doc(_fbDb, 'players', uid), { lastSeenAt: _n2 }, { merge: true }).catch(function(){});
            }
          }catch(_){}
        } catch(_){}
      }, 60000); // 60000ms = 1 分鐘
    } catch(e) {
      console.warn('[Presence] 啟動失敗(可能是 Firestore 配額或權限問題,已忽略):', e && e.message);
    }
  };

  // 停止 presence(登出 / 關閉頁面)
  window._fbStopPresence = async function(uid) {
    try {
      if(window._presenceHeartbeat){
        clearInterval(window._presenceHeartbeat);
        window._presenceHeartbeat = null;
      }
      if(window._presenceUnsubscribe){
        window._presenceUnsubscribe();
        window._presenceUnsubscribe = null;
      }
      if(uid){
        // ★ v3.33.0 — 登出/關頁前先把離線時間鏡射進 players 主檔(best-effort·好友清單顯示離線時間用)
        try{ setDoc(doc(_fbDb, 'players', uid), { lastSeenAt: Date.now() }, { merge: true }).catch(function(){}); }catch(_){}
        // 嘗試刪除自己的 presence(可能因權限或網路失敗,沒關係)
        await deleteDoc(doc(_fbDb, 'presences', uid));
      }
    } catch(_){}
  };

  // 監聽當前線上人數(60 秒內活躍)
  // ★ v5.235.0(2026-09-25・老師截圖荒島 429「Quota exceeded」根治)— 這裡原本是「每台裝置都 onSnapshot 監聽整個 presences 集合」,
  //   而每位登入玩家每 1 分鐘寫一次心跳 ⇒ 任何一個人的心跳都會推送給每一台正在監聽的裝置,每次推送算 1 次讀取:
  //   同時在線 N 人 ⇒ 每分鐘約 N×N 次讀取(30 人≈900 次/分、75 人≈5600 次/分),再加上每次開機先整包讀一次集合(含登出/當機殘留的舊文件)。
  //   Firebase 免費方案每天只有 5 萬次讀取,上課不到一小時就用完 ⇒ 全專案(大對抗/小遊戲/荒島)一起 429,荒島存檔就是這樣失敗的。
  //   修法:改成「每 2 分鐘問一次伺服器『最近 60 秒有心跳的有幾筆』」的計數查詢(getCountFromServer,每次查詢算 1 次讀取),
  //   背景分頁不查;回傳值維持「取消函式」,呼叫端(game_adv.js _initOnlineWatcher)不用改。單一欄位範圍條件,Firestore 內建索引即可,不必建複合索引。
  window._fbWatchOnlineCount = function(callback) {
    let _stopped = false, _t = 0;
    const _once = async () => {
      if(_stopped) return;
      if(document.visibilityState === 'hidden') return;
      try {
        const _q = query(collection(_fbDb, 'presences'), where('lastHeartbeat', '>', Date.now() - 60000));
        const _snap = await getCountFromServer(_q);
        if(!_stopped) callback(_snap.data().count);
      } catch(err) {
        console.warn('[Presence] 在線人數查詢失敗', err);
        if(!_stopped) callback(null);
      }
    };
    const _onVis = () => { if(document.visibilityState === 'visible') _once(); };
    try {
      _once();
      _t = setInterval(_once, 120000);
      document.addEventListener('visibilitychange', _onVis);
    } catch(e) {
      console.error('[Presence] 啟動在線人數查詢失敗', e);
    }
    return () => { _stopped = true; if(_t) clearInterval(_t); try{ document.removeEventListener('visibilitychange', _onVis); }catch(_){} };
  };
  /* ★ v5.235.0 舊版(整集合 onSnapshot)保留備查,已不再使用:
  window._fbWatchOnlineCount_old = function(callback) {
    try {
      // 用 onSnapshot 監聽整個 presences 集合
      const _q = collection(_fbDb, 'presences');
      return onSnapshot(_q, (snap) => {
        const _now = Date.now();
        let _count = 0;
        snap.forEach(d => {
          const data = d.data();
          if(data && data.lastHeartbeat && (_now - data.lastHeartbeat) < 60000){
            _count++;
          }
        });
        // ★ v3.35.0 — 好友「登入通知」偵測已改由常駐輕量輪詢負責(見 _fbFriendNotifyPollOnce),
        //   不再掛在此整集合 onSnapshot(大集合會停止推送→通知失靈;輪詢只讀有勾通知的好友那幾筆)。
        callback(_count);
      }, (err) => {
        console.warn('[Presence] 監聽錯誤', err);
        callback(0);
      });
    } catch(e) {
      console.error('[Presence] 啟動監聽失敗', e);
      return () => {};
    }
  };
  ★ v5.235.0 舊版備查結束 */

  // 管理員工具:列出當前所有在線玩家
  // ★ v5.235.0 — 改成只查「最近 60 秒有心跳」的文件(單一欄位範圍條件),不再整包讀取 presences(含大量登出/當機殘留文件)
  window._fbGetOnlineList = async function() {
    try {
      const _snap = await getDocs(query(collection(_fbDb, 'presences'), where('lastHeartbeat', '>', Date.now() - 60000)));
      const _now = Date.now();
      const list = [];
      _snap.forEach(d => {
        const data = d.data();
        if(data && data.lastHeartbeat && (_now - data.lastHeartbeat) < 60000){
          list.push({
            uid: d.id,
            name: data.name || '匿名',
            lastSeen: new Date(data.lastHeartbeat).toLocaleString('zh-TW'),
            secAgo: Math.floor((_now - data.lastHeartbeat) / 1000),
            loginAt: data.loginAt || 0,     // ★ v3.32.0 — 本次登入時間
            scene: data.scene || '',        // ★ v3.32.0 — 目前狀況
          });
        }
      });
      list.sort((a, b) => a.secAgo - b.secAgo);
      return { count: list.length, list };
    } catch(e) {
      console.error('[Presence] 查詢失敗', e);
      return { count: 0, list: [] };
    }
  };

  // ════════════════════════════════════════════════════════════════
  // ★ v3.32.0 — GM 線上玩家查詢 + GM↔玩家 雙向訊息(老師需求)
  // ────────────────────────────────────────────────────────────────
  //   入口:首頁「🔴 目前在線」點擊(僅管理員有反應·學生點了無事發生)。
  //   資料:presences(在線清單·loginAt/scene)+ players/{uid} 主檔欄位:
  //     _gmChat      = { msg, at, by }         — GM 寫(isAdmin 全權路徑·updateDoc 單欄不碰存檔)
  //     _gmChatReply = { msg, at, chatAt }     — 玩家寫自己(self-write 非停權欄位·免改 rules)
  //   玩家端即時彈窗:掛在既有 players/{uid} onSnapshot(_checkSnapData)偵測 _gmChat.at 比
  //   已讀基線新 → _gmChatIncoming 彈窗+可回覆;已讀基線存 localStorage _lxpsGmChatSeen(綁 @@uid)。
  //   ⚠ 安全:兩欄位皆非存檔白名單資料·gameCloudSave set(merge:true) 不會清掉;絕不觸碰任何權威存檔欄位。
  // ════════════════════════════════════════════════════════════════

  // GM → 玩家:發送訊息(只寫 _gmChat 一欄)
  window._fbGmSendChat = async function(uid, msg){
    if(!uid || !msg) return false;
    try{
      const _by = (window._fbUser && window._fbUser.email) || 'GM';
      await updateDoc(doc(_fbDb, 'players', uid), {
        _gmChat: { msg: String(msg).slice(0, 500), at: Date.now(), by: _by }
      });
      return true;
    }catch(e){ console.warn('[GM訊息] 發送失敗', e); return false; }
  };

  // 玩家 → GM:回覆(只寫自己主檔 _gmChatReply 一欄)
  window._fbSendGmChatReply = async function(msg){
    const _uid = window._gUserId || (window._fbUser && window._fbUser.uid);
    if(!_uid || !msg) return false;
    try{
      await updateDoc(doc(_fbDb, 'players', _uid), {
        _gmChatReply: { msg: String(msg).slice(0, 500), at: Date.now(), chatAt: window._gmChatCurAt || 0 }
      });
      return true;
    }catch(e){ console.warn('[GM訊息] 回覆失敗', e); return false; }
  };

  // GM:讀單一玩家摘要(email/暱稱/會員資料/最新回覆)
  window._fbGmReadPlayerBrief = async function(uid){
    try{
      const _s = await getDoc(doc(_fbDb, 'players', uid));
      if(!_s || !_s.exists()) return null;
      const d = _s.data() || {};
      return {
        uid, email: d.email || '', nickname: d.displayName || d.playerNickname || '',
        memberProfile: d.memberProfile || null,
        gmChat: d._gmChat || null, gmChatReply: d._gmChatReply || null,
      };
    }catch(e){ console.warn('[GM線上列表] 讀玩家失敗', uid, e); return null; }
  };

  // 玩家端:收到 GM 訊息彈窗(可回覆)。由 _checkSnapData hook 呼叫。
  window._gmChatIncoming = function(chat){
    try{
      if(!chat || !chat.msg) return;
      window._gmChatCurAt = chat.at || 0;
      // 立即標記已讀(記憶體+localStorage 綁 uid 命名空間),避免每次 snapshot 重複彈
      window._gmChatSeenAt = chat.at || Date.now();
      try{ localStorage.setItem('_lxpsGmChatSeen', String(window._gmChatSeenAt)); }catch(_){}
      const _old = document.getElementById('_gm-chat-ov');
      if(_old) _old.remove();
      const _esc = s => String(s||'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
      const _timeStr = chat.at ? new Date(chat.at).toLocaleString('zh-TW') : '';
      const ov = document.createElement('div');
      ov.id = '_gm-chat-ov';
      ov.style.cssText = 'position:fixed;inset:0;z-index:2147483200;background:rgba(0,0,0,0.72);display:flex;align-items:center;justify-content:center;padding:16px;';
      ov.innerHTML =
        '<div style="max-width:640px;width:96%;background:linear-gradient(160deg,#1c2340,#141a30);border:2px solid rgba(255,210,120,0.65);border-radius:18px;padding:22px 22px 18px;box-shadow:0 8px 40px rgba(0,0,0,0.7);font-family:\'M PLUS Rounded 1c\',sans-serif;">'
        + '<div style="font-size:26px;font-weight:900;color:#ffdd88;margin-bottom:8px;">👨‍🏫 老師傳來訊息</div>'
        + '<div style="font-size:14px;color:#99aacc;margin-bottom:12px;">' + _esc(_timeStr) + '</div>'
        + '<div style="font-size:22px;line-height:1.6;color:#fff;background:rgba(255,255,255,0.07);border-radius:12px;padding:14px 16px;margin-bottom:14px;white-space:pre-wrap;word-break:break-word;">' + _esc(chat.msg) + '</div>'
        + '<div style="font-size:16px;color:#aaddff;margin-bottom:6px;">💬 想回覆老師嗎?(選填)</div>'
        + '<textarea id="_gm-chat-reply-txt" maxlength="500" style="width:100%;box-sizing:border-box;min-height:76px;font-size:20px;padding:10px 12px;border-radius:10px;border:1px solid rgba(140,190,255,0.5);background:rgba(0,0,0,0.35);color:#fff;resize:vertical;" placeholder="輸入回覆內容…"></textarea>'
        + '<div style="display:flex;gap:10px;margin-top:14px;justify-content:flex-end;">'
        +   '<button id="_gm-chat-close" style="font-size:20px;padding:10px 22px;border-radius:12px;border:1px solid rgba(255,255,255,0.35);background:rgba(255,255,255,0.10);color:#ddd;cursor:pointer;">關閉</button>'
        +   '<button id="_gm-chat-send" style="font-size:20px;font-weight:800;padding:10px 26px;border-radius:12px;border:none;background:linear-gradient(45deg,#ffb347,#ff8844);color:#3a2000;cursor:pointer;">📨 送出回覆</button>'
        + '</div></div>';
      document.body.appendChild(ov);
      // ★ v3.33.0 — 老師訊息專屬音效(訊息.wav);找不到時退回通用確認音
      try{
        if(typeof playSfx === 'function'){
          if(document.getElementById('sfx-gm-message')) playSfx('sfx-gm-message', 0.85);
          else playSfx('sfx-confirm', 0.7);
        }
      }catch(_){}
      document.getElementById('_gm-chat-close').onclick = () => ov.remove();
      document.getElementById('_gm-chat-send').onclick = async () => {
        const _txt = (document.getElementById('_gm-chat-reply-txt').value || '').trim();
        if(!_txt){ ov.remove(); return; }
        const _btn = document.getElementById('_gm-chat-send');
        _btn.disabled = true; _btn.textContent = '送出中…';
        const _ok = await window._fbSendGmChatReply(_txt);
        ov.remove();
        try{ if(typeof _showInGameToast === 'function') _showInGameToast(_ok ? '📨 已把回覆送給老師!' : '⚠️ 回覆送出失敗,請稍後再試', _ok ? '#88dd88' : '#ffaa66', 2600); }catch(_){}
      };
    }catch(e){ console.warn('[GM訊息] 彈窗失敗', e); }
  };

  // GM:開啟線上玩家列表(首頁「目前在線」點擊·僅管理員)
  window._gmOpenOnlineList = async function(){
    try{
      if(typeof window._isAdminUser !== 'function' || !window._isAdminUser()) return;   // 學生點了無事發生
      const _old = document.getElementById('_gm-online-ov');
      if(_old) _old.remove();
      const _esc = s => String(s||'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
      const ov = document.createElement('div');
      ov.id = '_gm-online-ov';
      ov.style.cssText = 'position:fixed;inset:0;z-index:2147483100;background:rgba(0,0,0,0.78);display:flex;align-items:center;justify-content:center;padding:12px;';
      ov.innerHTML =
        '<div style="max-width:1080px;width:98%;max-height:92vh;display:flex;flex-direction:column;background:linear-gradient(160deg,#181f38,#10152a);border:2px solid rgba(120,255,170,0.55);border-radius:16px;box-shadow:0 8px 40px rgba(0,0,0,0.7);font-family:\'M PLUS Rounded 1c\',sans-serif;">'
        + '<div style="display:flex;align-items:center;gap:12px;padding:14px 18px;border-bottom:1px solid rgba(255,255,255,0.12);">'
        +   '<span style="font-size:24px;font-weight:900;color:#aaffcc;">🟢 線上玩家</span>'
        +   '<span id="_gmol-count" style="font-size:20px;color:#ffe080;font-weight:800;">讀取中…</span>'
        +   '<span style="flex:1;"></span>'
        +   '<button onclick="window._gmOpenOnlineList()" style="font-size:16px;padding:6px 14px;border-radius:9px;border:1px solid rgba(140,190,255,0.5);background:rgba(80,120,200,0.25);color:#bfe0ff;cursor:pointer;">🔄 重新整理</button>'
        +   '<button onclick="document.getElementById(\'_gm-online-ov\').remove()" style="font-size:16px;padding:6px 14px;border-radius:9px;border:1px solid rgba(255,255,255,0.35);background:rgba(255,255,255,0.10);color:#ddd;cursor:pointer;">✕ 關閉</button>'
        + '</div>'
        + '<div id="_gmol-body" style="flex:1;overflow-y:auto;padding:12px 16px 18px;-webkit-overflow-scrolling:touch;font-size:15px;color:#ccc;">讀取中…</div>'
        + '</div>';
      document.body.appendChild(ov);
      const _r = await window._fbGetOnlineList();
      const _cntEl = document.getElementById('_gmol-count');
      const _body = document.getElementById('_gmol-body');
      if(!_cntEl || !_body) return;   // 已被關閉
      _cntEl.textContent = '共 ' + _r.count + ' 人在線';
      if(!_r.list.length){ _body.innerHTML = '<div style="padding:30px;text-align:center;color:#889;">目前沒有在線玩家</div>'; return; }
      // 逐人補讀 players 主檔(email/暱稱/會員資料/回覆·GM 讀權限·上限 150 防爆量)
      const _rows = _r.list.slice(0, 150);
      const _briefs = await Promise.all(_rows.map(p => window._fbGmReadPlayerBrief(p.uid)));
      window._gmolBriefs = {};
      let _html = '<table style="width:100%;border-collapse:collapse;font-size:15px;">'
        + '<tr style="color:#88ddff;text-align:left;border-bottom:1px solid rgba(255,255,255,0.18);">'
        + '<th style="padding:6px 8px;">玩家</th><th style="padding:6px 8px;">登入時間</th><th style="padding:6px 8px;">目前狀況</th><th style="padding:6px 8px;">最近活動</th><th style="padding:6px 8px;">操作</th></tr>';
      _rows.forEach((p, i) => {
        const b = _briefs[i];
        if(b) window._gmolBriefs[p.uid] = b;
        let _label = p.name;
        try{
          if(b && b.email && typeof window._getAdminPlayerLabel === 'function'){
            _label = window._getAdminPlayerLabel(b.email, b.nickname || p.name) || p.name;
          } else if(b && b.email){ _label = b.email.split('@')[0] + '·' + p.name; }
        }catch(_){}
        const _loginStr = p.loginAt ? new Date(p.loginAt).toLocaleTimeString('zh-TW', {hour:'2-digit', minute:'2-digit'}) : '—';
        const _hasReply = !!(b && b.gmChatReply && b.gmChatReply.msg);
        _html += '<tr style="border-bottom:1px solid rgba(255,255,255,0.08);">'
          + '<td style="padding:7px 8px;color:#fff;font-weight:700;">' + _esc(_label) + (_hasReply ? ' <span style="color:#ffe080;">💬</span>' : '') + '</td>'
          + '<td style="padding:7px 8px;color:#aaddff;">' + _esc(_loginStr) + '</td>'
          + '<td style="padding:7px 8px;color:#aaffcc;">' + _esc(p.scene || '—') + '</td>'
          + '<td style="padding:7px 8px;color:#99aabb;">' + p.secAgo + ' 秒前</td>'
          + '<td style="padding:7px 8px;white-space:nowrap;">'
          +   '<button onclick="window._gmolShowProfile(\'' + p.uid + '\')" style="font-size:14px;padding:4px 10px;margin-right:6px;border-radius:8px;border:1px solid rgba(140,190,255,0.5);background:rgba(80,120,200,0.2);color:#bfe0ff;cursor:pointer;">📋 會員資料</button>'
          +   '<button onclick="window._gmolSendMsg(\'' + p.uid + '\')" style="font-size:14px;padding:4px 10px;margin-right:6px;border-radius:8px;border:1px solid rgba(255,200,120,0.55);background:rgba(255,170,60,0.18);color:#ffe0a0;cursor:pointer;">✉️ 傳訊</button>'
          +   (_hasReply ? '<button onclick="window._gmolShowReply(\'' + p.uid + '\')" style="font-size:14px;padding:4px 10px;border-radius:8px;border:1px solid rgba(120,255,170,0.55);background:rgba(80,220,140,0.18);color:#aaffcc;cursor:pointer;">💬 看回覆</button>' : '')
          + '</td></tr>';
      });
      _html += '</table>';
      if(_r.list.length > 150) _html += '<div style="padding:8px;color:#889;">(僅顯示前 150 人)</div>';
      _body.innerHTML = _html;
    }catch(e){ console.warn('[GM線上列表] 失敗', e); }
  };

  // GM 子彈窗共用殼
  window._gmolSubPopup = function(title, bodyHtml){
    const _old = document.getElementById('_gmol-sub');
    if(_old) _old.remove();
    const s = document.createElement('div');
    s.id = '_gmol-sub';
    s.style.cssText = 'position:fixed;inset:0;z-index:2147483150;background:rgba(0,0,0,0.6);display:flex;align-items:center;justify-content:center;padding:16px;';
    s.innerHTML = '<div style="max-width:560px;width:95%;max-height:85vh;overflow-y:auto;background:#1a2138;border:2px solid rgba(140,190,255,0.55);border-radius:14px;padding:18px;font-family:\'M PLUS Rounded 1c\',sans-serif;">'
      + '<div style="font-size:21px;font-weight:900;color:#bfe0ff;margin-bottom:10px;">' + title + '</div>'
      + '<div style="font-size:17px;line-height:1.6;color:#eee;">' + bodyHtml + '</div>'
      + '<div style="text-align:right;margin-top:14px;"><button onclick="document.getElementById(\'_gmol-sub\').remove()" style="font-size:17px;padding:7px 20px;border-radius:10px;border:1px solid rgba(255,255,255,0.35);background:rgba(255,255,255,0.10);color:#ddd;cursor:pointer;">關閉</button></div>'
      + '</div>';
    s.onclick = (e) => { if(e.target === s) s.remove(); };
    document.body.appendChild(s);
  };

  // GM:查看會員資料
  window._gmolShowProfile = function(uid){
    const _esc = s => String(s||'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
    const b = (window._gmolBriefs || {})[uid];
    if(!b){ window._gmolSubPopup('📋 會員資料', '讀取不到玩家主檔'); return; }
    // ★ v5.212.0 主檔寫不進去的學生,會員資料在 players/{uid}/saves/memberProfile ⇒ GM 也讀得到
    if(!b.memberProfile && !b._mpSubTried && window._fbFns && window._fbDb){
      b._mpSubTried = true;
      window._fbFns.getDoc(window._fbFns.doc(window._fbDb, 'players', uid, 'saves', 'memberProfile'))
        .then(function(s){ try{ if(s.exists() && s.data().memberProfile){ b.memberProfile = s.data().memberProfile; } }catch(_){} window._gmolShowProfile(uid); })
        .catch(function(){ window._gmolShowProfile(uid); });
      return;
    }
    const mp = b.memberProfile || {};
    const _row = (k, v) => '<div style="margin-bottom:6px;"><span style="color:#88ddff;">' + k + ':</span> ' + (_esc(v) || '<span style="color:#667;">未填</span>') + '</div>';
    let _h = _row('Email', b.email) + _row('遊戲暱稱', b.nickname) + _row('UID', uid.slice(0, 16) + '…');
    _h += '<div style="border-top:1px solid rgba(255,255,255,0.15);margin:10px 0;padding-top:10px;color:#ffe080;font-weight:800;">會員資料</div>';
    if(!b.memberProfile){ _h += '<div style="color:#889;">此玩家尚未填寫會員資料</div>'; }
    else{
      _h += _row('會員暱稱', mp.nickname)
          + _row('聯絡 E-mail', mp.email)
          + _row('玩家身分', Array.isArray(mp.identities) ? mp.identities.join('、') : mp.identities)
          + _row('出生年', mp.birthYear)
          + _row('在學年級', mp.grade)
          + _row('性別', mp.gender)
          + _row('主要平台', Array.isArray(mp.platforms) ? mp.platforms.join('、') : mp.platforms);
    }
    window._gmolSubPopup('📋 會員資料', _h);
  };

  // GM:傳訊給該玩家(玩家端 players/{uid} onSnapshot 即時彈窗+可回覆)
  window._gmolSendMsg = async function(uid){
    const _msg = prompt('✉️ 輸入要傳給這位玩家的訊息(即時彈窗顯示·玩家可回覆):');
    if(_msg === null) return;
    const _t = String(_msg).trim();
    if(!_t) return;
    const _ok = await window._fbGmSendChat(uid, _t);
    window._gmolSubPopup('✉️ GM 訊息', _ok ? '✅ 已送出!玩家在線時會立即彈窗顯示。<br>玩家的回覆會出現在列表的「💬 看回覆」(按 🔄 重新整理更新)。' : '❌ 送出失敗,請稍後再試。');
  };

  // GM:查看玩家最新回覆
  window._gmolShowReply = function(uid){
    const _esc = s => String(s||'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
    const b = (window._gmolBriefs || {})[uid];
    const r = b && b.gmChatReply;
    if(!r || !r.msg){ window._gmolSubPopup('💬 玩家回覆', '此玩家目前沒有回覆(按 🔄 重新整理更新)'); return; }
    const _q = b.gmChat && b.gmChat.msg ? '<div style="color:#99aabb;font-size:14px;margin-bottom:8px;">你上次傳的:「' + _esc(String(b.gmChat.msg).slice(0, 80)) + '」</div>' : '';
    window._gmolSubPopup('💬 玩家回覆',
      _q + '<div style="background:rgba(255,255,255,0.07);border-radius:10px;padding:12px 14px;white-space:pre-wrap;word-break:break-word;">' + _esc(r.msg) + '</div>'
      + '<div style="color:#889;font-size:14px;margin-top:8px;">' + (r.at ? new Date(r.at).toLocaleString('zh-TW') : '') + '</div>');
  };

  // ★ 管理員工具：手動設定 totalPlayers 絕對值（當 _fbBackfillTotalPlayers 因權限失敗時的替代方案）
  //   此函式直接寫 stats/global.totalPlayers，不需要讀取 /players 集合，可避開掃描權限問題
  window._fbSetTotalPlayers = async function(newTotal) {
    try {
      const n = Math.max(0, parseInt(newTotal, 10) || 0);
      const statsRef = doc(_fbDb, 'stats', 'global');
      await setDoc(statsRef, { totalPlayers: n, _manualSetAt: Date.now() }, { merge: true });
      console.info('✅ [管理] totalPlayers 已手動設為 ' + n);
      return n;
    } catch(e) {
      console.error('❌ [管理] 設定失敗', e);
      throw e;
    }
  };

  // ★ v1.0.20260419.1930 ── 玩家資料急救工具 ──
  //   診斷:讀取指定 uid 的雲端資料,回傳結構分析(哪些欄位有、哪些是空的、是否可疑)
  window._fbDiagnosePlayer = async function(uid) {
    if(!uid) throw new Error('必須提供 uid');
    const snap = await getDoc(doc(_fbDb, 'players', uid));
    if(!snap.exists()) return { exists: false, uid };
    const d = snap.data() || {};
    const heroLevelsCount = Object.keys(d.heroLevels || {}).length;
    const backpackCount = Object.keys(d.playerBackpack || {}).length;
    const coins = d.knowledgeCoins || 0;
    const unlockedCount = (d.unlockedHeroes || []).length;
    const hasRepHero = !!(d.representativeHero && d.representativeHero.name);
    // 可疑空白判定:有代表英雄或解鎖多位英雄但主要資料空白
    const looksDamaged = (hasRepHero || unlockedCount > 10) &&
                         heroLevelsCount === 0 && backpackCount === 0 && coins === 0;
    return {
      exists: true, uid,
      savedAt: d.savedAt || 0,
      email: d.email || '',
      displayName: d.displayName || '',
      knowledgeCoins: coins,
      heroLevelsCount,
      backpackCount,
      unlockedCount,
      hasRepHero,
      representativeHero: d.representativeHero || null,
      unlockedHeroes: d.unlockedHeroes || [],
      looksDamaged,
      rawData: d  // 提供完整資料供後續處理
    };
  };

  // 還原:把提供的 payload merge 進 players/{uid}(保留現有欄位)
  window._fbRestorePlayer = async function(uid, payload) {
    if(!uid || !payload) throw new Error('uid 與 payload 為必要');
    // 用 setDoc + merge:true 保留未提及的欄位(如 _countedInStats、email、displayName 等)
    await setDoc(doc(_fbDb, 'players', uid), payload, { merge: true });
    console.info('✅ [急救] 已還原玩家資料', uid, payload);
    return true;
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.5.37 — Lv1 救援深度診斷工具(學生雲端資料被空白覆蓋的最後一哩救援)
  // ────────────────────────────────────────────────────────────────────
  // 背景:老師回報「有學生帳號變回 Lv1」,原有的 _fbDiagnosePlayer 只看主文件
  //       players/{uid},沒去讀 saves/{live,safe} 子集合,所以看不到備份槽
  //       可能還活著的舊資料。v3.5.37 補上這個盲點。
  //
  // 核心概念:Firestore 路徑有三個地方藏著玩家資料,任何一個還有救就能救:
  //   ① players/{uid}            ← 主文件(_fbDiagnosePlayer 看的這個)
  //   ② players/{uid}/saves/live ← live 槽(每次 gameCloudSave 寫入)
  //   ③ players/{uid}/saves/safe ← safe 槽(5 分鐘定時備份,只在資料更健康時才覆蓋)
  //
  // safe 槽是救命槽,因為它有「健康才覆蓋」的保護;
  // 如果學生資料是被「下線/連線/閃退」造成 live 寫入空白,safe 槽通常還是好的。
  // ════════════════════════════════════════════════════════════════════

  // 1) 深度診斷:讀取三個地方,回傳完整摘要
  window._fbDiagnoseAllSlots = async function(uid) {
    if(!uid) throw new Error('必須提供 uid');
    const result = {
      uid,
      mainDoc: null,   // players/{uid}
      liveSlot: null,  // players/{uid}/saves/live
      safeSlot: null,  // players/{uid}/saves/safe
      recommendation: null  // 'use_safe' / 'use_live' / 'use_main' / 'all_damaged'
    };

    // 讀主文件
    try {
      const snap = await getDoc(doc(_fbDb, 'players', uid));
      if(snap.exists()){
        const d = snap.data() || {};
        result.mainDoc = {
          exists: true,
          savedAt: d.savedAt || 0,
          email: d.email || '',
          displayName: d.displayName || '',
          knowledgeCoins: d.knowledgeCoins || 0,
          heroLevelsCount: Object.keys(d.heroLevels || {}).length,
          maxHeroLv: Math.max(0, ...Object.values(d.heroLevels || {0:0})),
          totalHeroLv: Object.values(d.heroLevels || {}).reduce((a,b)=>a+b, 0),
          unlockedCount: (d.unlockedHeroes || []).length,
          backpackCount: Object.keys(d.playerBackpack || {}).length,
          medalsCount: Object.keys(d.playerMedals || {}).length,
          hasRepHero: !!(d.representativeHero && d.representativeHero.name),
          representativeHero: d.representativeHero || null,
          rawData: d
        };
      } else {
        result.mainDoc = { exists: false };
      }
    } catch(e){
      result.mainDoc = { exists: false, error: e.message || String(e) };
    }

    // 讀 live 槽
    try {
      const snap = await getDocFromServer(doc(_fbDb, 'players', uid, 'saves', 'live'));
      if(snap.exists()){
        const d = snap.data() || {};
        const summary = d._dataSummary || null;
        result.liveSlot = {
          exists: true,
          savedAt: d.savedAt || 0,
          summary: summary,
          knowledgeCoins: (summary && summary.coins) || d.knowledgeCoins || 0,
          maxHeroLv: (summary && summary.maxHeroLv) || Math.max(0, ...Object.values(d.heroLevels || {0:0})),
          totalHeroLv: (summary && summary.totalHeroLv) || Object.values(d.heroLevels || {}).reduce((a,b)=>a+b, 0),
          unlockedCount: (summary && summary.unlockedCount) || (d.unlockedHeroes || []).length,
          bagItems: (summary && summary.bagItems) || Object.keys(d.playerBackpack || {}).length,
          score: window._summaryRichnessScore ? window._summaryRichnessScore(summary) : -1,
          rawData: d
        };
      } else {
        result.liveSlot = { exists: false };
      }
    } catch(e){
      result.liveSlot = { exists: false, error: e.message || String(e) };
    }

    // 讀 safe 槽(救命槽)
    try {
      const snap = await getDocFromServer(doc(_fbDb, 'players', uid, 'saves', 'safe'));
      if(snap.exists()){
        const d = snap.data() || {};
        const summary = d._dataSummary || null;
        result.safeSlot = {
          exists: true,
          savedAt: d.savedAt || 0,
          summary: summary,
          knowledgeCoins: (summary && summary.coins) || d.knowledgeCoins || 0,
          maxHeroLv: (summary && summary.maxHeroLv) || Math.max(0, ...Object.values(d.heroLevels || {0:0})),
          totalHeroLv: (summary && summary.totalHeroLv) || Object.values(d.heroLevels || {}).reduce((a,b)=>a+b, 0),
          unlockedCount: (summary && summary.unlockedCount) || (d.unlockedHeroes || []).length,
          bagItems: (summary && summary.bagItems) || Object.keys(d.playerBackpack || {}).length,
          score: window._summaryRichnessScore ? window._summaryRichnessScore(summary) : -1,
          rawData: d
        };
      } else {
        result.safeSlot = { exists: false };
      }
    } catch(e){
      result.safeSlot = { exists: false, error: e.message || String(e) };
    }

    // 三槽比對,給推薦
    const _score = (s) => (s && s.exists ? Math.max(0, s.score) : -1);
    const _mainScore = result.mainDoc && result.mainDoc.exists
      ? ((result.mainDoc.totalHeroLv || 0) * 10 + (result.mainDoc.unlockedCount || 0) * 1000 + (result.mainDoc.knowledgeCoins || 0))
      : -1;
    const _liveScore = _score(result.liveSlot);
    const _safeScore = _score(result.safeSlot);

    const _scores = { main: _mainScore, live: _liveScore, safe: _safeScore };
    const _bestKey = Object.keys(_scores).reduce((a, b) => _scores[a] >= _scores[b] ? a : b);

    if(_scores[_bestKey] <= 0){
      result.recommendation = 'all_damaged';
    } else {
      result.recommendation = 'use_' + _bestKey;
    }
    result.scores = _scores;
    return result;
  };

  // 2) 從指定槽還原到主文件(把 safe 槽的舊資料複製回 players/{uid} 主文件 + saves/live)
  //    這樣下次學生登入,主程式會選到救援後的版本。
  window._fbRestoreFromSlot = async function(uid, slotName) {
    if(!uid) throw new Error('必須提供 uid');
    if(slotName !== 'safe' && slotName !== 'live' && slotName !== 'main'){
      throw new Error('slotName 必須是 safe / live / main');
    }

    // 讀來源資料
    let srcData = null;
    if(slotName === 'main'){
      const snap = await getDoc(doc(_fbDb, 'players', uid));
      if(!snap.exists()) throw new Error('主文件不存在,無法當還原來源');
      srcData = snap.data();
    } else {
      const snap = await getDocFromServer(doc(_fbDb, 'players', uid, 'saves', slotName));
      if(!snap.exists()) throw new Error(slotName + ' 槽不存在,無法當還原來源');
      srcData = snap.data();
    }

    if(!srcData) throw new Error('來源資料為空');

    // 加救援標記
    srcData._rescuedAt = Date.now();
    srcData._rescuedBy = (window._fbUser && window._fbUser.email) || 'admin';
    srcData._rescuedFromSlot = slotName;

    // ★ v3.5.39 — 救援信號(寫到主文件根層,讓學生端登入時/onSnapshot 即時偵測)
    //   學生端會比對 localStorage._lastSeenRescueTs:
    //     若雲端 ts > 本地 ts → 視為「未消化的救援」→ 強清本地 + 強吃雲端
    //   ts 是救援當下時戳(唯一),version 用於日後格式變動;by 與 fromSlot 給學生顯示
    const _rescueSignal = {
      ts: srcData._rescuedAt,
      by: srcData._rescuedBy,
      fromSlot: slotName,
      version: 1
    };
    srcData._adminRescueSignal = _rescueSignal;

    // 寫入三個地方,確保下次學生登入無論選哪槽都拿到救援結果
    //   主文件:setDoc merge,保留 email/displayName 等不在 srcData 中的欄位
    //   live 槽:直接用 setDoc(繞過 _fbSaveLive 健康度守門,因為這是管理員授權的救援操作)
    //   safe 槽:不動(可能本身就是來源,動了反而會破壞)
    // ★ v3.5.37 — 標記 _adminForceRestore:true,讓任何後續邏輯知道這是管理員強制操作
    srcData._adminForceRestore = true;
    srcData._authoritativeRestoreAt = Date.now();   // ★ v3.16.5 — 過時 session 偵測後鎖存+重載,不覆蓋本次強制還原
    await setDoc(doc(_fbDb, 'players', uid), srcData, { merge: true });
    // ⚠ 直接 setDoc 寫 live 槽,不走 _fbSaveLive (避免被健康度守門擋下管理員救援)
    await setDoc(doc(_fbDb, 'players', uid, 'saves', 'live'), srcData);
    // 若救援來源是 main 或 live,把 safe 也更新(只在救援來源比 safe 健康時)
    if(slotName === 'main' || slotName === 'live'){
      try {
        if(window._fbWriteSafeIfHealthier){
          await window._fbWriteSafeIfHealthier(uid, srcData);
        }
      } catch(_eSafe){
        console.warn('[Lv1 救援] safe 槽同步寫入失敗(不影響主救援)', _eSafe);
      }
    }

    console.info('✅ [Lv1 救援 v3.5.39] 已從 ' + slotName + ' 槽還原資料到 main + live + 寫入救援信號', { uid, srcData, rescueSignal: _rescueSignal });
    return true;
  };

  // 3) 緊急工具:把 safe 槽資料**強制**還原(忽略 safe 槽是否更健康的判定)
  //    用於 safe 比較舊但 live 完全空白的情境
  window._fbForceRestoreSafe = async function(uid) {
    return window._fbRestoreFromSlot(uid, 'safe');
  };

  // 4) ★ v3.5.37 — 一鍵自動還原:讀三槽,自動選最豐富的還原
  //    這是老師要的核心功能 — 不用管理員手動判斷選哪槽,系統自動選分數最高的
  //    回傳 { ok, restoredFrom, summary, scores }
  window._fbAutoRestoreBestSlot = async function(uid) {
    if(!uid) throw new Error('必須提供 uid');
    if(!window._fbDiagnoseAllSlots) throw new Error('_fbDiagnoseAllSlots 未載入');

    const diag = await window._fbDiagnoseAllSlots(uid);

    // 三槽都空 → 沒得救
    if(diag.recommendation === 'all_damaged'){
      return {
        ok: false,
        reason: 'all_slots_damaged',
        scores: diag.scores,
        message: '三槽都看不到有效資料,無法自動救援'
      };
    }

    // 推薦的最豐富槽
    const _bestSlot = diag.recommendation.replace('use_', '');

    // 取最豐富那槽的摘要,給 UI 顯示
    let _summary = null;
    if(_bestSlot === 'safe' && diag.safeSlot) _summary = {
      knowledgeCoins: diag.safeSlot.knowledgeCoins,
      unlockedCount: diag.safeSlot.unlockedCount,
      maxHeroLv: diag.safeSlot.maxHeroLv,
      totalHeroLv: diag.safeSlot.totalHeroLv,
      bagItems: diag.safeSlot.bagItems
    };
    else if(_bestSlot === 'live' && diag.liveSlot) _summary = {
      knowledgeCoins: diag.liveSlot.knowledgeCoins,
      unlockedCount: diag.liveSlot.unlockedCount,
      maxHeroLv: diag.liveSlot.maxHeroLv,
      totalHeroLv: diag.liveSlot.totalHeroLv,
      bagItems: diag.liveSlot.bagItems
    };
    else if(_bestSlot === 'main' && diag.mainDoc) _summary = {
      knowledgeCoins: diag.mainDoc.knowledgeCoins,
      unlockedCount: diag.mainDoc.unlockedCount,
      maxHeroLv: diag.mainDoc.maxHeroLv,
      totalHeroLv: diag.mainDoc.totalHeroLv
    };

    // 執行還原(已包含 _adminForceRestore 標記,繞過 _fbSaveLive 守門)
    await window._fbRestoreFromSlot(uid, _bestSlot);

    return {
      ok: true,
      restoredFrom: _bestSlot,
      scores: diag.scores,
      summary: _summary,
      message: '已從 ' + _bestSlot + ' 槽還原(分數最高的槽)'
    };
  };

  // ★ v3.5.72 — Email 入口 console 工具(老師救學生最常用)
  //   用法(老師按 F12 開 console):
  //     await _diagAndRescueByEmail('xxx@gmail.com')  → 只看三槽診斷(不動)
  //     await _smartRescueByEmail('xxx@gmail.com')    → 自動選最豐富槽還原
  //     await _manualRescueByEmail('xxx@gmail.com', 'safe')  → 手動指定 safe/live/main
  //   背景:管理員後台 3.7 區段要輸入 uid,但老師收到的學生回報通常只有 email
  //         舊流程要先到 Firebase Console 查 uid,費時且容易抄錯
  //         加這組 API 後一行搞定
  window._diagAndRescueByEmail = async function(email){
    if(!window._fbAdminFindPlayerByEmail) throw new Error('_fbAdminFindPlayerByEmail 未載入');
    if(!window._fbDiagnoseAllSlots) throw new Error('_fbDiagnoseAllSlots 未載入');
    console.log('🔍 [_diagAndRescueByEmail] 查詢 email:', email);
    const _find = await window._fbAdminFindPlayerByEmail(email);
    if(!_find.found){
      console.warn('❌ 找不到這個 email 的玩家:', email);
      return { found: false, email };
    }
    console.log('✅ 找到玩家 uid:', _find.uid, ', 暱稱:', _find.data.displayName || '(無)');
    const _diag = await window._fbDiagnoseAllSlots(_find.uid);
    console.group('📊 [三槽診斷] uid=' + _find.uid.slice(0,8) + '... email=' + email);
    console.log('① 主文件:', _diag.mainDoc && _diag.mainDoc.exists
      ? { unlocked: _diag.mainDoc.unlockedCount, maxLv: _diag.mainDoc.maxHeroLv,
          totalLv: _diag.mainDoc.totalHeroLv, coins: _diag.mainDoc.knowledgeCoins,
          savedAt: _diag.mainDoc.savedAt ? new Date(_diag.mainDoc.savedAt).toLocaleString('zh-TW') : '無' }
      : '不存在');
    console.log('② live 槽:', _diag.liveSlot && _diag.liveSlot.exists
      ? { unlocked: _diag.liveSlot.unlockedCount, maxLv: _diag.liveSlot.maxHeroLv,
          totalLv: _diag.liveSlot.totalHeroLv, coins: _diag.liveSlot.knowledgeCoins,
          savedAt: _diag.liveSlot.savedAt ? new Date(_diag.liveSlot.savedAt).toLocaleString('zh-TW') : '無' }
      : '不存在');
    console.log('③ safe 槽:', _diag.safeSlot && _diag.safeSlot.exists
      ? { unlocked: _diag.safeSlot.unlockedCount, maxLv: _diag.safeSlot.maxHeroLv,
          totalLv: _diag.safeSlot.totalHeroLv, coins: _diag.safeSlot.knowledgeCoins,
          savedAt: _diag.safeSlot.savedAt ? new Date(_diag.safeSlot.savedAt).toLocaleString('zh-TW') : '無' }
      : '不存在');
    console.log('📊 分數:', _diag.scores);
    console.log('🎯 推薦:', _diag.recommendation);
    console.groupEnd();
    console.log('💡 下一步:呼叫 await _smartRescueByEmail("' + email + '") 自動還原最豐富槽');
    return { found: true, uid: _find.uid, email, diag: _diag };
  };

  window._smartRescueByEmail = async function(email){
    if(!window._fbAdminFindPlayerByEmail) throw new Error('_fbAdminFindPlayerByEmail 未載入');
    if(!window._fbAutoRestoreBestSlot) throw new Error('_fbAutoRestoreBestSlot 未載入');
    const _find = await window._fbAdminFindPlayerByEmail(email);
    if(!_find.found){
      console.warn('❌ 找不到這個 email 的玩家:', email);
      return { found: false, email };
    }
    console.log('🆘 [_smartRescueByEmail] 開始救援 uid:', _find.uid, ', email:', email);
    const _result = await window._fbAutoRestoreBestSlot(_find.uid);
    if(_result.ok){
      console.log('✅ 救援完成!來源:', _result.restoredFrom, ', 摘要:', _result.summary);
      console.log('📣 請告知學生 ' + email + ':重新整理頁面 / 重新登入即可看到救援結果');
    } else {
      console.error('❌ 救援失敗:', _result.reason, ', message:', _result.message);
    }
    return { ..._result, uid: _find.uid, email };
  };

  window._manualRescueByEmail = async function(email, slotName){
    if(!['main','live','safe'].includes(slotName)) throw new Error('slotName 必須是 main / live / safe');
    if(!window._fbAdminFindPlayerByEmail) throw new Error('_fbAdminFindPlayerByEmail 未載入');
    if(!window._fbRestoreFromSlot) throw new Error('_fbRestoreFromSlot 未載入');
    const _find = await window._fbAdminFindPlayerByEmail(email);
    if(!_find.found){
      console.warn('❌ 找不到這個 email 的玩家:', email);
      return { found: false, email };
    }
    console.log('🛠 [_manualRescueByEmail] 從 ' + slotName + ' 槽還原 uid:', _find.uid);
    try{
      await window._fbRestoreFromSlot(_find.uid, slotName);
      console.log('✅ 已從 ' + slotName + ' 槽還原給 ' + email);
      console.log('📣 請告知學生:重新整理頁面 / 重新登入即可看到救援結果');
      return { ok: true, uid: _find.uid, email, restoredFrom: slotName };
    }catch(e){
      console.error('❌ 還原失敗:', e);
      return { ok: false, uid: _find.uid, email, error: e.message || String(e) };
    }
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.12.13(2026-05-31)— _heroLevels 污染診斷 + 清理工具
  // ────────────────────────────────────────────────────────────────────
  // 背景:這次老師回報「世界 BOSS 戰結算誤走台灣關流程 → 發 +306 萬 EXP + +91 萬幣」
  //       事後 console 看到「[存檔保護][heroLevels merge] 從雲端新增: 101 位」
  //       表示 _heroLevels 物件有 101 個 key,但玩家英雄白名單(_PLAYER_HERO_NAMES)只有 73 位
  //       多出來的 28 個 entry 是「不該存在的英雄」(BOSS、敵人、舊版殘留)
  //       這些污染 entry 會被以下流程當成真實英雄使用:
  //         • _getLocalSummary fallback 用 _heroLevels 推導解鎖數
  //         • 圖鑑「最高等級」統計
  //         • 雲端寫入時被一起送上去再被別裝置 merge 進去
  //
  // 工具用法:
  //   ① window._diagnoseHeroLevelsPollution()
  //      — 列出 _heroLevels 裡所有「不在玩家英雄白名單」的 entry,只看不改
  //   ② window._cleanseHeroLevelsPollution({ dryRun:false })
  //      — 實際刪除污染 entry,並 await gameCloudSave 同步回雲端
  //      — dryRun:true 時只列出將刪除哪些,不真刪(預設 false)
  //   ③ window._cleanseHeroLevelsByEmail(email, { dryRun:false })
  //      — 老師對別人(學生)的帳號做污染清理(需登入管理員身分)
  // ════════════════════════════════════════════════════════════════════
  window._diagnoseHeroLevelsPollution = function(){
    try{
      if(typeof _heroLevels === 'undefined' || !_heroLevels){
        console.warn('❌ _heroLevels 未載入,無法診斷');
        return { ok: false, reason: 'no_heroLevels' };
      }
      if(typeof _PLAYER_HERO_NAMES === 'undefined' || !_PLAYER_HERO_NAMES){
        console.warn('❌ _PLAYER_HERO_NAMES 未載入,無法判定污染');
        return { ok: false, reason: 'no_whitelist' };
      }
      const _allKeys = Object.keys(_heroLevels);
      const _clean = _allKeys.filter(k => _PLAYER_HERO_NAMES.has(k));
      const _pollution = _allKeys.filter(k => !_PLAYER_HERO_NAMES.has(k));
      const _result = {
        ok: true,
        totalKeys: _allKeys.length,
        cleanCount: _clean.length,
        pollutionCount: _pollution.length,
        pollutionDetails: _pollution.map(k => ({ name: k, lv: _heroLevels[k] || 0 })),
        whitelistSize: _PLAYER_HERO_NAMES.size,
      };
      console.group('🔍 [_diagnoseHeroLevelsPollution] _heroLevels 污染診斷');
      console.log('總 entry 數:', _result.totalKeys);
      console.log('白名單內(乾淨):', _result.cleanCount);
      console.log('白名單外(污染):', _result.pollutionCount);
      console.log('白名單總數:', _result.whitelistSize);
      if(_result.pollutionCount > 0){
        console.warn('⚠ 偵測到 ' + _result.pollutionCount + ' 個污染 entry:');
        console.table(_result.pollutionDetails);
        console.log('💡 下一步:呼叫 await _cleanseHeroLevelsPollution() 清理');
      } else {
        console.log('✅ 沒有污染 entry,_heroLevels 乾淨');
      }
      console.groupEnd();
      return _result;
    }catch(e){
      console.error('[_diagnoseHeroLevelsPollution] 例外', e);
      return { ok: false, reason: 'exception', error: e.message };
    }
  };

  window._cleanseHeroLevelsPollution = async function(opts){
    opts = opts || {};
    const _dryRun = !!opts.dryRun;
    try{
      const _diag = window._diagnoseHeroLevelsPollution();
      if(!_diag.ok) return _diag;
      if(_diag.pollutionCount === 0){
        console.log('✅ [_cleanseHeroLevelsPollution] 沒有污染要清,跳過');
        return { ok: true, cleaned: 0, dryRun: _dryRun };
      }
      console.warn((_dryRun ? '🧪 [DRY RUN] ' : '🧹 ')
        + '[_cleanseHeroLevelsPollution] 準備清除 ' + _diag.pollutionCount + ' 個污染 entry');
      if(_dryRun){
        console.log('(DRY RUN 模式,實際不刪除。下指令時加 { dryRun:false } 真的清)');
        return { ok: true, cleaned: 0, willClean: _diag.pollutionCount, dryRun: true, details: _diag.pollutionDetails };
      }
      _diag.pollutionDetails.forEach(p => {
        delete _heroLevels[p.name];
      });
      console.log('✅ 本地 _heroLevels 已清除 ' + _diag.pollutionCount + ' 個污染 entry');
      // 寫雲端
      if(typeof gameCloudSave === 'function'){
        try{
          window._adminForceRestore = true;  // 繞過健康度守門(這是縮減,不是擴增)
          const _r = await Promise.resolve(gameCloudSave());
          window._adminForceRestore = false;
          if(_r !== false){
            console.log('✅ 已同步寫雲端(_adminForceRestore=true 繞過守門)');
          } else {
            console.warn('⚠ gameCloudSave 回 false,本地已清但雲端未同步');
          }
        }catch(eS){
          window._adminForceRestore = false;
          console.warn('⚠ 寫雲端例外:', eS);
        }
      }
      return { ok: true, cleaned: _diag.pollutionCount, dryRun: false, details: _diag.pollutionDetails };
    }catch(e){
      console.error('[_cleanseHeroLevelsPollution] 例外', e);
      return { ok: false, reason: 'exception', error: e.message };
    }
  };

  // 對「別的帳號」做污染清理(需登入管理員身分)
  window._cleanseHeroLevelsByEmail = async function(email, opts){
    if(!window._fbAdminFindPlayerByEmail) throw new Error('_fbAdminFindPlayerByEmail 未載入');
    const _find = await window._fbAdminFindPlayerByEmail(email);
    if(!_find.found){
      console.warn('❌ 找不到這個 email 的玩家:', email);
      return { found: false, email };
    }
    opts = opts || {};
    const _dryRun = !!opts.dryRun;
    console.log('🧹 [_cleanseHeroLevelsByEmail] 開始清理 uid:', _find.uid, ', email:', email,
      (_dryRun ? '(DRY RUN)' : ''));
    try{
      // 從 live 槽讀
      if(!window._fbLoadSlot) throw new Error('_fbLoadSlot 未載入');
      const _live = await window._fbLoadSlot(_find.uid, 'live');
      if(!_live || !_live.heroLevels){
        console.warn('❌ 該帳號的 live 槽沒有 heroLevels 資料,跳過');
        return { ok: false, reason: 'no_heroLevels_in_live' };
      }
      const _whitelist = _PLAYER_HERO_NAMES;
      if(!_whitelist){
        console.warn('❌ _PLAYER_HERO_NAMES 未載入');
        return { ok: false, reason: 'no_whitelist' };
      }
      const _allKeys = Object.keys(_live.heroLevels);
      const _pollution = _allKeys.filter(k => !_whitelist.has(k));
      console.log('該帳號 heroLevels:', _allKeys.length, ' / 污染:', _pollution.length);
      if(_pollution.length === 0){
        console.log('✅ 該帳號沒有污染,跳過');
        return { ok: true, cleaned: 0 };
      }
      console.table(_pollution.map(k => ({ name: k, lv: _live.heroLevels[k] })));
      if(_dryRun){
        console.log('(DRY RUN,實際不寫)');
        return { ok: true, cleaned: 0, willClean: _pollution.length, dryRun: true };
      }
      // 真的寫:把 heroLevels 重新組成乾淨版,setDoc merge:true 到主文件 + live 槽
      const _cleanLv = {};
      _allKeys.forEach(k => { if(_whitelist.has(k)) _cleanLv[k] = _live.heroLevels[k]; });
      await setDoc(doc(_fbDb, 'players', _find.uid), {
        heroLevels: _cleanLv,
        heroLevels_s: JSON.stringify(_cleanLv),   // ★ v3.15.96 — 同步寫字串版,否則載入優先採信舊 _s 會把剛清掉的污染復活
        _adminForceRestore: true,
      }, { merge: true });
      console.log('✅ 已寫主文件 (清掉 ' + _pollution.length + ' 個污染)');
      // 也寫 live 槽
      try{
        await setDoc(doc(_fbDb, 'players', _find.uid, 'saves', 'live'), {
          heroLevels: _cleanLv,
          heroLevels_s: JSON.stringify(_cleanLv),   // ★ v3.15.96 — 同步寫字串版(三槽合併對 heroLevels 優先採信 _s)
          savedAt: Date.now(),
        }, { merge: true });
        console.log('✅ 已寫 live 槽');
      }catch(eL){ console.warn('⚠ 寫 live 槽失敗(主文件已寫):', eL); }
      console.log('📣 請告知 ' + email + ':重新整理頁面 / 重新登入即可生效');
      return { ok: true, cleaned: _pollution.length, uid: _find.uid, email };
    }catch(e){
      console.error('[_cleanseHeroLevelsByEmail] 例外', e);
      return { ok: false, reason: 'exception', error: e.message };
    }
  };

  // ★ v3.5.72 — 寄送「污染檢查提醒」(學生自己決定要不要用雲端覆蓋本地)
  //   跟 _fbRestoreFromSlot 的差異:
  //     • _fbRestoreFromSlot = 老師「強制」用雲端槽覆蓋本地(權威救援,學生只能按了解)
  //     • _fbSendPollutionCheckSignal = 老師「提醒」學生本地可能被污染,讓學生看對比後自己選
  //   使用情境:老師看到學生發 BUG 回報「健康度守門擋下解鎖英雄倒退」,認為這是共用平板殘留
  //              但又不想強制覆蓋(怕誤判),寄送提醒讓學生自己看對比再決定
  //   信號結構 _adminPollutionCheckSignal = { ts, by, byEmail, reason, version }
  window._fbSendPollutionCheckSignal = async function(uid, opts){
    if(!uid) throw new Error('必須提供 uid');
    opts = opts || {};
    const _signal = {
      ts: Date.now(),
      by: (window._fbUser && window._fbUser.displayName) || 'admin',
      byEmail: (window._fbUser && window._fbUser.email) || '',
      reason: opts.reason || '老師偵測到帳號可能被共用裝置殘留污染',
      version: 1,
    };
    // 寫到主文件,讓學生 gameCloudLoad 偵測得到
    await setDoc(doc(_fbDb, 'players', uid), {
      _adminPollutionCheckSignal: _signal,
      _adminForceRestore: true,  // 繞過健康度守門
    }, { merge: true });
    // 不需要寫 live/safe,因為這個信號的目的是「下次學生開遊戲時提醒」,
    // 主文件就是 gameCloudLoad 必讀的位置
    console.info('📢 [_fbSendPollutionCheckSignal] 已寄送污染檢查提醒給 uid=' + uid + ', 信號:', _signal);
    return { ok: true, signal: _signal };
  };

  // 配套 console API:email 入口
  window._sendPollutionCheckByEmail = async function(email, reason){
    if(!window._fbAdminFindPlayerByEmail) throw new Error('_fbAdminFindPlayerByEmail 未載入');
    const _find = await window._fbAdminFindPlayerByEmail(email);
    if(!_find.found){
      console.warn('❌ 找不到這個 email 的玩家:', email);
      return { found: false, email };
    }
    console.log('📢 [_sendPollutionCheckByEmail] 寄送污染檢查提醒給 uid:', _find.uid, ', email:', email);
    const _r = await window._fbSendPollutionCheckSignal(_find.uid, { reason: reason });
    console.log('✅ 已寄送!學生下次開遊戲會看到對比視窗自己決定');
    return { ..._r, uid: _find.uid, email };
  };

  // ★ v3.13.49(2026-06-05)— 學號 → 完整 email 共用 helper(老師需求:可輸入「lsps110046」直接查)
  //   舊版只認「純 6 位數字」學號(會補成 lspsXXXXXX@…),但老師習慣輸入完整「lsps110046」(帶字母前綴)
  //   → 之前會被當成姓名查、找不到。本 helper 同時涵蓋兩種:
  //     ① 「字母+數字」如 lsps110046 / lsps112055  → 直接當 local-part
  //     ② 純數字(6 位)                            → 補上 'lsps' 前綴
  //   網域:優先從 window._STUDENT_ROSTER 任一筆 key 推導(自動跟著學校網域走),fallback 'stu.lsps.tp.edu.tw'。
  //   回傳:成功 → 完整小寫 email 字串;不像學號 → null(caller fallback 原邏輯)。
  window._lxpsStudentIdToEmail = function(input){
    try{
      const _s = String(input == null ? '' : input).trim();
      if(!_s || _s.includes('@')) return null;
      // 推導學生網域
      let _domain = 'stu.lsps.tp.edu.tw';
      try{
        const _r = window._STUDENT_ROSTER;
        if(_r && typeof _r === 'object'){
          const _k = Object.keys(_r).find(k => k.indexOf('@') > 0);
          if(_k){ const _d = _k.split('@')[1]; if(_d) _domain = _d.toLowerCase(); }
        }
      }catch(_){}
      // ① 字母+數字(完整學號, 含 lsps 前綴)
      if(/^[A-Za-z]{2,8}\d{3,}$/.test(_s)){
        return (_s + '@' + _domain).toLowerCase();
      }
      // ② 純 6 位數字 → 補 lsps 前綴(與舊邏輯相容)
      if(/^\d{6}$/.test(_s)){
        return ('lsps' + _s + '@' + _domain).toLowerCase();
      }
      return null;
    }catch(_){ return null; }
  };

  // ★ v1.0.20260511.6100 — 學生補償工具:依 email 查詢玩家
  //
  // ★ v3.12.14 — 擴展為「任意輸入查玩家」:email / uid / 學號 / 班級碼 / 姓名 / 真實姓名
  //   流程:① 自動判斷輸入類型 ② 對應路徑查詢 ③ 唯一命中回原本 shape
  //   姓名命中多筆 → throw 特殊 error { _multi:true, _candidates:[...] },UI 顯示候選
  //
  //   輸入類型對照:
  //     email(含 @)              → 精確 email 查
  //     uid(英數 ≥ 20 字元)       → doc 直拉
  //     學號(lsps110046 / 純 6 位)→ ★ v3.13.49 _lxpsStudentIdToEmail 拼完整 email 走 email
  //     5 位班級碼(年級1+班級2+座號2)→ 班級<10 時去前導 0 變 4 碼,走姓名路徑
  //     其他                       → 走 _fbAdminFindPlayersByName(姓名/班級碼/真實姓名)
  // ════════════════════════════════════════════════════════════════════
  // ★ v3.13.74 — GM 課堂獎勵發放「送禮記錄」(老師指定:存 Firestore gmGiftLog 集合)
  //   結構:gmGiftLog/{auto-id} = { uid, label, email, items:[字串], by, at }
  //   ⚠ 需在 Firestore Console 部署 gmGiftLog 規則(allow read/create: if isAdmin())後才會生效。
  //   寫入失敗不影響發獎本身(發獎已由 _fbCompensatePlayer 完成),只記 console。
  // ════════════════════════════════════════════════════════════════════
  window._fbWriteGmGiftLog = async function(record){
    try{
      if(!record || typeof record !== 'object') return false;
      const _col = collection(_fbDb, 'gmGiftLog');
      const _ref = doc(_col);   // 自動 ID
      await setDoc(_ref, {
        uid:   String(record.uid || ''),
        label: String(record.label || ''),
        email: String(record.email || ''),
        items: Array.isArray(record.items) ? record.items.map(s => String(s)).slice(0, 30) : [],
        by:    String(record.by || 'admin'),
        at:    Date.now(),
        merit: String(record.merit || '').slice(0, 120),          // ★ v3.17.6 — 送禮理由(事蹟),供 GM 送禮記錄查詢
        rewardId: String(record.rewardId || ''),                  // ★ v3.17.6 — 對應收件箱 rewardId,交叉比對認領狀態/補發
        kind: String(record.kind || 'classReward').slice(0, 20),  // classReward / globalReward / redeemCode
      });
      return true;
    }catch(e){
      console.warn('[_fbWriteGmGiftLog] 送禮記錄寫入失敗(發獎不受影響)', e);
      return false;
    }
  };
  // 讀最近 N 筆送禮記錄(預設 80 筆,依時間新→舊)
  window._fbReadGmGiftLog = async function(maxN){
    try{
      const _n = parseInt(maxN, 10) || 80;
      let _snap;
      try{
        const _q = query(collection(_fbDb, 'gmGiftLog'), orderBy('at', 'desc'), limit(_n));
        _snap = await getDocs(_q);
      }catch(_eOrder){
        // orderBy 索引可能不存在 → fallback 不排序
        console.warn('[_fbReadGmGiftLog] orderBy fallback', _eOrder);
        const _q2 = query(collection(_fbDb, 'gmGiftLog'), limit(_n));
        _snap = await getDocs(_q2);
      }
      const _list = [];
      _snap.forEach(d => { const _x = d.data() || {}; _list.push({ id:d.id, uid:_x.uid||'', label:_x.label||'', email:_x.email||'', items:Array.isArray(_x.items)?_x.items:[], by:_x.by||'', at:_x.at||0, merit:_x.merit||'', rewardId:_x.rewardId||'', kind:_x.kind||'' }); });
      _list.sort((a, b) => (b.at || 0) - (a.at || 0));
      return _list;
    }catch(e){
      console.warn('[_fbReadGmGiftLog] 讀取失敗', e);
      throw e;
    }
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.15.80(2026-06-22)新增 — 召喚紀錄雲端化(原本只存 localStorage)
  // ────────────────────────────────────────────────────────────────────
  //   ① 玩家每次召喚(星空抽 + 合成召喚卷 + 至寶券)後,_recordSummonHistory 寫
  //      localStorage,並即時呼叫 _fbSaveSummonHistory 整包寫雲端 summonHistory/{uid}
  //      (fire-and-forget,失敗不影響召喚本身與本地)。
  //   ② 登入載入完成後 _fbSyncSummonHistoryOnLogin 讀雲端 + 與本地合併(以時間戳去重、
  //      保最新 60 筆,雲端為權威)→ 換裝置/清快取也看得到完整紀錄。
  //   ③ GM 用 _fbReadPlayerSummonHistory / _fbShowPlayerSummonHistory 查任一玩家紀錄,
  //      掌握其英雄與至寶解鎖來源。
  //   資料結構 summonHistory/{uid} = { uid, list:[{t,free,n,items:[{n,ic,q,r,hero}]}], updatedAt }
  //   ⚠ 需在 Firestore Console 部署 summonHistory 規則(見 MEMORY_HANDOFF / firestore.rules):
  //      玩家寫自己(hasOnly uid/list/updatedAt)、GM 讀全部;未部署則雲端寫入被拒,
  //      本地紀錄仍正常(belt-and-suspenders)。
  // ════════════════════════════════════════════════════════════════════
  window._fbSaveSummonHistory = async function(list){
    try{
      const _uid = window._gUserId;
      if(!_uid || !_fbDb) return false;
      const _safe = Array.isArray(list) ? list.slice(0, 60) : [];
      await setDoc(doc(_fbDb, 'summonHistory', _uid), {
        uid: String(_uid),
        list: _safe,
        updatedAt: Date.now(),
      }, { merge: false });
      return true;
    }catch(e){
      try{ console.warn('[_fbSaveSummonHistory] 召喚紀錄雲端寫入失敗(本地仍保留)', e); }catch(_){}
      return false;
    }
  };
  // ════════════════════════════════════════════════════════════════════
  // ★ v3.17.6(2026-07-01)新增 — 召喚卷 UID 帳本(老師需求 3b:獲得/使用即記錄證據,永久不刪)
  // ────────────────────────────────────────────────────────────────────
  //   每次「獲得(grant)/使用(use)」召喚卷或自選卷,_recordTicketLedger 即:
  //     ① append 一筆 { ev, item, n, uid(前12碼), at, result } 到 localStorage
  //        lxps_ticket_ledger_{uid}(append-only·封頂 500·永不移除舊筆)
  //     ② fire-and-forget 整包寫雲端 ticketLedger/{uid}(失敗不影響本地與召喚本身)
  //   召喚「結果」本身另由既有 _heroUnlockHistory / _treasureUnlockHistory / summonHistory
  //   記錄(自選卷抽到的英雄/至寶都帶 uid),與本帳本相互佐證 → GM 救援時有完整證據。
  //   資料結構 ticketLedger/{uid} = { uid, list:[{ev,item,n,uid,at,result}], updatedAt }
  //   ⚠ 需部署 firestore.rules ticketLedger/{uid}(玩家寫自己 hasOnly uid/list/updatedAt、GM 讀);
  //     未部署則雲端寫入被拒,本地帳本仍正常(belt-and-suspenders)。
  // ════════════════════════════════════════════════════════════════════
  window._TICKET_LEDGER_KEYS = {
    'summon_ticket_ssr':1,'summon_ticket_sr':1,'summon_ticket_ssr_pick':1,
    'summon_ticket_ur_pick':1,'summon_ticket_sr_pick':1,
    'summon_ticket_treasure':1,'summon_ticket_treasure_pick':1,
    'pet_summon_ticket':1   /* ★ v4.0.0 隨機寵物召喚卷(使用紀錄供 GM 稽核) */
  };
  window._fbSaveTicketLedger = async function(list){
    try{
      const _uid = window._gUserId;
      if(!_uid || !_fbDb) return false;
      const _safe = Array.isArray(list) ? list.slice(-500) : [];
      await setDoc(doc(_fbDb, 'ticketLedger', _uid), {
        uid: String(_uid),
        list: _safe,
        updatedAt: Date.now(),
      }, { merge: false });
      return true;
    }catch(e){
      try{ console.warn('[_fbSaveTicketLedger] 召喚卷帳本雲端寫入失敗(本地仍保留)', e); }catch(_){}
      return false;
    }
  };
  window._fbLoadTicketLedger = async function(uid){
    try{
      const _u = uid || window._gUserId;
      if(!_u || !_fbDb) return [];
      const _snap = await getDoc(doc(_fbDb, 'ticketLedger', _u));
      const _ex = (typeof _snap.exists === 'function') ? _snap.exists() : !!_snap.exists;
      if(!_ex) return [];
      const _d = _snap.data() || {};
      return Array.isArray(_d.list) ? _d.list : [];
    }catch(e){ try{ console.warn('[_fbLoadTicketLedger] 讀取失敗', e); }catch(_){} return []; }
  };
  // 核心:記一筆召喚卷帳本(獲得/使用)。ev='grant'|'use';n=張數;result=可選(如自選卷抽到的英雄/至寶名)
  // ★ v5.143.0（老師裁定）— 第 5 參數 tids：本次獲得／用掉的券 tid 陣列 → 寫成 tid 欄位（逗號串）。
  //   有了它，「哪一張券換到哪隻英雄／哪件至寶」可逐張追溯（英雄／至寶解鎖帳本同輪也補 tid）。
  window._recordTicketLedger = function(ev, item, n, result, tids){
    try{
      if(window._suppressTicketLedger) return;               // ★ v3.17.6 — GM 卷暫時實體化 base 卷時抑制,避免污染帳本
      if(!window._TICKET_LEDGER_KEYS[item]) return;          // 只記召喚卷/自選卷
      const _cnt = Math.abs(parseInt(n, 10) || 0);
      if(_cnt <= 0) return;
      const _uid = window._gUserId;
      if(!_uid) return;                                       // 未登入不記(避免無主帳本)
      const _key = 'lxps_ticket_ledger_' + _uid;
      let _list = [];
      try{ const _r = localStorage.getItem(_key); _list = _r ? JSON.parse(_r) : []; if(!Array.isArray(_list)) _list = []; }catch(_){ _list = []; }
      _list.push({
        ev: (ev === 'use') ? 'use' : 'grant',
        item: String(item),
        n: _cnt,
        uid: String(_uid).slice(0, 12),                       // 前12碼,與英雄/至寶解鎖帳本同口徑
        at: Date.now(),
        result: (result != null) ? String(result).slice(0, 40) : '',
        tid: (Array.isArray(tids) && tids.length) ? tids.join(',').slice(0, 200) : ''   // ★ v5.143.0 券 tid
      });
      if(_list.length > 500) _list = _list.slice(-500);       // 封頂但只砍最舊,永不主動刪個別紀錄
      try{ localStorage.setItem(_key, JSON.stringify(_list)); }catch(_){}
      try{ if(typeof window._fbSaveTicketLedger === 'function') window._fbSaveTicketLedger(_list); }catch(_){}  // fire-and-forget 上雲
    }catch(_e){ try{ console.warn('[_recordTicketLedger] 記錄失敗(不影響召喚)', _e); }catch(_){} }
  };
  // 登入後:雲端 ↔ 本地合併(去重保最新 500·雲端與本地皆為權威·只增不減)
  window._fbSyncTicketLedgerOnLogin = async function(){
    try{
      const _uid = window._gUserId;
      if(!_uid || !_fbDb) return;
      const _key = 'lxps_ticket_ledger_' + _uid;
      let _local = [];
      try{ const _r = localStorage.getItem(_key); _local = _r ? JSON.parse(_r) : []; if(!Array.isArray(_local)) _local = []; }catch(_){ _local = []; }
      const _cloud = await window._fbLoadTicketLedger(_uid);
      const _seen = {}; const _merged = [];
      [].concat(_cloud, _local).forEach(function(b){
        if(!b || typeof b !== 'object') return;
        const _k = (b.at || 0) + '_' + (b.ev || '') + '_' + (b.item || '') + '_' + (b.n || 0);
        if(_seen[_k]) return; _seen[_k] = 1; _merged.push(b);
      });
      _merged.sort(function(a, b){ return (a.at || 0) - (b.at || 0); });
      const _final = _merged.slice(-500);
      try{ localStorage.setItem(_key, JSON.stringify(_final)); }catch(_){}
      if(_final.length > _cloud.length){ try{ window._fbSaveTicketLedger(_final); }catch(_){} }
    }catch(e){ try{ console.warn('[_fbSyncTicketLedgerOnLogin] 同步失敗', e); }catch(_){} }
  };

  // 讀某 uid 的雲端召喚紀錄陣列(查無或失敗回 [])
  window._fbLoadSummonHistory = async function(uid){
    try{
      const _u = uid || window._gUserId;
      if(!_u || !_fbDb) return [];
      const _snap = await getDoc(doc(_fbDb, 'summonHistory', _u));
      const _ex = (typeof _snap.exists === 'function') ? _snap.exists() : !!_snap.exists;
      if(!_ex) return [];
      const _d = _snap.data() || {};
      return Array.isArray(_d.list) ? _d.list : [];
    }catch(e){
      try{ console.warn('[_fbLoadSummonHistory] 讀取失敗', e); }catch(_){}
      return [];
    }
  };
  // 登入載入後:雲端 + 本地合併(去重保最新 60)寫回本地;本地有雲端缺的離線紀錄則回寫雲端
  window._fbSyncSummonHistoryOnLogin = async function(){
    try{
      const _uid = window._gUserId;
      if(!_uid || !_fbDb) return;
      const _key = 'lxps_summon_history_' + _uid;
      let _local = [];
      try{ const _r = localStorage.getItem(_key); _local = _r ? JSON.parse(_r) : []; if(!Array.isArray(_local)) _local = []; }catch(_){ _local = []; }
      const _cloud = await window._fbLoadSummonHistory(_uid);
      const _seen = {};
      const _merged = [];
      [].concat(_cloud, _local).forEach(function(b){
        if(!b || typeof b !== 'object') return;
        const _k = (b.t || 0) + '_' + (b.n || (b.items ? b.items.length : 0));
        if(_seen[_k]) return;
        _seen[_k] = 1;
        _merged.push(b);
      });
      _merged.sort(function(a, b){ return (b.t || 0) - (a.t || 0); });
      const _final = _merged.slice(0, 60);
      try{ localStorage.setItem(_key, JSON.stringify(_final)); }catch(_){}
      // 合併後比雲端多(本地有離線紀錄)→ 回寫雲端讓雲端也完整
      if(_final.length > _cloud.length){
        try{ window._fbSaveSummonHistory(_final); }catch(_){}
      }
    }catch(e){
      try{ console.warn('[_fbSyncSummonHistoryOnLogin] 同步失敗', e); }catch(_){}
    }
  };
  // GM:用 email 或 uid 查某玩家召喚紀錄,回 { uid, email, list }
  window._fbReadPlayerSummonHistory = async function(emailOrUid){
    try{
      if(!_fbDb) throw new Error('Firestore 尚未就緒');
      const _raw = String(emailOrUid || '').trim();
      if(!_raw) throw new Error('請提供 email 或 uid');
      let _uid = '';
      let _email = '';
      // email、學號(lsps開頭→補校內信箱)走 where('email') 反查;其餘當 uid 直接用
      let _byEmail = '';
      if(_raw.indexOf('@') >= 0){ _byEmail = _raw.toLowerCase(); }
      else if(/^lsps\d+$/i.test(_raw)){ _byEmail = _raw.toLowerCase() + '@stu.lsps.tp.edu.tw'; }
      if(_byEmail){
        _email = _byEmail;
        const _q = query(collection(_fbDb, 'players'), where('email', '==', _email));
        const _snap = await getDocs(_q);
        if(_snap.empty) throw new Error('查無此玩家:' + _email);
        _snap.forEach(function(d){ if(!_uid){ _uid = d.id; } });
      } else {
        _uid = _raw;
        try{
          const _pSnap = await getDoc(doc(_fbDb, 'players', _uid));
          const _pEx = (typeof _pSnap.exists === 'function') ? _pSnap.exists() : !!_pSnap.exists;
          if(_pEx){ _email = (_pSnap.data() || {}).email || ''; }
        }catch(_){}
      }
      const _list = await window._fbLoadSummonHistory(_uid);
      return { uid: _uid, email: _email, list: _list };
    }catch(e){
      try{ console.warn('[_fbReadPlayerSummonHistory] 查詢失敗', e); }catch(_){}
      throw e;
    }
  };
  // ★ v3.16.84(老師裁示 2026-06-29)— GM「過度補回稽查與回收」彈窗:
  //   輸入 email/uid/學號 → 列出「舊救援沒審 uid 誤補的別位同學英雄/至寶」(簽章:有別人 uid 真實紀錄但本帳號無紀錄/未自確認)
  //   → 一鍵全數回收(寫三槽不復活)+ 對已練(lv>1)者補償知識幣+召喚水晶 + 寄道歉通知(玩家登入彈窗)。
  window._fbShowOverRestoredAudit = async function(emailOrUid){
    var _raw = String(emailOrUid || '').trim();
    if(!_raw){ try{ alert('請先輸入 email / uid / 學號'); }catch(_){} return; }
    var _uid = '';
    try{
      var _byEmail = '';
      if(_raw.indexOf('@') >= 0){ _byEmail = _raw.toLowerCase(); }
      else if(/^lsps\d+$/i.test(_raw)){ _byEmail = _raw.toLowerCase() + '@stu.lsps.tp.edu.tw'; }
      if(_byEmail){
        var _q = query(collection(_fbDb, 'players'), where('email', '==', _byEmail));
        var _snap = await getDocs(_q);
        if(_snap.empty) throw new Error('查無此玩家:' + _byEmail);
        _snap.forEach(function(d){ if(!_uid) _uid = d.id; });
      } else { _uid = _raw; }
    }catch(e){ try{ alert('查詢失敗:' + (e && e.message ? e.message : e)); }catch(_){} return; }
    if(!_uid){ try{ alert('無法解析帳號 uid'); }catch(_){} return; }
    var _scan;
    try{ _scan = await window._fbAdminScanOverRestoredForUid(_uid); }
    catch(e){ try{ alert('稽查失敗:' + (e && e.message ? e.message : e)); }catch(_){} return; }
    var _esc = function(s){ return String(s == null ? '' : s).replace(/[&<>"]/g, function(c){ return ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'})[c]; }); };
    var _old = document.getElementById('_gm-overrestore-popup'); if(_old) _old.remove();
    var _heroes = (_scan && _scan.heroes) || [];
    var _treasures = (_scan && _scan.treasures) || [];
    var _srcLabel = function(s){
      s = String(s == null ? '' : s);
      if(!s) return '未標記來源(較早期取得)';
      if(s === 'admin_delete') return '曾被老師移除';
      if(s.indexOf('summon') === 0 || s.indexOf('ticket') === 0) return '召喚(星空 / 召喚卷)';
      if(s === 'maokong_50pct') return '貓空通關獲得';
      if(s === 'darkorb_5pct') return '黑暗球掉落';
      if(s === 'japan_boss_5pct') return '日本 BOSS 獲得';
      if(s === 'yamata_miko_5pct') return '八岐大蛇(巫女)';
      if(s === 'taiwan_clear') return '台灣通關獲得';
      if(s === 'japan_clear') return '日本通關獲得';
      if(s === 'mainstory_clear') return '主線劇情獲得';
      if(s === 'event_quest') return '答題解鎖(活動英雄)';
      if(s === 'shop_purchase') return '商店購買';
      if(s === 'student_designer') return '學生設計英雄';
      if(s === 'admin_grant' || s === 'compensation') return '老師補發';
      if(s.indexOf('wb_') === 0 || s === 'wb_drop') return '世界 BOSS 獲得';
      if(s.indexOf('boss_') === 0 || s === 'boss_drop') return 'BOSS 戰獲得';
      return '其他來源';
    };
    var _fmt = function(at){ try{ if(!at) return '時間不明'; var d = new Date(Number(at) || 0); var p = function(n){ return (n < 10 ? '0' : '') + n; }; return (d.getMonth() + 1) + '/' + d.getDate() + ' ' + p(d.getHours()) + ':' + p(d.getMinutes()); }catch(_){ return '時間不明'; } };
    var _prov = function(it){
      var _by = it.byUid ? ('　⚠ 別帳號解鎖(…' + _esc(String(it.byUid).slice(0, 6)) + ')') : '';
      return '🔓 來源:' + _esc(_srcLabel(it.source)) + '　🕐 ' + _esc(_fmt(it.at)) + _by;
    };
    var _heroRows = _heroes.map(function(h){
      return '<label class="_orr-row" style="display:flex;align-items:flex-start;gap:8px;padding:7px 9px;margin-bottom:5px;background:#ff6e6e14;border:1px solid #ff6e6e44;border-radius:8px;cursor:pointer;">'
        + '<input type="checkbox" class="_orr-ck" data-kind="hero" data-key="' + _esc(h.name) + '" checked style="margin-top:3px;accent-color:#ff7a7a;flex:0 0 auto;">'
        + '<div style="flex:1;min-width:0;">'
        +   '<div style="font-size:14px;font-weight:800;color:#ffc0c0;">' + _esc(h.name) + ' <span style="color:#c89090;font-weight:600;">Lv' + (h.lv||1) + (h.lv>1 ? ' 🔥已練' : '') + '</span></div>'
        +   '<div style="font-size:11px;color:#9aa6ba;margin-top:2px;">' + _prov(h) + '</div>'
        + '</div></label>';
    }).join('');
    var _treRows = _treasures.map(function(t){
      return '<label class="_orr-row" style="display:flex;align-items:flex-start;gap:8px;padding:7px 9px;margin-bottom:5px;background:#c8a04014;border:1px solid #c8a04044;border-radius:8px;cursor:pointer;">'
        + '<input type="checkbox" class="_orr-ck" data-kind="tre" data-key="' + _esc(t.id) + '" checked style="margin-top:3px;accent-color:#e0c060;flex:0 0 auto;">'
        + '<div style="flex:1;min-width:0;">'
        +   '<div style="font-size:14px;font-weight:800;color:#f0d090;">💎 ' + _esc(t.name) + '</div>'
        +   '<div style="font-size:11px;color:#9aa6ba;margin-top:2px;">' + _prov(t) + '</div>'
        + '</div></label>';
    }).join('');
    var _trained = _heroes.filter(function(h){ return h.lv > 1; }).length;
    var _none = (!_heroes.length && !_treasures.length);
    var _ov = document.createElement('div');
    _ov.id = '_gm-overrestore-popup';
    _ov.style.cssText = 'position:fixed;inset:0;z-index:2147483600;background:rgba(0,0,0,0.72);display:flex;align-items:center;justify-content:center;padding:18px;';
    _ov.innerHTML =
      '<div style="background:#161826;border:2px solid rgba(255,140,140,0.5);border-radius:14px;max-width:640px;width:100%;max-height:88vh;overflow-y:auto;padding:20px;box-shadow:0 12px 40px rgba(0,0,0,0.6);">'
      + '<div style="font-size:20px;font-weight:900;color:#ffd0d0;margin-bottom:6px;">🔍 過度補回稽查與回收(逐項審查)</div>'
      + '<div style="font-size:12px;color:#a9b4c8;margin-bottom:12px;line-height:1.6;">帳號:' + _esc(_scan.email || _uid) + '<br>下列為「有<b style="color:#ffb0b0;">別位同學 uid 的真實解鎖紀錄</b>、但本帳號自己無紀錄/未自確認」的英雄/至寶。每筆顯示名稱·等級·解鎖來源·時間。<b style="color:#9fe0b0;">勾選後可分別「刪除(回收)」或「救回(確認是這位學生的)」。</b></div>'
      + (_none
          ? '<div style="text-align:center;color:#9fe0b0;padding:24px;font-size:16px;">✅ 沒有偵測到過度補回的角色/至寶,這個帳號乾淨。</div>'
          : ('<div style="display:flex;gap:8px;align-items:center;margin-bottom:8px;">'
              + '<b style="color:#ffb0b0;font-size:13px;">英雄 ' + _heroes.length + ' 隻' + (_trained ? ('·' + _trained + ' 已練') : '') + ' / 至寶 ' + _treasures.length + ' 件</b>'
              + '<button id="_orr-selall" style="margin-left:auto;padding:4px 10px;font-size:11px;font-weight:700;border:1px solid rgba(255,255,255,0.3);border-radius:6px;cursor:pointer;background:rgba(255,255,255,0.06);color:#cfd6e4;">全選</button>'
              + '<button id="_orr-selnone" style="padding:4px 10px;font-size:11px;font-weight:700;border:1px solid rgba(255,255,255,0.3);border-radius:6px;cursor:pointer;background:rgba(255,255,255,0.06);color:#cfd6e4;">全不選</button>'
            + '</div>'
            + '<div style="max-height:42vh;overflow-y:auto;padding-right:2px;margin-bottom:10px;">' + _heroRows + _treRows + '</div>'
            + '<div id="_gm-orr-msg" style="font-size:12px;color:#a9b4c8;margin-bottom:10px;line-height:1.6;">⚠ <b style="color:#ffb0b0;">刪除</b>=回收(寫三槽·不復活·已練英雄補償知識幣+召喚水晶·寄道歉通知);<b style="color:#9fe0b0;">救回</b>=確認是這位學生的(寫合法紀錄·還原等級·之後不再被判過度補回)。請先核對來源/時間再決定。</div>'
            + '<div style="display:flex;gap:8px;flex-wrap:wrap;">'
              + '<button id="_orr-del" style="flex:1;min-width:200px;padding:11px;font-size:14px;font-weight:900;border:none;border-radius:10px;cursor:pointer;background:linear-gradient(135deg,#ff5e5e,#c62828);color:#fff;">🗑 刪除勾選的(回收)</button>'
              + '<button id="_orr-restore" style="flex:1;min-width:200px;padding:11px;font-size:14px;font-weight:900;border:none;border-radius:10px;cursor:pointer;background:linear-gradient(135deg,#3fae66,#1f7a44);color:#fff;">🛟 救回勾選的(是這位學生的)</button>'
            + '</div>'))
      + '<button id="_gm-orr-close" style="width:100%;margin-top:10px;padding:10px;font-size:14px;font-weight:700;border:1px solid rgba(255,255,255,0.25);border-radius:10px;cursor:pointer;background:rgba(255,255,255,0.06);color:#cfd6e4;">關閉</button>'
      + '</div>';
    document.body.appendChild(_ov);
    var _closeBtn = document.getElementById('_gm-orr-close');
    if(_closeBtn) _closeBtn.onclick = function(){ _ov.remove(); };
    var _selAll = document.getElementById('_orr-selall');
    var _selNone = document.getElementById('_orr-selnone');
    var _setAll = function(v){ try{ _ov.querySelectorAll('._orr-ck').forEach(function(c){ c.checked = v; }); }catch(_){} };
    if(_selAll) _selAll.onclick = function(){ _setAll(true); };
    if(_selNone) _selNone.onclick = function(){ _setAll(false); };
    var _getChecked = function(){
      var _hn = [], _ti = [], _rows = [];
      try{ _ov.querySelectorAll('._orr-ck').forEach(function(c){ if(!c.checked) return; var _k = c.getAttribute('data-key'); var _kind = c.getAttribute('data-kind'); if(_kind === 'hero'){ _hn.push(_k); } else if(_kind === 'tre'){ _ti.push(_k); } var _row = c.closest('._orr-row'); if(_row) _rows.push(_row); }); }catch(_){}
      return { heroNames:_hn, treIds:_ti, rows:_rows };
    };
    var _msgEl2 = function(){ return document.getElementById('_gm-orr-msg'); };
    var _delBtn = document.getElementById('_orr-del');
    var _restoreBtn = document.getElementById('_orr-restore');
    if(_delBtn) _delBtn.onclick = async function(){
      var _sel = _getChecked();
      if(!_sel.heroNames.length && !_sel.treIds.length){ try{ alert('請先勾選要刪除的英雄/至寶'); }catch(_){} return; }
      if(!confirm('確定「刪除(回收)」勾選的 ' + _sel.heroNames.length + ' 隻英雄 + ' + _sel.treIds.length + ' 件至寶?\n\n(寫三槽·不復活;已練英雄自動補償知識幣+召喚水晶;寄道歉通知給玩家)')) return;
      _delBtn.disabled = true; if(_restoreBtn) _restoreBtn.disabled = true; _delBtn.textContent = '回收中…';
      try{
        var _r = await window._fbAdminReclaimOverRestoredForUid(_uid, _sel.heroNames, _sel.treIds);
        var _m = _msgEl2();
        if(_m){
          var _comp = _r && _r.compensation;
          _m.innerHTML = '✅ 已刪除(回收)英雄 ' + ((_r && _r.removedHeroes ? _r.removedHeroes.length : 0)) + ' 隻 + 至寶 ' + ((_r && _r.removedTreasures ? _r.removedTreasures.length : 0)) + ' 件。'
            + (_comp ? ('已補償 知識幣×' + _comp.coins + ' + 召喚水晶×' + _comp.crystals + ',並寄出道歉通知。') : '無已練英雄,未補償;已寄出道歉通知。');
          _m.style.color = '#9fe0b0';
        }
        _sel.rows.forEach(function(r){ try{ r.remove(); }catch(_){} });
        _delBtn.textContent = '🗑 刪除勾選的(回收)'; _delBtn.disabled = false; if(_restoreBtn) _restoreBtn.disabled = false;
      }catch(e){
        _delBtn.disabled = false; if(_restoreBtn) _restoreBtn.disabled = false; _delBtn.textContent = '❌ 失敗,點此重試';
        try{ alert('刪除失敗:' + (e && e.message ? e.message : e)); }catch(_){}
      }
    };
    if(_restoreBtn) _restoreBtn.onclick = async function(){
      var _sel = _getChecked();
      if(!_sel.heroNames.length && !_sel.treIds.length){ try{ alert('請先勾選要救回的英雄/至寶'); }catch(_){} return; }
      if(!confirm('確定「救回」勾選的 ' + _sel.heroNames.length + ' 隻英雄 + ' + _sel.treIds.length + ' 件至寶給這位學生?\n\n(寫合法紀錄·還原等級;之後不再被判過度補回。代表你已核對確認這些確實是這位學生的)')) return;
      _restoreBtn.disabled = true; if(_delBtn) _delBtn.disabled = true; _restoreBtn.textContent = '救回中…';
      try{
        var _hOk = 0, _tOk = 0;
        if(_sel.heroNames.length && typeof window._fbAdminRestoreLostHeroes === 'function'){ var _rh = await window._fbAdminRestoreLostHeroes(_uid, _sel.heroNames); if(_rh && _rh.ok !== false) _hOk = _sel.heroNames.length; }
        if(_sel.treIds.length && typeof window._fbAdminRestoreLostTreasures === 'function'){ var _rt = await window._fbAdminRestoreLostTreasures(_uid, _sel.treIds); if(_rt && _rt.ok !== false) _tOk = _sel.treIds.length; }
        try{ if(typeof window._fbAdminSendNotificationToPlayer === 'function') await window._fbAdminSendNotificationToPlayer(_uid, { title:'✅ 帳號審查完成', body:'老師已確認部分角色/至寶屬於你並保留。請登出後重新登入即可生效。', type:'account_rescue' }); }catch(_n){}
        var _m = _msgEl2();
        if(_m){ _m.innerHTML = '✅ 已救回(確認為這位學生的)英雄 ' + _hOk + ' 隻 + 至寶 ' + _tOk + ' 件,已寫合法紀錄並通知玩家。'; _m.style.color = '#9fe0b0'; }
        _sel.rows.forEach(function(r){ try{ r.remove(); }catch(_){} });
        _restoreBtn.textContent = '🛟 救回勾選的(是這位學生的)'; _restoreBtn.disabled = false; if(_delBtn) _delBtn.disabled = false;
      }catch(e){
        _restoreBtn.disabled = false; if(_delBtn) _delBtn.disabled = false; _restoreBtn.textContent = '❌ 失敗,點此重試';
        try{ alert('救回失敗:' + (e && e.message ? e.message : e)); }catch(_){}
      }
    };
  };
  // GM:彈窗檢視某玩家召喚紀錄(摘要:抽到的稀有英雄/至寶 + 逐次明細)
  window._fbShowPlayerSummonHistory = async function(emailOrUid){
    let _data;
    try{
      _data = await window._fbReadPlayerSummonHistory(emailOrUid);
    }catch(e){
      try{ alert('查詢失敗:' + (e && e.message ? e.message : e)); }catch(_){}
      return;
    }
    const _esc = function(s){ return String(s == null ? '' : s).replace(/[&<>"]/g, function(c){ return ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'})[c]; }); };
    const _old = document.getElementById('_gm-summon-history-popup');
    if(_old) _old.remove();
    const _hist = Array.isArray(_data.list) ? _data.list : [];
    const _heroes = {}; const _treasures = {}; let _totalRolls = 0;
    _hist.forEach(function(b){
      _totalRolls += (b && b.n) || ((b && b.items) ? b.items.length : 0);
      ((b && b.items) || []).forEach(function(it){
        if(!it) return;
        if(it.hero){ _heroes[it.n] = (_heroes[it.n] || 0) + 1; }
        else if(it.r === '至寶'){ _treasures[it.n] = (_treasures[it.n] || 0) + 1; }
      });
    });
    const _chips = function(obj, color){
      const _ks = Object.keys(obj);
      if(!_ks.length) return '<span style="color:#8a8a9a;">（無）</span>';
      return _ks.map(function(k){ return '<span style="display:inline-block;margin:3px 4px;padding:3px 9px;border-radius:7px;background:' + color + '22;border:1px solid ' + color + '66;color:' + color + ';font-size:14px;font-weight:700;">' + _esc(k) + (obj[k] > 1 ? ' ×' + obj[k] : '') + '</span>'; }).join('');
    };
    let _rows = '';
    if(!_hist.length){
      _rows = '<div style="text-align:center;color:#9a9aac;padding:24px;font-size:16px;">這位玩家還沒有召喚紀錄(或雲端規則尚未部署)。</div>';
    } else {
      _hist.forEach(function(b){
        const _dt = new Date(b && b.t ? b.t : 0);
        const _dstr = (_dt.getMonth() + 1) + '/' + _dt.getDate() + ' ' + String(_dt.getHours()).padStart(2, '0') + ':' + String(_dt.getMinutes()).padStart(2, '0');
        const _cnt = (b && b.n) || ((b && b.items) ? b.items.length : 0);
        const _title = (_cnt === 11) ? '10 + 1 連抽' : (_cnt > 1 ? (_cnt + ' 連抽') : '單抽');
        const _free = (b && b.free) ? ' ・免費' : '';
        let _its = '';
        ((b && b.items) || []).forEach(function(it){
          const _hi = (it && (it.r === 'SSR' || it.r === '至寶'));
          const _mid = (it && (it.r === 'SR' || it.r === '稀有'));
          const _col = _hi ? '#ffd33b' : _mid ? '#88bbff' : '#cfcfe6';
          _its += '<span style="display:inline-flex;align-items:center;gap:3px;margin:2px;padding:2px 7px;border-radius:6px;background:rgba(255,255,255,0.05);border:1px solid ' + _col + '44;font-size:13px;color:' + _col + ';">' + ((it && it.ic) || '🎁') + ' ' + _esc(it && it.n) + ((it && it.q > 1) ? ' ×' + it.q : '') + ((it && it.r) ? ' [' + _esc(it.r) + ']' : '') + '</span>';
        });
        _rows += '<div style="border-top:1px solid rgba(180,140,255,0.18);padding:7px 2px;"><div style="font-size:12px;color:#bbaadd;font-weight:700;margin-bottom:3px;">🕐 ' + _dstr + ' ・ ' + _title + _free + '</div><div style="line-height:1.6;">' + _its + '</div></div>';
      });
    }
    const _pop = document.createElement('div');
    _pop.id = '_gm-summon-history-popup';
    _pop.style.cssText = 'position:fixed;inset:0;z-index:100000;display:flex;align-items:center;justify-content:center;background:rgba(0,0,12,0.78);backdrop-filter:blur(4px);padding:18px;font-family:"M PLUS Rounded 1c","Nunito",sans-serif;';
    _pop.innerHTML = '<div style="background:linear-gradient(160deg,rgba(22,16,52,0.98),rgba(40,24,78,0.98));border:2.5px solid rgba(255,200,140,0.8);border-radius:16px;padding:20px 22px;max-width:min(96vw,720px);max-height:90vh;overflow-y:auto;-webkit-overflow-scrolling:touch;box-shadow:0 10px 50px rgba(120,80,200,0.5);text-align:left;">'
      + '<div style="font-size:23px;font-weight:900;color:#ffe6b8;text-align:center;letter-spacing:2px;margin-bottom:4px;">📜 玩家召喚紀錄(GM）</div>'
      + '<div style="font-size:13px;color:#bbaadd;text-align:center;margin-bottom:12px;word-break:break-all;">' + _esc(_data.email || '(無 email)') + ' ・ uid:' + _esc(_data.uid || '') + '</div>'
      + '<div style="background:rgba(0,0,16,0.4);border:1px solid rgba(180,120,255,0.3);border-radius:10px;padding:12px 14px;margin-bottom:14px;">'
        + '<div style="font-size:14px;color:#cbb8ff;margin-bottom:6px;">總召喚次數(含連抽單抽):<b style="color:#fff;font-size:17px;">' + _totalRolls + '</b> 抽 ・ 紀錄 <b style="color:#fff;">' + _hist.length + '</b> 批</div>'
        + '<div style="font-size:14px;color:#ffe066;margin:8px 0 3px;font-weight:800;">🌈 抽到的稀有英雄:</div><div>' + _chips(_heroes, '#ffd33b') + '</div>'
        + '<div style="font-size:14px;color:#7fe0ff;margin:10px 0 3px;font-weight:800;">🏆 抽到的台灣至寶:</div><div>' + _chips(_treasures, '#7fe0ff') + '</div>'
      + '</div>'
      + '<div style="font-size:14px;color:#cbb8ff;margin-bottom:4px;font-weight:700;">逐次明細(新→舊,最多 60 批):</div>'
      + _rows
      + '<div style="text-align:center;margin-top:14px;"><button onclick="document.getElementById(\'_gm-summon-history-popup\').remove()" style="padding:9px 30px;font-size:17px;font-weight:700;background:rgba(120,80,200,0.7);border:2px solid rgba(200,160,255,0.7);color:#fff;border-radius:9px;cursor:pointer;font-family:inherit;letter-spacing:2px;">關閉</button></div>'
      + '</div>';
    document.body.appendChild(_pop);
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v4.7.0(2026-07-04)新增 — GM「🐾 寵物紀錄」查詢 + 「🎁 指定補發寵物」
  //   對應 admin_panel.js v4.7.0 玩家活動記錄查詢卡的「🐾 寵物紀錄」鈕。
  //   ① _fbShowPlayerPetRecords(emailOrUid):反查 uid(比照召喚紀錄:email /
  //      lsps學號 / uid)→ getDocFromServer 讀 players/{uid} 權威主檔 +
  //      ticketLedger/{uid} → GM 彈窗:馴養總覽(N/28·稀有度分佈)/寵物明細
  //      (Lv/EXP/好感/跟隨)/互動累計(餵對/撫摸/玩耍勝·讀 medalStats)/
  //      寵物獎章 14 枚/馴養帳本(來源+uid12·自動紅底標「⚠非本人 uid」=
  //      共用平板污染線索)/寵物券帳本(領用+抽到哪隻)。唯讀不寫,零風險。
  //   ② _fbAdminGrantPetToPlayer(uid, petName, lv):彈窗底部指定補發。
  //      比照英雄 admin_grant 補發哲學:只增不減(已馴養者僅取 max(lv)·
  //      絕不降級·不動好感/跟隨/EXP 較高值)、主檔必寫 + saves/live·safe
  //      「存在才」同寫(三槽載入對 tamedPets 是跨槽 union → 主檔寫入即
  //      數學上充分;不創建殘缺槽 doc)、map+_s 雙寫(載入 _s 優先,漏寫 _s
  //      會被舊字串蓋回=補發隱形)、馴養帳本補一筆 src:'admin_grant'
  //      (帶 uid12+by=合法來源佐證)、petsEverCollected 一併 union(圖鑑
  //      收錄)、寄登入彈窗通知玩家(pendingAdminNotifications 既有路徑)。
  //   ⚠ 玩家「在線中」補發存在與英雄補發同型的整包覆寫 race → 建議玩家
  //      離線時操作;union 載入 + 三槽同寫已把風險壓到最低。
  //   Firestore 權限:isAdmin 對 players/{uid}+saves 全權(既有規則),
  //      ticketLedger GM 可讀(需部署 firestore.rules;未部署顯示提示)。
  // ════════════════════════════════════════════════════════════════════
  window._fbReadPlayerPetRecords = async function(emailOrUid){
    if(!_fbDb) throw new Error('Firestore 尚未就緒');
    const _raw = String(emailOrUid || '').trim();
    if(!_raw) throw new Error('請提供 email 或 uid');
    let _uid = '', _email = '', _byEmail = '';
    if(_raw.indexOf('@') >= 0){ _byEmail = _raw.toLowerCase(); }
    else if(/^lsps\d+$/i.test(_raw)){ _byEmail = _raw.toLowerCase() + '@stu.lsps.tp.edu.tw'; }
    if(_byEmail){
      _email = _byEmail;
      const _q = query(collection(_fbDb, 'players'), where('email', '==', _email));
      const _snap = await getDocs(_q);
      if(_snap.empty) throw new Error('查無此玩家:' + _email);
      _snap.forEach(function(d){ if(!_uid) _uid = d.id; });
    } else {
      _uid = _raw;
    }
    const _gds = (typeof getDocFromServer === 'function') ? getDocFromServer : getDoc;
    const _mainSnap = await _gds(doc(_fbDb, 'players', _uid));
    const _mainEx = (typeof _mainSnap.exists === 'function') ? _mainSnap.exists() : !!_mainSnap.exists;
    if(!_mainEx) throw new Error('查無此玩家 doc:' + _uid);
    const _d = _mainSnap.data() || {};
    if(!_email) _email = _d.email || '';
    // ── 各欄位取值口徑與 _applySafeData 完全一致:_s 字串優先,fallback map ──
    const _fromS = function(sKey, mKey, isArr){
      let v = null;
      if(typeof _d[sKey] === 'string'){ try{ const p = JSON.parse(_d[sKey]); if(p && (isArr ? Array.isArray(p) : (typeof p === 'object'))) v = p; }catch(_){} }
      if(!v && _d[mKey] && (isArr ? Array.isArray(_d[mKey]) : (typeof _d[mKey] === 'object'))) v = _d[mKey];
      return v || (isArr ? [] : {});
    };
    const _pets  = _fromS('tamedPets_s', 'tamedPets', false);
    const _hist  = _fromS('_petTameHistory_s', '_petTameHistory', true);
    const _stats = _fromS('medalStats_s', 'medalStats', false);
    const _daily = _fromS('petHouseDaily_s', 'petHouseDaily', false);
    let _slots = null;
    if(typeof _d.petHouseSlots_s === 'string'){ try{ const _hs = JSON.parse(_d.petHouseSlots_s); if(Array.isArray(_hs)) _slots = _hs; }catch(_){} }
    if(!_slots && Array.isArray(_d.petHouseSlots)) _slots = _d.petHouseSlots;
    _slots = _slots || [null, null, null];
    const _pMedals = (_d.playerMedals && typeof _d.playerMedals === 'object') ? _d.playerMedals : {};
    // 寵物券帳本(規則未部署/讀失敗 → null,前端顯示提示;無資料 → 空陣列)
    let _ledger = null;
    try{
      const _lSnap = await _gds(doc(_fbDb, 'ticketLedger', _uid));
      const _lEx = (typeof _lSnap.exists === 'function') ? _lSnap.exists() : !!_lSnap.exists;
      if(_lEx){
        const _ld = _lSnap.data() || {};
        _ledger = (Array.isArray(_ld.list) ? _ld.list : []).filter(function(e){ return e && typeof e.item === 'string' && e.item.indexOf('pet_') === 0; });
      } else { _ledger = []; }
    }catch(_eL){ _ledger = null; }
    return { uid:_uid, email:_email, pets:_pets, hist:_hist, stats:_stats, daily:_daily, slots:_slots, medals:_pMedals, ledger:_ledger };
  };

  window._fbShowPlayerPetRecords = async function(emailOrUid){
    let _data;
    try{ _data = await window._fbReadPlayerPetRecords(emailOrUid); }
    catch(e){ try{ alert('查詢失敗:' + (e && e.message ? e.message : e)); }catch(_){} return; }
    const _esc = function(s){ return String(s == null ? '' : s).replace(/[&<>"]/g, function(c){ return ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'})[c]; }); };
    const _old = document.getElementById('_gm-pet-records-popup');
    if(_old) _old.remove();
    const _R = window.PET_RARITY || {};
    const _allPets = Object.keys(_R);
    const _rOrder = { SSR:0, SR:1, R:2 };
    const _rCol = { SSR:'#ffd33b', SR:'#b48cff', R:'#8fd0ff' };
    const _icoOf = function(pn){
      try{
        const eq = (typeof CARRY_PET_DB !== 'undefined') ? CARRY_PET_DB.find(function(e){ return (e.pet || e.n) === pn; }) : null;
        return (eq && eq.icon) || '🐾';
      }catch(_){ return '🐾'; }
    };
    const _byRarity = function(a, b){
      const ra = (_rOrder[_R[a]] !== undefined) ? _rOrder[_R[a]] : 9;
      const rb = (_rOrder[_R[b]] !== undefined) ? _rOrder[_R[b]] : 9;
      return (ra - rb) || a.localeCompare(b, 'zh-Hant');
    };
    const _pets = _data.pets || {};
    const _tamedNames = Object.keys(_pets);
    let _cSSR = 0, _cSR = 0, _cR = 0, _tSSR = 0, _tSR = 0, _tR = 0;
    _allPets.forEach(function(pn){
      const r = _R[pn];
      if(r === 'SSR'){ _tSSR++; if(_pets[pn]) _cSSR++; }
      else if(r === 'SR'){ _tSR++; if(_pets[pn]) _cSR++; }
      else { _tR++; if(_pets[pn]) _cR++; }
    });
    const _st = _data.stats || {};
    const _feed = Number(_st.petFeedOk) || 0, _pat = Number(_st.petPat) || 0, _win = Number(_st.petPlayWin) || 0;
    const _petMedalIds = ['pet_tame_first','pet_tame_7','pet_tame_14','pet_tame_10','pet_tame_20','pet_tame_30','pet_tame_all','pet_tame_ssr_all','pet_lv20_first','pet_aff_100_first','pet_feed_20','pet_pat_20','pet_play_first','pet_play_20','pet_memory_fast','pet_hide_5click','pet_burst_first'];
    const _defOf = function(id){
      try{
        if(typeof MEDAL_DEFS !== 'undefined'){
          const m = MEDAL_DEFS.find(function(x){ return x.id === id; });
          if(m) return m;
        }
      }catch(_){}
      return { icon:'🏅', name:id };
    };
    let _mGot = 0, _mChips = '';
    _petMedalIds.forEach(function(id){
      const on = !!(_data.medals && _data.medals[id]);
      if(on) _mGot++;
      const m = _defOf(id);
      _mChips += '<span style="display:inline-block;margin:2px 3px;padding:2px 8px;border-radius:7px;font-size:13px;font-weight:700;'
        + (on ? 'background:rgba(255,211,59,0.14);border:1px solid rgba(255,211,59,0.55);color:#ffd33b;' : 'background:rgba(255,255,255,0.04);border:1px solid rgba(160,160,180,0.3);color:#77778a;')
        + '">' + m.icon + ' ' + _esc(m.name) + (on ? '' : '(未)') + '</span>';
    });
    const _srcLabel = function(src){
      return src === 'battle_tame' ? '戰鬥馴養'
        : src === 'pet_ticket' ? '寵物召喚卷'
        : src === 'summon' ? '召喚星空'
        : src === 'admin_grant' ? '老師補發'
        : _esc(src || '?');
    };
    // ★ v4.14.0 — 隨機素質分布(讀 rec.seed·retrainVer;缺 seed=legacy fallback hash(name))
    const _siStr = function(pn, rec){
      try{
        const b = (typeof window._petRandStatBonus === 'function') ? window._petRandStatBonus(pn, (rec.lv|0)||1, rec) : null;
        if(!b) return '—';
        return '體+' + (b.hp|0) + ' 攻+' + (b.atk|0) + ' 特+' + (b.sp|0) + ' 速+' + (b.spd|0);
      }catch(_){ return '—'; }
    };
    let _petRows = '';
    if(!_tamedNames.length){
      _petRows = '<div style="text-align:center;color:#9a9aac;padding:14px;font-size:15px;">尚未馴養任何寵物。</div>';
    } else {
      _tamedNames.slice().sort(_byRarity).forEach(function(pn){
        const rec = _pets[pn] || {};
        const r = _R[pn] || '?';
        const col = _rCol[r] || '#cfcfe6';
        _petRows += '<div style="display:flex;align-items:center;gap:8px;flex-wrap:wrap;border-top:1px solid rgba(140,220,180,0.15);padding:5px 2px;font-size:14px;">'
          + '<span style="min-width:150px;color:' + col + ';font-weight:800;">' + _icoOf(pn) + ' ' + _esc(pn) + ' <span style="font-size:11px;opacity:0.85;">[' + _esc(r) + ']</span></span>'
          + '<span style="color:#cfe8d8;">Lv.<b style="color:#fff;">' + ((rec.lv|0) || 1) + '</b></span>'
          + '<span style="color:#9ac0aa;font-size:12px;">EXP ' + ((rec.exp|0) || 0) + '/100</span>'
          + '<span style="color:#ff9ac0;">💖' + ((rec.aff|0) || 0) + '</span>'
          + '<span style="color:#a0b8ff;font-size:12px;">' + (rec.followTo ? ('跟隨:' + _esc(rec.followTo)) : '未跟隨') + '</span>'
          + '<span style="color:#d8b878;font-size:12px;width:100%;">🎲 隨機素質 ' + _siStr(pn, rec) + ' <span style="opacity:0.7;">(重擲 ' + ((rec.retrainVer|0)||0) + ' 次' + (typeof rec.seed === 'number' ? '' : '·legacy') + ')</span></span>'
          + '</div>';
      });
    }
    // 重置下拉:僅列「已馴養」寵物(重置需已馴養)
    let _resetOpts = '';
    _tamedNames.slice().sort(_byRarity).forEach(function(pn){
      const rec = _pets[pn] || {};
      _resetOpts += '<option value="' + _esc(pn) + '">[' + _esc(_R[pn] || '?') + '] ' + _esc(pn) + '(Lv.' + ((rec.lv|0) || 1) + ')</option>';
    });
    const _uid12 = String(_data.uid).slice(0, 12);
    let _histRows = '';
    const _histArr = Array.isArray(_data.hist) ? _data.hist.slice().reverse() : [];
    if(!_histArr.length){
      _histRows = '<div style="text-align:center;color:#9a9aac;padding:10px;font-size:14px;">(無馴養帳本紀錄)</div>';
    } else {
      _histArr.forEach(function(e){
        if(!e) return;
        const dt = new Date(e.at || 0);
        const ds = (dt.getMonth() + 1) + '/' + dt.getDate() + ' ' + String(dt.getHours()).padStart(2, '0') + ':' + String(dt.getMinutes()).padStart(2, '0');
        const foreign = !!(e.uid && _uid12 && e.uid !== _uid12);
        _histRows += '<div style="display:flex;gap:8px;flex-wrap:wrap;align-items:center;font-size:13px;border-top:1px solid rgba(140,220,180,0.12);padding:4px 2px;'
          + (foreign ? 'background:rgba(255,60,60,0.12);border-left:3px solid #ff5555;' : '')
          + '"><span style="color:#9ac0aa;min-width:88px;">🕐 ' + ds + '</span>'
          + '<span style="color:#eaffea;font-weight:700;">' + _icoOf(e.pet) + ' ' + _esc(e.pet) + '</span>'
          + '<span style="color:#ffd9a0;">' + _srcLabel(e.src) + (e.by ? ('・' + _esc(e.by)) : '') + '</span>'
          + '<span style="color:#8a9aac;font-size:11px;">uid:' + _esc(e.uid || '?') + '</span>'
          + (foreign ? '<span style="color:#ff8888;font-weight:800;font-size:12px;">⚠ 非本人 uid(共用裝置污染線索)</span>' : '')
          + '</div>';
      });
    }
    let _ledRows = '';
    if(_data.ledger === null){
      _ledRows = '<div style="color:#ffb080;font-size:13px;padding:6px 2px;">(讀取失敗:ticketLedger 規則可能尚未部署 → 見 firestore.rules;玩家本機帳本仍在記錄)</div>';
    } else if(!_data.ledger.length){
      _ledRows = '<div style="color:#9a9aac;font-size:13px;padding:6px 2px;">(無寵物券帳本紀錄)</div>';
    } else {
      _data.ledger.slice().reverse().forEach(function(e){
        const dt = new Date(e.at || 0);
        const ds = (dt.getMonth() + 1) + '/' + dt.getDate() + ' ' + String(dt.getHours()).padStart(2, '0') + ':' + String(dt.getMinutes()).padStart(2, '0');
        _ledRows += '<div style="display:flex;gap:8px;flex-wrap:wrap;font-size:13px;border-top:1px solid rgba(180,160,255,0.12);padding:4px 2px;">'
          + '<span style="color:#9ac0aa;min-width:88px;">🕐 ' + ds + '</span>'
          + '<span style="color:' + (e.ev === 'use' ? '#ffd580' : '#a0ffc0') + ';font-weight:800;">' + (e.ev === 'use' ? '使用' : '獲得') + '</span>'
          + '<span style="color:#eaeaff;">' + _esc(e.item) + ((e.n|0) > 1 ? ' ×' + (e.n|0) : '') + '</span>'
          + (e.result ? '<span style="color:#cbb8ff;">→ ' + _esc(e.result) + '</span>' : '')
          + '</div>';
      });
    }
    let _dailyStr = '(今日尚無互動)';
    try{
      const _rec = (_data.daily && _data.daily.rec) || {};
      const _parts = [];
      Object.keys(_rec).forEach(function(pn){
        const r = _rec[pn] || {};
        const acts = [];
        if(r.pat) acts.push('撫摸');
        if(r.fed) acts.push('餵食');
        if(r.play) acts.push('玩耍');
        if(acts.length) _parts.push(_esc(pn) + '(' + acts.join('/') + ')');
      });
      if(_parts.length) _dailyStr = _esc((_data.daily && _data.daily.d) || '') + ':' + _parts.join('、');
      else if(_data.daily && _data.daily.d) _dailyStr = _esc(_data.daily.d) + ':(無)';
    }catch(_){}
    let _opts = '';
    _allPets.slice().sort(_byRarity).forEach(function(pn){
      const has = _pets[pn];
      _opts += '<option value="' + _esc(pn) + '">[' + _esc(_R[pn] || '?') + '] ' + _esc(pn) + (has ? '(已馴養 Lv.' + ((has.lv|0) || 1) + ')' : '') + '</option>';
    });
    const _pop = document.createElement('div');
    _pop.id = '_gm-pet-records-popup';
    _pop.style.cssText = 'position:fixed;inset:0;z-index:100000;display:flex;align-items:center;justify-content:center;background:rgba(0,6,4,0.8);backdrop-filter:blur(4px);padding:18px;font-family:"M PLUS Rounded 1c","Nunito",sans-serif;';
    _pop.innerHTML = '<div style="background:linear-gradient(160deg,rgba(12,32,22,0.98),rgba(18,46,32,0.98));border:2.5px solid rgba(140,230,180,0.8);border-radius:16px;padding:20px 22px;max-width:min(96vw,760px);max-height:92vh;overflow-y:auto;-webkit-overflow-scrolling:touch;box-shadow:0 10px 50px rgba(60,180,120,0.4);text-align:left;">'
      + '<div style="font-size:23px;font-weight:900;color:#c8ffdc;text-align:center;letter-spacing:2px;margin-bottom:4px;">🐾 玩家寵物紀錄(GM)</div>'
      + '<div style="font-size:13px;color:#9adcb4;text-align:center;margin-bottom:12px;word-break:break-all;">' + _esc(_data.email || '(無 email)') + ' ・ uid:' + _esc(_data.uid || '') + '</div>'
      + '<div style="background:rgba(0,16,8,0.4);border:1px solid rgba(120,255,180,0.3);border-radius:10px;padding:12px 14px;margin-bottom:12px;">'
        + '<div style="font-size:15px;color:#d8ffe8;">已馴養 <b style="color:#fff;font-size:18px;">' + _tamedNames.length + '</b> / ' + _allPets.length
        + '(SSR ' + _cSSR + '/' + _tSSR + '・SR ' + _cSR + '/' + _tSR + '・R ' + _cR + '/' + _tR + ')</div>'
        + '<div style="font-size:14px;color:#cfe8d8;margin-top:6px;">互動累計:🍽 餵對 <b>' + _feed + '</b>・🤚 撫摸 <b>' + _pat + '</b>・🎾 玩耍勝 <b>' + _win + '</b></div>'
        + '<div style="font-size:14px;color:#cfe8d8;margin-top:6px;">小屋入住:' + (_esc((_data.slots || []).filter(function(x){ return x; }).join('、')) || '(空)') + '</div>'
        + '<div style="font-size:14px;color:#cfe8d8;margin-top:6px;">今日互動:' + _dailyStr + '</div>'
      + '</div>'
      + '<div style="font-size:14px;color:#aef0c8;font-weight:800;margin:8px 0 3px;">🏅 寵物獎章(' + _mGot + '/14):</div><div style="margin-bottom:10px;">' + _mChips + '</div>'
      + '<div style="font-size:14px;color:#aef0c8;font-weight:800;margin:8px 0 3px;">🐾 已馴養寵物明細:</div>' + _petRows
      + '<div style="font-size:14px;color:#aef0c8;font-weight:800;margin:12px 0 3px;">📒 馴養帳本(新→舊,最多 200 筆;紅底=帳本 uid 非本人=共用裝置污染線索):</div>'
      + '<div style="max-height:220px;overflow-y:auto;border:1px solid rgba(140,220,180,0.2);border-radius:8px;padding:4px 8px;background:rgba(0,10,6,0.3);">' + _histRows + '</div>'
      + '<div style="font-size:14px;color:#aef0c8;font-weight:800;margin:12px 0 3px;">🎟️ 寵物券帳本(領用紀錄・→後面=抽到哪隻):</div>'
      + '<div style="max-height:160px;overflow-y:auto;border:1px solid rgba(180,160,255,0.2);border-radius:8px;padding:4px 8px;background:rgba(4,4,16,0.3);">' + _ledRows + '</div>'
      + '<div style="margin-top:14px;border:1.5px dashed rgba(255,210,120,0.6);border-radius:10px;padding:10px 12px;background:rgba(30,22,4,0.35);">'
        + '<div style="font-size:15px;color:#ffd9a0;font-weight:900;margin-bottom:6px;">🎁 指定補發寵物(GM)</div>'
        + '<div style="display:flex;gap:8px;flex-wrap:wrap;align-items:center;">'
          + '<select id="_gm-pet-grant-sel" style="padding:7px 10px;font-size:14px;border-radius:8px;background:#0e2418;color:#eaffea;border:1px solid rgba(140,230,180,0.5);max-width:280px;">' + _opts + '</select>'
          + '<label style="font-size:13px;color:#cfe8d8;">Lv <input id="_gm-pet-grant-lv" type="number" min="1" max="20" value="1" style="width:64px;padding:6px;font-size:14px;border-radius:8px;background:#0e2418;color:#fff;border:1px solid rgba(140,230,180,0.5);"></label>'
          + '<button onclick="window._gmPetGrantClick(\'' + _esc(_data.uid) + '\')" style="padding:8px 18px;font-size:15px;font-weight:900;border-radius:9px;cursor:pointer;background:linear-gradient(135deg,#c8963c,#8a5f1a);border:1.5px solid rgba(255,220,140,0.7);color:#fff;">🎁 補發</button>'
        + '</div>'
        + '<div style="font-size:12px;color:#c0b090;margin-top:6px;line-height:1.5;">只增不減:已馴養者僅提升等級(取較大值),絕不降級、不動好感/跟隨;寫入馴養帳本 src=admin_grant(合法來源佐證)並寄登入通知給玩家。建議玩家「離線時」操作,避免整包存檔覆寫 race。</div>'
      + '</div>'
      + '<div style="margin-top:12px;border:1.5px dashed rgba(150,210,255,0.6);border-radius:10px;padding:10px 12px;background:rgba(6,20,32,0.4);">'
        + '<div style="font-size:15px;color:#a8d8ff;font-weight:900;margin-bottom:6px;">♻️ 重置寵物隨機素質(GM 補償)</div>'
        + (_tamedNames.length
            ? ('<div style="display:flex;gap:8px;flex-wrap:wrap;align-items:center;">'
                + '<select id="_gm-pet-reset-sel" style="padding:7px 10px;font-size:14px;border-radius:8px;background:#0e1e2e;color:#eaf4ff;border:1px solid rgba(150,210,255,0.5);max-width:280px;">' + _resetOpts + '</select>'
                + '<button onclick="window._gmPetResetClick(\'' + _esc(_data.uid) + '\')" style="padding:8px 18px;font-size:15px;font-weight:900;border-radius:9px;cursor:pointer;background:linear-gradient(135deg,#3c8ac8,#1a5f8a);border:1.5px solid rgba(140,210,255,0.7);color:#fff;">♻️ 重置</button>'
              + '</div>'
              + '<div style="display:flex;gap:16px;flex-wrap:wrap;align-items:center;margin-top:8px;font-size:13px;color:#cfe4f8;">'
                + '<label style="cursor:pointer;"><input type="radio" name="_gm-pet-reset-mode" value="reroll" checked style="vertical-align:middle;"> 🎲 重擲隨機素質<span style="color:#9ac0dd;">(等級/經驗/好感全保留)</span></label>'
                + '<label style="cursor:pointer;"><input type="radio" name="_gm-pet-reset-mode" value="full" style="vertical-align:middle;"> 🔄 完全重置<span style="color:#9ac0dd;">(退 Lv.1、經驗歸零、重擲素質;好感保留)</span></label>'
              + '</div>'
              + '<div style="font-size:12px;color:#90b0c0;margin-top:6px;line-height:1.5;">重擲=換一組隨機素質分配(乙案 seed);完全重置額外把等級退回 Lv.1。兩者皆保留好感度與跟隨對象,並三槽同寫(壓過舊高等紀錄)、寄登入通知給玩家。建議玩家「離線時」操作。</div>')
            : '<div style="font-size:13px;color:#90a0b0;">該玩家尚無已馴養寵物,無可重置對象。</div>')
      + '</div>'
      + '<div style="text-align:center;margin-top:14px;"><button onclick="document.getElementById(\'_gm-pet-records-popup\').remove()" style="padding:9px 30px;font-size:17px;font-weight:700;background:rgba(60,160,110,0.7);border:2px solid rgba(160,255,200,0.7);color:#fff;border-radius:9px;cursor:pointer;font-family:inherit;letter-spacing:2px;">關閉</button></div>'
      + '</div>';
    document.body.appendChild(_pop);
  };

  // 彈窗「🎁 補發」點擊入口(讀 select/Lv → confirm → 執行 → 重整彈窗)
  window._gmPetGrantClick = async function(uid){
    try{
      const _sel = document.getElementById('_gm-pet-grant-sel');
      const _lvEl = document.getElementById('_gm-pet-grant-lv');
      const _pn = _sel ? String(_sel.value || '') : '';
      let _lv = _lvEl ? parseInt(_lvEl.value, 10) : 1;
      if(!(_lv >= 1)) _lv = 1;
      if(_lv > 20) _lv = 20;
      if(!_pn){ alert('請先選擇要補發的寵物'); return; }
      if(!confirm('確定要補發寵物「' + _pn + '」(Lv.' + _lv + ')給這位玩家嗎?\n(只增不減:若已馴養且等級較高,不會降級)')) return;
      const _res = await window._fbAdminGrantPetToPlayer(uid, _pn, _lv);
      alert('✅ 補發完成:' + _pn + '\n' + ((_res && _res.msg) ? _res.msg : ''));
      try{ window._fbShowPlayerPetRecords(uid); }catch(_){}
    }catch(e){
      alert('補發失敗:' + (e && e.message ? e.message : e));
    }
  };

  window._fbAdminGrantPetToPlayer = async function(uid, petName, lv){
    if(!_fbDb) throw new Error('Firestore 尚未就緒');
    if(!uid) throw new Error('缺少 uid');
    const _pn = String(petName || '');
    const _R = window.PET_RARITY || {};
    if(!_R[_pn]) throw new Error('無效的寵物名稱:' + _pn);
    let _lv = parseInt(lv, 10);
    if(!(_lv >= 1)) _lv = 1;
    if(_lv > 20) _lv = 20;
    const _by = (_fbAuth && _fbAuth.currentUser && _fbAuth.currentUser.email) || 'admin';
    const _now = Date.now();
    const _uid12 = String(uid).slice(0, 12);
    const _gds = (typeof getDocFromServer === 'function') ? getDocFromServer : getDoc;
    const _targets = [
      { ref: doc(_fbDb, 'players', uid), label: '主檔', mustExist: true },
      { ref: doc(_fbDb, 'players', uid, 'saves', 'live'), label: 'live', mustExist: false },
      { ref: doc(_fbDb, 'players', uid, 'saves', 'safe'), label: 'safe', mustExist: false }
    ];
    const _wrote = [], _skipped = [];
    let _finalLv = _lv, _existedBefore = false, _prevLv = 0;
    for(let i = 0; i < _targets.length; i++){
      const _t = _targets[i];
      let _snap = null;
      try{ _snap = await _gds(_t.ref); }catch(_e1){ try{ _snap = await getDoc(_t.ref); }catch(_e2){ _snap = null; } }
      const _ex = _snap && ((typeof _snap.exists === 'function') ? _snap.exists() : !!_snap.exists);
      if(!_ex){
        if(_t.mustExist) throw new Error('查無玩家主檔:' + uid);
        _skipped.push(_t.label);   // 槽不存在 → 不創建殘缺槽 doc(union 載入下主檔寫入已充分)
        continue;
      }
      const _d = _snap.data() || {};
      // 讀取口徑與 _applySafeData 一致:_s 優先,fallback map(深拷貝避免污染 snapshot)
      let _pets = null;
      if(typeof _d.tamedPets_s === 'string'){ try{ const _p = JSON.parse(_d.tamedPets_s); if(_p && typeof _p === 'object') _pets = _p; }catch(_){} }
      if(!_pets && _d.tamedPets && typeof _d.tamedPets === 'object'){ try{ _pets = JSON.parse(JSON.stringify(_d.tamedPets)); }catch(_){ _pets = null; } }
      _pets = _pets || {};
      let _hist = null;
      if(typeof _d._petTameHistory_s === 'string'){ try{ const _h = JSON.parse(_d._petTameHistory_s); if(Array.isArray(_h)) _hist = _h; }catch(_){} }
      if(!_hist && Array.isArray(_d._petTameHistory)){ try{ _hist = JSON.parse(JSON.stringify(_d._petTameHistory)); }catch(_){ _hist = null; } }
      _hist = _hist || [];
      const _ever = Array.isArray(_d.petsEverCollected) ? _d.petsEverCollected.slice() : [];
      // ── 只增不減 merge ──
      const _cur = _pets[_pn];
      if(!_cur){
        _pets[_pn] = { lv: _lv, exp: 0, followTo: null, aff: 0 };
      } else {
        _existedBefore = true;
        _prevLv = Math.max(_prevLv, (_cur.lv|0) || 1);
        if(_lv > ((_cur.lv|0) || 1)){ _cur.lv = _lv; _cur.exp = 0; }
        // 好感 / 跟隨 / 較高等級:一律保留不動
      }
      _hist.push({ pet: _pn, at: _now, src: 'admin_grant', uid: _uid12, by: _by });
      if(_hist.length > 200) _hist = _hist.slice(-200);
      if(_ever.indexOf(_pn) < 0) _ever.push(_pn);
      await setDoc(_t.ref, {
        tamedPets: _pets,
        tamedPets_s: JSON.stringify(_pets),
        _petTameHistory: _hist,
        _petTameHistory_s: JSON.stringify(_hist),
        petsEverCollected: _ever
      }, { merge: true });
      _wrote.push(_t.label);
      if(_t.mustExist){ _finalLv = ((_pets[_pn] && _pets[_pn].lv)|0) || _lv; }
    }
    // 通知玩家(登入彈窗;失敗不影響補發本體)
    try{
      if(typeof window._fbAdminSendNotificationToPlayer === 'function'){
        await window._fbAdminSendNotificationToPlayer(uid, {
          title: '🐾 老師補發寵物',
          body: '老師幫你補發了寵物「' + _pn + '」(Lv.' + _finalLv + ')!到「🐾 寵物圖鑑」看看牠吧!',
          type: 'compensation'
        });
      }
    }catch(_eN){ console.warn('[寵物補發] 通知寄送失敗(補發本體已完成)', _eN); }
    const _msg = (_existedBefore
        ? ('原已馴養 Lv.' + _prevLv + ((_lv > _prevLv) ? (' → 提升至 Lv.' + _lv) : '(原等級較高或相同,維持不變)'))
        : ('新收錄 Lv.' + _lv))
      + '。寫入:' + _wrote.join('/') + (_skipped.length ? ('(略過不存在槽:' + _skipped.join('/') + ')') : '')
      + '。已寄登入通知。';
    console.log('[_fbAdminGrantPetToPlayer] ✅', uid, _pn, _msg);
    return { ok: true, pet: _pn, lv: _finalLv, existedBefore: _existedBefore, msg: _msg };
  };

  // ★ v4.14.0 — GM 寵物重置補償(老師裁決:兩模式皆可)
  //   mode 'reroll' = 重擲隨機素質(換新 seed + retrainVer++;等級/經驗/好感/跟隨全保留)
  //   mode 'full'   = 完全重置(等級→1、經驗→0、換新 seed + retrainVer++;好感/跟隨保留)
  //   ── 三槽同寫(主檔+live+safe),因 'full' 降等必須壓過所有槽的舊高等紀錄(見 S5 合併驗證);
  //      retrainVer 統一設為「所有槽現有最大值 +1」→ 新 seed 在合併時必勝。只動已馴養寵物,不新建。
  window._fbAdminResetPetForPlayer = async function(uid, petName, mode){
    if(!_fbDb) throw new Error('Firestore 尚未就緒');
    if(!uid) throw new Error('缺少 uid');
    const _pn = String(petName || '');
    const _R = window.PET_RARITY || {};
    if(!_R[_pn]) throw new Error('無效的寵物名稱:' + _pn);
    const _mode = (mode === 'full') ? 'full' : 'reroll';
    const _by = (_fbAuth && _fbAuth.currentUser && _fbAuth.currentUser.email) || 'admin';
    const _now = Date.now();
    const _uid12 = String(uid).slice(0, 12);
    const _gds = (typeof getDocFromServer === 'function') ? getDocFromServer : getDoc;
    const _newSeed = (typeof window._petNewSeed === 'function') ? window._petNewSeed() : (Math.floor(Math.random()*0xffffffff) >>> 0);
    const _targets = [
      { ref: doc(_fbDb, 'players', uid), label: '主檔', mustExist: true },
      { ref: doc(_fbDb, 'players', uid, 'saves', 'live'), label: 'live', mustExist: false },
      { ref: doc(_fbDb, 'players', uid, 'saves', 'safe'), label: 'safe', mustExist: false }
    ];
    // ── 第 1 趟:讀全部槽,擷取每槽的 pet 紀錄 + 計算最大 retrainVer(供統一 +1) ──
    const _slotData = [];
    let _maxRV = 0, _foundAny = false, _prevLv = 0;
    for(let i = 0; i < _targets.length; i++){
      const _t = _targets[i];
      let _snap = null;
      try{ _snap = await _gds(_t.ref); }catch(_e1){ try{ _snap = await getDoc(_t.ref); }catch(_e2){ _snap = null; } }
      const _ex = _snap && ((typeof _snap.exists === 'function') ? _snap.exists() : !!_snap.exists);
      if(!_ex){ if(_t.mustExist) throw new Error('查無玩家主檔:' + uid); _slotData.push(null); continue; }
      const _d = _snap.data() || {};
      let _pets = null;
      if(typeof _d.tamedPets_s === 'string'){ try{ const _p = JSON.parse(_d.tamedPets_s); if(_p && typeof _p === 'object') _pets = _p; }catch(_){} }
      if(!_pets && _d.tamedPets && typeof _d.tamedPets === 'object'){ try{ _pets = JSON.parse(JSON.stringify(_d.tamedPets)); }catch(_){ _pets = null; } }
      _pets = _pets || {};
      _slotData.push({ t: _t, pets: _pets });
      const _cur = _pets[_pn];
      if(_cur){ _foundAny = true; _prevLv = Math.max(_prevLv, (_cur.lv|0) || 1); _maxRV = Math.max(_maxRV, (_cur.retrainVer|0) || 0); }
    }
    if(!_foundAny) throw new Error('該玩家尚未馴養寵物「' + _pn + '」,無法重置(可先用「🎁 指定補發寵物」發給玩家)');
    const _newRV = _maxRV + 1;
    // ── 第 2 趟:對「有該寵物」的槽套用重置並寫回 ──
    const _wrote = [];
    for(let i = 0; i < _slotData.length; i++){
      const _sd = _slotData[i];
      if(!_sd) continue;
      const _cur = _sd.pets[_pn];
      if(!_cur) continue;   // 該槽沒有此寵物 → 不建、不動(無高等紀錄可衝突)
      _cur.seed = _newSeed;
      _cur.retrainVer = _newRV;
      if(_mode === 'full'){ _cur.lv = 1; _cur.exp = 0; }   // 完全重置:退 Lv1、經驗歸零;好感/跟隨保留
      // reroll 模式:lv/exp/aff/followTo 全不動
      await setDoc(_sd.t.ref, {
        tamedPets: _sd.pets,
        tamedPets_s: JSON.stringify(_sd.pets)
      }, { merge: true });
      _wrote.push(_sd.t.label);
    }
    // 通知玩家(登入彈窗)
    try{
      if(typeof window._fbAdminSendNotificationToPlayer === 'function'){
        await window._fbAdminSendNotificationToPlayer(uid, {
          title: '🐾 老師調整了你的寵物',
          body: (_mode === 'full')
            ? ('老師幫寵物「' + _pn + '」完全重置了(等級回到 Lv.1、隨機素質重擲;好感度保留)!快去「🐾 寵物圖鑑」看看,並重新培養牠吧!')
            : ('老師幫寵物「' + _pn + '」重新洗牌了隨機素質(等級、經驗、好感度都不變)!到「🐾 寵物圖鑑」看看牠的新素質吧!'),
          type: 'compensation'
        });
      }
    }catch(_eN){ console.warn('[寵物重置] 通知寄送失敗(重置本體已完成)', _eN); }
    const _msg = (_mode === 'full'
        ? ('完全重置:等級 Lv.' + _prevLv + ' → Lv.1、隨機素質重擲(好感/跟隨保留)')
        : ('重擲隨機素質(等級 Lv.' + _prevLv + '、經驗、好感全保留)'))
      + '。寫入:' + (_wrote.join('/') || '(無)') + '。retrainVer→' + _newRV + '。已寄登入通知。';
    console.log('[_fbAdminResetPetForPlayer] ✅', uid, _pn, _mode, _msg);
    return { ok: true, pet: _pn, mode: _mode, msg: _msg };
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v5.32.0 — GM 後台「🛡️ 裝備管理」後端三函式(裝備 Phase 4 後半·admin_delete 稽核前例)
  //   UI 在 admin_panel.js v5.32.0「🛡️ 裝備管理」卡(三點同步);本區只做資料層。
  //   ・查詢 _fbAdminReadPlayerGear:email/lsps學號/uid → 主檔最新(getDocFromServer)→
  //     實例清單(_s 字串優先·附 GEAR_DB 顯示資料+詞條文字)+ 裝備帳本近 300 筆。
  //   ・發放 _fbAdminGearGrantForUid:GM 端 roll 詞條一次(規則同玩家取得·永久固定)→
  //     三槽同寫(主檔 mustExist + saves/live + saves/safe·寵物重置 v4.14.0 前例)同一顆實例
  //     src='admin_grant' + 帳本 admin_grant 紀錄(uid12=目標玩家·grantedBy=GM)→
  //     主檔蓋 _authoritativeRestoreAt(在線玩家 piece3 乾淨重載不互蓋)→ 寄登入通知。
  //     GM 發放豁免 100 件上限(Phase 1 ignoreCap 前例);回傳 count 供 UI 警示。
  //   ・刪除 _fbAdminGearDeleteForUid:三槽同寫刪實例 + 每槽帳本 push source:'admin_delete'
  //     (_lxpsLatestDeletedMap 稽核感知既有支援 → 殘槽合併不復活·同 sold/gift_sent 機制)→
  //     主檔蓋 _authoritativeRestoreAt → 寄登入通知。裝備中自動視同卸下(實例整顆刪除)。
  // ════════════════════════════════════════════════════════════════════
  window._fbAdminReadPlayerGear = async function(emailOrUid){
    if(!_fbDb) throw new Error('Firestore 尚未就緒');
    const _raw = String(emailOrUid || '').trim();
    if(!_raw) throw new Error('請提供 email、學號或 uid');
    let _uid = '', _email = '', _byEmail = '';
    if(_raw.indexOf('@') >= 0){ _byEmail = _raw.toLowerCase(); }
    else if(/^lsps\d+$/i.test(_raw)){ _byEmail = _raw.toLowerCase() + '@stu.lsps.tp.edu.tw'; }
    if(_byEmail){
      _email = _byEmail;
      const _q = query(collection(_fbDb, 'players'), where('email', '==', _email));
      const _snap = await getDocs(_q);
      if(_snap.empty) throw new Error('查無此玩家:' + _email);
      _snap.forEach(function(d){ if(!_uid) _uid = d.id; });
    } else {
      _uid = _raw;
    }
    const _gds = (typeof getDocFromServer === 'function') ? getDocFromServer : getDoc;
    const _mainSnap = await _gds(doc(_fbDb, 'players', _uid));
    const _mainEx = (typeof _mainSnap.exists === 'function') ? _mainSnap.exists() : !!_mainSnap.exists;
    if(!_mainEx) throw new Error('查無此玩家 doc:' + _uid);
    const _d = _mainSnap.data() || {};
    if(!_email) _email = _d.email || '';
    const _fromS = function(sKey, mKey, isArr){
      let v = null;
      if(typeof _d[sKey] === 'string'){ try{ const p = JSON.parse(_d[sKey]); if(p && (isArr ? Array.isArray(p) : (typeof p === 'object'))) v = p; }catch(_){} }
      if(!v && _d[mKey] && (isArr ? Array.isArray(_d[mKey]) : (typeof _d[mKey] === 'object'))) v = _d[mKey];
      return v || (isArr ? [] : {});
    };
    const _gd = _fromS('equipmentData_s', 'equipmentData', false);
    const _inst = (_gd && _gd.instances && typeof _gd.instances === 'object') ? _gd.instances : {};
    const _hist = _fromS('_equipUnlockHistory_s', '_equipUnlockHistory', true);
    // 附顯示資料(GEAR_DB const 由全域詞法環境讀取·typeof 守門)
    const _hasDb = (typeof GEAR_DB !== 'undefined') && (typeof GEAR_RARITY !== 'undefined');
    const _items = [];
    Object.keys(_inst).forEach(function(eqUid){
      const rec = _inst[eqUid];
      if(!rec || !rec.itemId) return;
      const def = _hasDb ? GEAR_DB[rec.itemId] : null;
      const rr = (def && GEAR_RARITY[def.r]) || {};
      const si = (def && typeof GEAR_SLOT_INFO !== 'undefined' && GEAR_SLOT_INFO[def.slot]) || {};
      _items.push({
        eqUid: eqUid, itemId: rec.itemId,
        n: (def ? def.n : rec.itemId), icon: ((def && def.icon) || '🛡️'),
        slot: (def ? def.slot : '?'), slotName: (si.n || '?'),
        r: (def ? def.r : '?'), rn: (rr.name || '?'), rc: (rr.color || '#cfcfe6'),
        equippedTo: (rec.equippedTo || null), src: (rec.src || '?'),
        at: (rec.at || 0), gifted: !!rec.gifted, rr2: (rec.rr | 0) || 0,
        baseTxt: (def && typeof window._gearEffText === 'function')
          ? (def.eff || []).map(function(e){ return window._gearEffText(e, null); }).join('、') : '',
        affixLines: (typeof window._gearAffixText === 'function')
          ? (rec.affixes || []).map(function(af){ return window._gearAffixText(af); }) : []
      });
    });
    return { uid: _uid, email: _email, nickname: (_d.nickname || _d.displayName || ''),
             count: _items.length, items: _items, ledger: _hist };
  };

  // ════════════════════════════════════════════════════════════════
  // ★★ v5.135.0 — 召喚卷「逐張查證」稽核後端（老師裁定②甲）★★
  // ────────────────────────────────────────────────────────────────
  //   舊稽核靠減法推算（應有 = 發出 − 未用），券沒有身分就只能猜；
  //   券編號制上線後改成**逐張查證**：每一張券的編號、券種、來源、取得時間
  //   全部攤開，已用掉的有 used 清單為證，數字對不對一眼就看得出來。
  //   ★ 純讀取零寫入（同 v5.33.0 圖鑑頁鐵則⑧）。
  //   ★ 三槽都讀並套用與玩家端**完全同一套**合併規則
  //     （inst union → used union → un 取 max → inst \ used），
  //     否則 GM 看到的數字會和學生實際登入後看到的不一樣，比沒有工具更糟。
  //   ★ 對帳結論直接沿用玩家端公式：需補 = 背包 −(實例 + un)
  //     ＝ 老師看到的「幽靈券」數，同一條算式不會出現兩套口徑。
  window._fbAdminReadPlayerTickets = async function(emailOrUid){
    if(!_fbDb) throw new Error('Firestore 尚未就緒');
    const _raw = String(emailOrUid || '').trim();
    if(!_raw) throw new Error('請提供 email、學號或 uid');
    let _uid = '', _email = '';
    let _byEmail = '';
    if(_raw.indexOf('@') >= 0){ _byEmail = _raw.toLowerCase(); }
    else if(/^lsps\d+$/i.test(_raw)){ _byEmail = _raw.toLowerCase() + '@stu.lsps.tp.edu.tw'; }
    if(_byEmail){
      _email = _byEmail;
      const _q = query(collection(_fbDb, 'players'), where('email', '==', _email));
      const _snap = await getDocs(_q);
      if(_snap.empty) throw new Error('查無此玩家:' + _email);
      _snap.forEach(function(d){ if(!_uid) _uid = d.id; });
    } else { _uid = _raw; }

    const _gds = (typeof getDocFromServer === 'function') ? getDocFromServer : getDoc;
    const _targets = [
      { ref: doc(_fbDb, 'players', _uid), label: '主檔', mustExist: true },
      { ref: doc(_fbDb, 'players', _uid, 'saves', 'live'), label: 'live', mustExist: false },
      { ref: doc(_fbDb, 'players', _uid, 'saves', 'safe'), label: 'safe', mustExist: false }
    ];
    const _slots = [];
    let _mainD = null;
    for(let i = 0; i < _targets.length; i++){
      const _t = _targets[i];
      let _snap = null;
      try{ _snap = await _gds(_t.ref); }catch(_e1){ try{ _snap = await getDoc(_t.ref); }catch(_e2){ _snap = null; } }
      const _ex = _snap && ((typeof _snap.exists === 'function') ? _snap.exists() : !!_snap.exists);
      if(!_ex){ if(_t.mustExist) throw new Error('查無此玩家 doc:' + _uid); continue; }
      const _d = _snap.data() || {};
      if(_t.label === '主檔') _mainD = _d;
      _slots.push({ label: _t.label, d: _d });
    }
    if(!_mainD) throw new Error('查無此玩家 doc:' + _uid);
    if(!_email) _email = _mainD.email || '';

    // ── 券實例三槽合併（與 _lxpsMergeSlots 5c 完全同一套規則）──
    const _inst = {}, _usedSet = {}, _un = {};
    let _bf = false, _slotsWithData = [];
    _slots.forEach(function(sl){
      let td = null;
      try{ if(typeof sl.d.ticketData_s === 'string' && sl.d.ticketData_s){ const p = JSON.parse(sl.d.ticketData_s); if(p && typeof p === 'object' && !Array.isArray(p)) td = p; } }catch(_){}
      if(!td && sl.d.ticketData && typeof sl.d.ticketData === 'object' && !Array.isArray(sl.d.ticketData)){
        try{ td = JSON.parse(JSON.stringify(sl.d.ticketData)); }catch(_){ td = null; }
      }
      if(!td) return;
      _slotsWithData.push(sl.label);
      Object.keys(td.inst || {}).forEach(function(tid){
        const r = td.inst[tid];
        if(!r || typeof r !== 'object' || !r.it) return;
        if(!_inst[tid]){ try{ _inst[tid] = JSON.parse(JSON.stringify(r)); }catch(_){ _inst[tid] = r; } }
      });
      (Array.isArray(td.used) ? td.used : []).forEach(function(tid){ if(tid) _usedSet[tid] = 1; });
      Object.keys(td.un || {}).forEach(function(k){
        const v = parseInt(td.un[k], 10) || 0;
        if(!(k in _un) || v > _un[k]) _un[k] = v;
      });
      if(td.bf) _bf = true;
    });
    Object.keys(_usedSet).forEach(function(tid){ if(_inst[tid]) delete _inst[tid]; });   // ★ 已用券不列為持有

    // ── 主檔背包（_s 字串優先·v3.15.57 洗錢口徑）──
    let _bag = {};
    try{
      if(typeof _mainD.playerBackpack_s === 'string'){ _bag = JSON.parse(_mainD.playerBackpack_s) || {}; }
      else if(_mainD.playerBackpack && typeof _mainD.playerBackpack === 'object'){ _bag = Object.assign({}, _mainD.playerBackpack); }
    }catch(_){ _bag = {}; }
    if(!_bag || typeof _bag !== 'object' || Array.isArray(_bag)) _bag = {};

    // ── 逐張明細 ──
    const _defOf = function(id){ try{ return (typeof BACKPACK_ITEM_DEF !== 'undefined') ? BACKPACK_ITEM_DEF[id] : null; }catch(_){ return null; } };
    const _items = [];
    Object.keys(_inst).forEach(function(tid){
      const r = _inst[tid];
      const def = _defOf(r.it);
      _items.push({
        tid: tid, itemId: r.it,
        name: (def && def.name) || r.it,
        icon: (def && def.icon) || '\u{1F3AB}',
        src: r.src || '?', at: r.at || 0,
        legacy: !!r.lg,
        isGm: !!(window._isGmTicketId && window._isGmTicketId(r.it))
      });
    });
    _items.sort(function(a, b){ return (a.itemId < b.itemId ? -1 : a.itemId > b.itemId ? 1 : (a.at - b.at)); });

    // ── 逐券種對帳（口徑與玩家端 _ticketReconcile 完全一致）──
    const _rows = [];
    const _ids = (window._TICKET_INST_IDS && Object.keys(window._TICKET_INST_IDS)) || [];
    _ids.forEach(function(id){
      const _bagN  = parseInt(_bag[id], 10) || 0;
      const _haveN = _items.filter(function(o){ return o.itemId === id; }).length;
      const _unN   = parseInt(_un[id], 10) || 0;
      if(!_bagN && !_haveN && !_unN) return;
      const def = _defOf(id);
      _rows.push({
        itemId: id, name: (def && def.name) || id, icon: (def && def.icon) || '\u{1F3AB}',
        bag: _bagN, held: _haveN, used: _unN,
        // 需補 > 0 ＝ 背包數字比帳目多出來的部分（未納管的新券；正常應為 0）
        pending: Math.max(0, _bagN - (_haveN + _unN)),
        // 背包比實例少 ＝ 上次登入投影尚未寫回，屬正常（學生下次登入自動收斂）
        projectDiff: _bagN - _haveN,
        legacy: _items.filter(function(o){ return o.itemId === id && o.legacy; }).length
      });
    });

    return {
      uid: _uid, email: _email,
      nickname: (_mainD.nickname || _mainD.displayName || ''),
      migrated: _bf,                       // 是否已完成首次券編號 backfill
      slots: _slotsWithData,
      heldCount: _items.length,
      usedCount: Object.keys(_usedSet).length,
      items: _items, rows: _rows,
      legacyCount: _items.filter(function(o){ return o.legacy; }).length
    };
  };

  window._fbAdminGearGrantForUid = async function(uid, itemId){
    if(!_fbDb) throw new Error('Firestore 尚未就緒');
    if(!uid) throw new Error('缺少 uid');
    const _iid = String(itemId || '');
    if(typeof GEAR_DB === 'undefined' || !GEAR_DB[_iid]) throw new Error('無效的裝備 itemId:' + _iid);
    const _def = GEAR_DB[_iid];
    const _by = (_fbAuth && _fbAuth.currentUser && _fbAuth.currentUser.email) || 'admin';
    const _now = Date.now();
    const _uid12 = String(uid).slice(0, 12);
    const _gds = (typeof getDocFromServer === 'function') ? getDocFromServer : getDoc;
    // roll 一次·三槽共用同一顆實例(規則同玩家取得·取得當下永久固定)
    const _afs = (typeof window._gearRollAffixes === 'function') ? window._gearRollAffixes(_iid) : [];
    const _eqUid = (typeof window._gearGenUid === 'function') ? window._gearGenUid()
                 : ('eq_' + _now.toString(36) + '_' + Math.random().toString(36).slice(2, 8));
    const _newInst = { itemId: _iid, affixes: _afs, gifted: false, equippedTo: null,
                       src: 'admin_grant', at: _now, at2: _now, rr: 0 };
    const _ledgerRec = { eqUid: _eqUid, itemId: _iid, src: 'admin_grant', uid12: _uid12, at: _now, grantedBy: _by };
    const _targets = [
      { ref: doc(_fbDb, 'players', uid), label: '主檔', mustExist: true },
      { ref: doc(_fbDb, 'players', uid, 'saves', 'live'), label: 'live', mustExist: false },
      { ref: doc(_fbDb, 'players', uid, 'saves', 'safe'), label: 'safe', mustExist: false }
    ];
    const _wrote = [];
    let _mainCount = 0;
    for(let i = 0; i < _targets.length; i++){
      const _t = _targets[i];
      let _snap = null;
      try{ _snap = await _gds(_t.ref); }catch(_e1){ try{ _snap = await getDoc(_t.ref); }catch(_e2){ _snap = null; } }
      const _ex = _snap && ((typeof _snap.exists === 'function') ? _snap.exists() : !!_snap.exists);
      if(!_ex){ if(_t.mustExist) throw new Error('查無玩家主檔:' + uid); continue; }
      const _d = _snap.data() || {};
      let _gd = null;
      if(typeof _d.equipmentData_s === 'string'){ try{ const p = JSON.parse(_d.equipmentData_s); if(p && typeof p === 'object') _gd = p; }catch(_){} }
      if(!_gd && _d.equipmentData && typeof _d.equipmentData === 'object'){ try{ _gd = JSON.parse(JSON.stringify(_d.equipmentData)); }catch(_){ _gd = null; } }
      if(!_gd || typeof _gd !== 'object') _gd = { instances:{} };
      if(!_gd.instances || typeof _gd.instances !== 'object') _gd.instances = {};
      _gd.instances[_eqUid] = _newInst;
      let _hist = null;
      if(typeof _d._equipUnlockHistory_s === 'string'){ try{ const p = JSON.parse(_d._equipUnlockHistory_s); if(Array.isArray(p)) _hist = p; }catch(_){} }
      if(!_hist && Array.isArray(_d._equipUnlockHistory)) _hist = _d._equipUnlockHistory.slice();
      _hist = _hist || [];
      _hist.push(_ledgerRec);
      if(_hist.length > 300) _hist = _hist.slice(-300);
      const _patch = {
        equipmentData: _gd, equipmentData_s: JSON.stringify(_gd),
        _equipUnlockHistory: _hist, _equipUnlockHistory_s: JSON.stringify(_hist)
      };
      if(_t.label === '主檔'){ _patch._authoritativeRestoreAt = _now; _mainCount = Object.keys(_gd.instances).length; }
      await setDoc(_t.ref, _patch, { merge: true });
      _wrote.push(_t.label);
    }
    // 通知玩家(登入彈窗)
    try{
      if(typeof window._fbAdminSendNotificationToPlayer === 'function'){
        const _rn = (typeof GEAR_RARITY !== 'undefined' && GEAR_RARITY[_def.r] && GEAR_RARITY[_def.r].name) || '稀有';
        await window._fbAdminSendNotificationToPlayer(uid, {
          title: '🛡️ 老師送你一件裝備!',
          body: '老師發給你 ' + (_def.icon || '🛡️') + ' ' + _rn + '裝備「' + _def.n + '」×1(詞條 ' + _afs.length + ' 條·當場隨機決定永久固定)!到英雄圖鑑的「🛡️ 裝備欄位」幫夥伴穿上吧!',
          type: 'compensation'
        });
      }
    }catch(_eN){ console.warn('[GM裝備發放] 通知寄送失敗(發放本體已完成)', _eN); }
    console.log('[_fbAdminGearGrantForUid] ✅', uid, _iid, _eqUid, '寫入:' + _wrote.join('/'));
    return { ok: true, eqUid: _eqUid, itemId: _iid, name: _def.n, affixN: _afs.length,
             count: _mainCount, wrote: _wrote };
  };

  window._fbAdminGearDeleteForUid = async function(uid, eqUid){
    if(!_fbDb) throw new Error('Firestore 尚未就緒');
    if(!uid) throw new Error('缺少 uid');
    const _eq = String(eqUid || '');
    if(!_eq) throw new Error('缺少 eqUid');
    const _by = (_fbAuth && _fbAuth.currentUser && _fbAuth.currentUser.email) || 'admin';
    const _now = Date.now();
    const _uid12 = String(uid).slice(0, 12);
    const _gds = (typeof getDocFromServer === 'function') ? getDocFromServer : getDoc;
    const _targets = [
      { ref: doc(_fbDb, 'players', uid), label: '主檔', mustExist: true },
      { ref: doc(_fbDb, 'players', uid, 'saves', 'live'), label: 'live', mustExist: false },
      { ref: doc(_fbDb, 'players', uid, 'saves', 'safe'), label: 'safe', mustExist: false }
    ];
    let _itemId = '', _foundAny = false;
    const _wrote = [];
    for(let i = 0; i < _targets.length; i++){
      const _t = _targets[i];
      let _snap = null;
      try{ _snap = await _gds(_t.ref); }catch(_e1){ try{ _snap = await getDoc(_t.ref); }catch(_e2){ _snap = null; } }
      const _ex = _snap && ((typeof _snap.exists === 'function') ? _snap.exists() : !!_snap.exists);
      if(!_ex){ if(_t.mustExist) throw new Error('查無玩家主檔:' + uid); continue; }
      const _d = _snap.data() || {};
      let _gd = null;
      if(typeof _d.equipmentData_s === 'string'){ try{ const p = JSON.parse(_d.equipmentData_s); if(p && typeof p === 'object') _gd = p; }catch(_){} }
      if(!_gd && _d.equipmentData && typeof _d.equipmentData === 'object'){ try{ _gd = JSON.parse(JSON.stringify(_d.equipmentData)); }catch(_){ _gd = null; } }
      if(!_gd || typeof _gd !== 'object') _gd = { instances:{} };
      if(!_gd.instances || typeof _gd.instances !== 'object') _gd.instances = {};
      const _has = !!_gd.instances[_eq];
      if(_has){
        _foundAny = true;
        if(!_itemId) _itemId = (_gd.instances[_eq].itemId || '');
        delete _gd.instances[_eq];
      }
      // ★ 帳本 admin_delete「三槽都寫」:任一殘槽 union 到本筆即排除該 eqUid(稽核感知既有機制),
      //   玩家在線覆寫主檔/live 時 safe 槽仍留鐵證 → 永不復活(admin_delete 英雄前例同款保險)。
      let _hist = null;
      if(typeof _d._equipUnlockHistory_s === 'string'){ try{ const p = JSON.parse(_d._equipUnlockHistory_s); if(Array.isArray(p)) _hist = p; }catch(_){} }
      if(!_hist && Array.isArray(_d._equipUnlockHistory)) _hist = _d._equipUnlockHistory.slice();
      _hist = _hist || [];
      _hist.push({ eqUid: _eq, itemId: (_itemId || ''), src: 'admin', source: 'admin_delete',
                   uid12: _uid12, at: _now, removedBy: _by });
      if(_hist.length > 300) _hist = _hist.slice(-300);
      const _patch = {
        equipmentData: _gd, equipmentData_s: JSON.stringify(_gd),
        _equipUnlockHistory: _hist, _equipUnlockHistory_s: JSON.stringify(_hist)
      };
      if(_t.label === '主檔') _patch._authoritativeRestoreAt = _now;
      await setDoc(_t.ref, _patch, { merge: true });
      _wrote.push(_t.label);
    }
    if(!_foundAny) return { ok: false, reason: 'not_found', msg: '三槽皆查無此裝備實例(可能已被賣出/送出/刪除)' };
    const _def = (typeof GEAR_DB !== 'undefined') ? GEAR_DB[_itemId] : null;
    // 通知玩家(登入彈窗)
    try{
      if(typeof window._fbAdminSendNotificationToPlayer === 'function'){
        await window._fbAdminSendNotificationToPlayer(uid, {
          title: '🛡️ 老師調整了你的裝備',
          body: '老師從你的裝備背包移除了 ' + ((_def && _def.icon) || '🛡️') + '「' + ((_def && _def.n) || _itemId) + '」×1(若正裝備在夥伴身上會自動卸下)。有疑問可以用「🙋 求救與回報」告訴老師。',
          type: 'warning'
        });
      }
    }catch(_eN){ console.warn('[GM裝備刪除] 通知寄送失敗(刪除本體已完成)', _eN); }
    console.log('[_fbAdminGearDeleteForUid] ✅', uid, _eq, _itemId, '寫入:' + _wrote.join('/'));
    return { ok: true, eqUid: _eq, itemId: _itemId, name: ((_def && _def.n) || _itemId), wrote: _wrote };
  };

  // 彈窗「♻️ 重置」點擊入口(讀 select/mode → confirm → 執行 → 重整彈窗)
  window._gmPetResetClick = async function(uid){
    try{
      const _sel = document.getElementById('_gm-pet-reset-sel');
      const _pn = _sel ? String(_sel.value || '') : '';
      const _modeEl = document.querySelector('input[name="_gm-pet-reset-mode"]:checked');
      const _mode = _modeEl ? String(_modeEl.value) : 'reroll';
      if(!_pn){ alert('請先選擇要重置的寵物(需為該玩家已馴養者)'); return; }
      const _modeTxt = (_mode === 'full') ? '完全重置(退 Lv.1、重擲隨機素質;好感保留)' : '重擲隨機素質(等級/經驗/好感全保留)';
      if(!confirm('確定要對玩家的寵物「' + _pn + '」執行:\n' + _modeTxt + ' 嗎?')) return;
      const _res = await window._fbAdminResetPetForPlayer(uid, _pn, _mode);
      alert('✅ 重置完成:' + _pn + '\n' + ((_res && _res.msg) ? _res.msg : ''));
      try{ window._fbShowPlayerPetRecords(uid); }catch(_){}
    }catch(e){
      alert('重置失敗:' + (e && e.message ? e.message : e));
    }
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.15.26(2026-06-17)新增 — 虛寶序號(課堂獎勵兌換券)系統
  //   GM 後台勾獎勵+數量 → 批量產生「一次性序號」寫 redeemCodes/{CODE};
  //   學生主選單「🎟️ 序號兌換」輸入序號 → 兌換到自己帳號(複用 _fbCompensatePlayer)。
  //   ── 單次使用:每個序號只能被兌換一次,兌過再輸入顯示「此序號已被使用」。
  //   ⚠ 需在 Firestore Console 部署 redeemCodes 規則後才生效(見 MEMORY_HANDOFF / firestore.rules)。
  //   資料結構 redeemCodes/{CODE} = {
  //     code, reward(_fbCompensatePlayer payload), itemsLabel[],
  //     redeemed:bool, redeemedBy, redeemedByLabel, redeemedAt,
  //     expiresAt(0=永久), batchId, note, createdAt, createdBy, enabled
  //   }
  //   安全:玩家本來就能寫自己 doc(反作弊靠 GM 異常掃描),序號系統只多防「同序號重複兌換」。
  //         rule 限玩家僅能把 redeemed:false→true(且 redeemedBy==自己),動不了 reward/有效期。
  // ════════════════════════════════════════════════════════════════════
  // 序號產生器:12 碼大寫英數,排除易混淆字元 0/O/1/I/L(比照邀請碼)。
  window._lxpsGenRedeemCode = function(){
    const _chars = 'ABCDEFGHJKMNPQRSTUVWXYZ23456789';   // 排除 0 O 1 I L
    let _s = '';
    for(let i = 0; i < 12; i++){ _s += _chars.charAt(Math.floor(Math.random() * _chars.length)); }
    return _s;   // 例:XK3PQ9MNW2HT
  };

  // GM:批量產生序號。spec = { reward, itemsLabel, count, expiresAt, note }
  //   回傳 { ok, codes:[...], fail:[...], batchId, expiresAt, itemsLabel }
  window._fbGenerateRedeemCodes = async function(spec){
    if(!spec || typeof spec !== 'object') throw new Error('spec 為必要');
    const _reward = (spec.reward && typeof spec.reward === 'object') ? spec.reward : null;
    const _labels = Array.isArray(spec.itemsLabel) ? spec.itemsLabel.map(s => String(s)).slice(0, 30) : [];
    const _count  = Math.max(1, Math.min(200, parseInt(spec.count, 10) || 1));   // 一批最多 200
    const _expiresAt = (typeof spec.expiresAt === 'number' && spec.expiresAt > 0) ? spec.expiresAt : 0;
    const _note = String(spec.note || '').slice(0, 120);
    if(!_reward || !_labels.length) throw new Error('獎勵內容不可為空');
    const _by = (window._fbUser && window._fbUser.email) || 'admin';
    const _batchId = 'B' + Date.now().toString(36) + Math.random().toString(36).slice(2, 6);
    const _codes = [], _fail = [];
    for(let i = 0; i < _count; i++){
      const _code = window._lxpsGenRedeemCode();
      try{
        // runTransaction 確保「不存在才寫」,避免極低機率撞號覆蓋既有序號
        const _written = await runTransaction(_fbDb, async (tx) => {
          const _ref = doc(_fbDb, 'redeemCodes', _code);
          const _snap = await tx.get(_ref);
          if(_snap.exists()) return false;   // 撞號 → 跳過(不重試,避免迴圈)
          tx.set(_ref, {
            code: _code,
            reward: _reward,
            itemsLabel: _labels,
            redeemed: false,
            redeemedBy: '',
            redeemedByLabel: '',
            redeemedAt: 0,
            expiresAt: _expiresAt,
            batchId: _batchId,
            note: _note,
            createdAt: Date.now(),
            createdBy: _by,
            enabled: true,
          });
          return true;
        });
        if(_written){ _codes.push(_code); } else { _fail.push(_code + '(撞號跳過)'); }
      }catch(e){
        _fail.push(_code + ':' + (e && e.message || e));
      }
    }
    return { ok: _codes.length > 0, codes: _codes, fail: _fail, batchId: _batchId, expiresAt: _expiresAt, itemsLabel: _labels };
  };

  // GM:列出序號清單(新→舊;預設 200 筆)。回傳陣列。
  window._fbListRedeemCodes = async function(maxN){
    const _n = Math.max(1, Math.min(500, parseInt(maxN, 10) || 200));
    let _snap;
    try{
      const _q = query(collection(_fbDb, 'redeemCodes'), orderBy('createdAt', 'desc'), limit(_n));
      _snap = await getDocs(_q);
    }catch(_eOrder){
      console.warn('[_fbListRedeemCodes] orderBy fallback', _eOrder);
      const _q2 = query(collection(_fbDb, 'redeemCodes'), limit(_n));
      _snap = await getDocs(_q2);
    }
    const _list = [];
    _snap.forEach(d => {
      const _x = d.data() || {};
      _list.push({
        code: _x.code || d.id,
        itemsLabel: Array.isArray(_x.itemsLabel) ? _x.itemsLabel : [],
        redeemed: !!_x.redeemed,
        redeemedByLabel: _x.redeemedByLabel || '',
        redeemedAt: _x.redeemedAt || 0,
        expiresAt: _x.expiresAt || 0,
        enabled: _x.enabled !== false,
        note: _x.note || '',
        batchId: _x.batchId || '',
        createdAt: _x.createdAt || 0,
        createdBy: _x.createdBy || '',
      });
    });
    _list.sort((a, b) => (b.createdAt || 0) - (a.createdAt || 0));
    return _list;
  };

  // GM:刪除序號(已兌換者的玩家獎勵不受影響,只是讓此序號無法再被兌換/查到)
  window._fbAdminDeleteRedeemCode = async function(code){
    const _c = String(code || '').trim().toUpperCase().replace(/[^A-Z0-9]/g, '');
    if(!_c) throw new Error('序號不可為空');
    await deleteDoc(doc(_fbDb, 'redeemCodes', _c));
    return true;
  };

  // 玩家:兌換序號(單次使用)。回傳 { ok, itemsLabel, reason, _critical }
  window._fbRedeemCode = async function(code){
    const _uid = (window._fbUser && window._fbUser.uid) || null;
    if(!_uid) return { ok: false, reason: '尚未登入,請重新整理後再試' };
    // normalize:去掉非英數、轉大寫(學生可能打成小寫或加 - 空格)
    const _c = String(code || '').toUpperCase().replace(/[^A-Z0-9]/g, '');
    if(!_c || _c.length < 6) return { ok: false, reason: '請輸入正確的序號' };
    const _ref = doc(_fbDb, 'redeemCodes', _c);
    // 先讀一次給友善訊息(rule + transaction 是最後防線)
    let _data = null;
    try{
      const _snap = await getDocFromServer(_ref);
      // ★ v3.15.75 — 相容 .exists 為方法(v9 modular)或布林屬性,確保「查無此序號」一定判得出來,絕不 fall-through 成功
      const _exists = (typeof _snap.exists === 'function') ? _snap.exists() : !!_snap.exists;
      if(!_exists) return { ok: false, reason: '查無此序號,請確認有沒有打錯' };
      _data = _snap.data() || {};
    }catch(e){
      console.warn('[_fbRedeemCode] 讀取失敗', e);
      // ★ v3.15.75 — 權限被拒(redeemCodes 規則未部署)與一般網路問題分開提示;兩者一律「不發獎」
      const _pd = !!(e && (e.code === 'permission-denied' || /permission|insufficient/i.test(String((e && e.message) || ''))));
      return { ok: false, reason: _pd ? '序號系統尚未啟用,請聯絡老師' : '讀取序號失敗,請檢查網路後再試' };
    }
    // ★ v3.15.75 — 結構驗證:真正的序號一定帶 reward / itemsLabel / createdBy / batchId 其中之一,
    //   防「剛好存在但不是序號」的文件被當成功兌換(亂打序號 → 查無此序號,絕不發獎)。
    const _looksLikeCode = !!(_data && ((_data.reward && typeof _data.reward === 'object')
      || (Array.isArray(_data.itemsLabel) && _data.itemsLabel.length > 0)
      || _data.createdBy || _data.batchId));
    if(!_looksLikeCode) return { ok: false, reason: '查無此序號,請確認有沒有打錯' };
    if(_data.enabled === false) return { ok: false, reason: '此序號已停用' };
    if(_data.expiresAt && Date.now() > _data.expiresAt) return { ok: false, reason: '此序號已過期' };
    if(_data.redeemed) return { ok: false, reason: '此序號已被使用' };
    // transaction 原子標記:防同時兩人兌同一序號(誰先標記成功誰得)
    const _label = ((window._fbUser && window._fbUser.email) || '') || _uid;
    let _txResult;
    try{
      _txResult = await runTransaction(_fbDb, async (tx) => {
        const _s = await tx.get(_ref);
        // ★ v3.15.75 — 同上 .exists 相容,交易內存在性檢查也絕不誤判(最後防線)
        const _sExists = (typeof _s.exists === 'function') ? _s.exists() : !!_s.exists;
        if(!_sExists) return { ok: false, reason: '查無此序號' };
        const _d = _s.data() || {};
        if(_d.enabled === false) return { ok: false, reason: '此序號已停用' };
        if(_d.expiresAt && Date.now() > _d.expiresAt) return { ok: false, reason: '此序號已過期' };
        if(_d.redeemed) return { ok: false, reason: '此序號已被使用' };
        tx.update(_ref, {
          redeemed: true,
          redeemedBy: _uid,
          redeemedByLabel: String(_label || '').slice(0, 60),
          redeemedAt: Date.now(),
        });
        return { ok: true, reward: _d.reward || {}, itemsLabel: Array.isArray(_d.itemsLabel) ? _d.itemsLabel : [] };
      });
    }catch(e){
      // permission denied(rule resource.redeemed==false 不成立=剛被搶兌)或其他
      console.warn('[_fbRedeemCode] transaction 失敗', e);
      return { ok: false, reason: '兌換失敗,可能此序號剛被別人使用,請再確認' };
    }
    if(!_txResult || !_txResult.ok) return { ok: false, reason: (_txResult && _txResult.reason) || '兌換失敗' };
    // 標記成功 → 發獎到自己(複用 _fbCompensatePlayer,union 合併不降級)
    //   ★ 先標記再發獎:寧可偶爾(發獎異常時)漏發、由老師補,也不可「先發獎後標記失敗」造成重複刷
    try{
      if(typeof window._fbCompensatePlayer !== 'function') throw new Error('_fbCompensatePlayer 未載入');
      await window._fbCompensatePlayer(_uid, Object.assign({}, _txResult.reward, {
        reason: '虛寶序號兌換',
        summary: '虛寶序號:' + ((_txResult.itemsLabel || []).join(' + ') || _c),
        by: 'redeem:' + _c,
      }));
    }catch(e){
      console.error('[_fbRedeemCode] 已標記但發獎失敗(序號已消耗)', e);
      return { ok: false, reason: '兌換異常,請截圖這個序號找老師補發', _critical: true, itemsLabel: _txResult.itemsLabel };
    }
    // ★ v3.17.8 — 發獎成功 → 自寫一筆 _redeemClaimLog(self-write·不需改 rule),
    //   供 _fbReconcileRedeemCodeTickets 登入回溯對帳讀取(避免對 redeemCodes 集合做 list 查詢——
    //   該集合 list 權限僅限管理員,玩家用 query+getDocs 會被規則擋下,故改讀自己主檔的紀錄)。
    try{
      const _rps = await getDocFromServer(doc(_fbDb, 'players', _uid));
      let _rlog = [];
      if(_rps.exists()){ const _rpd = _rps.data() || {}; if(Array.isArray(_rpd._redeemClaimLog)) _rlog = _rpd._redeemClaimLog.slice(); }
      if(!_rlog.some(function(e){ return e && e.code === _c; })){
        _rlog.push({ code: _c, reward: _txResult.reward || {}, itemsLabel: (_txResult.itemsLabel || []).slice(0, 40), at: Date.now() });
        if(_rlog.length > 200) _rlog = _rlog.slice(-200);
        await setDoc(doc(_fbDb, 'players', _uid), { _redeemClaimLog: _rlog }, { merge: true });
      }
    }catch(_eLog){ console.warn('[_fbRedeemCode] 寫入 _redeemClaimLog 失敗(不影響已發獎·僅影響回溯對帳)', _eLog); }
    // ★ v3.17.8 — 回傳結構化 reward,讓 client 領取後「即時把獎勵套進記憶體」(比照 v3.16.97 GM課堂獎勵修法,
    //   免疫 _lxpsMergeSlots 消耗型欄位「只取最新槽」— 若玩家兌換後沒有立刻重整就繼續遊戲,
    //   後續 autosave 帶著舊記憶體背包寫槽會把剛兌換的券沖掉,套進記憶體後就不會了)
    return { ok: true, itemsLabel: _txResult.itemsLabel || [], reward: (_txResult.reward && typeof _txResult.reward === 'object') ? _txResult.reward : {} };
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.17.8 — 回溯救援:登入對帳「已兌換序號自選卷·實際沒拿到的補上」
  //   鏡像 v3.16.97 的 _fbReconcileGmClassRewardTickets;來源改讀玩家自己主檔的 _redeemClaimLog
  //   (self-write·由 _fbRedeemCode 兌換成功時寫入,不對 redeemCodes 集合做 list 查詢——
  //   該集合 list 權限僅限管理員,一般玩家 query+getDocs 會被 rules 擋下)。
  //   同一份 _GMCR_PICK_TICKET_SRC 定義(4 種自選卷)。
  //   安全不等式同款:已發(兌換序號 reward.backpack)− 已用(durable 帳本)− 現有(背包)− 已補(冪等記) > 0 才補。
  //   ⚠ 邊界:v3.17.8 之前兌換的序號沒有 _redeemClaimLog 紀錄,無法回溯對帳(僅涵蓋本次修復後的兌換)。
  // ════════════════════════════════════════════════════════════════════
  window._fbReconcileRedeemCodeTickets = async function(){
    try{
      var _uid = (window._fbUser && window._fbUser.uid) || window._gUserId || null;
      if(!_uid || !_fbDb || !window._GMCR_PICK_TICKET_SRC) return;
      var _pd = {};
      try{ var _ps = await getDocFromServer(doc(_fbDb, 'players', _uid)); if(_ps.exists()) _pd = _ps.data() || {}; }
      catch(_ePd){ return; }
      var _mine = Array.isArray(_pd._redeemClaimLog) ? _pd._redeemClaimLog : [];
      if(!_mine.length) return;
      var _recovered = (_pd._redeemRecoveredTickets && typeof _pd._redeemRecoveredTickets === 'object') ? Object.assign({}, _pd._redeemRecoveredTickets) : {};
      var _RECONCILE_WINDOW_MS = 30 * 24 * 60 * 60 * 1000;
      var _nowR = Date.now();
      var _granted = {};
      _mine.forEach(function(rw){
        if(!rw || !rw.at) return;
        var _bp = (rw.reward && typeof rw.reward === 'object') ? rw.reward.backpack : null;
        if(!_bp || typeof _bp !== 'object') return;
        var _isOld = !!((_nowR - rw.at) > _RECONCILE_WINDOW_MS);
        Object.keys(window._GMCR_PICK_TICKET_SRC).forEach(function(_k){
          if(_isOld && window._GMCR_PICK_TICKET_SRC[_k].ledger === '_heroUnlockHistory') return;
          var _n = Math.max(0, parseInt(_bp[_k], 10) || 0);
          if(_n > 0) _granted[_k] = (_granted[_k] || 0) + _n;
        });
      });
      if(!Object.keys(_granted).length) return;
      var _countSrc = function(ledgerKey, src){
        var _arr = Array.isArray(_pd[ledgerKey]) ? _pd[ledgerKey] : [];
        var _c = 0;
        _arr.forEach(function(e){ if(e && e.source === src) _c++; });
        return _c;
      };
      var _toGrant = {};
      Object.keys(_granted).forEach(function(_k){
        var _def = window._GMCR_PICK_TICKET_SRC[_k];
        var _used = _countSrc(_def.ledger, _def.source);
        var _held = (typeof backpackGet === 'function') ? (backpackGet(_k) || 0) : 0;
        var _rec  = Math.max(0, parseInt(_recovered[_k], 10) || 0);
        var _short = (_granted[_k] || 0) - _used - _held - _rec;
        if(_short > 0) _toGrant[_k] = _short;
      });
      if(!Object.keys(_toGrant).length) return;
      var _labels = [];
      Object.keys(_toGrant).forEach(function(_k){
        var _n = _toGrant[_k];
        try{ if(typeof backpackAdd === 'function') backpackAdd(_k, _n); }catch(_eAdd){}
        _recovered[_k] = (Math.max(0, parseInt(_recovered[_k], 10) || 0)) + _n;
        _labels.push(window._GMCR_PICK_TICKET_SRC[_k].label + '×' + _n);
      });
      try{ await setDoc(doc(_fbDb, 'players', _uid), { _redeemRecoveredTickets: _recovered }, { merge: true }); }
      catch(_eMark){ console.warn('[序號兌換對帳] 寫冪等標記失敗(背包已補·下次因 held 已有不會重補)', _eMark); }
      try{ if(typeof _showInGameToast === 'function') _showInGameToast('🎁 補發了你序號兌換漏拿到的：' + _labels.join('、'), '#66dd88', 4500); }catch(_eToast){}
      try{ console.log('[序號兌換對帳 v3.17.8] 補發漏領自選卷', _toGrant); }catch(_){}
    }catch(_eAll){ try{ console.warn('[序號兌換對帳] 例外(略過·不影響登入)', _eAll); }catch(_){} }
  };

  // ════════════════════════════════════════════════════════════════════════════
  // ★ v3.15.49 — 全體玩家獎勵(GM 一鍵發送給所有人,每人「保證只領一次」)
  // ────────────────────────────────────────────────────────────────────────────
  //   防重複設計(免疫存檔 richest-merge 復活):
  //     ① GM 建立 → 寫獨立 collection globalRewards/{autoId}
  //          = { reward(_fbCompensatePlayer payload), items[](顯示用), title, note,
  //              createdAt, createdBy, expiresAt(0=永久), enabled:true }
  //     ② 玩家領取 = 對「獨立認領文件」globalRewardClaims/{uid}_{rewardId} 做 transaction:
  //          tx.get 已存在 → 視為已領、略過;不存在 → tx.set 認領(原子標記)。
  //        認領文件與玩家存檔(players/{uid} 及 saves/*)完全分離 → 永不會被三槽 richest-merge
  //        「復活」成未領狀態 → 即使共用 iPad 清快取 / 雲端資料誤差,也絕不可能重複領取。
  //     ③ 標記成功後才 _fbCompensatePlayer 發獎(沿用序號兌換「先標記再發獎,寧漏不重複」原則)。
  //   效能:玩家主檔存 _grClaimed(陣列)做「跳過已領」快取,避免每次登入都對每個獎勵跑 transaction;
  //         但「權威防重複」永遠是 globalRewardClaims 認領文件,快取只是省讀取,壞了也擋得住雙領。
  // ════════════════════════════════════════════════════════════════════════════

  // GM:建立一筆全體獎勵。reward = _buildReward 產生的 payload;meta = { items, title, note, expiresDays }
  window._fbCreateGlobalReward = async function(reward, meta){
    if(!_fbDb) throw new Error('Firestore 未就緒');
    meta = meta || {};
    const _by = (window._fbUser && window._fbUser.email) || 'admin';
    const _days = Math.max(0, Math.floor(Number(meta.expiresDays) || 0));   // 0 = 永久
    const _now = Date.now();
    const _doc = {
      reward: reward || {},
      items: Array.isArray(meta.items) ? meta.items.slice(0, 40) : [],
      title: String(meta.title || '全體獎勵').slice(0, 60),
      note: String(meta.note || '').slice(0, 300),
      createdAt: _now,
      createdBy: String(_by).slice(0, 60),
      expiresAt: _days > 0 ? (_now + _days * 86400 * 1000) : 0,
      enabled: true,
    };
    // 無 addDoc 匯入 → 用 doc(collection(...)) 產生 auto-id ref 再 setDoc
    const _ref = doc(collection(_fbDb, 'globalRewards'));
    await setDoc(_ref, _doc);
    console.info('✅ [全體獎勵] 已建立 ' + _ref.id + '（' + _doc.title + '）');
    return { ok: true, id: _ref.id };
  };

  // GM:列出所有全體獎勵(新→舊)
  window._fbListGlobalRewards = async function(){
    if(!_fbDb) throw new Error('Firestore 未就緒');
    const _snap = await getDocs(collection(_fbDb, 'globalRewards'));
    const _list = [];
    _snap.forEach(function(d){ _list.push(Object.assign({ id: d.id }, d.data() || {})); });
    _list.sort(function(a, b){ return (b.createdAt || 0) - (a.createdAt || 0); });
    return _list;
  };

  // GM:啟用 / 停用一筆全體獎勵(停用後新登入的玩家不再領;已領者不受影響)
  window._fbSetGlobalRewardEnabled = async function(id, enabled){
    if(!_fbDb) throw new Error('Firestore 未就緒');
    if(!id) throw new Error('id 不可為空');
    await updateDoc(doc(_fbDb, 'globalRewards', id), { enabled: !!enabled });
    return { ok: true };
  };

  // GM:刪除一筆全體獎勵(認領文件 globalRewardClaims 保留即可,無害)
  window._fbDeleteGlobalReward = async function(id){
    if(!_fbDb) throw new Error('Firestore 未就緒');
    if(!id) throw new Error('id 不可為空');
    await deleteDoc(doc(_fbDb, 'globalRewards', id));
    return { ok: true };
  };

  // 玩家:登入時自動領取所有「尚未領過」的有效全體獎勵。回傳已領清單供通知。
  window._fbClaimGlobalRewards = async function(){
    const _uid = (window._fbUser && window._fbUser.uid) || window._gUserId || null;
    if(!_uid || !_fbDb) return { claimed: [] };
    let _activeList = [];
    try{
      // 只讀 enabled==true(需 globalRewards 規則允許登入者 list;見部署說明)
      const _q = query(collection(_fbDb, 'globalRewards'), where('enabled', '==', true));
      const _snap = await getDocs(_q);
      _snap.forEach(function(d){ _activeList.push(Object.assign({ id: d.id }, d.data() || {})); });
    }catch(e){
      // ★ v4.10.0 — 改為不早退:即使 globalRewards 查詢失敗(規則未部署/網路),
      //   仍要處理下方「寵物同萌上線禮」(硬編碼·不依賴 globalRewards 集合)。
      console.warn('[全體獎勵] 讀取有效獎勵失敗(可能規則未部署)·仍處理上線禮', e);
    }
    // ★ v4.10.0 需求1 — 「寵物同萌」全服上線禮(硬編碼注入·不需 GM 在後台建立):
    //   隨機寵物召喚卷×1 + 高級寵物飼料×10。防重複:走既有 globalRewardClaims/{uid}_{rid}
    //   transaction create-only 認領文件(UID 雲端權威·免疫三槽合併復活/跨裝置重複)。
    try{
      if(window._petSysOpenForMe && window._petSysOpenForMe()){
        _activeList.unshift({
          id: 'pet40_launch_v4100',
          title: '🐾 寵物同萌 正式上線禮',
          items: ['🎟️ 隨機寵物召喚卷 ×1', '🍖 高級寵物飼料 ×10'],
          note: '「寵物同萌」全面開放!快用召喚卷召喚你的第一隻寵物,再到寵物小屋跟牠互動吧!',
          reward: { backpack: { pet_summon_ticket: 1, pet_food_adv: 10 } },
          enabled: true
        });
      }
    }catch(_eGift){ console.warn('[寵物上線禮] 注入失敗', _eGift); }
    if(!_activeList.length) return { claimed: [] };

    // 讀玩家主檔的「已領快取」_grClaimed,先跳過已領,省去 transaction
    let _claimedCache = [];
    try{
      const _ps = await getDocFromServer(doc(_fbDb, 'players', _uid));
      if(_ps.exists()){
        const _pd = _ps.data() || {};
        if(Array.isArray(_pd._grClaimed)) _claimedCache = _pd._grClaimed.slice();
        // ★ v4.10.0 — 順讀寵物教學雲端旗標(同一次讀取·零額外成本):
        //   petTut1Done=第一階段聚光引導已完成 / petTut2Seen=馴養教學看過一次 / petTut2Off=永久關閉
        try{
          window._petTutCloudFlags = {
            t1Done: !!_pd.petTut1Done,
            t2Seen: !!_pd.petTut2Seen,
            t2Off:  !!_pd.petTut2Off,
            houseDone: !!_pd.petHouseTutDone   // ★ v5.125.0 小屋首入教學
          };
          try{ window._petTutCloudFlagsReady = true; }catch(_){}
        }catch(_eTf){}
      }
    }catch(_eRdCache){ /* 讀不到快取就一律走 transaction,仍安全 */ }
    const _claimedSet = new Set(_claimedCache);

    const _now = Date.now();
    const _newlyClaimed = [];   // 本次真的領到的(供通知)
    const _toCacheAppend = [];  // 要補進 _grClaimed 快取的 id(含「已被認領」者,下次省 transaction)

    for(const _rw of _activeList){
      const _rid = _rw.id;
      if(!_rid) continue;
      if(_claimedSet.has(_rid)) continue;                       // 快取已標記 → 跳過
      if(_rw.expiresAt && _now > _rw.expiresAt) continue;        // 已過期 → 不領
      const _claimRef = doc(_fbDb, 'globalRewardClaims', _uid + '_' + _rid);
      // ① transaction 原子認領(權威防重複,免疫存檔合併復活)
      let _tx;
      try{
        _tx = await runTransaction(_fbDb, async (tx) => {
          const _cs = await tx.get(_claimRef);
          if(_cs.exists()) return { already: true };
          tx.set(_claimRef, {
            uid: _uid,
            rewardId: _rid,
            at: _now,
            items: Array.isArray(_rw.items) ? _rw.items.slice(0, 40) : [],
          });
          return { claimed: true };
        });
      }catch(e){
        // permission/network → 本次略過,下次登入再試(未標記、未發獎,不會重複)
        console.warn('[全體獎勵] 認領 transaction 失敗 rid=' + _rid, e);
        continue;
      }
      if(_tx && _tx.already){
        _toCacheAppend.push(_rid);   // 別的裝置/分頁已領 → 補快取,下次省 transaction
        continue;
      }
      if(!_tx || !_tx.claimed) continue;
      // ② 標記成功 → 發獎到自己(沿用 _fbCompensatePlayer 三槽寫入,union/max 合併不降級)
      try{
        if(typeof window._fbCompensatePlayer !== 'function') throw new Error('_fbCompensatePlayer 未載入');
        const _items = Array.isArray(_rw.items) ? _rw.items : [];
        await window._fbCompensatePlayer(_uid, Object.assign({}, _rw.reward || {}, {
          reason: '全體獎勵發放',
          summary: '全體獎勵：' + (_rw.title || '') + (_items.length ? ('（' + _items.join('、') + '）') : ''),
          by: 'globalReward:' + _rid,
        }));
        // ★ v3.17.8 — 一併帶 reward 原始 payload,讓登入端呼叫者能即時把 backpack 部分套進記憶體
        //   (比照 v3.16.97 GM課堂獎勵修法),免疫「沒重整就繼續遊戲→autosave 用舊背包沖掉剛領的獎勵」。
        _newlyClaimed.push({ id: _rid, title: _rw.title || '全體獎勵', items: _items, note: _rw.note || '', reward: _rw.reward || {} });
        _toCacheAppend.push(_rid);
        // ★ v3.17.8 — 自寫一筆 _grClaimLog(self-write),供 _fbReconcileGlobalRewardTickets 登入回溯對帳
        try{
          const _lps = await getDocFromServer(doc(_fbDb, 'players', _uid));
          let _glog = [];
          if(_lps.exists()){ const _lpd = _lps.data() || {}; if(Array.isArray(_lpd._grClaimLog)) _glog = _lpd._grClaimLog.slice(); }
          if(!_glog.some(function(e){ return e && e.rewardId === _rid; })){
            _glog.push({ rewardId: _rid, reward: _rw.reward || {}, items: _items.slice(0, 40), at: _now });
            if(_glog.length > 200) _glog = _glog.slice(-200);
            await setDoc(doc(_fbDb, 'players', _uid), { _grClaimLog: _glog }, { merge: true });
          }
        }catch(_eGLog){ console.warn('[全體獎勵] 寫入 _grClaimLog 失敗(不影響已發獎·僅影響回溯對帳)', _eGLog); }
      }catch(e){
        // 已標記但發獎異常 → 該獎勵這次漏發(認領文件已佔位,不會重複);記錄供老師補發
        console.error('[全體獎勵] 已認領但發獎失敗 rid=' + _rid + '（已佔位,不會重複領）', e);
      }
    }

    // ③ 更新玩家主檔 _grClaimed 快取(本次新領 + 已被認領者),下次登入直接跳過
    if(_toCacheAppend.length){
      try{
        const _merged = Array.from(new Set(_claimedCache.concat(_toCacheAppend))).slice(-200);
        await setDoc(doc(_fbDb, 'players', _uid), { _grClaimed: _merged }, { merge: true });
      }catch(_eWrCache){ console.warn('[全體獎勵] 寫入已領快取失敗(不影響防重複)', _eWrCache); }
    }
    return { claimed: _newlyClaimed };
  };

  // ★ v4.10.0 — 寵物教學旗標雲端寫入 helper(self-write players/{uid} 單欄·merge:true·免改規則):
  //   petTut1Done(第一階段聚光引導完成)/ petTut2Seen(馴養教學看過一次)/ petTut2Off(永久關閉)。
  //   非存檔白名單欄位 → gameCloudSave merge:true 不會清掉;跨裝置由 _fbClaimGlobalRewards 順讀。
  window._fbSetPetTutFlag = async function(field){
    try{
      const _uid = (window._fbUser && window._fbUser.uid) || window._gUserId || null;
      if(!_uid || !_fbDb) return false;
      // ★ v5.125.0 — 新增 petHouseTutDone(小屋首入教學);原本純 localStorage ⇒ 換裝置必重跑
      if(['petTut1Done','petTut2Seen','petTut2Off','petHouseTutDone'].indexOf(field) < 0) return false;
      const _p = {}; _p[field] = Date.now();
      await setDoc(doc(_fbDb, 'players', _uid), _p, { merge: true });
      try{ window._petTutCloudFlags = window._petTutCloudFlags || {}; 
           if(field==='petTut1Done') window._petTutCloudFlags.t1Done = true;
           if(field==='petTut2Seen') window._petTutCloudFlags.t2Seen = true;
           if(field==='petTut2Off')  window._petTutCloudFlags.t2Off  = true;
           if(field==='petHouseTutDone') window._petTutCloudFlags.houseDone = true; }catch(_){}
      return true;
    }catch(_e){ console.warn('[寵物教學旗標寫入]', _e); return false; }
  };

  // ★ v5.141.0 — 教學旗標「主檔權威再確認」：啟動任何一次性教學前直接讀主檔（getDocFromServer 一次·零寫入），
  //   把四個旗標 OR 進 window._petTutCloudFlags。三槽合併與 _applySafeData 只是快取，主檔才是 _fbSetPetTutFlag 的落地點。
  window._fbRefreshPetTutFlags = async function(){
    try{
      const _uid = (window._fbUser && window._fbUser.uid) || window._gUserId || null;
      if(!_uid || !_fbDb) return null;
      const _ps = await getDocFromServer(doc(_fbDb, 'players', _uid));
      if(!_ps.exists()) return null;
      const _pd = _ps.data() || {};
      const _pf = window._petTutCloudFlags || {};
      window._petTutCloudFlags = {
        t1Done:    !!(_pf.t1Done    || _pd.petTut1Done),
        t2Seen:    !!(_pf.t2Seen    || _pd.petTut2Seen),
        t2Off:     !!(_pf.t2Off     || _pd.petTut2Off),
        houseDone: !!(_pf.houseDone || _pd.petHouseTutDone)
      };
      window._petTutCloudFlagsReady = true;
      return window._petTutCloudFlags;
    }catch(_e){ console.warn('[寵物教學旗標·主檔再確認] 讀取失敗，維持既有旗標', _e); return null; }
  };

  // ★ v4.65.0 — 主線劇情進度 self-write(players/{uid} merge·免改 rules·比照 _fbSetPetTutFlag)
  window._fbSaveMainStoryProgress = async function(progObj){
    try{
      const _uid = (window._fbUser && window._fbUser.uid) || window._gUserId || null;
      if(!_uid || !_fbDb) return false;
      if(!progObj || typeof progObj !== 'object') return false;
      await setDoc(doc(_fbDb, 'players', _uid), { mainStoryProgress: progObj }, { merge: true });
      return true;
    }catch(_e){ console.warn('[主線進度寫入]', _e); return false; }
  };
  // ★ v4.65.0 — 登入後讀一次主檔 mainStoryProgress → hydrate(getDocFromServer 一次·比照 _fbReconcile*)
  window._msHydrateFromCloud = async function(){
    try{
      const _uid = (window._fbUser && window._fbUser.uid) || window._gUserId || null;
      if(!_uid || !_fbDb){ if(typeof window._msHydrateProgress === 'function') window._msHydrateProgress(null); return; }
      let _pd = {};
      let _pdOk = false;   /* ★ v5.131.0 — 雲端主檔是否真的讀到(含「文件不存在」=合法的全新玩家);讀取失敗絕不解除造型上雲守門 */
      try{ const _ps = await getDocFromServer(doc(_fbDb, 'players', _uid)); _pdOk = true; if(_ps.exists()) _pd = _ps.data() || {}; }catch(_ep){}
      if(typeof window._msHydrateProgress === 'function') window._msHydrateProgress(_pd.mainStoryProgress || null);
      // ★ v5.7.0 — GM 回溯:avatarCard 帶 rst(後台重置寫入)且比本機 card 記錄的新 → 整份採用雲端重置版
      //   (cfg=預設・未覺醒・主線部件已剔),覆寫本機鏡像;同步覺醒旗標、清暱稱本機鍵、立繪簽章歸零重畫。
      //   只有被 GM 重置過的帳號才會有 rst 欄位,其他玩家此段零影響。
      //   (若不在登入時採用:avatar_db 的 _avatarPullFromCloud 只在造型工房開啟時跑,且要求 cloud.cfg 存在
      //    才採用;學生若不開工房,舊裝置的本機 card[舊臉/已覺醒]會一直生效,回溯等於沒發生。)
      try{
        const _avC = _pd.avatarCard;
        /* ★ v5.8.0 老師需求4 — 先做「卡片歸屬校正」:avatar_db 啟動時讀的是 guest 卡,
         *   這裡依當前 uid 重讀本機,下面的 rst/up 對時才是拿真的本機卡在比
         *   (v5.7.0 原邏輯若拿 guest 卡比 rst=0,雖冪等但不精確)。 */
        try{
          const _nowUH = _uid || 'guest';
          if(typeof window._avatarLoadLocal === 'function'
             && (!window._avatarLocalCard || window._avatarLocalCardUid !== _nowUH)){
            window._avatarLoadLocal();
          }
        }catch(_eLd){}
        /* ★ v5.131.0 — 採用雲端卡之前先快照「舊本機卡」的解鎖帳本與覺醒旗標:
         *   對帳前在本機入帳的部件解鎖(打BOSS/召喚/主線)只存在本機卡上,整份採用雲端會把它們蓋掉。
         *   unlock 是只增不減的帳本 → 採用後 union 回卡最安全;覺醒旗標取 OR。
         *   ★ GM 回溯(rst)分支刻意不 union —— 回溯的本意就是剝除,union 會讓回溯失效。 */
        let _avPrevUnlock = null, _avPrevAwk = false;
        try{
          const _pvC = window._avatarLocalCard;
          if(_pvC){ _avPrevUnlock = Array.isArray(_pvC.unlock) ? _pvC.unlock.slice() : null; _avPrevAwk = !!_pvC.protagAwakened; }
        }catch(_ePv){}
        const _avUnionPrev = function(){
          try{
            const _cd = window._avatarLocalCard; let _chg = false;
            if(_cd && _avPrevUnlock && _avPrevUnlock.length){
              if(!Array.isArray(_cd.unlock)) _cd.unlock = [];
              for(let _ui = 0; _ui < _avPrevUnlock.length; _ui++){
                if(_cd.unlock.indexOf(_avPrevUnlock[_ui]) === -1){ _cd.unlock.push(_avPrevUnlock[_ui]); _chg = true; }
              }
            }
            if(_cd && _avPrevAwk && !_cd.protagAwakened){
              _cd.protagAwakened = true;
              try{ window._protagAwakened = true; }catch(_eA){}
              _chg = true;
            }
            if(_chg){
              try{ if(typeof window._avatarSaveLocal === 'function') window._avatarSaveLocal(); }catch(_eS){}
              window._avatarCloudPushPending = true;   /* 對帳完成後補推,雲端帳本一併補齊 */
            }
          }catch(_eUn){}
        };
        /* ★ v5.131.0 — 「從未在工房儲存過的預設卡」判定(救回/補強分支共用):
         *   _avatarDefaultCfg 為純靜態物件 → JSON 比對可靠;工房的「全部重置/隨機」必經工房儲存
         *   ⇒ saveN≥1,而污染鏈產物來自 grant 路徑(工房關)⇒ saveN 不增,兩者可嚴格區分。 */
        const _avCfgIsDefault = function(c){
          try{ return !!c && typeof window._avatarDefaultCfg === 'function' && JSON.stringify(c) === JSON.stringify(window._avatarDefaultCfg()); }catch(_eD){ return false; }
        };
        /* 共用採用流程:整份採用雲端卡 + 覆寫本機鏡像 + 覺醒旗標 + 立繪簽章歸零重畫 */
        const _avAdopt = function(card, why){
          try{
            window._avatarLocalCard = card;
            try{ if(typeof window._avatarSaveLocal === 'function') window._avatarSaveLocal(); }catch(_eSv){}
            try{ window._protagAwakened = !!card.protagAwakened; }catch(_ePa){}
            try{
              window._lxpsProtagPortraitDone = false;
              window._lxpsProtagPortraitSig = '';
              window._lxpsProtagPortraitBusy = false;
              if(typeof window._lxpsProtagPortraitRefresh === 'function') window._lxpsProtagPortraitRefresh();
            }catch(_ePr){}
            console.warn('[主角造型] 已採用雲端卡(' + why + ')');
          }catch(_eAd){}
        };
        let _avAdopted = false;
        if(_avC && typeof _avC.rst === 'number'){
          const _avL = window._avatarLocalCard;
          const _avLr = (_avL && typeof _avL.rst === 'number') ? _avL.rst : 0;
          if(_avLr < _avC.rst){
            _avAdopt(_avC, 'GM 回溯 rst=' + _avC.rst);
            _avAdopted = true;
            try{ if(!_avC.nick && _uid) localStorage.removeItem('lxps_nickname_' + _uid); }catch(_eNk){}
            try{ window._avatarNickname = (_avC.nick || ''); }catch(_eNm){}
          }
        }
        /* ★ v5.8.0 老師需求4 — up 對時(跨裝置同步 + 雲端寫入失敗自癒):
         *   ①換新平板(本機 localStorage 根本沒這位學生的卡)且雲端有 cfg → 直接採用雲端
         *     (學生不必開造型工房,圖鑑/戰鬥卡登入即正確);
         *   ②雲端 up 較新(別台裝置存過)→ 採用雲端;
         *   ③本機 up 較新(上次雲端儲存失敗)→ 補推上雲自癒。
         *   舊卡沒有 up 欄位視為 0(0=0 → 維持本機,行為與 v5.7.0 相同);GM 回溯已在上方處理。 */
        if(!_avAdopted && _avC && _avC.cfg){
          try{
            const _lsRaw = _uid ? localStorage.getItem('lxps_avatarCard_' + _uid) : null;
            const _avL2 = window._avatarLocalCard;
            const _cUp = (typeof _avC.up === 'number') ? _avC.up : 0;
            const _lUp = (_avL2 && typeof _avL2.up === 'number') ? _avL2.up : 0;
            if(!_lsRaw || !_avL2 || !_avL2.cfg){
              _avAdopt(_avC, '新裝置還原');
              _avUnionPrev();   /* ★ v5.131.0 — 對帳前本機入帳的解鎖 union 回卡,不遺失 */
              try{ if(typeof _avC.nick === 'string' && _avC.nick) window._avatarNickname = _avC.nick; }catch(_eN2){}
            } else if(_lUp === 0 && !((_avL2.saveN|0)) && _avCfgIsDefault(_avL2.cfg)){
              /* ★ v5.131.0 補強 — 本機有卡但是「從未儲存過的預設卡」(up=0·saveN=0·cfg=預設):
               *   舊守門只認「本機完全沒卡」才還原,這種早期寫入的預設卡會卡在 0=0 分支永不還原。 */
              _avAdopt(_avC, '本機為未儲存預設卡,採用雲端');
              _avUnionPrev();
              try{ if(typeof _avC.nick === 'string' && _avC.nick) window._avatarNickname = _avC.nick; }catch(_eN4){}
            } else if(_cUp > _lUp){
              /* ★ v5.131.0 既損救回 — 雲端 up 雖較新,但雲端卡是「從未在工房儲存過的預設卡」
               *   (=污染鏈產物:別台裝置在對帳前把預設卡整包推上雲)而本機持有真造型
               *   → 不採用雲端,改保留本機並於對帳完成後推回上雲自癒。
               *   ★ 不會誤殺「玩家真的重置成預設」:工房重置必經工房儲存 ⇒ 雲端卡 saveN≥1 不命中本謂詞。 */
              if(!((_avC.saveN|0)) && !(typeof _avC.rst === 'number') && _avCfgIsDefault(_avC.cfg) && !_avCfgIsDefault(_avL2.cfg)){
                window._avatarCloudPushPending = true;
                console.warn('[主角造型] 雲端疑為被污染的預設卡(saveN=0),保留本機真造型並補推自癒');
              } else {
                _avAdopt(_avC, '雲端較新 up=' + _cUp);
                _avUnionPrev();   /* ★ v5.131.0 — 同上,解鎖帳本只增不減 */
                try{ if(typeof _avC.nick === 'string' && _avC.nick) window._avatarNickname = _avC.nick; }catch(_eN3){}
              }
            } else if(_lUp > _cUp){
              /* ★ v5.131.0 — 自癒改走 pending:此刻守門旗標尚未解除,直接呼叫會被自己的守門擋下;
               *   統一由段尾「對帳完成」補推(舊直接呼叫行保留備查)。 */
              window._avatarCloudPushPending = true;
              /* 舊:try{ if(typeof window._avatarSaveToCloud === 'function') Promise.resolve(window._avatarSaveToCloud()).catch(function(){}); }catch(_eHl){} */
              console.warn('[主角造型] 本機較新,對帳後補推上雲自癒(up=' + _lUp + ')');
            }
          }catch(_eUp){}
        }
        /* ★ v5.8.0 老師需求4(重開遊戲臉變預設根治)— 對帳後立繪簽章比對:
         *   啟動 poll 可能已用 guest 預設卡畫過並以預設 cfg 簽章上鎖,這裡拿「正確的卡」
         *   再比一次,簽章不一致 → 歸零重畫。無論上面有沒有採用雲端都要做(採用路徑已自帶重畫,冪等)。 */
        try{
          const _cardF = window._avatarLocalCard;
          if(_cardF && _cardF.cfg){
            let _sigF = ''; try{ _sigF = JSON.stringify(_cardF.cfg); }catch(_eSg){}
            try{ _sigF += '|D' + JSON.stringify(window._avatarPartDefaults || {}); }catch(_eSd){}   /* ★ v5.15.0 與立繪簽章同格式(cfg+管理員部件預設表) */
            if(_sigF && window._lxpsProtagPortraitSig !== _sigF){
              window._lxpsProtagPortraitDone = false;
              window._lxpsProtagPortraitSig = '';
              window._lxpsProtagPortraitBusy = false;
              if(typeof window._lxpsProtagPortraitRefresh === 'function') window._lxpsProtagPortraitRefresh();
            }
          }
        }catch(_eFix){}
        /* ★ v5.9.0 — 登入對帳完成後全面補判主角類獎章(主線進度/avatarCard/BD 都已就緒;
         *   已完成章節或已打扮過的舊玩家,登入即自動回溯發章,冪等零重複發獎)。 */
        try{ if(typeof window._protagMedalSweep === 'function') window._protagMedalSweep(); }catch(_eMdl){}
        try{ if(typeof window._minigameMedalSweep === 'function') window._minigameMedalSweep(); }catch(_eMg){}
        /* ★★ v5.131.0 — 造型對帳完成:解除上雲守門+補推。
         *   只有「雲端主檔真的讀到」(_pdOk·含合法的全新玩家無文件)才解除;讀取失敗維持守門
         *   =該次登入的部件解鎖只落地本機,下次成功對帳時由 union+pending 一併補上雲,零遺失。 */
        try{
          if(_pdOk){
            window._avatarCloudReconciled = true;
            if(window._avatarCloudPushPending){
              window._avatarCloudPushPending = false;
              if(typeof window._avatarSaveToCloud === 'function') Promise.resolve(window._avatarSaveToCloud()).catch(function(){});
            }
          }
        }catch(_eRc){}
      }catch(_eAvR){ console.warn('[主角回溯採用]', _eAvR); }
    }catch(_e){ console.warn('[主線進度讀取]', _e); if(typeof window._msHydrateProgress === 'function') window._msHydrateProgress(null); }
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.17.8 — 回溯救援:登入對帳「已領全體獎勵自選卷·實際沒拿到的補上」
  //   鏡像 v3.16.97 / v3.17.8 序號版,讀玩家自己主檔的 _grClaimLog(self-write)。
  //   同一份 _GMCR_PICK_TICKET_SRC 定義(4 種自選卷)。
  //   安全不等式同款:已發(全體獎勵 reward.backpack)− 已用(durable 帳本)− 現有(背包)− 已補(冪等記) > 0 才補。
  // ════════════════════════════════════════════════════════════════════
  window._fbReconcileGlobalRewardTickets = async function(){
    try{
      var _uid = (window._fbUser && window._fbUser.uid) || window._gUserId || null;
      if(!_uid || !_fbDb || !window._GMCR_PICK_TICKET_SRC) return;
      var _pd = {};
      try{ var _ps = await getDocFromServer(doc(_fbDb, 'players', _uid)); if(_ps.exists()) _pd = _ps.data() || {}; }
      catch(_ePd){ return; }
      var _mine = Array.isArray(_pd._grClaimLog) ? _pd._grClaimLog : [];
      if(!_mine.length) return;
      var _recovered = (_pd._grRecoveredTickets && typeof _pd._grRecoveredTickets === 'object') ? Object.assign({}, _pd._grRecoveredTickets) : {};
      var _RECONCILE_WINDOW_MS = 30 * 24 * 60 * 60 * 1000;
      var _nowR = Date.now();
      var _granted = {};
      _mine.forEach(function(rw){
        if(!rw || !rw.at) return;
        var _bp = (rw.reward && typeof rw.reward === 'object') ? rw.reward.backpack : null;
        if(!_bp || typeof _bp !== 'object') return;
        var _isOld = !!((_nowR - rw.at) > _RECONCILE_WINDOW_MS);
        Object.keys(window._GMCR_PICK_TICKET_SRC).forEach(function(_k){
          if(_isOld && window._GMCR_PICK_TICKET_SRC[_k].ledger === '_heroUnlockHistory') return;
          var _n = Math.max(0, parseInt(_bp[_k], 10) || 0);
          if(_n > 0) _granted[_k] = (_granted[_k] || 0) + _n;
        });
      });
      if(!Object.keys(_granted).length) return;
      var _countSrc = function(ledgerKey, src){
        var _arr = Array.isArray(_pd[ledgerKey]) ? _pd[ledgerKey] : [];
        var _c = 0;
        _arr.forEach(function(e){ if(e && e.source === src) _c++; });
        return _c;
      };
      var _toGrant = {};
      Object.keys(_granted).forEach(function(_k){
        var _def = window._GMCR_PICK_TICKET_SRC[_k];
        var _used = _countSrc(_def.ledger, _def.source);
        var _held = (typeof backpackGet === 'function') ? (backpackGet(_k) || 0) : 0;
        var _rec  = Math.max(0, parseInt(_recovered[_k], 10) || 0);
        var _short = (_granted[_k] || 0) - _used - _held - _rec;
        if(_short > 0) _toGrant[_k] = _short;
      });
      if(!Object.keys(_toGrant).length) return;
      var _labels = [];
      Object.keys(_toGrant).forEach(function(_k){
        var _n = _toGrant[_k];
        try{ if(typeof backpackAdd === 'function') backpackAdd(_k, _n); }catch(_eAdd){}
        _recovered[_k] = (Math.max(0, parseInt(_recovered[_k], 10) || 0)) + _n;
        _labels.push(window._GMCR_PICK_TICKET_SRC[_k].label + '×' + _n);
      });
      try{ await setDoc(doc(_fbDb, 'players', _uid), { _grRecoveredTickets: _recovered }, { merge: true }); }
      catch(_eMark){ console.warn('[全體獎勵對帳] 寫冪等標記失敗(背包已補·下次因 held 已有不會重補)', _eMark); }
      try{ if(typeof _showInGameToast === 'function') _showInGameToast('🎁 補發了你全體獎勵漏拿到的：' + _labels.join('、'), '#66dd88', 4500); }catch(_eToast){}
      try{ console.log('[全體獎勵對帳 v3.17.8] 補發漏領自選卷', _toGrant); }catch(_){}
    }catch(_eAll){ try{ console.warn('[全體獎勵對帳] 例外(略過·不影響登入)', _eAll); }catch(_){} }
  };

  // 玩家:全體獎勵到帳通知彈窗(列出本次領到的獎勵 + 重新整理鈕,沿用序號兌換成功的 UX)
  window._showGlobalRewardNotice = function(list){
    try{
      if(!Array.isArray(list) || !list.length) return;
      const _esc = function(s){ return String(s == null ? '' : s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;'); };
      let _blocks = '';
      list.forEach(function(rw){
        const _items = Array.isArray(rw.items) ? rw.items : [];
        _blocks += '<div style="margin:10px 0;padding:10px 12px;background:rgba(255,210,90,0.10);'
          + 'border:1px solid rgba(255,210,90,0.45);border-radius:10px;text-align:left;">'
          + '<div style="color:#ffe066;font-weight:900;font-size:17px;">🎁 ' + _esc(rw.title || '全體獎勵') + '</div>'
          + (rw.note ? ('<div style="color:#cbd6ea;font-size:12px;margin:3px 0 6px;">' + _esc(rw.note) + '</div>') : '')
          + (_items.length ? ('<div style="color:#fff;font-size:15px;font-weight:800;line-height:1.8;">'
              + _items.map(_esc).join('<br>') + '</div>') : '')
          + '</div>';
      });
      // 移除舊的(避免重複)
      try{ const _old = document.getElementById('_global-reward-notice'); if(_old) _old.remove(); }catch(_){}
      const _ov = document.createElement('div');
      _ov.id = '_global-reward-notice';
      _ov.style.cssText = 'position:fixed;inset:0;z-index:99000;display:flex;align-items:center;justify-content:center;'
        + 'background:rgba(0,0,0,0.72);padding:20px;';
      _ov.innerHTML = '<div style="max-width:440px;width:100%;max-height:86vh;overflow-y:auto;'
        + 'background:linear-gradient(160deg,#1a2548,#0e1730);border:2px solid rgba(255,210,90,0.6);'
        + 'border-radius:18px;padding:22px 20px;box-shadow:0 10px 40px rgba(0,0,0,0.6);text-align:center;'
        + 'font-family:inherit;">'
        + '<div style="font-size:26px;font-weight:900;color:#ffd54f;letter-spacing:1px;">🎉 你收到全體獎勵！</div>'
        + '<div style="font-size:13px;color:#bbcce0;margin-top:4px;">老師發送給全班的獎勵已存入你的帳號</div>'
        + _blocks
        + '<div style="font-size:12px;color:#9fb0cc;margin-top:6px;">重新整理後就能看到囉！</div>'
        + '<div style="display:flex;gap:10px;margin-top:14px;justify-content:center;flex-wrap:wrap;">'
        + '<button onclick="_lxpsReloadKeepLoginIfLoggedIn()" style="padding:11px 22px;font-size:15px;font-weight:800;'   /* ★ v5.148.0 舊:onclick=location.reload() 會被攔截器踢出 */
        + 'background:linear-gradient(135deg,#2a8a3a,#1d7a2a);border:2px solid #66dd88;color:#fff;'
        + 'border-radius:10px;cursor:pointer;font-family:inherit;letter-spacing:1px;">🔄 重新整理看獎勵</button>'
        + '<button onclick="(function(){var e=document.getElementById(\'_global-reward-notice\');if(e)e.remove();})()" '
        + 'style="padding:11px 18px;font-size:14px;font-weight:700;background:rgba(120,140,180,0.25);'
        + 'border:1.5px solid rgba(150,170,210,0.5);color:#dfe7f5;border-radius:10px;cursor:pointer;'
        + 'font-family:inherit;">稍後再看</button>'
        + '</div></div>';
      document.body.appendChild(_ov);
      try{ if(typeof playSfx === 'function') playSfx('sfx-coin', 0.85); }catch(_){}
    }catch(e){ console.warn('[全體獎勵通知] 彈窗失敗', e); }
  };

  // ════════════════════════════════════════════════════════════════════════════
  // ★ v3.16.65 — GM 課堂獎勵「拉取制」收件(逐玩家) + 手動「確認領取」
  // ────────────────────────────────────────────────────────────────────────────
  //   把「課堂獎勵發放」由 push(_fbCompensatePlayer 直接寫)改為 pull(玩家領取):
  //     ① GM 指派 → 寫該玩家收件箱 gmClassRewards/{uid}/items/{autoId}
  //          = { uid, label, email, reward(_fbCompensatePlayer payload),
  //              itemsLabel[](顯示用), merit(事蹟·發獎理由), by, at, enabled:true }
  //     ② 玩家在「🎁 GM獎勵」看到「事蹟 + 獎項名稱數量」→ 按「確認領取」:
  //        對「獨立認領文件」gmClassRewardClaims/{uid}_{rewardId} 做 transaction
  //        (tx.get 已存在→視為已領、不重發;不存在→tx.set 認領)。
  //        認領文件 docId 前綴綁 uid + create-only → 永不被三槽 richest-merge 復活、
  //        跨裝置/分頁不重複領、不跨帳號污染(同 globalRewardClaims 模型)。
  //     ③ 認領成功才以「伺服器重讀的權威 reward」_fbCompensatePlayer 入帳(不信任 client 傳入,防竄改 UR/數量)。
  //   ★ 防同裝置帳號污染核心:rule 只讓玩家讀/領自己 uid 的收件箱 → 共用 iPad 上 B 同學
  //     看不到也領不到 A 同學的 UR 獎勵;獎勵只在「正確學生登入且主動領取」時才套用到其存檔。
  //   ⚠ 需部署 firestore.rules:gmClassRewards(GM 寫·玩家讀自己) + gmClassRewardClaims(create-only·docId 前綴=uid)。
  // ════════════════════════════════════════════════════════════════════════════

  // GM:指派一筆課堂獎勵到某玩家收件箱。reward = _buildReward payload;itemsLabel = 顯示用文字陣列;merit = 事蹟
  window._fbWriteGmClassReward = async function(uid, label, email, reward, itemsLabel, merit){
    if(!_fbDb) throw new Error('Firestore 未就緒');
    if(!uid) throw new Error('uid 不可為空');
    const _by = (window._fbUser && window._fbUser.email) || 'admin';
    const _now = Date.now();
    const _doc = {
      uid: String(uid),
      label: String(label || '').slice(0, 80),
      email: String(email || '').slice(0, 80),
      reward: reward || {},
      itemsLabel: Array.isArray(itemsLabel) ? itemsLabel.slice(0, 40) : [],
      merit: String(merit || '').slice(0, 120),
      by: String(_by).slice(0, 60),
      at: _now,
      enabled: true,
    };
    const _ref = doc(collection(_fbDb, 'gmClassRewards', String(uid), 'items'));
    await setDoc(_ref, _doc);
    console.info('✅ [GM課堂獎勵] 已指派給 ' + uid + ' → ' + _ref.id);
    return { ok: true, id: _ref.id };
  };

  // 玩家:讀自己收件箱「尚未領取」的有效獎勵(供「🎁 GM獎勵」收件區 + 鈕上方標籤)。回傳待領陣列(新→舊)。
  window._fbReadMyGmClassRewards = async function(){
    const _uid = (window._fbUser && window._fbUser.uid) || window._gUserId || null;
    if(!_uid || !_fbDb) return [];
    let _all = [];
    try{
      const _snap = await getDocs(collection(_fbDb, 'gmClassRewards', _uid, 'items'));
      _snap.forEach(function(d){ _all.push(Object.assign({ id: d.id }, d.data() || {})); });
    }catch(e){
      console.warn('[GM獎勵] 讀取收件箱失敗(可能規則未部署)', e);
      return [];
    }
    if(!_all.length) return [];
    // 讀玩家主檔「已領快取」_gmcrClaimed → 過濾已領(權威仍是認領文件,快取只是省領取重試/避免顯示已領)
    let _claimedCache = [];
    try{
      const _ps = await getDocFromServer(doc(_fbDb, 'players', _uid));
      if(_ps.exists()){
        const _pd = _ps.data() || {};
        if(Array.isArray(_pd._gmcrClaimed)) _claimedCache = _pd._gmcrClaimed.slice();
      }
    }catch(_eRd){ /* 讀不到快取就不過濾,領取時 transaction 仍會擋重複 */ }
    const _claimedSet = new Set(_claimedCache);
    const _now = Date.now();
    const _pending = [];
    _all.forEach(function(rw){
      if(!rw.id) return;
      if(rw.enabled === false) return;
      if(rw.expiresAt && _now > rw.expiresAt) return;
      if(_claimedSet.has(rw.id)) return;
      _pending.push(rw);
    });
    _pending.sort(function(a, b){ return (b.at || 0) - (a.at || 0); });
    return _pending;
  };

  // 玩家:領取單筆課堂獎勵。transaction 原子認領(免疫存檔復活/跨裝置重複),認領成功才以「伺服器權威 reward」入帳。
  //   回傳 { ok, already, reason, itemsLabel, merit }
  window._fbClaimGmClassReward = async function(rewardId){
    const _uid = (window._fbUser && window._fbUser.uid) || window._gUserId || null;
    if(!_uid || !_fbDb) return { ok: false, reason: '尚未連線,請稍後再試' };
    if(!rewardId) return { ok: false, reason: '獎勵代碼錯誤' };
    // ① 伺服器重讀該獎勵(權威來源,不信任 client 傳入的 reward 內容,防竄改 UR/數量)
    let _rw;
    try{
      const _rs = await getDocFromServer(doc(_fbDb, 'gmClassRewards', _uid, 'items', rewardId));
      if(!_rs.exists()) return { ok: false, reason: '這筆獎勵不存在或已被收回' };
      _rw = Object.assign({ id: _rs.id }, _rs.data() || {});
    }catch(e){
      return { ok: false, reason: '讀取獎勵失敗,請稍後再試' };
    }
    if(_rw.enabled === false) return { ok: false, reason: '這筆獎勵已被老師停用' };
    if(_rw.expiresAt && Date.now() > _rw.expiresAt) return { ok: false, reason: '這筆獎勵已過期' };
    const _itemsLabel = Array.isArray(_rw.itemsLabel) ? _rw.itemsLabel : [];
    const _now = Date.now();
    const _claimRef = doc(_fbDb, 'gmClassRewardClaims', _uid + '_' + rewardId);
    const _playerRef = doc(_fbDb, 'players', _uid);
    // ★ v3.16.94 — 領取防重複「主閘門」改用玩家自己主檔的 _gmcrClaimed 陣列(self-write 永遠可寫,
    //   不依賴 gmClassRewards/gmClassRewardClaims 規則是否部署)。根治「transaction 寫入被規則擋 →
    //   領取失敗 re-enable → 無限空轉、沒入帳」:即使規則未部署,自寫 _gmcrClaimed 仍能標記已領,
    //   收件箱(_fbReadMyGmClassRewards 已用 _gmcrClaimed 過濾)立刻隱藏該筆 → 鈕不再空轉,
    //   發放成功就會出現在召喚頁。gmClassRewardClaims transaction 降為「最佳努力跨裝置去重」
    //   (規則部署時生效;未部署則靜默跳過,改靠 self-write 閘門 + 同頁鈕禁用防重複)。
    // ①.5 先讀自己主檔 _gmcrClaimed:若已含本筆 → 已領過,直接擋(免重複入帳)
    try{
      const _pre = await getDocFromServer(_playerRef);
      if(_pre.exists()){
        const _pd = _pre.data() || {};
        if(Array.isArray(_pd._gmcrClaimed) && _pd._gmcrClaimed.indexOf(rewardId) >= 0){
          return { ok: false, already: true, reason: '這筆獎勵已經領過了' };
        }
      }
    }catch(_ePre){ /* 讀不到快取就往下走,後面 transaction / 標記仍會把關 */ }
    // ② 最佳努力 transaction 跨裝置去重(規則部署時生效;未部署則 catch 後靜默改走 self-write 閘門)
    let _txAlready = false;
    try{
      const _tx = await runTransaction(_fbDb, async (tx) => {
        const _cs = await tx.get(_claimRef);
        if(_cs.exists()) return { already: true };
        tx.set(_claimRef, { uid: _uid, rewardId: rewardId, at: _now, items: _itemsLabel.slice(0, 40) });
        return { claimed: true };
      });
      if(_tx && _tx.already) _txAlready = true;
    }catch(_eTx){
      console.warn('[GM獎勵] 認領 transaction 無法寫入(規則可能未部署),改用主檔 _gmcrClaimed 自寫閘門 rid=' + rewardId, _eTx);
    }
    if(_txAlready) return { ok: false, already: true, reason: '這筆獎勵已經領過了' };
    // ②.5 ★ 主閘門:發獎「之前」先以 self-write 標記 _gmcrClaimed(read-modify-write,等效 arrayUnion)。
    //   self-write 永遠可寫(players/{uid} 規則允許本人寫非停權欄位),標記成功 → 收件箱立即隱藏該筆。
    //   標記失敗才中止(可重試;尚未發獎不會重複扣發)。
    try{
      const _ps = await getDocFromServer(_playerRef);
      let _cacheBefore = [];
      if(_ps.exists()){
        const _pd = _ps.data() || {};
        if(Array.isArray(_pd._gmcrClaimed)) _cacheBefore = _pd._gmcrClaimed.slice();
      }
      // 競態保險:重讀後再確認一次未領(防 ② 與此處之間被別處標記)
      if(_cacheBefore.indexOf(rewardId) >= 0) return { ok: false, already: true, reason: '這筆獎勵已經領過了' };
      const _markedCache = Array.from(new Set(_cacheBefore.concat([rewardId]))).slice(-300);
      await setDoc(_playerRef, { _gmcrClaimed: _markedCache }, { merge: true });
    }catch(_eMark){
      console.warn('[GM獎勵] 標記 _gmcrClaimed 失敗(尚未發獎·可重試)rid=' + rewardId, _eMark);
      return { ok: false, reason: '領取失敗,請稍後再試(未發放,可再試)' };
    }
    // ③ 已標記 → 以伺服器權威 reward 入帳(_fbCompensatePlayer 三槽 union/max,只增不減)
    try{
      if(typeof window._fbCompensatePlayer !== 'function') throw new Error('_fbCompensatePlayer 未載入');
      await window._fbCompensatePlayer(_uid, Object.assign({}, _rw.reward || {}, {
        reason: '課堂獎勵領取',
        summary: '課堂獎勵' + (_rw.merit ? ('（' + _rw.merit + '）') : '') + (_itemsLabel.length ? ('：' + _itemsLabel.join('、')) : ''),
        by: 'gmClassReward:' + rewardId,
      }));
    }catch(e){
      // ★ 發獎失敗 → 回滾標記(把 rewardId 從 _gmcrClaimed 移除),讓學生可重新領取,避免「標記了卻沒入帳」永久卡住
      console.error('[GM獎勵] 入帳失敗·回滾 _gmcrClaimed 標記 rid=' + rewardId, e);
      try{
        const _rb = await getDocFromServer(_playerRef);
        let _rbCache = [];
        if(_rb.exists()){ const _rbd = _rb.data() || {}; if(Array.isArray(_rbd._gmcrClaimed)) _rbCache = _rbd._gmcrClaimed.slice(); }
        const _rolled = _rbCache.filter(function(x){ return x !== rewardId; });
        await setDoc(_playerRef, { _gmcrClaimed: _rolled }, { merge: true });
      }catch(_eRb){ console.error('[GM獎勵] 回滾標記也失敗(請通知老師補發)rid=' + rewardId, _eRb); }
      return { ok: false, reason: '入帳發生異常,請稍後再試(已回復·可再領)' };
    }
    // ★ v3.17.6(老師需求 2)— 入帳成功 → 若本筆獎勵含 GM 召喚卷,記錄「發放理由(事蹟)+時間」到本機中繼資料,
    //   供背包/召喚星空顯示「🎁 老師發放理由…（時間）」。純本機顯示用,不影響入帳/存檔。
    try{
      if(typeof window._recordGmTicketMeta === 'function' && _rw && _rw.reward && _rw.reward.backpack){
        window._recordGmTicketMeta(_uid, _rw.reward.backpack, _rw.merit || '', _now);
      }
    }catch(_eMeta){ try{ console.warn('[GM獎勵] 記錄 GM 卷理由時間失敗(不影響領取)', _eMeta); }catch(_){} }
    // ④ 入帳成功 → 補寫 _gmcrClaimLog 領取紀錄(_gmcrClaimed 已於 ②.5 標記;此處只補紀錄)
    //    ★ v3.16.66 — 領取紀錄(獎項名/事蹟/獎項清單/領取時間)寫主檔(self-write·不需改 rule),
    //      供學生「✅已領取紀錄」顯示 + GM 後台查(根治「學生領完收件區空白→不認帳」)。
    try{
      const _ps2 = await getDocFromServer(_playerRef);
      let _cache2 = [], _log = [];
      if(_ps2.exists()){
        const _pd2 = _ps2.data() || {};
        if(Array.isArray(_pd2._gmcrClaimed)) _cache2 = _pd2._gmcrClaimed.slice();
        if(Array.isArray(_pd2._gmcrClaimLog)) _log = _pd2._gmcrClaimLog.slice();
      }
      const _merged = Array.from(new Set(_cache2.concat([rewardId]))).slice(-300);
      if(!_log.some(function(e){ return e && e.rewardId === rewardId; })){
        _log.push({ rewardId: rewardId, label: String(_rw.label || '').slice(0, 80),
                    merit: String(_rw.merit || '').slice(0, 120),
                    items: _itemsLabel.slice(0, 40), at: _now });
        if(_log.length > 200) _log = _log.slice(-200);
      }
      await setDoc(_playerRef, { _gmcrClaimed: _merged, _gmcrClaimLog: _log }, { merge: true });
    }catch(_eWr){ console.warn('[GM獎勵] 寫入已領紀錄失敗(不影響防重複·已標記)', _eWr); }
    // ★ v3.16.97 — 甲:回傳結構化 reward,讓 client 領取後「即時把獎勵套進記憶體背包」(防 autosave 覆蓋丟券)
    return { ok: true, itemsLabel: _itemsLabel, merit: _rw.merit || '', reward: (_rw.reward && typeof _rw.reward === 'object') ? _rw.reward : {} };
  };

  // ════════════════════════════════════════════════════════════════
  // ★ v3.16.97 — 回溯救援:登入對帳「已領課堂獎勵自選卷·實際沒拿到的補上」
  // ────────────────────────────────────────────────────────────────
  // 背景:v3.16.96 前的領取流程把獎勵寫雲端但「未即時套進記憶體」→ 若領取後 autosave
  //   搶先寫了「沒票券」的槽,_lxpsMergeSlots 消耗型背包「只取最新槽」會把雲端授予的自選卷丟掉
  //   → 學生「標記領過(_gmcrClaimed)卻沒實際拿到券」,且收件箱已過濾不再顯示=領不回。
  // 安全不等式(★永不重複發):
  //   被丟 = 已發(課堂獎勵·讀「伺服器」收件箱權威 reward.backpack)
  //          − 已用(durable 帳本:_heroUnlockHistory source ticket_*_pick
  //                   / _treasureUnlockHistory source treasure_pick_ticket·皆 union 撐過 merge)
  //          − 現有(背包) − 已補(_gmcrRecoveredTickets 冪等標記)
  //   > 0 才補。① 已用掉的(lucky+used)其帳本 source 計數會抵掉→不補(不重複發)
  //            ② 補後 held↑+recovered↑ 雙記→即使再被舊版 client 丟,recovered 擋住不無限補
  //            ③ 之後再發新一批同型券,granted↑→只補新缺額(invariant 自洽)
  // 僅針對「自選卷」4 種;只補不扣;不採信 client 數量(讀伺服器收件箱);全程 try-catch 不影響登入。
  // ════════════════════════════════════════════════════════════════
  window._GMCR_PICK_TICKET_SRC = {
    'summon_ticket_ur_pick':       { ledger: '_heroUnlockHistory',     source: 'ticket_ur_pick',       label: 'UR自選召喚卷' },
    'summon_ticket_ssr_pick':      { ledger: '_heroUnlockHistory',     source: 'ticket_ssr_pick',      label: 'SSR自選召喚卷' },
    'summon_ticket_sr_pick':       { ledger: '_heroUnlockHistory',     source: 'ticket_sr_pick',       label: 'SR自選召喚卷' },
    'summon_ticket_treasure_pick': { ledger: '_treasureUnlockHistory', source: 'treasure_pick_ticket', label: '自選至寶召喚卷' }
  };
  window._fbReconcileGmClassRewardTickets = async function(){
    try{
      var _uid = (window._fbUser && window._fbUser.uid) || window._gUserId || null;
      if(!_uid || !_fbDb) return;
      // ① 讀「伺服器」收件箱(權威 reward),失敗(規則未部署/離線)→ 靜默,下次再試
      var _inbox = [];
      try{
        var _snap = await getDocs(collection(_fbDb, 'gmClassRewards', _uid, 'items'));
        _snap.forEach(function(d){ _inbox.push(Object.assign({ id: d.id }, d.data() || {})); });
      }catch(_eRd){ return; }
      if(!_inbox.length) return;
      // ② 讀主檔:_gmcrClaimed(只對「已領」對帳)/ _gmcrRecoveredTickets(冪等)/ 兩 durable 帳本(已用)/ _gmcrClaimLog
      var _pd = {};
      try{ var _ps = await getDocFromServer(doc(_fbDb, 'players', _uid)); if(_ps.exists()) _pd = _ps.data() || {}; }
      catch(_ePd){ return; }
      var _claimedSet = {};
      (Array.isArray(_pd._gmcrClaimed) ? _pd._gmcrClaimed : []).forEach(function(x){ _claimedSet[x] = 1; });
      var _recovered = (_pd._gmcrRecoveredTickets && typeof _pd._gmcrRecoveredTickets === 'object') ? Object.assign({}, _pd._gmcrRecoveredTickets) : {};
      // granted[X] = Σ 已領收件箱 reward.backpack[X](只算自選卷 key·只算 _gmcrClaimed 內的)
      // ★ v3.16.97 防護:① 跳過已撤銷(enabled:false)獎勵→不補回 GM 已撤回的券
      //   ② 近 30 天窗:舊批次的「英雄自選卷」跳過(_heroUnlockHistory 封頂 200 筆·過舊的 used 紀錄
      //      可能被擠掉→誤判沒用過→多補);「至寶自選卷」豁免此窗(_treasureUnlockHistory 永不到 200
      //      筆·used 永遠算得到·任何時間都安全)。對齊老師「寧可少補不重複發」。
      var _RECONCILE_WINDOW_MS = 30 * 24 * 60 * 60 * 1000;   // 30 天
      var _nowR = Date.now();
      var _granted = {};
      _inbox.forEach(function(rw){
        if(!rw || !rw.id || !_claimedSet[rw.id]) return;     // 沒領的不對帳(會在收件箱正常顯示·走正常領取)
        if(rw.enabled === false) return;                     // ★ 防護①:已撤銷的不補
        var _bp = (rw.reward && rw.reward.backpack && typeof rw.reward.backpack === 'object') ? rw.reward.backpack : null;
        if(!_bp) return;
        var _isOld = !!(rw.at && (_nowR - rw.at) > _RECONCILE_WINDOW_MS);
        Object.keys(window._GMCR_PICK_TICKET_SRC).forEach(function(_k){
          // ★ 防護②:舊批次 + 帳本會封頂(英雄帳本)→ used 可能過期不可靠 → 跳過(至寶帳本不封頂→照算)
          if(_isOld && window._GMCR_PICK_TICKET_SRC[_k].ledger === '_heroUnlockHistory') return;
          var _q = Math.max(0, parseInt(_bp[_k], 10) || 0);
          if(_q > 0) _granted[_k] = (_granted[_k] || 0) + _q;
        });
      });
      if(!Object.keys(_granted).length) return;               // 從沒發過自選卷(或都被窗/撤銷過濾掉)→ 免對帳
      // used[X] = durable 帳本對應 source 計數(撐過 merge)
      var _countSrc = function(ledgerKey, src){
        var _arr = Array.isArray(_pd[ledgerKey]) ? _pd[ledgerKey] : [];
        var _c = 0;
        _arr.forEach(function(e){ if(e && e.source === src) _c++; });
        return _c;
      };
      // ③ 逐券種 shortfall = granted − used − held − recovered
      var _toGrant = {};
      Object.keys(_granted).forEach(function(_k){
        var _def = window._GMCR_PICK_TICKET_SRC[_k];
        var _used = _countSrc(_def.ledger, _def.source);
        var _held = (typeof backpackGet === 'function') ? (backpackGet(_k) || 0) : 0;
        var _rec  = Math.max(0, parseInt(_recovered[_k], 10) || 0);
        var _short = (_granted[_k] || 0) - _used - _held - _rec;
        if(_short > 0) _toGrant[_k] = _short;
      });
      if(!Object.keys(_toGrant).length) return;               // 都拿到/用掉了 → 不補(不重複發)
      // ④ 補:backpackAdd(即時記憶體+排程上雲)+ 冪等標記 + 救援紀錄(self-write·永遠可寫)
      var _labels = [];
      Object.keys(_toGrant).forEach(function(_k){
        var _n = _toGrant[_k];
        try{ if(typeof backpackAdd === 'function') backpackAdd(_k, _n); }catch(_eAdd){}
        _recovered[_k] = (Math.max(0, parseInt(_recovered[_k], 10) || 0)) + _n;
        _labels.push(window._GMCR_PICK_TICKET_SRC[_k].label + '×' + _n);
      });
      try{
        var _log = Array.isArray(_pd._gmcrClaimLog) ? _pd._gmcrClaimLog.slice() : [];
        _log.push({ rewardId: 'recover_ticket_' + Date.now(), label: '系統補發漏領自選卷',
                    merit: '', items: _labels.slice(0, 40), at: Date.now(), _recover: true });
        if(_log.length > 200) _log = _log.slice(-200);
        await setDoc(doc(_fbDb, 'players', _uid), { _gmcrRecoveredTickets: _recovered, _gmcrClaimLog: _log }, { merge: true });
      }catch(_eMark){ console.warn('[GM獎勵對帳] 寫冪等標記/紀錄失敗(背包已補·下次因 held 已有不會重補)', _eMark); }
      // ⑤ 即時提示玩家
      try{ if(typeof _showInGameToast === 'function') _showInGameToast('🎁 老師補發了你漏領的：' + _labels.join('、'), '#66dd88', 4500); }catch(_eToast){}
      try{ console.log('[GM獎勵對帳 v3.16.97] 補發漏領自選卷', _toGrant); }catch(_){}
    }catch(_eAll){ try{ console.warn('[GM獎勵對帳] 例外(略過·不影響登入)', _eAll); }catch(_){} }
  };

  // ★ v3.16.66 — 玩家:讀自己「已領取的 GM 獎勵紀錄」(_gmcrClaimLog·含獎項/事蹟/時間·新→舊)。
  //   供「🎁 GM獎勵」收件區下半「✅ 已領取紀錄」顯示(待領領完後不再空白)。
  window._fbReadMyGmClassRewardLog = async function(){
    const _uid = (window._fbUser && window._fbUser.uid) || window._gUserId || null;
    if(!_uid || !_fbDb) return [];
    try{
      const _ps = await getDocFromServer(doc(_fbDb, 'players', _uid));
      if(!_ps.exists()) return [];
      const _pd = _ps.data() || {};
      const _log = Array.isArray(_pd._gmcrClaimLog) ? _pd._gmcrClaimLog.slice() : [];
      _log.sort(function(a, b){ return (b && b.at || 0) - (a && a.at || 0); });
      return _log;
    }catch(e){ console.warn('[GM獎勵] 讀已領紀錄失敗', e); return []; }
  };

  // ★ v3.16.66 — GM:讀某玩家「GM 獎勵領取紀錄」(權威·直接讀 gmClassRewardClaims·含 transaction 寫入的精確時間)。
  //   email / 學號(lsps...) → where('email') 反查 uid;其餘當 uid。再 where('uid') 撈該玩家所有認領文件(GM isAdmin 放行)。
  //   獎項名/事蹟從 gmClassRewards/{uid}/items 反查補(若原獎勵還在);回傳 { uid, email, claims:[{rewardId,at,items,label,merit}...] }(新→舊)。
  window._fbReadPlayerGmClassRewardClaims = async function(emailOrUid){
    if(!_fbDb) throw new Error('Firestore 尚未就緒');
    const _raw = String(emailOrUid || '').trim();
    if(!_raw) throw new Error('請提供 email 或 uid');
    let _uid = '', _email = '', _byEmail = '';
    if(_raw.indexOf('@') >= 0){ _byEmail = _raw.toLowerCase(); }
    else if(/^lsps\d+$/i.test(_raw)){ _byEmail = _raw.toLowerCase() + '@stu.lsps.tp.edu.tw'; }
    if(_byEmail){
      _email = _byEmail;
      const _q = query(collection(_fbDb, 'players'), where('email', '==', _email));
      const _snap = await getDocs(_q);
      if(_snap.empty) throw new Error('查無此玩家:' + _email);
      _snap.forEach(function(d){ if(!_uid){ _uid = d.id; } });
    } else {
      _uid = _raw;
      try{
        const _pSnap = await getDoc(doc(_fbDb, 'players', _uid));
        const _pEx = (typeof _pSnap.exists === 'function') ? _pSnap.exists() : !!_pSnap.exists;
        if(_pEx){ _email = (_pSnap.data() || {}).email || ''; }
      }catch(_){}
    }
    let _claims = [];
    try{
      const _cq = query(collection(_fbDb, 'gmClassRewardClaims'), where('uid', '==', _uid));
      const _cs = await getDocs(_cq);
      _cs.forEach(function(d){ _claims.push(Object.assign({ docId: d.id }, d.data() || {})); });
    }catch(e){ console.warn('[GM獎勵紀錄] 讀認領文件失敗(可能規則未部署)', e); }
    // 補獎項名/事蹟:從該玩家 gmClassRewards/{uid}/items 反查(若原獎勵未被 GM 刪除)
    let _itemMap = {};
    try{
      const _is = await getDocs(collection(_fbDb, 'gmClassRewards', _uid, 'items'));
      _is.forEach(function(d){ _itemMap[d.id] = d.data() || {}; });
    }catch(_e){}
    _claims.forEach(function(c){
      const _src = _itemMap[c.rewardId] || {};
      c.label = _src.label || '';
      c.merit = _src.merit || '';
      if((!c.items || !c.items.length) && Array.isArray(_src.itemsLabel)) c.items = _src.itemsLabel;
    });
    _claims.sort(function(a, b){ return (b && b.at || 0) - (a && a.at || 0); });
    return { uid: _uid, email: _email, claims: _claims };
  };

  // ★ v3.16.66 — GM:彈窗檢視某玩家 GM 獎勵領取紀錄(清楚列出領過哪些獎項 + 時間 + 事蹟)。
  window._fbShowPlayerGmClassRewardClaims = async function(emailOrUid){
    let _data;
    try{ _data = await window._fbReadPlayerGmClassRewardClaims(emailOrUid); }
    catch(e){ try{ alert('查詢失敗:' + (e && e.message ? e.message : e)); }catch(_){} return; }
    const _esc = function(s){ return String(s == null ? '' : s).replace(/[&<>"]/g, function(c){ return ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'})[c]; }); };
    const _old = document.getElementById('_gm-gmcr-claims-popup'); if(_old) _old.remove();
    const _claims = Array.isArray(_data.claims) ? _data.claims : [];
    let _rows = '';
    if(!_claims.length){
      _rows = '<div style="text-align:center;color:#9a9aac;padding:24px;font-size:16px;">這位玩家還沒有領取過任何 GM 獎勵(或雲端規則尚未部署)。</div>';
    } else {
      _claims.forEach(function(c){
        const _dt = new Date(c && c.at ? c.at : 0);
        const _dstr = (c && c.at) ? ((_dt.getMonth() + 1) + '/' + _dt.getDate() + '/' + _dt.getFullYear() + ' ' + String(_dt.getHours()).padStart(2, '0') + ':' + String(_dt.getMinutes()).padStart(2, '0')) : '(無時間)';
        const _items = Array.isArray(c && c.items) ? c.items : [];
        const _meritHtml = (c && c.merit) ? ('<div style="font-size:13px;color:#ffd98c;margin-top:2px;">🏅 ' + _esc(c.merit) + '</div>') : '';
        _rows += '<div style="border-top:1px solid rgba(140,220,160,0.18);padding:9px 2px;">'
          + '<div style="font-size:12.5px;color:#9ad6b0;font-weight:700;margin-bottom:3px;">🕐 ' + _esc(_dstr) + '</div>'
          + '<div style="font-size:15px;color:#ffe066;font-weight:800;line-height:1.6;">🎁 ' + (_items.length ? _items.map(_esc).join('、') : '(獎勵內容)') + '</div>'
          + _meritHtml
          + '<button class="_gmcr-del-btn" data-rid="' + _esc(c && c.rewardId ? c.rewardId : '') + '" style="margin-top:7px;padding:5px 12px;font-size:12.5px;font-weight:800;background:rgba(120,40,40,0.5);border:1.5px solid #d66;color:#ffcaca;border-radius:7px;cursor:pointer;font-family:inherit;">🗑 刪除此筆(學生收件箱也清除)</button>'
          + '</div>';
      });
    }
    const _pop = document.createElement('div');
    _pop.id = '_gm-gmcr-claims-popup';
    _pop.style.cssText = 'position:fixed;inset:0;z-index:100000;display:flex;align-items:center;justify-content:center;background:rgba(0,0,12,0.78);backdrop-filter:blur(4px);padding:18px;font-family:"M PLUS Rounded 1c","Nunito",sans-serif;';
    _pop.innerHTML = '<div style="background:linear-gradient(160deg,rgba(16,34,22,0.98),rgba(24,52,34,0.98));border:2.5px solid rgba(140,220,160,0.8);border-radius:16px;padding:20px 22px;max-width:min(96vw,640px);max-height:90vh;overflow-y:auto;-webkit-overflow-scrolling:touch;box-shadow:0 10px 50px rgba(60,160,100,0.5);text-align:left;">'
      + '<div style="font-size:23px;font-weight:900;color:#bff0cf;text-align:center;letter-spacing:1px;margin-bottom:4px;">🎁 GM 獎勵領取紀錄</div>'
      + '<div style="font-size:13px;color:#9ad6b0;text-align:center;margin-bottom:12px;word-break:break-all;">' + _esc(_data.email || '(無 email)') + ' ・ uid:' + _esc(_data.uid || '') + '</div>'
      + '<div style="background:rgba(0,16,8,0.4);border:1px solid rgba(140,220,160,0.3);border-radius:10px;padding:10px 14px;margin-bottom:6px;">'
        + '<div style="font-size:14px;color:#cbeccb;">這位玩家總共領取過 <b style="color:#fff;font-size:17px;">' + _claims.length + '</b> 份 GM 獎勵(依時間新→舊):</div>'
      + '</div>'
      + _rows
      + '<button onclick="(function(){var e=document.getElementById(\'_gm-gmcr-claims-popup\');if(e)e.remove();})()" style="margin-top:14px;width:100%;padding:11px;font-size:15px;font-weight:800;background:rgba(60,120,80,0.4);border:1.5px solid #5cbf86;color:#cfeeda;border-radius:8px;cursor:pointer;font-family:inherit;">關閉</button>'
      + '</div>';
    _pop.onclick = function(ev){ if(ev.target === _pop) _pop.remove(); };
    document.body.appendChild(_pop);
    // ★ v3.16.83 — 逐筆「🗑 刪除此筆」鈕:GM 誤發回收後,連帶把學生「🎁 GM 獎勵」收件箱紀錄清乾淨(待領項 + 已領取紀錄)。
    try{
      Array.prototype.forEach.call(_pop.querySelectorAll('._gmcr-del-btn'), function(_b){
        _b.onclick = async function(){
          var _rid = _b.getAttribute('data-rid');
          if(!_rid){ try{ alert('此筆缺少獎勵代碼,無法刪除'); }catch(_){} return; }
          if(!confirm('確定刪除這筆 GM 獎勵紀錄?\n\n會一併從學生「🎁 GM 獎勵」收件箱移除(待領項 + 已領取紀錄)。\n\n⚠ 這只刪「紀錄」,不會自動扣回已經發出的獎勵 / 英雄。如需收回實際發出的東西,請另用「學生補償(負值扣回)」或「污染英雄刪除」工具。')) return;
          _b.disabled = true; _b.textContent = '刪除中…';
          try{
            if(typeof window._fbAdminDeleteGmClassReward !== 'function') throw new Error('_fbAdminDeleteGmClassReward 未載入(index.html 可能尚未更新)');
            await window._fbAdminDeleteGmClassReward(_data.uid, _rid);
            var _row = _b.parentElement; if(_row) _row.style.display = 'none';
            _b.textContent = '✅ 已刪除';
          }catch(_eDel){
            _b.disabled = false; _b.textContent = '🗑 刪除失敗,再試一次';
            try{ alert('刪除失敗:' + (_eDel && _eDel.message ? _eDel.message : _eDel)); }catch(_){}
          }
        };
      });
    }catch(_eWire){ console.warn('[GM獎勵刪除] 綁定刪除鈕失敗', _eWire); }
  };

  // ★ v3.16.83 — GM:刪除某玩家「一筆 GM 課堂獎勵」的所有痕跡(誤發回收後,避免學生收件箱還看得到)。
  //   清 ① 收件箱 items doc(刪不掉就改 enabled:false 隱藏待領)② 認領文件 claim doc ③ 玩家主檔 _gmcrClaimLog
  //   (收件箱「✅ 已領取紀錄」顯示來源·GM 對 players 全權必能寫)。_gmcrClaimed 待領抑制快取刻意保留:
  //   即使 items 沒刪成功,也不會讓該筆又冒出來變待領。⚠ 只刪「紀錄」,不自動扣回已入帳的獎勵/英雄(收回另用既有工具)。
  window._fbAdminDeleteGmClassReward = async function(uid, rewardId){
    if(!_fbDb) throw new Error('Firestore 未就緒');
    if(!uid || !rewardId) throw new Error('uid / rewardId 不可為空');
    var _uid = String(uid), _rid = String(rewardId);
    var _itemGone = false;
    // ① 收件箱 items doc:先試刪除;規則不允許刪除時退而停用 enabled:false(待領清單 _fbReadMyGmClassRewards 會過濾掉)
    try{
      await deleteDoc(doc(_fbDb, 'gmClassRewards', _uid, 'items', _rid));
      _itemGone = true;
    }catch(_eItem){
      console.warn('[GM獎勵刪除] items 刪除失敗,改試停用', _eItem);
      try{ await setDoc(doc(_fbDb, 'gmClassRewards', _uid, 'items', _rid), { enabled: false }, { merge: true }); _itemGone = true; }
      catch(_eDis){ console.warn('[GM獎勵刪除] items 停用也失敗(可能規則未允許)', _eDis); }
    }
    // ② 認領文件 claim doc(best-effort;未領取/規則未允許皆無妨)
    try{ await deleteDoc(doc(_fbDb, 'gmClassRewardClaims', _uid + '_' + _rid)); }
    catch(_eClaim){ console.warn('[GM獎勵刪除] 認領文件刪除失敗(可能未領取或規則未允許)', _eClaim); }
    // ③ 清玩家主檔 _gmcrClaimLog(收件箱「已領取紀錄」)·_gmcrClaimed 待領抑制快取保留
    try{
      var _ps = await getDocFromServer(doc(_fbDb, 'players', _uid));
      if(_ps.exists()){
        var _pd = _ps.data() || {};
        var _log = Array.isArray(_pd._gmcrClaimLog) ? _pd._gmcrClaimLog.slice() : [];
        var _newLog = _log.filter(function(e){ return !(e && e.rewardId === _rid); });
        if(_newLog.length !== _log.length){
          await setDoc(doc(_fbDb, 'players', _uid), { _gmcrClaimLog: _newLog }, { merge: true });
        }
      }
    }catch(_ePd){ console.warn('[GM獎勵刪除] 清玩家已領紀錄失敗', _ePd); }
    console.info('✅ [GM獎勵刪除] uid=' + _uid + ' rewardId=' + _rid + ' (itemGone=' + _itemGone + ')');
    return { ok: true, itemGone: _itemGone };
  };

  // ★ v3.17.6(老師需求 1)— 送禮記錄查詢(依「姓名/信箱/uid」或「獎項名稱/事蹟」關鍵字),
  //   交叉比對「gmClassRewardClaims 認領文件」→ 標記每筆:✅已領取(UID權威) / ❌未領取 / ❔未知。
  //   回傳 { keyword, total, rows:[ {uid,label,email,items,merit,at,rewardId,kind,claimed,claimedAt} ] }(新→舊)。
  window._fbAdminGiftAudit = async function(keyword, maxScan){
    if(!_fbDb) throw new Error('Firestore 未就緒');
    var _kw = String(keyword || '').trim().toLowerCase();
    var _n = parseInt(maxScan, 10) || 400;
    var _logs = [];
    try{
      var _q;
      try{ _q = query(collection(_fbDb, 'gmGiftLog'), orderBy('at', 'desc'), limit(_n)); }
      catch(_e1){ _q = query(collection(_fbDb, 'gmGiftLog'), limit(_n)); }
      var _snap = await getDocs(_q);
      _snap.forEach(function(d){ var _x = d.data() || {}; _logs.push({ id:d.id, uid:_x.uid||'', label:_x.label||'', email:_x.email||'', items:Array.isArray(_x.items)?_x.items:[], by:_x.by||'', at:_x.at||0, merit:_x.merit||'', rewardId:_x.rewardId||'', kind:_x.kind||'' }); });
    }catch(e){ throw new Error('讀取送禮記錄失敗(可能 gmGiftLog 規則未部署):' + (e && e.message || e)); }
    var _matched = _logs.filter(function(r){
      if(!_kw) return true;
      var _hay = ((r.label||'') + ' ' + (r.email||'') + ' ' + (r.uid||'') + ' ' + (r.items||[]).join(' ') + ' ' + (r.merit||'')).toLowerCase();
      return _hay.indexOf(_kw) >= 0;
    });
    _matched.sort(function(a, b){ return (b.at||0) - (a.at||0); });
    var _rows = [], _checkCap = 80;
    for(var i = 0; i < _matched.length; i++){
      var r = _matched[i];
      var _claimed = null, _claimedAt = 0;
      if(r.rewardId && r.uid && _checkCap > 0){
        _checkCap--;
        try{
          var _cs = await getDocFromServer(doc(_fbDb, 'gmClassRewardClaims', r.uid + '_' + r.rewardId));
          if(_cs.exists()){ _claimed = true; var _cd = _cs.data() || {}; _claimedAt = _cd.at || 0; }
          else { _claimed = false; }
        }catch(_ec){ _claimed = null; }
      }
      _rows.push(Object.assign({}, r, { claimed: _claimed, claimedAt: _claimedAt }));
    }
    return { keyword: _kw, total: _matched.length, rows: _rows };
  };

  // ★ v3.17.6(老師需求 3)— 安全補發「已發送但遺失」的獎勵(學生雲端查無獲得該獎勵記錄才補)。
  //   最沒風險做法:補發前用「兩道權威訊號」把關,任一顯示已獲得即阻擋,絕不重複發:
  //     ① gmClassRewardClaims 認領文件存在  = 已認領入帳(UID 權威)→ 阻擋
  //     ②.5 玩家主檔 _gmcrClaimed 含本筆     = 領取流程「入帳前」就標記(rollback 會移除)→ 幾乎確定已入帳 → 阻擋
  //   兩者皆查無 → 真遺失 → 停用原收件箱該筆(避免同時領原筆+補發筆)+ 寫「新一筆」收件箱(新 rewardId,
  //   create-only 認領規則天然防重複領)→ 補發即權威化,不會被污染或重複。
  window._fbAdminResendLostGift = async function(uid, rewardId){
    if(!_fbDb) throw new Error('Firestore 未就緒');
    var _uid = String(uid || ''), _rid = String(rewardId || '');
    if(!_uid || !_rid) throw new Error('uid / rewardId 不可為空');
    // ① 權威認領文件檢查(已領取入帳 → 阻擋)
    try{
      var _cs = await getDocFromServer(doc(_fbDb, 'gmClassRewardClaims', _uid + '_' + _rid));
      if(_cs.exists()){
        return { ok:false, blocked:true, reason:'這位學生已有「UID 權威認領記錄」(已領取入帳),補發會造成重複,已阻擋。' };
      }
    }catch(_ec){ throw new Error('讀取認領記錄失敗,為安全起見中止補發:' + (_ec && _ec.message || _ec)); }
    // ①.5 玩家主檔 _gmcrClaimed 檢查(領取流程入帳前就標記·含此筆幾乎確定已入帳 → 阻擋)
    try{
      var _ps = await getDocFromServer(doc(_fbDb, 'players', _uid));
      if(_ps.exists()){
        var _pd = _ps.data() || {};
        if(Array.isArray(_pd._gmcrClaimed) && _pd._gmcrClaimed.indexOf(_rid) >= 0){
          return { ok:false, blocked:true, reason:'這位學生的領取記錄(主檔 _gmcrClaimed)已含本筆,幾乎確定已入帳,補發會重複,已阻擋。若確認真的沒拿到,請改用「登入自動對帳補回」或個別補償工具。' };
        }
      }
    }catch(_eps){ /* 讀不到主檔就靠認領文件把關 */ }
    // ② 讀原收件箱獎勵(取得 reward/itemsLabel/merit/label/email 原樣補發)
    var _orig;
    try{
      var _rs = await getDocFromServer(doc(_fbDb, 'gmClassRewards', _uid, 'items', _rid));
      if(!_rs.exists()) throw new Error('查無原始獎勵資料(收件箱該筆已刪),無法安全補發原內容');
      _orig = Object.assign({ id:_rs.id }, _rs.data() || {});
    }catch(e){ throw new Error('讀取原始獎勵失敗:' + (e && e.message || e)); }
    // ③ 停用原收件箱該筆(避免學生同時領到「原筆 + 補發筆」造成重複)
    try{ await setDoc(doc(_fbDb, 'gmClassRewards', _uid, 'items', _rid), { enabled:false }, { merge:true }); }
    catch(_eDis){ console.warn('[補發] 停用原收件箱失敗(續行,補發筆仍為新 rewardId)', _eDis); }
    // ④ 寫「新的一筆」收件箱(新 rewardId·create-only 認領規則天然防重複領)
    var _label = _orig.label || '', _email = _orig.email || '', _reward = _orig.reward || {}, _merit = _orig.merit || '';
    var _itemsLabel = Array.isArray(_orig.itemsLabel) ? _orig.itemsLabel : [];
    var _res;
    try{
      if(typeof window._fbWriteGmClassReward !== 'function') throw new Error('_fbWriteGmClassReward 未載入');
      _res = await window._fbWriteGmClassReward(_uid, _label, _email, _reward, _itemsLabel, ('（補發）' + _merit).slice(0, 120));
    }catch(e){ throw new Error('寫入補發獎勵失敗:' + (e && e.message || e)); }
    // ⑤ 送禮記錄(稽核軌跡·標補發)
    try{
      if(typeof window._fbWriteGmGiftLog === 'function'){
        var _by = (window._fbUser && window._fbUser.email) || 'admin';
        await window._fbWriteGmGiftLog({ uid:_uid, label:_label, email:_email, items:_itemsLabel, by:_by, merit:('補發:' + _merit), rewardId:(_res && _res.id) || '', kind:'resendLost' });
      }
    }catch(_eLog){ console.warn('[補發] 送禮記錄寫入失敗', _eLog); }
    console.info('✅ [補發] uid=' + _uid + ' 原rid=' + _rid + ' → 新rid=' + ((_res && _res.id) || '?'));
    return { ok:true, newRewardId:(_res && _res.id) || '', label:_label };
  };

  window._fbAdminFindPlayerByEmail = async function(input) {
    const _raw = String(input || '').trim();
    if(!_raw) throw new Error('輸入不可為空');

    let _type;
    let _resolvedInput = _raw;
    const _sidEmail = (typeof window._lxpsStudentIdToEmail === 'function') ? window._lxpsStudentIdToEmail(_raw) : null;
    if(_raw.includes('@')) _type = 'email';
    else if(/^[A-Za-z0-9-]{20,}$/.test(_raw)) _type = 'uid';
    else if(_sidEmail){
      // ★ v3.13.49 — 學號(lsps110046 或純 6 位)→ 完整 email
      _type = 'email';
      _resolvedInput = _sidEmail;
      console.log('[_fbAdminFindPlayerByEmail] 學號自動拼成 email:' + _resolvedInput);
    }
    else if(/^\d{6}$/.test(_raw)){
      // 6 位學號 → 自動拼成完整 email

      _type = 'email';
      _resolvedInput = 'lsps' + _raw + '@stu.lsps.tp.edu.tw';
      console.log('[_fbAdminFindPlayerByEmail] 6 位學號自動拼成:' + _resolvedInput);
    }
    else if(/^\d{5}$/.test(_raw)){
      // 5 位班級碼(年級1+班級2+座號2)→ 班級<10 時去前導 0 變回 4 碼
      _type = 'name';
      if(_raw[1] === '0'){
        _resolvedInput = _raw[0] + _raw.slice(2);
        console.log('[_fbAdminFindPlayerByEmail] 5 位班級碼規範化:' + _raw + ' → ' + _resolvedInput);
      }
    }
    else _type = 'name';

    try {
      // ── 路徑 A:email ──
      if(_type === 'email'){
        const _norm = _resolvedInput.toLowerCase();
        const q = query(collection(_fbDb, 'players'), where('email', '==', _norm));
        const snap = await getDocs(q);
        if(snap.empty) {
          return { found: false, email: _norm, reason: 'not_found' };
        }
        const d = snap.docs[0];
        return {
          found: true,
          uid: d.id,
          email: _norm,
          data: d.data() || {}
        };
      }

      // ── 路徑 B:uid(直接拉文件) ──
      if(_type === 'uid'){
        const _ref = doc(_fbDb, 'players', _raw);
        const _docSnap = await getDoc(_ref);
        if(!_docSnap.exists()){
          return { found: false, email: '(uid: ' + _raw + ')', reason: 'not_found' };
        }
        const _data = _docSnap.data() || {};
        return {
          found: true,
          uid: _raw,
          email: _data.email || '',
          data: _data
        };
      }

      // ── 路徑 C:姓名 / 班級碼(複用 _fbAdminFindPlayersByName) ──
      if(typeof window._fbAdminFindPlayersByName !== 'function'){
        throw new Error('_fbAdminFindPlayersByName 未載入,請重新整理');
      }
      const _r = await window._fbAdminFindPlayersByName(_resolvedInput);
      const _players = (_r && _r.players) || [];
      if(!_players.length){
        return { found: false, email: '(姓名: ' + _raw + ')', reason: 'not_found' };
      }
      if(_players.length === 1){
        const _p = _players[0];
        const _ref = doc(_fbDb, 'players', _p.uid);
        const _docSnap = await getDoc(_ref);
        if(!_docSnap.exists()){
          return {
            found: true,
            uid: _p.uid,
            email: _p.email || '',
            data: { displayName: _p.name || '', email: _p.email || '' }
          };
        }
        return {
          found: true,
          uid: _p.uid,
          email: _docSnap.data().email || _p.email || '',
          data: _docSnap.data() || {}
        };
      }
      // 多筆候選
      const _err = new Error(
        '姓名「' + _raw + '」找到 ' + _players.length + ' 位候選,請改輸入更精確的姓名或直接複製其中一位的 email/uid:\n\n'
        + _players.slice(0, 10).map((p, i) => (i+1) + '. ' + p.name + (p.email ? ' (' + p.email + ')' : '')).join('\n')
        + (_players.length > 10 ? '\n... 還有 ' + (_players.length - 10) + ' 位候選未列出' : '')
      );
      _err._multi = true;
      _err._candidates = _players;
      throw _err;
    } catch(e) {
      if(e && e._multi) throw e;
      console.error('[_fbAdminFindPlayerByEmail] 查詢失敗', _raw, e);
      const _err = new Error((_type === 'email' ? 'email' : (_type === 'uid' ? 'uid' : '姓名')) + ' 查詢失敗:' + (e.code || e.message || e));
      _err.code = e.code;
      _err._isPermissionDenied = (e.code === 'permission-denied');
      throw _err;
    }
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.11.35+(2026-05-29) — 按姓名查玩家(回傳陣列,姓名可能多筆同名)
  //
  //   策略:Firestore 不支援「子字串 contains」查詢,所以策略是:
  //     1. 先嘗試精確比對(players where name === 輸入)→ 快
  //     2. 若 0 筆 → fallback 全掃 players(最多 limit 500),做本地子字串/前綴比對
  //     3. 回傳前 20 筆候選 + 總筆數
  //
  //   ⚠ 全掃成本:500 reads,只在「找不到精確比對」才會跑,日常使用幾乎不會踩到
  //   ⚠ 名稱可能包含座號前綴(例如「5324王同學」),所以也支援「同學名稱不含座號」的模糊匹配
  // ════════════════════════════════════════════════════════════════════
  window._fbAdminFindPlayersByName = async function(name, opts){
    let _norm = (name || '').trim();
    if(!_norm) throw new Error('姓名不可為空');
    opts = opts || {};
    // ★ v3.12.4(2026-05-30) — maxScan 預設 500→1500
    // ★ v3.13.74 — 再提高到 3000:涵蓋「900 學生 + 歷年測試/重註冊帳號」總數可能超過 1500 的情況
    //   (真名搜尋已由階段 1.5 名冊優先處理;此上限只影響「遊戲暱稱」全掃 fallback)
    const _maxScan = parseInt(opts.maxScan, 10) || 3000;
    const _maxReturn = parseInt(opts.maxReturn, 10) || 30;

    // ★ v3.16.61 — 信箱直查(校外帳號:無真名、無學號,只能憑信箱找)。
    //   輸入含 @ 視為完整信箱 → 精確查 email 欄位(涵蓋 gmail 等校外帳號,亦適用直接貼校內完整信箱)。
    //   0 筆才往下走原本流程(學號/班級/姓名)。
    if(_norm.indexOf('@') > 0){
      const _emailQ = _norm.toLowerCase();
      try{
        const _qEm = query(collection(_fbDb, 'players'), where('email', '==', _emailQ));
        const _snapEm = await getDocs(_qEm);
        if(!_snapEm.empty){
          const _listEm = [];
          _snapEm.forEach(d => {
            const _data = d.data() || {};
            _listEm.push({
              uid: d.id,
              name: _data.displayName || _data.name || '',
              email: _data.email || '',
              knowledgeCoins: _data.knowledgeCoins || 0,
              unlockedCount: Array.isArray(_data.unlockedHeroes) ? _data.unlockedHeroes.length : 0,
              matchType: 'email_exact',
            });
          });
          console.log('[_fbAdminFindPlayersByName] ✅ 信箱直查 → ' + _listEm.length + ' 筆');
          return { found: true, matchType: 'email_exact', count: _listEm.length, players: _listEm };
        }
        console.log('[_fbAdminFindPlayersByName] 信箱直查 0 筆,fallback 走原本流程');
      }catch(e){ console.warn('[_fbAdminFindPlayersByName] 信箱直查失敗,fallback', e); }
    }

    // ★ v3.12.14 — 純數字輸入規範化(與 _fbAdminFindPlayerByEmail 同步)
    //   6 位純數字 = 力行國小學號 → 拼 email 直查
    //   5 位純數字 = 5 碼班級碼(年級1+班級2+座號2)→ 班級<10 時去前導 0 變 4 碼
    //   4 位純數字 = 4 碼班級碼,直接走原本班級比對
    // ★ v3.13.49 — 完整學號(含字母前綴, 如 lsps110046)也支援:用共用 helper 拼 email 直查
    const _sidEmailN = (typeof window._lxpsStudentIdToEmail === 'function') ? window._lxpsStudentIdToEmail(_norm) : null;
    if(_sidEmailN && /^[A-Za-z]/.test(_norm)){
      console.log('[_fbAdminFindPlayersByName] 完整學號 → 直查 email:' + _sidEmailN);
      try {
        const _qSid = query(collection(_fbDb, 'players'), where('email', '==', _sidEmailN));
        const _snapSid = await getDocs(_qSid);
        if(!_snapSid.empty){
          const _listSid = [];
          _snapSid.forEach(d => {
            const _data = d.data() || {};
            _listSid.push({
              uid: d.id, name: _data.name || '', email: _data.email || '',
              knowledgeCoins: _data.knowledgeCoins || 0,
              unlockedCount: Array.isArray(_data.unlockedHeroes) ? _data.unlockedHeroes.length : 0,
              matchType: 'student_id_email',
            });
          });
          console.log('[_fbAdminFindPlayersByName] ✅ 完整學號 email 直查 → ' + _listSid.length + ' 筆');
          return { found: true, matchType: 'student_id_email', count: _listSid.length, players: _listSid };
        }
        console.log('[_fbAdminFindPlayersByName] 完整學號 email 直查 0 筆,fallback 走原本流程');
      } catch(e){
        console.warn('[_fbAdminFindPlayersByName] 完整學號 email 直查失敗,fallback', e);
      }
    }
    if(/^\d{6}$/.test(_norm)){
      const _expectedEmail = ('lsps' + _norm + '@stu.lsps.tp.edu.tw').toLowerCase();
      console.log('[_fbAdminFindPlayersByName] 6 位學號 → 直查 email:' + _expectedEmail);
      try {
        const _qEmail = query(collection(_fbDb, 'players'), where('email', '==', _expectedEmail));
        const _snap = await getDocs(_qEmail);
        if(!_snap.empty){
          const _list = [];
          _snap.forEach(d => {
            const _data = d.data() || {};
            _list.push({
              uid: d.id,
              name: _data.name || '',
              email: _data.email || '',
              knowledgeCoins: _data.knowledgeCoins || 0,
              unlockedCount: Array.isArray(_data.unlockedHeroes) ? _data.unlockedHeroes.length : 0,
              matchType: 'student_id_email',
            });
          });
          console.log('[_fbAdminFindPlayersByName] ✅ 6 位學號 email 直查 → ' + _list.length + ' 筆');
          return { found: true, matchType: 'student_id_email', count: _list.length, players: _list };
        }
        console.log('[_fbAdminFindPlayersByName] 6 位學號 email 直查 0 筆,fallback 走原本流程');
      } catch(e){
        console.warn('[_fbAdminFindPlayersByName] 6 位學號 email 直查失敗,fallback', e);
      }
    } else if(/^\d{5}$/.test(_norm) && _norm[1] === '0'){
      const _reduced = _norm[0] + _norm.slice(2);
      console.log('[_fbAdminFindPlayersByName] 5 位班級碼規範化:' + _norm + ' → ' + _reduced);
      _norm = _reduced;
    }

    // ★ v3.12.4(2026-05-30) — 班級座號純數字搜尋判定:4-6 位純數字 = 班級座號
    const _isClassNum = /^\d{3,6}$/.test(_norm);
    console.log('[_fbAdminFindPlayersByName] 開始查詢:「' + _norm + '」 isClassNum=' + _isClassNum);

    try {
      // ── 階段 1:精確比對(快路徑) ──
      // ★ v3.16.61 — 修中文姓名搜不到根因:players doc 實際存「displayName」(Google 真名/暱稱),
      //   並無「name」欄位 → 原 where('name','==') 永遠 0 筆,中文名只能靠名冊反查(覆蓋不全)。
      //   改查 displayName,讓所有「displayName 等於該真名/暱稱」的玩家被精確命中。
      const _qExact = query(collection(_fbDb, 'players'), where('displayName', '==', _norm));
      const _snapExact = await getDocs(_qExact);
      if(!_snapExact.empty){
        const _list = [];
        _snapExact.forEach(d => {
          const _data = d.data() || {};
          _list.push({
            uid: d.id,
            name: _data.displayName || _data.name || '',
            email: _data.email || '',
            knowledgeCoins: _data.knowledgeCoins || 0,
            unlockedCount: Array.isArray(_data.unlockedHeroes) ? _data.unlockedHeroes.length : 0,
            matchType: 'exact',
          });
          if(_list.length >= _maxReturn) return;
        });
        console.log('[_fbAdminFindPlayersByName] ✅ 精確比對 → ' + _list.length + ' 筆');
        return { found: true, matchType: 'exact', count: _list.length, players: _list };
      }

      // ── 階段 1.5(★ v3.13.74 新增):名冊優先比對(修復「中文姓名搜不到」)──
      //   問題根因:中文真名靠「階段 2 全掃 getDocs(limit 1500)」配對 roster fullName,
      //            但 email/學號搜尋走 where('email','==') 過濾查詢(永遠命中);若 players
      //            文件總數 > 1500(900 學生 + 歷年測試/重註冊帳號),全掃會漏掉沒被掃到的學生。
      //   修法:先在 client 端的 _STUDENT_ROSTER(706 筆)比對真名/座號短碼標籤 → 拿到 email,
      //        再用 where('email','in',[...])(每批 30 筆)精確查那些玩家 doc,完全不受掃描上限影響。
      //   注意:仍保留階段 2 全掃,讓「遊戲暱稱」(非真名)也搜得到;此前置只負責真名/座號。
      if(!_isClassNum){
        try{
          const _ros = (window._STUDENT_ROSTER && typeof window._STUDENT_ROSTER === 'object') ? window._STUDENT_ROSTER : null;
          if(_ros){
            const _lcNormR = _norm.toLowerCase();
            const _hitEmails = [];
            Object.keys(_ros).forEach(_em => {
              const _e = _ros[_em] || {};
              const _full = String(_e.fullName || '');
              const _sur = String(_e.surname || '');
              const _label = (typeof window._formatRosterLabel === 'function') ? String(window._formatRosterLabel(_e) || '') : '';
              // 命中:真名 includes / 短碼標籤(如 5324蔣同學)includes / 姓 完全等於
              if((_full && _full.toLowerCase().includes(_lcNormR))
                 || (_label && _label.toLowerCase().includes(_lcNormR))
                 || (_sur && _sur === _norm)){
                _hitEmails.push(_em);
              }
            });
            console.log('[_fbAdminFindPlayersByName] 名冊優先比對「' + _norm + '」→ 名冊命中 ' + _hitEmails.length + ' 筆 email');
            if(_hitEmails.length){
              const _rosterPlayers = [];
              const _seenUid = new Set();
              // where('email','in', batch) 每批最多 30 筆
              for(let _bi = 0; _bi < _hitEmails.length && _bi < 300; _bi += 30){
                const _batch = _hitEmails.slice(_bi, _bi + 30);
                try{
                  const _qIn = query(collection(_fbDb, 'players'), where('email', 'in', _batch));
                  const _snapIn = await getDocs(_qIn);
                  _snapIn.forEach(d => {
                    if(_seenUid.has(d.id)) return;
                    _seenUid.add(d.id);
                    const _dt = d.data() || {};
                    const _emLc = String(_dt.email || '').toLowerCase().trim();
                    const _rfn = (_ros[_emLc] && _ros[_emLc].fullName) ? _ros[_emLc].fullName : '';
                    _rosterPlayers.push({
                      uid: d.id,
                      name: _dt.name || '',
                      email: _dt.email || '',
                      knowledgeCoins: _dt.knowledgeCoins || 0,
                      unlockedCount: Array.isArray(_dt.unlockedHeroes) ? _dt.unlockedHeroes.length : 0,
                      matchType: 'roster_fullname',
                      _rosterFullName: _rfn,
                    });
                  });
                }catch(_eIn){
                  console.warn('[_fbAdminFindPlayersByName] 名冊 email in 查詢失敗(批 ' + _bi + ')', _eIn);
                }
              }
              if(_rosterPlayers.length){
                // 真名長度短的(更精確)排前面
                _rosterPlayers.sort((a, b) => (a.name.length || 0) - (b.name.length || 0));
                console.log('[_fbAdminFindPlayersByName] ✅ 名冊優先 → 查到 ' + _rosterPlayers.length + ' 位玩家 doc');
                const _trunc = _rosterPlayers.length > _maxReturn;
                return {
                  found: true,
                  matchType: 'roster',
                  count: _rosterPlayers.length,
                  truncated: _trunc,
                  players: _rosterPlayers.slice(0, _maxReturn),
                };
              }
              console.log('[_fbAdminFindPlayersByName] 名冊命中 email 但查無對應玩家 doc(該生可能尚未登入/用其他帳號),fallback 全掃');
            }
          } else {
            console.warn('[_fbAdminFindPlayersByName] _STUDENT_ROSTER 未載入,跳過名冊優先,直接全掃');
          }
        }catch(_eR){
          console.warn('[_fbAdminFindPlayersByName] 名冊優先比對例外,fallback 全掃', _eR);
        }
        // ★ v3.16.62 — 設計師名冊備援:上面 _STUDENT_ROSTER(student_roster.js)若缺該生或未載入而沒 return,
        //   就用 index.html 內建的 STUDENT_DESIGNER_HEROES(含 fullName 真名·約 30+ 名設計過英雄的學生)再試一次。
        //   讓「設計過英雄、但不在 student_roster.js / 名冊未上傳」的學生(如高廷睿)仍可用中文真名搜到。
        try{
          const _dh = (typeof STUDENT_DESIGNER_HEROES === 'object' && STUDENT_DESIGNER_HEROES) ? STUDENT_DESIGNER_HEROES : null;
          if(_dh){
            const _lcN = _norm.toLowerCase();
            const _dEmails = [];
            Object.keys(_dh).forEach(function(_em){
              const _e = _dh[_em] || {};
              const _full = String(_e.fullName || '');
              const _nm2 = String(_e.name || '');
              if((_full && _full.toLowerCase().includes(_lcN))
                 || (_nm2 && _nm2.toLowerCase().includes(_lcN))){
                _dEmails.push(_em);
              }
            });
            if(_dEmails.length){
              console.log('[_fbAdminFindPlayersByName] 設計師名冊命中 ' + _dEmails.length + ' 筆 email,查玩家 doc');
              const _dPlayers = [];
              const _seenD = new Set();
              for(let _bi = 0; _bi < _dEmails.length && _bi < 300; _bi += 30){
                const _batch = _dEmails.slice(_bi, _bi + 30);
                try{
                  const _qIn = query(collection(_fbDb, 'players'), where('email', 'in', _batch));
                  const _snapIn = await getDocs(_qIn);
                  _snapIn.forEach(function(d){
                    if(_seenD.has(d.id)) return;
                    _seenD.add(d.id);
                    const _dt = d.data() || {};
                    const _emLc = String(_dt.email || '').toLowerCase().trim();
                    const _rfn = (_dh[_emLc] && _dh[_emLc].fullName) ? _dh[_emLc].fullName : '';
                    _dPlayers.push({
                      uid: d.id,
                      name: _dt.displayName || _dt.name || '',
                      email: _dt.email || '',
                      knowledgeCoins: _dt.knowledgeCoins || 0,
                      unlockedCount: Array.isArray(_dt.unlockedHeroes) ? _dt.unlockedHeroes.length : 0,
                      matchType: 'designer_fullname',
                      _rosterFullName: _rfn,
                    });
                  });
                }catch(_eIn){ console.warn('[_fbAdminFindPlayersByName] 設計師名冊 email in 查詢失敗(批 ' + _bi + ')', _eIn); }
              }
              if(_dPlayers.length){
                _dPlayers.sort(function(a, b){ return (a.name.length || 0) - (b.name.length || 0); });
                console.log('[_fbAdminFindPlayersByName] ✅ 設計師名冊備援 → 查到 ' + _dPlayers.length + ' 位玩家 doc');
                return {
                  found: true,
                  matchType: 'designer',
                  count: _dPlayers.length,
                  truncated: _dPlayers.length > _maxReturn,
                  players: _dPlayers.slice(0, _maxReturn),
                };
              }
              console.log('[_fbAdminFindPlayersByName] 設計師名冊命中 email 但查無對應玩家 doc(該生可能尚未登入),fallback 全掃');
            }
          }
        }catch(_eDh){ console.warn('[_fbAdminFindPlayersByName] 設計師名冊備援例外,fallback 全掃', _eDh); }
      }

      // ── 階段 2:模糊比對(全掃 fallback) ──
      console.log('[_fbAdminFindPlayersByName] 精確比對 0 筆,改全掃做子字串比對(maxScan=' + _maxScan + ')');
      const _qAll = query(collection(_fbDb, 'players'), limit(_maxScan));
      const _snapAll = await getDocs(_qAll);
      const _fuzzy = [];
      const _lcNorm = _norm.toLowerCase();
      _snapAll.forEach(d => {
        const _data = d.data() || {};
        // ★ v3.16.61 — 同階段1:玩家真名/暱稱存於 displayName(非 name)。原讀 _data.name 永遠空字串,
        //   下方 if(!_name) return 直接略過全部 doc → 連 roster fullName 比對都跑不到。改讀 displayName。
        const _name = String(_data.displayName || _data.name || '');
        if(!_name) return;
        const _lcName = _name.toLowerCase();
        // 命中條件:子字串 / 去座號前綴比對
        // 去座號前綴:如「5324王同學」→「王同學」(前 3~5 位數字,放寬)
        const _nameNoPrefix = _name.replace(/^\d{1,6}/, '');
        // ★ v3.12.4 — 也抓「姓名(真實姓名)」括號裡的內容
        //   例「5324王同學(王小明)」→ ['5324王同學', '王小明', '5324', '王同學']
        const _parenInner = (_name.match(/[\(（]([^\)）]+)[\)）]/) || [])[1] || '';
        const _classNum = (_name.match(/^(\d{1,6})/) || [])[1] || '';
        // ★ v3.12.14 — 用 email 反查 _STUDENT_ROSTER 拿 fullName(真實姓名)
        //   讓老師可輸入「高廷睿」、「許立」等中文真名直接搜到玩家
        const _emailLc = String(_data.email || '').toLowerCase().trim();
        let _rosterFullName = '';
        if(_emailLc && typeof window._getRosterEntry === 'function'){
          const _rosEntry = window._getRosterEntry(_emailLc);
          if(_rosEntry && typeof _rosEntry.fullName === 'string'){
            _rosterFullName = _rosEntry.fullName.trim();
          }
        }
        let _matchType = null;
        // 班級座號純數字查詢:優先匹配開頭數字
        if(_isClassNum){
          if(_classNum === _norm){
            _matchType = 'class_exact'; // 班級完全匹配:5324
          } else if(_classNum && _classNum.startsWith(_norm)){
            _matchType = 'class_prefix'; // 班級前綴:輸入 53 → 5324、5326 都中
          } else if(_classNum && _norm.startsWith(_classNum) && _classNum.length >= 3){
            _matchType = 'class_reverse'; // 反向:輸入 53241 但存的是 5324
          }
        }
        // 非數字或數字也找不到,走文字比對
        if(!_matchType){
          // ★ v3.12.14 — roster fullName 真名比對優先(精確度高,老師心中的真名)
          if(_rosterFullName && _rosterFullName.toLowerCase().includes(_lcNorm)){
            _matchType = 'roster_fullname_contains';
          } else if(_lcName.includes(_lcNorm)){
            _matchType = 'contains';
          // ★ v3.16.79 — 反向模糊比對(輸入「包含」暱稱)僅限短輸入(真名/部分中文名 _norm.length<=6);
          //   長信箱帳號字串(如 clarebox123jp,13 字)不再反向誤命中某學生短暱稱(box/clare/123…)
          //   → 根治「成績獎勵只輸入 clarebox123jp 卻把 UR 發到 jlu1201」。本尊仍由 email_contains 正確找到。
          } else if(_lcNorm.includes(_lcName) && _lcName.length >= 2 && _norm.length <= 6){
            _matchType = 'reverse_contains';
          } else if(_nameNoPrefix && _nameNoPrefix.toLowerCase().includes(_lcNorm)){
            _matchType = 'no_prefix_contains';
          } else if(_nameNoPrefix && _lcNorm.includes(_nameNoPrefix.toLowerCase()) && _nameNoPrefix.length >= 2 && _norm.length <= 6){
            _matchType = 'no_prefix_reverse';
          } else if(_parenInner && _parenInner.toLowerCase().includes(_lcNorm)){
            // ★ v3.12.4 — 真實姓名(括號內)子字串比對
            _matchType = 'real_name_contains';
          } else if(!_isClassNum && _emailLc && _lcNorm.length >= 3 && _emailLc.includes(_lcNorm)){
            // ★ v3.16.61 — 校外帳號可用信箱(或片段≥3字)模糊搜尋;純數字輸入不走此路(避免班級碼誤配信箱數字)
            _matchType = 'email_contains';
          }
        }
        if(!_matchType) return;
        _fuzzy.push({
          uid: d.id,
          name: _name,
          email: _data.email || '',
          knowledgeCoins: _data.knowledgeCoins || 0,
          unlockedCount: Array.isArray(_data.unlockedHeroes) ? _data.unlockedHeroes.length : 0,
          matchType: _matchType,
          _rosterFullName: _rosterFullName,
        });
      });
      // 排序:exact > class_exact > class_prefix > roster_fullname > contains > no_prefix > reverse > real_name
      const _typeRank = {
        exact: 0,
        class_exact: 1,
        class_prefix: 2,
        // ★ v3.12.14 — roster fullname 排在 contains 之前
        roster_fullname_contains: 3,
        contains: 4,
        no_prefix_contains: 5,
        class_reverse: 6,
        reverse_contains: 6,
        no_prefix_reverse: 8,
        real_name_contains: 9,
        email_contains: 9,
      };
      _fuzzy.sort((a, b) => {
        const _ra = _typeRank[a.matchType] ?? 10;
        const _rb = _typeRank[b.matchType] ?? 10;
        if(_ra !== _rb) return _ra - _rb;
        return a.name.length - b.name.length;
      });
      const _truncated = _fuzzy.length > _maxReturn;
      const _result = _fuzzy.slice(0, _maxReturn);
      console.log('[_fbAdminFindPlayersByName] 模糊比對掃 ' + _snapAll.size + ' 位 → 命中 ' + _fuzzy.length
        + ' 位(回傳前 ' + _result.length + ')');
      return {
        found: _result.length > 0,
        matchType: _result.length > 0 ? 'fuzzy' : 'not_found',
        count: _fuzzy.length,
        truncated: _truncated,
        scanned: _snapAll.size,
        players: _result,
      };
    } catch(e) {
      console.error('[_fbAdminFindPlayersByName] 查詢失敗', _norm, e);
      const _err = new Error('姓名查詢失敗:' + (e.code || e.message || e));
      _err.code = e.code;
      throw _err;
    }
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.11.34(2026-05-29) — 異常解鎖偵測 + 清除 + 玩家通知 API 群
  //
  //   解決問題:黑暗球結算重複領獎 BUG(v3.11.34 阶段 1 已修)導致一些玩家
  //             已經刷出多隻不該有的稀有英雄/至寶。需要管理員工具:
  //               1. 掃描短時間大量解鎖的玩家
  //               2. 列出該玩家爆出的英雄/至寶
  //               3. 管理員指定刪除異常項目(寫 audit log)
  //               4. 發送通知給玩家(說明原因 + 連結補償)
  //
  //   ⚠ 階段 4 會加更精準的「同 battleId 多解鎖」偵測規則
  //     — 老師指示:1 場 BOSS 戰最多 1 隻新解鎖 + 1 次經驗值,超過視為異常。
  //     本階段先用「短時間爆增」基本規則,階段 4 升級。
  //
  //   資料來源:players/{uid}._heroUnlockHistory[]   — advSaveUnlockedHero v3.11.10 已寫入
  //             players/{uid}._treasureUnlockHistory[] — 本階段新增 hook
  //   結構:    { name/id, at, source, battleId?, uid }
  // ════════════════════════════════════════════════════════════════════

  // ── API 1:掃描異常解鎖英雄玩家 ──
  //
  // ★ v3.12.14 — 只算「BOSS 戰來源」的解鎖,排除星空召喚/抽卡/任務等合法多解鎖路徑
  //   合法路徑(完全排除):summon_*, ticket_ssr/sr, event_quest, student_designer,
  //                       shop_purchase, compensation, admin_grant, admin_delete
  //   只把以下視為 BOSS 戰來源(才算可能異常):
  //     maokong_50pct  / darkorb_5pct / japan_boss_5pct / yamata_miko_5pct
  //     taiwan_clear   / japan_clear  / boss_drop      / wb_* / boss_*
  //   空 source = 老資料,保守當作可能 BOSS 來源(避免漏抓)
  const _BOSS_UNLOCK_SOURCES = new Set([
    'maokong_50pct',
    'darkorb_5pct',
    'japan_boss_5pct',
    'yamata_miko_5pct',
    'taiwan_clear',
    'japan_clear',
    'boss_drop',
    'wb_drop',
  ]);
  function _isBossUnlockSource(s){
    if(!s) return true; // 空 source = 老資料,保守視為 BOSS 來源
    if(_BOSS_UNLOCK_SOURCES.has(s)) return true;
    if(typeof s === 'string' && (s.startsWith('wb_') || s.startsWith('boss_'))) return true;
    return false;
  }

  window._fbAdminScanAbnormalUnlocks = async function(windowMs, threshold){
    windowMs  = parseInt(windowMs, 10)  || (60 * 60 * 1000);   // 預設 1 小時
    threshold = parseInt(threshold, 10) || 5;                  // 預設 ≥ 5 隻
    const _now = Date.now();
    const _cutoff = _now - windowMs;
    try{
      const snap = await getDocs(collection(_fbDb, 'players'));
      const _abnormal = [];
      snap.forEach(docSnap => {
        try{
          const d = docSnap.data() || {};
          let _hist = Array.isArray(d._heroUnlockHistory) ? d._heroUnlockHistory : null;
          if(!_hist && typeof d._heroUnlockHistory_s === 'string'){
            try{ _hist = JSON.parse(d._heroUnlockHistory_s); }catch(_){ _hist = null; }
          }
          if(!Array.isArray(_hist) || !_hist.length) return;
          const _recent = _hist.filter(h => h && typeof h.at === 'number' && h.at >= _cutoff);
          // ★ v3.11.34 — 過濾 admin_delete / admin_grant
          // ★ v3.12.14 — 進一步:只算 BOSS 戰來源,排除 summon_rare 等合法路徑
          const _realUnlocks = _recent.filter(h =>
            h
            && h.source !== 'admin_delete'
            && h.source !== 'admin_grant'
            && _isBossUnlockSource(h.source)
          );
          // ════════════════════════════════════════════════════════════
          // ★ v3.13.89(2026-06-09)— 短時間密集多解鎖偵測(龍王戰洩漏鐵證)
          //   災情:打完世界 BOSS(火山炎龍王)後,殘留 timer 的 checkWin 在清理後
          //   (stage 已回 maokong)多次觸發冒險勝利序列,數秒內接連解鎖多隻木柵英雄。
          //   這些解鎖各自的 battleId 可能不同(每次殘留觸發 = 不同 _advCurrentBattleId),
          //   所以上面「同 battleId 多解鎖」規則抓不到。但合法路徑「每場 BOSS 戰只解鎖 1 隻、
          //   且每場戰鬥需數分鐘」,絕不可能在 90 秒內解鎖 ≥2 隻 BOSS 來源英雄。
          //   → 「90 秒內 ≥2 隻 BOSS 來源解鎖」= 龍王戰洩漏鐵證(不受 threshold 限制即列入)。
          // ════════════════════════════════════════════════════════════
          const _TIGHT_MS = 90 * 1000;
          const _sortedByAt = _realUnlocks.slice()
            .filter(h => typeof h.at === 'number')
            .sort((a, b) => a.at - b.at);
          const _tightClusters = [];
          let _cluster = [];
          for(let _i = 0; _i < _sortedByAt.length; _i++){
            if(_cluster.length === 0){ _cluster = [_sortedByAt[_i]]; continue; }
            if(_sortedByAt[_i].at - _cluster[_cluster.length - 1].at <= _TIGHT_MS){
              _cluster.push(_sortedByAt[_i]);
            } else {
              if(_cluster.length >= 2) _tightClusters.push(_cluster);
              _cluster = [_sortedByAt[_i]];
            }
          }
          if(_cluster.length >= 2) _tightClusters.push(_cluster);
          const _tightClusterInfo = _tightClusters.map(c => ({
            startAt: c[0].at,
            endAt: c[c.length - 1].at,
            spanMs: c[c.length - 1].at - c[0].at,
            count: c.length,
            heroes: c.map(x => x.name),
          }));
          const _tightHeroNames = [];
          _tightClusters.forEach(c => c.forEach(x => { if(x.name) _tightHeroNames.push(x.name); }));

          // 命中「總量 ≥ threshold」 或 「短時間密集多解鎖鐵證」→ 列為異常
          if(_realUnlocks.length >= threshold || _tightClusterInfo.length > 0){
            const _byBattle = {};
            _realUnlocks.forEach(h => {
              const _bk = h.battleId || '(無 battleId)';
              if(!_byBattle[_bk]) _byBattle[_bk] = [];
              _byBattle[_bk].push(h);
            });
            const _multiPerBattle = Object.entries(_byBattle)
              .filter(([k, arr]) => k !== '(無 battleId)' && arr.length >= 2)
              .map(([k, arr]) => ({ battleId: k, count: arr.length, heroes: arr.map(a => a.name) }));
            _abnormal.push({
              uid: docSnap.id,
              email: (d.email || '').toLowerCase(),
              displayName: (d.displayName || ''),
              count: _realUnlocks.length,
              heroes: _realUnlocks.map(h => ({
                name: h.name,
                at: h.at,
                source: h.source || 'unknown',
                battleId: h.battleId || null
              })).sort((a,b) => b.at - a.at),
              multiPerBattle: _multiPerBattle,
              tightClusters: _tightClusterInfo,        // ★ v3.13.89 龍王戰洩漏鐵證
              tightClusterHeroes: _tightHeroNames,      // ★ v3.13.89 預設勾選+標紅用
            });
          }
        }catch(eRow){ console.warn('[_fbAdminScanAbnormalUnlocks] row 例外', eRow); }
      });
      _abnormal.sort((a, b) => b.count - a.count);
      console.log('[_fbAdminScanAbnormalUnlocks v3.12.14] 掃描完成 windowMs=' + windowMs
        + ' threshold=' + threshold + ' → ' + _abnormal.length + ' 位異常玩家'
        + '(只算 BOSS 戰來源,排除星空召喚/抽卡/任務等合法路徑)');
      return { ok: true, abnormal: _abnormal, scannedAt: _now, windowMs, threshold };
    }catch(e){
      console.error('[_fbAdminScanAbnormalUnlocks] 掃描失敗', e);
      throw e;
    }
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v5.130.0 —【三槽同寫單一真相】老師 2026-09-02 裁定甲
  // ────────────────────────────────────────────────────────────────────
  // 病灶(實查全檔 36 支雲端寫入函式):除 _fbCompensatePlayer / _fbStudentSurplusURRemove(v5.92.0)
  //   / _fbAdminRevokeURTickets(v5.93.0) 三支外,**其餘 GM 增刪工具全部只寫主檔**,
  //   saves/live 與 saves/safe 原封不動 ⇒ 兩個症狀其實是同一條鏈:
  //   ❶【要刪的角色一直又回來】live/safe 仍有該英雄 + heroLevels>1 + **沒有 admin_delete 帳本**;
  //      學生下次存檔時 _fbSave 對 _heroUnlockHistory(陣列)是「整包取代」,本機帳本會蓋掉
  //      主檔那唯一一筆 admin_delete ⇒ 稽核感知失去證據 ⇒ 三槽合併把它 union 回來,
  //      就算沒 union 到,_applySafeData 的「幻影救回」(heroLevels>1 或身上有至寶)也會主動 push 回去。
  //   ❷【要拿的角色/券一直沒拿到】同一條鏈的鏡像:補發只寫主檔,帳本被蓋掉之後,
  //      live 槽裡**較舊的 admin_delete 反而變成「最近一筆」** ⇒ 剛補發的角色又被 latest-wins 濾掉;
  //      券則因 playerBackpack 屬「消耗型只取最新槽」,較新的 live 沒有券 ⇒ 券蒸發。
  //
  // 修法:所有 GM 端增刪一律走本函式。builder 收到「該槽自己的資料」、回傳「該槽自己的 patch」
  //   ⇒ 三槽各自計算。★★ 不可改成「同一份 patch 寫三槽」★★ —— 那會把主檔的值倒灌進
  //   live/safe,把學生在別台剛練的等級/剛拿的道具整組蓋掉。
  // ⚠ 只有主檔蓋 _authoritativeRestoreAt(在線學生走既有 piece3 乾淨重載);live/safe 蓋了
  //   只會讓畫面重複閃(同 v5.92.0 裁定)。
  // ⚠ savedAt 預設不動:純移除不碰消耗型欄位,亂 bump 會讓剛寫的槽變「最新槽」,
  //   反而讓該槽較舊的背包/知識幣被當權威。只有動到 playerBackpack 的呼叫端才傳 bumpSavedAt:true。
  // ⚠ 主檔失敗 = 整支 throw(不留半套);live/safe 失敗只記紅字,回傳值帶得出來讓 UI 提醒老師重試。
  // ════════════════════════════════════════════════════════════════════
  window._LXPS_GROWTH_KEYS = ['heroLevels','heroExp','heroSkillLevels','heroStatPoints',
    'heroStatInvested','heroCapsuleInvested','heroBurstLevels','heroTraitLevel'];

  window._lxpsAdminApplyAllSlots = async function(uid, builder, opts){
    if(!uid) throw new Error('_lxpsAdminApplyAllSlots:缺少 uid');
    if(typeof builder !== 'function') throw new Error('_lxpsAdminApplyAllSlots:缺少 builder');
    opts = opts || {};
    var _label = String(opts.label || 'GM操作');
    var _now = Date.now();
    var _targets = [
      { key:'main', ref: doc(_fbDb, 'players', uid) },
      { key:'live', ref: doc(_fbDb, 'players', uid, 'saves', 'live') },
      { key:'safe', ref: doc(_fbDb, 'players', uid, 'saves', 'safe') }
    ];
    var _out = { ok:false, at:_now, label:_label, main:null, live:null, safe:null,
                 mainData:null, written:0, failed:0, absent:0 };
    for(var _i = 0; _i < _targets.length; _i++){
      var _t = _targets[_i];
      var _r = { slot:_t.key, exists:false, written:false, skipped:false, err:null };
      try{
        var _snap = null;
        try{ _snap = await getDocFromServer(_t.ref); }
        catch(_eSrv){ _snap = await getDoc(_t.ref); }   // 配額/離線時退回快取,總比整支失敗好
        var _ex = _snap && (typeof _snap.exists === 'function' ? _snap.exists() : _snap.exists);
        if(!_ex){
          if(_t.key === 'main') throw new Error('查無玩家主檔');
          _out.absent++; _out[_t.key] = _r; continue;   // 沒有這個槽 = 本來就沒東西可復活
        }
        _r.exists = true;
        var _d = _snap.data() || {};
        if(_t.key === 'main') _out.mainData = _d;
        var _patch = builder(_d, _t.key);
        if(!_patch || typeof _patch !== 'object' || Object.keys(_patch).length === 0){
          _r.skipped = true; _out[_t.key] = _r; continue;   // 該槽無事可做
        }
        if(_t.key === 'main'){
          if(opts.stampRestore !== false) _patch._authoritativeRestoreAt = _now;
          _patch._adminForceRestore = true;
          if(opts.rescueSignal) _patch._adminRescueSignal = opts.rescueSignal;
          if(opts.lastAction)   _patch._adminLastAction   = opts.lastAction;
        }
        if(opts.bumpSavedAt === true) _patch.savedAt = _now;
        await setDoc(_t.ref, _patch, { merge: true });
        _r.written = true; _out.written++;
      }catch(_eSlot){
        _r.err = (_eSlot && _eSlot.message) || String(_eSlot);
        _out.failed++;
        _out[_t.key] = _r;
        if(_t.key === 'main') throw _eSlot;             // 主檔失敗 = 整支失敗
        console.error('[三槽同寫 ' + _label + '] ' + _t.key + ' 槽寫入失敗 —— 該槽仍留有舊資料,'
          + '下次三槽合併可能把它撿回來,請重試一次', _eSlot);
        continue;
      }
      _out[_t.key] = _r;
    }
    _out.ok = !!(_out.main && _out.main.written);
    var _fmt = function(r){
      if(!r) return '?';
      if(!r.exists) return '無槽';
      if(r.err) return '❌';
      return r.skipped ? '－' : '✅';
    };
    console.info('[三槽同寫 ' + _label + '] uid=' + uid
      + ' 主檔=' + _fmt(_out.main) + ' live=' + _fmt(_out.live) + ' safe=' + _fmt(_out.safe));
    return _out;
  };

  // ── 共用讀取:同一份「_s 字串優先」口徑(對齊 _lxpsObjFromSlot / _applySafeData) ──
  function _lxpsSlotObj(d, key){
    try{
      if(typeof d[key+'_s'] === 'string'){
        var p = JSON.parse(d[key+'_s']);
        if(p && typeof p === 'object' && !Array.isArray(p)) return p;
      }
      if(d[key] && typeof d[key] === 'object' && !Array.isArray(d[key])) return d[key];
    }catch(_){}
    return null;
  }
  function _lxpsSlotArr(d, key){
    try{
      if(Array.isArray(d[key])) return d[key];
      if(typeof d[key+'_s'] === 'string'){ var p = JSON.parse(d[key+'_s']); if(Array.isArray(p)) return p; }
    }catch(_){}
    return null;
  }

  // ── 共用 patch:移除英雄(8 張養成表 + _s + 至寶解裝 + 每槽帳本) ──
  //   ⚠ 8 張養成表少清任何一張,_lxpsHeroFloorOntoMain 的 effort 判定就會把英雄撿回來
  //     (舊 _fbAdminBulkRemoveHeroes 只清 7 張,漏 heroCapsuleInvested)。
  //   ⚠ 帳本「一定寫」,即使該槽本來就沒有這隻 —— 那筆 admin_delete 才是「不復活」的唯一證據。
  window._lxpsSlotRemoveHeroesPatch = function(d, names, meta){
    d = d || {}; meta = meta || {};
    var _list = (Array.isArray(names) ? names : []).filter(Boolean);
    if(!_list.length) return null;
    var _set = {}; _list.forEach(function(n){ _set[n] = 1; });
    var _now = (typeof meta.at === 'number') ? meta.at : Date.now();
    var _patch = {};
    var _unl = _lxpsSlotArr(d, 'unlockedHeroes');
    if(_unl){
      var _kept = _unl.filter(function(n){ return !_set[n]; });
      _patch.unlockedHeroes = _kept;
      try{ _patch.unlockedHeroes_s = JSON.stringify(_kept); }catch(_){}
    }
    window._LXPS_GROWTH_KEYS.forEach(function(k){
      var _src = _lxpsSlotObj(d, k);
      if(!_src) return;
      var _o = Object.assign({}, _src); var _hit = false;
      _list.forEach(function(n){ if(n in _o){ delete _o[n]; _hit = true; } });
      if(!_hit) return;
      _patch[k] = _o;
      try{ _patch[k+'_s'] = JSON.stringify(_o); }catch(_){}
    });
    var _tre = _lxpsSlotObj(d, 'taiwanTreasureData');
    if(_tre){
      var _tc = {}; var _tHit = false;
      Object.keys(_tre).forEach(function(id){
        var r = _tre[id];
        if(r && typeof r === 'object'){
          var c = Object.assign({}, r);
          if(c.equippedTo && _set[c.equippedTo]){ c.equippedTo = null; _tHit = true; }
          _tc[id] = c;
        } else { _tc[id] = r; }
      });
      if(_tHit){
        _patch.taiwanTreasureData = _tc;
        try{ _patch.taiwanTreasureData_s = JSON.stringify(_tc); }catch(_){}
      }
    }
    var _hist = _lxpsSlotArr(d, '_heroUnlockHistory');
    _hist = _hist ? _hist.slice() : [];
    _list.forEach(function(n){
      var _e = { name:n, at:_now, source:(meta.source || 'admin_delete'),
        adminAction:(meta.adminAction !== false),
        deletedBy:String(meta.by || 'admin'),
        reason:String(meta.reason || '').slice(0, 200),
        uid:String(meta.uid || '').slice(0, 12),
        slot:String(meta.slot || '') };
      if(meta.extra && typeof meta.extra === 'object') Object.assign(_e, meta.extra);   // 呼叫端專屬稽核標記(如 disownedByStudent)
      _hist.push(_e);
    });
    if(_hist.length > 300) _hist = _hist.slice(-300);
    _patch._heroUnlockHistory = _hist;
    try{ _patch._heroUnlockHistory_s = JSON.stringify(_hist); }catch(_){}
    return _patch;
  };

  // ── 共用 patch:補發英雄(union + 等級保底不降級 + 每槽帳本) ──
  //   ⚠ 帳本一定要三槽都寫:別槽若留著較舊的 admin_delete,latest-wins 會把剛補發的角色再濾掉一次。
  window._lxpsSlotGrantHeroesPatch = function(d, names, meta){
    d = d || {}; meta = meta || {};
    var _list = (Array.isArray(names) ? names : []).filter(Boolean);
    if(!_list.length) return null;
    var _now = (typeof meta.at === 'number') ? meta.at : Date.now();
    var _patch = {};
    var _unl = _lxpsSlotArr(d, 'unlockedHeroes');
    _unl = _unl ? _unl.slice() : [];
    var _seen = {}; _unl.forEach(function(n){ if(n) _seen[n] = 1; });
    _list.forEach(function(n){ if(!_seen[n]){ _unl.push(n); _seen[n] = 1; } });
    _patch.unlockedHeroes = _unl;
    try{ _patch.unlockedHeroes_s = JSON.stringify(_unl); }catch(_){}
    var _lvo = Object.assign({}, _lxpsSlotObj(d, 'heroLevels') || {});
    var _lvMap = (meta.levels && typeof meta.levels === 'object') ? meta.levels : {};
    _list.forEach(function(n){
      var _want = Math.max(1, Math.min(50, Math.floor(Number(_lvMap[n]) || 1)));
      if(typeof _lvo[n] !== 'number' || _lvo[n] < _want) _lvo[n] = _want;   // 只保底,絕不降級
    });
    _patch.heroLevels = _lvo;
    try{ _patch.heroLevels_s = JSON.stringify(_lvo); }catch(_){}
    var _hist = _lxpsSlotArr(d, '_heroUnlockHistory');
    _hist = _hist ? _hist.slice() : [];
    _list.forEach(function(n){
      var _e = { name:n, at:_now, source:(meta.source || 'admin_grant'), adminAction:true,
        by:String(meta.by || 'admin'),
        reason:String(meta.reason || '').slice(0, 200),
        uid:String(meta.uid || '').slice(0, 12),
        slot:String(meta.slot || '') };
      if(meta.extra && typeof meta.extra === 'object') Object.assign(_e, meta.extra);   // 呼叫端專屬稽核標記(如 auditRestored)
      _hist.push(_e);
    });
    if(_hist.length > 300) _hist = _hist.slice(-300);
    _patch._heroUnlockHistory = _hist;
    try{ _patch._heroUnlockHistory_s = JSON.stringify(_hist); }catch(_){}
    return _patch;
  };

  // ── 共用 patch:至寶增刪(mode:'remove' 整顆刪 / 'grant' 補發 Lv1;皆寫每槽帳本) ──
  window._lxpsSlotTreasurePatch = function(d, ids, meta){
    d = d || {}; meta = meta || {};
    var _list = (Array.isArray(ids) ? ids : []).filter(Boolean);
    if(!_list.length) return null;
    var _remove = (meta.mode === 'remove');
    var _now = (typeof meta.at === 'number') ? meta.at : Date.now();
    var _src = _lxpsSlotObj(d, 'taiwanTreasureData') || {};
    var _o = JSON.parse(JSON.stringify(_src));
    _list.forEach(function(id){
      if(_remove){ delete _o[id]; }
      else if(!_o[id]){ _o[id] = { lv:1, exp:0, equippedTo:null, invested:{ hp:0, atk:0, sp:0, spd:0 } }; }
    });
    var _patch = { taiwanTreasureData: _o };
    try{ _patch.taiwanTreasureData_s = JSON.stringify(_o); }catch(_){}
    var _hist = _lxpsSlotArr(d, '_treasureUnlockHistory');
    _hist = _hist ? _hist.slice() : [];
    _list.forEach(function(id){
      var _e = { id:id, at:_now, source:(meta.source || (_remove ? 'admin_delete' : 'admin_grant')),
        adminAction:true, by:String(meta.by || 'admin'),
        reason:String(meta.reason || '').slice(0, 200),
        uid:String(meta.uid || '').slice(0, 12),
        slot:String(meta.slot || '') };
      if(meta.extra && typeof meta.extra === 'object') Object.assign(_e, meta.extra);   // 呼叫端專屬稽核標記
      _hist.push(_e);
    });
    if(_hist.length > 300) _hist = _hist.slice(-300);
    _patch._treasureUnlockHistory = _hist;
    try{ _patch._treasureUnlockHistory_s = JSON.stringify(_hist); }catch(_){}
    return _patch;
  };

  // ── 共用 patch:背包增減(逐槽以該槽自己的存量夾 0~99;兩鍵同寫) ──
  //   ⚠ 呼叫端務必同時傳 bumpSavedAt:true —— 背包屬「消耗型只取最新槽」,不 bump 等於沒改。
  window._lxpsSlotBackpackPatch = function(d, deltas, meta){
    d = d || {}; meta = meta || {};
    if(!deltas || typeof deltas !== 'object') return null;
    var _keys = Object.keys(deltas);
    if(!_keys.length) return null;
    var _o = Object.assign({}, _lxpsSlotObj(d, 'playerBackpack') || {});
    var _applied = {};
    _keys.forEach(function(k){
      var _dv = Math.floor(Number(deltas[k]) || 0);
      if(!_dv) return;
      var _cur = Math.max(0, Math.floor(Number(_o[k]) || 0));
      var _next = Math.max(0, Math.min(99, _cur + _dv));
      if(_next === _cur) return;
      _applied[k] = _next - _cur;
      if(_next === 0){ delete _o[k]; } else { _o[k] = _next; }
    });
    if(!Object.keys(_applied).length) return null;
    if(meta.applied && typeof meta.applied === 'object'){
      Object.keys(_applied).forEach(function(k){ meta.applied[k] = _applied[k]; });
    }
    var _patch = { playerBackpack: _o };
    try{ _patch.playerBackpack_s = JSON.stringify(_o); }catch(_){}
    return _patch;
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v5.151.0 —【跨三槽「原始等級」canonical 真相】老師實錘「6213 魔劍姬 44 級救回變 26 級」
  //   根因:_fbStudentDisownHeroes / _fbAdminBulkRemoveHeroes / _fbAdminDeleteUnlockedHero
  //   移除英雄時,_auditRecoveredLevels 的暫存值是**逐槽各自看自己那槽的 heroLevels**
  //   (main/live/safe 三槽各跑一次 builder,每次只看得到「這一槽」的資料)。
  //   共用 iPad 環境下三槽等級本就常不同步(這正是 v5.130.0 整套三槽架構要處理的問題),
  //   於是同一隻英雄在三槽各自暫存出不同的「原等級」(例:main 44 / live 26 / safe 26)。
  //   之後「🛟 一鍵無損救回」同樣逐槽讀「這一槽自己的暫存」寫回「這一槽自己的 heroLevels」,
  //   三槽於是各自還原成不同等級;下次三槽合併時哪一槽 savedAt 較新就採用哪個值,
  //   時機不巧就會顯示救回後「降級」。
  //   ★ 修法:移除當下先讀三槽,取每隻英雄「heroLevels 與既有暫存」的跨槽最大值,
  //   當作唯一真相,三槽的 builder 一律暫存這同一個值(其餘欄位仍照舊各槽各自算,
  //   不違反「不可用同一份 patch 寫三槽」鐵律——那條鐵律是為了保護「還在練的欄位」,
  //   而這裡的英雄本體與等級正要在三槽一併移除,不存在「保護某槽正在練」的問題)。
  // ════════════════════════════════════════════════════════════════════
  window._lxpsCanonicalHeroLevels = async function(uid, names){
    var _list = (Array.isArray(names) ? names : []).filter(Boolean);
    var _out = {};
    if(!uid || !_list.length) return _out;
    var _refs = [
      doc(_fbDb, 'players', uid),
      doc(_fbDb, 'players', uid, 'saves', 'live'),
      doc(_fbDb, 'players', uid, 'saves', 'safe')
    ];
    for(var _i = 0; _i < _refs.length; _i++){
      try{
        var _snap = null;
        try{ _snap = await getDocFromServer(_refs[_i]); }catch(_eS){ _snap = await getDoc(_refs[_i]); }
        var _ex = _snap && (typeof _snap.exists === 'function' ? _snap.exists() : _snap.exists);
        if(!_ex) continue;
        var _d = _snap.data() || {};
        var _lvSrc = (_d.heroLevels && typeof _d.heroLevels === 'object') ? _d.heroLevels : {};
        var _stash = (_d._auditRecoveredLevels && typeof _d._auditRecoveredLevels === 'object') ? _d._auditRecoveredLevels : {};
        _list.forEach(function(n){
          var _cand = Math.max(
            (typeof _lvSrc[n] === 'number') ? _lvSrc[n] : 0,
            (typeof _stash[n] === 'number') ? _stash[n] : 0
          );
          if(_cand > (_out[n] || 0)) _out[n] = _cand;
        });
      }catch(_eSlot){ /* 該槽讀取失敗不影響其他槽,寧可少採信一槽也不整支失敗 */ }
    }
    _list.forEach(function(n){ if(!(_out[n] > 0)) _out[n] = 1; });
    return _out;
  };

  // ── API 2:刪除玩家異常解鎖英雄(寫 audit log,玩家本地會在下次登入時被雲端覆蓋) ──
  window._fbAdminDeleteUnlockedHero = async function(uid, heroName, opts){
    if(!uid || !heroName) throw new Error('uid 與 heroName 為必要');
    opts = opts || {};
    const _adminEmail = (_fbAuth.currentUser && _fbAuth.currentUser.email) || 'unknown';
    // ★★ v5.130.0(老師裁定甲)— 改走三槽同寫單一真相 _lxpsAdminApplyAllSlots ★★
    //   舊版只寫主檔 ⇒ saves/live 與 saves/safe 仍有這隻英雄、養成表殘留、且沒有 admin_delete 帳本
    //   ⇒ 學生下次存檔把本機帳本整包蓋回主檔後,刪除證據消失 ⇒ 三槽合併/幻影救回把英雄撿回來。
    //   舊版整段(單槽 patch 組裝)保留於下方 _LEGACY 註解備查。
    try{
      const _now130 = Date.now();
      // ★ v5.151.0 — 移除前先算跨三槽 canonical 原等級(見上方函式說明),避免三槽各自暫存出不同等級
      const _canon130 = await window._lxpsCanonicalHeroLevels(uid, [heroName]);
      const _res130 = await window._lxpsAdminApplyAllSlots(uid, function(d, slot){
        const _p = window._lxpsSlotRemoveHeroesPatch(d, [heroName], {
          at: _now130, by: _adminEmail, uid: uid, slot: slot,
          reason: opts.reason || '異常解鎖清除'
        });
        if(!_p) return null;
        // ★ v5.151.0 — 三槽一律暫存同一個 canonical 原等級(讓「🛟 一鍵無損救回」三槽還原成同一個值)
        var _stash = (d._auditRecoveredLevels && typeof d._auditRecoveredLevels === 'object') ? Object.assign({}, d._auditRecoveredLevels) : {};
        var _lv = (typeof _canon130[heroName] === 'number') ? _canon130[heroName] : 1;
        if(typeof _stash[heroName] !== 'number' || _stash[heroName] < _lv) _stash[heroName] = _lv || 1;
        _p._auditRecoveredLevels = _stash;
        return _p;
      }, {
        label: 'GM刪除單一英雄',
        lastAction: { type:'delete_hero', target:heroName, at:_now130, by:_adminEmail, reason: opts.reason || '' }
      });
      const _d130 = _res130.mainData || {};
      const _wasIn130 = Array.isArray(_d130.unlockedHeroes) ? (_d130.unlockedHeroes.indexOf(heroName) >= 0) : false;
      const _hadLv130 = !!(_d130.heroLevels && typeof _d130.heroLevels === 'object' && (heroName in _d130.heroLevels));
      // 至寶保護清單(僅供回報顯示;解除裝備已由 _lxpsSlotRemoveHeroesPatch 逐槽各自處理)
      const _prot130 = [];
      try{
        const _tre130 = (_d130.taiwanTreasureData && typeof _d130.taiwanTreasureData === 'object') ? _d130.taiwanTreasureData : {};
        Object.keys(_tre130).forEach(function(_tid){
          const _rec = _tre130[_tid];
          if(_rec && _rec.equippedTo === heroName){
            _prot130.push({ id:_tid, lv:_rec.lv || 1, invested:_rec.invested || null });
          }
        });
      }catch(_eT130){}
      console.log('[_fbAdminDeleteUnlockedHero v5.130.0] ✅ uid=' + uid + ' 已刪除 ' + heroName
        + '(wasInUnlocked=' + _wasIn130 + ', hadLv=' + _hadLv130
        + ', 保護至寶=' + _prot130.length + ' 個)');
      return { ok: true, uid, heroName, wasInUnlocked: _wasIn130, hadLv: _hadLv130,
               protectedTreasures: _prot130, slots: _res130 };
    }catch(e){
      console.error('[_fbAdminDeleteUnlockedHero] 刪除失敗', e);
      throw e;
    }
  };

  /* ── v5.130.0 前的舊版(只寫主檔)整段保留備查,勿刪 ────────────────────
  window._fbAdminDeleteUnlockedHero_LEGACY_v5129 = async function(uid, heroName, opts){
    if(!uid || !heroName) throw new Error('uid 與 heroName 為必要');
    opts = opts || {};
    const _adminEmail = (_fbAuth.currentUser && _fbAuth.currentUser.email) || 'unknown';
    try{
      const _ref = doc(_fbDb, 'players', uid);
      const _snap = await getDoc(_ref);
      if(!_snap.exists()) throw new Error('玩家不存在 uid=' + uid);
      const _d = _snap.data() || {};

      // 1) unlockedHeroes:陣列移除
      let _unlocked = Array.isArray(_d.unlockedHeroes) ? _d.unlockedHeroes.slice() : [];
      const _wasIn = _unlocked.includes(heroName);
      _unlocked = _unlocked.filter(n => n !== heroName);

      // 2) heroLevels:刪除整個 key
      const _heroLv = (_d.heroLevels && typeof _d.heroLevels === 'object') ? Object.assign({}, _d.heroLevels) : {};
      const _hadLv = (heroName in _heroLv);
      delete _heroLv[heroName];

      // 3) heroStatInvested:刪除整個 key
      const _heroStatInv = (_d.heroStatInvested && typeof _d.heroStatInvested === 'object') ? Object.assign({}, _d.heroStatInvested) : {};
      delete _heroStatInv[heroName];

      // 4) heroStatPoints:刪除整個 key
      const _heroStatPts = (_d.heroStatPoints && typeof _d.heroStatPoints === 'object') ? Object.assign({}, _d.heroStatPoints) : {};
      delete _heroStatPts[heroName];

      // 5) heroSkillLevels:刪除整個 key
      const _heroSkillLv = (_d.heroSkillLevels && typeof _d.heroSkillLevels === 'object') ? Object.assign({}, _d.heroSkillLevels) : {};
      delete _heroSkillLv[heroName];

      // 6) heroBurstLevels:刪除整個 key
      const _heroBurstLv = (_d.heroBurstLevels && typeof _d.heroBurstLevels === 'object') ? Object.assign({}, _d.heroBurstLevels) : {};
      delete _heroBurstLv[heroName];

      // ★ v3.16.6 — 6b) heroExp / heroTraitLevel 同步刪除(根治幻影復活)
      //   heroExp 全站無 _s,殘留會被 _lxpsRepairLevelExpDesync 重建 _heroLevels 等級 → 自癒網 advGetUnlockedHeroes
      //   把它 union 回 unlockedHeroes → 下次存檔回寫雲端 → 被刪英雄復活。故刪英雄時一併清掉 heroExp / 天賦殘留。
      const _heroExpClean = (_d.heroExp && typeof _d.heroExp === 'object') ? Object.assign({}, _d.heroExp) : {};
      delete _heroExpClean[heroName];
      const _heroTraitClean = (_d.heroTraitLevel && typeof _d.heroTraitLevel === 'object') ? Object.assign({}, _d.heroTraitLevel) : {};
      delete _heroTraitClean[heroName];

      // ★ v3.13.49(2026-06-05)— 至寶保護(老師需求):刪英雄前, 把「裝在這隻身上的至寶」解除裝備,
      //   但【保留至寶本體】(等級/經驗/至寶投資都不動)。避免玩家辛苦養的至寶因為英雄被刪而懸空 / 連帶消失。
      //   結構: taiwanTreasureData = { id: { lv, exp, equippedTo: '英雄名'|null, invested } }
      const _tre = (_d.taiwanTreasureData && typeof _d.taiwanTreasureData === 'object')
        ? JSON.parse(JSON.stringify(_d.taiwanTreasureData)) : {};
      const _protectedTreasures = [];
      let _treChanged = false;
      Object.keys(_tre).forEach(_tid => {
        const _rec = _tre[_tid];
        if(_rec && _rec.equippedTo === heroName){
          _protectedTreasures.push({ id: _tid, lv: _rec.lv || 1, invested: _rec.invested || null });
          _rec.equippedTo = null;   // 解除裝備, 至寶本體保留
          _treChanged = true;
        }
      });

      // 7) _heroUnlockHistory:寫 admin_delete audit 紀錄
      let _hist = Array.isArray(_d._heroUnlockHistory) ? _d._heroUnlockHistory.slice() : [];
      _hist.push({
        name: heroName,
        at: Date.now(),
        source: 'admin_delete',
        adminAction: true,
        deletedBy: _adminEmail,
        reason: opts.reason || '異常解鎖清除',
        protectedTreasures: _protectedTreasures.map(t => t.id),  // 稽核:這次保護了哪些至寶
      });
      if(_hist.length > 250) _hist = _hist.slice(-250);

      // 8) 一次性 updateDoc 寫回(走 _adminForceWrite 略過健康度守門 — 本鐵律 2.04)
      const _patch = {
        unlockedHeroes: _unlocked,
        heroLevels: _heroLv,
        heroLevels_s: JSON.stringify(_heroLv),   // ★ v3.15.96 — 同步寫字串版(否則刪英雄後載入採信舊 _s 會復活)
        heroStatInvested: _heroStatInv,
        heroStatInvested_s: JSON.stringify(_heroStatInv),   // ★ v3.16.4 — 補字串版:_applySafeData 已優先採信 _s,不寫則 GM 清除被舊髒 _s 復原
        heroStatPoints: _heroStatPts,
        heroStatPoints_s: JSON.stringify(_heroStatPts),     // ★ v3.16.4 — 為 PREFER_S 全採信鋪路(寫齊免不一致)
        heroSkillLevels: _heroSkillLv,
        heroSkillLevels_s: JSON.stringify(_heroSkillLv),    // ★ v3.16.4 — _applySafeData 已優先採信 _s,必須同步寫
        heroBurstLevels: _heroBurstLv,
        heroBurstLevels_s: JSON.stringify(_heroBurstLv),    // ★ v3.16.4 — 為 PREFER_S 全採信鋪路
        heroExp: _heroExpClean,                // ★ v3.16.6 — 清掉被刪英雄的經驗殘留
        heroExp_s: JSON.stringify(_heroExpClean||{}),       // ★ v3.16.33 — 補字串版(本版載入優先採信 _s,不寫則刪英雄後被舊髒 _s 復活)
        heroTraitLevel: _heroTraitClean,       // ★ v3.16.6 — 同步清天賦等級殘留
        heroTraitLevel_s: JSON.stringify(_heroTraitClean||{}), // ★ v3.16.33 — 補字串版(同上)
        _authoritativeRestoreAt: Date.now(),   // ★ v3.16.5 — 過時 session 偵測後鎖存+重載,不覆蓋本次刪除
        _heroUnlockHistory: _hist,
        _adminLastAction: {
          type: 'delete_hero',
          target: heroName,
          at: Date.now(),
          by: _adminEmail,
          reason: opts.reason || '',
          protectedTreasures: _protectedTreasures.map(t => t.id),
        }
      };
      // 只有真的有至寶被解除裝備才寫 taiwanTreasureData(物件 + 字串雙寫, 與存檔同模式)
      if(_treChanged){
        _patch.taiwanTreasureData = _tre;
        _patch.taiwanTreasureData_s = JSON.stringify(_tre);
      }
      await updateDoc(_ref, _patch);

      console.log('[_fbAdminDeleteUnlockedHero] ✅ uid=' + uid + ' 已刪除 ' + heroName
        + '(wasInUnlocked=' + _wasIn + ', hadLv=' + _hadLv
        + ', 保護至寶=' + _protectedTreasures.length + ' 個)');
      return { ok: true, uid, heroName, wasInUnlocked: _wasIn, hadLv: _hadLv, protectedTreasures: _protectedTreasures };
    }catch(e){
      console.error('[_fbAdminDeleteUnlockedHero] 刪除失敗', e);
      throw e;
    }
  };
  ──────────────────────────────────────────────────────────────────── */

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.13.39(2026-06-04) — 帳號汙染掃描:找出「瞬間解鎖大量 SSR」的帳號
  // ─────────────────────────────────────────────────────────────────────
  // 老師需求:共用平板把上一位學生的整批 SSR union 進下一位帳號 → 瞬間多出二十隻 SSR。
  // 偵測原理:跨帳號 union 只塞進 unlockedHeroes,不會產生 _heroUnlockHistory 解鎖事件。
  //   → 疑似汙染 = roster 內的 SSR 之中「找不到任何合法解鎖紀錄」的那些。
  //   (正常抽到/打到的 SSR 都會有 _heroUnlockHistory 紀錄;admin_delete 來源不算合法擁有)
  //   門檻 threshold:疑似 SSR ≥ N 才列出(預設 5,老師可調)。SSR 本就稀有,
  //   一個正常學生不會同時擁有一大票「沒有解鎖紀錄」的 SSR。
  // ⚠ 邊界:v3.11.10 之前解鎖的 SSR 沒有歷史紀錄 → 可能被列入「疑似」;
  //   故本工具只「列出候選 + 顯示英雄名」,實際收回需 GM 看名單確認(見 admin_panel.js)。
  // ════════════════════════════════════════════════════════════════════
  window._fbAdminScanInflatedRosters = async function(opts){
    opts = opts || {};
    const _threshold = parseInt(opts.threshold, 10) || 5;
    const _ssrSet = new Set(window.SUMMON_RARE_HEROES || []);
    if(!_ssrSet.size){
      console.warn('[_fbAdminScanInflatedRosters] window.SUMMON_RARE_HEROES 未就緒,無法判定 SSR');
      return { ok:false, reason:'no_ssr_set' };
    }
    const snap = await getDocs(collection(_fbDb, 'players'));
    const _out = [];
    snap.forEach(docSnap => {
      try{
        const d = docSnap.data() || {};
        const _unlocked = Array.isArray(d.unlockedHeroes) ? d.unlockedHeroes : [];
        if(!_unlocked.length) return;
        let _hist = Array.isArray(d._heroUnlockHistory) ? d._heroUnlockHistory : null;
        if(!_hist && typeof d._heroUnlockHistory_s === 'string'){
          try{ _hist = JSON.parse(d._heroUnlockHistory_s); }catch(_){ _hist = null; }
        }
        const _legitNames = new Set();
        if(Array.isArray(_hist)){
          _hist.forEach(h => { if(h && h.name && h.source !== 'admin_delete') _legitNames.add(h.name); });
        }
        const _ssrInRoster = _unlocked.filter(n => _ssrSet.has(n));
        const _suspected = _ssrInRoster.filter(n => !_legitNames.has(n));
        if(_suspected.length >= _threshold){
          const _hl = (d.heroLevels && typeof d.heroLevels === 'object') ? d.heroLevels : {};
          const _maxLv = Math.max(0, ...Object.values(_hl).map(v => v || 0));
          _out.push({
            uid: docSnap.id,
            name: d.displayName || '',
            email: d.email || '',
            totalUnlocked: _unlocked.length,
            ssrInRoster: _ssrInRoster.length,
            ssrWithHistory: _ssrInRoster.length - _suspected.length,
            suspectedCount: _suspected.length,
            suspected: _suspected,
            historyCount: Array.isArray(_hist) ? _hist.length : 0,
            knowledgeCoins: d.knowledgeCoins || 0,
            maxHeroLv: _maxLv,
          });
        }
      }catch(_e){ /* 單筆失敗略過 */ }
    });
    _out.sort((a,b) => b.suspectedCount - a.suspectedCount);
    console.log('[_fbAdminScanInflatedRosters] 掃描完成,門檻 ' + _threshold + ' → 命中 ' + _out.length + ' 個帳號');
    return { ok:true, threshold:_threshold, count:_out.length, players:_out };
  };

  // ── 批次收回多隻英雄(汙染清除用)──
  //   清 unlockedHeroes + 5 大養成表(對齊 _fbAdminDeleteUnlockedHero 的欄位,只是一次清多隻),
  //   寫 admin_delete 稽核,並寫 _adminRescueSignal(反污染:學生下次開遊戲會自動清本地殘留 +
  //   以雲端為準,配合 v3.13.39 union 熔斷,確保本地那批 SSR 不會再被併回來)。
  //   走 updateDoc 直接寫主文件(繞過 _fbSaveLive 健康度守門,因為這是管理員授權的縮減)。
  window._fbAdminBulkRemoveHeroes = async function(uid, heroNames, opts){
    if(!uid) throw new Error('必須提供 uid');
    const _list = Array.isArray(heroNames) ? heroNames.filter(Boolean) : [];
    if(!_list.length) return { ok:false, reason:'no_heroes' };
    opts = opts || {};
    const _removeSet = new Set(_list);
    const _adminEmail = (window._fbUser && window._fbUser.email) || 'admin';
    // ★★ v5.130.0(老師裁定甲)— 改走三槽同寫單一真相 ★★
    //   舊版只寫主檔:UR 稽核那顆「🗑 刪除勾選的 UR」用的就是本函式,
    //   live/safe 兩槽完全沒動 ⇒ 學生下次登入被合併/幻影救回撿回來 ⇒ 老師實測的「刪了又回來」。
    //   ★ 同時修好舊版少清一張養成表的缺口:舊版只清 7 張(漏 heroCapsuleInvested),
    //     而 _lxpsHeroFloorOntoMain 的 effort 判定會讀膠囊投資 ⇒ 漏清那張也足以讓英雄復活。
    try{
      const _now = Date.now();
      // ★ v5.151.0 — 移除前先算跨三槽 canonical 原等級,避免三槽各自暫存出不同等級
      //   (老師實錘「6213 魔劍姬 44 級救回變 26 級」根因:舊版逐槽只看自己那槽的 heroLevels)
      const _canon = await window._lxpsCanonicalHeroLevels(uid, _list);
      const _res130 = await window._lxpsAdminApplyAllSlots(uid, function(d, slot){
        const _p = window._lxpsSlotRemoveHeroesPatch(d, _list, {
          at: _now, by: _adminEmail, uid: uid, slot: slot,
          reason: opts.reason || '帳號汙染暴增英雄收回'
        });
        if(!_p) return null;
        // ★ v5.151.0 — 三槽一律暫存同一個 canonical 原等級到 _auditRecoveredLevels
        //   (v3.17.2「🛟 一鍵無損救回」讀存檔那條救星,三槽現在還原出同一個值)
        var _stash = (d._auditRecoveredLevels && typeof d._auditRecoveredLevels === 'object') ? Object.assign({}, d._auditRecoveredLevels) : {};
        _list.forEach(function(n){
          var _lv = (typeof _canon[n] === 'number') ? _canon[n] : 1;
          if(typeof _stash[n] !== 'number' || _stash[n] < _lv) _stash[n] = _lv || 1;
        });
        _p._auditRecoveredLevels = _stash;
        return _p;
      }, {
        label: 'GM批次收回英雄',
        rescueSignal: { ts:_now, by:_adminEmail, fromSlot:'admin_scrub', version:1 },
        lastAction: { type:'bulk_remove_polluted', count:_list.length, at:_now, by:_adminEmail, reason: opts.reason || '' }
      });
      const _d = _res130.mainData || {};
      const _unlocked = (Array.isArray(_d.unlockedHeroes) ? _d.unlockedHeroes : []).filter(n => !_removeSet.has(n));
      // 保護至寶清單(僅供回報顯示;解除裝備已由 patch builder 逐槽各自處理)
      const _protectedTreasures = [];
      try{
        const _treV = (_d.taiwanTreasureData && typeof _d.taiwanTreasureData === 'object') ? _d.taiwanTreasureData : {};
        Object.keys(_treV).forEach(function(_tid){
          const _rec = _treV[_tid];
          if(_rec && _rec.equippedTo && _removeSet.has(_rec.equippedTo)) _protectedTreasures.push(_tid);
        });
      }catch(_eTv){}
      console.log('[_fbAdminBulkRemoveHeroes v5.130.0] ✅ uid=' + uid + ' 收回 ' + _list.length
        + ' 隻(保護至寶 ' + _protectedTreasures.length + ' 個):', _list);
      // ★ v3.16.7 — 學生自助審查「不是我的」回收的投資補償金額(僅 opts.compensate 時回傳,呼叫端用 _fbCompensatePlayer 正規發放)
      var _auditCompCoins = 0;
      if(opts && opts.compensate){
        _list.forEach(function(_hn){
          var _lv = (_d.heroLevels && typeof _d.heroLevels[_hn] === 'number') ? _d.heroLevels[_hn] : 1;
          _auditCompCoins += Math.min(30000, Math.max(800, (Number(_lv) || 1) * 600));
        });
      }
      return { ok:true, uid, removed:_list.length, remaining:_unlocked.length, heroes:_list,
               protectedTreasures:_protectedTreasures, compensateCoins:_auditCompCoins, slots:_res130 };
    }catch(e){
      console.error('[_fbAdminBulkRemoveHeroes] 失敗', e);
      return { ok:false, reason:(e && (e.code||e.message)) || 'exception' };
    }
  };

  /* ── v5.130.0 前的舊版(只寫主檔)整段保留備查,勿刪 ────────────────────
  window._fbAdminBulkRemoveHeroes_LEGACY_v5129 = async function(uid, heroNames, opts){
    const _list = Array.isArray(heroNames) ? heroNames.filter(Boolean) : [];
    const _removeSet = new Set(_list);
    const _adminEmail = (window._fbUser && window._fbUser.email) || 'admin';
    const _ref = doc(_fbDb, 'players', uid);
    try{
      const _snap = await getDoc(_ref);
      if(!_snap.exists()) return { ok:false, reason:'player_not_found' };
      const _d = _snap.data() || {};
      const _unlocked = (Array.isArray(_d.unlockedHeroes) ? _d.unlockedHeroes : []).filter(n => !_removeSet.has(n));
      const _clean = (obj) => {
        const o = (obj && typeof obj === 'object') ? Object.assign({}, obj) : {};
        _list.forEach(n => { delete o[n]; });
        return o;
      };
      let _hist = Array.isArray(_d._heroUnlockHistory) ? _d._heroUnlockHistory.slice() : [];
      const _now = Date.now();
      // ★ v3.13.49 — 至寶保護(批次版):把「裝在被收回英雄身上的至寶」解除裝備但保留本體
      const _tre = (_d.taiwanTreasureData && typeof _d.taiwanTreasureData === 'object')
        ? JSON.parse(JSON.stringify(_d.taiwanTreasureData)) : {};
      const _protectedTreasures = [];
      let _treChanged = false;
      Object.keys(_tre).forEach(_tid => {
        const _rec = _tre[_tid];
        if(_rec && _rec.equippedTo && _removeSet.has(_rec.equippedTo)){
          _protectedTreasures.push(_tid);
          _rec.equippedTo = null;
          _treChanged = true;
        }
      });
      _list.forEach(n => _hist.push({
        name: n, at: _now, source: 'admin_delete', adminAction: true,
        deletedBy: _adminEmail, reason: opts.reason || '帳號汙染暴增英雄收回'
      }));
      if(_hist.length > 300) _hist = _hist.slice(-300);
      const _patch = {
        unlockedHeroes:    _unlocked,
        heroLevels:        _clean(_d.heroLevels),
        heroLevels_s:      JSON.stringify(_clean(_d.heroLevels)),   // ★ v3.15.96 — 同步寫字串版(否則收回污染英雄後載入採信舊 _s 會復活)
        heroStatInvested:  _clean(_d.heroStatInvested),
        heroStatInvested_s: JSON.stringify(_clean(_d.heroStatInvested)),   // ★ v3.16.4 — 補字串版(_applySafeData 已優先採信 _s,不寫則收回被舊髒 _s 復原)
        heroStatPoints:    _clean(_d.heroStatPoints),
        heroStatPoints_s:  JSON.stringify(_clean(_d.heroStatPoints)),      // ★ v3.16.4
        heroSkillLevels:   _clean(_d.heroSkillLevels),
        heroSkillLevels_s: JSON.stringify(_clean(_d.heroSkillLevels)),     // ★ v3.16.4 — _applySafeData 已優先採信 _s,必須同步寫
        heroBurstLevels:   _clean(_d.heroBurstLevels),
        heroBurstLevels_s: JSON.stringify(_clean(_d.heroBurstLevels)),     // ★ v3.16.4
        heroExp:           _clean(_d.heroExp),          // ★ v3.16.6 — 清經驗殘留 → 根治被收回英雄循 desync+自癒復活
        heroExp_s:         JSON.stringify(_clean(_d.heroExp)),        // ★ v3.16.33 — 補字串版(本版載入優先採信 _s,不寫則收回後被舊髒 _s 復活)
        heroTraitLevel:    _clean(_d.heroTraitLevel),   // ★ v3.16.6 — 同步清天賦等級殘留
        heroTraitLevel_s:  JSON.stringify(_clean(_d.heroTraitLevel)), // ★ v3.16.33 — 補字串版(同上)
        _authoritativeRestoreAt: Date.now(),   // ★ v3.16.5 — 過時 session 偵測後鎖存+重載,不覆蓋本次收回
        _heroUnlockHistory: _hist,
        _adminRescueSignal: { ts: _now, by: _adminEmail, fromSlot: 'admin_scrub', version: 1 },
        _adminForceRestore: true,
        _adminLastAction: { type: 'bulk_remove_polluted', count: _list.length, at: _now, by: _adminEmail, reason: opts.reason || '', protectedTreasures: _protectedTreasures }
      };
      if(_treChanged){
        _patch.taiwanTreasureData = _tre;
        _patch.taiwanTreasureData_s = JSON.stringify(_tre);
      }
      await updateDoc(_ref, _patch);
      console.log('[_fbAdminBulkRemoveHeroes] ✅ uid=' + uid + ' 收回 ' + _list.length + ' 隻(保護至寶 ' + _protectedTreasures.length + ' 個):', _list);
      // ★ v3.16.7 — 計算學生自助審查「不是我的」回收的投資補償金額(僅 opts.compensate 時回傳,由呼叫端用 _fbCompensatePlayer 正規發放;類a自動移除/掃描收回不補)
      var _auditCompCoins = 0;
      if(opts && opts.compensate){
        _list.forEach(function(_hn){
          var _lv = (_d.heroLevels && typeof _d.heroLevels[_hn] === 'number') ? _d.heroLevels[_hn] : 1;
          _auditCompCoins += Math.min(30000, Math.max(800, (Number(_lv) || 1) * 600));
        });
      }
      return { ok:true, uid, removed:_list.length, remaining:_unlocked.length, heroes:_list, protectedTreasures:_protectedTreasures, compensateCoins:_auditCompCoins };
    }catch(e){
      console.error('[_fbAdminBulkRemoveHeroes] 失敗', e);
      return { ok:false, reason:(e && (e.code||e.message)) || 'exception' };
    }
  };
  ──────────────────────────────────────────────────────────────────── */

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.16.19(2026-06-25)— 「系統審核誤判英雄·投資證據版回收」雲端寫入(學生自助·一次性)
  //   背景:舊 migration_seal 稽核 bug 把「借來練過、沒投資、查無紀錄」的角色洗白成合法擁有,
  //         幾乎全部 SSR 對幾乎全部玩家解鎖。本函式把這類「無投資證據」角色回收上鎖。
  //   與 _fbAdminBulkRemoveHeroes 的關鍵差異:
  //     ① 帳本來源標 'audit_error_recovered'(可逆·與 admin_delete 區分,非永久刪除);
  //     ② 不寫 GM 跨玩家信號(_adminForceRestore / _adminRescueSignal / _adminLastAction);
  //     ③ 寫 _auditRecoverDoneV1=true(雲端一次性旗標,防重跑);
  //     ④ 沿用 _authoritativeRestoreAt:寫入後本 session 由既有 piece3 機制乾淨重載入(避免記憶體手術),
  //        同時保護「同帳號另一過時 session」不覆蓋本次回收。
  //   安全:直接 updateDoc(繞存檔倒退守門,與 GM 收回同路徑)+ 清齊六養成表 _s + heroExp + heroTraitLevel
  //         (杜絕載入採信舊 _s / desync 自癒 / phantom lv>1 把回收角色復活)。
  //   lockNames 由呼叫端用「記憶體 _advHasGenuineUnlock(證據集 ⊇ 雲端)」判定;此處只把「確實在雲端
  //   unlockedHeroes 內」的 lockNames 移除(雲端沒有的 name 一律略過 → 不會誤動)。
  // ════════════════════════════════════════════════════════════════════
  window._fbApplyAuditErrorRecover = async function(uid, lockNames){
    if(!uid) return { ok:false, reason:'no_uid' };
    const _names = Array.isArray(lockNames) ? lockNames.filter(Boolean) : [];
    const _ref = doc(_fbDb, 'players', uid);
    try{
      const _snap = await getDoc(_ref);
      if(!_snap.exists()) return { ok:false, reason:'player_not_found' };
      const _d = _snap.data() || {};
      // 雲端一次性旗標:已跑過 → 不重複(防 localStorage 被清後重跑/重載迴圈)
      if(_d._auditRecoverDoneV1) return { ok:true, alreadyDone:true, recovered:[] };

      const _cloudUnlocked = Array.isArray(_d.unlockedHeroes) ? _d.unlockedHeroes : [];
      const _cloudSet = new Set(_cloudUnlocked);
      // 只回收「確實在雲端清單內」的 lockNames(雲端沒有的略過)
      const _toRemove = _names.filter(function(n){ return _cloudSet.has(n); });

      if(_toRemove.length === 0){
        // 沒有可回收的 → 只寫一次性旗標(不蓋 _authoritativeRestoreAt → 不觸發重載)
        try{ await updateDoc(_ref, { _auditRecoverDoneV1: true, _auditRecoverAt: Date.now(), _auditRecoverCount: 0 }); }catch(_eFlag){ return { ok:false, reason:'flag_write_failed' }; }
        return { ok:true, recovered:[], protectedTreasures:[] };
      }

      // ★★ v5.130.0(老師裁定甲)— 改走三槽同寫單一真相 ★★
      //   舊版只寫主檔 ⇒ live/safe 殘留讓被回收英雄反覆復活(與 GM 刪除同病灶)。
      //   一次性旗標/計數只寫主檔;帳本與養成清除三槽各自處理;暫存原等級每槽各存(取較大)。
      const _now = Date.now();
      const _removeSet = new Set(_toRemove);
      const _res130 = await window._lxpsAdminApplyAllSlots(uid, function(d, slot){
        const _p = window._lxpsSlotRemoveHeroesPatch(d, _toRemove, {
          at:_now, by:'system_audit', uid:uid, slot:slot, adminAction:false,
          source:'audit_error_recovered',
          reason:'系統審核誤判·無投資證據回收(可由帳號救援申請補回)'
        });
        if(!_p) return null;
        var _stash = (d._auditRecoveredLevels && typeof d._auditRecoveredLevels === 'object')
          ? Object.assign({}, d._auditRecoveredLevels) : {};
        var _lvSrc = (d.heroLevels && typeof d.heroLevels === 'object') ? d.heroLevels : {};
        _toRemove.forEach(function(n){
          var _lv = (typeof _lvSrc[n] === 'number') ? Number(_lvSrc[n]) : 1;
          if(typeof _stash[n] !== 'number' || _stash[n] < _lv) _stash[n] = _lv || 1;
        });
        _p._auditRecoveredLevels = _stash;
        if(slot === 'main'){
          _p._auditRecoverDoneV1 = true;
          _p._auditRecoverAt = _now;
          _p._auditRecoverCount = _toRemove.length;
        }
        return _p;
      }, { label:'系統審核誤判回收' });
      const _protectedTreasures = [];
      try{
        const _dM = _res130.mainData || {};
        const _treV = (_dM.taiwanTreasureData && typeof _dM.taiwanTreasureData === 'object') ? _dM.taiwanTreasureData : {};
        Object.keys(_treV).forEach(function(_tid){
          const _rec = _treV[_tid];
          if(_rec && _rec.equippedTo && _removeSet.has(_rec.equippedTo)) _protectedTreasures.push(_tid);
        });
      }catch(_eTv){}
      console.log('[_fbApplyAuditErrorRecover v5.130.0] ✅ uid=' + uid + ' 回收 ' + _toRemove.length + ' 隻(保護至寶 ' + _protectedTreasures.length + ' 個):', _toRemove);
      return { ok:true, recovered:_toRemove, protectedTreasures:_protectedTreasures, slots:_res130 };
    }catch(e){
      console.error('[_fbApplyAuditErrorRecover] 失敗', e);
      return { ok:false, reason:(e && (e.code||e.message)) || 'exception' };
    }
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.16.99(2026-06-30·老師裁示)— 玩家端「登入自動回收 GM 錯誤補發批次」(階段1)
  // ────────────────────────────────────────────────────────────────────
  //   老師指定「錯誤補發日期」(如 6/25 那次過度救援)→ 玩家登入時自動找出自己帳號裡
  //   「該日期被 GM compensation 補進、但查無自己真實解鎖紀錄」的英雄 → 彈清單告知 → 回收。
  //   ★ 判定完全沿用 GM 工具 _computeCompBatchesFromDoc(精準·已單元測試):
  //     有自己 compensation 紀錄 且 無真實解鎖紀錄(summon/ticket/maokong/admin_grant/initial/
  //     player_confirmed/空字串=真實·排除 legacy_grandfather 洗白)→ 才回收;學生自己抽過/解鎖過絕不誤刪。
  //   ★ 只回收「白名單日期」→ 老師正常補發/其他日期的 compensation 完全不動(防誤刪正常補償)。
  //   ★ 總開關 _GMCR_AUTO_RECLAIM_ENABLED(出問題可即時設 false 全域停);白名單空→不跑。
  //   ★ 可逆:標 audit_error_recovered(GM「🛟 一鍵永久復原」/救援卡 _fbAdminRestoreLostHeroes 可還原)。
  //   ★ 一次性:綁「白名單內容版本」_gmcrAutoReclaimVer——老師日後在白名單加新錯誤日期→自動再跑一次補收。
  // ════════════════════════════════════════════════════════════════════
  // 老師指定的「GM 錯誤補發日期」白名單(台灣時間 YYYY-MM-DD);日後發現其他錯誤批次在此加日期即可。
  // ⚠ 只放「確定是錯誤過度救援」的日期;老師正常補發/補償的日期「絕對不要」放進來(會誤刪正常英雄)。
  window._GMCR_BAD_COMP_DATES = ['2026-06-25'];
  window._GMCR_AUTO_RECLAIM_ENABLED = false;  // ★ v3.17.3 — 永久停用「玩家端登入自動回收 compensation 英雄」:帳本不全會誤收正當英雄·已從源頭根治(發放改寫 admin_grant)·與 GM「補償批次回收」UI 一併下架

  // 玩家端回收引擎:對傳入英雄無條件回收(僅限雲端確實擁有者),標可逆 audit_error_recovered,
  //   用「獨立旗標 _gmcrAutoReclaimVer」(不碰 v3.16.19 的 _auditRecoverDoneV1),至寶解裝保留本體,
  //   清養成全 _s,暫存原等級供無損還原,蓋 _authoritativeRestoreAt 乾淨重載(沿用 piece3)。
  window._fbApplyBadCompReclaim = async function(uid, names, verTag){
    if(!uid) return { ok:false, reason:'no_uid' };
    var _names = Array.isArray(names) ? names.filter(Boolean) : [];
    var _ref = doc(_fbDb, 'players', uid);
    try{
      var _snap = await getDocFromServer(_ref);
      if(!_snap || !_snap.exists()) return { ok:false, reason:'player_not_found' };
      var _d = _snap.data() || {};
      var _cloudUnlocked = Array.isArray(_d.unlockedHeroes) ? _d.unlockedHeroes : [];
      var _cloudSet = {}; _cloudUnlocked.forEach(function(n){ _cloudSet[n] = 1; });
      var _toRemove = _names.filter(function(n){ return _cloudSet[n]; });
      if(_toRemove.length === 0){
        // 沒有可回收的 → 只寫版本旗標(不蓋 _authoritativeRestoreAt → 不觸發重載)
        try{ await updateDoc(_ref, { _gmcrAutoReclaimVer: String(verTag||''), _gmcrAutoReclaimAt: Date.now(), _gmcrAutoReclaimCount: 0 }); }catch(_eFlag){ return { ok:false, reason:'flag_write_failed' }; }
        return { ok:true, recovered:[], protectedTreasures:[] };
      }
      var _removeSet = {}; _toRemove.forEach(function(n){ _removeSet[n] = 1; });
      // ★★ v5.130.0(老師裁定甲)— 改走三槽同寫單一真相(同 _fbApplyAuditErrorRecover 病灶;
      //   本路徑雖已被 _GMCR_AUTO_RECLAIM_ENABLED=false 永久停用,仍一併修齊以免日後誤啟用時復發)★★
      var _now = Date.now();
      var _res130 = await window._lxpsAdminApplyAllSlots(uid, function(d, slot){
        var _p = window._lxpsSlotRemoveHeroesPatch(d, _toRemove, {
          at:_now, by:'system_gmcr', uid:uid, slot:slot, adminAction:false,
          source:'audit_error_recovered',
          reason:'GM 補償批次回收(登入自動·無真實解鎖紀錄·可由帳號救援補回)'
        });
        if(!_p) return null;
        var _stash = (d._auditRecoveredLevels && typeof d._auditRecoveredLevels === 'object') ? Object.assign({}, d._auditRecoveredLevels) : {};
        var _lvSrc = (d.heroLevels && typeof d.heroLevels === 'object') ? d.heroLevels : {};
        _toRemove.forEach(function(n){
          var _lv = (typeof _lvSrc[n] === 'number') ? Number(_lvSrc[n]) : 1;
          if(typeof _stash[n] !== 'number' || _stash[n] < _lv) _stash[n] = _lv || 1;
        });
        _p._auditRecoveredLevels = _stash;
        if(slot === 'main'){
          _p._gmcrAutoReclaimVer = String(verTag||'');
          _p._gmcrAutoReclaimAt = _now;
          _p._gmcrAutoReclaimCount = _toRemove.length;
        }
        return _p;
      }, { label:'補償批次自動回收' });
      var _protectedTreasures = [];
      try{
        var _dM = _res130.mainData || {};
        var _treV = (_dM.taiwanTreasureData && typeof _dM.taiwanTreasureData === 'object') ? _dM.taiwanTreasureData : {};
        Object.keys(_treV).forEach(function(_tid){ var _rec = _treV[_tid]; if(_rec && _rec.equippedTo && _removeSet[_rec.equippedTo]) _protectedTreasures.push(_tid); });
      }catch(_eTv){}
      console.log('[_fbApplyBadCompReclaim v5.130.0] ✅ uid=' + String(uid).slice(0,8) + ' 自動回收 ' + _toRemove.length + ' 隻(保護至寶 ' + _protectedTreasures.length + '):', _toRemove);
      return { ok:true, recovered:_toRemove, protectedTreasures:_protectedTreasures, slots:_res130 };
    }catch(e){
      console.error('[_fbApplyBadCompReclaim] 失敗', e);
      return { ok:false, reason:(e && (e.code||e.message)) || 'exception' };
    }
  };

  // 讀自己雲端 doc → 回傳「白名單日期內、無真實解鎖紀錄的 compensation 英雄」清單(供登入告知 UI)。
  //   已對此白名單版本跑過(_gmcrAutoReclaimVer 相符)→ alreadyDone(不再彈)。讀取失敗 → ok:false(下次再試·只漏不誤刪)。
  window._fbScanMyBadCompHeroes = async function(uid, dates, verTag){
    if(!uid) return { ok:false, reason:'no_uid' };
    var _dates = Array.isArray(dates) ? dates.filter(Boolean) : [];
    if(!_dates.length) return { ok:true, alreadyDone:false, list:[] };
    try{
      var _snap = await getDocFromServer(doc(_fbDb, 'players', uid));
      if(!_snap || !_snap.exists()) return { ok:false, reason:'player_not_found' };
      var _d = _snap.data() || {};
      if(String(_d._gmcrAutoReclaimVer || '') === String(verTag||'')) return { ok:true, alreadyDone:true, list:[] };
      var _r = (typeof _computeCompBatchesFromDoc === 'function') ? _computeCompBatchesFromDoc(_d, uid) : { byDate:{} };
      var _byDate = (_r && _r.byDate) || {};
      var _hitNames = {}, _list = [];
      _dates.forEach(function(dk){
        var _l = _byDate[dk] || [];
        _l.forEach(function(x){ if(x && x.name && !_hitNames[x.name]){ _hitNames[x.name] = 1; _list.push({ name:x.name, lv:x.lv, date:dk }); } });
      });
      return { ok:true, alreadyDone:false, list:_list };
    }catch(e){
      return { ok:false, reason:(e && (e.code||e.message)) || 'exception' };
    }
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.16.19(2026-06-25)— GM「一鍵永久復原」被系統審核誤判回收的英雄(救援申請閉環)
  //   情境:學生在「📨 帳號救援申請」勾「🔓 我遺失的英雄要回來」(claims.lostHeroes)送審;
  //         GM 後台救援卡點「🛟 一鍵永久復原」→ 呼叫本函式把那些英雄永久還給學生。
  //   永久關鍵:帳本補一筆 source='admin_grant'(合法來源·新 timestamp → 最新一筆非 audit_error_recovered)
  //     → advGetUnlockedHeroes 出口過濾不再隱藏、_advHasGenuineUnlock 判為合法擁有、各補回路徑保留
  //     → 即使日後再跑審查/重建也不會再被回收(admin_grant 是公認合法來源)。附 auditRestored 標記供稽核。
  //   無損還原:從 _auditRecoveredLevels 取回收當時暫存的原等級(沒有則 Lv1),補進 heroLevels(+_s)。
  //   安全:直接 updateDoc(GM 路徑·與收回同款)+ 蓋 _authoritativeRestoreAt(學生下次登入由 piece3 乾淨套用)。
  //   只增不減:只把指定英雄加回 + 補等級,不動其他英雄/養成/資源。
  // ════════════════════════════════════════════════════════════════════
  window._fbAdminRestoreLostHeroes = async function(uid, heroNames){
    if(!uid) return { ok:false, reason:'no_uid' };
    const _names = Array.isArray(heroNames) ? heroNames.filter(Boolean) : [];
    if(!_names.length) return { ok:false, reason:'no_heroes' };
    const _adminEmail = (window._fbUser && window._fbUser.email) || 'admin';
    // ★★ v5.130.0(老師裁定甲)— 改走三槽同寫單一真相 ★★
    //   舊版只寫主檔 ⇒ 這正是老師回報「補發了學生還是沒拿到」的那條鏈:
    //   admin_grant 帳本只落在主檔,主檔帳本被學生本機存檔整包蓋掉之後,
    //   live/safe 裡**較舊的 admin_delete / audit_error_recovered 反而變成「最近一筆」**
    //   ⇒ 三槽合併的 latest-wins 把剛補發的角色又濾掉一次,而且會反覆發生。
    try{
      const _now = Date.now();
      var _restored = [];
      // ★ v5.151.0 — 還原前先算跨三槽 canonical 暫存等級(舊帳號可能是 v5.151.0 之前留下的、
      //   三槽本就各存不同暫存值的殘留 ⇒ 一律採三槽最大值,不再逐槽各自還原成不同等級)
      const _canonR = await window._lxpsCanonicalHeroLevels(uid, _names);
      const _res130 = await window._lxpsAdminApplyAllSlots(uid, function(d, slot){
        // 等級來源:三槽 canonical 暫存(涵蓋該槽自己的暫存與現值)→ Lv1 保底,由 patch builder 夾
        var _stash = (d._auditRecoveredLevels && typeof d._auditRecoveredLevels === 'object')
          ? Object.assign({}, d._auditRecoveredLevels) : {};
        var _want = {};
        _names.forEach(function(n){
          _want[n] = (typeof _canonR[n] === 'number' && _canonR[n] >= 1) ? _canonR[n] : 1;
          delete _stash[n];   // 消耗暫存
        });
        var _p = window._lxpsSlotGrantHeroesPatch(d, _names, {
          at:_now, by:_adminEmail, uid:uid, slot:slot, levels:_want,
          source:'admin_grant',
          extra:{ auditRestored:true, restoredBy:_adminEmail },
          reason:'帳號救援·永久復原系統誤判回收的英雄'
        });
        if(!_p) return null;
        _p._auditRecoveredLevels = _stash;
        if(slot === 'main') _restored = _names.slice();
        return _p;
      }, { label:'GM復原漏失英雄' });
      console.log('[_fbAdminRestoreLostHeroes v5.130.0] ✅ uid=' + uid + ' 永久復原 ' + _restored.length + ' 隻:', _restored);
      return { ok:true, uid, restored:_restored, slots:_res130 };
    }catch(e){
      console.error('[_fbAdminRestoreLostHeroes] 失敗', e);
      return { ok:false, reason:(e && (e.code||e.message)) || 'exception' };
    }
  };

  /* ── v5.130.0 前的舊版(只寫主檔)整段保留備查,勿刪 ────────────────────
  window._fbAdminRestoreLostHeroes_LEGACY_v5129 = async function(uid, heroNames){
    const _names = Array.isArray(heroNames) ? heroNames.filter(Boolean) : [];
    const _adminEmail = (window._fbUser && window._fbUser.email) || 'admin';
    const _ref = doc(_fbDb, 'players', uid);
    try{
      const _snap = await getDoc(_ref);
      if(!_snap.exists()) return { ok:false, reason:'player_not_found' };
      const _d = _snap.data() || {};
      const _uid12 = String(uid).slice(0, 12);
      const _now = Date.now();

      const _unlocked = Array.isArray(_d.unlockedHeroes) ? _d.unlockedHeroes.slice() : [];
      const _uSet = new Set(_unlocked);
      const _levels = (_d.heroLevels && typeof _d.heroLevels === 'object') ? Object.assign({}, _d.heroLevels) : {};
      const _stash = (_d._auditRecoveredLevels && typeof _d._auditRecoveredLevels === 'object') ? Object.assign({}, _d._auditRecoveredLevels) : {};
      let _hist = Array.isArray(_d._heroUnlockHistory) ? _d._heroUnlockHistory.slice() : [];

      const _restored = [];
      _names.forEach(function(n){
        if(!n) return;
        // 加回解鎖清單
        if(!_uSet.has(n)){ _unlocked.push(n); _uSet.add(n); }
        // 還原等級(暫存的原等級,沒有則保留現值或 Lv1)
        var _lv = (typeof _stash[n] === 'number' && _stash[n] >= 1) ? _stash[n]
                : ((typeof _levels[n] === 'number' && _levels[n] >= 1) ? _levels[n] : 1);
        _levels[n] = _lv;
        delete _stash[n];   // 消耗暫存
        // 帳本補合法紀錄(admin_grant + auditRestored 標記)→ 最新一筆非 audit_error_recovered → 永久保留
        _hist.push({ name: n, at: _now, source: 'admin_grant', uid: _uid12, auditRestored: true, restoredBy: _adminEmail, reason: '帳號救援·永久復原系統誤判回收的英雄' });
        _restored.push(n);
      });
      if(_hist.length > 300) _hist = _hist.slice(-300);

      const _patch = {
        unlockedHeroes:        _unlocked,
        heroLevels:            _levels,
        heroLevels_s:          JSON.stringify(_levels),
        _auditRecoveredLevels: _stash,
        _heroUnlockHistory:    _hist,
        _authoritativeRestoreAt: _now
      };
      await updateDoc(_ref, _patch);
      console.log('[_fbAdminRestoreLostHeroes] ✅ uid=' + uid + ' 永久復原 ' + _restored.length + ' 隻:', _restored);
      return { ok:true, uid, restored:_restored };
    }catch(e){
      console.error('[_fbAdminRestoreLostHeroes] 失敗', e);
      return { ok:false, reason:(e && (e.code||e.message)) || 'exception' };
    }
  };
  ──────────────────────────────────────────────────────────────────── */

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.16.26(2026-06-26·老師裁示「從頭開始·簡單版」)— 自動還原「被 v3.16.19 誤回收的練過英雄」
  //   背景:v3.16.19「投資證據版」把練過卻沒投資的角色當污染回收(_fbApplyAuditErrorRecover),
  //         並把原等級暫存到 _auditRecoveredLevels。新政策「練過一律保留」→ 須把這些練過的全部補回。
  //   只還原「練過(暫存等級>1)」:沒練過(Lv1)的暫存維持回收狀態(那才是要清掉的純污染)。
  //   永久關鍵:帳本補 source='audit_auto_restored'(合法·新 ts → 最新非 audit_error_recovered)
  //     → advGetUnlockedHeroes 出口過濾不再隱藏、_advHasGenuineUnlock 判合法擁有(且 lv>1 本就保留)。
  //   無損還原:從 _auditRecoveredLevels 取原等級補回 heroLevels(+_s),只升不降。
  //   ⚠ 刻意不寫 _authoritativeRestoreAt(不觸發 piece3 重載)→ 由呼叫端做記憶體/本機同步即時顯示。
  //   一次性:雲端旗標 _auditLeveledRestoreV1 防重跑。只增不減,不動其他英雄/養成/資源。
  //   ⚠ 既定損失(v3.16.19 已造成·無法挽回):回收當下已清掉這些英雄的素質投資/技能/天賦/爆發,
  //     且當時只暫存「等級」。本還原把「英雄本體 + 等級(含等級換算的素質點)」補回,
  //     但技能/天賦/爆發的「升級」回不來,需重練。
  // ════════════════════════════════════════════════════════════════════
  window._fbRestoreLeveledAuditRecovered = async function(uid){
    if(!uid) return { ok:false, reason:'no_uid' };
    const _ref = doc(_fbDb, 'players', uid);
    try{
      const _snap = await getDoc(_ref);
      if(!_snap.exists()) return { ok:false, reason:'player_not_found' };
      const _d = _snap.data() || {};
      if(_d._auditLeveledRestoreV1) return { ok:true, alreadyDone:true, restored:[], levels:{} };
      const _stash = (_d._auditRecoveredLevels && typeof _d._auditRecoveredLevels === 'object') ? _d._auditRecoveredLevels : {};
      // 只還原練過的(暫存等級>1);Lv1 暫存維持回收(純污染)
      const _toRestore = Object.keys(_stash).filter(function(n){ return n && (Number(_stash[n]) || 0) > 1; });
      const _now = Date.now();
      if(!_toRestore.length){
        try{ await updateDoc(_ref, { _auditLeveledRestoreV1: true, _auditLeveledRestoreAt: _now, _auditLeveledRestoreCount: 0 }); }catch(_eF){}
        return { ok:true, restored:[], levels:{} };
      }
      const _uid12 = String(uid).slice(0, 12);
      // ★★ v5.130.0(老師裁定甲)— 改走三槽同寫單一真相 ★★
      //   舊版只寫主檔 ⇒ admin_grant/audit_auto_restored 帳本被學生本機存檔蓋掉後,
      //   live/safe 較舊的回收帳本反而變最近一筆 ⇒ 還原的英雄又被 latest-wins 濾掉(還原了等於沒還)。
      //   一次性旗標只寫主檔;等級來源=各槽自己的暫存(該槽沒暫存則採主檔算出的目標等級保底)。
      var _restoredLv = {};
      var _restored = [];
      const _res130 = await window._lxpsAdminApplyAllSlots(uid, function(d, slot){
        var _stashS = (d._auditRecoveredLevels && typeof d._auditRecoveredLevels === 'object')
          ? Object.assign({}, d._auditRecoveredLevels) : {};
        var _cur = (d.heroLevels && typeof d.heroLevels === 'object') ? d.heroLevels : {};
        var _want = {};
        _toRestore.forEach(function(n){
          var _lvStash = Number(_stashS[n]) || Number(_stash[n]) || 1;   // 該槽暫存→主檔暫存
          var _lvCur = (typeof _cur[n] === 'number') ? _cur[n] : 0;
          _want[n] = Math.max(_lvCur, _lvStash, 1);   // 只升不降
        });
        var _p = window._lxpsSlotGrantHeroesPatch(d, _toRestore, {
          at:_now, by:'system_audit', uid:uid, slot:slot, levels:_want,
          source:'audit_auto_restored',
          extra:{ auditRestored:true },
          reason:'練過的英雄一律保留(老師新政策)·自動還原'
        });
        if(!_p) return null;
        if(slot === 'main'){
          _p._auditLeveledRestoreV1 = true;
          _p._auditLeveledRestoreAt = _now;
          _p._auditLeveledRestoreCount = _toRestore.length;
          _restored = _toRestore.slice();
          _toRestore.forEach(function(n){ _restoredLv[n] = _want[n]; });
        }
        return _p;
      }, { label:'練過英雄自動還原', stampRestore:false });   // ⚠ 沿用原設計:不蓋 restore → 不觸發重載迴圈
      console.log('[_fbRestoreLeveledAuditRecovered v5.130.0] ✅ uid=' + uid + ' 自動還原練過英雄 ' + _restored.length + ' 隻:', _restored);
      return { ok:true, restored:_restored, levels:_restoredLv, slots:_res130 };
    }catch(e){
      console.error('[_fbRestoreLeveledAuditRecovered] 失敗', e);
      return { ok:false, reason:(e && (e.code||e.message)) || 'exception' };
    }
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.16.27(2026-06-26)— 學生自助「確認不是我的 → 當場移除」(乙·甲案:即時移除·不經老師)
  //   情境:學生在「🔍 英雄來源自我審查」對「查無證據」的英雄按「不是我的」+ 確認 → 立即移除。
  //   永久關鍵:帳本補 source='audit_error_recovered'(+disownedByStudent 標記)→ 各補回路徑/出口過濾/
  //     自癒守門(L≈85956)全認得不復活 → 移除後不會被自癒從 _heroLevels 撈回來(這是「多出一直回來」的根治)。
  //   無損可逆:暫存原等級到 _auditRecoveredLevels(誤移除可由「📨 救援申請→🔓 我遺失的英雄要回來」→
  //     GM「🛟 一鍵永久復原」補回原等級;技能/天賦/爆發升級當下清除無法復原,與 v3.16.19 同性質)。
  //   安全:① 只移除「確實在雲端 unlockedHeroes 內」的(雲端沒有的略過)② 清齊六養成表 _s + heroExp +
  //     heroTraitLevel(杜絕 desync/採信舊 _s 復活)③ 至寶解裝保留本體 ④ 刻意不寫 _authoritativeRestoreAt
  //     (學生在 session 內·改本機同步即時更新·不重載)⑤ 刻意不寫 _auditRecoverDoneV1(與 orchestrator 一次性旗標無關)。
  // ════════════════════════════════════════════════════════════════════
  // ★ v3.16.29(甲)— 讀雲端解鎖帳本(供圖鑑自我審查「這是我的」核對 uid)。
  //   雲端 players/{uid}._heroUnlockHistory 比共用 iPad 本機帳本不易被跨帳號污染 → 當作「符合記錄」的權威依據。
  // ★ v3.16.30 — 待審查英雄(圖鑑審查勾「這是我的」但與雲端記錄不符)凍結狀態。
  //   可出戰、可裝至寶;但不可獲得經驗升級/投資素質點/升級技能/升級極限爆發(戰鬥中爆發照常),待 GM 審核。
  //   清單 players/{uid}._auditPendingHeroes(陣列·arrayUnion 加入·GM 審核時移除);_applySafeData 載入到 window._auditPendingSet。
  window._auditPendingSet = (window._auditPendingSet instanceof Set) ? window._auditPendingSet : new Set();
  // ★ v5.36.0(2026-08-14·老師需求2)— 主角(主角/主角‧覺醒)永久免 GM 審查、自動歸屬本帳號 uid:
  //   主角設計上「預設持有、綁 UID、刻意不寫解鎖帳本」(PHASE 2 A1/v4.87.0),稽核鏈早已全面豁免;
  //   但歷史上(豁免上線前)被自我審查勾進 _auditPendingHeroes 的主角會永久凍結升級/投資。三點根治:
  //   ①本函式主角早退 false(單點涵蓋全部 20+ 凍結/徽章消費點)②_fbMarkAuditPending 過濾主角永不再標
  //   ③_applySafeData 載入時過濾 + 雲端 arrayRemove 自癒(見該處)。
  //   ★ 刻意不補寫解鎖帳本紀錄:寫了會讓「GM 帳號重建(帳本反推)」把主角誤推進 unlockedHeroes,
  //     違反主角不入清單的設計;歸屬 uid 由 _isProtagHero 白名單(_advHasGenuineUnlock 一律 true)天生保證。
  window._isAuditPending = function(n){
    try{ if(window._isProtagHero && window._isProtagHero(n)) return false; }catch(_){}
    try{ return !!(window._auditPendingSet && window._auditPendingSet.has(n)); }catch(_){ return false; }
  };
  window._fbMarkAuditPending = function(names){
    try{
      if(!names || !names.length) return;
      // ★ v5.36.0 — 主角永不進待審查清單(需求2·過濾後全空即整筆略過)
      names = names.filter(function(n){ return n && !(window._isProtagHero && window._isProtagHero(n)); });
      if(!names.length) return;
      names.forEach(function(n){ if(n) window._auditPendingSet.add(n); });
      if(typeof window._lxpsCloudInstantUnlock === 'function' && window._fbFns && typeof window._fbFns.arrayUnion === 'function'){
        window._lxpsCloudInstantUnlock({ _auditPendingHeroes: window._fbFns.arrayUnion.apply(window._fbFns, names) }, '待審查 ' + names.length + ' 隻');
      }
    }catch(e){ console.warn('[待審查標記] 例外', e); }
  };

  // ★ v3.16.30 — GM 審核「待審查」英雄(由 admin_panel.js 救援卡呼叫):
  //   通過 → 清 _auditPendingHeroes 解凍 + 寫 admin_grant 合法紀錄(雲端·之後不再被審查)+ 蓋 _authoritativeRestoreAt 讓玩家端重載生效。
  //   不通過 → 清 pending + 移出 unlockedHeroes 轉灰 + 標 audit_error_recovered(可逆≠admin_delete·六補回路徑不復活·重抽會寫新紀錄自動解鎖)+ 蓋 restore。
  //   ★ 鐵則:可逆、uid 閘(寫 players/{傳入uid})、寧漏不誤刪;養成資料保留(轉灰僅動解鎖清單,不清等級)。
  window._fbAdminApproveAuditHeroes = async function(uid, names){
    try{
      if(!uid || !names || !names.length || !window._fbDb || !window._fbFns) return false;
      var F = window._fbFns, ref = F.doc(window._fbDb, 'players', uid), now = Date.now();
      var recs = names.map(function(n){ return { name:n, at:now, source:'admin_grant', uid:String(uid).slice(0,12) }; });
      var patch = { _auditPendingHeroes: F.arrayRemove.apply(F, names), _heroUnlockHistory: F.arrayUnion.apply(F, recs), _authoritativeRestoreAt: now };
      try{ await F.updateDoc(ref, patch); }
      catch(_eU){ await F.setDoc(ref, patch, { merge:true }); }   // 欄位不存在 → 退 setDoc merge
      return true;
    }catch(e){ console.warn('[GM審核通過] 失敗', e); return false; }
  };
  window._fbAdminRejectAuditHeroes = async function(uid, names){
    try{
      if(!uid || !names || !names.length || !window._fbDb || !window._fbFns) return false;
      // ★★ v5.130.0(老師裁定甲)— 改走三槽同寫單一真相 ★★
      //   舊版不只單寫主檔,連養成表/_s 都沒清 ⇒ 審核不通過的英雄是最容易復活的一路;比照其他移除路徑修齊。
      var now = Date.now();
      await window._lxpsAdminApplyAllSlots(uid, function(d, slot){
        var p = window._lxpsSlotRemoveHeroesPatch(d, names, {
          at:now, by:'admin_audit', uid:uid, slot:slot,
          source:'audit_error_recovered',
          reason:'GM 圖鑑審查不通過·轉灰(可逆·重抽會寫新紀錄自動解鎖)'
        });
        if(!p) return null;
        if(slot === 'main'){
          p._auditPendingHeroes = window._fbFns.arrayRemove.apply(window._fbFns, names);   // 待審清單只存在主檔
        }
        return p;
      }, { label:'GM審核不通過英雄' });
      return true;
    }catch(e){ console.warn('[GM審核不通過] 失敗', e); return false; }
  };

  window._fbGetCloudUnlockHistory = async function(uid){
    try{
      if(!uid) return [];
      const _snap = await getDoc(doc(_fbDb, 'players', uid));
      if(!_snap.exists()) return [];
      const _d = _snap.data() || {};
      return Array.isArray(_d._heroUnlockHistory) ? _d._heroUnlockHistory : [];
    }catch(e){ console.warn('[_fbGetCloudUnlockHistory] 失敗', e); return null; }
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.16.31(2026-06-26)— 至寶版「待審查凍結」(比照英雄版 v3.16.30)。
  //   情境:圖鑑審查送出時,持有但「雲端/本機帳本都查無自己取得紀錄」的至寶 → 進「🔺審查中」凍結。
  //   凍結效果:可裝備、戰鬥照常;但不可獲得經驗升級/用至寶經驗卷軸/投資素質點,待 GM 審核。
  //   清單 players/{uid}._auditPendingTreasures(陣列·arrayUnion 加入·GM 審核時移除);_applySafeData 載入到 window._auditPendingTreasureSet。
  //   ⚠ 日本三神器(tengu/shutendoji/tamamo)是任務物品非至寶,審查/判定/移除一律排除(window._isJapanTreasureKey)。
  window._auditPendingTreasureSet = (window._auditPendingTreasureSet instanceof Set) ? window._auditPendingTreasureSet : new Set();
  window._isAuditPendingTreasure = function(k){ try{ return !!(k && window._auditPendingTreasureSet && window._auditPendingTreasureSet.has(k)); }catch(_){ return false; } };
  window._fbMarkAuditPendingTreasure = function(keys){
    try{
      if(!keys || !keys.length) return;
      var _ks = [];
      keys.forEach(function(k){ if(k && !(window._isJapanTreasureKey && window._isJapanTreasureKey(k))){ window._auditPendingTreasureSet.add(k); _ks.push(k); } });
      if(!_ks.length) return;
      if(typeof window._lxpsCloudInstantUnlock === 'function' && window._fbFns && typeof window._fbFns.arrayUnion === 'function'){
        window._lxpsCloudInstantUnlock({ _auditPendingTreasures: window._fbFns.arrayUnion.apply(window._fbFns, _ks) }, '待審查至寶 ' + _ks.length + ' 個');
      }
    }catch(e){ console.warn('[待審查至寶標記] 例外', e); }
  };

  // ★ v3.16.31 — 讀雲端至寶帳本(供圖鑑審查掃描「持有但查無紀錄」的至寶)。讀取失敗回 null(不掃描·避免無證據誤判)。
  window._fbGetCloudTreasureHistory = async function(uid){
    try{
      if(!uid) return [];
      const _snap = await getDoc(doc(_fbDb, 'players', uid));
      if(!_snap.exists()) return [];
      const _d = _snap.data() || {};
      if(Array.isArray(_d._treasureUnlockHistory)) return _d._treasureUnlockHistory;
      if(typeof _d._treasureUnlockHistory_s === 'string'){ try{ const _p = JSON.parse(_d._treasureUnlockHistory_s); if(Array.isArray(_p)) return _p; }catch(_){} }
      return [];
    }catch(e){ console.warn('[_fbGetCloudTreasureHistory] 失敗', e); return null; }
  };

  // ★ v3.16.31 — GM 審核「待審查至寶」(由 admin_panel.js 救援卡呼叫):
  //   通過 → 清 _auditPendingTreasures 解凍 + 寫合法紀錄(admin_grant·之後不再被審查)+ 蓋 _authoritativeRestoreAt 讓玩家端重載生效。
  //   不通過 → 清 pending + 移出 taiwanTreasureData(連 _s 整包覆蓋·避免 _applySafeData 採信舊 _s 復活)+ 解除裝備
  //            + 標 audit_error_recovered(可逆≠刪除·_applySafeData 載入會剔除本機殘留·重抽/自選/冒險解鎖會寫新紀錄自動正式解鎖)+ 蓋 restore。
  //   ★ 鐵則:可逆、uid 閘(寫 players/{傳入uid})、寧漏不誤刪;連 _s 整包覆蓋(map merge:true 是landmine)。
  window._fbAdminApproveAuditTreasures = async function(uid, keys){
    try{
      if(!uid || !keys || !keys.length || !window._fbDb || !window._fbFns) return false;
      var F = window._fbFns, ref = F.doc(window._fbDb, 'players', uid), now = Date.now();
      var recs = keys.map(function(k){ return { id:k, at:now, source:'admin_grant', uid:String(uid).slice(0,12) }; });
      var patch = { _auditPendingTreasures: F.arrayRemove.apply(F, keys), _treasureUnlockHistory: F.arrayUnion.apply(F, recs), _authoritativeRestoreAt: now };
      try{ await F.updateDoc(ref, patch); }
      catch(_eU){ await F.setDoc(ref, patch, { merge:true }); }
      return true;
    }catch(e){ console.warn('[GM至寶審核通過] 失敗', e); return false; }
  };
  window._fbAdminRejectAuditTreasures = async function(uid, keys){
    try{
      if(!uid || !keys || !keys.length || !window._fbDb || !window._fbFns) return false;
      // ★★ v5.130.0(老師裁定甲)— 改走三槽同寫單一真相 ★★
      //   舊版只寫主檔 ⇒ live/safe 至寶殘留 + 沒有 audit_error_recovered 帳本 ⇒ 被駁回的至寶
      //   會被三槽合併/幻影救回撿回來(至寶身上還可能帶 equippedTo 連英雄一起救活)。
      var now = Date.now();
      await window._lxpsAdminApplyAllSlots(uid, function(d, slot){
        var p = window._lxpsSlotTreasurePatch(d, keys, {
          at:now, mode:'remove', by:'admin_audit', uid:uid, slot:slot,
          source:'audit_error_recovered'
        });
        if(!p) return null;
        if(slot === 'main'){
          p._auditPendingTreasures = window._fbFns.arrayRemove.apply(window._fbFns, keys);   // 待審清單只存在主檔
        }
        return p;
      }, { label:'GM審核不通過至寶' });
      return true;
    }catch(e){ console.warn('[GM至寶審核不通過] 失敗', e); return false; }
  };

  // ★ v3.16.67 — GM 救回/補回至寶(玩家圖鑑「🔓 要救回」送 claims.lostTreasures→ 老師確認後補回):
  //   至寶不存在 → 補回 {lv:1,exp:0,equippedTo:null,invested:0};已存在 → 保留(等級/投資不動)。
  //   一律寫 admin_grant 合法紀錄 + 解凍 pending + _s 整包覆蓋 + _authoritativeRestoreAt(重載生效)。
  window._fbAdminRestoreLostTreasures = async function(uid, ids){
    try{
      if(!uid || !ids || !ids.length || !window._fbDb || !window._fbFns) return { ok:false, reason:'參數不足' };
      // ★★ v5.130.0(老師裁定甲)— 改走三槽同寫單一真相 ★★
      //   舊版只寫主檔 ⇒ 補回的至寶帳本被學生本機存檔蓋掉後,live/safe 較舊的回收帳本變最近一筆
      //   ⇒ 剛補回的至寶又被濾掉(與英雄補發同一條「補了拿不到」的鏈)。
      //   已存在的至寶原樣保留(等級/投資不動),只補不存在的 Lv1(由 _lxpsSlotTreasurePatch grant 模式保證)。
      var now = Date.now();
      var restored = ids.filter(Boolean);
      var _res130 = await window._lxpsAdminApplyAllSlots(uid, function(d, slot){
        var p = window._lxpsSlotTreasurePatch(d, restored, {
          at:now, mode:'grant', by:'admin_rescue', uid:uid, slot:slot,
          source:'admin_grant', extra:{ auditRestored:true }
        });
        if(!p) return null;
        if(slot === 'main'){
          p._auditPendingTreasures = window._fbFns.arrayRemove.apply(window._fbFns, restored);   // 解凍(只存在主檔)
        }
        return p;
      }, { label:'GM救回至寶' });
      return { ok:true, restored:restored, slots:_res130 };
    }catch(e){ console.warn('[GM至寶救回] 失敗', e); return { ok:false, reason:(e && e.message) || '失敗' }; }
  };

  window._fbStudentDisownHeroes = async function(uid, names){
    if(!uid) return { ok:false, reason:'no_uid' };
    const _names = Array.isArray(names) ? names.filter(Boolean) : [];
    if(!_names.length) return { ok:true, removed:[] };
    // ★★ v5.130.0(老師裁定甲)— 改走三槽同寫單一真相 ★★
    //   舊版只寫主檔 ⇒ 學生自助移除後,live/safe 仍留著該英雄與養成資料、且沒有
    //   audit_error_recovered 帳本 ⇒ 下次登入三槽合併/幻影救回把它撿回來(v5.92.0 已對
    //   UR 超額退回那條路修過同款病灶,本函式一直沒修)。
    //   ⚠ 沿用原設計刻意不蓋 _authoritativeRestoreAt(學生 session 內本機即時同步,不重載)。
    try{
      const _now = Date.now();
      var _toRemove = [];
      // ★ v5.151.0 — 移除前先算跨三槽 canonical 原等級,避免三槽各自暫存出不同等級
      //   (老師實錘「6213 魔劍姬 44 級救回變 26 級」根因:舊版逐槽只看自己那槽的 heroLevels,
      //   共用 iPad 上三槽等級本就常不同步,於是三槽各暫存出不同的「原等級」)
      const _canon = await window._lxpsCanonicalHeroLevels(uid, _names);
      const _res130 = await window._lxpsAdminApplyAllSlots(uid, function(d, slot){
        if(slot === 'main'){
          const _cloudSet = new Set(Array.isArray(d.unlockedHeroes) ? d.unlockedHeroes : []);
          _toRemove = _names.filter(function(n){ return _cloudSet.has(n); });   // 只移除雲端確實有的
        }
        if(!_toRemove.length) return null;
        const _p = window._lxpsSlotRemoveHeroesPatch(d, _toRemove, {
          at:_now, by:'student', uid:uid, slot:slot,
          source:'audit_error_recovered',
          extra:{ disownedByStudent:true },
          reason:'學生自我審查·確認「不是我的」·自助移除(可由帳號救援申請補回)'
        });
        if(!_p) return null;
        // 暫存原等級供 GM「🛟 一鍵無損救回」原封還原:三槽一律暫存同一個 canonical 值
        var _stash = (d._auditRecoveredLevels && typeof d._auditRecoveredLevels === 'object')
          ? Object.assign({}, d._auditRecoveredLevels) : {};
        _toRemove.forEach(function(n){
          var _lv = (typeof _canon[n] === 'number') ? _canon[n] : 1;
          if(!(typeof _stash[n] === 'number') || _stash[n] < _lv) _stash[n] = _lv || 1;
        });
        _p._auditRecoveredLevels = _stash;
        return _p;
      }, { label:'學生自助移除英雄', stampRestore:false });
      if(!_toRemove.length) return { ok:true, removed:[] };
      const _removeSet = new Set(_toRemove);
      const _dMain = _res130.mainData || {};
      const _protectedTreasures = [];
      try{
        const _treV = (_dMain.taiwanTreasureData && typeof _dMain.taiwanTreasureData === 'object') ? _dMain.taiwanTreasureData : {};
        Object.keys(_treV).forEach(function(_tid){
          const _rec = _treV[_tid];
          if(_rec && _rec.equippedTo && _removeSet.has(_rec.equippedTo)) _protectedTreasures.push(_tid);
        });
      }catch(_eTv){}
      console.log('[_fbStudentDisownHeroes v5.130.0] ✅ uid=' + uid + ' 學生自助移除 ' + _toRemove.length
        + ' 隻(保護至寶 ' + _protectedTreasures.length + ' 個):', _toRemove);
      return { ok:true, removed:_toRemove, protectedTreasures:_protectedTreasures, slots:_res130 };
    }catch(e){
      console.error('[_fbStudentDisownHeroes] 失敗', e);
      return { ok:false, reason:(e && (e.code||e.message)) || 'exception' };
    }
  };

  /* ── v5.130.0 前的舊版(只寫主檔)整段保留備查,勿刪 ────────────────────
  window._fbStudentDisownHeroes_LEGACY_v5129 = async function(uid, names){
    const _names = Array.isArray(names) ? names.filter(Boolean) : [];
    const _ref = doc(_fbDb, 'players', uid);
    try{
      const _snap = await getDoc(_ref);
      if(!_snap.exists()) return { ok:false, reason:'player_not_found' };
      const _d = _snap.data() || {};
      const _cloudUnlocked = Array.isArray(_d.unlockedHeroes) ? _d.unlockedHeroes : [];
      const _cloudSet = new Set(_cloudUnlocked);
      const _toRemove = _names.filter(function(n){ return _cloudSet.has(n); });   // 只移除雲端確實有的
      if(_toRemove.length === 0) return { ok:true, removed:[] };
      const _removeSet = new Set(_toRemove);
      const _unlocked = _cloudUnlocked.filter(function(n){ return !_removeSet.has(n); });
      const _clean = function(obj){
        const o = (obj && typeof obj === 'object') ? Object.assign({}, obj) : {};
        _toRemove.forEach(function(n){ delete o[n]; });
        return o;
      };
      let _hist = Array.isArray(_d._heroUnlockHistory) ? _d._heroUnlockHistory.slice() : [];
      const _now = Date.now();
      const _myUid12 = String(uid).slice(0, 12);
      // 至寶保護:裝在被移除英雄身上的至寶 → 解除裝備、保留本體
      const _tre = (_d.taiwanTreasureData && typeof _d.taiwanTreasureData === 'object')
        ? JSON.parse(JSON.stringify(_d.taiwanTreasureData)) : {};
      const _protectedTreasures = [];
      let _treChanged = false;
      Object.keys(_tre).forEach(function(_tid){
        const _rec = _tre[_tid];
        if(_rec && _rec.equippedTo && _removeSet.has(_rec.equippedTo)){
          _protectedTreasures.push(_tid); _rec.equippedTo = null; _treChanged = true;
        }
      });
      // 帳本補「不復活」守門紀錄(沿用 audit_error_recovered → 所有守門路徑全認得;加 disownedByStudent 供稽核)
      _toRemove.forEach(function(n){ _hist.push({
        name: n, at: _now, source: 'audit_error_recovered', uid: _myUid12,
        disownedByStudent: true, reason: '學生自我審查·確認「不是我的」·自助移除(可由帳號救援申請補回)'
      }); });
      if(_hist.length > 300) _hist = _hist.slice(-300);
      // 暫存原等級供 GM 一鍵復原(與既有暫存合併)
      var _stashLvls = (_d._auditRecoveredLevels && typeof _d._auditRecoveredLevels === 'object') ? Object.assign({}, _d._auditRecoveredLevels) : {};
      _toRemove.forEach(function(n){
        var _lv = (_d.heroLevels && typeof _d.heroLevels[n] === 'number') ? _d.heroLevels[n] : 1;
        _stashLvls[n] = Number(_lv) || 1;
      });
      const _patch = {
        unlockedHeroes:        _unlocked,
        _auditRecoveredLevels: _stashLvls,
        heroLevels:            _clean(_d.heroLevels),
        heroLevels_s:          JSON.stringify(_clean(_d.heroLevels)),
        heroStatInvested:      _clean(_d.heroStatInvested),
        heroStatInvested_s:    JSON.stringify(_clean(_d.heroStatInvested)),
        heroStatPoints:        _clean(_d.heroStatPoints),
        heroStatPoints_s:      JSON.stringify(_clean(_d.heroStatPoints)),
        heroSkillLevels:       _clean(_d.heroSkillLevels),
        heroSkillLevels_s:     JSON.stringify(_clean(_d.heroSkillLevels)),
        heroBurstLevels:       _clean(_d.heroBurstLevels),
        heroBurstLevels_s:     JSON.stringify(_clean(_d.heroBurstLevels)),
        heroExp:               _clean(_d.heroExp),
        heroExp_s:             JSON.stringify(_clean(_d.heroExp)),    // ★ v3.16.33 — 補字串版(本版載入優先採信 _s,不寫則移除後被舊髒 _s 復活)
        heroTraitLevel:        _clean(_d.heroTraitLevel),
        heroTraitLevel_s:      JSON.stringify(_clean(_d.heroTraitLevel)),// ★ v3.16.33 — 補字串版(同上)
        _heroUnlockHistory:    _hist
        // ⚠ 刻意不寫 _authoritativeRestoreAt(學生 session 內·本機同步即時更新·不重載)
        // ⚠ 刻意不寫 _auditRecoverDoneV1(那是 orchestrator 一次性旗標,與本自助移除無關)
      };
      if(_treChanged){
        _patch.taiwanTreasureData = _tre;
        _patch.taiwanTreasureData_s = JSON.stringify(_tre);
      }
      await updateDoc(_ref, _patch);
      console.log('[_fbStudentDisownHeroes] ✅ uid=' + uid + ' 學生自助移除 ' + _toRemove.length + ' 隻(保護至寶 ' + _protectedTreasures.length + ' 個):', _toRemove);
      return { ok:true, removed:_toRemove, protectedTreasures:_protectedTreasures };
    }catch(e){
      console.error('[_fbStudentDisownHeroes] 失敗', e);
      return { ok:false, reason:(e && (e.code||e.message)) || 'exception' };
    }
  };
  ──────────────────────────────────────────────────────────────────── */

  // ════════════════════════════════════════════════════════════════════
  // ★ v5.92.0 追加（老師 2026-08-20 需求）— UR 超額「學生同意後收回」·三槽同寫·不復活
  // ────────────────────────────────────────────────────────────────────
  //   與 _fbStudentDisownHeroes（自審 disown）的三個關鍵差異：
  //     ① **三槽同寫**（主檔 + saves/live + saves/safe）：學生按下同意後會 reload，
  //        reload 前本機 session 若剛好觸發 gameCloudSave，就會拿「還含這幾隻 UR 的本機資料」
  //        寫回 live/safe → 下次登入三槽合併時整組復活。只寫主檔擋不住這條路，必須三槽都寫。
  //     ② **每槽都寫 admin_delete 帳本**：_lxpsLatestDeletedMap 的稽核感知是「以最近一筆為準」，
  //        任一殘槽 union 進來的 unlockedHeroes 都會被這筆 admin_delete 濾掉 → 永不復活
  //        （與 _fbAdminGearDeleteForUid v5.32.0「safe 槽留鐵證防在線覆寫」同款保險）。
  //     ③ 帳本 source 用 'admin_delete'（正式回收語義·學生已明示同意），並帶
  //        studentAgreed:true / agreedAt 供稽核；★ 仍暫存原等級到 _auditRecoveredLevels
  //        → 學生日後申訴成立時，老師可用既有「🛟 一鍵無損救回」原封還原（不是不可逆刪除）。
  //   ⚠ 至寶保護沿用既有口徑：裝在被收回英雄身上的至寶只解除裝備、本體保留。
  //   ⚠ 學生寫自己的 players/{uid}/saves/{slot} 由既有 rules 允許 → 零規則改動。
  //   ⚠ 刻意不寫 _authoritativeRestoreAt（那會讓學生自己這個 session 被判過時而重載，
  //     本函式後面緊接著就是 location.reload()，重複重載只會讓畫面閃兩次）。
  // ════════════════════════════════════════════════════════════════════
  window._fbStudentSurplusURRemove = async function(uid, names, opts){    if(!uid) return { ok:false, reason:'no_uid' };
    const _names = Array.isArray(names) ? names.filter(Boolean) : [];
    if(!_names.length) return { ok:true, removed:[], slots:[] };
    opts = opts || {};
    const _removeSet = new Set(_names);
    const _now = Date.now();
    const _uid12 = String(uid).slice(0, 12);
    const _gds = (typeof getDocFromServer === 'function') ? getDocFromServer : getDoc;
    const _targets = [
      { ref: doc(_fbDb, 'players', uid),                    label: '主檔', mustExist: true  },
      { ref: doc(_fbDb, 'players', uid, 'saves', 'live'),   label: 'live', mustExist: false },
      { ref: doc(_fbDb, 'players', uid, 'saves', 'safe'),   label: 'safe', mustExist: false }
    ];
    const _clean = function(obj){
      const o = (obj && typeof obj === 'object') ? Object.assign({}, obj) : {};
      _names.forEach(function(n){ delete o[n]; });
      return o;
    };
    const _wrote = [];
    let _removedAny = [];
    let _protectedTreasures = [];
    try{
      for(let i = 0; i < _targets.length; i++){
        const _t = _targets[i];
        let _snap = null;
        try{ _snap = await _gds(_t.ref); }catch(_e1){ try{ _snap = await getDoc(_t.ref); }catch(_e2){ _snap = null; } }
        const _ex = _snap && ((typeof _snap.exists === 'function') ? _snap.exists() : !!_snap.exists);
        if(!_ex){
          if(_t.mustExist) return { ok:false, reason:'player_not_found' };
          continue;   // live/safe 槽不存在是正常的（尚未建立）
        }
        const _d = _snap.data() || {};
        const _cloudUnlocked = Array.isArray(_d.unlockedHeroes) ? _d.unlockedHeroes : [];
        const _hit = _cloudUnlocked.filter(function(n){ return _removeSet.has(n); });
        if(_t.label === '主檔'){
          if(!_hit.length) return { ok:true, removed:[], slots:[], note:'main_has_none' };
          _removedAny = _hit.slice();
        }
        // 至寶保護（每槽各自處理自己的 taiwanTreasureData）
        let _tre = {};
        try{ _tre = (_d.taiwanTreasureData && typeof _d.taiwanTreasureData === 'object') ? JSON.parse(JSON.stringify(_d.taiwanTreasureData)) : {}; }catch(_){ _tre = {}; }
        let _treChanged = false;
        Object.keys(_tre).forEach(function(_tid){
          const _rec = _tre[_tid];
          if(_rec && _rec.equippedTo && _removeSet.has(_rec.equippedTo)){
            if(_protectedTreasures.indexOf(_tid) < 0) _protectedTreasures.push(_tid);
            _rec.equippedTo = null; _treChanged = true;
          }
        });
        // 暫存原等級（申訴成立時供 GM 一鍵無損救回）
        let _stashLvls = (_d._auditRecoveredLevels && typeof _d._auditRecoveredLevels === 'object')
          ? Object.assign({}, _d._auditRecoveredLevels) : {};
        _names.forEach(function(n){
          const _lv = (_d.heroLevels && typeof _d.heroLevels[n] === 'number') ? _d.heroLevels[n] : null;
          if(_lv !== null && typeof _stashLvls[n] !== 'number') _stashLvls[n] = Number(_lv) || 1;
        });
        // ★ 帳本：三槽都寫（任一殘槽 union 進來也會被這筆 admin_delete 濾掉）
        let _hist = Array.isArray(_d._heroUnlockHistory) ? _d._heroUnlockHistory.slice() : [];
        _names.forEach(function(n){ _hist.push({
          name: n, at: _now, source: 'admin_delete', uid: _uid12,
          studentAgreed: true, agreedAt: _now,
          urSurplus: true, slot: _t.label,
          reason: opts.reason || '學生於「UR 角色數量確認」視窗勾選同意·退回多召喚出來的 UR(v5.92.0)'
        }); });
        if(_hist.length > 300) _hist = _hist.slice(-300);
        const _patch = {
          unlockedHeroes:        _cloudUnlocked.filter(function(n){ return !_removeSet.has(n); }),
          _auditRecoveredLevels: _stashLvls,
          heroLevels:            _clean(_d.heroLevels),
          heroLevels_s:          JSON.stringify(_clean(_d.heroLevels)),
          heroStatInvested:      _clean(_d.heroStatInvested),
          heroStatInvested_s:    JSON.stringify(_clean(_d.heroStatInvested)),
          heroStatPoints:        _clean(_d.heroStatPoints),
          heroStatPoints_s:      JSON.stringify(_clean(_d.heroStatPoints)),
          heroSkillLevels:       _clean(_d.heroSkillLevels),
          heroSkillLevels_s:     JSON.stringify(_clean(_d.heroSkillLevels)),
          heroBurstLevels:       _clean(_d.heroBurstLevels),
          heroBurstLevels_s:     JSON.stringify(_clean(_d.heroBurstLevels)),
          heroExp:               _clean(_d.heroExp),
          heroExp_s:             JSON.stringify(_clean(_d.heroExp)),
          heroTraitLevel:        _clean(_d.heroTraitLevel),
          heroTraitLevel_s:      JSON.stringify(_clean(_d.heroTraitLevel)),
          _heroUnlockHistory:    _hist
        };
        if(_treChanged){
          _patch.taiwanTreasureData   = _tre;
          _patch.taiwanTreasureData_s = JSON.stringify(_tre);
        }
        await setDoc(_t.ref, _patch, { merge: true });
        _wrote.push(_t.label);
      }
    }catch(e){
      console.error('[_fbStudentSurplusURRemove] 失敗(已寫入的槽:' + _wrote.join('/') + ')', e);
      return { ok:false, reason:(e && (e.code||e.message)) || 'exception', slots:_wrote };
    }
    // ★ 本機同步移除：擋掉「reload 前最後一次 gameCloudSave 把舊資料寫回」的窗口。
    //   ⚠ advGetUnlockedHeroes() 的真實來源是 localStorage 'adv_unlocked_heroes'（不是 window 變數），
    //     所以要改的是它；養成表另有 'adv_hero_levels' 等鏡像，一併清掉對應鍵。
    //   ⚠ 這只是額外保險：即使這段完全失敗，三槽（尤其 safe 槽）的 admin_delete 帳本仍會讓
    //     _fbSave 的稽核感知守門與登入三槽合併都判定「已移除」→ 不會復活。
    try{
      var _lsList = [];
      try{ var _rawLs = localStorage.getItem('adv_unlocked_heroes'); _lsList = _rawLs ? JSON.parse(_rawLs) : []; }catch(_){ _lsList = []; }
      if(Array.isArray(_lsList) && _lsList.length){
        var _kept = _lsList.filter(function(n){ return !_removeSet.has(n); });
        if(_kept.length !== _lsList.length){
          try{ localStorage.setItem('adv_unlocked_heroes', JSON.stringify(_kept)); }catch(_){}
        }
      }
      // 本機養成表鏡像：只有 adv_hero_levels 是實際存在的鍵（其餘養成表不做本機鏡像）
      try{
        var _rawLv = localStorage.getItem('adv_hero_levels');
        if(_rawLv){
          var _oLv = JSON.parse(_rawLv);
          if(_oLv && typeof _oLv === 'object' && !Array.isArray(_oLv)){
            var _chLv = false;
            _names.forEach(function(n){ if(_oLv[n] !== undefined){ delete _oLv[n]; _chLv = true; } });
            if(_chLv) localStorage.setItem('adv_hero_levels', JSON.stringify(_oLv));
          }
        }
      }catch(_){}
      // 本機解鎖帳本也補一筆 admin_delete：本機端的稽核感知（_lxpsGetHeroUnlockInfo 等）
      // 與 gameCloudSave 組裝 _writeData._heroUnlockHistory 時都會讀到 → 不會反手把舊紀錄蓋回雲端。
      try{
        var _rawH = localStorage.getItem('adv_hero_unlock_history');
        var _lh = _rawH ? JSON.parse(_rawH) : [];
        if(!Array.isArray(_lh)) _lh = [];
        _names.forEach(function(n){ _lh.push({
          name:n, at:_now, source:'admin_delete', uid:_uid12,
          studentAgreed:true, urSurplus:true,
          reason:'學生同意退回多召喚出來的 UR(v5.92.0)'
        }); });
        if(_lh.length > 300) _lh = _lh.slice(-300);
        localStorage.setItem('adv_hero_unlock_history', JSON.stringify(_lh));
      }catch(_){}
    }catch(_eLocal){ console.warn('[_fbStudentSurplusURRemove] 本機同步移除失敗(雲端三槽已完成·不影響結果)', _eLocal); }
    console.log('[_fbStudentSurplusURRemove] ✅ uid=' + String(uid).slice(0,8) + ' 學生同意收回 '
      + _removedAny.length + ' 隻·已寫槽:' + _wrote.join('/') + '(保護至寶 ' + _protectedTreasures.length + ' 個)', _removedAny);
    return { ok:true, removed:_removedAny, slots:_wrote, protectedTreasures:_protectedTreasures };
  };

  // ════════════════════════════════════════════════════════════════════
  // ★★ v5.227.0（老師「新增 UR 英雄替換卷…可以將自己已持有的 UR 重新轉換成當前所有 UR 中的任意一隻，
  //     英雄等級、技能等級、爆發等級全部轉移，原有至寶、寵物、裝備全部卸下。
  //     此功能必須嚴防玩家被轉換的 UR 復活或遺失，導致額外獲得或遺失 UR 英雄」）★★
  // ────────────────────────────────────────────────────────────────────
  // 【為什麼不能只改本機再 gameCloudSave】「移除一隻已解鎖英雄」在本專案是出了名難做對的事：
  //   只寫主檔會被 saves/live、saves/safe 兩個殘槽在下次登入 union 回來（v5.92.0 就是踩這個坑），
  //   而且學生本機 session 只要在 reload 前剛好觸發一次 gameCloudSave，就會拿「還含舊 UR 的本機資料」
  //   把雲端寫回去 ⇒ 被換掉的 UR 當場復活，學生等於白賺一隻。
  // 【所以本函式沿用 _fbStudentSurplusURRemove(v5.92.0) 已驗證過的那一整套「不復活」機制】
  //   ① 主檔 + saves/live + saves/safe **三槽各寫一次**；
  //   ② **每槽都寫 `admin_delete` 帳本**：`_lxpsLatestDeletedMap` 的稽核感知是「以最近一筆為準」，
  //      任一殘槽 union 進來的 unlockedHeroes 都會被這筆濾掉 ⇒ 永不復活。
  // 【本函式與那支的關鍵差異：這是「換」不是「刪」，加減必須在同一次寫入裡】
  //   每一槽的 patch 同時做完「移除 from」與「加入 to」＋七張養成表整筆搬家，
  //   所以**單一槽不可能停在半套**（一次 setDoc 就是一次原子寫）。
  // 【跨槽中斷怎麼辦 ⇒ 寫入順序刻意是 live → safe → 主檔】
  //   主檔是登入時的第一權威。把它排在最後寫，代表整段若在中途死掉：
  //     ・主檔還是舊狀態（仍持有 from、沒有 to）
  //     ・券也還沒扣（扣券是全部成功之後才做）
  //   ⇒ 學生的狀態是「什麼都沒發生」，完全沒有「多一隻／少一隻」的可能，只要再按一次就好。
  //   ⚠ 這個順序是本函式最重要的一行設計，日後不可以為了「先確認主檔存在」而改回主檔優先。
  // 【至寶／寵物／裝備一律卸下不轉移（老師指定）】
  //   至寶 taiwanTreasureData[tid].equippedTo、裝備 equipmentData.instances[eqUid].equippedTo、
  //   寵物 tamedPets[pn].followTo，三者只要指向 from 一律改 null；**本體一件都不刪**。
  // ⚠ 學生寫自己的 players/{uid}/saves/{slot} 由既有 rules 允許 ⇒ 零規則改動。
  // ⚠ 刻意不寫 _authoritativeRestoreAt：本函式不 reload，寫了只會讓自己這個 session 被判過時。
  // ════════════════════════════════════════════════════════════════════
  window._fbURSwapApply = async function(uid, fromName, toName, opts){
    if(!uid) return { ok:false, reason:'no_uid' };
    if(!fromName || !toName || fromName === toName) return { ok:false, reason:'bad_args' };
    // ★ v5.232.0 — 縱深防禦:換出/換入兩端都必須是 UR(名單讀 UR 唯一權威 ⇒ 新增的究極神兵/天照大神/月讀大神自動可換)。
    //   面板本來就只列 UR,這一道是防「日後有人從別處呼叫本函式」把非 UR 換成 UR(= 憑空多出 UR)。
    try{
      const _isURn = function(n){
        try{ if(typeof window._lxpsIsUR === 'function') return !!window._lxpsIsUR(n); }catch(_){}
        try{ return Array.isArray(window._LXPS_RARITY_UR) && window._LXPS_RARITY_UR.indexOf(n) >= 0; }catch(_){ return false; }
      };
      if(!_isURn(fromName) || !_isURn(toName)) return { ok:false, reason:'not_ur' };
    }catch(_eUrChk){ return { ok:false, reason:'not_ur' }; }
    opts = opts || {};
    const _now = Date.now();
    const _uid12 = String(uid).slice(0, 12);
    const _gds = (typeof getDocFromServer === 'function') ? getDocFromServer : getDoc;
    // ★ 順序：live → safe → 主檔（見上方說明，主檔最後寫＝中斷時一律回到「什麼都沒發生」）
    const _targets = [
      { ref: doc(_fbDb, 'players', uid, 'saves', 'live'),   label: 'live', mustExist: false },
      { ref: doc(_fbDb, 'players', uid, 'saves', 'safe'),   label: 'safe', mustExist: false },
      { ref: doc(_fbDb, 'players', uid),                    label: '主檔', mustExist: true  }
    ];
    // 七張養成表 + 膠囊投資：整筆從 from 搬到 to（老師：等級/技能/爆發全部轉移）
    const _MOVE_TABLES = [
      'heroLevels', 'heroExp', 'heroStatInvested', 'heroStatPoints',
      'heroSkillLevels', 'heroBurstLevels', 'heroTraitLevel', 'heroCapsuleInvested'
    ];
    const _wrote = [];
    let _moved = [];
    try{
      for(let i = 0; i < _targets.length; i++){
        const _t = _targets[i];
        let _snap = null;
        try{ _snap = await _gds(_t.ref); }catch(_e1){ try{ _snap = await getDoc(_t.ref); }catch(_e2){ _snap = null; } }
        const _ex = _snap && ((typeof _snap.exists === 'function') ? _snap.exists() : !!_snap.exists);
        if(!_ex){
          if(_t.mustExist) return { ok:false, reason:'player_not_found', slots:_wrote };
          continue;   // live/safe 尚未建立是正常的
        }
        const _d = _snap.data() || {};
        const _cloudUnlocked = Array.isArray(_d.unlockedHeroes) ? _d.unlockedHeroes : [];
        if(_t.label === '主檔'){
          // 主檔是唯一權威：這裡才做最後一次資格複查（live/safe 可能是舊的，不能拿來判定）
          if(_cloudUnlocked.indexOf(fromName) < 0) return { ok:false, reason:'not_owned', slots:_wrote };
          if(_cloudUnlocked.indexOf(toName) >= 0)  return { ok:false, reason:'target_owned', slots:_wrote };
        }
        const _patch = {};
        // ── 名單：移除 from、加入 to（同一個陣列一次寫完，單槽不可能半套）
        let _nextList = _cloudUnlocked.filter(function(n){ return n !== fromName; });
        if(_nextList.indexOf(toName) < 0) _nextList.push(toName);
        _patch.unlockedHeroes = _nextList;
        // ── 養成表整筆搬家
        _MOVE_TABLES.forEach(function(k){
          let _o = {};
          try{ _o = (_d[k] && typeof _d[k] === 'object') ? JSON.parse(JSON.stringify(_d[k])) : {}; }catch(_){ _o = {}; }
          if(Object.prototype.hasOwnProperty.call(_o, fromName)){
            _o[toName] = _o[fromName];
            delete _o[fromName];
          }else{
            delete _o[fromName];
          }
          _patch[k] = _o;
          _patch[k + '_s'] = JSON.stringify(_o);
        });
        // ── 至寶：只解除裝備，本體保留
        try{
          const _tre = (_d.taiwanTreasureData && typeof _d.taiwanTreasureData === 'object') ? JSON.parse(JSON.stringify(_d.taiwanTreasureData)) : null;
          if(_tre){
            let _ch = false;
            Object.keys(_tre).forEach(function(_tid){
              const _r = _tre[_tid];
              if(_r && _r.equippedTo === fromName){ _r.equippedTo = null; _ch = true; }
            });
            if(_ch){ _patch.taiwanTreasureData = _tre; _patch.taiwanTreasureData_s = JSON.stringify(_tre); }
          }
        }catch(_eTre){}
        // ── 裝備：只解除裝備，本體保留
        try{
          const _gd = (_d.equipmentData && typeof _d.equipmentData === 'object') ? JSON.parse(JSON.stringify(_d.equipmentData)) : null;
          if(_gd && _gd.instances && typeof _gd.instances === 'object'){
            let _ch = false;
            Object.keys(_gd.instances).forEach(function(_eq){
              const _r = _gd.instances[_eq];
              if(_r && _r.equippedTo === fromName){ _r.equippedTo = null; _ch = true; }
            });
            if(_ch){ _patch.equipmentData = _gd; _patch.equipmentData_s = JSON.stringify(_gd); }
          }
        }catch(_eGear){}
        // ── 寵物：只解除跟隨，本體保留
        try{
          const _tp = (_d.tamedPets && typeof _d.tamedPets === 'object') ? JSON.parse(JSON.stringify(_d.tamedPets)) : null;
          if(_tp){
            let _ch = false;
            Object.keys(_tp).forEach(function(_pn){
              const _r = _tp[_pn];
              if(_r && _r.followTo === fromName){ _r.followTo = null; _ch = true; }
            });
            if(_ch){ _patch.tamedPets = _tp; _patch.tamedPets_s = JSON.stringify(_tp); }
          }
        }catch(_ePet){}
        // ── 帳本：三槽都寫。from 用 admin_delete（稽核感知會據此濾掉任何殘槽 union）；to 用 ticket_ur_swap
        let _hist = Array.isArray(_d._heroUnlockHistory) ? _d._heroUnlockHistory.slice() : [];
        _hist.push({
          name: fromName, at: _now, source: 'admin_delete', uid: _uid12,
          urSwap: true, swapTo: toName, slot: _t.label,
          reason: opts.reason || '玩家使用「UR 英雄替換卷」把 ' + fromName + ' 換成 ' + toName + '(v5.227.0)'
        });
        _hist.push({
          name: toName, at: _now + 1, source: 'ticket_ur_swap', uid: _uid12,
          urSwap: true, swapFrom: fromName, slot: _t.label
        });
        if(_hist.length > 300) _hist = _hist.slice(-300);
        _patch._heroUnlockHistory = _hist;

        await setDoc(_t.ref, _patch, { merge: true });
        _wrote.push(_t.label);
        if(_t.label === '主檔') _moved = _MOVE_TABLES.slice();
      }
    }catch(e){
      console.error('[_fbURSwapApply] 失敗(已寫入的槽:' + _wrote.join('/') + ')', e);
      return { ok:false, reason:(e && (e.code||e.message)) || 'exception', slots:_wrote };
    }
    console.log('[_fbURSwapApply] ✅ uid=' + String(uid).slice(0,8) + ' ' + fromName + ' → ' + toName + '·已寫槽:' + _wrote.join('/'));
    return { ok:true, from:fromName, to:toName, slots:_wrote, moved:_moved };
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v5.92.0 追加（老師 2026-08-20 需求）— UR 超額「與事實不符·我要申訴」
  // ────────────────────────────────────────────────────────────────────
  //   ★ 刻意複用既有 bugReports 集合（不新開 collection）：
  //     ① firestore.rules 既有條款＝學生 create + 讀自己的 + GM update(adminReply)
  //        + 學生只能改 adminReplyRead → **零規則改動**；
  //     ② GM 端既有「🐛 BUG 回報」卡可直接看到並回覆；
  //     ③ 學生端既有 v3.16.64「📩 老師的回覆」右下角小視窗＋鈴聲自動生效
  //        → 雙向溝通整條鏈路現成可用，不必再造一套。
  //   page='ur_surplus_appeal' 供老師一眼辨識；categories 帶 'data_incorrect' 供既有篩選。
  //   ⚠ 申訴送出後**不移除任何角色**（原封不動），並消化該封通知不再打擾學生；
  //     老師若判定申訴不成立，用稽核工具重新寄一次通知即可。
  // ════════════════════════════════════════════════════════════════════
  window._fbSubmitURSurplusAppeal = async function(uid, payload){
    if(!uid) throw new Error('缺少 uid');
    const _p = payload || {};
    const _reason = String(_p.reason || '').slice(0, 100);   // 硬夾 100 字（UI 也擋一次）
    if(!_reason) throw new Error('申訴理由不可空白');
    const _id = 'bug_' + Date.now() + '_' + Math.random().toString(36).slice(2, 8);
    const _email = (window._fbUser && window._fbUser.email) || '';
    const _nick  = String(_p.nickname || window._playerNickname || '');
    const _names = Array.isArray(_p.heroes) ? _p.heroes.map(function(x){ return String(x && x.name ? x.name : x); }) : [];
    const _lines = [];
    _lines.push('## 👑 UR 角色數量確認 — 學生申訴');
    _lines.push('- 申訴理由(學生填寫)：' + _reason);
    _lines.push('');
    _lines.push('## 📊 老師寄出通知時的對帳');
    _lines.push('- 事蹟：' + (String(_p.merit || '') || '(未填)'));
    _lines.push('- 應有：' + (parseInt(_p.expected, 10) || 0) + ' 隻');
    _lines.push('- 實有：' + (parseInt(_p.actual, 10) || _names.length) + ' 隻');
    _lines.push('- 要求退回：' + (parseInt(_p.surplus, 10) || 0) + ' 隻');
    _lines.push('- 目前持有的 UR：' + (_names.length ? _names.join('、') : '(無)'));
    _lines.push('');
    _lines.push('## 👤 帳號');
    _lines.push('- 暱稱：' + (_nick || '(未設定)'));
    _lines.push('- 信箱：' + _email);
    _lines.push('- 版本：' + (window._GAME_LOADED_VERSION || 'unknown'));
    _lines.push('- 送出時間：' + new Date().toLocaleString('zh-TW'));
    _lines.push('');
    _lines.push('⚠ 學生已選擇申訴 → 系統**未移除任何角色**，資料原封不動，等老師處理。');
    await setDoc(doc(_fbDb, 'bugReports', _id), {
      id: _id,
      email: _email.slice(0, 200),
      uid: String(uid).slice(0, 200),
      content: _lines.join('\n').slice(0, 7900),
      page: 'ur_surplus_appeal',
      version: (window._GAME_LOADED_VERSION || 'unknown'),
      timestamp: Date.now(),
      _userText: _reason,
      _hasConsoleDump: false,
      categories: ['data_incorrect'],
      // 結構化欄位（供 GM 後台日後做專屬列表；既有 BUG 卡忽略不影響）
      _urAppeal: {
        merit:    String(_p.merit || ''),
        expected: parseInt(_p.expected, 10) || 0,
        actual:   parseInt(_p.actual, 10) || _names.length,
        surplus:  parseInt(_p.surplus, 10) || 0,
        nickname: _nick.slice(0, 60),
        heroes:   _names.slice(0, 20),
        at:       Date.now()
      }
    });
    console.info('[UR 超額申訴 v5.92.0] 已送出 id=' + _id + ' uid=' + String(uid).slice(0,8));
    return { ok:true, id:_id };
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v5.99.0（老師 2026-08-21 需求）— 學生回覆「召喚卷自我確認」
  // ────────────────────────────────────────────────────────────────────
  // ★ 刻意複用既有 bugReports 集合（同 v5.92.0 申訴的做法）：
  //   firestore.rules 既有條款＝學生 create ＋ 讀自己的 ＋ GM update(adminReply)
  //   ⇒ **零 rules 改動**，且 GM 端既有「🐛 錯誤回報」卡可直接回覆、
  //     學生端既有「📩 老師的回覆」右下角小視窗＋鈴聲自動生效。
  // ⚠ 本函式**不會動任何資產**：純粹把學生的回答記錄下來給老師看。
  // ════════════════════════════════════════════════════════════════════
  window._ASSET_SELFCHECK_OPTS = {
    have_unused:   '① 有拿到召喚卷，還沒使用',
    used_no_item:  '② 有拿到召喚卷，使用了但沒有獲得角色／至寶',
    used_not_gone: '③ 老師只發 N 張，但使用之後又多出 1 張，所以繼續召喚出角色／至寶（BUG）',
    never_got:     '④ 沒有拿到召喚卷',
    other:         '⑤ 其他（見說明）'
  };
  window._fbSubmitAssetSelfCheck = async function(uid, payload){
    if(!uid) throw new Error('缺少 uid');
    const _p = payload || {};
    const _ans = Array.isArray(_p.answers) ? _p.answers : [];
    if(!_ans.length) throw new Error('沒有任何回答');
    const _note = String(_p.note || '').slice(0, 200);
    // ★ v5.99.0（老師修正①）— 有沒有「系統判定相符、學生仍主張沒拿到」的項目？
    //   有的話整筆標成申訴，老師才不必逐字讀內容就能分流。
    const _disputed = _ans.some(function(a){ return a && a.disputed; });
    const _id = 'bug_' + Date.now() + '_' + Math.random().toString(36).slice(2, 8);
    const _email = (window._fbUser && window._fbUser.email) || '';
    const _nick  = String(_p.nickname || window._playerNickname || '');
    const _lines = [];
    _lines.push('## 🎫 召喚卷確認 — 學生回覆' + (_disputed ? '（⚠ 含申訴：系統判定相符，學生主張沒拿到）' : ''));
    _ans.forEach(function(a){
      const _lbl = window._ASSET_SELFCHECK_OPTS[String(a && a.opt)] || String((a && a.opt) || '(未選)');
      let _tail = '';
      if(a && String(a.opt) === 'used_no_item'){
        _tail = (a.disputed
          ? '　→ ⚠ 系統稽查：數量相符（短少 0）⇒ **本次不補發**，學生仍主張沒拿到，請人工再查'
          : ('　→ ✅ 系統稽查：確實短少 ' + (parseInt(a.shortage, 10) || 0) + ' ⇒ 應補發'));
      }
      _lines.push('- ' + String((a && a.label) || '') + '：' + _lbl + _tail);
    });
    if(_note){ _lines.push(''); _lines.push('- 學生補充說明：' + _note); }
    _lines.push('');
    _lines.push('## 📊 老師寄出通知時的對帳');
    (Array.isArray(_p.assets) ? _p.assets : []).forEach(function(a){
      if(!a) return;
      _lines.push('- ' + String(a.label || '') + '：發出 ' + (parseInt(a.granted,10)||0)
        + ' 張／學生已領 ' + (parseInt(a.quota,10)||0)
        + ' 張／帳本實查用掉 ' + (parseInt(a.used,10)||0)
        + ' 次／背包還剩 ' + (parseInt(a.left,10)||0)
        + ' 張／目前持有 ' + (parseInt(a.actual,10)||0)
        + (a.flagText ? ('　[' + String(a.flagText) + ']') : ''));
    });
    _lines.push('');
    _lines.push('## 👤 帳號');
    _lines.push('- 暱稱：' + (_nick || '(未設定)'));
    _lines.push('- 信箱：' + _email);
    _lines.push('- 版本：' + (window._GAME_LOADED_VERSION || 'unknown'));
    _lines.push('- 送出時間：' + new Date().toLocaleString('zh-TW'));
    _lines.push('');
    _lines.push('⚠ 這是學生的自我回報，**系統沒有變更任何資料**，請老師據此判斷後續補發或收回。');
    await setDoc(doc(_fbDb, 'bugReports', _id), {
      id: _id,
      email: _email.slice(0, 200),
      uid: String(uid).slice(0, 200),
      content: _lines.join('\n').slice(0, 7900),
      page: 'asset_self_check',
      version: (window._GAME_LOADED_VERSION || 'unknown'),
      timestamp: Date.now(),
      _userText: _note,
      _hasConsoleDump: false,
      categories: ['data_incorrect'],
      // 結構化欄位（供 GM 端聚合統計；既有 BUG 卡忽略不影響）
      _assetSelfCheck: {
        answers:  _ans.slice(0, 4).map(function(a){
          return { kind:String((a && a.kind) || ''), opt:String((a && a.opt) || ''),
                   shortage:(parseInt(a && a.shortage, 10) || 0),
                   disputed:((a && a.disputed) ? 1 : 0) };
        }),
        disputed: _disputed ? 1 : 0,
        note:     _note.slice(0, 200),
        nickname: _nick.slice(0, 60),
        at:       Date.now()
      }
    });
    console.info('[召喚卷自我確認 v5.99.0] 已送出 id=' + _id + ' uid=' + String(uid).slice(0,8));
    return { ok:true, id:_id };
  };

  // ★ v3.16.86 — 學生端「不是我的」至寶可逆移除(鏡像 _fbStudentDisownHeroes)
  //   從 taiwanTreasureData 移除指定鍵(整包覆蓋 + _s 防 merge 殘留)、暫存原始至寶資料供 GM 復原、
  //   帳本寫可逆標記 audit_error_recovered(disownedByStudent)→ GM 可由帳號救援申請補回(_fbAdminRestoreLostTreasures)。
  window._fbStudentDisownTreasures = async function(uid, ids){
    if(!uid) return { ok:false, reason:'no_uid' };
    const _ids = Array.isArray(ids) ? ids.filter(Boolean) : [];
    if(!_ids.length) return { ok:true, removed:[] };
    const _ref = doc(_fbDb, 'players', uid);
    try{
      const _snap = await getDoc(_ref);
      if(!_snap.exists()) return { ok:false, reason:'player_not_found' };
      const _d = _snap.data() || {};
      let _tre = (_d.taiwanTreasureData && typeof _d.taiwanTreasureData === 'object')
        ? JSON.parse(JSON.stringify(_d.taiwanTreasureData)) : {};
      if(!(_d.taiwanTreasureData && typeof _d.taiwanTreasureData === 'object') && typeof _d.taiwanTreasureData_s === 'string'){
        try{ const _p = JSON.parse(_d.taiwanTreasureData_s); if(_p && typeof _p === 'object') _tre = _p; }catch(_){}
      }
      const _toRemove = _ids.filter(function(id){ return _tre && Object.prototype.hasOwnProperty.call(_tre, id); });
      if(_toRemove.length === 0) return { ok:true, removed:[] };
      // ★★ v5.130.0(老師裁定甲)— 改走三槽同寫單一真相 ★★
      //   舊版只寫主檔 ⇒ live/safe 至寶殘留(可能還 equippedTo)+ 沒帳本 ⇒ 自助移除的至寶反覆復活。
      //   暫存原始至寶資料(含投資)每槽各存供 GM 精確復原;沿用原設計不蓋 restore(學生 session 內)。
      const _now = Date.now();
      const _res130 = await window._lxpsAdminApplyAllSlots(uid, function(d, slot){
        var p = window._lxpsSlotTreasurePatch(d, _toRemove, {
          at:_now, mode:'remove', by:'student', uid:uid, slot:slot,
          source:'audit_error_recovered',
          extra:{ disownedByStudent:true, reason:'學生自我審查·確認「不是我的」·自助移除至寶(可由帳號救援申請補回)' }
        });
        if(!p) return null;
        var _slotTre = null;
        try{
          if(typeof d.taiwanTreasureData_s === 'string'){ var _q = JSON.parse(d.taiwanTreasureData_s); if(_q && typeof _q === 'object') _slotTre = _q; }
        }catch(_){}
        if(!_slotTre && d.taiwanTreasureData && typeof d.taiwanTreasureData === 'object') _slotTre = d.taiwanTreasureData;
        var _stash = (d._auditRecoveredTreasureData && typeof d._auditRecoveredTreasureData === 'object')
          ? Object.assign({}, d._auditRecoveredTreasureData) : {};
        _toRemove.forEach(function(id){ try{ if(_slotTre && _slotTre[id]) _stash[id] = _slotTre[id]; }catch(_){} });
        p._auditRecoveredTreasureData = _stash;
        return p;
      }, { label:'學生自助移除至寶', stampRestore:false });
      console.log('[_fbStudentDisownTreasures v5.130.0] ✅ uid=' + uid + ' 學生自助移除至寶 ' + _toRemove.length + ' 件:', _toRemove);
      return { ok:true, removed:_toRemove, slots:_res130 };
    }catch(e){
      console.error('[_fbStudentDisownTreasures] 失敗', e);
      return { ok:false, reason:(e && (e.code||e.message)) || 'exception' };
    }
  };
  //   並把每隻稀有英雄依「獲得方式」分四類,讓 GM 逐隻勾選收回。
  // ─────────────────────────────────────────────────────────────────────
  // 這是 v3.13.39「🧹 帳號汙染掃描(只掃 SSR、只抓無紀錄)」的強化超集合:
  //   ① 同時掃 SSR + SR(SR 也會被跨帳號 union 汙染)
  //   ② 每隻稀有英雄依 _heroUnlockHistory 分四類:
  //        boss     = BOSS 解鎖(貓空/黑暗球/日本 BOSS/八岐巫女/通關掉落/世界BOSS…)
  //        summon   = 召喚(星空抽卡 / 入場券抽卡)
  //        gm       = GM 補償 / 給予(補償 / admin_grant / 學生設計英雄 / 活動任務)
  //        pollution= 不明汙染(roster 內有、但「查無任何合法解鎖紀錄」→ 跨帳號 union 的指紋)
  //      (有紀錄但來源是空字串/未知 → 'other' 未標記,不算汙染,避免誤刪舊資料)
  //   ③ 兩種命中訊號(任一達門檻即列出):
  //        pollutionCount = 不明汙染稀有數(跨帳號汙染主訊號)
  //        recentCount    = 窗內(預設 1 天)「有紀錄」的稀有新增數(重複領獎 BUG / 活動爆發訊號)
  // ⚠ 邊界:v3.11.10(2026-05-28)之前解鎖的舊稀有英雄沒有歷史紀錄,會被歸到「不明汙染」。
  //   故本工具只「列出候選 + 完整來源明細」,實際收回一律由 GM 逐隻勾選確認(見 admin_panel.js)。
  // ⚠ 本函式「唯讀」,不寫任何資料;收回走既有 _fbAdminBulkRemoveHeroes(已含 5 養成表清理 +
  //   反汙染信號 + 稽核 + 冪等)。
  // ════════════════════════════════════════════════════════════════════
  function _rareSrcCat(source){
    const s = String(source == null ? '' : source);
    if(!s) return 'other';                       // 有紀錄但無來源標記 = 舊資料,非汙染
    if(s === 'admin_delete') return 'deleted';   // 刪除稽核紀錄(不算擁有來源)
    if(s.indexOf('summon') === 0 || s.indexOf('ticket') === 0) return 'summon';
    if(s === 'admin_grant' || s === 'compensation' || s === 'student_designer' || s === 'event_quest') return 'gm';
    if(s === 'maokong_50pct' || s === 'darkorb_5pct' || s === 'japan_boss_5pct' || s === 'yamata_miko_5pct'
       || s === 'taiwan_clear' || s === 'japan_clear' || s === 'boss_drop' || s === 'wb_drop'
       || s.indexOf('wb_') === 0 || s.indexOf('boss_') === 0) return 'boss';
    return 'other';
  }
  // ★ v5.93.0 — 提升為跨 script 區塊的單一真相：UR 券稽核（_fbAdminScanURTicketAudit）
  //   也要判斷「這隻是不是老師給的」。⚠ 刻意不另立第二張表 —— v5.92.0 的 _pageLabel /
  //   _pageLabel2 教訓：同一份對照表在兩處各有一份，只補一邊不會報錯、驗證腳本也抓不到。
  //   ★ 關鍵：舊帳號老師補發寫的是 source='compensation'（v3.17.3 之後才改 'admin_grant'），
  //     兩者本表都已歸為 'gm'，漏認任一個都會把老師親手發的 UR 判成來路不明＝冤枉學生。
  try{ window._lxpsRareSrcCat = _rareSrcCat; }catch(_){}

  window._fbAdminScanRareSpikes = async function(opts){
    opts = opts || {};
    const _windowMs = parseInt(opts.windowMs, 10) || (24 * 60 * 60 * 1000); // 預設 1 天
    const _threshold = parseInt(opts.threshold, 10) || 5;
    const _ssrSet = new Set(window.SUMMON_RARE_HEROES || []);
    const _srSet  = new Set(window.SUMMON_SR_HEROES || []);
    if(!_ssrSet.size && !_srSet.size){
      console.warn('[_fbAdminScanRareSpikes] SSR/SR 集合都未就緒(window.SUMMON_RARE_HEROES / SUMMON_SR_HEROES),無法判定稀有度');
      return { ok:false, reason:'no_rarity_set' };
    }
    const _rarityOf = (n) => _ssrSet.has(n) ? 'SSR' : (_srSet.has(n) ? 'SR' : null);
    const _now = Date.now();
    const _cutoff = _now - _windowMs;
    const _catRank = (c) => (c==='pollution'?0:(c==='other'?1:(c==='boss'?2:(c==='summon'?3:(c==='gm'?4:9)))));
    try{
      const snap = await getDocs(collection(_fbDb, 'players'));
      const _out = [];
      snap.forEach(docSnap => {
        try{
          const d = docSnap.data() || {};
          const _unlocked = Array.isArray(d.unlockedHeroes) ? d.unlockedHeroes : [];
          if(!_unlocked.length) return;

          // 解析解鎖歷史:每隻取「最近一次的合法(非 admin_delete)紀錄」
          let _hist = Array.isArray(d._heroUnlockHistory) ? d._heroUnlockHistory : null;
          if(!_hist && typeof d._heroUnlockHistory_s === 'string'){
            try{ _hist = JSON.parse(d._heroUnlockHistory_s); }catch(_){ _hist = null; }
          }
          const _legitByName = {};  // name -> { source, at }
          if(Array.isArray(_hist)){
            _hist.forEach(h => {
              if(!h || !h.name) return;
              if(h.source === 'admin_delete') return;
              const at = (typeof h.at === 'number') ? h.at : 0;
              const cur = _legitByName[h.name];
              if(!cur || at >= cur.at) _legitByName[h.name] = { source: h.source || '', at };
            });
          }

          // 逐隻挑出 SSR/SR,分類
          const _heroes = [];
          let _ssrTotal = 0, _srTotal = 0, _pollutionCount = 0, _recentCount = 0;
          const _catCounts = { boss:0, summon:0, gm:0, pollution:0, other:0 };
          _unlocked.forEach(name => {
            const _rar = _rarityOf(name);
            if(!_rar) return;  // 只看 SSR/SR
            if(_rar === 'SSR') _ssrTotal++; else _srTotal++;
            const _rec = _legitByName[name];
            let _cat, _src, _at;
            if(_rec){
              _src = _rec.source || '';
              _at  = _rec.at || 0;
              _cat = _rareSrcCat(_src);
              if(_cat === 'deleted') _cat = 'pollution'; // 只剩刪除紀錄卻仍在 roster → 視為汙染殘留
            } else {
              _src = null; _at = 0; _cat = 'pollution';  // 完全查無合法紀錄 → 不明汙染
            }
            if(_cat === 'pollution') _pollutionCount++;
            const _recent = (_at >= _cutoff && _at > 0);
            if(_recent) _recentCount++;
            if(_catCounts[_cat] == null) _catCounts[_cat] = 0;
            _catCounts[_cat]++;
            _heroes.push({ name, rarity:_rar, cat:_cat, source:_src, at:_at, recent:_recent });
          });

          // 命中條件:不明汙染數 或 窗內新增數 任一 ≥ 門檻
          if(_pollutionCount >= _threshold || _recentCount >= _threshold){
            _heroes.sort((a,b) => {
              const ra = _catRank(a.cat), rb = _catRank(b.cat);
              if(ra !== rb) return ra - rb;
              if((b.at||0) !== (a.at||0)) return (b.at||0) - (a.at||0);
              return String(a.name).localeCompare(String(b.name));
            });
            const _hl = (d.heroLevels && typeof d.heroLevels === 'object') ? d.heroLevels : {};
            const _maxLv = Math.max(0, ...Object.values(_hl).map(v => v || 0));
            _out.push({
              uid: docSnap.id,
              name: d.displayName || '',
              email: d.email || '',
              totalUnlocked: _unlocked.length,
              ssrTotal: _ssrTotal,
              srTotal: _srTotal,
              pollutionCount: _pollutionCount,
              recentCount: _recentCount,
              catCounts: _catCounts,
              historyCount: Array.isArray(_hist) ? _hist.length : 0,
              knowledgeCoins: d.knowledgeCoins || 0,
              maxHeroLv: _maxLv,
              heroes: _heroes,
            });
          }
        }catch(_eRow){ /* 單筆失敗略過,不影響整體掃描 */ }
      });
      // 排序:汙染多的優先,其次窗內新增多的
      _out.sort((a,b) => (b.pollutionCount - a.pollutionCount) || (b.recentCount - a.recentCount));
      console.log('[_fbAdminScanRareSpikes] 掃描完成 windowMs=' + _windowMs + ' threshold=' + _threshold
        + ' → 命中 ' + _out.length + ' 個帳號(SSR 集合 ' + _ssrSet.size + ' / SR 集合 ' + _srSet.size + ')');
      return { ok:true, windowMs:_windowMs, threshold:_threshold, count:_out.length, players:_out, scannedAt:_now };
    }catch(e){
      console.error('[_fbAdminScanRareSpikes] 掃描失敗', e);
      return { ok:false, reason:(e && (e.code||e.message)) || 'exception' };
    }
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.13.47(2026-06-04) — 皮膚(英雄肖像)復原 / 稽核 後端
  //   問題:部分學生帳號被汙染後,花錢買的皮膚消失。
  //   分兩類:① 等級解鎖皮膚 = 可由 heroLevels 重新推導(登入時 _scanAllLvUnlockPortraits 自動補)
  //          ② 商店購買皮膚 = 無法重推,只能靠「跨槽聯集 + 紀錄稽核 + 手動補發」復原。
  //   本組函式:讀三槽 → 稽核 / 跨槽聯集復原 / 手動補發,皆寫 main+live+safe(merge,只增不減)。
  // ════════════════════════════════════════════════════════════════════
  async function _adminReadSlotSafe(ref){
    try{ const s = await getDocFromServer(ref); return s.exists() ? (s.data()||{}) : null; }
    catch(_){ try{ const s2 = await getDoc(ref); return s2.exists() ? (s2.data()||{}) : null; }catch(__){ return null; } }
  }
  window._fbAdminAuditSkins = async function(uid){
    if(!uid) throw new Error('必須提供 uid');
    const _main = await _adminReadSlotSafe(doc(_fbDb,'players',uid));
    const _live = await _adminReadSlotSafe(doc(_fbDb,'players',uid,'saves','live'));
    const _safe = await _adminReadSlotSafe(doc(_fbDb,'players',uid,'saves','safe'));
    const _portOf   = (d) => (d && d.heroPortraitUnlocked && typeof d.heroPortraitUnlocked==='object') ? d.heroPortraitUnlocked : {};
    const _choiceOf = (d) => (d && d.heroPortraitChoice   && typeof d.heroPortraitChoice==='object')   ? d.heroPortraitChoice   : {};
    // 跨槽聯集已解鎖肖像
    const _union = {};
    [_main,_live,_safe].forEach(d => {
      const p = _portOf(d);
      Object.keys(p).forEach(h => {
        const set = new Set(_union[h]||[]);
        (Array.isArray(p[h])?p[h]:[]).forEach(id => { if(id) set.add(id); });
        _union[h] = Array.from(set);
      });
    });
    // 皮膚解鎖紀錄(三槽合併,hero|id 去重)
    const _histOf = (d) => {
      if(!d) return [];
      let h = Array.isArray(d._skinUnlockHistory) ? d._skinUnlockHistory : null;
      if(!h && typeof d._skinUnlockHistory_s === 'string'){ try{ h = JSON.parse(d._skinUnlockHistory_s); }catch(_){ h = null; } }
      return Array.isArray(h) ? h : [];
    };
    const _seen = new Set(); const _histMerged = [];
    [_histOf(_main),_histOf(_live),_histOf(_safe)].forEach(arr => arr.forEach(e => {
      if(!e || !e.hero || !e.id) return; const k=e.hero+'|'+e.id; if(_seen.has(k)) return; _seen.add(k); _histMerged.push(e);
    }));
    // 選擇:safe < main < live(live 最新),非空才覆蓋
    const _choice = {};
    [_choiceOf(_safe),_choiceOf(_main),_choiceOf(_live)].forEach(c => {
      Object.keys(c).forEach(h => { if(c[h] && typeof c[h]==='string') _choice[h] = c[h]; });
    });
    return {
      ok:true, uid,
      email: (_main&&_main.email)||(_live&&_live.email)||'',
      name:  (_main&&_main.displayName)||'',
      slots: {
        main: { exists: !!_main, portraits: _portOf(_main), choice: _choiceOf(_main) },
        live: { exists: !!_live, portraits: _portOf(_live), choice: _choiceOf(_live) },
        safe: { exists: !!_safe, portraits: _portOf(_safe), choice: _choiceOf(_safe) },
      },
      unionPortraits: _union,
      choice: _choice,
      heroLevels: (_live&&_live.heroLevels)||(_main&&_main.heroLevels)||{},
      skinHistory: _histMerged,
    };
  };

  // 一鍵跨槽復原:把三槽的已解鎖皮膚聯集後寫回三槽(只增不減),選擇也一併還原。
  //   不下 _adminRescueSignal(避免整份清本地),靠載入時 union-merge 自然帶回學生端。
  window._fbAdminRecoverSkins = async function(uid){
    if(!uid) throw new Error('必須提供 uid');
    const _adminEmail = (window._fbUser && window._fbUser.email) || 'admin';
    const _audit = await window._fbAdminAuditSkins(uid);
    const _union  = _audit.unionPortraits || {};
    const _choice = _audit.choice || {};
    let _total = 0; Object.keys(_union).forEach(h => _total += (_union[h]||[]).length);
    const _now = Date.now();
    await setDoc(doc(_fbDb,'players',uid), {
      heroPortraitUnlocked: _union,
      heroPortraitChoice:   _choice,
      _adminLastAction: { type:'recover_skins', at:_now, by:_adminEmail, portraitCount:_total },
    }, { merge:true });
    await setDoc(doc(_fbDb,'players',uid,'saves','live'), { heroPortraitUnlocked:_union, heroPortraitChoice:_choice }, { merge:true });
    try{ await setDoc(doc(_fbDb,'players',uid,'saves','safe'), { heroPortraitUnlocked:_union, heroPortraitChoice:_choice }, { merge:true }); }catch(_){}
    console.log('[_fbAdminRecoverSkins] ✅ uid=' + uid + ' 跨槽聯集復原皮膚 ' + _total + ' 個');
    return { ok:true, uid, portraitCount:_total, perHero:_union, choice:_choice };
  };

  // 手動補發指定皮膚:grants = [{hero, id}, ...](給跨槽都查無、但學生有合理憑證的情況)
  window._fbAdminGrantSkins = async function(uid, grants){
    if(!uid) throw new Error('必須提供 uid');
    const _glist = Array.isArray(grants) ? grants.filter(g => g && g.hero && g.id) : [];
    if(!_glist.length) return { ok:false, reason:'no_grants' };
    const _adminEmail = (window._fbUser && window._fbUser.email) || 'admin';
    const _ref = doc(_fbDb,'players',uid);
    const _snap = await getDoc(_ref);
    const _d = _snap.exists() ? (_snap.data()||{}) : {};
    const _port = (_d.heroPortraitUnlocked && typeof _d.heroPortraitUnlocked==='object')
      ? JSON.parse(JSON.stringify(_d.heroPortraitUnlocked)) : {};
    let _skHist = Array.isArray(_d._skinUnlockHistory) ? _d._skinUnlockHistory.slice() : null;
    if(!_skHist && typeof _d._skinUnlockHistory_s==='string'){ try{ _skHist=JSON.parse(_d._skinUnlockHistory_s); }catch(_){ _skHist=null; } }
    if(!Array.isArray(_skHist)) _skHist = [];
    const _now = Date.now();
    let _added = 0;
    _glist.forEach(g => {
      if(!_port[g.hero]) _port[g.hero] = [];
      if(!_port[g.hero].includes(g.id)){ _port[g.hero].push(g.id); _added++; }
      if(!_skHist.some(e => e && e.hero===g.hero && e.id===g.id)){
        _skHist.push({ hero:g.hero, id:g.id, source:'admin_grant', at:_now, by:_adminEmail });
      }
    });
    if(_skHist.length > 300) _skHist = _skHist.slice(-300);
    await setDoc(_ref, {
      heroPortraitUnlocked: _port,
      _skinUnlockHistory: _skHist,
      _skinUnlockHistory_s: JSON.stringify(_skHist),
      _adminLastAction: { type:'grant_skins', at:_now, by:_adminEmail, count:_added },
    }, { merge:true });
    await setDoc(doc(_fbDb,'players',uid,'saves','live'), { heroPortraitUnlocked:_port }, { merge:true });
    try{ await setDoc(doc(_fbDb,'players',uid,'saves','safe'), { heroPortraitUnlocked:_port }, { merge:true }); }catch(_){}
    console.log('[_fbAdminGrantSkins] ✅ uid=' + uid + ' 補發皮膚 ' + _added + ' 個');
    return { ok:true, uid, added:_added, perHero:_port };
  };

  // ★ v3.13.48 — 知識幣稽核:跨 main/live/safe 三槽 + 帳本,找出「歷史可證的最高持有」與目前餘額。
  //   背景:健康度守門不擋知識幣倒退(鐵律 1.51,正常消費),所以幣會掉是正常的,
  //         但若被汙染(別帳號低幣覆蓋)也會掉。本工具給老師判斷依據。
  //   ⚠ 帳本(_coinTransactions)是 v3.13.48 起才記;更早的高峰只能從三槽 _dataSummary/_prevDataSummary 快照推估。
  window._fbAdminAuditCoins = async function(uid){
    if(!uid) throw new Error('必須提供 uid');
    const _main = await _adminReadSlotSafe(doc(_fbDb,'players',uid));
    const _live = await _adminReadSlotSafe(doc(_fbDb,'players',uid,'saves','live'));
    const _safe = await _adminReadSlotSafe(doc(_fbDb,'players',uid,'saves','safe'));
    const _snaps = [];
    const _pull = (d, slot) => {
      if(!d) return;
      const _sum = (d._dataSummary && typeof d._dataSummary.coins==='number') ? d._dataSummary.coins : null;
      const _prev = (d._prevDataSummary && typeof d._prevDataSummary.coins==='number') ? d._prevDataSummary.coins : null;
      const _cur = (typeof d.knowledgeCoins==='number') ? d.knowledgeCoins : (_sum!=null ? _sum : 0);
      _snaps.push({ slot, knowledgeCoins:_cur, summaryCoins:_sum, prevSummaryCoins:_prev, savedAt:d.savedAt||0 });
    };
    _pull(_main,'main'); _pull(_live,'live'); _pull(_safe,'safe');
    // 帳本:三槽取最長的一份
    const _ledOf = (d) => {
      if(!d) return [];
      let l = Array.isArray(d._coinTransactions) ? d._coinTransactions : null;
      if(!l && typeof d._coinTransactions_s==='string'){ try{ l = JSON.parse(d._coinTransactions_s); }catch(_){ l=null; } }
      return Array.isArray(l) ? l : [];
    };
    let _ledger = _ledOf(_main);
    [_live,_safe].forEach(d => { const l=_ledOf(d); if(l.length>_ledger.length) _ledger=l; });
    let _maxObserved = 0;
    _snaps.forEach(s => { [s.knowledgeCoins,s.summaryCoins,s.prevSummaryCoins].forEach(v => { if(typeof v==='number' && v>_maxObserved) _maxObserved=v; }); });
    _ledger.forEach(e => { if(e && typeof e.balance==='number' && e.balance>_maxObserved) _maxObserved=e.balance; });
    const _cur = _snaps.length ? Math.max.apply(null, _snaps.map(s => s.knowledgeCoins||0)) : 0;
    return {
      ok:true, uid,
      email:(_main&&_main.email)||(_live&&_live.email)||'',
      name:(_main&&_main.displayName)||'',
      currentCoins:_cur, maxObserved:_maxObserved,
      snapshots:_snaps,
      ledger:_ledger.slice(-200).sort((a,b)=>(b.at||0)-(a.at||0)),
      ledgerCount:_ledger.length,
    };
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.13.47(2026-06-04) — SSR/SR「同台 iPad 汙染分組」偵測(全校掃描)
  //   原理:跨帳號「整份複製」會把 _heroUnlockHistory 一起複製,每筆紀錄帶建立者 uid 前 12 碼。
  //     → 某帳號的 SSR 紀錄 creatorUid ≠ 自己 uid = 那筆從別人複製來的(被汙染);
  //       creatorUid 的主人 = 原始解鎖者。
  //   分組:依「SSR/SR 解鎖序列(依時間排序)」相同分組(老師選項 A+B):
  //     A 鐵證 = 同序列 + 時間戳完全相同 (exactTimeMatch) 或 紀錄 creatorUid 指向同一人 (isPollution)
  //     B 疑似 = 同序列(容忍時間戳微差)
  //   排序:鐵證群 → 成員多 → 序列長,優先列出。班級座號顯示在 admin_panel.js。
  // ════════════════════════════════════════════════════════════════════
  window._fbAdminScanPollutionClusters = async function(opts){
    opts = opts || {};
    const _minSeq = parseInt(opts.minSeq,10) || 2;          // 序列至少 N 隻才分組(避免單隻雜訊)
    const _minMembers = parseInt(opts.minMembers,10) || 2;  // 至少 N 位玩家共用同序列
    const _ssrSet = new Set(window.SUMMON_RARE_HEROES || []);
    const _srSet  = new Set(window.SUMMON_SR_HEROES || []);
    if(!_ssrSet.size && !_srSet.size) return { ok:false, reason:'no_rarity_set' };
    const _rarityOf = (n) => _ssrSet.has(n) ? 'SSR' : (_srSet.has(n) ? 'SR' : null);
    try{
      const snap = await getDocs(collection(_fbDb,'players'));
      const _players = [];
      snap.forEach(docSnap => {
        try{
          const d = docSnap.data() || {};
          const _unlocked = Array.isArray(d.unlockedHeroes) ? d.unlockedHeroes : [];
          if(!_unlocked.length) return;
          let _hist = Array.isArray(d._heroUnlockHistory) ? d._heroUnlockHistory : null;
          if(!_hist && typeof d._heroUnlockHistory_s==='string'){ try{ _hist=JSON.parse(d._heroUnlockHistory_s); }catch(_){ _hist=null; } }
          const _recByName = {};
          if(Array.isArray(_hist)) _hist.forEach(h => {
            if(!h || !h.name || h.source==='admin_delete') return;
            const at = (typeof h.at==='number')?h.at:0;
            const cur = _recByName[h.name];
            if(!cur || at >= cur.at) _recByName[h.name] = { source:h.source||'', at, uid:(h.uid||'') };
          });
          const _ownUid = (docSnap.id||'').slice(0,12);
          const _hl = (d.heroLevels && typeof d.heroLevels==='object') ? d.heroLevels : {};
          const _ssrList = [];
          _unlocked.forEach(name => {
            const _rar = _rarityOf(name); if(!_rar) return;
            const _rec = _recByName[name];
            _ssrList.push({
              name, rarity:_rar,
              lv: (_hl[name]||0),                         // ★ v3.13.49b — 帶上等級供後台顯示/判斷誰是真玩家
              at: _rec ? _rec.at : 0,
              source: _rec ? (_rec.source||'') : null,
              creatorUid: _rec ? (_rec.uid||'') : '',
              cat: _rec ? _rareSrcCat(_rec.source||'') : 'pollution',
            });
          });
          if(_ssrList.length < _minSeq) return;
          _ssrList.sort((a,b)=> (a.at||0)-(b.at||0) || String(a.name).localeCompare(String(b.name)));
          const _orderSig = _ssrList.map(x=>x.name).join('>');
          const _timeSig  = _ssrList.map(x=> x.name+'@'+(x.at||0)).join('>');
          const _clusterHeroLvSum = _ssrList.reduce((s,x)=> s+(x.lv||0), 0);   // ★ v3.13.49b — 這組稀有英雄的等級總和
          const _totalHeroLv = Object.values(_hl).reduce((s,v)=> s+(v||0), 0); // ★ v3.13.49b — 全英雄等級總和
          // ★ v3.13.63 — 至寶數 + 獎章數(供「豐富度」原主預判);至寶只比對、不改變分群(鐵律 3.b)
          const _ownedTre = Array.isArray(d.ownedTreasures) ? d.ownedTreasures : [];
          const _treasureCount = _ownedTre.length;
          // 鐵證至寶:cat='pollution'(查無紀錄) 或 creatorUid 非本人(從別帳號複製來)
          const _ironTreasures = _ownedTre
            .filter(t => t && ((t.cat==='pollution') || (t.creatorUid && t.creatorUid !== _ownUid)))
            .map(t => ({ id:t.id, name:t.name||t.id, lv:t.lv||1, creatorUid:t.creatorUid||'', cat:t.cat||'' }));
          let _medalObj = (d.playerMedals && typeof d.playerMedals==='object') ? d.playerMedals : null;
          if(!_medalObj && typeof d.playerMedals_s==='string'){ try{ _medalObj = JSON.parse(d.playerMedals_s); }catch(_){ _medalObj = null; } }
          const _medalCount = _medalObj ? Object.keys(_medalObj).length : 0;
          let _rosterName = '';
          try{ const _re = (typeof window._getRosterEntry==='function') ? window._getRosterEntry((d.email||'').toLowerCase()) : null; if(_re && _re.fullName) _rosterName = _re.fullName; }catch(_){}
          _players.push({
            uid: docSnap.id, ownUid:_ownUid,
            email: d.email||'', name: d.displayName||'', rosterName:_rosterName,
            ssrList:_ssrList, orderSig:_orderSig, timeSig:_timeSig,
            totalUnlocked:_unlocked.length,
            maxHeroLv: Math.max(0, ...Object.values(_hl).map(v=>v||0)),
            totalHeroLv:_totalHeroLv, clusterHeroLvSum:_clusterHeroLvSum,
            knowledgeCoins: d.knowledgeCoins||0,
            treasureCount:_treasureCount, medalCount:_medalCount, ironTreasures:_ironTreasures,
          });
        }catch(_){}
      });
      const _byOrder = {};
      _players.forEach(p => { (_byOrder[p.orderSig] = _byOrder[p.orderSig] || []).push(p); });
      const _clusters = [];
      Object.keys(_byOrder).forEach(sig => {
        const members = _byOrder[sig];
        if(members.length < _minMembers) return;
        const _seqLen = sig.split('>').length;
        if(_seqLen < _minSeq) return;
        const _uidTally = {};
        let _anyCreatorUid = false;
        members.forEach(m => m.ssrList.forEach(s => { if(s.creatorUid){ _uidTally[s.creatorUid] = (_uidTally[s.creatorUid]||0)+1; _anyCreatorUid = true; } }));
        let _domUid = null, _domN = 0;
        Object.keys(_uidTally).forEach(u => { if(_uidTally[u] > _domN){ _domN = _uidTally[u]; _domUid = u; } });
        // ★ v3.13.49b — 時間戳全相同「只有在真的有時間戳(非全 0)」時才算鐵證:
        //   否則一堆 5/28 前「無紀錄(at=0)」的舊英雄會讓 timeSig 全變 '@0' 假性相同 → 誤判鐵證群。
        const _anyRealTime = members.some(m => (m.ssrList||[]).some(s => (s.at||0) > 0));
        const _timeSigSet = new Set(members.map(m=>m.timeSig));
        const _realExactTimeMatch = (_timeSigSet.size === 1) && _anyRealTime;
        let _hasForeignCopy = false;
        members.forEach(m => {
          let self=0, foreign=0;
          m.ssrList.forEach(s => { if(!s.creatorUid) return; if(s.creatorUid === m.ownUid) self++; else foreign++; });
          m.selfCreated = self; m.foreignCreated = foreign;
          // ★ v3.13.63 — 鐵證英雄:creatorUid 為他人(從別帳號複製來)= 建議刪除清單用的鐵證
          m.ironHeroes = (m.ssrList||[])
            .filter(s => s.creatorUid && s.creatorUid !== m.ownUid)
            .map(s => ({ name:s.name, rarity:s.rarity, lv:s.lv||0, creatorUid:s.creatorUid }));
          // ★ v3.13.63 — 豐富度分數(僅供顯示與「無 creatorUid 鐵證」時推測原主;權重偏向難造假的投入)
          //   稀有英雄等級總和×5 + 全英雄等級×1 + 最高Lv×3 + 解鎖數×10 + 至寶數×30 + 獎章數×20 + 知識幣/100
          m.richnessScore = (m.clusterHeroLvSum||0)*5 + (m.totalHeroLv||0)*1 + (m.maxHeroLv||0)*3
            + (m.totalUnlocked||0)*10 + (m.treasureCount||0)*30 + (m.medalCount||0)*20
            + Math.floor((m.knowledgeCoins||0)/100);
          if(_domUid && m.ownUid === _domUid && self > 0) m.role = 'original';
          else if(foreign > 0 && _domUid && m.ownUid !== _domUid){ m.role = 'polluted'; _hasForeignCopy = true; }
          else m.role = 'unknown';
        });
        // ★ v3.13.49b / 強化 v3.13.63 — 完全沒有 creatorUid 鐵證時(英雄多為 5/28 前無紀錄, 無法用 creatorUid 判原主):
        //   改用「豐富度」推測誰是真玩家(原主):稀有英雄等級總和 → 全英雄等級 → 最高Lv → 總解鎖 →
        //   至寶數 → 獎章數 → 知識幣(都在玩家文件上、無需額外讀取;至寶/獎章較難造假, 提高準度)。
        //   並明確標為『推測(依豐富度)』。若最高分與第二名在所有關鍵指標都「完全分不出高下」
        //   (整份複製、等級/至寶/獎章都一樣)→ 維持 unknown, 老實告訴老師資料無法分辨, 不亂指原主。
        if(!_anyCreatorUid){
          const _ranked = members.slice().sort((a,b) =>
            ((b.clusterHeroLvSum||0) - (a.clusterHeroLvSum||0)) ||
            ((b.totalHeroLv||0)    - (a.totalHeroLv||0)) ||
            ((b.maxHeroLv||0)      - (a.maxHeroLv||0)) ||
            ((b.totalUnlocked||0)  - (a.totalUnlocked||0)) ||
            ((b.treasureCount||0)  - (a.treasureCount||0)) ||
            ((b.medalCount||0)     - (a.medalCount||0)) ||
            ((b.knowledgeCoins||0) - (a.knowledgeCoins||0)));
          const _top = _ranked[0], _second = _ranked[1];
          const _distinguishable = _top && _second && (
            (_top.clusterHeroLvSum||0) !== (_second.clusterHeroLvSum||0) ||
            (_top.totalHeroLv||0)    !== (_second.totalHeroLv||0) ||
            (_top.maxHeroLv||0)      !== (_second.maxHeroLv||0) ||
            (_top.totalUnlocked||0)  !== (_second.totalUnlocked||0) ||
            (_top.treasureCount||0)  !== (_second.treasureCount||0) ||
            (_top.medalCount||0)     !== (_second.medalCount||0));
          if(_distinguishable){
            _ranked.forEach((m,i) => { m.role = (i===0) ? 'likely_original' : 'likely_polluted'; });
          }
        }
        const _isPollution = _hasForeignCopy || _realExactTimeMatch;
        const _evidence = _hasForeignCopy ? 'creatorUid'
                        : (_realExactTimeMatch ? 'exactTime'
                        : (_anyCreatorUid ? 'creatorUid_no_foreign' : 'no_record'));
        _clusters.push({
          orderSig: sig, seqLen:_seqLen, memberCount: members.length,
          exactTimeMatch: _realExactTimeMatch, isPollution: _isPollution, originalUid: _domUid,
          hasCreatorEvidence: _anyCreatorUid, evidence: _evidence,
          members: members.map(m => ({
            uid:m.uid, email:m.email, name:m.name, rosterName:m.rosterName||'', ownUid:m.ownUid,
            role:m.role, selfCreated:m.selfCreated, foreignCreated:m.foreignCreated,
            ssrCount:m.ssrList.length, ssrList:m.ssrList,
            totalUnlocked:m.totalUnlocked, maxHeroLv:m.maxHeroLv,
            totalHeroLv:m.totalHeroLv||0, clusterHeroLvSum:m.clusterHeroLvSum||0,
            knowledgeCoins:m.knowledgeCoins,
            treasureCount:m.treasureCount||0, medalCount:m.medalCount||0,        // ★ v3.13.63 — 豐富度顯示
            ironHeroes:m.ironHeroes||[], ironTreasures:m.ironTreasures||[],       // ★ v3.13.63 — 鐵證清單用
            richnessScore:m.richnessScore||0,                                    // ★ v3.13.63 — 豐富度分數
          })),
        });
      });
      _clusters.sort((a,b) =>
        (Number(b.isPollution)-Number(a.isPollution)) ||
        (Number(b.exactTimeMatch)-Number(a.exactTimeMatch)) ||
        (b.memberCount-a.memberCount) ||
        (b.seqLen-a.seqLen)
      );
      // ★ v3.13.63 — 彙整「鐵證快速清單」(A):跨所有組,凡帶 creatorUid 鐵證(英雄/至寶為他人複製來)的帳號
      //   集中成一張優先處理表。每位玩家只屬於一組(orderSig 唯一),不需去重。
      const _ironList = [];
      _clusters.forEach(c => {
        (c.members||[]).forEach(m => {
          const _ih = m.ironHeroes||[], _it = m.ironTreasures||[];
          if(_ih.length || _it.length){
            _ironList.push({
              uid:m.uid, email:m.email, name:m.name, rosterName:m.rosterName||'', role:m.role,
              orderSig:c.orderSig, heroes:_ih, treasures:_it,
              heroCount:_ih.length, treasureCount:_it.length,
            });
          }
        });
      });
      _ironList.sort((a,b) => ((b.heroCount+b.treasureCount) - (a.heroCount+a.treasureCount)));
      console.log('[_fbAdminScanPollutionClusters] 掃描完成 → ' + _clusters.length + ' 組(掃 ' + _players.length + ' 位有 SSR/SR 的玩家), 鐵證帳號 ' + _ironList.length + ' 個');
      return { ok:true, scannedAt:Date.now(), totalPlayers:_players.length, clusterCount:_clusters.length, clusters:_clusters, ironList:_ironList };
    }catch(e){
      console.error('[_fbAdminScanPollutionClusters] 失敗', e);
      return { ok:false, reason:(e && (e.code||e.message)) || 'exception' };
    }
  };

  // ── API 3:掃描異常獲取至寶玩家 ──
  //   ⚠ _treasureUnlockHistory 是新欄位 — 老資料可能沒有,沒有的玩家會跳過
  window._fbAdminScanAbnormalTreasures = async function(windowMs, threshold){
    windowMs  = parseInt(windowMs, 10)  || (60 * 60 * 1000);
    threshold = parseInt(threshold, 10) || 3;                  // 至寶門檻較低,3 件就算異常
    const _now = Date.now();
    const _cutoff = _now - windowMs;
    try{
      const snap = await getDocs(collection(_fbDb, 'players'));
      const _abnormal = [];
      snap.forEach(docSnap => {
        try{
          const d = docSnap.data() || {};
          let _hist = Array.isArray(d._treasureUnlockHistory) ? d._treasureUnlockHistory : null;
          if(!Array.isArray(_hist) || !_hist.length) return;
          const _recent = _hist.filter(h => h && typeof h.at === 'number' && h.at >= _cutoff);
          const _real = _recent.filter(h => h && h.source !== 'admin_delete' && h.source !== 'admin_grant');
          if(_real.length >= threshold){
            const _byBattle = {};
            _real.forEach(h => {
              const _bk = h.battleId || '(無 battleId)';
              if(!_byBattle[_bk]) _byBattle[_bk] = [];
              _byBattle[_bk].push(h);
            });
            const _multiPerBattle = Object.entries(_byBattle)
              .filter(([k, arr]) => k !== '(無 battleId)' && arr.length >= 2)
              .map(([k, arr]) => ({ battleId: k, count: arr.length, treasures: arr.map(a => a.id || a.name) }));
            _abnormal.push({
              uid: docSnap.id,
              email: (d.email || '').toLowerCase(),
              displayName: (d.displayName || ''),
              count: _real.length,
              treasures: _real.map(h => ({
                id: h.id || h.name,
                at: h.at,
                source: h.source || 'unknown',
                battleId: h.battleId || null
              })).sort((a,b) => b.at - a.at),
              multiPerBattle: _multiPerBattle,
            });
          }
        }catch(eRow){ console.warn('[_fbAdminScanAbnormalTreasures] row 例外', eRow); }
      });
      _abnormal.sort((a, b) => b.count - a.count);
      console.log('[_fbAdminScanAbnormalTreasures v3.11.34] 掃描完成 → ' + _abnormal.length + ' 位異常玩家');
      return { ok: true, abnormal: _abnormal, scannedAt: _now, windowMs, threshold };
    }catch(e){
      console.error('[_fbAdminScanAbnormalTreasures] 掃描失敗', e);
      throw e;
    }
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.15.58(2026-06-20)— GM 洗錢查緝:掃描「短時間重複賣出同額」的洗錢痕跡
  //   背景:v3.15.57 修掉「賣出物品重整後復活可重複賣」的洗錢漏洞;此工具用來事後查緝
  //         「漏洞修復前已大量洗錢」的玩家並回收贓款。
  //   原理(老師裁示):正常賣出同價道具不會在數十秒內連續多次;洗錢=賣→重整→再賣,
  //         會在短時間內反覆賣出「同一金額」。故偵測:玩家知識幣帳本(_coinTransactions)中
  //         「賣出類」交易,依金額分桶、桶內依時間排序,凡相鄰時間間隔 ≤ windowSec 的連續同額簇,
  //         簇內筆數 ≥ minRepeat 即判定為一組洗錢;該簇贓款 =(簇筆數 - 1)× 金額(保留 1 次合法賣出)。
  //   限制:帳本跨槽 union 僅保留最近 400 筆(極早期洗錢可能已滾出);僅能估算、供 GM 人工裁量回收。
  //   參數:windowSec 視窗秒數(預設 60)、minRepeat 同額簇門檻筆數(預設 3)。
  // ════════════════════════════════════════════════════════════════════
  window._fbAdminScanMoneyLaundering = async function(windowSec, minRepeat){
    var _winMs = (parseInt(windowSec, 10) || 60) * 1000;
    var _minN  = parseInt(minRepeat, 10) || 3;
    if(_minN < 2) _minN = 2;
    var _now = Date.now();
    try{
      var snap = await getDocs(collection(_fbDb, 'players'));
      var _suspects = [];
      var _scanned = 0;
      snap.forEach(function(docSnap){
        _scanned++;
        try{
          var d = docSnap.data() || {};
          // 讀帳本:物件陣列 或 _s 字串(雲端可能存任一形式)
          var _tx = Array.isArray(d._coinTransactions) ? d._coinTransactions : null;
          if(!_tx && typeof d._coinTransactions_s === 'string'){
            try{ var _p = JSON.parse(d._coinTransactions_s); if(Array.isArray(_p)) _tx = _p; }catch(_){}
          }
          if(!Array.isArray(_tx) || !_tx.length) return;
          // 篩「賣出類」交易:reason 含「賣出」且金額為正
          var _sells = _tx.filter(function(e){
            return e && typeof e.at === 'number' && typeof e.amount === 'number'
                   && e.amount > 0 && typeof e.reason === 'string' && e.reason.indexOf('賣出') !== -1;
          });
          if(_sells.length < _minN) return;
          // 依金額分桶
          var _byAmt = {};
          _sells.forEach(function(e){
            var k = String(e.amount);
            if(!_byAmt[k]) _byAmt[k] = [];
            _byAmt[k].push(e);
          });
          var _groups = [];
          var _launderedEst = 0;
          Object.keys(_byAmt).forEach(function(k){
            var arr = _byAmt[k];
            if(arr.length < _minN) return;
            arr.sort(function(a,b){ return (a.at||0) - (b.at||0); });
            // 貪婪分簇:相鄰時間間隔 ≤ _winMs 視為同簇(短時間連續同額賣出)
            var _clusterStart = 0;
            for(var i = 1; i <= arr.length; i++){
              var _broke = (i === arr.length) || ((arr[i].at - arr[i-1].at) > _winMs);
              if(_broke){
                var _size = i - _clusterStart;
                if(_size >= _minN){
                  var amt = arr[_clusterStart].amount;
                  var _laund = (_size - 1) * amt;   // 保留 1 次合法賣出
                  _launderedEst += _laund;
                  _groups.push({
                    amount: amt, count: _size, laundered: _laund,
                    firstAt: arr[_clusterStart].at, lastAt: arr[i-1].at,
                    spanSec: Math.round((arr[i-1].at - arr[_clusterStart].at) / 1000),
                    reason: (arr[_clusterStart].reason || '')
                  });
                }
                _clusterStart = i;
              }
            }
          });
          if(_groups.length){
            _groups.sort(function(a,b){ return b.laundered - a.laundered; });
            _suspects.push({
              uid: docSnap.id,
              email: (d.email || '').toLowerCase(),
              displayName: (d.displayName || d.displayLabel || ''),
              currentCoins: (typeof d.knowledgeCoins === 'number') ? d.knowledgeCoins : 0,
              launderedEst: _launderedEst,
              sellTxCount: _sells.length,
              groups: _groups
            });
          }
        }catch(eRow){ console.warn('[_fbAdminScanMoneyLaundering] row 例外', eRow); }
      });
      _suspects.sort(function(a,b){ return b.launderedEst - a.launderedEst; });
      console.log('[_fbAdminScanMoneyLaundering v3.15.58] 掃描完成 windowSec=' + (_winMs/1000)
        + ' minRepeat=' + _minN + ' → 掃 ' + _scanned + ' 位, 嫌疑 ' + _suspects.length + ' 位');
      return { ok: true, suspects: _suspects, scanned: _scanned, scannedAt: _now, windowSec: _winMs/1000, minRepeat: _minN };
    }catch(e){
      console.error('[_fbAdminScanMoneyLaundering] 掃描失敗', e);
      throw e;
    }
  };

  // ★ v3.15.58 — 回收洗錢贓款:扣減指定玩家知識幣(複用 _fbCompensatePlayer 的「負值 add + 三槽寫入」,防合併復活)
  window._fbAdminRecoverLaunderedCoins = async function(uid, amount, note){
    if(!uid) throw new Error('uid 為必要');
    var _amt = Math.abs(Math.round(Number(amount) || 0));
    if(_amt <= 0) throw new Error('回收金額必須 > 0');
    if(typeof window._fbCompensatePlayer !== 'function') throw new Error('_fbCompensatePlayer 未載入');
    // coinsMode:add + 負值 → _newCoins = Math.max(0, current - _amt);主檔/live/safe 三槽同寫(防跨槽合併把高餘額復活)
    var _r = await window._fbCompensatePlayer(uid, {
      coins: -_amt, coinsMode: 'add',
      _recoverLaundered: true, recoverNote: String(note || 'GM 回收洗錢贓款').slice(0, 48)
    });
    console.log('[_fbAdminRecoverLaunderedCoins v3.15.58] uid=' + uid + ' 扣減 ' + _amt + ' 知識幣', _r);
    return { ok: true, uid: uid, recovered: _amt, result: _r };
  };

  // ── API 4:刪除玩家異常獲取的台灣至寶 ──
  window._fbAdminDeleteTreasure = async function(uid, treasureId, opts){
    if(!uid || !treasureId) throw new Error('uid 與 treasureId 為必要');
    opts = opts || {};
    const _adminEmail = (_fbAuth.currentUser && _fbAuth.currentUser.email) || 'unknown';
    try{
      // ★★ v5.130.0(老師裁定甲)— 改走三槽同寫單一真相 ★★
      //   舊版不只單寫主檔,連 taiwanTreasureData_s 都沒同步 ⇒ 載入採信舊 _s 就地復活;一併修齊。
      const _now130 = Date.now();
      const _res130 = await window._lxpsAdminApplyAllSlots(uid, function(d, slot){
        return window._lxpsSlotTreasurePatch(d, [treasureId], {
          at:_now130, mode:'remove', by:_adminEmail, uid:uid, slot:slot,
          source:'admin_delete', reason: opts.reason || '異常獲取清除'
        });
      }, {
        label:'GM刪除至寶',
        lastAction:{ type:'delete_treasure', target:treasureId, at:_now130, by:_adminEmail, reason: opts.reason || '' }
      });
      const _d130 = _res130.mainData || {};
      const _hadTw = !!(_d130.taiwanTreasureData && typeof _d130.taiwanTreasureData === 'object' && (treasureId in _d130.taiwanTreasureData));
      console.log('[_fbAdminDeleteTreasure v5.130.0] ✅ uid=' + uid + ' 已刪除至寶 ' + treasureId
        + '(hadTwData=' + _hadTw + ')');
      return { ok: true, uid, treasureId, hadTwData: _hadTw, slots: _res130 };
    }catch(e){
      console.error('[_fbAdminDeleteTreasure] 刪除失敗', e);
      throw e;
    }
  };

  /* ── v5.130.0 前的舊版(只寫主檔·且漏寫 _s)整段保留備查,勿刪 ────────────
  window._fbAdminDeleteTreasure_LEGACY_v5129 = async function(uid, treasureId, opts){
    const _adminEmail = (_fbAuth.currentUser && _fbAuth.currentUser.email) || 'unknown';
    try{
      const _ref = doc(_fbDb, 'players', uid);
      const _snap = await getDoc(_ref);
      if(!_snap.exists()) throw new Error('玩家不存在 uid=' + uid);
      const _d = _snap.data() || {};

      // 台灣至寶結構 taiwanTreasureData = { id: { lv, exp, equippedTo } }
      const _twData = (_d.taiwanTreasureData && typeof _d.taiwanTreasureData === 'object')
        ? Object.assign({}, _d.taiwanTreasureData) : {};
      const _hadTw = (treasureId in _twData);
      delete _twData[treasureId];

      // _treasureUnlockHistory:寫 audit 紀錄
      let _hist = Array.isArray(_d._treasureUnlockHistory) ? _d._treasureUnlockHistory.slice() : [];
      _hist.push({
        id: treasureId,
        at: Date.now(),
        source: 'admin_delete',
        adminAction: true,
        deletedBy: _adminEmail,
        reason: opts.reason || '異常獲取清除',
      });
      if(_hist.length > 250) _hist = _hist.slice(-250);

      await updateDoc(_ref, {
        taiwanTreasureData: _twData,
        _treasureUnlockHistory: _hist,
        _adminLastAction: {
          type: 'delete_treasure',
          target: treasureId,
          at: Date.now(),
          by: _adminEmail,
          reason: opts.reason || '',
        }
      });

      console.log('[_fbAdminDeleteTreasure] ✅ uid=' + uid + ' 已刪除至寶 ' + treasureId
        + '(hadTwData=' + _hadTw + ')');
      return { ok: true, uid, treasureId, hadTwData: _hadTw };
    }catch(e){
      console.error('[_fbAdminDeleteTreasure] 刪除失敗', e);
      throw e;
    }
  };
  ──────────────────────────────────────────────────────────────────── */

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.11.35e(2026-05-29) — 清除異常果實(從 playerBackpack.burst_upgrade_fruit 扣)
  //
  //   邏輯:
  //     1. 讀當前果實數 _curCount
  //     2. 實扣 = min(requestedCount, _curCount) — 老師指示:若不足扣到 0
  //     3. 更新 playerBackpack.burst_upgrade_fruit = _curCount - 實扣
  //     4. 寫 _fruitHistory 一筆 admin_delete audit
  //     5. 寫 _adminLastAction
  //
  //   ⚠ 不動英雄的 heroBurstLevels(果實「已用掉」升爆發的部分視同合法消耗,
  //     不溯及既往)— 老師指示「若玩家已用掉,扣到 0 為止」
  // ════════════════════════════════════════════════════════════════════
  window._fbAdminClearAbnormalFruits = async function(uid, requestedCount, opts){
    if(!uid) throw new Error('uid 為必要');
    requestedCount = parseInt(requestedCount, 10) || 0;
    if(requestedCount <= 0) throw new Error('requestedCount 必須 > 0');
    opts = opts || {};
    const _adminEmail = (_fbAuth.currentUser && _fbAuth.currentUser.email) || 'unknown';

    try{
      // ★★ v5.130.0(老師裁定甲)— 改走三槽同寫單一真相 ★★
      //   v5.93.0 handoff 早已記載本函式病灶:不寫 playerBackpack_s、不寫 live/safe ⇒
      //   背包屬「消耗型只取最新槽」,學生下次存檔的較新槽把清除整個蓋回去(清了等於沒清)。
      //   修法:三槽各自以「該槽自己的存量」夾 0 扣除 + 兩鍵同寫 + bumpSavedAt;主檔記 _fruitHistory。
      const _now130 = Date.now();
      var _mainApplied = { burst_upgrade_fruit: 0 };
      var _beforeCount = 0, _afterCount = 0, _actualRemove = 0;
      const _res130 = await window._lxpsAdminApplyAllSlots(uid, function(d, slot){
        var _meta = { applied: {} };
        var P = window._lxpsSlotBackpackPatch(d, { burst_upgrade_fruit: -requestedCount }, _meta);
        if(slot === 'main'){
          var _bpM = null;
          try{ if(typeof d.playerBackpack_s === 'string'){ var _q = JSON.parse(d.playerBackpack_s); if(_q && typeof _q === 'object') _bpM = _q; } }catch(_){}
          if(!_bpM && d.playerBackpack && typeof d.playerBackpack === 'object') _bpM = d.playerBackpack;
          _beforeCount = parseInt((_bpM || {}).burst_upgrade_fruit, 10) || 0;
          _actualRemove = Math.min(requestedCount, _beforeCount);
          _afterCount = _beforeCount - _actualRemove;
          _mainApplied = _meta.applied;
          if(!P && _actualRemove <= 0){
            // 主檔本來就 0 顆:仍寫一筆 audit(維持舊行為「扣到 0 為止」有跡可查)
            P = {};
          }
          if(P){
            var _hist = Array.isArray(d._fruitHistory) ? d._fruitHistory.slice() : [];
            _hist.push({
              source: 'admin_delete', count: -_actualRemove, at: _now130, adminAction: true,
              deletedBy: _adminEmail, reason: opts.reason || '異常果實清除',
              requestedCount, actualRemove: _actualRemove, beforeCount: _beforeCount, afterCount: _afterCount,
            });
            if(_hist.length > 250) _hist = _hist.slice(-250);
            P._fruitHistory = _hist;
          }
        }
        return P;
      }, {
        label:'GM清除異常果實',
        bumpSavedAt: true,   // ⚠ 背包=消耗型只取最新槽,不 bump 等於沒清(v5.93.0 病灶主因)
        stampRestore: false, // 只動背包不動英雄,沿用舊行為不強制重載
        lastAction:{ type:'clear_abnormal_fruits', target:'burst_upgrade_fruit', requestedCount, at:_now130, by:_adminEmail, reason: opts.reason || '' }
      });
      console.log('[_fbAdminClearAbnormalFruits v5.130.0] ✅ uid=' + uid
        + ' 要求扣 ' + requestedCount + ' / 主檔實際扣 ' + _actualRemove
        + ' (' + _beforeCount + ' → ' + _afterCount + ')'
        + ' live=' + ((_res130.live && _res130.live.written) ? '已同步' : '無需/無槽')
        + ' safe=' + ((_res130.safe && _res130.safe.written) ? '已同步' : '無需/無槽'));
      return {
        ok: true, uid,
        requestedCount,
        actualRemove: _actualRemove,
        beforeCount: _beforeCount,
        afterCount: _afterCount,
        slots: _res130,
      };
    }catch(e){
      console.error('[_fbAdminClearAbnormalFruits] 失敗', e);
      throw e;
    }
  };

  // ── API 5:發送通知給玩家(玩家下次登入時顯示) ──
  //   ⚠ 寫入 pendingAdminNotifications/{uid}/items/{ts} 子集合,玩家端在登入後讀取並顯示
  //   ⚠ 老師端 UI 串接後,玩家端需配合在 onAuth 後讀取此集合並彈窗(階段 6 加)
  window._fbAdminSendNotificationToPlayer = async function(uid, payload){
    if(!uid || !payload) throw new Error('uid 與 payload 為必要');
    const _adminEmail = (_fbAuth.currentUser && _fbAuth.currentUser.email) || 'unknown';
    try{
      const _ts = Date.now();
      const _noteRef = doc(_fbDb, 'pendingAdminNotifications', uid, 'items', String(_ts));
      const _noteData = {
        title: String(payload.title || '管理員通知').slice(0, 100),
        body: String(payload.body || '').slice(0, 2000),
        type: payload.type || 'info',  // info / warning / compensation
        createdAt: _ts,
        createdBy: _adminEmail,
        // 補償項目可選(配合 _fbCompensatePlayer 結構)
        compensation: payload.compensation || null,
        // 異常清除明細(讓玩家看到清除了什麼)
        removedItems: payload.removedItems || null,
        read: false,
      };
      await setDoc(_noteRef, _noteData);
      console.log('[_fbAdminSendNotificationToPlayer] ✅ 已發送通知 uid=' + uid);
      return { ok: true, uid, ts: _ts };
    }catch(e){
      console.error('[_fbAdminSendNotificationToPlayer] 發送失敗', e);
      throw e;
    }
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.11.35 階段 4b(2026-05-29) — 玩家活動記錄查詢 API 群
  //   給 GM 後台「玩家活動記錄查詢」用,以 battleId 為單位精準偵測異常。
  //
  //   新規則(取代「短時間爆增」基本規則):
  //     一場戰鬥(同 battleId)最多 1 隻新解鎖英雄 + 1 個至寶
  //     超過視為異常,GM 可逐項刪除「第二次以上的獲得」
  //
  //   ⚠ 全部用 _adminForceWrite 旗標 + audit log + updateDoc,鐵律 2.04
  // ════════════════════════════════════════════════════════════════════

  // ── API 6:統一查詢玩家活動記錄 ──
  //   回傳 { player, heroUnlockHistory, treasureUnlockHistory, battleHistory, coinTransactions }
  window._fbAdminQueryPlayerActivity = async function(uid){
    if(!uid) throw new Error('uid 為必要');
    try{
      const _ref = doc(_fbDb, 'players', uid);
      const _snap = await getDocFromServer(_ref);
      if(!_snap.exists()) throw new Error('玩家不存在 uid=' + uid);
      const _d = _snap.data() || {};

      // 基本卡資料
      const _player = {
        uid,
        name: _d.name || '',
        email: _d.email || '',
        knowledgeCoins: _d.knowledgeCoins || 0,
        unlockedCount: Array.isArray(_d.unlockedHeroes) ? _d.unlockedHeroes.length : 0,
        // 總等級
        totalHeroLv: (function(){
          if(!_d.heroLevels || typeof _d.heroLevels !== 'object') return 0;
          return Object.values(_d.heroLevels).reduce((s,v) => s + (parseInt(v)||0), 0);
        })(),
        maxHeroLv: (function(){
          if(!_d.heroLevels || typeof _d.heroLevels !== 'object') return 0;
          return Object.values(_d.heroLevels).reduce((m,v) => Math.max(m, parseInt(v)||0), 0);
        })(),
        // 至寶數
        treasureCount: (function(){
          let _c = 0;
          if(_d.taiwanTreasureData && typeof _d.taiwanTreasureData === 'object'){
            _c += Object.keys(_d.taiwanTreasureData).length;
          }
          const _jp = (_d.japanProgress && _d.japanProgress.treasures) || {};
          ['tengu','shutendoji','tamamo'].forEach(k => { if(_jp[k]) _c++; });
          // ★ v5.41.0 — 中國三神物一併計入 GM 摘要(同日本三神器口徑)
          // ★ v5.101.0 — 改讀「取得次數 > 消耗次數」的可見件數(舊存檔無計數欄位 → 退回布林,零行為變更)
          const _cnP = _d.chinaProgress || {};
          const _cnT = _cnP.treasures || {};
          const _cnG = _cnP.treasuresGot || {};
          const _cnU = Math.max(0, _cnP.treasuresUsed || 0);
          ['fengshen','xiyou','sanguo'].forEach(k => {
            const _got = Math.max(0, (_cnG[k] || 0), (_cnT[k] ? 1 : 0));
            if(_got > _cnU) _c++;
          });
          return _c;
        })(),
      };

      // 各 history(取最近 200 / 100 筆)
      const _heroH = Array.isArray(_d._heroUnlockHistory)
        ? _d._heroUnlockHistory.slice(-200).sort((a,b) => (b.at||0) - (a.at||0))
        : [];
      const _treasureH = Array.isArray(_d._treasureUnlockHistory)
        ? _d._treasureUnlockHistory.slice(-200).sort((a,b) => (b.at||0) - (a.at||0))
        : [];
      const _battleH = Array.isArray(_d._battleHistory)
        ? _d._battleHistory.slice(-50).sort((a,b) => (b.at||0) - (a.at||0))
        : [];
      // 知識幣帳目(若存在)
      const _coinTx = Array.isArray(_d._coinTransactions)
        ? _d._coinTransactions.slice(-100).sort((a,b) => (b.at||0) - (a.at||0))
        : [];
      // ★ v3.13.58 — 召喚水晶帳目(若存在;含字串 fallback,模式同 _coinTransactions_s)
      let _crystalRaw = Array.isArray(_d._crystalTransactions) ? _d._crystalTransactions : null;
      if(!_crystalRaw && typeof _d._crystalTransactions_s === 'string'){ try{ const _pc = JSON.parse(_d._crystalTransactions_s); if(Array.isArray(_pc)) _crystalRaw = _pc; }catch(_){} }
      const _crystalTx = Array.isArray(_crystalRaw)
        ? _crystalRaw.slice(-100).sort((a,b) => (b.at||0) - (a.at||0))
        : [];
      // ★ v3.11.35e — 果實獲得紀錄
      const _fruitH = Array.isArray(_d._fruitHistory)
        ? _d._fruitHistory.slice(-200).sort((a,b) => (b.at||0) - (a.at||0))
        : [];
      // ★ v3.13.94 — 統一活動紀錄(知識王成績/送禮/友情之心/鬥技場/召喚券),供 GM 補發查證
      let _activityRaw = Array.isArray(_d._activityHistory) ? _d._activityHistory : null;
      if(!_activityRaw && typeof _d._activityHistory_s === 'string'){ try{ const _pa = JSON.parse(_d._activityHistory_s); if(Array.isArray(_pa)) _activityRaw = _pa; }catch(_){} }
      const _activityH = Array.isArray(_activityRaw)
        ? _activityRaw.slice(-800).sort((a,b) => (b.at||0) - (a.at||0))
        : [];
      // 當前果實庫存(從 playerBackpack 讀)
      const _bp = (_d.playerBackpack && typeof _d.playerBackpack === 'object') ? _d.playerBackpack : {};
      const _currentFruitCount = parseInt(_bp.burst_upgrade_fruit, 10) || 0;

      console.log('[_fbAdminQueryPlayerActivity] ✅ uid=' + uid
        + ' heroH=' + _heroH.length + ' treasureH=' + _treasureH.length
        + ' battleH=' + _battleH.length + ' coinTx=' + _coinTx.length
        + ' fruitH=' + _fruitH.length + ' curFruit=' + _currentFruitCount);
      // ★ v3.11.35d — 加上補償計算需要的英雄明細
      const _heroDetails = {
        heroLevels: (_d.heroLevels && typeof _d.heroLevels === 'object') ? _d.heroLevels : {},
        heroExp: (_d.heroExp && typeof _d.heroExp === 'object') ? _d.heroExp : {},
        heroSkillLevels: (_d.heroSkillLevels && typeof _d.heroSkillLevels === 'object') ? _d.heroSkillLevels : {},
        heroBurstLevels: (_d.heroBurstLevels && typeof _d.heroBurstLevels === 'object') ? _d.heroBurstLevels : {},
        heroStatInvested: (_d.heroStatInvested && typeof _d.heroStatInvested === 'object') ? _d.heroStatInvested : {},
        heroStatPoints: (_d.heroStatPoints && typeof _d.heroStatPoints === 'object') ? _d.heroStatPoints : {},
        heroTraitLevel: (_d.heroTraitLevel && typeof _d.heroTraitLevel === 'object') ? _d.heroTraitLevel : {},
      };
      // ★ v3.13.49(2026-06-05)— 「全加」:回傳玩家文件「所有」記錄, 供 GM 一頁看完整資料
      //   admin_panel 與本檔同頁(window), 稀有度/至寶名稱在 UI 端用 window._getHeroRarity / window.TAIWAN_TREASURES 補,
      //   所以這裡只回原始資料 map; 皮膚/勳章/背包/答題等「只增不減」集合 cloud 端就是權威。
      const _safeObj = (o) => (o && typeof o === 'object') ? o : {};
      const _safeArr = (a) => Array.isArray(a) ? a : [];
      // ★ v5.111.0 — 預設陣容一律先讀字串版(原生欄位是攤平版,少了 treasures/gear)
      const _presetFromS = (d, key) => {
        try{
          const s = d && d[key + '_s'];
          if(typeof s !== 'string' || !s) return null;
          const p = JSON.parse(s);
          return Array.isArray(p) ? p : null;
        }catch(_){ return null; }
      };
      // 至寶解析:物件優先, 退而求其次讀字串版(_s)
      let _taiwanTre = _safeObj(_d.taiwanTreasureData);
      if(!Object.keys(_taiwanTre).length && typeof _d.taiwanTreasureData_s === 'string'){
        try{ _taiwanTre = _safeObj(JSON.parse(_d.taiwanTreasureData_s)); }catch(_){}
      }

      // ★ v3.13.49 — owned-centric 英雄明細(逐隻 unlockedHeroes):稀有度/等級/投資/技能/爆發/天賦 + 取得來源(_rareSrcCat 分類, 查無紀錄=pollution)+ 裝備中的至寶
      //   來源分類用本檔同 scope 的 _rareSrcCat(與 _fbAdminScanPollutionClusters 同引擎, 確保一致)。
      let _histRaw = Array.isArray(_d._heroUnlockHistory) ? _d._heroUnlockHistory : null;
      if(!_histRaw && typeof _d._heroUnlockHistory_s === 'string'){ try{ _histRaw = JSON.parse(_d._heroUnlockHistory_s); }catch(_){ _histRaw = null; } }
      const _recByName = {};
      if(Array.isArray(_histRaw)) _histRaw.forEach(h => {
        if(!h || !h.name || h.source === 'admin_delete') return;
        const _at = (typeof h.at === 'number') ? h.at : 0;
        const _cur = _recByName[h.name];
        if(!_cur || _at >= _cur.at) _recByName[h.name] = { source: h.source || '', at: _at, uid: (h.uid || '') };
      });
      // 至寶 → 裝備在哪隻英雄(equippedTo)分組
      const _treByHero = {};
      Object.keys(_taiwanTre).forEach(_tid => {
        const _rec = _taiwanTre[_tid];
        if(_rec && _rec.equippedTo){ (_treByHero[_rec.equippedTo] = _treByHero[_rec.equippedTo] || []).push(_tid); }
      });
      const _hl  = _safeObj(_d.heroLevels);
      const _hxp = _safeObj(_d.heroExp);
      const _hsi = _safeObj(_d.heroStatInvested);
      const _hsp = _safeObj(_d.heroStatPoints);
      const _hsk = _safeObj(_d.heroSkillLevels);
      const _hbl = _safeObj(_d.heroBurstLevels);
      const _htl = _safeObj(_d.heroTraitLevel);
      const _ownedHeroes = _safeArr(_d.unlockedHeroes).map(_n => {
        let _rar = '?';
        try{ if(typeof window._getHeroRarity === 'function'){ _rar = window._getHeroRarity(_n) || '?'; } }catch(_){}
        const _rec = _recByName[_n];
        return {
          name: _n,
          rarity: _rar,
          lv: _hl[_n] || 1,
          exp: _hxp[_n] || 0,
          invested: (_hsi[_n] != null) ? _hsi[_n] : null,
          freePts: _hsp[_n] || 0,
          skill: (_hsk[_n] != null) ? _hsk[_n] : null,
          burst: (_hbl[_n] != null) ? _hbl[_n] : null,
          trait: (_htl[_n] != null) ? _htl[_n] : null,
          source: _rec ? (_rec.source || '') : null,
          cat: _rec ? _rareSrcCat(_rec.source || '') : ((window._ARENA_INITIAL_HEROES||[]).indexOf(_n) >= 0 ? 'initial' : 'pollution'),   // ★ v3.13.52 初始8隻起始就有、無紀錄=initial(非汙染);其餘無紀錄=跨帳號汙染
          at: _rec ? _rec.at : 0,
          creatorUid: _rec ? (_rec.uid || '') : '',
          equippedTreasures: _treByHero[_n] || [],
        };
      });
      // ★ v3.13.49b — 至寶解鎖紀錄 by id(取最新一筆)→ 供 owned 至寶判來源/creatorUid/汙染
      const _treRecById = {};
      try{
        let _tHistRaw = Array.isArray(_d._treasureUnlockHistory) ? _d._treasureUnlockHistory : null;
        if(!_tHistRaw && typeof _d._treasureUnlockHistory_s === 'string'){ try{ _tHistRaw = JSON.parse(_d._treasureUnlockHistory_s); }catch(_){ _tHistRaw = null; } }
        if(Array.isArray(_tHistRaw)) _tHistRaw.forEach(h => {
          if(!h || !h.id || h.source === 'admin_delete') return;
          const _at = (typeof h.at === 'number') ? h.at : 0;
          const _cur = _treRecById[h.id];
          if(!_cur || _at >= _cur.at) _treRecById[h.id] = { source:h.source||'', at:_at, uid:(h.uid||'') };
        });
      }catch(_){}
      const _TRE_DB = (typeof window.TAIWAN_TREASURES === 'object' && window.TAIWAN_TREASURES) ? window.TAIWAN_TREASURES : {};
      // ★ v3.13.49 — owned-centric 至寶明細(逐個 taiwanTreasureData)+ v3.13.49b 來源/汙染
      const _ownedTreasures = Object.keys(_taiwanTre).map(_tid => {
        const _rec = _taiwanTre[_tid] || {};
        const _trec = _treRecById[_tid];
        const _info = _TRE_DB[_tid] || {};
        return {
          id: _tid, name: _info.name || _tid,
          lv: _rec.lv || 1, exp: _rec.exp || 0,
          equippedTo: _rec.equippedTo || null,
          invested: (_rec.invested != null) ? _rec.invested : null,
          source: _trec ? (_trec.source || '') : null,
          cat: _trec ? _rareSrcCat(_trec.source || '') : 'pollution',  // 查無解鎖紀錄 = 疑似跨帳號汙染
          creatorUid: _trec ? (_trec.uid || '') : '',
          at: _trec ? _trec.at : 0,
        };
      }).sort((a,b)=> (b.lv||0)-(a.lv||0) || String(a.id).localeCompare(String(b.id)));

      const _full = {
        // 基本
        name: _d.name || '', email: _d.email || '', knowledgeCoins: _d.knowledgeCoins || 0,
        savedAt: _d.savedAt || 0, saveVersion: _d.v || 0,
        friendshipHeart: (typeof _d.friendshipHeart === 'number') ? _d.friendshipHeart : 0,
        advBestScore: _d.advBestScore || 0,
        dailyFreeSummonDate: _d.dailyFreeSummonDate || '',
        // 英雄(擁有清單 + 第二至寶欄解鎖)
        unlockedHeroes: _safeArr(_d.unlockedHeroes),
        heroSlot2Unlocked: _safeArr(_d.heroSlot2Unlocked),
        ownedHeroes: _ownedHeroes,        // ★ v3.13.49 — owned-centric 明細(來源/汙染/裝備至寶)
        ownedTreasures: _ownedTreasures,  // ★ v3.13.49 — owned-centric 至寶明細
        // 至寶(完整:lv/exp/equippedTo/invested)+ 日本三神器
        taiwanTreasureData: _taiwanTre,
        japanProgress: _safeObj(_d.japanProgress),
        chinaProgress: _safeObj(_d.chinaProgress),   // ★ v5.41.0 — 中國關三神物/官渡解鎖
        // 皮膚 / 肖像
        heroPortraitUnlocked: _safeObj(_d.heroPortraitUnlocked),
        heroPortraitChoice: _safeObj(_d.heroPortraitChoice),
        skinUnlockHistory: _safeArr(_d._skinUnlockHistory).slice(-200).sort((a,b)=>(b.at||0)-(a.at||0)),
        // 勳章
        playerMedals: _safeObj(_d.playerMedals),
        medalStats: _safeObj(_d.medalStats),
        // 背包 / 寵物
        playerBackpack: _safeObj(_d.playerBackpack),
        petsEverCollected: _safeArr(_d.petsEverCollected),
        // 答題進度 / 知識王挑戰
        persistentCorrectQuestions: _safeObj(_d.persistentCorrectQuestions),
        kingChallenge: _safeObj(_d._kingChallenge),
        // 陣容 / 每日商店
        // ★ v5.111.0 — 優先採信 _s 字串版(原生欄位自 v5.111.0 起是攤平版,不含 treasures/gear)
        arenaPresetTeams: _presetFromS(_d, 'arenaPresetTeams') || _d.arenaPresetTeams || _d.presetTeams || null,
        advPresetTeams: _presetFromS(_d, 'advPresetTeams') || _d.advPresetTeams || null,
        shopDailyData: _safeObj(_d.shopDailyData),
        shopWeeklyData: _safeObj(_d.shopWeeklyData),
        // 好友 / 送禮
        giftHistory: _safeObj(_d.giftHistory),
        // 進度快照 / 健康度摘要 / 管理員最後動作
        advSnapshot: _d.advSnapshot || null,
        dataSummary: _safeObj(_d._dataSummary),
        prevDataSummary: _safeObj(_d._prevDataSummary),
        adminLastAction: _d._adminLastAction || null,
        settings: _safeObj(_d.settings),
        // ★ v3.13.49 — 汙染刪除預告狀態 + 刪除記錄(供 GM 後台顯示/套用/復原)
        pendingPollutionPurge: _d._pendingPollutionPurge || null,
        pollutionPurgeLog: _safeArr(_d._pollutionPurgeLog),
      };
      return {
        ok: true,
        player: _player,
        heroUnlockHistory: _heroH,
        treasureUnlockHistory: _treasureH,
        battleHistory: _battleH,
        coinTransactions: _coinTx,
        crystalTransactions: _crystalTx,
        fruitHistory: _fruitH,
        activityHistory: _activityH,
        currentFruitCount: _currentFruitCount,
        heroDetails: _heroDetails,
        full: _full,  // ★ v3.13.49 — 完整記錄(活動頁「全加」用)
      };
    }catch(e){
      console.error('[_fbAdminQueryPlayerActivity] 查詢失敗', e);
      throw e;
    }
  };

  // ── API 7:刪除玩家活動記錄單筆條目 ──
  //   type: 'hero_history' | 'treasure_history' | 'battle_history' | 'coin_tx'
  //   entryKey:
  //     hero_history     → { at, name }
  //     treasure_history → { at, id }
  //     battle_history   → { battleId, type }
  //     coin_tx          → { at, id }
  //   ⚠ 只刪 history 條目,不會回滾實際擁有的英雄/至寶/幣
  //     若要把英雄/至寶實體也刪掉,呼叫 _fbAdminDeleteUnlockedHero / _fbAdminDeleteTreasure
  window._fbAdminDeletePlayerActivityEntry = async function(uid, type, entryKey, opts){
    if(!uid || !type || !entryKey) throw new Error('uid / type / entryKey 為必要');
    opts = opts || {};
    const _adminEmail = (_fbAuth.currentUser && _fbAuth.currentUser.email) || 'unknown';
    try{
      const _ref = doc(_fbDb, 'players', uid);
      const _snap = await getDoc(_ref);
      if(!_snap.exists()) throw new Error('玩家不存在 uid=' + uid);
      const _d = _snap.data() || {};

      let _patch = {};
      let _removed = null;

      if(type === 'hero_history'){
        let _h = Array.isArray(_d._heroUnlockHistory) ? _d._heroUnlockHistory.slice() : [];
        const _idx = _h.findIndex(e => e && e.at === entryKey.at && e.name === entryKey.name);
        if(_idx < 0) throw new Error('找不到對應的英雄解鎖紀錄');
        _removed = _h[_idx];
        _h.splice(_idx, 1);
        _patch._heroUnlockHistory = _h;
      } else if(type === 'treasure_history'){
        let _h = Array.isArray(_d._treasureUnlockHistory) ? _d._treasureUnlockHistory.slice() : [];
        const _idx = _h.findIndex(e => e && e.at === entryKey.at && e.id === entryKey.id);
        if(_idx < 0) throw new Error('找不到對應的至寶解鎖紀錄');
        _removed = _h[_idx];
        _h.splice(_idx, 1);
        _patch._treasureUnlockHistory = _h;
      } else if(type === 'battle_history'){
        let _h = Array.isArray(_d._battleHistory) ? _d._battleHistory.slice() : [];
        const _idx = _h.findIndex(e => e && e.battleId === entryKey.battleId
          && (e.type || 'boss_battle') === (entryKey.type || 'boss_battle'));
        if(_idx < 0) throw new Error('找不到對應的戰鬥紀錄');
        _removed = _h[_idx];
        _h.splice(_idx, 1);
        _patch._battleHistory = _h;
      } else if(type === 'coin_tx'){
        let _h = Array.isArray(_d._coinTransactions) ? _d._coinTransactions.slice() : [];
        const _idx = _h.findIndex(e => e && e.at === entryKey.at && (!entryKey.id || e.id === entryKey.id));
        if(_idx < 0) throw new Error('找不到對應的幣帳紀錄');
        _removed = _h[_idx];
        _h.splice(_idx, 1);
        _patch._coinTransactions = _h;
      } else {
        throw new Error('未知 type=' + type);
      }

      _patch._adminLastAction = {
        type: 'delete_activity_entry',
        subtype: type,
        target: JSON.stringify(entryKey),
        at: Date.now(),
        by: _adminEmail,
        reason: opts.reason || '',
      };

      await updateDoc(_ref, _patch);
      console.log('[_fbAdminDeletePlayerActivityEntry] ✅ uid=' + uid + ' type=' + type
        + ' entry=' + JSON.stringify(entryKey));
      return { ok: true, uid, type, entryKey, removed: _removed };
    }catch(e){
      console.error('[_fbAdminDeletePlayerActivityEntry] 刪除失敗', e);
      throw e;
    }
  };

  // ── API 8:強制覆寫玩家欄位(白名單) ──
  //   patch 物件,只允許覆寫 SAFE_FIELDS 內的欄位,避免誤刪結構性資料
  window._fbAdminOverwritePlayerData = async function(uid, patch, opts){
    if(!uid || !patch || typeof patch !== 'object') throw new Error('uid 與 patch 為必要');
    opts = opts || {};
    const _adminEmail = (_fbAuth.currentUser && _fbAuth.currentUser.email) || 'unknown';

    // 白名單:這些欄位可以強制覆寫
    const SAFE_FIELDS = new Set([
      'knowledgeCoins',      // 數字
      'crystals',            // 數字
      'unlockedHeroes',      // 陣列
      'heroLevels',          // 物件
      'heroExp',             // 物件
      'heroStatInvested',    // 物件
      'heroStatPoints',      // 物件
      'heroSkillLevels',     // 物件
      'heroBurstLevels',     // 物件
      'taiwanTreasureData',  // 物件
      'japanProgress',       // 物件
      'chinaProgress',       // 物件 ★ v5.41.0 — 中國關三神物/官渡解鎖
      'playerBackpack',      // 陣列/物件
      'playerMedals',        // 物件
      '_heroUnlockHistory',  // 陣列
      '_treasureUnlockHistory', // 陣列
      '_battleHistory',      // 陣列
    ]);

    try{
      const _ref = doc(_fbDb, 'players', uid);
      const _safePatch = {};
      const _rejected = [];
      for(const k of Object.keys(patch)){
        if(SAFE_FIELDS.has(k)){
          _safePatch[k] = patch[k];
        } else {
          _rejected.push(k);
        }
      }
      if(Object.keys(_safePatch).length === 0){
        throw new Error('patch 中沒有任何允許的欄位 (rejected: ' + _rejected.join(',') + ')');
      }

      // audit log
      _safePatch._adminLastAction = {
        type: 'overwrite_player_data',
        target: Object.keys(_safePatch).filter(k => k !== '_adminLastAction').join(','),
        at: Date.now(),
        by: _adminEmail,
        reason: opts.reason || '',
        forceWrite: true,
      };

      await updateDoc(_ref, _safePatch);

      console.log('[_fbAdminOverwritePlayerData] ✅ uid=' + uid
        + ' 寫入 ' + Object.keys(_safePatch).length + ' 個欄位'
        + (_rejected.length ? ',拒絕 ' + _rejected.length + ' 個' : ''));
      return { ok: true, uid, written: Object.keys(_safePatch), rejected: _rejected };
    }catch(e){
      console.error('[_fbAdminOverwritePlayerData] 寫入失敗', e);
      throw e;
    }
  };

  // ── API 9:battleId 精準異常偵測 ──
  //   規則:同 battleId 解鎖 > 1 隻英雄 / 至寶 = 異常
  //   回傳每個玩家的 battleId 衝突清單,GM 可逐筆決定保留誰、刪誰
  //
  //   為了效能,只對「指定 uid 或 limit」玩家檢查;不傳就掃所有
  window._fbAdminScanBattleAnomaly = async function(opts){
    opts = opts || {};
    // ★ v3.11.35g(2026-05-29) — 老師指示「應該要掃全帳號」,移除 limit 預設值
    //   若呼叫端要限制請明確傳 opts.limit;不傳 = 掃全部
    const _limit = parseInt(opts.limit, 10) || 0;  // 0 = 無限制
    const _targetUid = opts.uid || null;
    try{
      const _abnormal = [];
      // 單一玩家:直接讀,不走 query
      if(_targetUid){
        const _snap = await getDocFromServer(doc(_fbDb, 'players', _targetUid));
        if(!_snap.exists()) return { ok: true, abnormal: [], scanned: 0 };
        const _result = _analyzeBattleAnomalySingle(_targetUid, _snap.data());
        if(_result) _abnormal.push(_result);
        return { ok: true, abnormal: _abnormal, scanned: 1 };
      }

      // 全玩家掃描(無 limit 預設掃全部)
      const _qy = _limit > 0
        ? query(collection(_fbDb, 'players'), limit(_limit))
        : collection(_fbDb, 'players');
      const _qsnap = await getDocs(_qy);
      let _scanned = 0;
      _qsnap.forEach(_docSnap => {
        _scanned++;
        try{
          const _r = _analyzeBattleAnomalySingle(_docSnap.id, _docSnap.data());
          if(_r) _abnormal.push(_r);
        }catch(_eOne){ console.warn('[_fbAdminScanBattleAnomaly] 單筆分析失敗', _docSnap.id, _eOne); }
      });

      // ★ v3.11.35g — 同步檢查 stats/global.totalPlayers 與實際文件數是否一致
      let _statsTotal = null;
      let _mismatch = false;
      try{
        const _statsSnap = await getDocFromServer(doc(_fbDb, 'stats', 'global'));
        if(_statsSnap.exists()){
          _statsTotal = (_statsSnap.data() || {}).totalPlayers || 0;
          _mismatch = (_statsTotal !== _scanned);
        }
      }catch(_eStats){ console.warn('[_fbAdminScanBattleAnomaly] 讀 stats 失敗', _eStats); }

      console.log('[_fbAdminScanBattleAnomaly v3.11.35g] 掃描完成 → '
        + _abnormal.length + ' 位異常玩家(掃 ' + _scanned + ' 位)'
        + (_statsTotal !== null
            ? ' / stats.totalPlayers=' + _statsTotal + (_mismatch ? ' ⚠ 不一致' : ' ✓')
            : ''));
      return {
        ok: true,
        abnormal: _abnormal,
        scanned: _scanned,
        actualPlayerCount: _scanned,      // ★ Firebase 實際 /players 文件數
        statsTotalPlayers: _statsTotal,   // ★ 首頁顯示的數字來源
        totalPlayersMismatch: _mismatch,  // ★ 兩者不一致 → 提示老師回填
      };
    }catch(e){
      console.error('[_fbAdminScanBattleAnomaly] 掃描失敗', e);
      throw e;
    }
  };

  // 內部 helper:分析單一玩家
  function _analyzeBattleAnomalySingle(uid, data){
    if(!data) return null;
    const _hH = Array.isArray(data._heroUnlockHistory) ? data._heroUnlockHistory : [];
    const _tH = Array.isArray(data._treasureUnlockHistory) ? data._treasureUnlockHistory : [];

    // ── BOSS 來源白名單(只有這些 source 算進「打 BOSS 解鎖」異常) ──
    // ⚠ summon_* / event_quest / shop_purchase 不在此清單(那些是合法多解鎖路徑)
    // ★ v3.13.11(2026-06-01)— 機率調整:三條 _5pct 來源由 5% → 15%(命名保留向下相容)
    const BOSS_SOURCES = new Set([
      'darkorb_5pct',       // 黑暗球‧希望型態 稀有掉落(v3.13.11 起 15%,命名保留)
      'japan_boss_5pct',    // 日本 BOSS S 評價解鎖(v3.13.11 起 15%,命名保留)
      'egypt_boss_clear',   // ★ v3.15.0 — 埃及雙王 S 評價解鎖(各 15%)
      'maokong_50pct',      // 貓空 50% 解鎖
      'yamata_miko_5pct',   // 八岐大蛇 / 巫女解救(v3.13.11 起 15%,命名保留)
      'taiwan_clear',       // 台灣 BOSS 通關獎勵(至寶)
      'japan_clear',        // 日本 BOSS 通關獎勵(至寶)
      'boss_drop',          // 通用 BOSS 掉落(若有用到)
    ]);
    const _isBossSource = (s) => !s || BOSS_SOURCES.has(s);  // 空 source = 老資料,當作可能是 BOSS 來源

    // ── 規則 1:battleId 衝突(精確,但需要新資料才有 battleId) ──
    const _heroByBattle = {};
    const _treByBattle  = {};
    _hH.forEach(e => {
      if(!e || !e.battleId) return;
      if(e.source === 'admin_delete' || e.source === 'admin_grant') return;
      (_heroByBattle[e.battleId] = _heroByBattle[e.battleId] || []).push(e);
    });
    _tH.forEach(e => {
      if(!e || !e.battleId) return;
      if(e.source === 'admin_delete' || e.source === 'admin_grant') return;
      (_treByBattle[e.battleId] = _treByBattle[e.battleId] || []).push(e);
    });
    const _conflictsHero = [];
    const _conflictsTre = [];
    Object.keys(_heroByBattle).forEach(bid => {
      const _list = _heroByBattle[bid];
      if(_list.length > 1) _conflictsHero.push({ battleId: bid, entries: _list });
    });
    Object.keys(_treByBattle).forEach(bid => {
      const _list = _treByBattle[bid];
      if(_list.length > 1) _conflictsTre.push({ battleId: bid, entries: _list });
    });

    // ── 規則 2:時間窗異常(主規則,適用老資料) ──
    //   3 分鐘內靠 BOSS 來源解鎖英雄 > 2 隻 = 異常
    //   滑動窗演算法:對 BOSS 來源的英雄/至寶 entries 按 at 排序,
    //                  每個 entry 往前看 180 秒內有幾筆,> 2 → 標異常
    //   ⚠ 我們列出「整個 3 分鐘窗內的所有 entries」作為一個 cluster
    //   ⚠ v3.11.35g 從 1 分鐘擴大到 3 分鐘(老師指示,涵蓋更慢的連刷)
    const TIME_WINDOW_MS = 3 * 60 * 1000;   // 3 分鐘
    const HERO_THRESHOLD = 2;            // > 2 隻 → 異常(即 3 隻起算)
    const TRE_THRESHOLD = 2;             // 至寶同樣門檻

    function _findTimeClusters(entries, threshold, idField){
      // entries 已過濾過 admin_* 但可能仍含非 BOSS 來源
      const _bossEntries = entries
        .filter(e => e && _isBossSource(e.source) && e.at)
        .sort((a,b) => (a.at || 0) - (b.at || 0));
      const _clusters = [];
      const _consumedKeys = new Set();  // 避免同 entry 被多個 cluster 收(避免重複)
      for(let i = 0; i < _bossEntries.length; i++){
        const _seed = _bossEntries[i];
        const _key0 = _seed.at + '|' + (_seed[idField] || '');
        if(_consumedKeys.has(_key0)) continue;
        // 找 [seed.at, seed.at + window] 內的所有 entries
        const _window = [];
        for(let j = i; j < _bossEntries.length; j++){
          const _e = _bossEntries[j];
          if((_e.at || 0) - (_seed.at || 0) > TIME_WINDOW_MS) break;
          _window.push(_e);
        }
        if(_window.length > threshold){
          _clusters.push({
            startAt: _seed.at,
            endAt: _window[_window.length - 1].at,
            durationMs: (_window[_window.length - 1].at - _seed.at),
            entries: _window,
            count: _window.length,
          });
          // 標記此 cluster 內所有 entry 已被收(避免重疊 cluster)
          _window.forEach(_e => _consumedKeys.add(_e.at + '|' + (_e[idField] || '')));
        }
      }
      return _clusters;
    }

    const _filteredH = _hH.filter(e => e && e.source !== 'admin_delete' && e.source !== 'admin_grant');
    const _filteredT = _tH.filter(e => e && e.source !== 'admin_delete' && e.source !== 'admin_grant');
    const _timeClustersHero = _findTimeClusters(_filteredH, HERO_THRESHOLD, 'name');
    const _timeClustersTre  = _findTimeClusters(_filteredT, TRE_THRESHOLD, 'id');

    // ════════════════════════════════════════════════════════════════════
    // ── 規則 3:短時間大量「非正常管道」新增(汙染偵查,★ v3.13.67)──
    //   老師指示:偵測「並非召喚/擊敗BOSS等正常管道」而短時間冒出的 SSR/SR/至寶/知識幣。
    //   2-A 最嚴格:source 不在白名單(含空值/other/未知)一律視為「可疑來源」。
    //   2-B:可疑 SSR 出現 1 隻就標;可疑 SR 在 10 分鐘窗內 ≥3;可疑至寶在窗內 ≥2。
    //   2-C 丙+:知識幣抓 ①對帳跳變(竄改)②短時間大量購買(盜登亂買沒用的東西)
    //            ③短時間大量賣出貴重物(盜登賤賣)。
    //   ⚠ 窗+門檻本身會自然濾掉「分散的舊資料」;但「可疑 SSR ≥1」會連 5/28 前無來源的舊 SSR
    //     一起標,渲染端已標註來源+「舊資料可能誤報」提醒,請對照同台 iPad 人工判斷。
    // ════════════════════════════════════════════════════════════════════
    const _LEGIT_SOURCES = new Set([
      'summon_rare','ticket_ssr','ticket_sr',                                  // 召喚
      'maokong_50pct','japan_boss_5pct','darkorb_5pct','yamata_miko_5pct',      // 擊敗 BOSS(英雄)
      'egypt_boss_clear',                                                       // ★ v3.15.0 埃及雙王解鎖
      'taiwan_clear','japan_clear','boss_drop',                                // 擊敗 BOSS(至寶/通用)
      'shop','shop_purchase','arena_shop',                                     // 商店/兌換
      'event_quest','student_designer',                                        // 活動/設計師
      'admin_grant','admin_restore',                                           // GM 操作(合法)
    ]);
    const _isSuspiciousSrc = (s) => {
      if(s === 'admin_delete') return false;   // 刪除稽核,不是解鎖
      return !_LEGIT_SOURCES.has(s || '');     // 空值/other/未知 = 最嚴格視為可疑
    };
    const _ssrSet3 = new Set(window.SUMMON_RARE_HEROES || []);
    const _srSet3  = new Set(window.SUMMON_SR_HEROES || []);
    const _rarityOf3 = (n) => _ssrSet3.has(n) ? 'SSR' : (_srSet3.has(n) ? 'SR' : null);
    const _W3 = 10 * 60 * 1000;   // 英雄/至寶可疑成群:10 分鐘窗
    // 在排序後 entries 中找「任一 10 分鐘窗內筆數 ≥ minCount」,回傳整窗 entries(去重)
    function _windowCluster3(entries, minCount){
      const _es = entries.filter(e => e && e.at).sort((a,b)=>(a.at||0)-(b.at||0));
      const _used = new Set();
      const _clusters = [];
      for(let i=0;i<_es.length;i++){
        if(_used.has(i)) continue;
        const _win = [i];
        for(let j=i+1;j<_es.length;j++){ if((_es[j].at||0)-(_es[i].at||0) > _W3) break; _win.push(j); }
        if(_win.length >= minCount){ _win.forEach(k=>_used.add(k)); _clusters.push(_win.map(k=>_es[k])); }
      }
      return _clusters;
    }
    // 規則 3a:英雄 SSR/SR 可疑來源
    const _suspHeroes = _filteredH.filter(e => e && e.name && _isSuspiciousSrc(e.source))
      .map(e => ({ name:e.name, source:(e.source||''), at:(e.at||0), rarity:_rarityOf3(e.name) }));
    const _suspSSR = _suspHeroes.filter(h => h.rarity === 'SSR');                          // SSR:任一就報
    const _suspSRClusters = _windowCluster3(_suspHeroes.filter(h => h.rarity === 'SR'), 3); // SR:窗內 ≥3
    // 規則 3b:至寶可疑來源(窗內 ≥2)
    const _suspTreClusters = _windowCluster3(
      _filteredT.filter(e => e && _isSuspiciousSrc(e.source))
        .map(e => ({ id:(e.id||''), source:(e.source||''), at:(e.at||0) })), 2);
    // 規則 3c:知識幣異常(竄改 / 盜登亂買 / 盜登賤賣)
    let _coinTx3 = Array.isArray(data._coinTransactions) ? data._coinTransactions : null;
    if(!_coinTx3 && typeof data._coinTransactions_s === 'string'){ try{ const _p=JSON.parse(data._coinTransactions_s); if(Array.isArray(_p)) _coinTx3=_p; }catch(_){} }
    _coinTx3 = Array.isArray(_coinTx3) ? _coinTx3 : [];
    const _COIN_W3 = 15 * 60 * 1000;            // 知識幣成群:15 分鐘窗
    const _TAMPER_MIN = 5000;                   // 對帳跳變視為竄改的金額門檻
    const _SPREE_SUM = 15000, _SPREE_CNT = 6;   // 盜登亂買:窗內累計花費 / 筆數
    const _SELLOFF_SUM = 15000, _SELLOFF_CNT = 6; // 盜登賤賣:窗內累計賣出收入 / 筆數
    const _coinAnomalies = [];
    // ① 竄改:對帳補出的「(未分類變動)」大跳變
    _coinTx3.forEach(t => {
      if(!t) return;
      const _r = String(t.reason||'');
      if(_r.indexOf('未分類變動') >= 0 && Math.abs(Number(t.amount)||0) >= _TAMPER_MIN){
        _coinAnomalies.push({ kind:'tamper', at:(t.at||0), amount:(t.amount||0), balance:(t.balance||0), reason:_r });
      }
    });
    // ②③ 短窗成群:購買(消費:商店-)/ 賣出(收入:賣出-)
    function _coinSpree3(prefix, kind, sumMin, cntMin){
      const _es = _coinTx3.filter(t => t && String(t.reason||'').indexOf(prefix)===0 && t.at)
        .sort((a,b)=>(a.at||0)-(b.at||0));
      const _used = new Set();
      for(let i=0;i<_es.length;i++){
        if(_used.has(i)) continue;
        const _win=[i]; let _sum=Math.abs(Number(_es[i].amount)||0);
        for(let j=i+1;j<_es.length;j++){ if((_es[j].at||0)-(_es[i].at||0) > _COIN_W3) break; _win.push(j); _sum+=Math.abs(Number(_es[j].amount)||0); }
        if(_win.length >= cntMin || _sum >= sumMin){
          _win.forEach(k=>_used.add(k));
          _coinAnomalies.push({ kind:kind, at:(_es[i].at||0), count:_win.length, sum:_sum,
            entries:_win.map(k=>({ at:(_es[k].at||0), amount:(_es[k].amount||0), reason:String(_es[k].reason||'') })) });
        }
      }
    }
    _coinSpree3('消費:商店-', 'spree',   _SPREE_SUM,   _SPREE_CNT);     // 盜登亂買
    _coinSpree3('收入:賣出-', 'selloff', _SELLOFF_SUM, _SELLOFF_CNT);   // 盜登賤賣貴重物
    const _hasRule3 = (_suspSSR.length>0 || _suspSRClusters.length>0 || _suspTreClusters.length>0 || _coinAnomalies.length>0);

    // ── 沒任何異常 → return null ──
    if(_conflictsHero.length === 0 && _conflictsTre.length === 0
       && _timeClustersHero.length === 0 && _timeClustersTre.length === 0
       && !_hasRule3){
      return null;
    }

    return {
      uid,
      name: data.name || '',
      email: data.email || '',
      // 規則 1:battleId
      conflictsHero: _conflictsHero,
      conflictsTreasure: _conflictsTre,
      // 規則 2:時間窗(★ 新增)
      timeClustersHero: _timeClustersHero,
      timeClustersTreasure: _timeClustersTre,
      // 規則 3:汙染偵查(★ v3.13.67)
      suspiciousSSR: _suspSSR,
      suspiciousSRClusters: _suspSRClusters,
      suspiciousTreasureClusters: _suspTreClusters,
      coinAnomalies: _coinAnomalies,
      totalConflicts: _conflictsHero.length + _conflictsTre.length
        + _timeClustersHero.length + _timeClustersTre.length
        + _suspSSR.length + _suspSRClusters.length + _suspTreClusters.length + _coinAnomalies.length,
    };
  }

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.11.35d(2026-05-29) — 一鍵清除異常 + 自動補償 + 玩家通知
  //
  //   單一 API 完成整套災情後處理:
  //     1. 接收 GM 計算好的「要刪英雄清單」+「補償明細」+「公告文字」
  //     2. 對每隻英雄呼叫 _fbAdminDeleteUnlockedHero(已存在,完整清 7 欄位)
  //     3. 累加補償結果一次寫入(走 _fbCompensatePlayer)
  //     4. 寄 pendingAdminNotifications 通知(玩家下次登入彈窗)
  //     5. 寫 _adminLastAction audit
  //
  //   API 設計:由 admin_panel.js 端先計算好補償明細傳進來,讓本 API 維持
  //   純粹「執行 + 寫入」職責,計算邏輯放在 admin_panel.js 可隨時調整公式
  //   不用改 index.html。
  //
  //   參數:
  //     uid          玩家 uid
  //     deleteList   [{name, at}]  要刪除的英雄清單(name 唯一,at 給 audit 用)
  //     compensation { coins, backpack: {itemId: qty}, summary: '簡短摘要' }
  //     notification { title, body }  公告文字(老師親自撰寫)
  //     opts         { reason }
  //
  //   回傳:{ ok, deleted: [{name, ok, error?}], compensation: {...}, notificationTs }
  // ════════════════════════════════════════════════════════════════════
  // ════════════════════════════════════════════════════════════════════
  // ★ v3.11.35e(2026-05-29) — 簽名擴充:加 opts.fruitsToRemove 處理果實清除
  //   ⚠ deleteList 可為空(只清果實的情境)
  // ════════════════════════════════════════════════════════════════════
  window._fbAdminCleanupAndCompensate = async function(uid, deleteList, compensation, notification, opts){
    if(!uid) throw new Error('uid 為必要');
    deleteList = Array.isArray(deleteList) ? deleteList : [];
    compensation = compensation || {};
    notification = notification || {};
    opts = opts || {};
    const _fruitsToRemove = parseInt(opts.fruitsToRemove, 10) || 0;
    if(deleteList.length === 0 && _fruitsToRemove === 0){
      throw new Error('deleteList 與 fruitsToRemove 至少一項必須有內容');
    }
    const _adminEmail = (_fbAuth.currentUser && _fbAuth.currentUser.email) || 'unknown';
    const _reason = opts.reason || '系統 BUG 異常解鎖回收';

    const _result = {
      ok: true,
      uid,
      deleted: [],
      fruitsRemoved: null,  // ★ v3.11.35e — 果實清除結果
      compensationApplied: null,
      notificationTs: null,
      timestamp: Date.now(),
    };

    try{
      // ── 步驟 1:逐個刪除英雄(沿用既有 API,有 audit log) ──
      if(deleteList.length > 0){
        console.log('[_fbAdminCleanupAndCompensate] 步驟 1:刪除 ' + deleteList.length + ' 隻英雄');
        for(const _item of deleteList){
          const _name = _item && _item.name;
          if(!_name){ _result.deleted.push({ name: '(空)', ok: false, error: 'name 為空' }); continue; }
          try{
            await window._fbAdminDeleteUnlockedHero(uid, _name, { reason: _reason + ' / 批次清除' });
            _result.deleted.push({ name: _name, ok: true });
          }catch(eDel){
            console.warn('[_fbAdminCleanupAndCompensate] 刪除失敗 ' + _name, eDel);
            _result.deleted.push({ name: _name, ok: false, error: String(eDel.message || eDel) });
          }
        }
        const _successCount = _result.deleted.filter(d => d.ok).length;
        console.log('[_fbAdminCleanupAndCompensate] 步驟 1 完成:成功 ' + _successCount + ' / ' + deleteList.length);
      }

      // ── 步驟 1.5(★ v3.11.35e 新增):清除異常果實(若指定) ──
      if(_fruitsToRemove > 0){
        console.log('[_fbAdminCleanupAndCompensate] 步驟 1.5:清除果實 ' + _fruitsToRemove + ' 顆');
        try{
          const _fruitR = await window._fbAdminClearAbnormalFruits(uid, _fruitsToRemove, { reason: _reason + ' / 果實清除' });
          _result.fruitsRemoved = _fruitR;
        }catch(eFruit){
          console.warn('[_fbAdminCleanupAndCompensate] 果實清除失敗', eFruit);
          _result.fruitsRemovedError = String(eFruit.message || eFruit);
        }
      }

      // ── 步驟 2:套用補償(沿用 _fbCompensatePlayer,coinsMode='add' 累加) ──
      if(compensation && (compensation.coins || (compensation.backpack && Object.keys(compensation.backpack).length))){
        console.log('[_fbAdminCleanupAndCompensate] 步驟 2:套用補償', compensation);
        const _compPayload = {
          coins: compensation.coins || 0,
          coinsMode: 'add',
          backpack: compensation.backpack || {},
        };
        try{
          const _compResult = await window._fbCompensatePlayer(uid, _compPayload);
          _result.compensationApplied = _compResult;
        }catch(eComp){
          console.error('[_fbAdminCleanupAndCompensate] 補償寫入失敗', eComp);
          _result.compensationError = String(eComp.message || eComp);
        }
      } else {
        console.log('[_fbAdminCleanupAndCompensate] 步驟 2 跳過(無補償項目)');
      }

      // ── 步驟 3:發送通知給玩家(走 _fbAdminSendNotificationToPlayer 子集合) ──
      if(notification && (notification.title || notification.body)){
        console.log('[_fbAdminCleanupAndCompensate] 步驟 3:發送公告通知');
        try{
          // removedItems 加上果實摘要
          const _removedItems = {
            heroes: _result.deleted.filter(d => d.ok).map(d => d.name),
          };
          if(_result.fruitsRemoved && _result.fruitsRemoved.actualRemove > 0){
            _removedItems.fruits = _result.fruitsRemoved.actualRemove;
          }
          const _noteResult = await window._fbAdminSendNotificationToPlayer(uid, {
            title: notification.title || '管理員公告',
            body: notification.body || '',
            type: 'compensation',
            compensation: {
              knowledgeCoins: compensation.coins || 0,
              items: compensation.backpack ? Object.keys(compensation.backpack).map(k => ({
                id: k,
                count: compensation.backpack[k],
              })) : [],
            },
            removedItems: _removedItems,
          });
          _result.notificationTs = _noteResult.ts;
        }catch(eNote){
          console.warn('[_fbAdminCleanupAndCompensate] 通知發送失敗(不致命)', eNote);
          _result.notificationError = String(eNote.message || eNote);
        }
      }

      // ── 步驟 4:寫 audit log ──
      const _successCount = _result.deleted.filter(d => d.ok).length;
      try{
        await updateDoc(doc(_fbDb, 'players', uid), {
          _adminLastAction: {
            type: 'cleanup_and_compensate',
            target: 'heroes: ' + _result.deleted.filter(d => d.ok).map(d => d.name).join(',')
              + (_result.fruitsRemoved ? ' + fruits:' + _result.fruitsRemoved.actualRemove : ''),
            count: _successCount,
            fruitsRemoved: _result.fruitsRemoved ? _result.fruitsRemoved.actualRemove : 0,
            at: Date.now(),
            by: _adminEmail,
            reason: _reason,
            compensationSummary: compensation.summary || '',
          }
        });
      }catch(eAudit){ console.warn('[_fbAdminCleanupAndCompensate] audit 寫入失敗', eAudit); }

      console.log('[_fbAdminCleanupAndCompensate] ✅ 完成 uid=' + uid
        + ' 刪除 ' + _successCount + ' / ' + deleteList.length + ' 隻'
        + (_result.fruitsRemoved ? ' + 果實 ' + _result.fruitsRemoved.actualRemove + ' 顆' : ''));
      return _result;
    }catch(e){
      console.error('[_fbAdminCleanupAndCompensate] 流程例外', e);
      _result.ok = false;
      _result.error = String(e.message || e);
      throw e;
    }
  };

  // ════════════════════════════════════════════════════════════════════
  // ★★★ v3.13.49(2026-06-05)— 汙染英雄「刪除預告制」(老師需求:不直接刪, 改預告 + 玩家保留 + 可復原)★★★
  // ────────────────────────────────────────────────────────────────────
  //   流程:
  //     1) GM 發預告 _fbAdminCreatePollutionPurgeNotice → 只寫 _pendingPollutionPurge 欄位, 不動英雄(零風險)
  //     2) 玩家登入彈窗 → 勾選保留 ≤1 隻 → _fbPlayerRespondPollutionPurge 回寫保留選擇 + status=acknowledged(極小自寫)
  //     3) GM 套用 _fbAdminApplyPollutionPurge → 刪前存「完整快照」到 _pollutionPurgeLog(可復原)→ 走既有
  //        _fbAdminCleanupAndCompensate(含至寶保護 + 補償 + 通知)→ status=applied
  //     4) GM 復原 _fbAdminRestorePurgedHeroes → 從快照重建(純加回, admin 強制寫)
  //     5) GM 撤銷 _fbAdminCancelPollutionPurgeNotice(套用前可撤)
  //   安全:只有步驟 3 動到英雄資料, 且 admin 專屬 + 刪前快照 + 可逆; 其餘步驟都不動英雄/幣。
  // ════════════════════════════════════════════════════════════════════
  window._fbAdminCreatePollutionPurgeNotice = async function(uid, heroes, opts){
    if(!uid) throw new Error('uid 為必要');
    heroes = Array.isArray(heroes) ? heroes : [];
    opts = opts || {};
    const _treasuresIn = Array.isArray(opts.treasures) ? opts.treasures : [];
    if(!heroes.length && !_treasuresIn.length) throw new Error('英雄與至寶清單不可都為空');
    const _adminEmail = (_fbAuth.currentUser && _fbAuth.currentUser.email) || 'unknown';
    const _ref = doc(_fbDb, 'players', uid);
    const _snap = await getDoc(_ref);
    if(!_snap.exists()) throw new Error('找不到玩家');
    const _d = _snap.data() || {};
    const _exist = _d._pendingPollutionPurge;
    if(_exist && _exist.status === 'pending') throw new Error('此玩家已有一筆「待確認」預告, 請先撤銷或等套用');
    if(_exist && _exist.status === 'acknowledged') throw new Error('此玩家有一筆「玩家已確認、待套用」的預告, 請先套用或撤銷');
    // 防呆:只允許「玩家目前真的擁有」的英雄/至寶進預告
    const _owned = new Set(Array.isArray(_d.unlockedHeroes) ? _d.unlockedHeroes : []);
    const _heroes = heroes.filter(h => h && h.name && _owned.has(h.name)).map(h => ({
      name: h.name, rarity: h.rarity || '', lv: h.lv || 1,
      source: (h.source != null) ? h.source : null, cat: h.cat || 'pollution',
      comp: h.comp || { coins: 0, items: {} },
    }));
    const _twOwned = (_d.taiwanTreasureData && typeof _d.taiwanTreasureData === 'object') ? _d.taiwanTreasureData : {};
    const _treasures = _treasuresIn.filter(t => t && t.id && (t.id in _twOwned)).map(t => ({
      id: t.id, name: t.name || t.id, lv: t.lv || 1,
      source: (t.source != null) ? t.source : null, cat: t.cat || 'pollution',
      comp: t.comp || { coins: 0, items: {} },
    }));
    if(!_heroes.length && !_treasures.length) throw new Error('清單中沒有玩家目前擁有的英雄/至寶(可能已被移除)');
    const _purge = {
      id: 'pp_' + Date.now() + '_' + Math.random().toString(36).slice(2,8),
      createdAt: Date.now(), createdBy: _adminEmail,
      reason: opts.reason || '清除來源不明(疑似跨帳號汙染)的英雄/至寶',
      evidence: opts.evidence || '',
      heroes: _heroes,
      treasures: _treasures,
      status: 'pending', playerKeptHero: null, playerKeptTreasure: null, playerRespondedAt: null,
      appliedAt: null, appliedBy: null,
    };
    await updateDoc(_ref, {
      _pendingPollutionPurge: _purge,
      _adminLastAction: { type:'pollution_purge_notice', count:(_heroes.length+_treasures.length), at:Date.now(), by:_adminEmail }
    });
    console.log('[_fbAdminCreatePollutionPurgeNotice] ✅ uid=' + uid + ' 發出刪除預告(英雄 ' + _heroes.length + ' / 至寶 ' + _treasures.length + ')');
    return { ok:true, uid, purgeId:_purge.id, count:(_heroes.length+_treasures.length), heroCount:_heroes.length, treasureCount:_treasures.length };
  };

  // 玩家端:確認預告 + 勾選保留(極小自寫, 不動英雄/幣 → 不觸發健康度守門; 與 wbAbnormalRemoval 確認同模式)
  window._fbPlayerRespondPollutionPurge = async function(keptHeroName, keptTreasureId){
    const _uid = window._gUserId;
    if(!_uid || !_fbDb) throw new Error('尚未登入');
    const _ref = doc(_fbDb, 'players', _uid);
    const _snap = await getDoc(_ref);
    if(!_snap.exists()) throw new Error('找不到玩家文件');
    const _pp = (_snap.data() || {})._pendingPollutionPurge;
    if(!_pp || _pp.status !== 'pending') return { ok:true, noop:true };
    let _kept = null;
    if(keptHeroName){
      const _inList = (_pp.heroes||[]).some(h => h && h.name === keptHeroName);
      if(_inList) _kept = keptHeroName;
    }
    let _keptTre = null;
    if(keptTreasureId){
      const _inT = (_pp.treasures||[]).some(t => t && t.id === keptTreasureId);
      if(_inT) _keptTre = keptTreasureId;
    }
    await updateDoc(_ref, {
      '_pendingPollutionPurge.status': 'acknowledged',
      '_pendingPollutionPurge.playerKeptHero': _kept,
      '_pendingPollutionPurge.playerKeptTreasure': _keptTre,
      '_pendingPollutionPurge.playerRespondedAt': Date.now(),
    });
    console.log('[_fbPlayerRespondPollutionPurge] ✅ 玩家已確認, 保留英雄:' + (_kept || '(無)') + ' / 至寶:' + (_keptTre || '(無)'));
    return { ok:true, keptHero:_kept, keptTreasure:_keptTre };
  };

  // GM 套用刪除(admin):刪前存完整快照可復原, 再走既有 _fbAdminCleanupAndCompensate
  window._fbAdminApplyPollutionPurge = async function(uid, opts){
    if(!uid) throw new Error('uid 為必要');
    opts = opts || {};
    const _adminEmail = (_fbAuth.currentUser && _fbAuth.currentUser.email) || 'unknown';
    const _ref = doc(_fbDb, 'players', uid);
    const _snap = await getDoc(_ref);
    if(!_snap.exists()) throw new Error('找不到玩家');
    const _d = _snap.data() || {};
    const _pp = _d._pendingPollutionPurge;
    if(!_pp) throw new Error('此玩家沒有刪除預告');
    if(_pp.status === 'applied') throw new Error('此預告已套用過');
    if(_pp.status === 'cancelled') throw new Error('此預告已被撤銷');
    if(_pp.status !== 'acknowledged' && !opts.force) throw new Error('玩家尚未確認預告(可用 force=true 強制套用, 但建議等玩家先看到)');
    const _kept = _pp.playerKeptHero || (opts.force ? (opts.keptHero || null) : null);
    const _keptTre = _pp.playerKeptTreasure || (opts.force ? (opts.keptTreasure || null) : null);
    const _ownedSet = new Set(Array.isArray(_d.unlockedHeroes) ? _d.unlockedHeroes : []);
    const _toDelete = (_pp.heroes||[]).filter(h => h && h.name && h.name !== _kept && _ownedSet.has(h.name));
    // ── 英雄刪除前快照 ──
    const _hl=_d.heroLevels||{}, _hxp=_d.heroExp||{}, _hsi=_d.heroStatInvested||{}, _hsp=_d.heroStatPoints||{},
          _hsk=_d.heroSkillLevels||{}, _hbl=_d.heroBurstLevels||{}, _htl=_d.heroTraitLevel||{};
    let _tre = (_d.taiwanTreasureData && typeof _d.taiwanTreasureData==='object') ? _d.taiwanTreasureData : {};
    const _treByHero = {};
    Object.keys(_tre).forEach(tid => { const r=_tre[tid]; if(r && r.equippedTo){ (_treByHero[r.equippedTo]=_treByHero[r.equippedTo]||[]).push(tid); } });
    const _snapshots = _toDelete.map(h => ({
      name:h.name, rarity:h.rarity||'',
      lv:_hl[h.name]||1, exp:_hxp[h.name]||0,
      invested:(_hsi[h.name]!=null)?_hsi[h.name]:null, freePts:_hsp[h.name]||0,
      skill:(_hsk[h.name]!=null)?_hsk[h.name]:null, burst:(_hbl[h.name]!=null)?_hbl[h.name]:null, trait:(_htl[h.name]!=null)?_htl[h.name]:null,
      equippedTreasures:_treByHero[h.name]||[], comp:h.comp||{coins:0,items:{}},
    }));
    // ── 至寶刪除前快照(★ v3.13.49b)──
    const _twData = (typeof _tre==='object' && _tre) ? Object.assign({}, _tre) : {};
    const _toDeleteTre = (_pp.treasures||[]).filter(t => t && t.id && t.id !== _keptTre && (t.id in _twData));
    const _treSnapshots = _toDeleteTre.map(t => {
      const r = _twData[t.id] || {};
      return { id:t.id, name:t.name||t.id, lv:r.lv||1, exp:r.exp||0,
               equippedTo:(r.equippedTo!=null)?r.equippedTo:null, invested:(r.invested!=null)?r.invested:null,
               comp:t.comp||{coins:0,items:{}} };
    });
    // ── 聚合補償(英雄 + 至寶;只賠被刪、保留的不賠)──
    let _coins=0; const _items={};
    _snapshots.forEach(s => { _coins += (s.comp.coins||0); Object.keys(s.comp.items||{}).forEach(k => { _items[k]=(_items[k]||0)+(s.comp.items[k]||0); }); });
    _treSnapshots.forEach(s => { _coins += (s.comp.coins||0); Object.keys(s.comp.items||{}).forEach(k => { _items[k]=(_items[k]||0)+(s.comp.items[k]||0); }); });
    // ── 先寫快照 log(確保中斷也能復原)──
    let _log = Array.isArray(_d._pollutionPurgeLog) ? _d._pollutionPurgeLog.slice() : [];
    _log.push({
      purgeId:_pp.id, appliedAt:Date.now(), appliedBy:_adminEmail,
      reason:_pp.reason||'', evidence:_pp.evidence||'',
      keptHero:_kept||null, keptTreasure:_keptTre||null,
      deletedHeroes:_snapshots, deletedTreasures:_treSnapshots,
      compGranted:{coins:_coins, items:_items}, restored:false,
    });
    if(_log.length>50) _log = _log.slice(-50);
    await updateDoc(_ref, { _pollutionPurgeLog:_log });
    // ── 移除至寶 ──
    // ★★ v5.130.0(老師裁定甲)— 改走三槽同寫單一真相(舊版只寫主檔 ⇒ live/safe 至寶殘留復活);
    //   英雄側免改:下方走 _fbAdminCleanupAndCompensate → _fbAdminDeleteUnlockedHero(v5.130.0 已三槽)。
    if(_toDeleteTre.length){
      const _treIds130 = _toDeleteTre.map(t => t.id);
      await window._lxpsAdminApplyAllSlots(uid, function(d, slot){
        return window._lxpsSlotTreasurePatch(d, _treIds130, {
          mode:'remove', by:_adminEmail, uid:uid, slot:slot,
          source:'admin_delete', reason:'汙染至寶刪除預告套用'
        });
      }, { label:'汙染至寶刪除', stampRestore:false });   // restore 交由本函式收尾段的既有旗標統一處理
    }
    // ── 通知文案(英雄 + 至寶)──
    const _bodyParts = [ _pp.reason || '清除來源不明的英雄/至寶' ];
    if(_kept) _bodyParts.push('你選擇保留英雄:' + _kept + '(保留的不補償)。');
    if(_keptTre) _bodyParts.push('你選擇保留至寶:' + _keptTre + '(保留的不補償)。');
    if(_toDeleteTre.length) _bodyParts.push('一併整理了 ' + _toDeleteTre.length + ' 件來源不明的至寶。');
    _bodyParts.push('已自動補償你在被移除英雄/至寶上的投入(知識幣 + 至寶經驗卷軸等)。');
    const _bodyText = _bodyParts.join('\n');
    // ── 執行英雄刪除 + 發「總補償」+ 通知(走既有批次 API, 含至寶保護);純至寶時走 inline 補償+通知 ──
    let _cleanupResult = null;
    if(_toDelete.length){
      _cleanupResult = await window._fbAdminCleanupAndCompensate(
        uid, _toDelete.map(h => ({name:h.name})),
        { coins:_coins, backpack:_items },
        { title:'🧹 帳號資料整理通知', body:_bodyText },
        { reason:'汙染英雄/至寶刪除預告套用 / ' + (_pp.reason||'') }
      );
    } else if(_coins>0 || Object.keys(_items).length){
      // 純至寶:直接補償 + 通知
      const _g = await getDoc(_ref); const _gd = _g.exists() ? (_g.data()||{}) : {};
      const _newCoins = (_gd.knowledgeCoins||0) + _coins;
      const _bp = Object.assign({}, _gd.playerBackpack||{});
      Object.keys(_items).forEach(k => { const _cap=(k==='treasure_exp_scroll')?99:999; _bp[k]=Math.min(_cap,(_bp[k]||0)+(_items[k]||0)); });
      await updateDoc(_ref, { knowledgeCoins:_newCoins, playerBackpack:_bp, playerBackpack_s:JSON.stringify(_bp) });
      try{
        if(typeof window._fbAdminSendNotificationToPlayer === 'function'){
          await window._fbAdminSendNotificationToPlayer(uid, {
            title:'🧹 帳號資料整理通知', body:_bodyText, type:'compensation',
            compensation:{ knowledgeCoins:_coins, items:Object.keys(_items).map(k=>({id:k,count:_items[k]})) },
            removedItems:{ treasures:_toDeleteTre.map(t=>t.name||t.id) },
          });
        }
      }catch(_eNote){ console.warn('[apply 純至寶] 通知失敗', _eNote); }
    }
    // ── 收尾:標記 applied + 反汙染信號 ──
    await updateDoc(_ref, {
      '_pendingPollutionPurge.status':'applied',
      '_pendingPollutionPurge.appliedAt':Date.now(),
      '_pendingPollutionPurge.appliedBy':_adminEmail,
      '_pendingPollutionPurge.playerKeptHero':_kept,
      '_pendingPollutionPurge.playerKeptTreasure':_keptTre,
      _adminForceRestore:true,
      _adminRescueSignal:{ ts:Date.now(), by:_adminEmail, fromSlot:'pollution_purge', version:1 },
      _adminLastAction:{ type:'pollution_purge_apply', count:(_toDelete.length+_toDeleteTre.length), heroes:_toDelete.length, treasures:_toDeleteTre.length, kept:_kept||null, keptTreasure:_keptTre||null, at:Date.now(), by:_adminEmail },
    });
    console.log('[_fbAdminApplyPollutionPurge] ✅ uid='+uid+' 已套用(英雄 '+_toDelete.length+' / 至寶 '+_toDeleteTre.length+', 保留英雄 '+(_kept||'無')+' / 至寶 '+(_keptTre||'無')+')');
    return { ok:true, uid, deleted:_toDelete.length, deletedTreasures:_toDeleteTre.length, kept:_kept, keptTreasure:_keptTre, compGranted:{coins:_coins,items:_items}, cleanupResult:_cleanupResult, purgeId:_pp.id };
  };

  // GM 復原(admin):從快照重建被刪英雄(純加回)
  window._fbAdminRestorePurgedHeroes = async function(uid, purgeId){
    if(!uid) throw new Error('uid 為必要');
    const _adminEmail = (_fbAuth.currentUser && _fbAuth.currentUser.email) || 'unknown';
    const _ref = doc(_fbDb, 'players', uid);
    const _snap = await getDoc(_ref);
    if(!_snap.exists()) throw new Error('找不到玩家');
    const _d = _snap.data() || {};
    let _log = Array.isArray(_d._pollutionPurgeLog) ? _d._pollutionPurgeLog.slice() : [];
    const _idx = _log.findIndex(e => e && e.purgeId === purgeId);
    if(_idx < 0) throw new Error('找不到該刪除記錄');
    const _entry = _log[_idx];
    if(_entry.restored) throw new Error('此刪除記錄已復原過');
    const _deleted = Array.isArray(_entry.deletedHeroes) ? _entry.deletedHeroes : [];
    const _deletedTre = Array.isArray(_entry.deletedTreasures) ? _entry.deletedTreasures : [];
    if(!_deleted.length && !_deletedTre.length) throw new Error('此記錄沒有可復原的英雄/至寶');
    // ★★ v5.130.0(老師裁定甲)— 改走三槽同寫單一真相 ★★
    //   舊版只寫主檔 ⇒ 復原後 live/safe 仍留著較新的 admin_delete 帳本,latest-wins 又把
    //   剛復原的英雄濾掉(復原白做);快照(_pollutionPurgeLog)只存於主檔,三槽套同一份快照值,
    //   但每槽只動被復原英雄/至寶的鍵、以該槽自己的表為底 ⇒ 不會蓋掉學生別台其他進度。
    const _now = Date.now();
    const _GK7 = ['heroLevels','heroExp','heroStatInvested','heroStatPoints','heroSkillLevels','heroBurstLevels','heroTraitLevel'];
    const _SNAP_KEY = { heroLevels:'lv', heroExp:'exp', heroStatInvested:'invested', heroStatPoints:'freePts', heroSkillLevels:'skill', heroBurstLevels:'burst', heroTraitLevel:'trait' };
    const _res130 = await window._lxpsAdminApplyAllSlots(uid, function(dd, slot){
      var P = {};
      // ① unlockedHeroes union
      var un = _lxpsSlotArr(dd, 'unlockedHeroes'); un = un ? un.slice() : [];
      var seen = {}; un.forEach(function(n){ if(n) seen[n]=1; });
      _deleted.forEach(function(s){ if(s && s.name && !seen[s.name]){ un.push(s.name); seen[s.name]=1; } });
      P.unlockedHeroes = un;
      try{ P.unlockedHeroes_s = JSON.stringify(un); }catch(_){}
      // ② 七張養成表:以該槽自己的表為底,只覆寫被復原英雄的鍵(快照值)
      _GK7.forEach(function(k){
        var o = Object.assign({}, _lxpsSlotObj(dd, k) || {});
        var hit = false;
        _deleted.forEach(function(s){
          if(!s || !s.name) return;
          var v = s[_SNAP_KEY[k]];
          if(v != null){ o[s.name] = v; hit = true; }
        });
        if(!hit) return;
        P[k] = o;
        try{ P[k+'_s'] = JSON.stringify(o); }catch(_){}
      });
      // ③ 至寶:重建被刪至寶 + 重掛裝備對象(以該槽自己的至寶表為底)
      var tre = JSON.parse(JSON.stringify(_lxpsSlotObj(dd, 'taiwanTreasureData') || {}));
      var treChanged = false;
      _deleted.forEach(function(s){
        if(!s || !s.name) return;
        (s.equippedTreasures || []).forEach(function(tid){ if(tre[tid]){ tre[tid].equippedTo = s.name; treChanged = true; } });
      });
      _deletedTre.forEach(function(t){
        if(!t || !t.id) return;
        tre[t.id] = {
          lv: t.lv || 1, exp: t.exp || 0,
          equippedTo: (t.equippedTo != null) ? t.equippedTo : null,
          invested: (t.invested != null) ? t.invested : { hp:0, atk:0, sp:0, spd:0 },
        };
        treChanged = true;
      });
      if(treChanged){
        P.taiwanTreasureData = tre;
        try{ P.taiwanTreasureData_s = JSON.stringify(tre); }catch(_){}
      }
      // ④ 帳本:每槽都寫 admin_restore(蓋掉該槽較新的 admin_delete → latest-wins 不再濾掉)
      var hist = _lxpsSlotArr(dd, '_heroUnlockHistory'); hist = hist ? hist.slice() : [];
      _deleted.forEach(function(s){ if(s && s.name) hist.push({ name:s.name, at:_now, source:'admin_restore', adminAction:true, restoredBy:_adminEmail, fromPurge:purgeId, slot:slot }); });
      if(hist.length > 300) hist = hist.slice(-300);
      P._heroUnlockHistory = hist;
      try{ P._heroUnlockHistory_s = JSON.stringify(hist); }catch(_){}
      if(_deletedTre.length){
        var treHist = _lxpsSlotArr(dd, '_treasureUnlockHistory'); treHist = treHist ? treHist.slice() : [];
        _deletedTre.forEach(function(t){ if(t && t.id) treHist.push({ id:t.id, at:_now, source:'admin_restore', adminAction:true, restoredBy:_adminEmail, fromPurge:purgeId, slot:slot }); });
        if(treHist.length > 300) treHist = treHist.slice(-300);
        P._treasureUnlockHistory = treHist;
        try{ P._treasureUnlockHistory_s = JSON.stringify(treHist); }catch(_){}
      }
      // ⑤ 只有主檔:快照標記已復原 + 反汙染信號
      if(slot === 'main'){
        _log[_idx] = Object.assign({}, _entry, { restored:true, restoredAt:_now, restoredBy:_adminEmail });
        P._pollutionPurgeLog = _log;
      }
      return P;
    }, {
      label:'汙染刪除復原',
      rescueSignal:{ ts:_now, by:_adminEmail, fromSlot:'pollution_restore', version:1 },
      lastAction:{ type:'pollution_purge_restore', purgeId, count:(_deleted.length+_deletedTre.length), heroes:_deleted.length, treasures:_deletedTre.length, at:_now, by:_adminEmail }
    });
    console.log('[_fbAdminRestorePurgedHeroes v5.130.0] ✅ uid='+uid+' 復原 英雄 '+_deleted.length+' / 至寶 '+_deletedTre.length+'(purge '+purgeId+')');
    return { ok:true, uid, restored:_deleted.length, restoredTreasures:_deletedTre.length, purgeId, slots:_res130 };
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.15.84 — GM「英雄誤刪救回」一鍵掃描 + 復原(排除 GM 手動刪除 admin_delete 的)
  //   找出因 v3.15.40 稽核熔斷被誤刪的英雄:雲端 heroLevels 等級>1(練過)或 身上裝著至寶(equippedTo),
  //   但不在 unlockedHeroes,且最近一筆解鎖紀錄不是 GM admin_delete → 視為「被誤刪、可救回」。
  //   復原:_fbCompensatePlayer 只帶 unlockedHeroes → union 加回、heroLevels 取 max(不重置等級)、
  //         主檔+live+safe 三槽同寫、_adminForceRestore 繞健康度守門、記 compensation 歷史、不彈玩家通知。
  //   ★ 排除 admin_delete(老師要求):GM 之前手動刪除的英雄不會被救回。
  // ════════════════════════════════════════════════════════════════════
  function _computeDeletedHeroesFromDoc(d){
    try{
      if(!d) return [];
      var _isPHN = function(n){ try{ if(typeof _PLAYER_HERO_NAMES==='undefined'||!_PLAYER_HERO_NAMES) return true; return _PLAYER_HERO_NAMES.has(n); }catch(_){ return true; } };
      var _unl = new Set(Array.isArray(d.unlockedHeroes) ? d.unlockedHeroes : []);
      var _lv = (d.heroLevels && typeof d.heroLevels === 'object') ? d.heroLevels : {};
      // 身上裝著至寶的英雄(equippedTo;字串版 taiwanTreasureData_s 優先免 map 污染)
      var _equip = new Set();
      try{
        var _ttd = null;
        if(typeof d.taiwanTreasureData_s === 'string'){ try{ var _p = JSON.parse(d.taiwanTreasureData_s); if(_p && typeof _p==='object') _ttd = _p; }catch(_){} }
        if(!_ttd && d.taiwanTreasureData && typeof d.taiwanTreasureData==='object') _ttd = d.taiwanTreasureData;
        if(_ttd){ Object.keys(_ttd).forEach(function(tid){ var t=_ttd[tid]; if(t && t.equippedTo) _equip.add(t.equippedTo); }); }
      }catch(_){}
      // 解鎖紀錄最近一筆(判 admin_delete)
      var _rec = {};
      try{ if(Array.isArray(d._heroUnlockHistory)){ d._heroUnlockHistory.forEach(function(e){ if(e && e.name){ if(!_rec[e.name] || (e.at||0) >= _rec[e.name].at) _rec[e.name] = { at:(e.at||0), source:e.source||'' }; } }); } }catch(_){}
      // 候選 = 練過(等級>1)∪ 身上裝著至寶
      var _cand = new Set();
      Object.keys(_lv).forEach(function(n){ if(typeof _lv[n]==='number' && _lv[n] > 1) _cand.add(n); });
      _equip.forEach(function(n){ if(n) _cand.add(n); });
      var _out = [];
      _cand.forEach(function(n){
        if(_unl.has(n)) return;                       // 已解鎖,沒被刪
        if(!_isPHN(n)) return;                         // 非玩家英雄白名單(過濾 BOSS/小怪/舊名雜訊)
        var r = _rec[n];
        if(r && r.source === 'admin_delete') return;   // ★ GM 手動刪除 → 排除(老師要求)
        _out.push({ name:n, lv:(typeof _lv[n]==='number' ? _lv[n] : 1), equipped:_equip.has(n) });
      });
      return _out;
    }catch(_e){ return []; }
  }

  // 掃描全體玩家,回傳「有被誤刪英雄」的玩家清單(依數量降序)
  window._fbAdminScanDeletedHeroes = async function(){
    var snap = await getDocs(collection(_fbDb, 'players'));
    var _affected = [];
    var _scanned = 0, _totalHeroes = 0;
    snap.forEach(function(docSnap){
      _scanned++;
      try{
        var d = docSnap.data() || {};
        var _missing = _computeDeletedHeroesFromDoc(d);
        if(_missing.length){
          _totalHeroes += _missing.length;
          _affected.push({
            uid: docSnap.id,
            email: (d.email || '').toLowerCase(),
            displayName: (d.displayName || d.displayLabel || ''),
            heroes: _missing.map(function(h){ return h.name; }),
            detail: _missing,
            count: _missing.length
          });
        }
      }catch(_){}
    });
    _affected.sort(function(a,b){ return b.count - a.count; });
    console.log('[_fbAdminScanDeletedHeroes] 掃描 ' + _scanned + ' 名玩家,' + _affected.length + ' 名有被誤刪英雄,共 ' + _totalHeroes + ' 隻待救回');
    return { scanned:_scanned, affectedCount:_affected.length, totalHeroes:_totalHeroes, affected:_affected };
  };

  // 復原單一玩家(重新讀 server doc 再算一次,避免用過時清單)
  window._fbAdminRestoreDeletedHeroesForUid = async function(uid){
    if(!uid) throw new Error('uid 為必要');
    if(typeof window._fbCompensatePlayer !== 'function') throw new Error('_fbCompensatePlayer 未載入');
    var _adminEmail = (_fbAuth.currentUser && _fbAuth.currentUser.email) || 'unknown';
    var d = {};
    try{ var snap = await getDocFromServer(doc(_fbDb, 'players', uid)); if(snap.exists()) d = snap.data() || {}; }
    catch(e){ throw new Error('讀取玩家失敗: ' + ((e && e.message) || e)); }
    var _missing = _computeDeletedHeroesFromDoc(d);
    if(!_missing.length) return { ok:true, uid:uid, restored:0, heroes:[] };
    var _names = _missing.map(function(h){ return h.name; });
    // 只帶 unlockedHeroes → _fbCompensatePlayer union 加回、heroLevels 取 max(不重置)、三槽同寫、不彈通知
    await window._fbCompensatePlayer(uid, {
      unlockedHeroes: _names,
      by: _adminEmail,
      reason: '英雄誤刪救回(v3.15.84 GM 一鍵掃描)',
      summary: '救回被誤刪英雄 ' + _names.length + ' 隻: ' + _names.join('、')
    });
    console.log('[_fbAdminRestoreDeletedHeroesForUid] ✅ uid=' + uid + ' 救回 ' + _names.length + ' 隻: ' + _names.join('、'));
    return { ok:true, uid:uid, restored:_names.length, heroes:_names };
  };

  // 一鍵全部復原(可傳 uid 陣列;不傳則先掃描全體再逐一復原)
  window._fbAdminRestoreAllDeletedHeroes = async function(uids){
    var _list = Array.isArray(uids) ? uids.slice() : null;
    if(!_list){
      var _scan = await window._fbAdminScanDeletedHeroes();
      _list = _scan.affected.map(function(a){ return a.uid; });
    }
    var _okCount = 0, _heroCount = 0, _failCount = 0;
    var _fails = [];
    for(var i=0;i<_list.length;i++){
      try{
        var _r = await window._fbAdminRestoreDeletedHeroesForUid(_list[i]);
        if(_r && _r.ok){ _okCount++; _heroCount += (_r.restored || 0); }
      }catch(e){ _failCount++; _fails.push({ uid:_list[i], err:((e && e.message) || String(e)) }); console.warn('[_fbAdminRestoreAllDeletedHeroes] 復原失敗 uid=' + _list[i], e); }
    }
    console.log('[_fbAdminRestoreAllDeletedHeroes] 完成:成功 ' + _okCount + ' 名 / 救回 ' + _heroCount + ' 隻 / 失敗 ' + _failCount + ' 名');
    return { ok:true, okCount:_okCount, heroCount:_heroCount, failCount:_failCount, fails:_fails };
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.16.31(2026-06-26·救火)— 「自我審查『不是我的』誤移除(disown)」批次救回
  //   背景:_fbStudentDisownHeroes 移除英雄時會把雲端 heroLevels 一併清掉(原等級暫存於 _auditRecoveredLevels),
  //   所以舊的「等級>1 / 裝至寶」掃描(_fbAdminScanDeletedHeroes)抓不到這批。本工具改用「帳本反推」:
  //   某英雄『最近一筆解鎖紀錄 = audit_error_recovered』且『現在不在 unlockedHeroes』→ 判為被 disown 待救回。
  //   ・已被 GM 救回者最近一筆會是 admin_grant → 自動跳過(idempotent,重跑安全)
  //   ・GM 手動刪除(admin_delete)→ 不在此列(那是老師刻意刪除,尊重)
  //   救回走既有 _fbAdminRestoreLostHeroes:還原暫存原等級 + 寫 admin_grant 合法紀錄(之後不再被隱藏) + GM 直寫繞守門、只增不減。
  // ════════════════════════════════════════════════════════════════════
  window._fbAdminScanDisownedHeroes = async function(){
    var snap = await getDocs(collection(_fbDb, 'players'));
    var _affected = [];
    var _scanned = 0, _totalHeroes = 0;
    var _isPHN = function(n){ try{ if(typeof _PLAYER_HERO_NAMES==='undefined'||!_PLAYER_HERO_NAMES) return true; return _PLAYER_HERO_NAMES.has(n); }catch(_){ return true; } };
    snap.forEach(function(docSnap){
      _scanned++;
      try{
        var d = docSnap.data() || {};
        var _unl = new Set(Array.isArray(d.unlockedHeroes) ? d.unlockedHeroes : []);
        var _hist = Array.isArray(d._heroUnlockHistory) ? d._heroUnlockHistory : [];
        if(!_hist.length) return;
        // 每隻英雄最近一筆解鎖紀錄
        var _latest = {};
        _hist.forEach(function(e){
          if(!e || !e.name) return;
          if(!_latest[e.name] || (e.at||0) >= _latest[e.name].at){
            _latest[e.name] = { at:(e.at||0), source:e.source||'', disownedByStudent:!!e.disownedByStudent };
          }
        });
        var _stash = (d._auditRecoveredLevels && typeof d._auditRecoveredLevels === 'object') ? d._auditRecoveredLevels : {};
        var _missing = [];
        Object.keys(_latest).forEach(function(n){
          if(_unl.has(n)) return;                                   // 現在還在擁有 → 沒被移除
          if(!_isPHN(n)) return;                                     // 非玩家英雄白名單(濾 BOSS/小怪/舊名)
          if(_latest[n].source !== 'audit_error_recovered') return;  // 最近一筆非審查移除 → 跳過(admin_grant=已救回 / admin_delete=GM刻意刪)
          var _lv = (typeof _stash[n] === 'number' && _stash[n] >= 1) ? _stash[n] : 1;
          _missing.push({ name:n, lv:_lv, byStudent:_latest[n].disownedByStudent });
        });
        if(_missing.length){
          _totalHeroes += _missing.length;
          _affected.push({
            uid: docSnap.id,
            email: (d.email || '').toLowerCase(),
            displayName: (d.displayName || d.displayLabel || ''),
            heroes: _missing.map(function(h){ return h.name; }),
            detail: _missing,
            count: _missing.length
          });
        }
      }catch(_){}
    });
    _affected.sort(function(a,b){ return b.count - a.count; });
    console.log('[_fbAdminScanDisownedHeroes] 掃描 ' + _scanned + ' 名玩家,' + _affected.length + ' 名有被審查移除英雄,共 ' + _totalHeroes + ' 隻待救回');
    return { scanned:_scanned, affectedCount:_affected.length, totalHeroes:_totalHeroes, affected:_affected };
  };

  // 一鍵把「被審查移除」的英雄全部補回(可傳 uid 陣列;不傳則先掃描全體再逐一救回)
  window._fbAdminRestoreAllDisownedHeroes = async function(uids){
    if(typeof window._fbAdminRestoreLostHeroes !== 'function') throw new Error('_fbAdminRestoreLostHeroes 未載入');
    var _scan = await window._fbAdminScanDisownedHeroes();   // 一律重掃取最新名單(原等級/帳本可能已變)
    var _list = _scan.affected;
    if(Array.isArray(uids)){
      var _want = new Set(uids);
      _list = _list.filter(function(a){ return _want.has(a.uid); });
    }
    var _okCount = 0, _heroCount = 0, _failCount = 0;
    var _fails = [];
    for(var i=0;i<_list.length;i++){
      try{
        var _r = await window._fbAdminRestoreLostHeroes(_list[i].uid, _list[i].heroes);
        if(_r && _r.ok){ _okCount++; _heroCount += ((_r.restored && _r.restored.length) || 0); }
        else { _failCount++; _fails.push({ uid:_list[i].uid, err:((_r && _r.reason) || 'unknown') }); }
      }catch(e){
        _failCount++; _fails.push({ uid:_list[i].uid, err:((e && e.message) || String(e)) });
        console.warn('[_fbAdminRestoreAllDisownedHeroes] 救回失敗 uid=' + _list[i].uid, e);
      }
    }
    console.log('[_fbAdminRestoreAllDisownedHeroes] 完成:成功 ' + _okCount + ' 名 / 救回 ' + _heroCount + ' 隻 / 失敗 ' + _failCount + ' 名');
    return { ok:true, okCount:_okCount, heroCount:_heroCount, failCount:_failCount, fails:_fails };
  };

  // GM 撤銷預告(套用前)
  window._fbAdminCancelPollutionPurgeNotice = async function(uid){
    if(!uid) throw new Error('uid 為必要');
    const _adminEmail = (_fbAuth.currentUser && _fbAuth.currentUser.email) || 'unknown';
    const _ref = doc(_fbDb, 'players', uid);
    const _snap = await getDoc(_ref);
    if(!_snap.exists()) throw new Error('找不到玩家');
    const _pp = (_snap.data() || {})._pendingPollutionPurge;
    if(!_pp) throw new Error('沒有預告可撤銷');
    if(_pp.status === 'applied') throw new Error('預告已套用, 無法撤銷(如需復原請用「復原」)');
    await updateDoc(_ref, {
      '_pendingPollutionPurge.status':'cancelled',
      '_pendingPollutionPurge.cancelledAt':Date.now(),
      '_pendingPollutionPurge.cancelledBy':_adminEmail,
    });
    console.log('[_fbAdminCancelPollutionPurgeNotice] ✅ uid='+uid+' 已撤銷預告');
    return { ok:true, uid };
  };

  // ── 玩家端:汙染英雄刪除預告確認視窗(登入後彈出)──
  window._showPollutionPurgeNotice = function(purge){
    try{
      if(!purge) return;
      const _heroes = Array.isArray(purge.heroes) ? purge.heroes : [];
      const _treasures = Array.isArray(purge.treasures) ? purge.treasures : [];
      if(!_heroes.length && !_treasures.length) return;
      if(document.getElementById('_pp-notice-modal')) return;
      const _esc = (s)=>String(s==null?'':s).replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));
      const _itemLabel = { skill_upgrade_book:'技能升級書', burst_upgrade_fruit:'超越極限果實', hero_exp_book_premium:'豪華典藏版經驗之書', hero_exp_book:'英雄經驗之書', summon_ticket_ssr:'SSR召喚卷', treasure_exp_scroll:'至寶經驗卷軸' };
      const _compStr = (comp)=>{ comp=comp||{coins:0,items:{}}; const _i=Object.keys(comp.items||{}).map(k=>(_itemLabel[k]||k)+'×'+comp.items[k]).join('、'); return '💰'+(comp.coins||0)+(_i?(' + '+_i):''); };
      // 英雄區
      const _heroRows = _heroes.map((h)=>
        '<tr style="border-bottom:1px solid rgba(255,255,255,0.08);">'
        + '<td style="padding:8px 6px;text-align:center;"><input type="radio" name="_pp-keep" value="'+_esc(h.name)+'" class="_pp-keep-radio" style="width:18px;height:18px;cursor:pointer;"></td>'
        + '<td style="padding:8px 6px;font-size:13px;color:#ffe;font-weight:700;">'+_esc(h.name)+'<span style="color:#ffcc66;font-size:11px;margin-left:6px;">'+_esc(h.rarity||'')+'</span></td>'
        + '<td style="padding:8px 6px;font-size:11px;color:#aac;">Lv.'+(h.lv||1)+'</td>'
        + '<td style="padding:8px 6px;font-size:11px;color:#88dd99;">'+_esc(_compStr(h.comp))+'</td></tr>').join('');
      const _heroSection = _heroes.length ? (
        '<div style="font-size:12px;color:#ffcc88;margin:10px 0 6px;">🦸 來源不明的英雄(可<b style="color:#88ff88;">保留 1 隻</b>, 保留的不補償):</div>'
        + '<table style="width:100%;border-collapse:collapse;margin-bottom:6px;"><thead><tr style="background:rgba(200,120,60,0.18);">'
        + '<th style="padding:6px;font-size:11px;color:#ffd;">保留</th><th style="padding:6px;font-size:11px;color:#ffd;text-align:left;">英雄</th><th style="padding:6px;font-size:11px;color:#ffd;text-align:left;">等級</th><th style="padding:6px;font-size:11px;color:#ffd;text-align:left;">移除後補償</th>'
        + '</tr></thead><tbody>'+_heroRows+'</tbody></table>'
        + '<label style="display:flex;align-items:center;gap:8px;font-size:12px;color:#bbc;cursor:pointer;margin-bottom:12px;"><input type="radio" name="_pp-keep" value="" class="_pp-keep-radio" checked style="width:16px;height:16px;cursor:pointer;"> 英雄都不保留(拿全部補償)</label>'
      ) : '';
      // 至寶區
      const _treRows = _treasures.map((t)=>
        '<tr style="border-bottom:1px solid rgba(255,255,255,0.08);">'
        + '<td style="padding:8px 6px;text-align:center;"><input type="radio" name="_pp-keep-tre" value="'+_esc(t.id)+'" class="_pp-keep-tre-radio" style="width:18px;height:18px;cursor:pointer;"></td>'
        + '<td style="padding:8px 6px;font-size:13px;color:#ffe;font-weight:700;">'+_esc(t.name||t.id)+'</td>'
        + '<td style="padding:8px 6px;font-size:11px;color:#aac;">Lv.'+(t.lv||1)+'</td>'
        + '<td style="padding:8px 6px;font-size:11px;color:#88dd99;">'+_esc(_compStr(t.comp))+'</td></tr>').join('');
      const _treSection = _treasures.length ? (
        '<div style="font-size:12px;color:#ffcc88;margin:10px 0 6px;">💎 來源不明的至寶(可<b style="color:#88ff88;">保留 1 件</b>, 保留的不補償):</div>'
        + '<table style="width:100%;border-collapse:collapse;margin-bottom:6px;"><thead><tr style="background:rgba(120,160,255,0.18);">'
        + '<th style="padding:6px;font-size:11px;color:#ffd;">保留</th><th style="padding:6px;font-size:11px;color:#ffd;text-align:left;">至寶</th><th style="padding:6px;font-size:11px;color:#ffd;text-align:left;">等級</th><th style="padding:6px;font-size:11px;color:#ffd;text-align:left;">移除後補償</th>'
        + '</tr></thead><tbody>'+_treRows+'</tbody></table>'
        + '<label style="display:flex;align-items:center;gap:8px;font-size:12px;color:#bbc;cursor:pointer;margin-bottom:12px;"><input type="radio" name="_pp-keep-tre" value="" class="_pp-keep-tre-radio" checked style="width:16px;height:16px;cursor:pointer;"> 至寶都不保留(拿全部補償)</label>'
      ) : '';
      // 開場白
      const _cntParts = [];
      if(_heroes.length) _cntParts.push('<b style="color:#ff9966;">'+_heroes.length+' 隻英雄</b>');
      if(_treasures.length) _cntParts.push('<b style="color:#9cf;">'+_treasures.length+' 件至寶</b>');
      const _intro = '系統發現你的帳號裡有 '+_cntParts.join(' 和 ')+' 來源不明(很可能是在共用裝置上,從別人的帳號被帶進來的)。為了讓每個人的帳號乾淨公平,管理員將整理移除它們。';
      const _ov=document.createElement('div');
      _ov.id='_pp-notice-modal';
      _ov.style.cssText='position:fixed;inset:0;z-index:200050;background:rgba(0,0,0,0.88);display:flex;align-items:center;justify-content:center;padding:18px;backdrop-filter:blur(6px);font-family:"M PLUS Rounded 1c","Nunito",sans-serif;overflow-y:auto;';
      _ov.innerHTML =
        '<div style="max-width:560px;width:100%;background:linear-gradient(160deg,#1e1828,#120c18);border:2.5px solid #cc7744;border-radius:16px;padding:22px 24px;color:#fff;box-shadow:0 0 40px rgba(200,120,60,0.5);max-height:92vh;overflow-y:auto;">'
        + '<div style="font-size:21px;font-weight:900;color:#ffaa66;margin-bottom:6px;">🧹 帳號資料整理通知</div>'
        + '<div style="font-size:13px;color:#ffd9bb;line-height:1.7;margin-bottom:12px;">'+_intro+'</div>'
        + '<div style="background:rgba(0,0,0,0.4);border-radius:8px;padding:10px 12px;margin-bottom:6px;font-size:12px;color:#ccd;line-height:1.6;">'
        +   '<div><b style="color:#aaccff;">緣由:</b>'+_esc(purge.reason||'清除來源不明的英雄/至寶')+'</div>'
        +   (purge.evidence?('<div style="margin-top:4px;"><b style="color:#aaccff;">說明:</b>'+_esc(purge.evidence)+'</div>'):'')
        + '</div>'
        + '<div style="font-size:12px;color:#ffcc88;margin-bottom:2px;">💝 移除後你會自動收到補償(賠回你花在它們身上的投入)。</div>'
        + _heroSection
        + _treSection
        + '<button id="_pp-notice-confirm" style="width:100%;padding:13px;font-size:15px;font-weight:800;background:linear-gradient(135deg,#cc7744,#aa5522);color:#fff;border:none;border-radius:10px;cursor:pointer;box-shadow:0 3px 12px rgba(200,120,60,0.5);margin-top:4px;">我了解了,確認</button>'
        + '<div style="font-size:11px;color:#889;text-align:center;margin-top:8px;">(確認後管理員會完成整理;沒勾保留的會全部移除並補償)</div>'
        + '</div>';
      document.body.appendChild(_ov);
      const _btn=document.getElementById('_pp-notice-confirm');
      if(_btn) _btn.onclick=async function(){
        _btn.disabled=true; _btn.textContent='處理中...';
        let _kept='', _keptTre='';
        try{ const _c=_ov.querySelector('._pp-keep-radio:checked'); _kept=_c?_c.value:''; }catch(_){}
        try{ const _ct=_ov.querySelector('._pp-keep-tre-radio:checked'); _keptTre=_ct?_ct.value:''; }catch(_){}
        try{ if(typeof window._fbPlayerRespondPollutionPurge==='function'){ await window._fbPlayerRespondPollutionPurge(_kept||null, _keptTre||null); } }
        catch(e){ console.warn('[汙染預告確認] 回寫失敗', e); }
        try{
          if(typeof window._showSimpleToast==='function'){
            const _msgs=[]; if(_kept) _msgs.push('英雄「'+_kept+'」'); if(_keptTre) _msgs.push('至寶「'+_keptTre+'」');
            window._showSimpleToast(_msgs.length?('✅ 已收到,將保留'+_msgs.join('、')):'✅ 已收到你的確認','ok');
          }
        }catch(_){}
        try{ _ov.remove(); }catch(_){}
      };
    }catch(e){ console.warn('[_showPollutionPurgeNotice] 例外', e); }
  };

  window._fbCompensatePlayer = async function(uid, compensation) {
    if(!uid || !compensation) throw new Error('uid 與 compensation 為必要');
    // ★ v3.15.41(2026-06-18)— 乙:_dataSummary 口徑統一。
    //   舊版補償算 summary 用 raw `.length` / 整本 heroLevels 加總(未過濾白名單),
    //   而 client 端 _buildSafeData / _getLocalSummary 都過濾 _PLAYER_HERO_NAMES。
    //   → 帳號 unlockedHeroes/heroLevels 含非白名單殘留(BOSS/小怪/舊名)時,雲端 summary 偏高、
    //     client 算偏低 → 健康度守門誤判「解鎖英雄倒退 N 隻」,把正常存檔擋死(5407 案例)。
    //   修:補償三槽 summary 也用同一支白名單過濾,雲端與 client 口徑一致。
    const _isPlayerHeroName = function(name){
      try{ if(typeof _PLAYER_HERO_NAMES === 'undefined' || !_PLAYER_HERO_NAMES) return true; return _PLAYER_HERO_NAMES.has(name); }catch(_){ return true; }
    };
    // 1) 先讀現有資料(從 server,避免本地快取)
    let existing = {};
    try {
      const snap = await getDocFromServer(doc(_fbDb, 'players', uid));
      if(snap.exists()) existing = snap.data() || {};
    } catch(e) {
      // 讀取失敗就用空物件,後續 merge:true 仍會保留雲端未提及欄位
      console.warn('[_fbCompensatePlayer] 讀取現有資料失敗,將以空白起點 merge', e);
    }

    // 2) 合併 unlockedHeroes(union,不重複)
    const _existUnlocked = Array.isArray(existing.unlockedHeroes) ? existing.unlockedHeroes : [];
    const _newUnlocked = Array.isArray(compensation.unlockedHeroes) ? compensation.unlockedHeroes : [];
    const _mergedUnlocked = Array.from(new Set([..._existUnlocked, ..._newUnlocked]));

    // 3) 合併 heroLevels(逐欄取較大值,讓玩家既有的高等級不會被低等級補償蓋掉)
    const _existLevels = (existing.heroLevels && typeof existing.heroLevels === 'object') ? existing.heroLevels : {};
    const _newLevels = (compensation.heroLevels && typeof compensation.heroLevels === 'object') ? compensation.heroLevels : {};
    // ★ v3.15.41 — 甲:補償授予的每隻英雄都保證有 heroLevels ≥Lv1。
    //   舊版只把英雄塞進 unlockedHeroes,若 compensation.heroLevels 沒帶該隻 → heroLevels 缺項。
    //   client 解鎖數三源取最大會用 unlockedHeroes 蓋過,但 heroLevels 缺項仍會讓 totalLv 對不上、
    //   且 v3.15.40 載入 reconcile(丙)前的時序仍可能偏低。這裡對 _newLevels 補 Lv1 floor
    //   (max-merge 不會降級既有高等級),會一併流入主檔/live/safe 三槽。
    try{
      _newUnlocked.forEach(function(_n){
        if(_n && !(_n in _newLevels)) _newLevels[_n] = 1;
      });
    }catch(_eFloorLv){}
    const _mergedLevels = Object.assign({}, _existLevels);
    Object.keys(_newLevels).forEach(name => {
      const _newLv = Math.max(1, Math.min(50, _newLevels[name] || 1));
      const _curLv = _mergedLevels[name] || 0;
      // 「保留現有資料再覇上去」原則:若玩家現有等級更高就保留,否則用新等級
      _mergedLevels[name] = Math.max(_curLv, _newLv);
    });

    // 4) 合併 heroStatPoints(對每位角色:新值至少要 = level - 1 - invested)
    const _existSP = (existing.heroStatPoints && typeof existing.heroStatPoints === 'object') ? existing.heroStatPoints : {};
    const _existInv = (existing.heroStatInvested && typeof existing.heroStatInvested === 'object') ? existing.heroStatInvested : {};
    const _mergedSP = Object.assign({}, _existSP);
    Object.keys(_mergedLevels).forEach(name => {
      const lv = _mergedLevels[name] || 1;
      if(lv <= 1) return;
      const expected = lv - 1;
      const inv = _existInv[name] || {};
      const invTotal = (inv.hp||0) + (inv.atk||0) + (inv.sp||0) + (inv.spd||0);
      const curFree = _mergedSP[name] || 0;
      const shouldHave = Math.max(0, expected - invTotal);
      if(shouldHave > curFree){
        _mergedSP[name] = shouldHave;
      }
    });

    // 5) 知識幣:依 compensation.coinsMode 決定 add 或 set
    let _newCoins = existing.knowledgeCoins || 0;
    if(typeof compensation.coins === 'number'){
      if(compensation.coinsMode === 'add'){
        _newCoins = Math.max(0, _newCoins + compensation.coins);
      } else {
        // set:取較大值,符合「保留現有再覇上去」原則
        _newCoins = Math.max(_newCoins, Math.max(0, compensation.coins));
      }
    }

    // 6) 背包物品:加總式合併(各物品 += 補償數量,上限 99)
    const _existBp = (existing.playerBackpack && typeof existing.playerBackpack === 'object') ? existing.playerBackpack : {};
    const _newBp = Object.assign({}, _existBp);
    if(compensation.backpack && typeof compensation.backpack === 'object'){
      Object.keys(compensation.backpack).forEach(itemId => {
        const add = Math.max(0, compensation.backpack[itemId] || 0);
        if(add === 0) return;
        _newBp[itemId] = Math.min(99, (_newBp[itemId] || 0) + add);
      });
    }

    // ★ v1.0.20260513.6450 — 計算「實際生效」diff(讓老師事後可逐筆稽核)
    //   原則:只記錄「規則保護後真的有變」的項目,被 max 規則擋住的不會進來
    const _existUnlockedSet = new Set(_existUnlocked);
    const _diff = {
      // 真的新解鎖的角色(原本沒有的才會進來)
      unlockedHeroesAdded: _newUnlocked.filter(n => !_existUnlockedSet.has(n)),
      // 實際升級的角色 {name: {from, to}}(原本等級已 >= 補償等級者不會進來)
      heroLevelsRaised: {},
      // 知識幣變化前後值
      coinsBefore: existing.knowledgeCoins || 0,
      coinsAfter: _newCoins,
      coinsDelta: _newCoins - (existing.knowledgeCoins || 0),
      // 背包物品實際加上的數量 {itemId: addedQty}(命中 99 上限的會以實際加上的為準)
      backpackAdded: {}
    };
    // 算出每個英雄真的升了幾級
    Object.keys(_newLevels).forEach(name => {
      const _was = _existLevels[name] || 0;
      const _now2 = _mergedLevels[name] || 0;
      if(_now2 > _was){
        _diff.heroLevelsRaised[name] = { from: _was, to: _now2 };
      }
    });
    // 算出背包物品真的加上幾個(被 99 上限擋掉的會用實際差值)
    if(compensation.backpack && typeof compensation.backpack === 'object'){
      Object.keys(compensation.backpack).forEach(itemId => {
        const _was = _existBp[itemId] || 0;
        const _now3 = _newBp[itemId] || 0;
        const _delta = _now3 - _was;
        if(_delta > 0) _diff.backpackAdded[itemId] = _delta;
      });
    }

    // 7) 寫入補償歷史
    const _now = Date.now();
    const _existHistory = Array.isArray(existing._compensationHistory) ? existing._compensationHistory : [];
    const _existCount = existing._compensationCount || 0;
    // ★ v3.15.41 — 甲:為「真的新解鎖」的英雄寫 _heroUnlockHistory,
    //   讓稽核感知/跨槽合併認得補償英雄是本人合法擁有(否則「查無解鎖紀錄」會被當跨帳號殘留指紋丟掉)。
    // ★ v3.17.3 根治(從源頭斷掉污染↔救援拉鋸):source 由 'compensation' 改 'admin_grant'
    //   (+adminAction+uid·對齊下方 L≈10629 至寶補償早就用的 admin_grant)→「老師正式發放的英雄」
    //   永久合法、永不被任何「補償批次回收/查無紀錄回收」工具當 compensation 誤收。
    // ★★ v5.158.0(老師需求「補償英雄後又消失/刪除英雄後又出現徹底根治」)——
    //   根因:下面這段只把新的 admin_grant 紀錄寫進「主文件的 _heroUnlockHistory」,live/safe 兩槽
    //   的 payload(下方 _livePayload/_safePayload)完全沒有 _heroUnlockHistory 欄位 ⇒ 該英雄只有主文件
    //   查得到「這是老師發的、合法」的證據,live/safe 兩槽對這隻英雄永遠是「查無紀錄」。共用 iPad 環境
    //   下三槽 savedAt 常態不同步,一旦 live 或 safe 之後變成「較新/較豐富」而被選為權威槽(或被
    //   _fbRebuildAccountFromLedgers 等審核工具讀到這兩槽的合併結果),這隻剛補償的英雄在證據上就跟
    //   「查無來源的幻影英雄」一模一樣 ⇒ 被同一套「查無紀錄回收/過度補回稽查」工具當可疑對象處理掉,
    //   老師會看到「明明補了,過一陣子又不見了」。至寶同理(_treHistComp 同一病灶)。
    //   ★ 修法:把「這次要新增的 admin_grant 紀錄」抽成獨立陣列(不含 existing 舊資料),
    //   main/live/safe 三槽各自在「自己既有的 history」基礎上疊加同一批新紀錄(不是三槽共用同一份完
    //   整陣列——那樣會讓某槽獨有的舊紀錄被蓋掉,違反「不可用同一份 patch 寫三槽」鐵律),
    //   確保三槽對「這次補償」的證據永遠一致,不會有任何一槽獨漏。
    const _grantHeroEntries = [];
    try{
      const _grantedNames = (_diff && Array.isArray(_diff.unlockedHeroesAdded)) ? _diff.unlockedHeroesAdded : [];
      _grantedNames.forEach(function(_gn){
        if(_gn) _grantHeroEntries.push({ name:_gn, at:_now, source:'admin_grant', adminAction:true, uid:String(uid).slice(0,12), by: compensation.by || 'admin' });
      });
    }catch(_eHeroHist){}
    const _appendHeroHist = function(baseArr){
      let _h = Array.isArray(baseArr) ? baseArr.slice() : [];
      _grantHeroEntries.forEach(function(_e){ _h.push(_e); });
      if(_h.length > 300) _h = _h.slice(-300);
      return _h;
    };
    let _newHeroHist = _appendHeroHist(existing._heroUnlockHistory);
    const _entry = {
      at: _now,
      by: compensation.by || 'admin',
      reason: (compensation.reason || '').slice(0, 500),
      summary: (compensation.summary || '').slice(0, 1000),
      heroes: _newUnlocked.slice(0, 50),  // 只記補償的英雄名單,避免超大
      // ★ v1.0.20260513.6450 — 把 diff 一併寫入,讓事後翻歷史也能看到「真的補了什麼」
      diff: _diff
    };
    const _newHistory = [..._existHistory, _entry].slice(-30);  // 最多保留最近 30 筆

    // ★ v3.13.94 — B7 友情之心補償(+N,上限 99;保留現有再加上去)
    let _newFriendshipHeart = (typeof existing.friendshipHeart === 'number') ? existing.friendshipHeart : 0;
    if(typeof compensation.friendshipHeart === 'number' && compensation.friendshipHeart > 0){
      _newFriendshipHeart = Math.min(99, _newFriendshipHeart + Math.max(0, Math.floor(compensation.friendshipHeart)));
    }
    // ★ v3.13.94 — B8 台灣至寶補償(union 進 taiwanTreasureData,新至寶給 Lv1;已擁有不動,保留現有)
    const _existTwData = (existing.taiwanTreasureData && typeof existing.taiwanTreasureData === 'object') ? existing.taiwanTreasureData : {};
    const _mergedTwData = Object.assign({}, _existTwData);
    const _grantTreEntries = [];
    const _treasuresAdded = [];
    if(Array.isArray(compensation.treasures)){
      compensation.treasures.forEach(function(tid){
        if(!tid || _mergedTwData[tid]) return;   // 已擁有 → 不動
        _mergedTwData[tid] = { lv:1, exp:0, equippedTo:null };
        _treasuresAdded.push(tid);
        // ★ v3.17.7(老師需求甲)— 至寶授權紀錄「同英雄召喚規模」補齊 uid 綁定:
        //   對齊上方英雄 admin_grant(L≈10796)的 {source:'admin_grant', adminAction:true, uid, by}。
        //   根因:舊版此筆漏 uid → GM 發放的至寶在「過度補回/查無紀錄」稽核眼中「查無自己 uid 的解鎖紀錄」
        //   → 被誤判成別帳號殘留(over-restored)而可能被收回。補 uid 後=老師正式發放的至寶永久合法、
        //   永不被任何回收工具當殘留誤收(與 v3.17.3 英雄發放即合法對稱)。
        _grantTreEntries.push({ id: tid, at: _now, source: 'admin_grant', adminAction: true, uid: String(uid).slice(0, 12), by: compensation.by || 'admin' });
      });
    }
    const _appendTreHist = function(baseArr){
      let _t = Array.isArray(baseArr) ? baseArr.slice() : [];
      _grantTreEntries.forEach(function(_e){ _t.push(_e); });
      if(_t.length > 300) _t = _t.slice(-300);
      return _t;
    };
    let _treHistComp = _appendTreHist(existing._treasureUnlockHistory);

    // ★ v3.15.37 — B9 鬥技之證補償(持有量 zhengHeld + 累積 zhengLifetime,在現有基礎上 +N)
    //   注意:鬥技之證是玩家可在鬥技場商店消費的「持有量」,與「本週排名 zheng」(arenaWeekly)不同。
    //   寫入 players/{uid} 主檔 + live/safe 三槽的 arenaZhengHeld / arenaZhengLifetime;
    //   玩家登入時 _applySafeData 會以 max 合併帶進本機(共用 iPad 清快取也能還原)。
    const _compArenaZheng = Math.max(0, Math.floor(Number(compensation.arenaZheng) || 0));
    const _existArenaHeld = (typeof existing.arenaZhengHeld === 'number' && existing.arenaZhengHeld >= 0) ? Math.floor(existing.arenaZhengHeld) : 0;
    const _existArenaLife = (typeof existing.arenaZhengLifetime === 'number' && existing.arenaZhengLifetime >= 0) ? Math.floor(existing.arenaZhengLifetime) : 0;
    const _newArenaHeld = _existArenaHeld + _compArenaZheng;
    const _newArenaLife = Math.max(_existArenaLife + _compArenaZheng, _newArenaHeld);
    if(_compArenaZheng > 0) _diff.arenaZhengAdded = _compArenaZheng;

    // 8) 組 payload
    const payload = {
      savedAt: _now,
      unlockedHeroes: _mergedUnlocked,
      heroLevels: _mergedLevels,
      heroLevels_s: JSON.stringify(_mergedLevels),
      _authoritativeRestoreAt: Date.now(),   // ★ v3.16.5 — 蓋戳記:過時 session(補償前已載入)偵測到後鎖存+重載,不覆蓋本次補償(110082 補了又消失根治)
      // ★ v3.17.3 — 補償英雄解鎖紀錄(source:'admin_grant'·見上方 _newHeroHist),永久合法不被回收
      _heroUnlockHistory: _newHeroHist,
      _heroUnlockHistory_s: JSON.stringify(_newHeroHist),
      heroStatPoints_s: JSON.stringify(_mergedSP),
      knowledgeCoins: _newCoins,
      playerBackpack: _newBp,
      playerBackpack_s: JSON.stringify(_newBp),
      friendshipHeart: _newFriendshipHeart,
      // ★ v3.15.37 — 鬥技之證持有量/累積(三槽同寫,確保 gameCloudLoad 選到哪槽都帶到補償)
      arenaZhengHeld: _newArenaHeld,
      arenaZhengLifetime: _newArenaLife,
      taiwanTreasureData: _mergedTwData,
      taiwanTreasureData_s: JSON.stringify(_mergedTwData),
      _treasureUnlockHistory: _treHistComp,
      _treasureUnlockHistory_s: JSON.stringify(_treHistComp),
      _compensationCount: _existCount + 1,
      _compensationHistory: _newHistory,
      _compensationHistory_s: JSON.stringify(_newHistory),
      _lastCompensatedAt: _now,
      _lastCompensatedBy: compensation.by || 'admin'
    };

    // ────────────────────────────────────────────────────────────────
    // ★ v3.9.0(2026-05-26) — 三槽同寫修補
    //
    // ROOT CAUSE(老師 2026-05-26 回報):
    //   舊版只寫主文件 players/{uid},但學生登入時 gameCloudLoad 會並行讀
    //   live + safe + 主文件,並用 _summaryRichnessScore 選分數最高那份。
    //   學生既有的 live/safe 分數通常遠高於管理員剛剛寫的主文件
    //   → 補償的英雄永遠不會被讀到 → 老師看到「✅ 補償成功」,學生卻沒解鎖
    //
    // 修補:
    //   1) 算一份 _dataSummary 放進 payload(讓 _summaryRichnessScore 能比對)
    //   2) 主文件:照舊用 setDoc + merge
    //   3) live 槽:讀現有 live → 對 unlocked / heroLevels 做相同的 max-merge →
    //              merge 完成的版本寫回 live(直接 setDoc,_adminForceRestore 旗標
    //              繞過健康度守門)
    //   4) safe 槽:用 _fbWriteSafeIfHealthier API,只在補完版本更健康時才寫入
    //
    // 政策(老師確認):
    //   - 玩家既有的高等級絕不會被低補償蓋掉(max-merge)
    //   - unlockedHeroes 仍是 union(不重複)
    // ────────────────────────────────────────────────────────────────

    // 計算 _dataSummary(用補完後的 _mergedXxx,代表「補償後玩家應該有的水準」)
    const _bagItemsCount = Object.keys(_newBp).reduce(function(s, k){
      return s + (_newBp[k] || 0);
    }, 0);
    let _maxHeroLv = 0;
    let _totalHeroLv = 0;
    Object.keys(_mergedLevels).forEach(function(n){
      if(!_isPlayerHeroName(n)) return;   // ★ v3.15.41 乙:只算白名單英雄(對齊 client)
      const _lv = _mergedLevels[n] || 0;
      if(_lv > _maxHeroLv) _maxHeroLv = _lv;
      _totalHeroLv += _lv;
    });
    payload._dataSummary = {
      unlockedCount: _mergedUnlocked.filter(_isPlayerHeroName).length,
      totalHeroLv:   _totalHeroLv,
      maxHeroLv:     _maxHeroLv,
      coins:         _newCoins,
      bagItems:      _bagItemsCount,
      ts:            _now,
      _source:       '_fbCompensatePlayer_v3.9.0'
    };
    // 旗標讓任何後續健康度守門/驗證邏輯知道這是管理員授權的寫入
    payload._adminForceRestore = true;
    payload._lastAdminCompensateAt = _now;

    // (1) 主文件 — 照舊
    await setDoc(doc(_fbDb, 'players', uid), payload, { merge: true });
    console.info('✅ [補償 v3.9.0] (1/3) 主文件寫入完成 ' + uid);

    // (2) live 槽 — 讀現有 live、max-merge、寫回
    //   live 槽是學生登入時 gameCloudLoad 並行讀的第一個目標,寫到這裡學生才看得到
    let _liveWriteOk = false;
    try {
      const _liveRef = doc(_fbDb, 'players', uid, 'saves', 'live');
      let _existingLive = {};
      try {
        const _liveSnap = await getDocFromServer(_liveRef);
        if(_liveSnap.exists()) _existingLive = _liveSnap.data() || {};
      } catch(_eRead){
        // 讀失敗就用空白起點,後面 merge 仍然會把補償欄位寫進去
        console.warn('[補償 v3.9.0] live 槽讀取失敗,將以空白起點寫入', _eRead);
      }
      // 對 unlockedHeroes 做 union(以 live 既有版本為基底,再 union 補償清單)
      const _liveExistUnlocked = Array.isArray(_existingLive.unlockedHeroes) ? _existingLive.unlockedHeroes : [];
      const _liveMergedUnlocked = Array.from(new Set([..._liveExistUnlocked, ..._newUnlocked]));
      // 對 heroLevels 做 max-merge(以 live 既有版本為基底)
      const _liveExistLevels = (_existingLive.heroLevels && typeof _existingLive.heroLevels === 'object') ? _existingLive.heroLevels : {};
      const _liveMergedLevels = Object.assign({}, _liveExistLevels);
      Object.keys(_newLevels).forEach(function(name){
        const _newLv = Math.max(1, Math.min(50, _newLevels[name] || 1));
        const _curLv = _liveMergedLevels[name] || 0;
        _liveMergedLevels[name] = Math.max(_curLv, _newLv);
      });
      // 知識幣(同主文件邏輯:add 模式相加,set 模式取較大值)
      let _liveNewCoins = _existingLive.knowledgeCoins || 0;
      if(typeof compensation.coins === 'number'){
        if(compensation.coinsMode === 'add'){
          _liveNewCoins = Math.max(0, _liveNewCoins + compensation.coins);
        } else {
          _liveNewCoins = Math.max(_liveNewCoins, Math.max(0, compensation.coins));
        }
      }
      // 背包(以 live 既有版本為基底,加總,上限 99)
      const _liveExistBp = (_existingLive.playerBackpack && typeof _existingLive.playerBackpack === 'object') ? _existingLive.playerBackpack : {};
      const _liveNewBp = Object.assign({}, _liveExistBp);
      if(compensation.backpack && typeof compensation.backpack === 'object'){
        Object.keys(compensation.backpack).forEach(function(itemId){
          const add = Math.max(0, compensation.backpack[itemId] || 0);
          if(add === 0) return;
          _liveNewBp[itemId] = Math.min(99, (_liveNewBp[itemId] || 0) + add);
        });
      }
      // 算 live 版本的 _dataSummary(用 live merge 後的數字,以對玩家既有水準為基準)
      const _liveBagItems = Object.keys(_liveNewBp).reduce(function(s, k){ return s + (_liveNewBp[k] || 0); }, 0);
      let _liveMaxLv = 0, _liveTotalLv = 0;
      Object.keys(_liveMergedLevels).forEach(function(n){
        if(!_isPlayerHeroName(n)) return;   // ★ v3.15.41 乙:只算白名單英雄
        const _lv = _liveMergedLevels[n] || 0;
        if(_lv > _liveMaxLv) _liveMaxLv = _lv;
        _liveTotalLv += _lv;
      });
      // 組 live payload(用 spread 保留 live 既有的所有欄位,只覆蓋有變動的)
      const _livePayload = Object.assign({}, _existingLive, {
        savedAt:            _now,
        unlockedHeroes:     _liveMergedUnlocked,
        heroLevels:         _liveMergedLevels,
        heroLevels_s:       JSON.stringify(_liveMergedLevels),
        knowledgeCoins:     _liveNewCoins,
        playerBackpack:     _liveNewBp,
        playerBackpack_s:   JSON.stringify(_liveNewBp),
        // ★ v3.15.37 — live 槽鬥技之證(以 live 既有值 +N,並確保 lifetime >= held)
        arenaZhengHeld: (function(){ var b=(typeof _existingLive.arenaZhengHeld==='number'&&_existingLive.arenaZhengHeld>=0)?Math.floor(_existingLive.arenaZhengHeld):0; return b + _compArenaZheng; })(),
        arenaZhengLifetime: (function(){ var bl=(typeof _existingLive.arenaZhengLifetime==='number'&&_existingLive.arenaZhengLifetime>=0)?Math.floor(_existingLive.arenaZhengLifetime):0; var bh=(typeof _existingLive.arenaZhengHeld==='number'&&_existingLive.arenaZhengHeld>=0)?Math.floor(_existingLive.arenaZhengHeld):0; return Math.max(bl + _compArenaZheng, bh + _compArenaZheng); })(),
        // ★ v3.13.94 — B7/B8 友情之心 + 台灣至寶(對 live 既有 union,保留現有不縮減)
        friendshipHeart: (function(){ let h=(typeof _existingLive.friendshipHeart==='number')?_existingLive.friendshipHeart:0; if(typeof compensation.friendshipHeart==='number'&&compensation.friendshipHeart>0) h=Math.min(99,h+Math.max(0,Math.floor(compensation.friendshipHeart))); return h; })(),
        taiwanTreasureData: (function(){ const base=Object.assign({},(_existingLive.taiwanTreasureData&&typeof _existingLive.taiwanTreasureData==='object')?_existingLive.taiwanTreasureData:{}); if(Array.isArray(compensation.treasures)) compensation.treasures.forEach(function(tid){ if(tid&&!base[tid]) base[tid]={lv:1,exp:0,equippedTo:null}; }); return base; })(),
        // ★ v5.158.0 — live 槽自己疊上同一批 admin_grant 紀錄(在 live 既有的 history 基礎上,不是整批覆蓋)
        //   否則這隻剛補償的英雄在 live 槽查無紀錄,日後被任何「查無紀錄回收」工具當幻影誤收。
        _heroUnlockHistory: _appendHeroHist(_existingLive._heroUnlockHistory),
        _heroUnlockHistory_s: JSON.stringify(_appendHeroHist(_existingLive._heroUnlockHistory)),
        _treasureUnlockHistory: _appendTreHist(_existingLive._treasureUnlockHistory),
        _treasureUnlockHistory_s: JSON.stringify(_appendTreHist(_existingLive._treasureUnlockHistory)),
        _dataSummary: {
          unlockedCount: _liveMergedUnlocked.filter(_isPlayerHeroName).length,
          totalHeroLv:   _liveTotalLv,
          maxHeroLv:     _liveMaxLv,
          coins:         _liveNewCoins,
          bagItems:      _liveBagItems,
          ts:            _now,
          _source:       '_fbCompensatePlayer_v3.9.0_live'
        },
        _adminForceRestore: true,
        _lastAdminCompensateAt: _now,
        _compensationCount:    _existCount + 1,
        _compensationHistory:  _newHistory,
        _compensationHistory_s: JSON.stringify(_newHistory)
      });
      // 直接 setDoc(不走 _fbSaveLive 的健康度守門,因為這是管理員授權的補償操作)
      await setDoc(_liveRef, _livePayload);
      _liveWriteOk = true;
      console.info('✅ [補償 v3.9.0] (2/3) live 槽寫入完成 ' + uid
        + ' — unlocked=' + _liveMergedUnlocked.length
        + ', maxLv=' + _liveMaxLv);
      // ★ 把 live merged 結果存到 payload 給 safe 槽用
      payload._liveMerged = _livePayload;
    } catch(_eLive){
      console.error('🚨 [補償 v3.9.0] (2/3) live 槽寫入失敗 — 學生可能仍然看不到補償!', _eLive);
    }

    // (3) safe 槽 — ★ v3.9.2(2026-05-26)補償消失 BUG 修補:強制寫入,不再走健康度守門
    //   ROOT CAUSE:舊版用 _fbWriteSafeIfHealthier,但補償後 coins/bagItems 比 safe 低
    //   (玩家正常消費過後),會被拒絕寫入。下次學生選槽選到 safe,補償消失。
    //   ────────
    //   修補:把 safe 既有資料讀出來,跟補償結果做 max-merge(英雄/等級/coins/背包),
    //   產出「safe 應有版本」,直接 setDoc 覆蓋。
    //   邏輯:
    //   - unlockedHeroes:safe 既有 union 補償清單
    //   - heroLevels:safe 既有 vs 補償,取較大值(保留玩家既有高等級)
    //   - coins:safe 既有 + 補償(如果是 add 模式)/ max(如果是 set 模式)
    //   - 背包:safe 既有 + 補償,上限 99
    //   - 其他欄位:全部從 safe 既有複製(spread),不動
    //   - _adminForceRestore: true 旗標讓任何下游守門知道這是管理員授權
    let _safeWriteResult = null;
    try {
      const _safeRef = doc(_fbDb, 'players', uid, 'saves', 'safe');
      let _existingSafe = {};
      try {
        const _safeSnap = await getDocFromServer(_safeRef);
        if(_safeSnap.exists()) _existingSafe = _safeSnap.data() || {};
      } catch(_eReadSafe){
        console.warn('[補償 v3.9.2] safe 槽讀取失敗,將以空白起點寫入', _eReadSafe);
      }
      // unlockedHeroes union
      const _safeExistUnlocked = Array.isArray(_existingSafe.unlockedHeroes) ? _existingSafe.unlockedHeroes : [];
      const _safeMergedUnlocked = Array.from(new Set([..._safeExistUnlocked, ..._newUnlocked]));
      // heroLevels max-merge
      const _safeExistLevels = (_existingSafe.heroLevels && typeof _existingSafe.heroLevels === 'object') ? _existingSafe.heroLevels : {};
      const _safeMergedLevels = Object.assign({}, _safeExistLevels);
      Object.keys(_newLevels).forEach(function(name){
        const _newLv = Math.max(1, Math.min(50, _newLevels[name] || 1));
        const _curLv = _safeMergedLevels[name] || 0;
        _safeMergedLevels[name] = Math.max(_curLv, _newLv);
      });
      // coins — 同主文件邏輯
      let _safeNewCoins = _existingSafe.knowledgeCoins || 0;
      if(typeof compensation.coins === 'number'){
        if(compensation.coinsMode === 'add'){
          _safeNewCoins = Math.max(0, _safeNewCoins + compensation.coins);
        } else {
          _safeNewCoins = Math.max(_safeNewCoins, Math.max(0, compensation.coins));
        }
      }
      // 背包 — 同主文件邏輯
      const _safeExistBp = (_existingSafe.playerBackpack && typeof _existingSafe.playerBackpack === 'object') ? _existingSafe.playerBackpack : {};
      const _safeNewBp = Object.assign({}, _safeExistBp);
      if(compensation.backpack && typeof compensation.backpack === 'object'){
        Object.keys(compensation.backpack).forEach(function(itemId){
          const add = Math.max(0, compensation.backpack[itemId] || 0);
          if(add === 0) return;
          _safeNewBp[itemId] = Math.min(99, (_safeNewBp[itemId] || 0) + add);
        });
      }
      // 算 safe 版 _dataSummary
      const _safeBagItems = Object.keys(_safeNewBp).reduce(function(s, k){ return s + (_safeNewBp[k] || 0); }, 0);
      let _safeMaxLv = 0, _safeTotalLv = 0;
      Object.keys(_safeMergedLevels).forEach(function(n){
        if(!_isPlayerHeroName(n)) return;   // ★ v3.15.41 乙:只算白名單英雄
        const _lv = _safeMergedLevels[n] || 0;
        if(_lv > _safeMaxLv) _safeMaxLv = _lv;
        _safeTotalLv += _lv;
      });
      const _safePayload = Object.assign({}, _existingSafe, {
        savedAt:           _now,
        unlockedHeroes:    _safeMergedUnlocked,
        heroLevels:        _safeMergedLevels,
        heroLevels_s:      JSON.stringify(_safeMergedLevels),
        knowledgeCoins:    _safeNewCoins,
        playerBackpack:    _safeNewBp,
        playerBackpack_s:  JSON.stringify(_safeNewBp),
        // ★ v5.158.0 — safe 槽原本完全沒寫 taiwanTreasureData,補償的至寶對 safe 槽等於沒補
        //   (下次選到 safe 槽=至寶消失);比照 live 槽同一套 union 寫法補上。
        taiwanTreasureData: (function(){ const base=Object.assign({},(_existingSafe.taiwanTreasureData&&typeof _existingSafe.taiwanTreasureData==='object')?_existingSafe.taiwanTreasureData:{}); if(Array.isArray(compensation.treasures)) compensation.treasures.forEach(function(tid){ if(tid&&!base[tid]) base[tid]={lv:1,exp:0,equippedTo:null}; }); return base; })(),
        taiwanTreasureData_s: (function(){ const base=Object.assign({},(_existingSafe.taiwanTreasureData&&typeof _existingSafe.taiwanTreasureData==='object')?_existingSafe.taiwanTreasureData:{}); if(Array.isArray(compensation.treasures)) compensation.treasures.forEach(function(tid){ if(tid&&!base[tid]) base[tid]={lv:1,exp:0,equippedTo:null}; }); return JSON.stringify(base); })(),
        // ★ v5.158.0 — safe 槽自己疊上同一批 admin_grant 紀錄(在 safe 既有的 history 基礎上,不是整批覆蓋)
        //   否則這隻剛補償的英雄/至寶在 safe 槽查無紀錄,日後被任何「查無紀錄回收」工具當幻影誤收。
        _heroUnlockHistory: _appendHeroHist(_existingSafe._heroUnlockHistory),
        _heroUnlockHistory_s: JSON.stringify(_appendHeroHist(_existingSafe._heroUnlockHistory)),
        _treasureUnlockHistory: _appendTreHist(_existingSafe._treasureUnlockHistory),
        _treasureUnlockHistory_s: JSON.stringify(_appendTreHist(_existingSafe._treasureUnlockHistory)),
        // ★ v3.15.37 — safe 槽鬥技之證(以 safe 既有值 +N,並確保 lifetime >= held)
        arenaZhengHeld: (function(){ var b=(typeof _existingSafe.arenaZhengHeld==='number'&&_existingSafe.arenaZhengHeld>=0)?Math.floor(_existingSafe.arenaZhengHeld):0; return b + _compArenaZheng; })(),
        arenaZhengLifetime: (function(){ var bl=(typeof _existingSafe.arenaZhengLifetime==='number'&&_existingSafe.arenaZhengLifetime>=0)?Math.floor(_existingSafe.arenaZhengLifetime):0; var bh=(typeof _existingSafe.arenaZhengHeld==='number'&&_existingSafe.arenaZhengHeld>=0)?Math.floor(_existingSafe.arenaZhengHeld):0; return Math.max(bl + _compArenaZheng, bh + _compArenaZheng); })(),
        _dataSummary: {
          unlockedCount: _safeMergedUnlocked.filter(_isPlayerHeroName).length,
          totalHeroLv:   _safeTotalLv,
          maxHeroLv:     _safeMaxLv,
          coins:         _safeNewCoins,
          bagItems:      _safeBagItems,
          ts:            _now,
          _source:       '_fbCompensatePlayer_v3.9.2_safe_force'
        },
        _adminForceRestore:     true,
        _lastAdminCompensateAt: _now,
        _compensationCount:     _existCount + 1,
        _compensationHistory:   _newHistory,
        _compensationHistory_s: JSON.stringify(_newHistory)
      });
      // ★ 強制 setDoc(不走 _fbWriteSafeIfHealthier 的健康度守門)
      //   理由:這是管理員授權的補償操作,且我們已經做了 max-merge 確保 safe 不會被弱化
      await setDoc(_safeRef, _safePayload);
      _safeWriteResult = { written: true, reason: 'force_write_admin_compensate' };
      console.info('✅ [補償 v3.9.2] (3/3) safe 槽強制寫入完成 ' + uid
        + ' — unlocked=' + _safeMergedUnlocked.length
        + ', maxLv=' + _safeMaxLv);
    } catch(_eSafe){
      console.error('🚨 [補償 v3.9.2] (3/3) safe 槽強制寫入失敗 — 學生下次選 safe 槽會看不到補償!', _eSafe);
      _safeWriteResult = { written: false, reason: 'force_write_exception', error: _eSafe && _eSafe.message };
    }
    // 清掉中間欄位(不要寫進 return)
    delete payload._liveMerged;

    console.info('✅ [補償 v3.9.0] 三槽寫入完成 ' + uid, {
      main: true,
      live: _liveWriteOk,
      safe: _safeWriteResult && _safeWriteResult.written
    });
    return {
      ok: true,
      uid,
      count: _existCount + 1,
      mergedUnlocked: _mergedUnlocked,
      mergedLevels: _mergedLevels,
      mergedCoins: _newCoins,
      // ★ v3.9.0 — 回傳三槽寫入結果,給後台 UI 顯示
      slotsWritten: {
        main: true,
        live: _liveWriteOk,
        safe: !!(_safeWriteResult && _safeWriteResult.written),
        safeReason: _safeWriteResult ? _safeWriteResult.reason : 'api_unavailable'
      },
      // ★ v1.0.20260513.6450 — 回傳 diff 與「補了什麼但被規則擋下沒生效」的資訊,UI 拿來做對照表
      diff: _diff,
      // 「想補但沒生效」:讓 UI 提示老師「這隻其實本來就比較高,沒升」
      requested: {
        unlockedHeroes: _newUnlocked,
        heroLevels: _newLevels,
        coins: (typeof compensation.coins === 'number') ? compensation.coins : 0,
        coinsMode: compensation.coinsMode || 'add',
        backpack: compensation.backpack || {},
        arenaZheng: _compArenaZheng
      },
      // 補償前的舊值,UI 對照表會用到
      before: {
        unlockedHeroes: _existUnlocked,
        heroLevels: _existLevels,
        coins: existing.knowledgeCoins || 0,
        backpack: _existBp,
        arenaZhengHeld: _existArenaHeld
      }
    };
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.15.40 — GM 一鍵帳號重建(ledger-based account rebuild)
  // ────────────────────────────────────────────────────────────────────
  // 用途:對「已被舊版弄壞」的帳號,用雲端帳本(歷史紀錄)反推「這個帳號本來應該有什麼」,
  //       跟現況(三槽合併)比對出「缺了什麼」,一鍵補回。跨槽合併(_lxpsMergeSlots)已能在每次
  //       登入自動把散在各槽的英雄/至寶/等級/友情/鬥技證撿回;此工具額外救援:
  //         ① 三槽全都掉、但解鎖紀錄還在(_heroUnlockHistory)的英雄
  //         ② 消耗型餘額(水晶/知識幣)— 用 _crystalTransactions / _coinTransactions 最後一筆 balance 反推
  //       不會降級:只補不減(走 _fbCompensatePlayer 的 union/max 寫入),且排除被 GM 刪除的英雄。
  // 流程:_fbRebuildAccountFromLedgers(uid) 回傳 { payload, diff, current, ... } 供 GM 審閱;
  //       GM 確認後 _fbApplyAccountRebuild(uid, payload) 套用(內部走 _fbCompensatePlayer 三槽寫入)。
  // ════════════════════════════════════════════════════════════════════
  window._fbRebuildAccountFromLedgers = async function(uid){
    if(!uid) throw new Error('缺少 uid');
    // ① 讀三槽 + 合併成權威現況
    var _slots = [];
    try{
      var _r = await Promise.allSettled([
        window._fbLoadSlot ? window._fbLoadSlot(uid, 'live') : Promise.resolve(null),
        window._fbLoadSlot ? window._fbLoadSlot(uid, 'safe') : Promise.resolve(null),
        window._fbLoad ? window._fbLoad(uid) : Promise.resolve(null)
      ]);
      _r.forEach(function(x){ if(x.status === 'fulfilled' && x.value) _slots.push(x.value); });
    }catch(_eLoad){ throw new Error('讀取帳號資料失敗:' + (_eLoad && _eLoad.message)); }
    if(_slots.length === 0) throw new Error('此帳號三槽皆無資料(可能尚未登入過)');
    var _cur = (typeof window._lxpsMergeSlots === 'function' && _slots.length > 1)
      ? (window._lxpsMergeSlots(_slots) || _slots[0]) : _slots[0];

    // ② 從合併後的帳本反推「應該有什麼」
    var _arr = function(o, k){ try{ if(Array.isArray(o[k])) return o[k]; if(typeof o[k+'_s']==='string'){ var p=JSON.parse(o[k+'_s']); if(Array.isArray(p)) return p; } }catch(_){} return []; };
    var _heroHist = _arr(_cur, '_heroUnlockHistory');
    // ★ v3.16.84 — 修「帳本核對補回英雄錯很多·學生說都不是他們的」根因(共用 iPad 跨帳號污染 + 退回標記被當該有):
    //   (1) 只認「本帳號 uid」自己的解鎖紀錄 → 濾掉別帳號殘留在 _heroUnlockHistory 的解鎖事件
    //       (advSaveUnlockedHero 自 v3.11.9 起每筆都寫 uid=自己前12碼;共用平板上別人留在本機/雲端的紀錄帶別人的 uid)。
    //   (2) 「已移除/退回」類來源一律不補回:admin_delete(GM刪)、audit_error_recovered(審核判無真實取得而回收·或學生圖鑑自審按「不是我的」)
    //       → 等同 admin_delete 排除,不把審核已剔除的借用/污染英雄又補回去。(真的自己抽到卻被誤回收 → 走「我遺失的英雄要回來」人工復原,不靠這支自動補)
    var _uid12 = String(uid).slice(0, 12);
    var _REMOVE_SRC = { 'admin_delete': 1, 'audit_error_recovered': 1 };
    // 每隻英雄「本帳號自己」最近一筆動作(admin_delete/audit_error_recovered=已移除/退回,排除)
    var _latest = {};
    _heroHist.forEach(function(e){
      if(!e || !e.name) return;
      if(String(e.uid || '') !== _uid12) return;   // ★ 只認本帳號自己的解鎖紀錄(濾跨帳號污染)
      if(!_latest[e.name] || (e.at||0) >= _latest[e.name].at) _latest[e.name] = { at:(e.at||0), source:e.source||'' };
    });
    var _shouldHaveHeroes = {};
    Object.keys(_latest).forEach(function(n){ if(!_REMOVE_SRC[_latest[n].source]) _shouldHaveHeroes[n] = 1; });
    // ★ v3.16.84(老師裁示)— 另記「別人 uid 真實解鎖紀錄」的英雄(非 seal/delete/player_confirmed)→
    //   區分「GM 過度補回的別人英雄」(有別人紀錄·真的是別位同學的)與「真正舊英雄」(完全無紀錄)。
    //   前者交「過度補回稽查與回收」工具回收+補償+道歉,不當 grandfather 歸屬本帳號。
    var _otherUidRec = {};
    _heroHist.forEach(function(e){
      if(!e || !e.name) return;
      var _es = String(e.source || '');
      if(_es === 'migration_seal' || _es === 'admin_delete' || _es === 'player_confirmed') return;
      var _eu = String(e.uid || '');
      if(_eu && _eu.slice(0, 12) !== _uid12) _otherUidRec[e.name] = 1;
    });
    // 現有英雄(合併後)
    var _curHeroes = {};
    _arr(_cur, 'unlockedHeroes').forEach(function(n){ if(n) _curHeroes[n] = 1; });
    // 缺的英雄 = 應該有 − 現有(且非被刪)
    var _missingHeroes = Object.keys(_shouldHaveHeroes).filter(function(n){ return !_curHeroes[n]; });
    // ★ v3.15.85(需求2)— 缺漏英雄附等級(從合併後 heroLevels 讀,即學生原本練到的等級;補回時 _fbCompensatePlayer max-merge 保留不重置)
    var _curLevels = (_cur.heroLevels && typeof _cur.heroLevels === 'object') ? _cur.heroLevels : {};
    var _missingHeroDetail = _missingHeroes.map(function(n){ return { name:n, lv:(typeof _curLevels[n]==='number' && _curLevels[n]>0 ? _curLevels[n] : 1) }; });
    // ★ v3.16.84(老師裁示 2026-06-29)— 「沒有 UID 紀錄的舊英雄」歸屬判定:只要練過(lv>1)/裝至寶/投資過 → 視為合法擁有,
    //   不列入「帳本查無紀錄」污染清單、不移除(玩家端登入會自動補蓋 legacy_grandfather UID 紀錄·永久歸屬不再被誤判)。
    //   證據一律從「載入合併後的 _cur」算(不可用記憶體全域 — 那是別的玩家);含 _s 字串版兜底。
    var _equipSetRb = {};
    try{
      var _tdRb = (_cur.taiwanTreasureData && typeof _cur.taiwanTreasureData==='object') ? _cur.taiwanTreasureData
                : (typeof _cur.taiwanTreasureData_s==='string' ? JSON.parse(_cur.taiwanTreasureData_s) : null);
      if(_tdRb && typeof _tdRb==='object'){ Object.keys(_tdRb).forEach(function(k){ var t=_tdRb[k]; if(t && t.equippedTo) _equipSetRb[t.equippedTo]=1; }); }
    }catch(_){}
    var _objOfRb = function(k){ try{ if(_cur[k] && typeof _cur[k]==='object') return _cur[k]; if(typeof _cur[k+'_s']==='string'){ var p=JSON.parse(_cur[k+'_s']); if(p&&typeof p==='object') return p; } }catch(_){} return {}; };
    var _invStatRb=_objOfRb('heroStatInvested'), _invSkillRb=_objOfRb('heroSkillLevels'), _invBurstRb=_objOfRb('heroBurstLevels'), _invTraitRb=_objOfRb('heroTraitLevel');
    var _isLegacyOwned = function(n){
      try{
        if(typeof _curLevels[n]==='number' && _curLevels[n] > 1) return true;       // 練過(等級>1)
        if(_equipSetRb[n]) return true;                                            // 裝至寶
        var _iv=_invStatRb[n]; if(_iv){ if(typeof _iv==='object'){ for(var k in _iv){ if((_iv[k]||0)>0) return true; } } else if((_iv||0)>0) return true; }
        var _sk=_invSkillRb[n]; if(_sk){ if(typeof _sk==='object'){ for(var k2 in _sk){ if((_sk[k2]||0)>1) return true; } } else if((_sk||0)>1) return true; }
        if((_invBurstRb[n]||0)>1) return true;
        if((_invTraitRb[n]||0)>1) return true;
      }catch(_){}
      return false;
    };
    var _grandfathered = [];   // ★ v3.16.84 無自己 uid 紀錄但練過/裝至寶/投資過 → 舊英雄歸屬玩家(報告供 GM 知悉·不補回不移除)
    // ★ v3.16.5(piece2)— 幻影角色偵測:現有 unlockedHeroes 中「帳本判定不該有」的英雄
    //   (a) _extraDeleted:帳本最近一筆=admin_delete(被刪卻又出現=污染復活/過時 session 蓋回)→ 安全自動移除(帳本鐵證)
    //   (b) _extraNoRecord:帳本完全查無紀錄(跨帳號殘留 或 舊版 pre-ledger 合法角色,兩者皆無紀錄、無法自動分辨)→ 僅報告供老師審核,絕不自動移除
    var _extraDeleted = [];
    var _crossAccount = [];   // ★ v3.16.84 有別人 uid 真實紀錄 + 自己無 → GM 過度補回的別人英雄(交回收工具)
    var _extraNoRecord = [];
    Object.keys(_curHeroes).forEach(function(n){
      if(_shouldHaveHeroes[n]) return;  // 帳本說該有 → 正常,跳過
      // ★ v3.16.21 — 初始 8 隻起始角色(電腦繪圖師/程式設計師/弦樂團員/小劇團員/田徑隊員/直笛團員/籃球隊員/動物學家)
      //   是「帳號建立即贈」的基底英雄,本來就不會有「解鎖帳本紀錄」→ 永久視為合法、免審查:
      //   既不列入「帳本查無紀錄」清單(否則每個帳號都會把這 8 隻誤標成可疑),也不會被當幻影移除。
      //   與玩家端自助審查(_rareSrcCat 旁 initial 判定·v3.13.52)同口徑,對齊。
      if((window._ARENA_INITIAL_HEROES || []).indexOf(n) >= 0) return;
      // ★ PHASE 2 A1 — 主角:預設持有、無解鎖帳本紀錄(與初始8同輪廓)→ 永久免審查、不列查無紀錄、不當幻影移除
      if(window._isProtagHero && window._isProtagHero(n)) return;
      if(_latest[n] && _latest[n].source === 'admin_delete'){ _extraDeleted.push(n); return; }
      // ★ v3.16.84 — 有「別人 uid 真實解鎖紀錄」+ 自己無紀錄 → GM 過度補回的別人英雄(跨帳號污染·真的是別位同學的)。
      //   不歸屬玩家、不列查無紀錄;報告供老師用「🔍 過度補回稽查與回收」工具回收+補償+道歉。
      if(_otherUidRec[n]){ _crossAccount.push(n); return; }
      // ★ v3.16.84(老師裁示)— 完全無紀錄(自己/別人都無)但練過(lv>1)/裝至寶/投資過 → 真正舊英雄歸屬玩家(grandfather):
      //   不列「帳本查無紀錄」污染、不移除(玩家端登入自動補蓋 UID 紀錄)。
      if(_isLegacyOwned(n)){ _grandfathered.push(n); return; }
      if(!_latest[n]) _extraNoRecord.push(n);   // 真正查無紀錄 + 沒練過/沒投資/沒裝至寶 → 純疑似污染,僅報告(仍不自動移除)
    });
    // ★ v3.15.85(需求2)— 缺漏至寶:解鎖紀錄有、但現況 taiwanTreasureData 沒有(三槽全掉)的至寶,補回(新補給 Lv1;排除 GM admin_delete)
    var _treHist = _arr(_cur, '_treasureUnlockHistory');
    var _treLatest = {};
    // ★ v3.16.84 — 同英雄:只認本帳號 uid 自己的至寶解鎖紀錄,濾掉共用 iPad 別帳號殘留(_advSaveTreasureUnlockHistory 亦每筆寫 uid=自己前12碼)
    _treHist.forEach(function(e){ if(!e || !e.id) return; if(String(e.uid || '') !== _uid12) return; if(!_treLatest[e.id] || (e.at||0) >= _treLatest[e.id].at) _treLatest[e.id] = { at:(e.at||0), source:e.source||'' }; });
    var _curTre = {};
    try{ var _td = _cur.taiwanTreasureData; if(!(_td && typeof _td==='object') && typeof _cur.taiwanTreasureData_s==='string'){ _td = JSON.parse(_cur.taiwanTreasureData_s); } if(_td && typeof _td==='object'){ Object.keys(_td).forEach(function(id){ _curTre[id] = _td[id]; }); } }catch(_){}
    var _treNameOf = function(id){ try{ var t = window.TAIWAN_TREASURES && window.TAIWAN_TREASURES[id]; return (t && t.name) ? t.name : id; }catch(_){ return id; } };
    // ★ v3.15.97 — 排除日本三神器(tengu/shutendoji/tamamo):任務物品不是至寶,不可從至寶帳本反推補回。
    //   雙保險:即使舊帳本殘留三神器解鎖紀錄(source=japan_clear),救援也不會把它們當至寶補進 taiwanTreasureData。
    var _missingTreasures = Object.keys(_treLatest).filter(function(id){ return _treLatest[id].source !== 'admin_delete' && !_curTre[id] && !window._isJapanTreasureKey(id); });
    var _missingTreasureDetail = _missingTreasures.map(function(id){ return { id:id, name:_treNameOf(id), lv:1 }; });

    // ③ 消耗型餘額:帳本最後一筆 balance vs 現況
    var _latestBal = function(txArr){
      var best = null;
      (Array.isArray(txArr)?txArr:[]).forEach(function(t){ if(t && typeof t.balance==='number' && (!best || (t.at||0) >= best.at)) best = { at:(t.at||0), balance:t.balance }; });
      return best ? best.balance : null;
    };
    var _crysLedger = _latestBal(_arr(_cur, '_crystalTransactions'));
    var _coinLedger = _latestBal(_arr(_cur, '_coinTransactions'));
    var _curBag = {}; try{ var b = _cur.playerBackpack; if(b && typeof b==='object') _curBag = b; else if(typeof _cur.playerBackpack_s==='string'){ var pb=JSON.parse(_cur.playerBackpack_s); if(pb&&typeof pb==='object') _curBag=pb; } }catch(_){}
    var _curCrys = (typeof _curBag.summon_crystal === 'number') ? _curBag.summon_crystal : 0;
    var _curCoins = (typeof _cur.knowledgeCoins === 'number') ? _cur.knowledgeCoins : 0;
    // 缺的水晶/幣 = 帳本餘額 − 現況(只在帳本較高時補,代表「被弄丟的」;帳本較低=正常花掉,不動)
    var _missingCrys = (_crysLedger != null && _crysLedger > _curCrys) ? (_crysLedger - _curCrys) : 0;
    var _missingCoins = (_coinLedger != null && _coinLedger > _curCoins) ? (_coinLedger - _curCoins) : 0;
    // 水晶有上限 99,補到不超過上限
    if(_missingCrys > 0) _missingCrys = Math.min(_missingCrys, Math.max(0, 99 - _curCrys));

    // ④ 組補償 payload(餵 _fbCompensatePlayer;只補不減)
    var _payload = { reason: 'GM 一鍵帳號重建(由帳本反推)', coinsMode: 'add' };
    if(_missingHeroes.length) _payload.unlockedHeroes = _missingHeroes;
    if(_missingCoins > 0) _payload.coins = _missingCoins;
    if(_missingCrys > 0) _payload.backpack = { summon_crystal: _missingCrys };
    if(_missingTreasures.length) _payload.treasures = _missingTreasures;   // ★ v3.15.85 補缺漏至寶(union 進 taiwanTreasureData)
    if(_extraDeleted.length) _payload.removeHeroes = _extraDeleted;        // ★ v3.16.5(piece2)只移除類(a)帳本鐵證「已刪卻又出現」者(類b 無紀錄者不自動移除)

    var _hasChanges = (_missingHeroes.length > 0) || (_missingCoins > 0) || (_missingCrys > 0) || (_missingTreasures.length > 0) || (_extraDeleted.length > 0);
    var _diff = {
      missingHeroes: _missingHeroes,
      missingHeroCount: _missingHeroes.length,
      missingHeroDetail: _missingHeroDetail,            // ★ v3.15.85 含等級
      missingTreasures: _missingTreasureDetail,          // ★ v3.15.85 缺漏至寶 id/name/lv
      missingTreasureCount: _missingTreasures.length,
      extraDeletedHeroes: _extraDeleted,         // ★ v3.16.5(piece2)帳本鐵證「已刪卻又出現」→ 將自動移除
      extraDeletedCount: _extraDeleted.length,
      extraNoRecordHeroes: _extraNoRecord,       // ★ v3.16.5(piece2)帳本查無紀錄(跨帳號殘留 或 舊版合法,需老師人工審核;不自動移除)
      extraNoRecordCount: _extraNoRecord.length,
      grandfatheredHeroes: _grandfathered,       // ★ v3.16.84 無 UID 紀錄但練過/裝至寶/投資 → 已歸屬玩家(玩家端登入補蓋 UID·不補回不移除)
      grandfatheredCount: _grandfathered.length,
      crossAccountHeroes: _crossAccount,         // ★ v3.16.84 有別人 uid 真實紀錄 → GM 過度補回的別人英雄(請用「🔍 過度補回稽查與回收」回收+補償+道歉)
      crossAccountCount: _crossAccount.length,
      crystal: { current: _curCrys, ledger: _crysLedger, willAdd: _missingCrys },
      coins: { current: _curCoins, ledger: _coinLedger, willAdd: _missingCoins },
      hasChanges: _hasChanges
    };
    console.info('[GM 帳號重建 v3.15.40] uid=' + String(uid).slice(0,8) + ' diff:', _diff);
    return {
      uid: uid, payload: _payload, diff: _diff, hasChanges: _hasChanges,
      current: { heroes: Object.keys(_curHeroes).length, crystal: _curCrys, coins: _curCoins },
      shouldHave: { heroes: Object.keys(_shouldHaveHeroes).length }
    };
  };

  window._fbApplyAccountRebuild = async function(uid, payload){
    if(!uid) throw new Error('缺少 uid');
    if(typeof window._fbCompensatePlayer !== 'function') throw new Error('_fbCompensatePlayer 未載入');
    var _hasRemove = !!(payload && payload.removeHeroes && payload.removeHeroes.length);
    if(!payload || (!payload.unlockedHeroes && !payload.coins && !payload.backpack && !payload.treasures && !_hasRemove)){
      return { ok: true, noop: true, msg: '無需重建(帳號資料已完整)' };
    }
    var _res = null;
    // 先補缺(add-only,_fbCompensatePlayer;只在有要補的東西時呼叫)
    if(payload.unlockedHeroes || payload.coins || payload.backpack || payload.treasures){
      _res = await window._fbCompensatePlayer(uid, payload);
    }
    // ★ v3.16.5(piece2)— 再移除類(a)幻影角色(帳本鐵證「已刪卻又出現」;復用已補 _s + _authoritativeRestoreAt 的 _fbAdminBulkRemoveHeroes)。
    //   類(b)帳本查無紀錄者「不」在 removeHeroes 內,故絕不會被自動移除(僅在 diff.extraNoRecordHeroes 報告供老師人工審核)。
    var _rmRes = null;
    if(_hasRemove && typeof window._fbAdminBulkRemoveHeroes === 'function'){
      _rmRes = await window._fbAdminBulkRemoveHeroes(uid, payload.removeHeroes, { reason: 'GM 帳號重建—移除帳本鐵證已刪幻影角色' });
    }
    console.info('[GM 帳號重建 v3.16.5] 已套用 uid=' + String(uid).slice(0,8), { add:(_res && _res.slotsWritten), removed:(_rmRes && _rmRes.removed) });
    return { ok: !!((!_res || _res.ok) && (!_rmRes || _rmRes.ok)), result: _res, removeResult: _rmRes };
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.16.84(老師裁示 2026-06-29)— 「過度補回稽查與回收」(修「沒審 uid 的舊救援『確認救援並補回』誤補別人英雄」災難)
  // ────────────────────────────────────────────────────────────────────
  //   過度補回的英雄/至寶「簽章」=在 _heroUnlockHistory / _treasureUnlockHistory 有「別人 uid 的真實解鎖紀錄」
  //   (非 seal/delete)但「自己 uid 完全無真實紀錄、也未 player_confirmed」→ 跨帳號污染被舊救援當「該有」補回的別人英雄。
  //   _fbAdminScanOverRestoredForUid:讀三槽合併,列出這些英雄(名+等級)與至寶(供 GM 核對)。
  //   _fbAdminReclaimOverRestoredForUid:回收(走既有 _fbAdminBulkRemoveHeroes / _fbAdminRejectAuditTreasures·寫三槽+標記不復活)
  //     + 對「已練(lv>1)」者補償(知識幣+召喚水晶)+ 寄道歉通知(玩家登入彈窗·列回收項目與補償)。
  // ════════════════════════════════════════════════════════════════════
  window._fbAdminScanOverRestoredForUid = async function(uid){
    if(!uid) throw new Error('缺少 uid');
    var _slots = [];
    try{
      var _r = await Promise.allSettled([
        window._fbLoadSlot ? window._fbLoadSlot(uid, 'live') : Promise.resolve(null),
        window._fbLoadSlot ? window._fbLoadSlot(uid, 'safe') : Promise.resolve(null),
        window._fbLoad ? window._fbLoad(uid) : Promise.resolve(null)
      ]);
      _r.forEach(function(x){ if(x.status === 'fulfilled' && x.value) _slots.push(x.value); });
    }catch(_e){ throw new Error('讀取帳號資料失敗:' + (_e && _e.message)); }
    if(!_slots.length) throw new Error('此帳號三槽皆無資料(可能尚未登入過)');
    var _cur = (typeof window._lxpsMergeSlots === 'function' && _slots.length > 1) ? (window._lxpsMergeSlots(_slots) || _slots[0]) : _slots[0];
    var _uid12 = String(uid).slice(0, 12);
    var _arr = function(o, k){ try{ if(Array.isArray(o[k])) return o[k]; if(typeof o[k+'_s']==='string'){ var p=JSON.parse(o[k+'_s']); if(Array.isArray(p)) return p; } }catch(_){} return []; };
    var _initSet = {}; (window._ARENA_INITIAL_HEROES || []).forEach(function(n){ _initSet[n] = 1; });
    // 英雄:逐隻判 own/other 真實紀錄 + player_confirmed
    var _heroHist = _arr(_cur, '_heroUnlockHistory');
    var _ownRec = {}, _otherRec = {}, _confirmed = {};
    _heroHist.forEach(function(e){
      if(!e || !e.name) return;
      var src = String(e.source || '');
      var _eu = String(e.uid || '');
      var _mine = (!_eu || _eu.slice(0, 12) === _uid12);
      if(src === 'player_confirmed'){ if(_mine) _confirmed[e.name] = 1; return; }
      if(src === 'migration_seal' || src === 'admin_delete') return;
      if(_mine){ _ownRec[e.name] = 1; }
      else { var _pe = _otherRec[e.name]; if(!_pe || (e.at||0) >= (_pe.at||0)) _otherRec[e.name] = { source:src, at:(e.at||0), uid:_eu }; }
    });
    var _curHeroes = {}; _arr(_cur, 'unlockedHeroes').forEach(function(n){ if(n) _curHeroes[n] = 1; });
    var _curLevels = (_cur.heroLevels && typeof _cur.heroLevels === 'object') ? _cur.heroLevels : {};
    var _heroes = [];
    Object.keys(_curHeroes).forEach(function(n){
      if(_initSet[n] || _ownRec[n] || _confirmed[n]) return;   // 初始8 / 自己有真實紀錄 / 已確認 → 不是過度補回
      if(_otherRec[n]){ var _or = _otherRec[n]; _heroes.push({ name:n, lv:(typeof _curLevels[n]==='number' && _curLevels[n]>0 ? _curLevels[n] : 1), source:(_or.source||''), at:(_or.at||0), byUid:(_or.uid||'') }); }
    });
    // 至寶:同理
    var _treHist = _arr(_cur, '_treasureUnlockHistory');
    var _tOwn = {}, _tOther = {}, _tConf = {};
    _treHist.forEach(function(e){
      if(!e || !e.id) return;
      var src = String(e.source || '');
      var _eu = String(e.uid || '');
      var _mine = (!_eu || _eu.slice(0, 12) === _uid12);
      if(src === 'player_confirmed'){ if(_mine) _tConf[e.id] = 1; return; }
      if(src === 'migration_seal' || src === 'admin_delete') return;
      if(_mine){ _tOwn[e.id] = 1; }
      else { var _tpe = _tOther[e.id]; if(!_tpe || (e.at||0) >= (_tpe.at||0)) _tOther[e.id] = { source:src, at:(e.at||0), uid:_eu }; }
    });
    var _curTre = {};
    try{ var _td = _cur.taiwanTreasureData; if(!(_td && typeof _td==='object') && typeof _cur.taiwanTreasureData_s==='string') _td = JSON.parse(_cur.taiwanTreasureData_s); if(_td && typeof _td==='object') Object.keys(_td).forEach(function(id){ _curTre[id] = 1; }); }catch(_){}
    var _treNameOf = function(id){ try{ var t = window.TAIWAN_TREASURES && window.TAIWAN_TREASURES[id]; return (t && t.name) ? t.name : id; }catch(_){ return id; } };
    var _treasures = [];
    Object.keys(_curTre).forEach(function(id){
      if(window._isJapanTreasureKey && window._isJapanTreasureKey(id)) return;
      if(_tOwn[id] || _tConf[id]) return;
      if(_tOther[id]){ var _tor = _tOther[id]; _treasures.push({ id:id, name:_treNameOf(id), source:(_tor.source||''), at:(_tor.at||0), byUid:(_tor.uid||'') }); }
    });
    return { uid: uid, email: (_cur.email || ''), heroes: _heroes, treasures: _treasures,
             trainedCount: _heroes.filter(function(h){ return h.lv > 1; }).length };
  };

  window._fbAdminReclaimOverRestoredForUid = async function(uid, heroNames, treIds){
    if(!uid) throw new Error('缺少 uid');
    var _hn = Array.isArray(heroNames) ? heroNames.filter(Boolean) : [];
    var _ti = Array.isArray(treIds) ? treIds.filter(Boolean) : [];
    if(!_hn.length && !_ti.length) return { ok:false, reason:'no_items' };
    // 移除前先讀目前等級(算補償用)
    var _lvMap = {};
    try{ var _scan = await window._fbAdminScanOverRestoredForUid(uid); (_scan.heroes||[]).forEach(function(h){ _lvMap[h.name] = h.lv || 1; }); }catch(_){}
    // 補償:對「已練(lv>1)」者給知識幣+召喚水晶(先補後收·比照帳號重建 add→remove 順序)
    var _trained = _hn.filter(function(n){ return (_lvMap[n] || 1) > 1; });
    var _lvSum = _trained.reduce(function(s, n){ return s + (_lvMap[n] || 1); }, 0);
    var _compApplied = null;
    if(_trained.length){
      var _coins = _lvSum * 500;
      var _crys = Math.min(_trained.length * 2, 20);
      try{
        if(typeof window._fbCompensatePlayer === 'function'){
          await window._fbCompensatePlayer(uid, { reason:'過度補回回收補償(練功心力)', coinsMode:'add', coins:_coins, backpack:{ summon_crystal:_crys } });
        }
        _compApplied = { coins:_coins, crystals:_crys, trained:_trained.slice() };
      }catch(_){}
    }
    // 回收英雄(寫三槽 + admin_delete 標記不復活 + 清養成 _s)
    var _hRes = null;
    if(_hn.length && typeof window._fbAdminBulkRemoveHeroes === 'function'){
      _hRes = await window._fbAdminBulkRemoveHeroes(uid, _hn, { reason:'GM 過度補回回收—沒審 uid 的舊救援誤補的別位同學英雄' });
    }
    // 回收至寶
    var _tRes = null;
    if(_ti.length && typeof window._fbAdminRejectAuditTreasures === 'function'){
      _tRes = await window._fbAdminRejectAuditTreasures(uid, _ti);
    }
    // 道歉通知(玩家登入彈窗)
    try{
      if(typeof window._fbAdminSendNotificationToPlayer === 'function'){
        var _treNames = _ti.map(function(id){ try{ var t = window.TAIWAN_TREASURES && window.TAIWAN_TREASURES[id]; return (t && t.name) ? t.name : id; }catch(_){ return id; } });
        var _removedAll = _hn.concat(_treNames);
        var _body = '老師發現先前的「帳號救援」因系統設定問題,誤把不屬於你的角色／至寶補進了你的帳號(這些其實是別位同學的)。'
          + '現在已把以下誤發的項目收回:' + _removedAll.join('、') + '。'
          + (_compApplied ? ('因為你已經花了心力培養其中一些,老師補償你 💰知識幣 ×' + _compApplied.coins + ' ＋ 🔮召喚水晶 ×' + _compApplied.crystals + ',請拿去培養你自己真正的英雄！') : '這些角色你還沒開始培養,所以沒有額外補償喔。')
          + ' 造成困擾非常抱歉，你原本就有的英雄完全不受影響！';
        await window._fbAdminSendNotificationToPlayer(uid, { title:'🙇 老師的道歉與補償通知', body:_body, type:'compensation', compensation:_compApplied, removedItems:_removedAll });
      }
    }catch(_){}
    console.info('[過度補回回收 v3.16.84] uid=' + String(uid).slice(0,8) + ' 回收英雄' + _hn.length + '隻/至寶' + _ti.length + '件·補償', _compApplied);
    return { ok:true, removedHeroes:_hn, removedTreasures:_ti, compensation:_compApplied, heroResult:_hRes, treResult:_tRes };
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.16.85(老師裁示 2026-06-29·甲)— 「過度補回稽查與回收」掃全體版
  // ────────────────────────────────────────────────────────────────────
  //   逐帳號版(_fbShowOverRestoredAudit / _fbAdminScanOverRestoredForUid)需 GM 一個個輸入查;
  //   本掃全體版一次 getDocs(players),用同一套「過度補回簽章」從「頂層 player doc」直接判
  //   (頂層 doc 已含 _heroUnlockHistory(_s)/unlockedHeroes/heroLevels(_s)/taiwanTreasureData(_s)/
  //    _treasureUnlockHistory ── 後者由 _advSaveTreasureUnlockHistory + _lxpsCloudInstantUnlock 寫,每筆帶 uid),
  //   不逐人讀三槽 → 便宜(一次查詢)。此為「快篩」,只決定「該看哪些帳號」。
  //   ★ 實際回收仍走逐帳號權威路徑:UI 對每位先呼 _fbAdminScanOverRestoredForUid(三槽合併重判)
  //     再 _fbAdminReclaimOverRestoredForUid → 絕不會因「頂層 doc 與三槽差異」而誤刪(一律以三槽確認為準)。
  //   簽章:某英雄/至寶在帳本有「別人 uid 的真實解鎖紀錄(非 seal/delete)」但本帳號自己無紀錄、
  //         也未 player_confirmed → 舊救援沒審 uid 誤補的別人項目。初始 8 隻/自己有紀錄/已自確認 → 排除。
  // ════════════════════════════════════════════════════════════════════
  function _computeOverRestoredFromDoc(d, uid){
    try{
      if(!d) return { heroes:[], treasures:[], extraNoRecordHeroes:[], extraNoRecordDetail:[] };
      var _uid12 = String(uid || d.uid || '').slice(0, 12);
      var _arr = function(o, k){ try{ if(Array.isArray(o[k])) return o[k]; if(typeof o[k+'_s']==='string'){ var p=JSON.parse(o[k+'_s']); if(Array.isArray(p)) return p; } }catch(_){} return []; };
      var _initSet = {}; (window._ARENA_INITIAL_HEROES || []).forEach(function(n){ _initSet[n] = 1; });
      // 英雄:逐隻判 own/other 真實紀錄 + player_confirmed
      var _heroHist = _arr(d, '_heroUnlockHistory');
      var _ownRec = {}, _otherRec = {}, _confirmed = {};
      _heroHist.forEach(function(e){
        if(!e || !e.name) return;
        var src = String(e.source || '');
        var _eu = String(e.uid || '');
        var _mine = (!_eu || _eu.slice(0, 12) === _uid12);
        if(src === 'player_confirmed'){ if(_mine) _confirmed[e.name] = 1; return; }
        if(src === 'migration_seal' || src === 'admin_delete') return;
        if(_mine) _ownRec[e.name] = 1; else _otherRec[e.name] = 1;
      });
      var _curHeroes = {}; _arr(d, 'unlockedHeroes').forEach(function(n){ if(n) _curHeroes[n] = 1; });
      var _curLevels = (d.heroLevels && typeof d.heroLevels === 'object') ? d.heroLevels : {};
      if(!(d.heroLevels && typeof d.heroLevels === 'object') && typeof d.heroLevels_s === 'string'){ try{ var _hl = JSON.parse(d.heroLevels_s); if(_hl && typeof _hl === 'object') _curLevels = _hl; }catch(_){} }
      var _heroes = [];
      Object.keys(_curHeroes).forEach(function(n){
        if(_initSet[n] || _ownRec[n] || _confirmed[n]) return;   // 初始8 / 自己有真實紀錄 / 已確認 → 不是過度補回
        if(_otherRec[n]) _heroes.push({ name:n, lv:(typeof _curLevels[n] === 'number' && _curLevels[n] > 0 ? _curLevels[n] : 1) });
      });
      // ════════════════════════════════════════════════════════════════
      // ★ v3.16.94 — 「查無紀錄過度補償」批次掃描欄位(extraNoRecordHeroes/Detail)
      //   獨立第二趟,完全不動上方既有 heroes/treasures 分類。
      //   目的:掃出「GM 補償(compensation)發放、或帳本完全查無取得紀錄」、
      //         且學生「沒有任何投資證據(effort)」的英雄 → 供 GM 批次標記+一鍵收回。
      //   反向白名單 _REMOVED_OR_SUSPECT:這些 source 不算「真實取得」。
      //   _genuineOwn(真實取得·永久保護):admin_grant/legacy_grandfather/summon_rare/
      //         initial/summon… 任何非 suspect 的本人紀錄 → 一律保護不收回。
      //   ★ effort 閘門 + genuineOwn + 收回前權威重判 + 老師人工審查 → 絕不誤收學生心血。
      // ────────────────────────────────────────────────────────────────
      var _genuineOwn = {}, _compRec = {}, _compAt = {}, _anyOwnRec = {};
      var _REMOVED_OR_SUSPECT = { compensation:1, migration_seal:1, admin_delete:1, audit_error_recovered:1, player_confirmed:1 };
      _heroHist.forEach(function(e){
        if(!e || !e.name) return;
        var src = String(e.source || '');
        var _eu = String(e.uid || '');
        var _mine = (!_eu || _eu.slice(0, 12) === _uid12);
        if(!_mine) return;
        if(src !== 'migration_seal' && src !== 'admin_delete') _anyOwnRec[e.name] = 1;
        if(src === 'compensation'){
          _compRec[e.name] = 1;
          var _at = (typeof e.at === 'number') ? e.at : 0;
          if(!_compAt[e.name] || _at > _compAt[e.name]) _compAt[e.name] = _at;
        }else if(!_REMOVED_OR_SUSPECT[src]){
          _genuineOwn[e.name] = 1;   // 真實取得來源 → 永久保護
        }
      });
      // effort 閘門:任何投資證據(等級>1/素質/技能/天賦/爆發/點數/經驗/裝至寶)→ 視為學生心血,絕不收回
      var _effMap = function(key){
        try{
          if(d[key] && typeof d[key] === 'object') return d[key];
          if(typeof d[key+'_s'] === 'string'){ var p = JSON.parse(d[key+'_s']); if(p && typeof p === 'object') return p; }
        }catch(_){}
        return {};
      };
      var _hasEffort = function(n){
        try{
          var lv = (typeof _curLevels[n] === 'number') ? _curLevels[n] : 0;
          if(lv > 1) return true;
          var inv = _effMap('heroStatInvested');
          if(inv[n] && typeof inv[n] === 'object'){ var _sv = 0; Object.keys(inv[n]).forEach(function(k){ var v = inv[n][k]; if(typeof v === 'number') _sv += v; }); if(_sv > 0) return true; }
          var sk = _effMap('heroSkillLevels');
          if(sk[n] && typeof sk[n] === 'object'){ var _any = false; Object.keys(sk[n]).forEach(function(k){ if((sk[n][k] || 0) > 0) _any = true; }); if(_any) return true; }
          if((_effMap('heroTraitLevel')[n] || 0) > 0) return true;
          if((_effMap('heroBurstLevels')[n] || 0) > 0) return true;
          if((_effMap('heroStatPoints')[n] || 0) > 0) return true;
          if((_effMap('heroExp')[n] || 0) > 0) return true;
          var _tdE = d.taiwanTreasureData;
          if(!(_tdE && typeof _tdE === 'object') && typeof d.taiwanTreasureData_s === 'string'){ try{ _tdE = JSON.parse(d.taiwanTreasureData_s); }catch(_){ _tdE = null; } }
          if(_tdE && typeof _tdE === 'object'){ var _eq = false; Object.keys(_tdE).forEach(function(id){ var t = _tdE[id]; if(t && t.equippedTo === n) _eq = true; }); if(_eq) return true; }
        }catch(_){}
        return false;
      };
      var _extraNoRecordHeroes = [], _extraNoRecordDetail = [];
      Object.keys(_curHeroes).forEach(function(n){
        if(window._isProtagHero && window._isProtagHero(n)) return;  // ★ PHASE 2 A1 — 主角預設持有無帳本紀錄,永久保護不列過度補回
        if(_initSet[n] || _confirmed[n] || _genuineOwn[n]) return;   // 初始8 / 已確認 / 真實取得 → 永久保護
        if(_hasEffort(n)) return;                                    // 有投資證據 → 學生心血,絕不收回
        var _isComp = !!_compRec[n];
        var _noRec  = !_anyOwnRec[n];
        if(!_isComp && !_noRec) return;                             // 既非補償、又有其他本人紀錄 → 跳過(理論上已被 genuineOwn 擋掉)
        _extraNoRecordHeroes.push(n);
        _extraNoRecordDetail.push({
          name: n,
          lv: (typeof _curLevels[n] === 'number' && _curLevels[n] > 0 ? _curLevels[n] : 1),
          compAt: (_compAt[n] || 0),
          reason: (_isComp ? 'compensation' : 'no_record'),
          hasOther: !!_otherRec[n]
        });
      });
      // 至寶:同理
      var _treHist = _arr(d, '_treasureUnlockHistory');
      var _tOwn = {}, _tOther = {}, _tConf = {};
      _treHist.forEach(function(e){
        if(!e || !e.id) return;
        var src = String(e.source || '');
        var _eu = String(e.uid || '');
        var _mine = (!_eu || _eu.slice(0, 12) === _uid12);
        if(src === 'player_confirmed'){ if(_mine) _tConf[e.id] = 1; return; }
        if(src === 'migration_seal' || src === 'admin_delete') return;
        if(_mine) _tOwn[e.id] = 1; else _tOther[e.id] = 1;
      });
      var _curTre = {};
      try{ var _td = d.taiwanTreasureData; if(!(_td && typeof _td === 'object') && typeof d.taiwanTreasureData_s === 'string') _td = JSON.parse(d.taiwanTreasureData_s); if(_td && typeof _td === 'object') Object.keys(_td).forEach(function(id){ _curTre[id] = 1; }); }catch(_){}
      var _treNameOf = function(id){ try{ var t = window.TAIWAN_TREASURES && window.TAIWAN_TREASURES[id]; return (t && t.name) ? t.name : id; }catch(_){ return id; } };
      var _treasures = [];
      Object.keys(_curTre).forEach(function(id){
        if(window._isJapanTreasureKey && window._isJapanTreasureKey(id)) return;
        if(_tOwn[id] || _tConf[id]) return;
        if(_tOther[id]) _treasures.push({ id:id, name:_treNameOf(id) });
      });
      return { heroes:_heroes, treasures:_treasures, extraNoRecordHeroes:_extraNoRecordHeroes, extraNoRecordDetail:_extraNoRecordDetail };
    }catch(_e){ return { heroes:[], treasures:[], extraNoRecordHeroes:[], extraNoRecordDetail:[] }; }
  }

  // 掃全體:回傳受影響玩家清單(uid/email/暱稱 + 過度補回英雄[名+Lv]/至寶[id+名] + 計數),按數量排序
  window._fbAdminScanAllOverRestored = async function(){
    var snap = await getDocs(collection(_fbDb, 'players'));
    var _affected = [];
    var _scanned = 0, _totalHeroes = 0, _totalTre = 0;
    snap.forEach(function(docSnap){
      _scanned++;
      try{
        var d = docSnap.data() || {};
        var _r = _computeOverRestoredFromDoc(d, docSnap.id);
        var _h = _r.heroes || [], _t = _r.treasures || [];
        if(_h.length || _t.length){
          _totalHeroes += _h.length; _totalTre += _t.length;
          _affected.push({
            uid: docSnap.id,
            email: (d.email || '').toLowerCase(),
            displayName: (d.displayName || d.displayLabel || ''),
            heroes: _h,
            treasures: _t,
            trainedCount: _h.filter(function(x){ return x.lv > 1; }).length,
            count: _h.length + _t.length
          });
        }
      }catch(_){}
    });
    _affected.sort(function(a,b){ return b.count - a.count; });
    console.log('[_fbAdminScanAllOverRestored] 掃描 ' + _scanned + ' 名玩家,' + _affected.length + ' 名疑似過度補回(英雄 ' + _totalHeroes + ' 隻/至寶 ' + _totalTre + ' 件)');
    return { scanned:_scanned, affectedCount:_affected.length, totalHeroes:_totalHeroes, totalTreasures:_totalTre, affected:_affected };
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.16.92(老師裁示 2026-06-29)— 6/24~6/25 救援「查無紀錄過度補償」掃全體 + GM 直接收回
  // ────────────────────────────────────────────────────────────────────
  //   背景:6/24、6/25 的「帳號救援(確認救援並補回)」因設定問題,自動把一些玩家原本沒有的
  //         英雄補進了 unlockedHeroes,但「沒有寫任何解鎖紀錄」(查無紀錄)。
  //   偵測:_computeOverRestoredFromDoc 的 extraNoRecordHeroes ── 帳本完全查無紀錄、且
  //         「沒練(Lv1)/沒投資/沒裝至寶」者。已投資/已練的英雄會被 legacy_grandfather 補蓋 UID
  //         歸屬(v3.16.84·只補蓋練過/裝至寶/投資過者)→ 不在此清單 → 絕不誤收學生有心力的英雄。
  //   收回:GM 審查清單後一鍵收回;收回前對「最新雲端 doc」再判一次(權威重判),只收回「仍然
  //         查無紀錄」者 → 防玩家在掃描後已練/已補紀錄而誤刪。查無紀錄皆 Lv1 → 不補償。
  // ════════════════════════════════════════════════════════════════════
  window._fbAdminScanAllNoRecordOverComp = async function(){
    var snap = await getDocs(collection(_fbDb, 'players'));
    var _affected = [];
    var _scanned = 0, _totalHeroes = 0;
    var _batchMap = {};        // 'YYYY-MM-DD'(台灣時區) → 該日補償的英雄數
    var _noRecordTotal = 0;    // 查無紀錄(無補償日期)英雄總數
    snap.forEach(function(docSnap){
      _scanned++;
      try{
        var d = docSnap.data() || {};
        var _r = _computeOverRestoredFromDoc(d, docSnap.id);
        var _nr = (_r && Array.isArray(_r.extraNoRecordHeroes)) ? _r.extraNoRecordHeroes.filter(Boolean) : [];
        var _det = (_r && Array.isArray(_r.extraNoRecordDetail)) ? _r.extraNoRecordDetail : [];
        if(_nr.length){
          _totalHeroes += _nr.length;
          _affected.push({
            uid: docSnap.id,
            email: (d.email || '').toLowerCase(),
            displayName: (d.displayName || d.displayLabel || ''),
            heroes: _nr.slice(),
            detail: _det.slice()
          });
          // 聚合批次:補償來源依「補償日期(台灣時區 UTC+8)」分組;查無日期者另計
          _det.forEach(function(x){
            if(x && x.reason === 'compensation' && x.compAt > 0){
              var _dk = '';
              try{ _dk = new Date(x.compAt + 8*3600*1000).toISOString().slice(0,10); }catch(_){ _dk = ''; }
              if(_dk){ _batchMap[_dk] = (_batchMap[_dk] || 0) + 1; }
              else { _noRecordTotal++; }
            } else {
              _noRecordTotal++;
            }
          });
        }
      }catch(_){}
    });
    _affected.sort(function(a,b){ return b.heroes.length - a.heroes.length; });
    var _batches = Object.keys(_batchMap).sort().map(function(k){ return { date:k, count:_batchMap[k] }; });
    console.log('[_fbAdminScanAllNoRecordOverComp] 掃描 ' + _scanned + ' 名,' + _affected.length + ' 名有查無紀錄過度補償英雄(共 ' + _totalHeroes + ' 隻;補償批次 ' + _batches.length + ' 組,查無日期 ' + _noRecordTotal + ' 隻)');
    return { scanned:_scanned, affectedCount:_affected.length, totalHeroes:_totalHeroes, affected:_affected, batches:_batches, noRecordTotal:_noRecordTotal };
  };

  window._fbAdminReclaimNoRecordForUid = async function(uid, heroNames){
    if(!uid) throw new Error('缺少 uid');
    var _req = Array.isArray(heroNames) ? heroNames.filter(Boolean) : [];
    if(!_req.length) return { ok:false, reason:'no_items' };
    // ★ 權威重判:讀最新雲端 doc,只收回「仍然查無紀錄(extraNoRecord)」者 → 防玩家掃描後已練/已補紀錄而誤刪
    var _confirmed = [];
    try{
      var _snap = await getDocFromServer(doc(_fbDb, 'players', uid));
      var _d = (_snap && _snap.exists()) ? (_snap.data() || {}) : {};
      var _rr = _computeOverRestoredFromDoc(_d, uid);
      var _nr = (_rr && Array.isArray(_rr.extraNoRecordHeroes)) ? _rr.extraNoRecordHeroes : [];
      var _nrSet = {}; _nr.forEach(function(n){ _nrSet[n] = 1; });
      _confirmed = _req.filter(function(n){ return _nrSet[n]; });
    }catch(_e){ throw new Error('權威重判失敗:' + (_e && _e.message || _e)); }
    if(!_confirmed.length) return { ok:false, reason:'none_confirmed', requested:_req };
    // 移除(寫三槽 + admin_delete 標記不復活 + 清養成 _s);查無紀錄皆 Lv1 未投資 → 不補償
    var _hRes = null;
    if(typeof window._fbAdminBulkRemoveHeroes === 'function'){
      _hRes = await window._fbAdminBulkRemoveHeroes(uid, _confirmed, { reason:'GM 收回 6/24-6/25 救援查無紀錄過度補償英雄(Lv1 未投資)' });
    } else { throw new Error('_fbAdminBulkRemoveHeroes 未載入(請確認 index.html 版本)'); }
    // 更正通知(玩家登入彈窗)
    try{
      if(typeof window._fbAdminSendNotificationToPlayer === 'function'){
        var _body = '老師發現先前(6/24~6/25)的「帳號救援」因系統設定問題,多補了一些你原本沒有的英雄('
          + _confirmed.join('、') + ')進你的帳號。現在已經把這些誤發的英雄收回。'
          + '這些都是還沒開始培養的,不影響你自己原本的英雄與進度,造成困擾非常抱歉！';
        await window._fbAdminSendNotificationToPlayer(uid, { title:'🙇 老師的更正通知', body:_body, type:'over_comp_reclaim', removedItems:_confirmed });
      }
    }catch(_){}
    console.info('[查無紀錄過度補償回收 v3.16.92] uid=' + String(uid).slice(0,8) + ' 收回 ' + _confirmed.length + ' 隻:', _confirmed);
    return { ok:true, removedHeroes:_confirmed, skipped:_req.filter(function(n){ return _confirmed.indexOf(n) < 0; }) };
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.16.98(2026-06-30·老師裁示)— GM「補償批次回收」(依台灣日期·無真實解鎖紀錄即收·含已練)
  // ────────────────────────────────────────────────────────────────────
  //   背景:某次救援(如 6/25 21:25~21:31)大量 compensation 補進英雄,部分學生已練很高
  //         (拘留者 Lv.36 裝至寶等),被 v3.16.92「查無紀錄」工具的 effort 閘門排除收不回;
  //         圖鑑左下角顯示「來源 compensation」讓學生以為是自己的、不敢動,佔圖鑑、不敢用自選卷。
  //   ★ 老師新標準:不看「練沒練」,改看「有沒有自己真正的解鎖紀錄」——
  //     只有 compensation(且無 summon/ticket/BOSS掉落/admin_grant/初始 等真實紀錄)→ 即使
  //     練到 Lv.36 也回收;學生若自己也抽到/解鎖過(有真實紀錄)→ 一律保留,絕不誤刪。
  //   判定 _computeCompBatchesFromDoc:
  //     SUSPECT(不算真實取得)= compensation / legacy_grandfather / migration_seal /
  //                            admin_delete / audit_error_recovered
  //     realUnlock = 任一「自己 uid(或無 uid 早期)·source 不在 SUSPECT」紀錄
  //                  → 含 player_confirmed / 空字串 / summon_rare / ticket_* / maokong /
  //                    admin_grant(老師正式補發) / initial → 一律保護,不收
  //     候選 = 非初始8 且 無 realUnlock 且 有「自己 compensation」紀錄 → 依該紀錄台灣日期分組
  //   ★ legacy_grandfather 刻意算 SUSPECT(不算真實):這是 v3.16.84「練過自動補蓋 UID」洗白,
  //     正是要突破的點(否則練過的 compensation 英雄被洗白就收不回·違背老師意圖)。
  //   ★ 讀「完整」_heroUnlockHistory 逐筆判定(非圖鑑只顯示的最新一筆)→ 學生若真有 summon 紀錄就保留。
  //   ★ 回收前對最新雲端 doc 權威重判(只收仍符合者)→ 防學生掃描後又自己抽到/確認而誤刪。
  //   ★ 收回走既有 _fbAdminBulkRemoveHeroes(三槽+admin_delete 可逆+清養成_s+至寶解裝保留本體)。
  //   ★ 與 v3.16.92 工具並存:該工具仍保守只收「查無紀錄+Lv1」;此工具放寬收「compensation+已練+無真實解鎖」。
  // ════════════════════════════════════════════════════════════════════
  window._GMCR_COMP_SUSPECT_SRC = { compensation:1, legacy_grandfather:1, migration_seal:1, admin_delete:1, audit_error_recovered:1 };
  function _twDateKeyForComp(at){
    try{ var d = new Date(Number(at) + 8*3600*1000); var y=d.getUTCFullYear(), m=d.getUTCMonth()+1, dd=d.getUTCDate(); return y + '-' + (m<10?'0':'') + m + '-' + (dd<10?'0':'') + dd; }catch(_){ return ''; }
  }
  function _computeCompBatchesFromDoc(d, uid){
    try{
      if(!d) return { byDate:{} };
      var _uid12 = String(uid || d.uid || '').slice(0, 12);
      var _arr = function(o, k){ try{ if(Array.isArray(o[k])) return o[k]; if(typeof o[k+'_s']==='string'){ var p=JSON.parse(o[k+'_s']); if(Array.isArray(p)) return p; } }catch(_){} return []; };
      var _initSet = {}; (window._ARENA_INITIAL_HEROES || []).forEach(function(n){ _initSet[n] = 1; });
      var _hist = _arr(d, '_heroUnlockHistory');
      var _realUnlock = {}, _compDates = {};   // name → realUnlock 1 / name → {date:1}
      _hist.forEach(function(e){
        if(!e || !e.name) return;
        var src = String(e.source || '');
        var _eu = String(e.uid || '');
        var _mine = (!_eu || _eu.slice(0, 12) === _uid12);
        if(!_mine) return;                                  // 別人 uid 紀錄 → 不影響本帳號判定(那是跨帳號污染·另案)
        if(src === 'compensation'){
          var _dk = _twDateKeyForComp(e.at);
          if(_dk){ if(!_compDates[e.name]) _compDates[e.name] = {}; _compDates[e.name][_dk] = 1; }
        }
        if(!window._GMCR_COMP_SUSPECT_SRC[src]) _realUnlock[e.name] = 1;   // 真實取得(含空字串/player_confirmed/summon/ticket/maokong/admin_grant/initial)→ 永久保護
      });
      var _curLevels = (d.heroLevels && typeof d.heroLevels === 'object') ? d.heroLevels : {};
      if(!(d.heroLevels && typeof d.heroLevels === 'object') && typeof d.heroLevels_s === 'string'){ try{ var _hl = JSON.parse(d.heroLevels_s); if(_hl && typeof _hl === 'object') _curLevels = _hl; }catch(_){} }
      var _cur = {}; _arr(d, 'unlockedHeroes').forEach(function(n){ if(n) _cur[n] = 1; });
      var _byDate = {};
      Object.keys(_cur).forEach(function(n){
        if(_initSet[n]) return;            // 初始8 → 保護
        if(_realUnlock[n]) return;         // 有真實解鎖紀錄 → 保護(學生自己有就不收)
        var _dates = _compDates[n];
        if(!_dates) return;                // 無自己 compensation 紀錄 → 不是這次回收對象
        var _lv = (typeof _curLevels[n] === 'number' && _curLevels[n] > 0) ? _curLevels[n] : 1;
        Object.keys(_dates).forEach(function(dk){
          if(!_byDate[dk]) _byDate[dk] = [];
          _byDate[dk].push({ name:n, lv:_lv });
        });
      });
      return { byDate:_byDate };
    }catch(_e){ return { byDate:{} }; }
  }

  // ════════════════════════════════════════════════════════════════════
  // ★ v5.91.0（老師 2026-08-20 需求）— UR 券發放 vs UR 角色持有「全體對帳稽核」
  // ────────────────────────────────────────────────────────────────────
  //   背景：老師只發一張 UR 自選券給某位學生，該生卻在三個不同時間各用一次、拿到三隻 UR。
  //   根因（同版已根治）＝扣券沒上雲導致券復活；本工具負責「找出已經受影響的所有帳號」。
  //   ★ UR 的取得管道只有 GM 發放一條（_RARITY_UR_HEROES 註解：只送不抽、鬥技場禁用），
  //     所以「應有 UR 數」可以完全由 GM 發放紀錄推導出來，比對才有意義。
  //   對帳公式：
  //     應有 = （GM 發出的 UR 券張數 − 手上還沒用的 UR 券張數）＋ GM 直接發放的 UR 英雄隻數
  //     實有 = unlockedHeroes 中屬於 UR 名單的隻數
  //     差額 = 實有 − 應有   （>0 ＝ 多出來的；<0 ＝ 券還沒用或發放紀錄缺漏，僅提示不視為異常）
  //   ⚠ 讀取量控制：只做 2 次 collection 全掃（players + gmGiftLog），不逐一讀 ticketLedger
  //     （900 名學生逐讀會直接打爆 Spark 配額）。ticketLedger 留給 GM 對「單一可疑帳號」深查。
  //   ⚠ 純讀取零寫入：本函式只負責找出名單，刪除與補償一律由 GM 在 UI 上逐項確認後
  //     走既有 _fbAdminBulkRemoveHeroes / _fbCompensatePlayer（可逆、有帳本、會通知學生）。
  //   回傳：{ scanned, giftDocs, rows:[...] }，rows 依差額大→小排序。
  // ════════════════════════════════════════════════════════════════════
  // ════════════════════════════════════════════════════════════════════
  // ★★ v5.97.0（老師 2026-08-20：課堂獎勵發放的「自選至寶卷」要與 UR 一起稽查）★★
  //   ── 資產設定表 ＋ 泛化對帳引擎 ──
  // ────────────────────────────────────────────────────────────────────
  // 【範圍】老師收斂：只查 GM 課堂獎勵發的「💠 自選至寶召喚卷」（期末 95 分以上發的那種）。
  //   至寶本身有九條以上取得管道，若全部納入「應有數」根本推導不出來 ⇒
  //   只有 source='treasure_pick_ticket' 佔用券額度，其餘一律「✅ 合法取得」永不計入超額。
  //
  // 【為什麼不直接把 _fbAdminScanURTicketAudit 整支改寫成設定表驅動】
  //   企畫書原案是一次改寫成 _fbAdminScanAssetAudit(opts)。實作前評估後改採**漸進遷移**：
  //     ・UR 那段 per-player 邏輯約 300 行、是 v5.93.0–v5.96.1 連續五輪修出來的成果，
  //       一次搬家的回歸風險（v5.96.0 _psnap 誤刪的教訓）遠大於本輪要解的問題；
  //     ・老師真正要的是「同一次掃描、同一張表、每位學生一列」—— 這點本輪完全做到：
  //       仍然只有 2 次 collection 全掃、只有一支 scan 函式、每位學生一列（列內分區）。
  //   ⇒ 本輪：UR 走原生程式碼（零改動＝零回歸），至寶走下面這支泛化引擎；
  //     下一輪再把 UR 切過來，屆時可用本輪沙箱逐案比對「泛化結果 == 原生結果」再切換。
  //   ⚠ 在切換完成前，對帳語義若要改，**兩邊都要改**（此註記即為防呆）。
  //
  // 【★★ 三個必須寫死在程式碼裡的解析陷阱 ★★】
  //   陷阱①「隨機」與「自選」標籤共用子字串：
  //     隨機版 '💎GM獎勵隨機至寶召喚卷×2' / 自選版 '💠GM獎勵自選至寶召喚卷×1'
  //     用 indexOf('至寶召喚卷') 兩者都命中 ⇒ 分母灌水 ⇒ 學生被判「領太多」。
  //     ⇒ 一律用全詞 '自選至寶召喚卷'（隨機版不含此字串，已實查驗證）。
  //   陷阱②背包有兩個鍵，必須同時計：
  //     summon_ticket_treasure_pick（_useGmTicket 換券後的殘留）
  //     ＋ gm_summon_ticket_treasure_pick（老師直接發的）。只計一個會漏報幽靈券。
  //   陷阱③「持有」與「歸因」的讀法都與 UR 不同：
  //     ・UR 持有 = unlockedHeroes（陣列）；至寶持有 = taiwanTreasureData（map，key=tid）
  //       ⚠ 讀法採「map 優先、map 空才 fallback _s」—— 這是遊戲載入端的實際口徑
  //         （_advSaveTreasureUnlockHistory v3.15.21 註解），與背包 _s 優先**相反**，不可照抄。
  //     ・帳本每筆的鍵是 `id` 不是 `name`。
  //     ・★ 券換至寶會產生**兩筆**紀錄：_grantTaiwanTreasure 內部先寫 'taiwan_clear'，
  //       _useTreasureTicketPick 再補寫 'treasure_pick_ticket'（v3.16.92 的無條件權威寫入）。
  //       兩筆 at 可能落在同一毫秒 ⇒ 若照 UR 那樣「只看最後一筆」，排序不穩時會判成
  //       首通合法 ⇒ 券換取的至寶漏抓（超額低報）。
  //       ⇒ 至寶歸因改看「該件的所有紀錄集合」：含 pick 就是券換的，不管它排第幾。
  // ════════════════════════════════════════════════════════════════════
  window._LXPS_ASSET_KINDS = [
    { key:'ur', label:'👑 UR 角色', native:true },      // 目前仍走 _fbAdminScanURTicketAudit 原生段落
    {
      key:        'treasure',
      label:      '💠 自選至寶',
      ownKind:    'map',                                 // 持有清單是 map（key=tid）
      ownField:   'taiwanTreasureData',
      ledger:     '_treasureUnlockHistory',
      idField:    'id',                                  // 帳本每筆的識別鍵（UR 是 name）
      giftTag:    '自選至寶召喚卷',                       // ★ 陷阱① 全詞比對
      bagKeys:    ['summon_ticket_treasure_pick', 'gm_summon_ticket_treasure_pick'],
      ticketSrc:  { 'treasure_pick_ticket': 1 },
      // ★ 移除類：企畫書原寫「至寶沒有 admin_delete」，實查有 —— 汙染清除
      //   （_fbAdminApplyPollutionPurge）與 GM 單件刪除都會寫 admin_delete。
      removedSrc: { 'audit_error_recovered': 1, 'admin_delete': 1 },
      // GM 給予類：補償面板 _fbCompensatePlayer({treasures}) 寫 admin_grant、
      //   汙染復原寫 admin_restore、GM 一鍵全解鎖寫 gm_auto_unlock
      gmSrc:      { 'admin_grant': 1, 'admin_restore': 1, 'gm_auto_unlock': 1 },
      // 合法白名單（★ 逐條從程式碼實查，不是推測）：首通／龍王排名／隨機券／學生自我確認
      //   ⚠ 'china_clear' 只出現在註解裡：中國關實際走 _grantTaiwanTreasure → 寫 'taiwan_clear'。
      //     仍列入白名單，日後若真的改寫成 china_clear 也不會冤枉學生。
      legitSrc:   { 'taiwan_clear':1, 'japan_clear':1, 'china_clear':1,
                    'treasure_random_ticket':1, 'wb_rank_reward':1, 'wb_rank_reward_dragon':1,
                    'wb_rank_compensate':1, 'wb_rank_compensate_exact':1, 'wb_rank_compensate_dragon':1,
                    'player_confirmed':1 },
      extraKey:   'summon_ticket_treasure_pick',         // _ticketExtraGrants／_ticketScrollUses 的 item 值
      quotaField: '_treQuotaAdjust',
      compField:  '_treAuditCompensated'
    }
  ];

  // ── 泛化 per-player 對帳（本輪只供至寶使用；回傳結構與 UR 的欄位語義刻意一致）──
  //   ⚠ 純計算、零寫入、零額外讀取：所有欄位都在同一份 player 主檔內
  //     ⇒ 900 人的稽核成本與只查 UR 時完全相同。
  window._lxpsAssetAuditOne = function(d, uid, gift, K){
    var _r = { flags: [] };
    try{
      d = d || {}; gift = gift || { ticketN:0, recs:[] };
      var _MISDELETE_MS = 10 * 60 * 1000;

      // ── (a) 持有清單（map 優先、map 空才 _s；見陷阱③）──
      var _own = {};
      try{
        if(d[K.ownField] && typeof d[K.ownField] === 'object' && Object.keys(d[K.ownField]).length){
          _own = d[K.ownField];
        } else if(typeof d[K.ownField + '_s'] === 'string'){
          var _p = JSON.parse(d[K.ownField + '_s']);
          if(_p && typeof _p === 'object') _own = _p;
        } else if(d[K.ownField] && typeof d[K.ownField] === 'object'){
          _own = d[K.ownField];
        }
      }catch(_){ _own = (d[K.ownField] && typeof d[K.ownField] === 'object') ? d[K.ownField] : {}; }

      // ── (b) 帳本（陣列版優先，缺才讀 _s）──
      var _histAll = [];
      try{
        var _raw = Array.isArray(d[K.ledger]) ? d[K.ledger] : null;
        if(!_raw && typeof d[K.ledger + '_s'] === 'string'){
          try{ var _pp = JSON.parse(d[K.ledger + '_s']); if(Array.isArray(_pp)) _raw = _pp; }catch(_){}
        }
        (_raw || []).forEach(function(h){
          if(!h) return;
          var _id = String(h[K.idField] || '');
          if(!_id) return;
          _histAll.push({
            id: _id,
            at: (typeof h.at === 'number') ? h.at : 0,
            source: String(h.source || ''),
            reason: String(h.reason || ''),
            by: String(h.deletedBy || h.by || ''),
            uid12: String(h.uid || '')
          });
        });
        _histAll.sort(function(a, b){ return (a.at || 0) - (b.at || 0); });
      }catch(_){}

      // ★ 歸因用：每件的「所有 source 集合」＋最後一筆（陷阱③：不能只看最後一筆）
      var _srcSetOf = {}, _lastRecOf = {}, _lastLiveOf = {};
      _histAll.forEach(function(x){
        if(!_srcSetOf[x.id]) _srcSetOf[x.id] = {};
        _srcSetOf[x.id][x.source] = 1;
        _lastRecOf[x.id] = x;
        if(!K.removedSrc[x.source]) _lastLiveOf[x.id] = x;
      });
      // 用券總次數（含後來被刪掉的；券用掉就是用掉了，不因刪除而還原）
      var _usedLedger = 0;
      _histAll.forEach(function(x){ if(K.ticketSrc[x.source]) _usedLedger++; });

      // ★ v5.97.0 甲5 — 「全收齊改贈經驗卷軸」也消耗了券，但沒有解鎖紀錄可寫，
      //   額外帳本 _ticketScrollUses 補上，否則已用少算 → 誤判「券憑空消失」。
      var _usedScroll = 0;
      try{
        (Array.isArray(d._ticketScrollUses) ? d._ticketScrollUses : []).forEach(function(e){
          if(e && String(e.item || '') === K.extraKey) _usedScroll++;
        });
      }catch(_){}
      var _usedReal = _usedLedger + _usedScroll;

      // ── (c) 實際持有（帳本最後一筆是移除類 → 即使 map 殘留也不算持有）──
      var _owned = [];
      Object.keys(_own).forEach(function(id){
        if(!id) return;
        var _rec = _lastRecOf[id];
        if(_rec && K.removedSrc[_rec.source]) return;
        _owned.push(id);
      });
      var _actual = _owned.length;

      // ── (d) 券額度：學生「實際領到」幾張（三個 claimLog，皆在主檔內＝零額外讀取）──
      var _claimedTk = 0, _claimLogSeen = false, _claimSeenRid = {}, _recoverSkipped = 0;
      var _dupSkipped = 0;   // ★ v5.124.0 ⑤ — 被備援鍵去重掉的重複領取紀錄筆數（UI 明示，不靜默）
      // ★★ v5.124.0【甲案・誤收防呆】對不上權威發放文件的領取筆數 ★★
      //   全體獎勵／序號的發放數改由權威文件（globalRewardClaims／redeemCodes）認列後，
      //   額度夾制式才會對這兩條路生效。但只要有一筆權威文件讀不到或已被刪掉，
      //   granted 就會偏低 ⇒ 夾制把 quota 壓低 ⇒ 合法資產被判超額 ⇒ 老師收回 ⇒ 事後要回溯。
      //   ⇒ 只要這位學生有任何一筆「領了券、卻找不到對應發放文件」的紀錄，本列一律不夾制。
      var _coverGap = 0;
      var _giftCover = (gift && gift.cover && typeof gift.cover === 'object') ? gift.cover : {};
      try{
        var _scanClaim = function(arr, itemKey, srcTag){
          if(!Array.isArray(arr) || !arr.length) return;
          _claimLogSeen = true;
          arr.forEach(function(e){
            if(!e) return;
            if(e._recover){ _recoverSkipped++; return; }      // 系統補發＝同一張券的下游動作，不是新券
            var _rid = String(e.rewardId || '');
            // ★★ v5.124.0 ⑤【防禦性補丁】rewardId 缺失時舊版完全不去重 ★★
            //   實查三份 claimLog 的欄位形狀：
            //     _gmcrClaimLog → { rewardId, label, merit, items, at }      有 rewardId
            //     _grClaimLog   → { rewardId, reward, items, at }            有 rewardId
            //     _redeemClaimLog → { code, reward, itemsLabel, at }         ★ 只有 code，沒有 rewardId
            //   ⇒ 序號兌換那一份從來沒進過去重集合，同一筆若被重複寫入（多裝置／重試／
            //     三槽合併）就會被重複計 ⇒ 分母虛高 ⇒ 真的多拿也判不出來 ⇒ 漏收。
            //   修法：序號用 code 當鍵（它本來就是全域唯一的 12 碼序號，比指紋精確）；
            //         兩者皆無才退回「時間戳＋標題」指紋，且 at 必須 > 0 才去重
            //         ——at 缺失時刻意不去重，寧可多算也不冤枉學生（誤收代價 > 漏收）。
            if(!_rid && e.code) _rid = 'code:' + String(e.code);
            if(!_rid && typeof e.at === 'number' && e.at > 0) _rid = 'at:' + e.at + '|' + String(e.label || '');
            if(_rid){ if(_claimSeenRid[_rid]){ _dupSkipped++; return; } _claimSeenRid[_rid] = 1; }
            // ★ v5.124.0 — 改為「先算出這一筆領到幾張」再累加，這樣才有辦法逐筆比對權威文件
            var _n = 0;
            var _rb = (e.reward && typeof e.reward === 'object' && e.reward.backpack
                       && typeof e.reward.backpack === 'object') ? e.reward.backpack : null;
            if(_rb){
              for(var _bi = 0; _bi < K.bagKeys.length; _bi++){
                _n += (parseInt(_rb[K.bagKeys[_bi]], 10) || 0);
              }
            } else {
              var _its = Array.isArray(e[itemKey]) ? e[itemKey] : [];
              _its.forEach(function(it){
                var s = String(it || '');
                if(s.indexOf(K.giftTag) < 0) return;             // ★ 陷阱① 全詞比對
                var m = s.match(/×\s*(\d+)/);
                _n += m ? (parseInt(m[1], 10) || 1) : 1;
              });
            }
            _claimedTk += _n;
            // ★ 誤收防呆：這一筆真的領到券，且來源是「權威文件應該查得到」的兩條路，
            //   卻在 gift.cover 裡找不到對應 ⇒ 發放文件缺漏，本列不可套用夾制。
            //   ⚠ 課堂獎勵（gmcr）不檢查：它走 gmGiftLog，本來就有紀錄，不在本次新增範圍。
            if(_n > 0 && (srcTag === 'gr' || srcTag === 'redeem')){
              var _ck = (srcTag === 'gr') ? String(e.rewardId || '') : ('code:' + String(e.code || ''));
              if(!_ck || !_giftCover[_ck]) _coverGap++;
            }
          });
        };
        _scanClaim(d._gmcrClaimLog,   'items',      'gmcr');
        _scanClaim(d._grClaimLog,     'items',      'gr');
        _scanClaim(d._redeemClaimLog, 'itemsLabel', 'redeem');
      }catch(_){}
      var _quotaFromLog = _claimLogSeen && _claimedTk > 0;
      var _claimedRaw   = _claimedTk;
      var _quotaAdj     = Math.max(0, parseInt(d[K.quotaField], 10) || 0);
      // ★★ v5.97.0（老師 2026-08-20 指示：老師發的 vs 學生鬥技場換的要分開查）★★
      //   自選至寶券有兩條合法取得管道，兩者的帳必須分開，否則老師看不出「這張是不是我發的」：
      //     ① 老師發放（課堂獎勵／全體獎勵／序號）→ gmGiftLog 有紀錄＝**唯一權威分母**
      //     ② 學生鬥技場兌換（40 鬥技之證換 1 張）→ v5.97.0 起寫 _ticketExtraGrants（甲4）
      //   ⚠ 兩者在背包裡最後會混成同一鍵（gm_ 券經 _useGmTicket 換發會變成 base 券），
      //     所以**不能靠背包鍵分辨**，只能靠這兩份各自獨立的帳本。
      //   ⚠ v5.97.0 之前的鬥技場兌換只寫在學生自己裝置的 localStorage，雲端查不到 ⇒
      //     舊帳的 ② 一律為 0 ⇒ 額度會偏低 ⇒ 故至寶的超額／幽靈券一律只標示不回收。
      var _extraTk = 0, _extraRecs = [];
      try{
        (Array.isArray(d._ticketExtraGrants) ? d._ticketExtraGrants : []).forEach(function(e){
          if(!e || String(e.item || '') !== K.extraKey) return;
          _extraTk++;
          _extraRecs.push({ at: (typeof e.at === 'number') ? e.at : 0, via: String(e.via || '') });
        });
        _extraRecs.sort(function(a, b){ return (a.at || 0) - (b.at || 0); });
      }catch(_){}
      var _quotaCapped = 0, _gmQuota;
      if(_quotaFromLog){
        _gmQuota = _claimedTk;
        // ★ v5.124.0 — 夾制多一道守門 `_coverGap === 0`：只要有任何一筆領取紀錄
        //   找不到對應的權威發放文件（全體獎勵／序號），就代表分母可能不完整，
        //   一律不夾（寧可漏收，也絕不誤收 —— 誤收要回溯，代價遠大於漏收）。
        if(gift.ticketN > 0 && _coverGap === 0 && _gmQuota > gift.ticketN){
          _quotaCapped = _gmQuota - gift.ticketN;      // 領取紀錄比老師發放紀錄多 → 不採信多的部分
          _gmQuota = gift.ticketN;
        }
      } else {
        _gmQuota = gift.ticketN;                        // 查無領取紀錄 → 保守退回發出數（寧寬勿冤）
      }
      // 總額度 = 老師發的 ＋ 鬥技場換的 ＋ 稽核補發（★ 三者各自可查證，夾制只作用在「老師發的」那一項）
      var _ticketQuota = _gmQuota + _extraTk + _quotaAdj;
      var _pendingTk   = Math.max(0, gift.ticketN - _gmQuota);

      // 型態 B1：老師發了但學生還沒在收件箱按「確認領取」（完全正常，不算異常）
      var _claimedIdSet = {}, _unclaimedTk = 0;
      try{
        (Array.isArray(d._gmcrClaimed) ? d._gmcrClaimed : []).forEach(function(x){ _claimedIdSet[String(x)] = 1; });
        (gift.recs || []).forEach(function(g){
          if(!g || !g.ticketN) return;
          if(g.rewardId && !_claimedIdSet[g.rewardId]) _unclaimedTk += g.ticketN;
        });
      }catch(_){}

      // ── (e) 逐件歸因（★ 看 source 集合，不是最後一筆）──
      var _originOf = {}, _gmOwned = [], _ticketOwned = [], _legitOwned = [], _unknownOwned = [];
      _owned.forEach(function(id){
        var _set = _srcSetOf[id] || {};
        var _o = 'unknown', _k;
        for(_k in _set){
          if(!Object.prototype.hasOwnProperty.call(_set, _k)) continue;
          if(K.ticketSrc[_k]){ _o = 'ticket'; break; }         // 券換取＝佔用額度（優先級最高）
          if(K.gmSrc[_k])    { _o = 'gm';    continue; }
          if(K.legitSrc[_k] && _o !== 'gm') _o = 'legit';
        }
        _originOf[id] = _o;
        if(_o === 'gm') _gmOwned.push(id);
        else if(_o === 'ticket') _ticketOwned.push(id);
        else if(_o === 'legit') _legitOwned.push(id);
        else _unknownOwned.push(id);
      });
      _ticketOwned.sort(function(a, b){
        return ((_lastLiveOf[a] && _lastLiveOf[a].at) || 0) - ((_lastLiveOf[b] && _lastLiveOf[b].at) || 0);
      });

      // ════════════════════════════════════════════════════════════════
      // ★★ v5.97.0（老師指示：老師發的 vs 學生鬥技場換的要分開）— 逐件歸屬 ★★
      //   券池（依取得時間升冪，FIFO 配對）：
      //     gm     ＝ 老師發的（gmGiftLog 的發放時間；舊帳無紀錄者 at=0 ＝永遠可用）
      //     arena  ＝ 學生鬥技場兌換（_ticketExtraGrants 的兌換時間，v5.97.0 起才有）
      //     adjust ＝ 稽核補發（_treQuotaAdjust；補發本來就不在任何領取紀錄裡，at=0）
      //   每一件「券換取的至寶」配一張取得時間早於它的、最早的未用券。
      //   ⚠⚠ 只用來**顯示這一件的券是哪來的**，絕不拿來判超額：
      //     v3.17.6 前的 gmGiftLog 沒有 at、v5.97.0 前的鬥技場兌換整批查不到，
      //     時間配對會偏嚴 ⇒ 拿來判定就會冤枉學生。超額一律走下面保守的數量法。
      // ════════════════════════════════════════════════════════════════
      var _tkPool = [], _tkFromOf = {}, _fromGmN = 0, _fromArenaN = 0, _fromNoneN = 0;
      try{
        var _gmLeft = _gmQuota;
        var _recsSorted = (gift.recs || []).slice().sort(function(a, b){ return (a.at || 0) - (b.at || 0); });
        _recsSorted.forEach(function(g){
          if(!g || !g.ticketN || _gmLeft <= 0) return;
          var _nn = Math.min(g.ticketN, _gmLeft);
          for(var _i2 = 0; _i2 < _nn; _i2++) _tkPool.push({ from:'gm', at:(g.at || 0) });
          _gmLeft -= _nn;
        });
        for(var _i3 = 0; _i3 < _gmLeft; _i3++) _tkPool.push({ from:'gm', at:0 });
        _extraRecs.forEach(function(e){ _tkPool.push({ from:'arena', at:(e.at || 0) }); });
        for(var _i4 = 0; _i4 < _quotaAdj; _i4++) _tkPool.push({ from:'adjust', at:0 });
        _tkPool.sort(function(a, b){ return (a.at || 0) - (b.at || 0); });
        var _poolUsed = {};
        _ticketOwned.forEach(function(id){
          var _atI = (_lastLiveOf[id] && _lastLiveOf[id].at) || 0;
          var _pick = -1;
          for(var _i5 = 0; _i5 < _tkPool.length; _i5++){
            if(_poolUsed[_i5]) continue;
            if((_tkPool[_i5].at || 0) > _atI) continue;
            _pick = _i5; break;
          }
          if(_pick >= 0){ _poolUsed[_pick] = 1; _tkFromOf[id] = _tkPool[_pick].from; }
          else _tkFromOf[id] = 'none';
          if(_tkFromOf[id] === 'gm') _fromGmN++;
          else if(_tkFromOf[id] === 'arena') _fromArenaN++;
          else if(_tkFromOf[id] === 'none') _fromNoneN++;
        });
      }catch(_){}

      var _overTicket = Math.max(0, _ticketOwned.length - _ticketQuota);
      var _surplus    = _overTicket + _unknownOwned.length;

      // ── (f) 背包剩餘券 ＋ 幽靈券（★ 陷阱② 兩鍵都要計；_s 字串版優先＝v3.15.57 洗錢口徑）──
      var _bag = {};
      try{
        if(typeof d.playerBackpack_s === 'string'){ _bag = JSON.parse(d.playerBackpack_s) || {}; }
        else if(d.playerBackpack && typeof d.playerBackpack === 'object'){ _bag = d.playerBackpack; }
      }catch(_){ _bag = (d.playerBackpack && typeof d.playerBackpack === 'object') ? d.playerBackpack : {}; }
      var _leftBase   = parseInt(_bag[K.bagKeys[0]], 10) || 0;
      var _leftGm     = parseInt(_bag[K.bagKeys[1]], 10) || 0;
      var _left       = _leftBase + _leftGm;
      var _shouldLeft = Math.max(0, _ticketQuota - _usedReal);
      var _ghost      = Math.max(0, _left - _shouldLeft);

      // ── (g) 短少（型態 B2／C／C2）──
      var _ownedSet = {};
      _owned.forEach(function(id){ _ownedSet[id] = 1; });
      var _lostItems = [], _lostTkItems = [], _compensatedLost = [];
      try{
        var _everMap = {};   // id -> 'gm' | 'ticket' | 'legit'
        _histAll.forEach(function(x){
          if(K.removedSrc[x.source]) return;
          if(K.ticketSrc[x.source]){ _everMap[x.id] = 'ticket'; return; }   // 券換取優先（補的是券）
          if(_everMap[x.id] === 'ticket') return;
          if(K.gmSrc[x.source]){ _everMap[x.id] = 'gm'; return; }
          if(K.legitSrc[x.source] && _everMap[x.id] !== 'gm') _everMap[x.id] = 'legit';
        });
        var _compSet = {};
        try{
          (Array.isArray(d[K.compField]) ? d[K.compField] : []).forEach(function(x){ _compSet[String(x)] = 1; });
        }catch(_){}
        Object.keys(_everMap).forEach(function(id){
          if(_ownedSet[id]) return;                                  // 還在他身上 → 沒事
          if(_compSet[id]){ _compensatedLost.push(id); return; }     // 已補償過 → 不再列（冪等）
          var _org = _everMap[id];
          var _rec = _lastRecOf[id];
          var _item;
          if(!_rec){
            _item = { id:id, origin:_org, at:0, grantAt:0, aliveMs:-1,
                      source:'(帳本查無入帳紀錄)', reason:'', by:'', misdelete:0, vanish:0, noRec:1 };
          } else if(!K.removedSrc[_rec.source]){
            // 帳本有入帳、最後一筆不是移除類、東西卻不在持有清單 ＝ 存檔遺失／雲端合併吃掉
            _item = { id:id, origin:_org, at:_rec.at, grantAt:_rec.at, aliveMs:-1,
                      source:_rec.source, reason:'', by:'', misdelete:0, vanish:1, noRec:0 };
          } else {
            var _grantAt = 0;
            for(var _q = _histAll.length - 1; _q >= 0; _q--){
              var _e = _histAll[_q];
              if(_e.id !== id) continue;
              if(_e.at >= _rec.at) continue;
              if(!K.removedSrc[_e.source]){ _grantAt = _e.at; break; }
            }
            var _alive = (_grantAt && _rec.at) ? (_rec.at - _grantAt) : -1;
            _item = { id:id, origin:_org, at:_rec.at, grantAt:_grantAt, aliveMs:_alive,
                      source:_rec.source, reason:_rec.reason, by:_rec.by,
                      misdelete: (_alive >= 0 && _alive <= _MISDELETE_MS) ? 1 : 0,
                      vanish:0, noRec:0 };
          }
          if(_org === 'ticket') _lostTkItems.push(_item); else _lostItems.push(_item);
        });
        var _sortLost = function(a, b){ return (b.at || 0) - (a.at || 0); };
        _lostItems.sort(_sortLost);
        _lostTkItems.sort(_sortLost);
      }catch(_){}
      var _lostTickets = Math.max(0, _ticketQuota - _unclaimedTk - _usedReal - _left);
      // ★★ v5.97.0（沙箱案⑰抓到）— 還在收件箱沒領的券，不可再算成「券憑空消失」★★
      //   查無領取紀錄時額度會保守退回「老師發出數」，而發出但學生還沒按「確認領取」的券，
      //   背包當然是 0 ⇒ 舊算式 quota − used − left 會讓同一張券同時掛上
      //   「⏳ 還沒領取 N 張（正常）」與「💨 券憑空消失 N 張」兩個互相矛盾的旗標
      //   ⇒ 老師照著補一張，等學生真的去領時就變成多一張。故先扣掉未領的部分。
      //   ⚠ 額度若來自領取紀錄，未領的券本來就不在額度內，扣它只會讓本項更保守（寧漏報不誤補）。
      //   ⚠ UR 原生段落目前仍是舊算式（同樣的矛盾），本輪刻意不動 UR 以免回歸，已列入下輪待辦。
      var _shortage    = _lostItems.length + _lostTkItems.length + _lostTickets;

      // ── (h) 取得紀錄（逐筆標類別，供 UI 上色）──
      var _tkSeq = 0;
      var _hist = _histAll.map(function(x){
        var _kind = K.removedSrc[x.source] ? 'removed'
                  : (K.ticketSrc[x.source] ? 'ticket'
                  : (K.gmSrc[x.source] ? 'gm'
                  : (K.legitSrc[x.source] ? 'legit' : 'unknown')));
        var _seq = 0, _over = false;
        if(_kind === 'ticket'){ _tkSeq++; _seq = _tkSeq; _over = (_tkSeq > _ticketQuota); }
        return { id:x.id, at:x.at, source:x.source, uid12:x.uid12,
                 kind:_kind, tkSeq:_seq, overQuota:_over };
      });

      // ── (i) 持有明細 ──
      var _detail = _owned.map(function(id){
        var _rec2 = (typeof _own[id] === 'object' && _own[id]) ? _own[id] : {};
        var _live = _lastLiveOf[id] || null;
        var _org  = _originOf[id] || 'unknown';
        var _tkIdx = (_org === 'ticket') ? (_ticketOwned.indexOf(id) + 1) : 0;
        return {
          id: id,
          name: (window.TAIWAN_TREASURES && window.TAIWAN_TREASURES[id] && window.TAIWAN_TREASURES[id].name) || id,
          icon: (window.TAIWAN_TREASURES && window.TAIWAN_TREASURES[id] && window.TAIWAN_TREASURES[id].icon) || '💠',
          lv: parseInt(_rec2.lv, 10) || 1,
          exp: parseInt(_rec2.exp, 10) || 0,
          equippedTo: String(_rec2.equippedTo || ''),
          at: _live ? _live.at : 0,
          source: _live ? _live.source : '(查無取得紀錄)',
          origin: _org,
          tkFrom: (_org === 'ticket') ? (_tkFromOf[id] || '') : '',   // ★ v5.97.0 gm / arena / adjust / none
          tkIndex: _tkIdx,
          tkQuota: _ticketQuota,
          overQuota: (_org === 'ticket' && _tkIdx > _ticketQuota) ? 1 : 0,
          suspect: (_org === 'unknown' || (_org === 'ticket' && _tkIdx > _ticketQuota)) ? 1 : 0
        };
      });
      _detail.sort(function(a, b){ return (b.suspect - a.suspect) || ((a.at || 0) - (b.at || 0)); });

      // ── (j) 型態旗標（語義與 UR 完全對應）──
      var _flags = [];
      if(_surplus > 0)             _flags.push('A_surplus');
      if(_ghost > 0)               _flags.push('A_ghost');
      if(_unclaimedTk > 0)         _flags.push('B1_unclaimed');
      if(_lostTickets > 0)         _flags.push('B2_lostTicket');
      if(_lostItems.length > 0)    _flags.push('C_lostHero');
      if(_lostTkItems.length > 0)  _flags.push('C2_lostTkHero');
      if(_lostItems.concat(_lostTkItems).some(function(x){ return x.misdelete; })) _flags.push('C_misdelete');
      if(_unknownOwned.length > 0) _flags.push('A_unknown');

      _r = {
        kind: K.key, label: K.label,
        granted: gift.ticketN, ticketQuota: _ticketQuota,
        // ★★ v5.97.0 — 額度三來源分開回傳（老師指示：老師發的 vs 鬥技場換的要分得開）★★
        gmQuota: _gmQuota,                       // 老師發的（gmGiftLog 權威 ＋ 領取紀錄夾制）
        extraTk: _extraTk,                       // 學生鬥技場兌換（v5.97.0 起可查證）
        extraRecs: _extraRecs.slice(0, 30),      // 兌換時間明細（供 UI 展開查看）
        fromGmN: _fromGmN,                       // 逐件歸屬：用老師的券換到 N 件
        fromArenaN: _fromArenaN,                 // 逐件歸屬：用鬥技場的券換到 N 件
        fromNoneN: _fromNoneN,                   // 逐件歸屬：配不到任何券（★ 僅顯示，不等於超額）
        quotaFromLog: _quotaFromLog ? 1 : 0,
        claimedRaw: _claimedRaw, quotaCapped: _quotaCapped, quotaAdj: _quotaAdj,
        extraTk: _extraTk,                       // ★ 鬥技場等非 GM 管道取得的券（已計入額度）
        recoverSkipped: _recoverSkipped,
        claimDupSkipped: _dupSkipped,            // ★ v5.124.0 ⑤ 被備援鍵去重掉的重複領取紀錄筆數
        coverGap: _coverGap,                     // ★ v5.124.0 甲案 查無對應發放文件的領取筆數（>0＝本列不夾制）
        pendingTk: _pendingTk, unclaimedTk: _unclaimedTk,
        used: _usedReal, usedLedger: _usedLedger, usedScroll: _usedScroll,
        left: _left, leftBase: _leftBase, leftGm: _leftGm,
        shouldLeft: _shouldLeft, ghost: _ghost,
        owned: _owned, actual: _actual, detail: _detail, history: _hist,
        gmOwned: _gmOwned, ticketOwned: _ticketOwned,
        legitOwned: _legitOwned, unknownOwned: _unknownOwned,
        overTicket: _overTicket, surplus: _surplus,
        lostItems: _lostItems, lostTkItems: _lostTkItems,
        compensatedLost: _compensatedLost,
        lostTickets: _lostTickets, shortage: _shortage,
        flags: _flags,
        // ★ 有沒有東西可講（完全乾淨且從未接觸過此資產 → 該列不必為它多長一段）
        relevant: !!(_actual || gift.ticketN || _left || _flags.length || _hist.length)
      };
    }catch(_eA){
      try{ console.warn('[_lxpsAssetAuditOne] uid=' + String(uid || '').slice(0, 8) + ' 例外', _eA); }catch(_){}
      _r = { kind: (K && K.key) || '?', label: (K && K.label) || '', flags: [], relevant: false, error: 1 };
    }
    return _r;
  };

  // ════════════════════════════════════════════════════════════════════
  // ★★ v5.124.0【單一真相解析器】／ v5.128.0 搬到模組頂層 ★★
  // ────────────────────────────────────────────────────────────────────
  //   稽核認列三個「GM 端權威發放來源」：
  //     📜 課堂獎勵 gmGiftLog ／ 🎟️ 虛寶序號 redeemCodes ／ 🎉 全體獎勵 globalRewards+Claims
  //   ⇒ 三者必須用**同一支**解析器，否則日後改標籤必有一邊漏，
  //     而漏一邊＝那批券的分母憑空消失＝老師照表收回學生的合法資產。
  //
  //   解析優先序刻意與領取端 `_scanClaim` 完全一致：
  //     ① 結構化 reward.backpack（序號／全體獎勵文件都有，最精準、不受文案改動影響）
  //     ② 退回文字標籤（gmGiftLog 的 items 只有文字）
  //   ⚠ 全詞比對陷阱（v5.97.0）：至寶一定要用全詞「自選至寶召喚卷」，
  //     寫成「至寶召喚卷」會把「隨機至寶召喚卷」也吃進來 ⇒ 分母灌水 ⇒ 冤枉學生。
  //
  //   ★★ v5.128.0 修正（老師實測回報）★★
  //   v5.127.0 把這支寫在 `_fbAdminScanURTicketAudit` **函式內部**，
  //   要等全體稽核**真的跑過一次**才會掛上 window ⇒ 老師直接開「🔎 單人獎勵查核」
  //   會收到「解析器未載入」。單人查一個人不該先被迫做 900 人全掃。
  //   ⇒ 改成模組載入時就定義好；`_urNames` 由解析器自己從 window._LXPS_RARITY_UR 取
  //     （不再依賴呼叫端的區域變數）。
  // ════════════════════════════════════════════════════════════════════
  window._LXPS_UR_BAG_KEYS  = ['summon_ticket_ur_pick', 'gm_summon_ticket_ur_pick'];
  window._LXPS_TRE_BAG_KEYS = ['summon_ticket_treasure_pick', 'gm_summon_ticket_treasure_pick'];

  window._lxpsParseGrantItems = function(items, urNames){
    var _o = { ticketN:0, treTicketN:0, heroes:[] };
    var _urn = [];
    try{
      if(Array.isArray(urNames)) _urn = urNames;
      else if(Array.isArray(window._LXPS_RARITY_UR)) _urn = window._LXPS_RARITY_UR;
    }catch(_){}
    (Array.isArray(items) ? items : []).forEach(function(it){
      var s = String(it || '');
      // UR 自選券：抓「UR自選召喚卷×N」的 N（無 ×N 視為 1）
      if(s.indexOf('UR自選召喚卷') >= 0){
        var m = s.match(/×\s*(\d+)/);
        _o.ticketN += m ? (parseInt(m[1], 10) || 1) : 1;
      }
      // 自選至寶券（★ 全詞比對，見上方陷阱說明）
      if(s.indexOf('自選至寶召喚卷') >= 0){
        var mT = s.match(/×\s*(\d+)/);
        _o.treTicketN += mT ? (parseInt(mT[1], 10) || 1) : 1;
      }
      // 直接發放的 UR 英雄：逐隻比對（標籤把「‧／．」去掉了，故兩邊都去標點再比）
      _urn.forEach(function(hn){
        var _plain = String(hn).replace(/[‧．·・\s]/g, '');
        if(s.indexOf(hn) >= 0 || s.indexOf(_plain) >= 0){ _o.heroes.push(hn); }
      });
    });
    return _o;
  };

  // 結構化 reward（序號／全體獎勵文件）優先；沒有背包欄位才退回文字標籤
  window._lxpsParseGrantDoc = function(reward, items, urNames){
    var _bp = (reward && typeof reward === 'object' && reward.backpack
               && typeof reward.backpack === 'object') ? reward.backpack : null;
    var _txt = window._lxpsParseGrantItems(items, urNames);
    if(!_bp) return _txt;
    var _UK = window._LXPS_UR_BAG_KEYS, _TK = window._LXPS_TRE_BAG_KEYS;
    var _o = { ticketN:0, treTicketN:0, heroes:_txt.heroes };   // 英雄只有文字標籤查得到
    for(var i = 0; i < _UK.length; i++) _o.ticketN    += (parseInt(_bp[_UK[i]], 10) || 0);
    for(var j = 0; j < _TK.length; j++) _o.treTicketN += (parseInt(_bp[_TK[j]], 10) || 0);
    // ★ 結構化欄位為 0 但文字標籤有寫 → 採信文字（防舊文件只寫 items 沒寫 backpack）
    if(!_o.ticketN)    _o.ticketN    = _txt.ticketN;
    if(!_o.treTicketN) _o.treTicketN = _txt.treTicketN;
    return _o;
  };

  // ════════════════════════════════════════════════════════════════════
  // ★★ v5.127.0【老師需求 2026-09-01】🔎 單人期末獎勵查核 ★★
  // ────────────────────────────────────────────────────────────────────
  //   老師輸入姓名或班級座號 → 查出該生拿過哪些券（名目／張數／時間／來源）、
  //   每一張券的下落（用了沒、換到什麼、什麼時候），以及「查無對應券的持有」。
  //
  //   ★ 與全體稽核共用同一支解析器（window._lxpsParseGrantItems / _lxpsParseGrantDoc），
  //     口徑保證一致；絕不自建第二份名單（鐵律 1.98 精神）。
  //   ★ 讀取量：單人查詢刻意用「等值查詢 + 逐筆 getDoc」，全程不做 collection 全掃：
  //       gmGiftLog       where uid == 該生            （單一等值，免複合索引）
  //       redeemCodes     where redeemedBy == 該生      （單一等值，免複合索引）
  //       globalRewards   全讀（很小）→ 只對「有發券」的逐筆 getDoc claim 文件
  //       ticketLedger/{uid}、players/{uid}            （各 1 次 getDoc）
  //     ⇒ 900 人的班級也不會打爆配額，可以放心一個一個查。
  //   ⚠ 本函式**完全唯讀**，不寫任何資料（收回動作另走 v5.92.0 的 urSurplusReview）。
  // ════════════════════════════════════════════════════════════════════
  window._fbAdminAuditOnePlayerRewards = async function(uid){
    if(!uid) throw new Error('缺少 uid');
    if(typeof _fbDb === 'undefined' || !_fbDb) throw new Error('_fbDb 未載入');
    // ★ v5.128.0 — 解析器已在模組載入時就定義好，不再需要「先跑一次全體稽核」
    var _urNames = [];
    try{ if(Array.isArray(window._LXPS_RARITY_UR)) _urNames = window._LXPS_RARITY_UR.slice(); }catch(_){}
    if(!_urNames.length) throw new Error('讀不到 UR 名單(window._LXPS_RARITY_UR)，請重新整理頁面');
    var _warn = [];

    // ── ① 主檔 ──
    var _snap = await getDoc(doc(_fbDb, 'players', uid));
    if(!(typeof _snap.exists === 'function' ? _snap.exists() : _snap.exists)) throw new Error('查無此玩家主檔');
    var _d = _snap.data() || {};
    // 持有 UR：陣列版優先，退回字串版（同 v5.124.0 補丁③）
    var _unlocked = Array.isArray(_d.unlockedHeroes) ? _d.unlockedHeroes : [];
    if(!_unlocked.length && typeof _d.unlockedHeroes_s === 'string'){
      try{ var _up = JSON.parse(_d.unlockedHeroes_s); if(Array.isArray(_up)) _unlocked = _up; }catch(_){}
    }
    var _urSet = {}; _urNames.forEach(function(n){ _urSet[n] = 1; });
    var _ownedUR = _unlocked.filter(function(n){ return _urSet[n]; });

    // ── ② 發放紀錄（三個來源，與全體稽核同口徑）──
    var _grants = [];
    var _push = function(src, label, at, p, extra){
      if(!p) return;
      if(!p.ticketN && !p.treTicketN && !(p.heroes || []).length) return;
      _grants.push({ src:src, label:String(label || ''), at:(typeof at === 'number' ? at : 0),
                     urTk:(p.ticketN || 0), treTk:(p.treTicketN || 0),
                     heroes:(p.heroes || []).slice(), extra:String(extra || '') });
    };
    // (a) 課堂獎勵 gmGiftLog
    try{
      var _gs = await getDocs(query(collection(_fbDb, 'gmGiftLog'), where('uid', '==', uid)));
      _gs.forEach(function(g){
        var _g = g.data() || {};
        _push('課堂獎勵', (_g.merit || '(未填名目)'), _g.at,
              window._lxpsParseGrantItems(Array.isArray(_g.items) ? _g.items : []),
              (_g.kind === 'compensation') ? '學生補償工具' : (_g.by || ''));
      });
    }catch(_e1){ _warn.push('課堂獎勵發放紀錄讀取失敗：' + ((_e1 && _e1.message) || _e1)); }
    // (b) 虛寶序號 redeemCodes
    try{
      var _rs = await getDocs(query(collection(_fbDb, 'redeemCodes'), where('redeemedBy', '==', uid)));
      _rs.forEach(function(c){
        var _c = c.data() || {};
        if(_c.redeemed !== true) return;
        _push('虛寶序號', ('序號 ' + String(_c.code || c.id || '')), _c.redeemedAt,
              window._lxpsParseGrantDoc(_c.reward, _c.itemsLabel), (_c.note || ''));
      });
    }catch(_e2){ _warn.push('序號發放紀錄讀取失敗：' + ((_e2 && _e2.message) || _e2)); }
    // (c) 全體玩家獎勵：先讀獎勵清單（很小），只對「有發券」的逐筆查該生有沒有領
    try{
      var _grs = await getDocs(collection(_fbDb, 'globalRewards'));
      var _defs = [];
      _grs.forEach(function(r){
        var _r = r.data() || {};
        var _p = window._lxpsParseGrantDoc(_r.reward, _r.items);
        if(!_p.ticketN && !_p.treTicketN && !(_p.heroes || []).length) return;   // ★ v5.129.0 修錯字 _p.treTkN(永遠 undefined)
        _defs.push({ id:r.id, p:_p, title:String(_r.title || '全體獎勵') });
      });
      for(var _i = 0; _i < _defs.length; _i++){
        try{
          var _cs = await getDoc(doc(_fbDb, 'globalRewardClaims', uid + '_' + _defs[_i].id));
          var _ex = (typeof _cs.exists === 'function') ? _cs.exists() : _cs.exists;
          if(!_ex) continue;
          var _cd = _cs.data() || {};
          _push('全體獎勵', _defs[_i].title, _cd.at, _defs[_i].p, '');
        }catch(_){}
      }
    }catch(_e3){ _warn.push('全體獎勵發放紀錄讀取失敗：' + ((_e3 && _e3.message) || _e3)); }
    _grants.sort(function(a, b){ return (b.at || 0) - (a.at || 0); });

    // ── ③ 每張券的下落（ticketLedger：ev/item/n/at/result）──
    var _led = [];
    try{
      if(typeof window._fbLoadTicketLedger === 'function') _led = await window._fbLoadTicketLedger(uid) || [];
    }catch(_e4){ _warn.push('召喚卷帳本讀取失敗：' + ((_e4 && _e4.message) || _e4)); }
    var _KIND = {
      'summon_ticket_ur_pick':       { k:'ur',      nm:'👑 UR 自選卷' },
      'summon_ticket_treasure_pick': { k:'tre',     nm:'💠 自選至寶卷' },
      'summon_ticket_ssr_pick':      { k:'ssrPick', nm:'🌟 SSR 自選卷' }
    };
    var _uses = [];
    var _usedN = { ur:0, tre:0, ssrPick:0 };
    _led.forEach(function(e){
      if(!e || e.ev !== 'use') return;
      var _m = _KIND[String(e.item || '')];
      if(!_m) return;
      var _n = Math.abs(parseInt(e.n, 10) || 0) || 1;
      _usedN[_m.k] += _n;
      _uses.push({ kind:_m.k, kindName:_m.nm, n:_n,
                   at:(typeof e.at === 'number' ? e.at : 0), result:String(e.result || ''),
                   tid:String(e.tid || '') });   // ★ v5.143.0 券 tid（逗號串）
    });
    _uses.sort(function(a, b){ return (b.at || 0) - (a.at || 0); });

    // ── ④ 對帳（★ 只對 UR 與自選至寶下判斷；SSR 自選卷目前不在稽核範圍，只顯示不判定）──
    var _grantedN = { ur:0, tre:0, ssrPick:0 };
    _grants.forEach(function(g){ _grantedN.ur += g.urTk; _grantedN.tre += g.treTk; });
    // ★ v5.129.0 — 背包改「字串版優先」（與全體稽核／洗錢查緝同一口徑；只讀物件版會漏）
    var _bag = {};
    try{
      if(typeof _d.playerBackpack_s === 'string'){ _bag = JSON.parse(_d.playerBackpack_s) || {}; }
      else if(_d.playerBackpack && typeof _d.playerBackpack === 'object'){ _bag = _d.playerBackpack; }
    }catch(_){ _bag = (_d.playerBackpack && typeof _d.playerBackpack === 'object') ? _d.playerBackpack : {}; }
    var _left = {
      ur:  (parseInt(_bag['summon_ticket_ur_pick'], 10) || 0) + (parseInt(_bag['gm_summon_ticket_ur_pick'], 10) || 0),
      tre: (parseInt(_bag['summon_ticket_treasure_pick'], 10) || 0) + (parseInt(_bag['gm_summon_ticket_treasure_pick'], 10) || 0),
      ssrPick: (parseInt(_bag['summon_ticket_ssr_pick'], 10) || 0) + (parseInt(_bag['gm_summon_ticket_ssr_pick'], 10) || 0)
    };
    // ★ v5.133.0（老師需求「更精確看到自選至寶/自選SSR的來源」）— 手上的券依鍵分列：
    //   gm_ 前綴鍵＝老師發放通道專用（v3.17.6 起三個獎勵產生器一律發 gm_ 鍵）；
    //   非 gm_ 鍵＝鬥技場兌換、v3.17.6 前的舊發放、或 _useGmTicket 換券殘留
    //   ⇒ 老師一眼分辨「手上這張是不是我發的」。
    var _lk = function(k){ return parseInt(_bag[k], 10) || 0; };
    var _leftDetail = {
      ur:      { gm: _lk('gm_summon_ticket_ur_pick'),       base: _lk('summon_ticket_ur_pick') },
      tre:     { gm: _lk('gm_summon_ticket_treasure_pick'), base: _lk('summon_ticket_treasure_pick') },
      ssrPick: { gm: _lk('gm_summon_ticket_ssr_pick'),      base: _lk('summon_ticket_ssr_pick') }
    };
    // 老師直接發的 UR 英雄（不佔券額度）
    var _grantedHeroes = {};
    _grants.forEach(function(g){ (g.heroes || []).forEach(function(h){ _grantedHeroes[h] = 1; }); });
    // 應有 UR 隻數 = 老師直接發的 + UR 券已使用張數
    var _shouldUR = Object.keys(_grantedHeroes).length + _usedN.ur;
    var _surplusUR = Math.max(0, _ownedUR.length - _shouldUR);
    // 券換到的 UR（有明確結果的）→ 供老師區分「這隻是券換的、那隻查無來源」
    var _fromTicket = {};
    _uses.forEach(function(u){ if(u.kind === 'ur' && u.result) _fromTicket[u.result] = u.at; });
    // ★ 培育快照：派發「UR 角色數量確認」時要帶（退還培育資源用，與 GM 端同一組欄位）
    var _pick = function(o, n){ return (o && typeof o === 'object' && o[n] != null) ? o[n] : null; };
    var _num = function(v){ var x = parseInt(v, 10); return isNaN(x) ? 0 : x; };
    // heroSkillLevels[n] 可能是數字（單技能）或 { s1, s2 } 物件 → 兩種都吃
    var _skLv = function(n, which){
      var _v = _pick(_d.heroSkillLevels, n);
      if(_v && typeof _v === 'object') return _num(_v[which]);
      return (which === 's1') ? _num(_v) : 0;
    };
    var _snapOf = function(n){
      var _inv = _pick(_d.heroStatInvested, n) || {};
      return {
        name: n,
        lv:       Math.max(1, _num(_pick(_d.heroLevels, n)) || 1),
        exp:      Math.max(0, _num(_pick(_d.heroExp, n))),
        capsule:  { hp:_num(_inv.hp), atk:_num(_inv.atk), sp:_num(_inv.sp), spd:_num(_inv.spd) },
        s1:       Math.max(0, _skLv(n, 's1')),
        s2:       Math.max(0, _skLv(n, 's2')),   // ★ v5.129.0 補：漏了 s2，技能2 的退款會少算
        burst:    Math.max(0, _num(_pick(_d.heroBurstLevels, n))),
        invested: Math.max(0, _num(_pick(_d.heroStatPoints, n)))
      };
    };

    // ════════════════════════════════════════════════════════════════
    // ★★ v5.129.0【自我複查修正】改用與「全體稽核中心」完全相同的判定口徑 ★★
    // ────────────────────────────────────────────────────────────────
    //   v5.127/128 的第一版用「ticketLedger 的 use 筆數」推算應有隻數 —— 那是**第二套口徑**，
    //   而且四個地方都會低估學生的合法額度：
    //     ① 完全沒算合法的額度加項：稽核補發授權 `_urQuotaAdjust`、🏟 鬥技場兌換
    //     ② 沒認 `_heroUnlockHistory` 的 GM 給予類來源
    //        （admin_grant / compensation / student_designer / event_quest）
    //     ③ ticketLedger 只留最後 500 筆，舊紀錄被截掉 ⇒「已使用」被少算
    //     ④ ticketLedger 是**學生端寫的**，本來就不該當判定依據
    //   ⇒ 四項全部指向同一個後果：**把合法取得的 UR 判成「查無來源」⇒ 誤收**。
    //     而我上一輪才對老師說「與全體稽核共用同一把尺」，實際上只共用了解析器。
    //   ⇒ 改為與全體稽核逐字對齊：`_heroUnlockHistory` 逐隻歸因（gm／ticket／unknown），
    //     額度 = 三來源發出 + 稽核補發授權 + 鬥技場兌換，
    //     超額 = max(0, 券換取隻數 − 額度) + 查無來源隻數。
    //   ⚠ ticketLedger 降級為**純展示**（老師要的「每張券換到什麼、什麼時候」），不參與判定。
    // ════════════════════════════════════════════════════════════════
    var _srcCat = function(x){
      try{ if(typeof window._lxpsRareSrcCat === 'function') return window._lxpsRareSrcCat(x); }catch(_){}
      var t = String(x == null ? '' : x);
      if(!t) return 'other';
      if(t === 'admin_delete') return 'deleted';
      if(t.indexOf('summon') === 0 || t.indexOf('ticket') === 0) return 'summon';
      if(t === 'admin_grant' || t === 'compensation' || t === 'student_designer' || t === 'event_quest') return 'gm';
      return 'other';
    };
    var _hist = [];
    try{
      if(Array.isArray(_d._heroUnlockHistory)) _hist = _d._heroUnlockHistory;
      else if(typeof _d._heroUnlockHistory_s === 'string'){
        var _hp = JSON.parse(_d._heroUnlockHistory_s); if(Array.isArray(_hp)) _hist = _hp;
      }
    }catch(_){}
    var _lastRec = {};
    _hist.forEach(function(r){
      if(!r || !r.name) return;
      var _p0 = _lastRec[r.name];
      if(!_p0 || ((r.at || 0) >= (_p0.at || 0))) _lastRec[r.name] = r;
    });
    // 額度加項：稽核補發授權 + 🏟 鬥技場兌換（都是合法來源，漏算就會冤枉學生）
    var _quotaAdj = Math.max(0, parseInt(_d._urQuotaAdjust, 10) || 0);
    var _arenaTk = 0;
    try{
      var _ar = Array.isArray(_d._arenaExchangeLog) ? _d._arenaExchangeLog : [];
      _ar.forEach(function(x){
        if(!x) return;
        if(String(x.item || x.key || '').indexOf('ur_pick') >= 0) _arenaTk += Math.abs(parseInt(x.n, 10) || 1);
      });
    }catch(_){}
    var _urQuota = _grantedN.ur + _quotaAdj + _arenaTk;

    var _urDetail = _ownedUR.map(function(n){
      var _r = _lastRec[n];
      var _src = _r ? String(_r.source || '') : '';
      var _o = _snapOf(n);
      // ★ 與全體稽核完全相同的三分法
      if(_grantedHeroes[n] || _srcCat(_src) === 'gm'){ _o.origin = 'gm';      _o.src = '老師直接發放'; }
      else if(_src === 'ticket_ur_pick'){              _o.origin = 'ticket';  _o.src = 'UR 自選卷兌換'; }
      else {                                           _o.origin = 'unknown'; _o.src = ''; }
      _o.at = (_r && typeof _r.at === 'number') ? _r.at : (_fromTicket[n] || 0);
      _o.rawSrc = _src;
      _o.byGmDirect = (_o.origin === 'gm') ? 1 : 0;
      _o.noSrc = (_o.origin === 'unknown');
      return _o;
    });
    // ★ v5.143.0 — 券 tid ↔ 英雄／至寶 互相對上（純展示；判定引擎零改動）
    //   解鎖帳本從 v5.143.0 起在券換取的紀錄上寫 tid；這裡把「每張券的下落」表的 use 紀錄依 tid 反查是哪隻／哪件。
    var _tidHit = {};   // tid → [顯示名]
    try{
      _hist.forEach(function(r){
        if(!r || !r.tid || !r.name) return;
        String(r.tid).split(',').forEach(function(t){ t = t.trim(); if(!t) return; (_tidHit[t] = _tidHit[t] || []).push(String(r.name)); });
      });
      var _treH = [];
      if(Array.isArray(_d._treasureUnlockHistory)) _treH = _d._treasureUnlockHistory;
      else if(typeof _d._treasureUnlockHistory_s === 'string'){ try{ var _tp = JSON.parse(_d._treasureUnlockHistory_s); if(Array.isArray(_tp)) _treH = _tp; }catch(_){} }
      _treH.forEach(function(r){
        if(!r || !r.tid || !r.id) return;
        var _nm = String(r.id);
        try{ var _tdb = window.TAIWAN_TREASURES; if(_tdb && _tdb[r.id] && _tdb[r.id].name) _nm = _tdb[r.id].name; }catch(_){}
        String(r.tid).split(',').forEach(function(t){ t = t.trim(); if(!t) return; (_tidHit[t] = _tidHit[t] || []).push('💎' + _nm); });
      });
      _uses.forEach(function(u){
        if(!u.tid) return;
        var _got = [];
        u.tid.split(',').forEach(function(t){ t = t.trim(); if(t && _tidHit[t]) _got = _got.concat(_tidHit[t]); });
        u.tidHit = _got.join('、');
      });
    }catch(_eTid){ try{ console.warn('[單人查核] tid 反查失敗（不影響判定）', _eTid); }catch(_){} }
    _urDetail.forEach(function(x){ try{ var _r2 = _lastRec[x.name]; x.tid = (_r2 && _r2.tid) ? String(_r2.tid) : ''; }catch(_){ x.tid = ''; } });
    var _ticketOwned  = _urDetail.filter(function(x){ return x.origin === 'ticket'; });
    var _unknownOwned = _urDetail.filter(function(x){ return x.origin === 'unknown'; });
    var _gmOwned      = _urDetail.filter(function(x){ return x.origin === 'gm'; });
    _ticketOwned.sort(function(a, b){ return (a.at || 0) - (b.at || 0); });
    var _overTicket = Math.max(0, _ticketOwned.length - _urQuota);
    var _surplusUR  = _overTicket + _unknownOwned.length;
    var _shouldUR   = Math.max(0, _ownedUR.length - _surplusUR);
    var _histEmpty  = (_hist.length === 0);
    _urDetail.sort(function(a, b){ return (a.noSrc === b.noSrc) ? 0 : (a.noSrc ? -1 : 1); });

    // ════════════════════════════════════════════════════════════════
    // ★ v5.132.0（老師需求「直接在這個視窗中補發缺失的獎勵或刪除多出來的
    //   卷、多出來的 UR/SSR 英雄和至寶」）— 回傳補上兩份持有清單：
    //   ① ssrDetail：持有的 SSR 英雄（帶 _snapOf 培育快照＝刪除時可直接餵
    //      _lxpsHeroRefundPlanMerge 退款）。⚠ SSR 可由召喚正常取得，
    //      「查無來源」對 SSR **不是罪證**，只做中性顯示供老師自行判斷，
    //      刻意不做任何超額判定（與 UR 的三分法性質不同）。
    //   ② treasures：taiwanTreasureData 持有 map ＋ 帳本最新一筆歸因
    //      （讀法對齊既有 _fbAdminScanItemOwners 口徑）。
    //   兩段各自 try 包住：讀取失敗只進 _warn，不能拖垮整份查核。
    // ════════════════════════════════════════════════════════════════
    var _ssrDetail = [];
    try{
      if(typeof _getHeroRarity === 'function'){
        _unlocked.forEach(function(n){
          if(_getHeroRarity(n) !== 'SSR') return;
          var _r2 = _lastRec[n];
          var _s2 = _r2 ? String(_r2.source || '') : '';
          var _o2 = _snapOf(n);
          _o2.rawSrc = _s2;
          _o2.at  = (_r2 && typeof _r2.at === 'number') ? _r2.at : 0;
          _o2.cat = _srcCat(_s2);   // gm / summon / other（'' → other）
          _o2.inGrant = !!_grantedHeroes[n];   // ★ v5.133.0 — 三來源發放紀錄裡有這隻＝老師直接發放的最強證據
          _ssrDetail.push(_o2);
        });
      }
    }catch(_eSsr){ _warn.push('SSR 持有清單讀取失敗：' + ((_eSsr && _eSsr.message) || _eSsr)); }
    var _treList = [];
    try{
      var _twMap = (_d.taiwanTreasureData && typeof _d.taiwanTreasureData === 'object') ? _d.taiwanTreasureData : {};
      var _tHist2 = [];
      if(Array.isArray(_d._treasureUnlockHistory)) _tHist2 = _d._treasureUnlockHistory;
      else if(typeof _d._treasureUnlockHistory_s === 'string'){
        try{ var _thp = JSON.parse(_d._treasureUnlockHistory_s); if(Array.isArray(_thp)) _tHist2 = _thp; }catch(_){}
      }
      var _tLatest2 = {};
      _tHist2.forEach(function(r){
        if(!r || !r.id) return;
        var _a = Number(r.at) || 0;
        if(!(r.id in _tLatest2) || _a >= _tLatest2[r.id].at) _tLatest2[r.id] = { at:_a, source:String(r.source || '') };
      });
      Object.keys(_twMap).forEach(function(_tid){
        var _tr = _twMap[_tid]; if(!_tr) return;
        var _rec = _tLatest2[_tid] || { at:0, source:'' };
        var _tname = (window.TAIWAN_TREASURES && window.TAIWAN_TREASURES[_tid] && window.TAIWAN_TREASURES[_tid].name) || _tid;
        _treList.push({ id:_tid, name:_tname, lv:(Number(_tr.lv) || 1), source:_rec.source, at:_rec.at });
      });
    }catch(_eTre){ _warn.push('至寶持有清單讀取失敗：' + ((_eTre && _eTre.message) || _eTre)); }

    // ★ v5.133.0 — 券的取得/異動時間線：ticketLedger 的 grant（獲得）與 revoke（GM 回收）
    //   逐筆帶出（use 已在 r.uses）。⚠ 帳本是學生端寫的、500 筆截斷 ⇒ 純展示僅供參考
    //   （v5.129.0 既有裁定：ledger 不參與判定）。gm_ / 一般鍵在 UI 端分別標示，
    //   老師可對照「這張券是走老師發放通道拿的，還是鬥技場兌換/舊發放」。
    var _tkGrants = [];
    try{
      var _TKK = {
        'summon_ticket_ur_pick':1,       'gm_summon_ticket_ur_pick':1,
        'summon_ticket_treasure_pick':1, 'gm_summon_ticket_treasure_pick':1,
        'summon_ticket_ssr_pick':1,      'gm_summon_ticket_ssr_pick':1
      };
      _led.forEach(function(e){
        if(!e || !_TKK[e.item]) return;
        if(e.ev !== 'grant' && e.ev !== 'revoke') return;
        _tkGrants.push({ ev:String(e.ev), item:String(e.item), n:(parseInt(e.n, 10) || 0),
                         at:(typeof e.at === 'number' ? e.at : 0), by:String(e.by || '') });
      });
      _tkGrants.sort(function(a, b){ return (b.at || 0) - (a.at || 0); });
      if(_tkGrants.length > 40) _tkGrants = _tkGrants.slice(0, 40);
    }catch(_eTk){ _warn.push('券取得紀錄讀取失敗：' + ((_eTk && _eTk.message) || _eTk)); }

    return {
      uid: uid,
      email: String(_d.email || ''),
      nickname: String(_d.displayName || _d.name || ''),
      grants: _grants,
      uses: _uses,
      granted: _grantedN,
      used: _usedN,
      left: _left,
      leftDetail: _leftDetail,             // ★ v5.133.0 — 手上的券分列（gm=老師發放通道 / base=鬥技場兌換或舊發放）
      tkGrants: _tkGrants,                 // ★ v5.133.0 — 券的取得/異動時間線（帳本 grant/revoke·僅供參考）
      grantedHeroes: Object.keys(_grantedHeroes),
      ownedUR: _ownedUR,
      urDetail: _urDetail,
      ssrDetail: _ssrDetail,               // ★ v5.132.0 — 持有 SSR 英雄（含培育快照·中性歸因不判超額）
      treasures: _treList,                 // ★ v5.132.0 — 持有至寶（id/name/lv/source/at）
      shouldUR: _shouldUR,
      surplusUR: _surplusUR,
      // ★ v5.129.0 — 讓老師看得出「這個數字是怎麼算出來的」，而不是只丟一個結論
      urQuota: _urQuota,                    // 券總額度 = 三來源發出 + 稽核補發授權 + 鬥技場兌換
      quotaAdj: _quotaAdj,                  // 其中：稽核補發授權(_urQuotaAdjust)
      arenaTk: _arenaTk,                    // 其中：鬥技場兌換
      gmCount: _gmOwned.length,             // 老師直接發放的隻數
      ticketCount: _ticketOwned.length,     // 券換取的隻數
      unknownCount: _unknownOwned.length,   // 查無來源的隻數
      overTicket: _overTicket,              // 券換取超出額度的隻數
      histEmpty: _histEmpty,                // ★ 解鎖帳本空的 → 判定不可信，不可派發
      ledgerEmpty: (_led.length === 0),
      warn: _warn
    };
  };

  window._fbAdminScanURTicketAudit = async function(){
    // ① UR 名單（唯一權威 _RARITY_UR_HEROES 的既有投影，日後新增 UR 自動跟上）
    var _urNames = [];
    try{
      if(Array.isArray(window._LXPS_RARITY_UR) && window._LXPS_RARITY_UR.length){
        _urNames = window._LXPS_RARITY_UR.slice();
      }
    }catch(_){}
    if(!_urNames.length){ throw new Error('讀不到 UR 名單(window._LXPS_RARITY_UR),請重新整理頁面'); }
    var _urSet = {};
    _urNames.forEach(function(n){ _urSet[n] = 1; });

    // ★ v5.93.0 — 取得來源分類：一律走既有單一真相 window._lxpsRareSrcCat（v3.15.x 起就有的
    //   _rareSrcCat，本輪提升為 window）。⚠ 刻意不自建白名單：老師補發在 v3.17.3 之前寫的是
    //   'compensation'、之後才是 'admin_grant'，另有 'student_designer'/'event_quest'，
    //   自建表必漏（老師 2026-08-20 實錘 lsps110019 克雷爾 source=compensation）。
    //   讀不到時退回最小內建表（只在載入順序異常時生效，且刻意涵蓋 compensation）。
    var _urSrcCat = function(s){
      try{
        if(typeof window._lxpsRareSrcCat === 'function') return window._lxpsRareSrcCat(s);
      }catch(_){}
      var t = String(s == null ? '' : s);
      if(!t) return 'other';
      if(t === 'admin_delete') return 'deleted';
      if(t.indexOf('summon') === 0 || t.indexOf('ticket') === 0) return 'summon';
      if(t === 'admin_grant' || t === 'compensation' || t === 'student_designer' || t === 'event_quest') return 'gm';
      return 'other';
    };
    var _UR_TICKET_SRC = { 'ticket_ur_pick': 1 };
    // 移除類：帳本最後一筆是這些 → 視同已不持有（同 _lxpsLatestDeletedMap 口徑）
    var _UR_REMOVED_SRC = { 'admin_delete': 1, 'audit_error_recovered': 1 };
    // ★ 裁定 1 乙：入帳後這段時間內被 admin_delete → 標「疑似誤收」（僅標示，不自動救回）
    var _UR_MISDELETE_MS = 10 * 60 * 1000;

    // ② 掃 gmGiftLog：聚合每位學生「被發過幾張 UR 券」「被直接發過哪幾隻 UR 英雄」
    //    items 是文字標籤陣列，格式由課堂獎勵/全體獎勵/序號三個產生器寫入，例：
    //      '👑GM獎勵UR自選召喚卷×2' / '🌟UR藝天使克雷爾' / '⚡UR主神奧汀'
    var _giftByUid = {};
    var _giftDocs = 0;

    // ★ v5.128.0 — 解析器已搬到模組頂層（見 window._lxpsParseGrantItems 定義處）。
    //   這裡只留區域別名，內部呼叫零改動；`_urNames` 改由解析器自己從 window._LXPS_RARITY_UR 取。
    var _UR_BAG_KEYS  = window._LXPS_UR_BAG_KEYS;
    var _TRE_BAG_KEYS = window._LXPS_TRE_BAG_KEYS;
    var _parseGrantItems = window._lxpsParseGrantItems;
    var _parseGrantDoc   = window._lxpsParseGrantDoc;

    // 併入某位學生的發放帳（三個來源共用；只加不減）
    //   coverKey = 該筆在「學生領取紀錄」裡的對應鍵（全體獎勵＝rewardId／序號＝'code:'+序號），
    //              供下游的「查無對應發放文件就不夾制」防呆比對；gmGiftLog 不需要（傳空字串）。
    var _addGrant = function(uid, p, meta, coverKey){
      if(!uid) return;
      if(!_giftByUid[uid]) _giftByUid[uid] = { ticketN:0, treTicketN:0, heroes:{}, recs:[], cover:{} };
      if(!_giftByUid[uid].cover) _giftByUid[uid].cover = {};
      var G = _giftByUid[uid];
      G.ticketN    += (p.ticketN    || 0);
      G.treTicketN += (p.treTicketN || 0);
      (p.heroes || []).forEach(function(hn){ G.heroes[hn] = 1; });
      if(coverKey) G.cover[coverKey] = 1;
      if((p.ticketN || 0) || (p.treTicketN || 0) || (p.heroes || []).length){
        G.recs.push({
          at: (typeof meta.at === 'number') ? meta.at : 0,
          ticketN: (p.ticketN || 0),
          treTicketN: (p.treTicketN || 0),
          heroes: (p.heroes || []).slice(),
          merit: String(meta.merit || ''),
          by: String(meta.by || ''),
          kind: String(meta.kind || ''),
          rewardId: String(meta.rewardId || '')
        });
      }
    };

    try{
      var _gsnap = await getDocs(collection(_fbDb, 'gmGiftLog'));
      _gsnap.forEach(function(gd){
        _giftDocs++;
        try{
          var g = gd.data() || {};
          var _u = String(g.uid || '');
          if(!_u) return;
          if(!_giftByUid[_u]) _giftByUid[_u] = { ticketN:0, treTicketN:0, heroes:{}, recs:[], cover:{} };
          var _items = Array.isArray(g.items) ? g.items : [];
          // ★ v5.124.0 — 改讀單一真相解析器（舊版的逐行 inline 解析已整段搬進 _parseGrantItems，
          //   邏輯一字未改；三個發放來源自此保證同一口徑）
          var _pg = _parseGrantItems(_items);
          var _thisTicket = _pg.ticketN, _thisHeroes = _pg.heroes, _thisTreTicket = _pg.treTicketN;
          _giftByUid[_u].ticketN += _thisTicket;
          _giftByUid[_u].treTicketN += _thisTreTicket;      // ★ v5.97.0 自選至寶券
          _thisHeroes.forEach(function(hn){ _giftByUid[_u].heroes[hn] = 1; });
          if(_thisTicket || _thisHeroes.length || _thisTreTicket){
            _giftByUid[_u].recs.push({
              at: (typeof g.at === 'number') ? g.at : 0,
              ticketN: _thisTicket,
              treTicketN: _thisTreTicket,                   // ★ v5.97.0
              heroes: _thisHeroes.slice(),
              merit: String(g.merit || ''),
              by: String(g.by || ''),
              kind: String(g.kind || ''),
              // ★ v5.93.0 — 供比對玩家主檔 _gmcrClaimed，分辨「還沒領」vs「領了卻消失」
              //   （v3.17.6 起 _send 會寫入 rewardId；更早的紀錄沒有 → 空字串＝不判定為未領，保守）
              rewardId: String(g.rewardId || g.rid || '')
            });
          }
        }catch(_){}
      });
    }catch(_eGift){
      throw new Error('讀取送禮記錄失敗(可能 gmGiftLog 規則未部署):' + (_eGift && _eGift.message || _eGift));
    }

    // ════════════════════════════════════════════════════════════════════
    // ★★ v5.124.0【甲案・治本】另外兩條發券路徑的「發放紀錄」從權威文件直接讀 ★★
    // ────────────────────────────────────────────────────────────────────
    // 【為什麼不是「補寫 gmGiftLog」】
    //   🎉 全體玩家獎勵 與 🎟️ 虛寶序號 的**領取／兌換是學生端做的**，
    //   GM 發放當下根本不知道誰會領；而 firestore.rules 的 gmGiftLog 是
    //   `allow read, create: if isAdmin()` ⇒ 學生端寫入必定被規則擋下。
    //   要讓它們寫得進去，就得開放學生自己寫「發放紀錄」＝讓學生偽造稽核的分母，
    //   比現在的問題嚴重得多。⇒ 這條路不能走。
    //
    // 【治本做法】這兩條路的權威紀錄**本來就已經存在**，只是稽核沒去讀：
    //   ・redeemCodes/{code}          有 redeemed / redeemedBy / redeemedAt（GM 專屬可列）
    //   ・globalRewardClaims/{uid}_{rewardId}  記錄誰領了哪一筆全體獎勵
    //   兩者都是「GM 才寫得動」或「學生只能佔位、動不了獎勵內容」的文件 ⇒ 可信。
    //   ⇒ 併進既有的 `_giftByUid`（發放帳的唯一入口）後，
    //     **UR 原生段與至寶泛化引擎兩套引擎一行未改就同時生效**。
    //
    // 【為什麼比「GM 一鍵補寫」好】
    //   零寫入、永遠即時、不必老師記得按、連 v3.17.8 前沒有領取紀錄的舊帳也一起救回來
    //   （那些人現在會因為 granted=0 且 quota=0 被判超額）。不會有「補寫失敗 → 誤判 →
    //   老師處理完才發現要回溯」的中間狀態。
    //
    // 【讀取量】刻意分兩段守門：
    //   ・redeemCodes 用 where('redeemed','==',true) 單一等值條件、無 orderBy
    //     ⇒ 不需要複合索引，也不會把未兌換的序號整批拉下來。
    //   ・globalRewards 先讀（很小），**只有真的發過券／UR 英雄的獎勵**才去掃 claims；
    //     全體獎勵多半只發水晶／知識幣 ⇒ 常見情況零額外讀取。
    //
    // 【誤收防呆・本段最重要的一行】
    //   併入之後，額度夾制式（學生已領 ≤ 老師已發）會開始對這兩條路生效。
    //   若某筆權威文件讀不到或已被刪除，granted 會偏低 ⇒ 夾制把 quota 壓低
    //   ⇒ 合法資產被判超額 ⇒ 老師收回 ⇒ 事後要回溯。
    //   ⇒ 故逐筆記下「這位學生的哪些領取紀錄有對應到權威文件」（cover 集合），
    //     下游若發現有對不上的，該列**一律不套用夾制**並明示原因（見 _lxpsGrantCoverGap）。
    //   ⇒ 任一來源讀取失敗 → 該來源**整批不併**（all-or-nothing），
    //     絕不用半份資料去夾制；並把失敗原因往上帶，UI 明示、不靜默。
    // ════════════════════════════════════════════════════════════════════
    var _extraGrantStat = { redeemDocs:0, redeemUsed:0, grDocs:0, grClaims:0, warn:'' };
    // ── (A) 🎟️ 虛寶序號：已兌換的序號 → 記到 redeemedBy 那位學生頭上 ──
    try{
      var _rcSnap;
      try{
        _rcSnap = await getDocs(query(collection(_fbDb, 'redeemCodes'), where('redeemed', '==', true)));
      }catch(_eQ){
        // 極少數環境不吃 where（規則／索引差異）→ 退回全掃，仍只採信 redeemed===true
        _rcSnap = await getDocs(collection(_fbDb, 'redeemCodes'));
      }
      var _rcBuf = [];
      _rcSnap.forEach(function(cd){
        _extraGrantStat.redeemDocs++;
        try{
          var c = cd.data() || {};
          if(c.redeemed !== true) return;
          var _cu = String(c.redeemedBy || '');
          if(!_cu) return;                       // 沒記兌換者＝無從歸屬，寧可不算（不冤枉別人）
          var _code = String(c.code || cd.id || '');
          var _p = _parseGrantDoc(c.reward, c.itemsLabel);
          if(!_p.ticketN && !_p.treTicketN && !_p.heroes.length) return;
          _rcBuf.push({ uid:_cu, p:_p, code:_code,
            meta:{ at:(typeof c.redeemedAt === 'number') ? c.redeemedAt : 0,
                   merit:('序號兌換:' + _code), by:String(c.createdBy || ''), kind:'redeemCode', rewardId:'' } });
        }catch(_){}
      });
      // ★ 全部解析完才一次併入＝all-or-nothing，中途拋錯不會留下半份分母
      _rcBuf.forEach(function(b){ _addGrant(b.uid, b.p, b.meta, 'code:' + b.code); _extraGrantStat.redeemUsed++; });
    }catch(_eRc){
      _extraGrantStat.warn += '｜序號發放紀錄讀取失敗(' + ((_eRc && _eRc.message) || _eRc) + ')：這批券的發放數暫以 0 計，該列不會套用額度夾制';
    }

    // ── (B) 🎉 全體玩家獎勵：先看有沒有發過券／UR 英雄，有才去掃認領文件 ──
    try{
      var _grDefs = {};      // rewardId → 解析結果
      var _grAny = false;
      var _grSnap = await getDocs(collection(_fbDb, 'globalRewards'));
      _grSnap.forEach(function(rd){
        _extraGrantStat.grDocs++;
        try{
          var r = rd.data() || {};
          var _p = _parseGrantDoc(r.reward, r.items);
          if(!_p.ticketN && !_p.treTicketN && !_p.heroes.length) return;   // 只發水晶／幣的獎勵不必掃認領
          _grDefs[rd.id] = { p:_p, title:String(r.title || '全體獎勵'), by:String(r.createdBy || '') };
          _grAny = true;
        }catch(_){}
      });
      if(_grAny){
        var _gcBuf = [];
        var _gcSnap = await getDocs(collection(_fbDb, 'globalRewardClaims'));
        _gcSnap.forEach(function(cd){
          try{
            var _id = String(cd.id || '');
            var _cut = _id.indexOf('_');
            if(_cut <= 0) return;
            var _cuid = _id.slice(0, _cut);
            var _crid = _id.slice(_cut + 1);
            var _def = _grDefs[_crid];
            if(!_def) return;                    // 這筆獎勵沒發券／UR ⇒ 與稽核無關
            var _cdd = cd.data() || {};
            _gcBuf.push({ uid:_cuid, p:_def.p, rid:_crid,
              meta:{ at:(typeof _cdd.at === 'number') ? _cdd.at : 0,
                     merit:('全體獎勵:' + _def.title), by:_def.by, kind:'globalReward', rewardId:_crid } });
          }catch(_){}
        });
        _gcBuf.forEach(function(b){ _addGrant(b.uid, b.p, b.meta, b.rid); _extraGrantStat.grClaims++; });
      }
    }catch(_eGr){
      _extraGrantStat.warn += '｜全體獎勵發放紀錄讀取失敗(' + ((_eGr && _eGr.message) || _eGr) + ')：這批券的發放數暫以 0 計，該列不會套用額度夾制';
    }

    // ③ 掃 players：算實有 UR、手上剩餘券、召喚時間與結果、培育情形
    var _rows = [];
    var _scanned = 0;
    // ★★ v5.96.0（老師 2026-08-20 需求①）— 三個管理員帳號一律排除稽核 ★★
    //   理由：管理員自動解鎖全英雄（GM 測試需要），持有的數十隻 UR 帳本上完全沒有取得紀錄
    //   → 逐隻歸因全判 'unknown' → `A_unknown` + `A_surplus` 雙旗標 → 三個帳號恆定霸佔
    //     「🔴 多拿」榜首，還把摘要人數灌高，每次掃描都要老師手動略過。
    //   ⚠ 口徑刻意與 index.html 既有 `window._isAdminUser` 的 `_devEmails` 完全一致
    //     （本函式在別的 script 區塊、且是對「別人的 doc」判定不是對當前登入者，
    //      無法直接呼叫 _isAdminUser()，故此處以同一份 email 清單比對）。
    //   ⚠ 排除數量會回傳給 UI 明示「已排除管理員 N 位」，絕不靜默篩掉。
    var _URA_ADMIN_EMAILS = {
      'clarebox123@gmail.com': 1,
      'clarebox123@lsps.tp.edu.tw': 1,
      'asemia@gmail.com': 1
    };
    var _adminSkipped = 0;
    // ⚠ v5.96.1 — 本行於 v5.96.0 加入管理員排除時被誤刪，導致 `_psnap is not defined`
    //   整支稽核當場中斷（老師實測截圖）。誤刪是大忌，保留此註記以防再犯。
    var _psnap = await getDocs(collection(_fbDb, 'players'));
    _psnap.forEach(function(ds){
      _scanned++;
      try{
        var d = ds.data() || {};
        var _uid = ds.id;
        // ★ v5.96.0 — 管理員帳號直接略過（放在最前面＝完全不進任何統計與旗標運算）
        if(_URA_ADMIN_EMAILS[String(d.email || '').toLowerCase()]){ _adminSkipped++; return; }
        var _unlocked = Array.isArray(d.unlockedHeroes) ? d.unlockedHeroes : [];
        // ★★ v5.124.0 ③【防禦性補丁】主檔只剩字串版 unlockedHeroes_s 時的備援 ★★
        //   舊碼只讀陣列版 ⇒ 主檔若只剩 `unlockedHeroes_s`（_buildSafeData 兩鍵同寫，
        //   但三槽合併／舊 client 寫入等情況可能只留下字串版），該生會被判「持有 0 隻 UR」
        //   ⇒ 整列從稽核表上消失，老師完全看不到他。同檔 _dataSummary 的 `_flUnlocked`
        //   與泛化引擎 `_lxpsAssetAuditOne` 的持有讀法本來就有這道備援，只有這裡漏了。
        //   ⚠ 只在陣列版為空時才退回字串版＝純備援、只加不減，陣列版有值時行為一行未變。
        if(!_unlocked.length && typeof d.unlockedHeroes_s === 'string'){
          try{ var _uParsed = JSON.parse(d.unlockedHeroes_s); if(Array.isArray(_uParsed)) _unlocked = _uParsed; }catch(_eUS){}
        }
        var _gift = _giftByUid[_uid] || { ticketN:0, heroes:{}, recs:[], cover:{} };
        var _grantedHeroes = Object.keys(_gift.heroes);

        // ════════════════════════════════════════════════════════════════
        // ★ v5.93.0 — 帳本權威改寫（老師 2026-08-20 實錘：lsps110109 顯示「已用 0 張」
        //   但帳本明明有兩筆 ticket_ur_pick）
        // ────────────────────────────────────────────────────────────────
        // 【舊病灶】舊版三個關鍵數字全是「推算」不是「查證」：
        //   ① _used = max(0, 發出券 − 手上剩餘券)  ← 純減法，完全不讀帳本。
        //      券該扣沒扣（本輪要抓的 BUG 本身）時，那張幽靈券會被當成「還沒用」，
        //      → _expected 被灌高 → **差額低報**，受影響最重的帳號反而看起來正常。
        //   ② GM 直接發放只認 gmGiftLog ← 但學生補償工具 / 誤刪救回 / 過度補回救回 /
        //      _fbCompensatePlayer / 早期無 gmGiftLog 的年代 都只寫 _heroUnlockHistory
        //      的 source='admin_grant'，不寫 gmGiftLog → 老師親手發的 UR 被判成來路不明
        //      → **差額高報＝冤枉學生**（老師 2026-08-20 指示「GM 直接發的要算是他的」）。
        //   ③ 發出券 ≠ 學生拿到券：課堂獎勵是寄到收件箱、學生自己按「確認領取」才入帳。
        //      老師發了但學生還沒領的券，舊版照算進額度 → 額度虛胖 → 差額低報。
        // 【新口徑】不再算總數，改**逐隻歸因**：每一隻現在持有的 UR 都要講得出出處。
        //   gm      = gmGiftLog 有記 ∪ 帳本最後一筆 source 屬 GM 給予類 → 合法，永遠不算超額
        //   ticket  = 帳本最後一筆 source='ticket_ur_pick' → 佔用一格券額度
        //   unknown = 兩者皆非（含查無紀錄）→ 全數可疑
        //   券額度 = 學生「實際領到」的券張數（讀三個 claimLog，不是老師發出數）
        //   超額 = max(0, 券換取隻數 − 券額度) + 查無來源隻數
        // ════════════════════════════════════════════════════════════════

        // ── (a) 帳本：取每隻 UR 的最後一筆紀錄 + 統計用券總次數 ──
        var _urHistAll = [];
        try{
          var _hhRaw = Array.isArray(d._heroUnlockHistory) ? d._heroUnlockHistory : [];
          _hhRaw.forEach(function(h){
            if(!h || !_urSet[h.name]) return;
            _urHistAll.push({
              name: String(h.name || ''),
              at: (typeof h.at === 'number') ? h.at : 0,
              source: String(h.source || ''),
              uid12: String(h.uid || '')
            });
          });
          _urHistAll.sort(function(a, b){ return (a.at || 0) - (b.at || 0); });
        }catch(_){}
        var _lastRecOf = {};
        _urHistAll.forEach(function(x){ _lastRecOf[x.name] = x; });   // 已排序 → 最後寫入者即最後一筆
        // 用券總次數（含後來被刪掉的英雄；券一旦被用掉就是用掉了，不因刪角色而還原）
        var _usedReal = 0;
        _urHistAll.forEach(function(x){ if(_UR_TICKET_SRC[x.source]) _usedReal++; });

        // ── (b) 稽核感知：帳本最後一筆為移除類者，即使 unlockedHeroes 殘留也不算持有 ──
        //   （與 _lxpsLatestDeletedMap 同口徑；此處自建以免依賴另一 script 區塊的載入順序）
        var _owned = _unlocked.filter(function(n){
          if(!_urSet[n]) return false;
          var _r = _lastRecOf[n];
          if(_r && _UR_REMOVED_SRC[_r.source]) return false;
          return true;
        });
        var _actual = _owned.length;

        // ── (c) 券額度：學生「實際領到」幾張（三個 claimLog，皆在主檔內＝零額外讀取）──
        var _claimedTk = 0, _claimLogSeen = false;
        var _claimSeenRid = {};      // ★ v5.95.0 ❷ 跨三份 log 共用的 rewardId 去重集合
        var _recoverSkipped = 0;     // ★ v5.95.0 ❶ 被濾掉的「系統補發」筆數（UI 顯示用）
        var _dupSkipped = 0;         // ★ v5.124.0 ⑤ 被備援鍵去重掉的重複領取紀錄筆數（UI 明示，不靜默）
        // ★★ v5.124.0【甲案・誤收防呆】對不上權威發放文件的領取筆數（>0 ⇒ 本列一律不夾制）★★
        //   口徑與泛化引擎 _lxpsAssetAuditOne 完全一致，兩邊要一起改。
        var _coverGap = 0;
        var _giftCover = (_gift.cover && typeof _gift.cover === 'object') ? _gift.cover : {};
        try{
          var _scanClaim = function(arr, itemKey, srcTag){
            if(!Array.isArray(arr) || !arr.length) return;
            _claimLogSeen = true;
            arr.forEach(function(e){
              if(!e) return;
              // ★★ v5.95.0 ❶【老師 2026-08-20 實錘：發 1 張卻算成領 2 張】根因就在這裡 ★★
              //   _fbReconcileGmClassRewardTickets（登入回溯對帳）補發漏領的券時，會補寫一筆
              //   { rewardId:'recover_ticket_<時間戳>', label:'系統補發漏領自選卷',
              //     items:['UR自選召喚卷×N'], _recover:true }
              //   舊碼只掃 items 有沒有「UR自選召喚卷」→ 同一張券被記兩次
              //   （① 學生原本領取那筆 ② 系統補發那筆），額度虛高。
              //   ⚠ 補發是「同一張券的下游動作」，不是新的一張券，一律不計入額度。
              //   ⚠ 正常領取路徑本身有 rewardId 去重，唯獨這筆的 rewardId 每次都不同 → 天生繞過去重。
              if(e._recover){ _recoverSkipped++; return; }
              // ★ v5.95.0 ❷ 同一 rewardId 只計一次（防多裝置／重試重複寫入；三份 log 共用一個集合）
              var _rid = String(e.rewardId || '');
              // ★★ v5.124.0 ⑤【防禦性補丁】rewardId 缺失時舊版完全不去重 ★★
              //   _redeemClaimLog（序號兌換）的欄位是 { code, reward, itemsLabel, at }＝**沒有 rewardId**，
              //   所以那一份從來沒進過去重集合；重複寫入會被重複計 ⇒ 分母虛高 ⇒ 漏收。
              //   序號用 code 當鍵（全域唯一 12 碼，比指紋精確）；兩者皆無才退回「時間戳＋標題」，
              //   且 at 必須 > 0 才去重 —— at 缺失時刻意不去重（寧可多算也不冤枉學生）。
              //   ⚠ 與泛化引擎 _lxpsAssetAuditOne 的同一段口徑完全一致，兩邊要一起改。
              if(!_rid && e.code) _rid = 'code:' + String(e.code);
              if(!_rid && typeof e.at === 'number' && e.at > 0) _rid = 'at:' + e.at + '|' + String(e.label || '');
              if(_rid){
                if(_claimSeenRid[_rid]){ _dupSkipped++; return; }
                _claimSeenRid[_rid] = 1;
              }
              // ★ v5.124.0 — 改為「先算出這一筆領到幾張」再累加，才有辦法逐筆比對權威發放文件
              var _n = 0;
              // structured reward 優先（_grClaimLog / _redeemClaimLog 有；最精準）
              var _rb = (e.reward && typeof e.reward === 'object' && e.reward.backpack
                         && typeof e.reward.backpack === 'object') ? e.reward.backpack : null;
              if(_rb){
                _n = (parseInt(_rb['gm_summon_ticket_ur_pick'], 10) || 0)
                   + (parseInt(_rb['summon_ticket_ur_pick'], 10) || 0);
              } else {
                // 退回解析文字標籤（_gmcrClaimLog 只有 items）
                var _its = Array.isArray(e[itemKey]) ? e[itemKey] : [];
                _its.forEach(function(it){
                  var s = String(it || '');
                  if(s.indexOf('UR自選召喚卷') < 0) return;
                  var m = s.match(/×\s*(\d+)/);
                  _n += m ? (parseInt(m[1], 10) || 1) : 1;
                });
              }
              _claimedTk += _n;
              // ★ 誤收防呆：真的領到券、且來源是「權威文件應該查得到」的兩條路，
              //   卻在 _gift.cover 找不到對應 ⇒ 發放文件缺漏 ⇒ 本列不可套用夾制。
              //   ⚠ 課堂獎勵（gmcr）走 gmGiftLog，本來就有紀錄，不在檢查範圍。
              if(_n > 0 && (srcTag === 'gr' || srcTag === 'redeem')){
                var _ck = (srcTag === 'gr') ? String(e.rewardId || '') : ('code:' + String(e.code || ''));
                if(!_ck || !_giftCover[_ck]) _coverGap++;
              }
            });
          };
          _scanClaim(d._gmcrClaimLog,   'items',      'gmcr');
          _scanClaim(d._grClaimLog,     'items',      'gr');
          _scanClaim(d._redeemClaimLog, 'itemsLabel', 'redeem');
        }catch(_){}
        // ⚠ 領取紀錄是 v3.16.66 / v3.17.8 才開始寫的，更早領的沒有紀錄。
        //   完全查無領取紀錄 → 保守退回「老師發出數」（寧可額度給寬，絕不冤枉學生），UI 會標示。
        var _quotaFromLog = _claimLogSeen && _claimedTk > 0;
        var _claimedRaw   = _claimedTk;   // 夾制前的原始領取數（UI 標示用）
        // ★★ v5.95.0 ❹【老師裁定甲】總則：學生「已領」不可能大於老師「已發」★★
        //   ❶❷ 修完本例就正常了，本項是防「日後還有第三種沒想到的重複來源」的最後一道。
        //   ★ 只在 gmGiftLog 有該生發放紀錄（ticketN > 0）時才夾：
        //     ticketN = 0 代表早期沒寫 gmGiftLog 的年代（v3.17.6 前）＝紀錄缺漏，
        //     一律夾會把舊帳號的合法券判成超額 → 違反 v5.93.0「寧可額度給寬，絕不冤枉學生」。
        // ★★ v5.95.0【老師裁定乙】稽核補發的額度調整走獨立欄位 _urQuotaAdjust ★★
        //   ⚠ 刻意不寫進 gmGiftLog：gmGiftLog 是「老師實際發了幾張」的唯一權威，
        //     也正是上面夾制式的分母；拿補發去墊高分母＝讓分母被自己的下游動作推著跑，
        //     日後任何對帳都說不清哪一張是老師真的發的。獨立欄位保住分母乾淨。
        //   ⚠ 不加這個欄位的話：狀況 D（券用掉、英雄卻不見）補發 1 張券之後
        //     quota 沒跟著加 → 應剩 = quota − used = 0，而背包多了 1 張
        //     → ghost = 1 ⇒ 剛補的券當場被判成幽靈券，下次掃描叫老師回收（補一張多一隻鬼）。
        var _quotaAdj = Math.max(0, parseInt(d._urQuotaAdjust, 10) || 0);
        var _quotaCapped = 0;            // 被夾掉幾張（>0 代表領取紀錄比發放紀錄多，UI 要明示不靜默）
        var _ticketQuota;
        // ⚠⚠ 順序關鍵：**先夾制、再加補發調整**，兩者是不同性質的東西，不可混在一起：
        //     夾制 = 拿「學生領取紀錄」對「老師發放紀錄」做上限比對；
        //     _urQuotaAdjust = 稽核補發的額外授權，本來就不在任何領取紀錄裡
        //       （補發走 _fbCompensatePlayer，不寫 _gmcrClaimLog）。
        //   ★ 沙箱實測：若把 adj 併進 cap 寫成 min(claimed, granted+adj)，
        //     當 claimed(1) < granted+adj(2) 時 adj 完全不生效 → 補完的券當場又變幽靈券。
        if(_quotaFromLog){
          _ticketQuota = _claimedTk;
          // ★ v5.124.0 — 夾制多一道守門 `_coverGap === 0`：只要有任何一筆領取紀錄
          //   找不到對應的權威發放文件（全體獎勵／序號），分母就可能不完整，一律不夾。
          //   寧可漏收，也絕不誤收 —— 誤收要回溯，代價遠大於漏收。
          if(_gift.ticketN > 0 && _coverGap === 0 && _ticketQuota > _gift.ticketN){
            _quotaCapped = _ticketQuota - _gift.ticketN;
            _ticketQuota = _gift.ticketN;
          }
          _ticketQuota += _quotaAdj;
        } else {
          _ticketQuota = _gift.ticketN + _quotaAdj;
        }
        var _pendingTk    = Math.max(0, _gift.ticketN - _ticketQuota);   // 老師發了但查無領取紀錄

        // ── (c2) ★ v5.93.0 型態 B 分辨：老師發的券「是不是還躺在收件箱沒領」──
        //   課堂獎勵是寄到 gmClassRewards 收件箱、學生自己按「確認領取」才入帳。
        //   _gmcrClaimed = 已按過確認領取的 rewardId 清單（自寫欄位，永遠可讀）。
        //   老師發過券的那幾筆 rewardId 若不在 _gmcrClaimed 裡 → 純粹「還沒領」，
        //   ★ 這是完全正常的狀態，絕不能跟「領了但券消失」混為一談（老師 2026-08-20 lsps111001）。
        var _claimedIdSet = {};
        try{
          (Array.isArray(d._gmcrClaimed) ? d._gmcrClaimed : []).forEach(function(x){ _claimedIdSet[String(x)] = 1; });
        }catch(_){}
        var _unclaimedTk = 0;      // 還在收件箱沒領的券張數
        try{
          _gift.recs.forEach(function(g){
            if(!g || !g.ticketN) return;
            if(g.rewardId && !_claimedIdSet[g.rewardId]) _unclaimedTk += g.ticketN;
          });
        }catch(_){}

        // ── (d) 逐隻歸因 ──
        var _originOf = {}, _gmOwned = [], _ticketOwned = [], _unknownOwned = [];
        _owned.forEach(function(n){
          var _r = _lastRecOf[n];
          var _src = _r ? _r.source : '';
          var _cat = _urSrcCat(_src);
          var _o;
          // gmGiftLog 有記 或 帳本判為 GM 給予類（含 compensation / student_designer / event_quest）
          if(_gift.heroes[n] || _cat === 'gm') _o = 'gm';
          else if(_UR_TICKET_SRC[_src]) _o = 'ticket';
          else _o = 'unknown';
          _originOf[n] = _o;
          if(_o === 'gm') _gmOwned.push(n);
          else if(_o === 'ticket') _ticketOwned.push(n);
          else _unknownOwned.push(n);
        });
        // 券換取的依取得時間排序 → 第幾隻超出額度一目了然
        _ticketOwned.sort(function(a, b){
          return ((_lastRecOf[a] && _lastRecOf[a].at) || 0) - ((_lastRecOf[b] && _lastRecOf[b].at) || 0);
        });
        var _overTicket = Math.max(0, _ticketOwned.length - _ticketQuota);
        var _surplus    = _overTicket + _unknownOwned.length;   // 多拿（型態 A）

        // ── (e) 手上剩餘券 + 幽靈券（兩鍵分開列；_s 字串版優先＝v3.15.57 洗錢口徑）──
        var _bag = {};
        try{
          if(typeof d.playerBackpack_s === 'string'){ _bag = JSON.parse(d.playerBackpack_s) || {}; }
          else if(d.playerBackpack && typeof d.playerBackpack === 'object'){ _bag = d.playerBackpack; }
        }catch(_){ _bag = (d.playerBackpack && typeof d.playerBackpack === 'object') ? d.playerBackpack : {}; }
        var _leftBase = parseInt(_bag['summon_ticket_ur_pick'], 10) || 0;
        var _leftGm   = parseInt(_bag['gm_summon_ticket_ur_pick'], 10) || 0;
        var _left     = _leftBase + _leftGm;
        // 應該還剩幾張 = 已領額度 − 已用次數；比實際少的部分＝該扣沒扣的「幽靈券」
        var _shouldLeft = Math.max(0, _ticketQuota - _usedReal);
        var _ghost      = Math.max(0, _left - _shouldLeft);
        var _used       = _usedReal;   // 對外欄位改為帳本實查值（舊版是減法推算）

        // ════════════════════════════════════════════════════════════════
        // ★ v5.93.0 —「短少」判定（★ 舊版與本輪第一版都完全算不出來，會讓吃虧的學生
        //   整批從名單上消失。老師 2026-08-20 連續三個實例即為此類）
        // ────────────────────────────────────────────────────────────────
        //   ① 型態 C：GM 直接發過、帳本也入帳了，現在卻不在持有清單 → 被刪掉了
        //      逐隻附上刪除時間 / reason / deletedBy，並依裁定 1 乙標「疑似誤收」
        //      （入帳後 _UR_MISDELETE_MS 內被 admin_delete）。★ 只標示，不自動救回。
        //   ② 型態 B2：券已領到、卻既沒用掉也不在背包 → 券憑空消失，學生吃虧
        //      （B1「還沒領」由 _unclaimedTk 分流，不算短少）
        // ════════════════════════════════════════════════════════════════
        var _ownedSet = {};
        _owned.forEach(function(n){ _ownedSet[n] = 1; });
        var _lostHeroes   = [];    // 型態 C ：老師直接發的 UR 不在了 → 補發同名英雄
        var _lostTkHeroes = [];    // ★ v5.95.0 型態 C2：券換取的 UR 不在了 → 補發召喚卷（老師裁定）
        var _compensatedLost = []; // ★ v5.95.0 已補償過、不再列入待處理的（UI 仍顯示一行讓老師知道）
        try{
          // ★★ v5.95.0【老師狀況 D】候選名單擴為「帳本曾經入帳過的所有 UR」★★
          //   舊版候選只收兩種：gmGiftLog 記過的 ∪ 帳本 _urSrcCat==='gm' 的。
          //   而券換來的英雄帳本 source='ticket_ur_pick' → _urSrcCat 判為 'summon' ⇒ 永遠不進候選
          //   ⇒「學生用券換到 UR、後來這隻不見了」整類從來沒被偵測過：
          //     券的帳（用 1 剩 0）完全平、英雄卻憑空少一隻 → 舊表判「帳目吻合」。
          //   ★ 補償方式不同，故分流兩個型態：gm→補同名英雄／ticket→補召喚卷。
          var _everMap = {};   // name -> 'gm' | 'ticket'
          _grantedHeroes.forEach(function(n){ _everMap[n] = 'gm'; });
          _urHistAll.forEach(function(x){
            if(_UR_REMOVED_SRC[x.source]) return;
            if(_urSrcCat(x.source) === 'gm'){ _everMap[x.name] = 'gm'; return; }
            if(_UR_TICKET_SRC[x.source] && _everMap[x.name] !== 'gm') _everMap[x.name] = 'ticket';
          });
          // ★★ v5.95.0 冪等：已經補償過的不再列入待處理 ★★
          //   ⚠ 型態 C2「券換取的 UR 不見了」補的是召喚卷，那隻英雄本身**不會回來**
          //     → 下次掃描帳本上它仍然「曾入帳、現在不在」⇒ 會再列一次、老師再按一次
          //     ⇒ 重複補發。（型態 B2 補券後 quota 與 left 同時 +N 自然抵銷，天生冪等；
          //        型態 C 補同名英雄後人就回到 ownedSet 了，也天生冪等 —— 只有 C2 需要名單。）
          var _compSet = {};
          try{
            (Array.isArray(d._urAuditCompensated) ? d._urAuditCompensated : [])
              .forEach(function(x){ _compSet[String(x)] = 1; });
          }catch(_){}
          Object.keys(_everMap).forEach(function(n){
            if(_ownedSet[n]) return;                       // 還在他身上 → 沒事
            if(_compSet[n]){ _compensatedLost.push(n); return; }   // 老師已補償過 → 不再列
            var _org = _everMap[n];
            var _rec = _lastRecOf[n];
            var _item;
            if(!_rec){
              // 曾經發過但帳本完全查不到入帳紀錄 → 發放當下就沒進去
              _item = { name:n, origin:_org, at:0, grantAt:0, aliveMs:-1,
                        source:'(帳本查無入帳紀錄)', reason:'', by:'', misdelete:0, vanish:0, noRec:1 };
            } else if(!_UR_REMOVED_SRC[_rec.source]){
              // ★★ v5.95.0【老師狀況 F 缺口①】帳本有入帳、最後一筆不是移除類、人卻不在持有清單 ★★
              //   舊版走到這裡直接 return 靜默略過 → 這正是「雲端合併吃掉／存檔遺失」的典型，
              //   學生吃虧卻完全查不出來。改為列入短少，並與 admin_delete 明確區分標示。
              _item = { name:n, origin:_org, at:_rec.at, grantAt:_rec.at, aliveMs:-1,
                        source:_rec.source, reason:'', by:'', misdelete:0, vanish:1, noRec:0 };
            } else {
              // 找「被刪之前最後一筆入帳紀錄」→ 算存活時間
              var _grantAt = 0;
              for(var _q = _urHistAll.length - 1; _q >= 0; _q--){
                var _e = _urHistAll[_q];
                if(_e.name !== n) continue;
                if(_e.at >= _rec.at) continue;
                if(!_UR_REMOVED_SRC[_e.source]){ _grantAt = _e.at; break; }
              }
              var _alive = (_grantAt && _rec.at) ? (_rec.at - _grantAt) : -1;
              _item = {
                name: n, origin: _org,
                at: _rec.at,
                grantAt: _grantAt,
                aliveMs: _alive,
                source: _rec.source,
                reason: String(_rec.reason || ''),
                by: String(_rec.deletedBy || _rec.by || ''),
                // ★ 裁定 1 乙：入帳後 10 分鐘內就被刪 → 疑似自動回收工具誤收
                misdelete: (_alive >= 0 && _alive <= _UR_MISDELETE_MS) ? 1 : 0,
                vanish: 0, noRec: 0
              };
            }
            if(_org === 'ticket') _lostTkHeroes.push(_item); else _lostHeroes.push(_item);
          });
          var _sortLost = function(a, b){ return (b.at || 0) - (a.at || 0); };
          _lostHeroes.sort(_sortLost);
          _lostTkHeroes.sort(_sortLost);
        }catch(_){}
        // 型態 B2：已領到的券，既沒用掉也不在背包
        var _lostTickets = Math.max(0, _ticketQuota - _usedReal - _left);
        var _shortage    = _lostHeroes.length + _lostTkHeroes.length + _lostTickets;   // 少拿

        // 對外差額：雙向（正＝多拿、負＝少拿）。★ 兩者可能同時存在，故另附明細欄位。
        var _diff     = _surplus - _shortage;
        var _expected = _actual - _surplus + _lostHeroes.length + _lostTkHeroes.length;

        // ── (f) 型態標籤（一列可同時命中多型態）──
        var _flags = [];
        if(_surplus > 0)                 _flags.push('A_surplus');      // 多拿 UR
        if(_ghost > 0)                   _flags.push('A_ghost');        // 幽靈券（還能再被用一次）
        if(_unclaimedTk > 0)             _flags.push('B1_unclaimed');   // 券還在收件箱沒領（正常）
        if(_lostTickets > 0)             _flags.push('B2_lostTicket');  // 券憑空消失
        if(_lostHeroes.length > 0)       _flags.push('C_lostHero');     // GM 發過卻不在了
        if(_lostTkHeroes.length > 0)     _flags.push('C2_lostTkHero');  // ★ v5.95.0 券換取的 UR 不在了
        if(_lostHeroes.concat(_lostTkHeroes).some(function(x){ return x.misdelete; })) _flags.push('C_misdelete');
        if(_unknownOwned.length > 0)     _flags.push('A_unknown');      // 查無來源

        // ════════════════════════════════════════════════════════════════
        // ★★ v5.97.0（老師需求）— 同一次掃描、同一列，一併對帳「💠 自選至寶」★★
        //   ⚠ 零額外 Firestore 讀取：至寶要用的 taiwanTreasureData / _treasureUnlockHistory /
        //     playerBackpack / 三個 claimLog 全都在同一份 player 主檔 `d` 裡面，
        //     所以 900 人的稽核成本與只查 UR 時完全相同（仍然只有 2 次 collection 全掃）。
        //   ⚠ 刻意不另寫一支 scan 函式（那會變成第二套口徑＋第二次全掃，
        //     正是 v5.92.0 _pageLabel／_pageLabel2 的教訓）。
        // ════════════════════════════════════════════════════════════════
        var _assets = {};
        try{
          var _KT = null;
          for(var _ki = 0; _ki < (window._LXPS_ASSET_KINDS || []).length; _ki++){
            if(window._LXPS_ASSET_KINDS[_ki] && window._LXPS_ASSET_KINDS[_ki].key === 'treasure'){
              _KT = window._LXPS_ASSET_KINDS[_ki]; break;
            }
          }
          if(_KT && typeof window._lxpsAssetAuditOne === 'function'){
            _assets.treasure = window._lxpsAssetAuditOne(
              d, _uid,
              { ticketN: (_gift.treTicketN || 0), recs: (_gift.recs || []).map(function(g){
                  return { rewardId: g.rewardId, ticketN: (g.treTicketN || 0), at: g.at };
                }) },
              _KT
            );
          }
        }catch(_eAst){ try{ console.warn('[至寶對帳] uid=' + String(_uid).slice(0,8), _eAst); }catch(_){} }
        var _treA     = _assets.treasure || null;
        var _treFlags = (_treA && _treA.flags) ? _treA.flags : [];
        var _treBad   = _treFlags.some(function(f){ return f !== 'B1_unclaimed'; });

        // 完全無關者略過（UR 與至寶都沒東西、沒發過券、沒剩券、沒任何異常）
        if(!_actual && !_gift.ticketN && !_grantedHeroes.length && !_left && !_flags.length
           && !(_treA && _treA.relevant)) return;

        // 召喚時間與結果：★ v5.93.0 改為複用 (a) 已解析排序好的 _urHistAll（同一份真相，不重複解析）
        //   每筆另標 kind（gm/ticket/removed/unknown）供 UI 上色，並標出「這一筆有沒有券可用」。
        var _tkSeq = 0;
        var _hist = _urHistAll.map(function(x){
          // ★★ v5.95.0 — 根治：原寫 _UR_GM_SRC[x.source]，但 `_UR_GM_SRC` 全檔從未定義 ★★
          //   （與 v5.94.0 的 _diffColor 同源＝v5.93.0 改版時把定義刪了、使用點沒清乾淨。）
          //   ⚠ 這一行在 _urHistAll.map 內，per-player 處理整段包在 try/catch：
          //     → 只要該生「有任何 UR 取得紀錄」就在此拋 ReferenceError、被靜默吞掉、
          //       _rows.push 永遠執行不到 ⇒ 該生完全不出現在稽核表。
          //     ⇒ 比整表全滅更陰險：只有真正需要處理的人被篩掉，剩下的看起來一切正常。
          //   ★ 修法刻意不補一張 _UR_GM_SRC 白名單，改走既有單一真相 _urSrcCat
          //     —— 這正是 v5.93.0 自己訂下的鐵則（「刻意不自建第二張白名單」＝
          //        v5.92.0 _pageLabel／_pageLabel2 的教訓），這兩行等於違反了它。
          var _kind = _UR_REMOVED_SRC[x.source] ? 'removed'
                    : ((_urSrcCat(x.source) === 'gm') ? 'gm'
                    : (_UR_TICKET_SRC[x.source] ? 'ticket' : 'unknown'));
          var _seq = 0, _over = false;
          if(_kind === 'ticket'){ _tkSeq++; _seq = _tkSeq; _over = (_tkSeq > _ticketQuota); }
          return { name:x.name, at:x.at, source:x.source, uid12:x.uid12,
                   kind:_kind, tkSeq:_seq, overQuota:_over };
        });

        // 英雄培育情形：等級 / 素質投資 / 技能等級 / 爆發等級（判斷刪掉會不會很傷）
        var _lv = (d.heroLevels && typeof d.heroLevels === 'object') ? d.heroLevels : {};
        var _inv = (d.heroStatInvested && typeof d.heroStatInvested === 'object') ? d.heroStatInvested : {};
        var _sk = (d._heroSkillLevels && typeof d._heroSkillLevels === 'object') ? d._heroSkillLevels : {};
        var _bl = (d._heroBurstLevels && typeof d._heroBurstLevels === 'object') ? d._heroBurstLevels : {};
        var _ex = (d.heroExp && typeof d.heroExp === 'object') ? d.heroExp
                : ((d._heroExp && typeof d._heroExp === 'object') ? d._heroExp : {});
        // ★ v5.91.0 — 極限膠囊投資（獨立於自由素質點，1 顆 = +1，刪英雄時要原數退回）
        var _cap = (d.heroCapsuleInvested && typeof d.heroCapsuleInvested === 'object') ? d.heroCapsuleInvested : {};
        var _detail = _owned.map(function(n){
          var _iv = _inv[n] || {};
          var _ivTotal = (_iv.hp||0) + (_iv.atk||0) + (_iv.sp||0) + (_iv.spd||0);
          var _skv = _sk[n] || {};
          var _cv = _cap[n] || {};
          var _capObj = {
            hp: parseInt(_cv.hp, 10) || 0,
            atk: parseInt(_cv.atk, 10) || 0,
            sp: parseInt(_cv.sp, 10) || 0,
            spd: parseInt(_cv.spd, 10) || 0
          };
          var _capTotal = _capObj.hp + _capObj.atk + _capObj.sp + _capObj.spd;
          var _rec = _lastRecOf[n] || null;
          var _lvN = parseInt(_lv[n], 10) || 1;
          var _restExp = parseInt(_ex[n], 10) || 0;
          // ★ v5.93.0 — 逐隻歸因：這一隻到底憑什麼在他身上
          var _org = _originOf[n] || 'unknown';
          var _tkIdx = (_org === 'ticket') ? (_ticketOwned.indexOf(n) + 1) : 0;   // 第幾隻用券取得
          return {
            name: n,
            lv: _lvN,
            exp: _restExp,
            totalExp: (typeof window._lxpsHeroTotalExp === 'function') ? window._lxpsHeroTotalExp(_lvN, _restExp) : 0,
            invested: _ivTotal,
            capsule: _capObj,
            capsuleTotal: _capTotal,
            s1: parseInt(_skv.s1, 10) || 0,
            s2: parseInt(_skv.s2, 10) || 0,
            burst: parseInt(_bl[n], 10) || 0,
            at: _rec ? _rec.at : 0,
            source: _rec ? _rec.source : '(查無取得紀錄)',
            byGmDirect: (_gift.heroes[n] ? 1 : 0),
            // ★ v5.93.0 新增
            origin: _org,                                   // 'gm' | 'ticket' | 'unknown'
            // ★ v5.95.0 — 同上：_UR_GM_SRC 未定義 → 改走單一真相 _urSrcCat。
            //   本行在 _owned 的 map 內 ⇒ 只要該生「持有任何 UR」就會拋錯被吞掉。
            byGmLedger: (_rec && _urSrcCat(_rec.source) === 'gm') ? 1 : 0,   // 帳本認證的 GM 給予（gmGiftLog 沒記也算）
            tkIndex: _tkIdx,                                // 第幾隻用券取得（依時間）
            tkQuota: _ticketQuota,
            overQuota: (_org === 'ticket' && _tkIdx > _ticketQuota) ? 1 : 0,   // 超出券額度
            suspect: (_org === 'unknown' || (_org === 'ticket' && _tkIdx > _ticketQuota)) ? 1 : 0
          };
        });
        // 培育投入量（供 GM 決定補償多寡）
        var _effort = 0;
        _detail.forEach(function(x){ _effort += (x.lv - 1) + x.invested + x.s1 + x.s2 + x.burst; });

        _rows.push({
          uid: _uid,
          email: String(d.email || '').toLowerCase(),
          displayName: String(d.displayName || d.displayLabel || ''),
          granted: _gift.ticketN,
          left: _left,
          used: _used,
          gmHeroes: _grantedHeroes,
          expected: _expected,
          actual: _actual,
          diff: _diff,
          owned: _owned,
          detail: _detail,
          history: _hist,
          giftRecs: _gift.recs.sort(function(a, b){ return (a.at || 0) - (b.at || 0); }),
          effort: _effort,
          // ★ v5.93.0 新增：讓 GM 看到的每個數字都講得出出處
          ticketQuota: _ticketQuota,       // 學生實際領到的券張數（額度）
          quotaFromLog: _quotaFromLog ? 1 : 0,   // 額度是否來自領取紀錄（0＝查無紀錄，已保守退回發出數）
          pendingTk: _pendingTk,           // 老師發了但查無領取紀錄的券
          unclaimedTk: _unclaimedTk,       // ★ 型態 B1：還在收件箱沒按「確認領取」（正常，不必處理）
          usedReal: _usedReal,             // 帳本實查用券次數
          leftBase: _leftBase,             // 背包 summon_ticket_ur_pick（換券殘留）
          leftGm: _leftGm,                 // 背包 gm_summon_ticket_ur_pick（老師發的）
          shouldLeft: _shouldLeft,         // 依帳本推算「應該還剩」幾張
          ghost: _ghost,                   // ★ 幽靈券＝該扣沒扣的張數（要收回）
          gmOwned: _gmOwned,               // 歸因：GM 給的
          ticketOwned: _ticketOwned,       // 歸因：用券換的（依時間排序）
          unknownOwned: _unknownOwned,     // 歸因：查無來源
          overTicket: _overTicket,         // 超出券額度的隻數
          surplus: _surplus,               // 多拿總數（型態 A）
          shortage: _shortage,             // 少拿總數（型態 B2 + C）
          lostHeroes: _lostHeroes,         // ★ 型態 C：GM 發過卻不在了（含刪除時間/reason/疑似誤收旗標）
          lostTkHeroes: _lostTkHeroes,     // ★ v5.95.0 型態 C2：券換取的 UR 不在了（補發召喚卷）
          compensatedLost: _compensatedLost, // ★ v5.95.0 已補償過 → 不再列入待處理（防重複補發）
          lostTickets: _lostTickets,       // ★ 型態 B2：券憑空消失張數
          // ★ v5.95.0 額度查證欄位（讓 UI 說得出「為什麼是這個數字」）
          claimedRaw: _claimedRaw,         // 夾制前的原始領取筆數合計
          quotaCapped: _quotaCapped,       // 被甲式夾制夾掉幾張（>0＝領取紀錄比發放紀錄多，需明示）
          quotaAdj: _quotaAdj,             // 稽核補發的額度調整（_urQuotaAdjust·乙裁定獨立欄位）
          recoverSkipped: _recoverSkipped, // 被濾掉的「系統補發漏領」紀錄筆數
          claimDupSkipped: _dupSkipped,    // ★ v5.124.0 ⑤ 被備援鍵去重掉的重複領取紀錄筆數
          coverGap: _coverGap,             // ★ v5.124.0 甲案 查無對應發放文件的領取筆數（>0＝本列不夾制）
          flags: _flags,                   // 型態標籤陣列（★ UR 的；至寶的在 assets.treasure.flags）
          // ★★ v5.97.0 — 資產分區結果。UR 欄位刻意維持攤平在頂層（既有 UI／既有補發鈕零改動），
          //   至寶放在 assets.treasure；日後 UR 遷移到泛化引擎時再一併收進 assets.ur。
          assets: _assets,
          treBad: _treBad ? 1 : 0,         // 至寶有無「需處理」旗標（B1 未領取不算）
          // ★★ v5.97.0（老師需求）— 管理員對這位學生做過的處理紀錄（時間＋動作＋操作者）★★
          //   ⚠ 就在同一份 player 主檔內 ⇒ 零額外 Firestore 讀取量。
          //   ⚠ 解析走 admin_panel.js 的通用層單一真相；未載入時回空陣列（不阻斷稽核）。
          gmLog: (function(){
            try{
              if(typeof window._gmActionLogOf === 'function') return window._gmActionLogOf(d, 30);
            }catch(_){}
            return [];
          })()
        });
      }catch(_){}
    });

    // ★ v5.93.0 — 排序改依「異常嚴重度」而非單一 diff：
    //   多拿(surplus) → 幽靈券(ghost，還能再被用一次的未爆彈) → 少拿(shortage，學生吃虧)
    //   ⚠ 舊版只看 diff，且 diff 恆為「多拿」方向 → 吃虧的學生永遠排在最後甚至被過濾掉。
    //   ★ v5.97.0 — 至寶的異常一併計入嚴重度，否則「UR 乾淨但至寶出事」的學生會排到最後面。
    var _sev = function(r){
      var _t = (r.assets && r.assets.treasure) ? r.assets.treasure : null;
      var _s = (r.surplus || 0) * 1000 + (r.ghost || 0) * 100 + (r.shortage || 0) * 10;
      if(_t) _s += (_t.surplus || 0) * 1000 + (_t.ghost || 0) * 100 + (_t.shortage || 0) * 10;
      return _s;
    };
    _rows.sort(function(a, b){
      var _d = _sev(b) - _sev(a);
      if(_d) return _d;
      return b.actual - a.actual;
    });
    try{
      var _abn = _rows.filter(function(r){
        return (r.flags || []).some(function(f){ return f !== 'B1_unclaimed'; }) || r.treBad;
      }).length;
      var _abnT = _rows.filter(function(r){ return !!r.treBad; }).length;
      console.log('[_fbAdminScanURTicketAudit v5.97.0] 掃描 ' + _scanned + ' 名玩家 / 送禮記錄 ' + _giftDocs + ' 筆 → 相關 '
        + _rows.length + ' 名，需處理 ' + _abn + ' 名（B1 未領取不計；其中至寶 ' + _abnT + ' 名）'
        + '／已排除管理員 ' + _adminSkipped + ' 位');
    }catch(_){}
    // ★ v5.96.0 — adminSkipped 一併回傳，UI 明示「已排除管理員 N 位」（不靜默篩掉）
    // ★ v5.124.0 甲案 — 兩個新增發放來源的統計與失敗警告一併回傳，UI 明示（讀不到絕不靜默）
    return { scanned:_scanned, giftDocs:_giftDocs, rows:_rows, adminSkipped:_adminSkipped,
             extraGrant:_extraGrantStat };
  };

  //   掃全體:把所有玩家身上「無真實解鎖紀錄的 compensation 英雄」依台灣日期聚合成批次。
  //   回傳:{ scanned, batches:[{date, ownerCount, totalHeroes, owners:[{uid,email,displayName,heroes:[{name,lv}]}]}] }(日期新→舊)
  window._fbAdminScanAllCompBatches = async function(){
    var snap = await getDocs(collection(_fbDb, 'players'));
    var _scanned = 0;
    var _batchMap = {};   // dateStr → { date, owners:[], totalHeroes }
    snap.forEach(function(docSnap){
      _scanned++;
      try{
        var d = docSnap.data() || {};
        var _r = _computeCompBatchesFromDoc(d, docSnap.id);
        var _bd = (_r && _r.byDate) || {};
        Object.keys(_bd).forEach(function(dk){
          var _list = _bd[dk] || [];
          if(!_list.length) return;
          if(!_batchMap[dk]) _batchMap[dk] = { date:dk, owners:[], totalHeroes:0 };
          _batchMap[dk].owners.push({
            uid: docSnap.id,
            email: (d.email || '').toLowerCase(),
            displayName: (d.displayName || d.displayLabel || ''),
            heroes: _list.slice()
          });
          _batchMap[dk].totalHeroes += _list.length;
        });
      }catch(_){}
    });
    var _batches = Object.keys(_batchMap).sort().reverse().map(function(k){
      var b = _batchMap[k];
      b.ownerCount = b.owners.length;
      return b;
    });
    console.log('[_fbAdminScanAllCompBatches] 掃描 ' + _scanned + ' 名,補償批次 ' + _batches.length + ' 組:', _batches.map(function(b){ return b.date + '(' + b.ownerCount + '位/' + b.totalHeroes + '隻)'; }).join(' · '));
    return { scanned:_scanned, batches:_batches };
  };

  //   回收某玩家「某台灣日期」的 compensation 批次(權威重判 → 只收仍符合者 → _fbAdminBulkRemoveHeroes)
  window._fbAdminReclaimCompBatchForUid = async function(uid, dateStr, heroNames){
    if(!uid) throw new Error('缺少 uid');
    if(!dateStr) throw new Error('缺少日期');
    var _req = Array.isArray(heroNames) ? heroNames.filter(Boolean) : null;   // null = 收該日全部仍符合者
    // ★ 權威重判:讀最新雲端 doc,只收回「仍是該日 compensation 且無真實解鎖」者
    var _confirmed = [];
    try{
      var _snap = await getDocFromServer(doc(_fbDb, 'players', uid));
      var _d = (_snap && _snap.exists()) ? (_snap.data() || {}) : {};
      var _rr = _computeCompBatchesFromDoc(_d, uid);
      var _list = (_rr && _rr.byDate && _rr.byDate[dateStr]) ? _rr.byDate[dateStr] : [];
      var _names = _list.map(function(x){ return x.name; });
      if(_req){ var _reqSet = {}; _req.forEach(function(n){ _reqSet[n] = 1; }); _confirmed = _names.filter(function(n){ return _reqSet[n]; }); }
      else { _confirmed = _names; }
    }catch(_e){ throw new Error('權威重判失敗:' + (_e && _e.message || _e)); }
    if(!_confirmed.length) return { ok:false, reason:'none_confirmed' };
    // 移除(寫三槽 + admin_delete 標記不復活 + 清養成 _s + 至寶解裝保留本體);Lv1/已練皆不補償
    var _hRes = null;
    if(typeof window._fbAdminBulkRemoveHeroes === 'function'){
      _hRes = await window._fbAdminBulkRemoveHeroes(uid, _confirmed, { reason:'GM 補償批次回收(' + dateStr + '·無真實解鎖紀錄·含已練)' });
    } else { throw new Error('_fbAdminBulkRemoveHeroes 未載入(請確認 index.html 版本)'); }
    // 更正通知(玩家登入彈窗)
    try{
      if(typeof window._fbAdminSendNotificationToPlayer === 'function'){
        var _body = '老師發現先前的「帳號救援」因系統設定問題,把一些你原本沒有的英雄('
          + _confirmed.join('、') + ')誤補進了你的帳號。現在已經把這些誤發的英雄收回,讓你的圖鑑回到原本乾淨的狀態，你也可以重新用自選召喚卷抽你真正想要的英雄。'
          + '你自己抽到、解鎖過的英雄與所有進度都不受影響，造成困擾非常抱歉！';
        await window._fbAdminSendNotificationToPlayer(uid, { title:'🙇 老師的更正通知', body:_body, type:'comp_batch_reclaim', removedItems:_confirmed });
      }
    }catch(_){}
    console.info('[補償批次回收 v3.16.98] uid=' + String(uid).slice(0,8) + ' 日期=' + dateStr + ' 收回 ' + _confirmed.length + ' 隻:', _confirmed);
    return { ok:true, removedHeroes:_confirmed, skipped:(_req ? _req.filter(function(n){ return _confirmed.indexOf(n) < 0; }) : []) };
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.17.2(2026-06-30)— 補償批次回收「一鍵無損救回」(救回被 v3.16.98 補償批次回收
  //   誤收、但其實該保留的英雄)。
  //   背景:_fbAdminBulkRemoveHeroes 收回時把等級+5養成表+經驗整批 _clean 清掉,且未暫存
  //         原等級(不像 v3.16.19 會存 _auditRecoveredLevels)→ 直接用舊救回工具會還原成 Lv1。
  //   救星:回收只 updateDoc 主檔 players/{uid},完全沒碰三槽存檔(saves/live、saves/safe)→
  //         回收前的完整等級/養成大機率原封不動留在存檔槽 → 從那裡讀回 = 無損還原。
  //   ★ 鎖定條件:某英雄「最新一筆紀錄 = admin_delete 且 reason 含『補償批次回收』、且現已不在
  //     unlockedHeroes」→ 只救這次工具收掉的,不誤救正常手動刪/暴增收回的 admin_delete。
  //   ★ 還原後寫一筆 admin_grant(auditRestored)→ 最新一筆非 suspect →
  //     _computeCompBatchesFromDoc 的 realUnlock 永久保護,從此不會再被任何回收工具掃到。
  //   ★ 取捨:帳本當初沒寫真實解鎖紀錄 → 分不出「純污染 vs 正當取得」,一律救回
  //     (合「誤刪是大忌、保守漏收可接受」鐵律);少數連 safe 槽都被覆蓋者只還原本體(Lv1·會標示)。
  // ════════════════════════════════════════════════════════════════════
  //   讀某玩家「主檔 + saves/live + saves/safe」三槽,對每隻英雄取「等級最高」那一槽,
  //   整包回傳該英雄回收前的養成(等級/經驗/素質投資/素質點/技能/爆發/天賦)。
  window._fbReadHeroGrowthFromSaves = async function(uid, heroNames){
    var _names = Array.isArray(heroNames) ? heroNames.filter(Boolean) : [];
    var _out = {};
    if(!uid || !_names.length) return _out;
    var _GKEYS = ['heroLevels','heroExp','heroStatInvested','heroStatPoints','heroSkillLevels','heroBurstLevels','heroTraitLevel'];
    function _parse(d){
      var g = {};
      if(!d || typeof d !== 'object'){ _GKEYS.forEach(function(k){ g[k] = {}; }); return g; }
      _GKEYS.forEach(function(k){
        var v = (d[k] && typeof d[k] === 'object') ? d[k] : null;
        if((!v || !Object.keys(v).length) && typeof d[k+'_s'] === 'string'){ try{ var p = JSON.parse(d[k+'_s']); if(p && typeof p === 'object') v = p; }catch(_){} }
        g[k] = v || {};
      });
      return g;
    }
    var _slots = [];
    try{ var _m = await getDocFromServer(doc(_fbDb, 'players', uid)); if(_m && _m.exists()) _slots.push(_parse(_m.data() || {})); }catch(_){}
    try{ var _l = await getDocFromServer(doc(_fbDb, 'players', uid, 'saves', 'live')); if(_l && _l.exists()) _slots.push(_parse(_l.data() || {})); }catch(_){}
    try{ var _sf = await getDocFromServer(doc(_fbDb, 'players', uid, 'saves', 'safe')); if(_sf && _sf.exists()) _slots.push(_parse(_sf.data() || {})); }catch(_){}
    _names.forEach(function(n){
      var _best = null, _bestLv = 0;
      _slots.forEach(function(g){
        var _lv = (typeof g.heroLevels[n] === 'number' && g.heroLevels[n] >= 1) ? g.heroLevels[n] : 0;
        if(_lv > _bestLv){ _bestLv = _lv; _best = g; }
      });
      if(_best && _bestLv >= 1){
        _out[n] = {
          lv:      _bestLv,
          exp:     (typeof _best.heroExp[n] === 'number') ? _best.heroExp[n] : null,
          statInv: (_best.heroStatInvested[n] != null) ? _best.heroStatInvested[n] : null,
          statPts: (_best.heroStatPoints[n] != null) ? _best.heroStatPoints[n] : null,
          skillLv: (_best.heroSkillLevels[n] != null) ? _best.heroSkillLevels[n] : null,
          burstLv: (_best.heroBurstLevels[n] != null) ? _best.heroBurstLevels[n] : null,
          traitLv: (_best.heroTraitLevel[n] != null) ? _best.heroTraitLevel[n] : null,
          hasGrowth: true
        };
      } else {
        _out[n] = { lv:1, hasGrowth:false };
      }
    });
    return _out;
  };

  //   每隻英雄「最新一筆紀錄」工具(取 at 最大那筆 source/reason)。
  function _gmcrLatestHistMap(d){
    var _hist = Array.isArray(d._heroUnlockHistory) ? d._heroUnlockHistory : (function(){ try{ var p = JSON.parse(d._heroUnlockHistory_s || 'null'); return Array.isArray(p) ? p : []; }catch(_){ return []; } })();
    var _latest = {};
    _hist.forEach(function(e){
      if(!e || !e.name) return;
      var _at = Number(e.at) || 0;
      if(!_latest[e.name] || _at >= _latest[e.name]._at){ _latest[e.name] = { _at:_at, src:String(e.source || ''), reason:String(e.reason || '') }; }
    });
    return _latest;
  }
  // ★ v3.17.4 — 放寬救回判定:從「只認補償批次回收的 admin_delete」改成「任何 admin_delete 或 audit_error_recovered」
  //   → 一個工具涵蓋所有「被回收/刪除」途徑(補償批次回收 / 查無紀錄回收 / GM 手動刪污染 / 帳號重建移除幻影 /
  //     玩家端登入自動回收 / 學生圖鑑自審 disown),全部從存檔(saves/live·safe)還原原等級救回。
  //   符合鐵律「誤刪是大忌·漏收可接受」:寧可把當初該刪的污染也一起救回,也不漏救任何正當英雄。
  function _gmcrIsCompReclaimed(L){ return !!(L && (L.src === 'admin_delete' || L.src === 'audit_error_recovered')); }

  //   掃全體:列出「被補償批次回收誤收、現已不在 unlockedHeroes」的英雄,並從存檔讀回原等級。
  //   回傳:{ scanned, owners:[{uid,email,displayName,heroes:[{name,lv,hasGrowth}]}], total }
  window._fbAdminScanCompReclaimedHeroes = async function(){
    var snap = await getDocs(collection(_fbDb, 'players'));
    var _docs = []; snap.forEach(function(ds){ _docs.push(ds); });
    var _scanned = 0, _owners = [], _total = 0;
    for(var di = 0; di < _docs.length; di++){
      var ds = _docs[di]; _scanned++;
      try{
        var d = ds.data() || {};
        var _latest = _gmcrLatestHistMap(d);
        var _names = Object.keys(_latest);
        if(!_names.length) continue;
        var _cur = {}; (Array.isArray(d.unlockedHeroes) ? d.unlockedHeroes : []).forEach(function(n){ if(n) _cur[n] = 1; });
        var _cand = _names.filter(function(n){ return _gmcrIsCompReclaimed(_latest[n]) && !_cur[n]; });
        if(!_cand.length) continue;
        var _growth = await window._fbReadHeroGrowthFromSaves(ds.id, _cand);
        var _heroes = _cand.map(function(n){ var g = _growth[n] || { lv:1, hasGrowth:false }; return { name:n, lv:(g.lv || 1), hasGrowth:!!g.hasGrowth }; });
        _owners.push({ uid:ds.id, email:(d.email || '').toLowerCase(), displayName:(d.displayName || d.displayLabel || ''), heroes:_heroes });
        _total += _heroes.length;
      }catch(_){}
    }
    _owners.sort(function(a, b){ return (b.heroes.length) - (a.heroes.length); });
    console.log('[_fbAdminScanCompReclaimedHeroes] 掃 ' + _scanned + ' 位 → ' + _owners.length + ' 位有被補償批次回收誤收的英雄,共 ' + _total + ' 隻');
    return { scanned:_scanned, owners:_owners, total:_total };
  };

  //   對某玩家無損救回:權威重判 → 從存檔還原等級/養成(只升不降) → 寫 admin_grant 永久免疫 → 通知。
  window._fbAdminRestoreCompReclaimedForUid = async function(uid, heroNames){
    if(!uid) throw new Error('缺少 uid');
    var _req = Array.isArray(heroNames) ? heroNames.filter(Boolean) : [];
    if(!_req.length) return { ok:false, reason:'no_heroes' };
    var _ref = doc(_fbDb, 'players', uid);
    var _snap = await getDocFromServer(_ref);
    if(!_snap || !_snap.exists()) return { ok:false, reason:'player_not_found' };
    var _d = _snap.data() || {};
    var _uid12 = String(uid).slice(0, 12);
    var _now = Date.now();
    var _adminEmail = (window._fbUser && window._fbUser.email) || 'admin';

    // 權威重判:只救「最新一筆仍是 admin_delete·補償批次回收·且不在 unlockedHeroes」者
    var _latest = _gmcrLatestHistMap(_d);
    var _curSet = {}; (Array.isArray(_d.unlockedHeroes) ? _d.unlockedHeroes : []).forEach(function(n){ if(n) _curSet[n] = 1; });
    var _confirmed = _req.filter(function(n){ return _gmcrIsCompReclaimed(_latest[n]) && !_curSet[n]; });
    if(!_confirmed.length) return { ok:false, reason:'none_confirmed' };

    // 從存檔讀回收前養成
    var _growth = await window._fbReadHeroGrowthFromSaves(uid, _confirmed);

    // ★★ v5.130.0(老師裁定甲)— 改走三槽同寫單一真相 ★★
    //   舊版只寫主檔 ⇒ live/safe 較新的 admin_delete 帳本讓救回的英雄又被 latest-wins 濾掉。
    //   養成 _growth 已是「三槽取最佳」的一份,逐槽套用時對該槽只做 max/補空(不降級不覆蓋)。
    var _GKPAIR = [['heroExp','exp'],['heroStatInvested','statInv'],['heroStatPoints','statPts'],
                   ['heroSkillLevels','skillLv'],['heroBurstLevels','burstLv'],['heroTraitLevel','traitLv']];
    var _restored = [], _lvLost = [];
    _confirmed.forEach(function(n){
      var g = _growth[n] || { lv:1, hasGrowth:false };
      if(!g.hasGrowth) _lvLost.push(n);
    });
    var _res130 = await window._lxpsAdminApplyAllSlots(uid, function(dd, slot){
      var _want = {};
      var _stashS = (dd._auditRecoveredLevels && typeof dd._auditRecoveredLevels === 'object') ? dd._auditRecoveredLevels : {};
      _confirmed.forEach(function(n){
        var g = _growth[n] || { lv:1 };
        _want[n] = Math.max((typeof g.lv === 'number' && g.lv >= 1) ? g.lv : 1,
                            (typeof _stashS[n] === 'number') ? _stashS[n] : 1);   // v5.130.0 起的暫存也納入
      });
      var P = window._lxpsSlotGrantHeroesPatch(dd, _confirmed, {
        at:_now, by:_adminEmail, uid:uid, slot:slot, levels:_want,
        source:'admin_grant',
        extra:{ auditRestored:true, restoredBy:_adminEmail },
        reason:'補償批次回收誤收·從存檔無損救回·永久保留'
      });
      if(!P) return null;
      // 其餘六張養成表:該槽現值優先,只補空/取較大
      _GKPAIR.forEach(function(pair){
        var k = pair[0], gk = pair[1];
        var o = Object.assign({}, _lxpsSlotObj(dd, k) || {});
        var hit = false;
        _confirmed.forEach(function(n){
          var g = _growth[n] || {};
          if(!g.hasGrowth || g[gk] == null) return;
          if(k === 'heroExp'){
            var nv = (typeof o[n] === 'number') ? Math.max(o[n], g[gk]) : g[gk];
            if(nv !== o[n]){ o[n] = nv; hit = true; }
          } else if(o[n] == null){ o[n] = g[gk]; hit = true; }
        });
        if(!hit) return;
        P[k] = o;
        try{ P[k+'_s'] = JSON.stringify(o); }catch(_){}
      });
      if(slot === 'main') _restored = _confirmed.slice();
      return P;
    }, { label:'補償誤收無損救回' });

    try{
      if(typeof window._fbAdminSendNotificationToPlayer === 'function'){
        var _body = '老師之前因系統設定問題,誤把一些你原本就有的英雄(' + _restored.join('、')
          + ')收走了,非常抱歉!現在已經把它們和原本的等級一起還給你了,你的其他進度都不受影響。請登出後重新登入即可看到。';
        await window._fbAdminSendNotificationToPlayer(uid, { title:'🙇 老師的道歉與英雄還原通知', body:_body, type:'comp_reclaim_restore', restoredItems:_restored });
      }
    }catch(_){}

    console.info('[補償批次回收·救回 v3.17.2] uid=' + String(uid).slice(0, 8) + ' 還原 ' + _restored.length + ' 隻:', _restored, _lvLost.length ? ('·存檔無料只能Lv1:' + _lvLost.join('、')) : '');
    return { ok:true, restored:_restored, lvLost:_lvLost };
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v3.16.86(老師裁示 2026-06-29·①乙②乙③甲)— 過度補回「派發學生自我審核」(拉取制)
  // ────────────────────────────────────────────────────────────────────
  //   掃全體卡的「回收」改為:GM 不直接刪,而是把過度補回的英雄/至寶清單(名+等級)寫成一筆
  //   pendingAdminNotifications/{uid}/items/{ts}(type='overRestoreReview')。學生下次登入彈
  //   「自我審核視窗」逐一勾「✅是我的(綁 UID 永久保留·player_confirmed)/🗑不是我的(可逆移除+補償)」。
  //   ★ 防刷:此集合只有 GM 能 create(firestore 規則),學生偽造不了清單;補償依「GM 記錄的等級」算,
  //          學生改本機等級也沒用。★ 沿用既有 pendingAdminNotifications 集合與規則 → 免新增 firestore 規則。
  //   ★ GM 逐項審查彈窗(_fbShowOverRestoredAudit)保留(①乙·GM 自己很確定時仍可直接刪/救回·備用)。
  // ════════════════════════════════════════════════════════════════════
  window._fbAdminQueueOverRestoreReview = async function(uid, heroes, treasures){
    if(!uid) throw new Error('缺少 uid');
    var _h = Array.isArray(heroes) ? heroes : [];
    var _t = Array.isArray(treasures) ? treasures : [];
    var _items = [];
    _h.forEach(function(x){ if(x && x.name) _items.push({ kind:'hero', key:String(x.name), name:String(x.name), lv:(typeof x.lv==='number'&&x.lv>0?x.lv:1) }); });
    _t.forEach(function(x){ if(x && x.id) _items.push({ kind:'tre', key:String(x.id), name:String(x.name||x.id), lv:0 }); });
    if(!_items.length) return { ok:false, reason:'no_items', heroCount:0, treCount:0 };
    var _ts = Date.now();
    var _adminEmail = (_fbAuth && _fbAuth.currentUser && _fbAuth.currentUser.email) || 'admin';
    var _ref = doc(_fbDb, 'pendingAdminNotifications', uid, 'items', String(_ts));
    await setDoc(_ref, {
      type:      'overRestoreReview',
      title:     '⚠️ 英雄／至寶歸屬確認',
      items:     _items,
      createdAt: _ts,
      createdBy: _adminEmail,
      read:      false
    });
    console.info('[過度補回派發自審 v3.16.86] uid=' + String(uid).slice(0,8) + ' 派發英雄' + _h.length + '隻/至寶' + _t.length + '件待學生確認');
    return { ok:true, heroCount:_h.length, treCount:_t.length, total:_items.length };
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v5.92.0（老師需求 2026-08-20）— UR 超額「派發學生自己確認並退回」
  // ────────────────────────────────────────────────────────────────────
  //   背景：v5.91.0 根治了「UR 券用掉之後沒有正確消失 → 同一張券被重複使用」的資產複製
  //   漏口，但那只防未來；已經到手的多餘 UR 不會自動收回。GM 端「🔎 UR 券／UR 角色 對帳
  //   稽核」可以直接刪，本函式提供**更尊重學生的第二條路**：把事實與應有數寫成一筆
  //   pendingAdminNotifications/{uid}/items/{ts}（type='urSurplusReview'），學生下次登入
  //   彈出確認視窗，自己勾選要退回哪幾隻（老師 2026-08-20 指示原文：親切理性的語氣，
  //   讓學生知道自己是退回本來就不該多得的珍貴獎勵）。
  //   ★ 沿用既有 pendingAdminNotifications 集合與規則 → 免新增 firestore 規則（同 v3.16.86）。
  //   ★ 防偽：此集合只有 GM 能 create，學生偽造不了清單；且退還的培育資源一律依
  //     **GM 派發當下記錄的培育快照**計算（heroes[].lv/exp/capsule/s1/s2/burst），
  //     學生改本機等級也沒用（口徑完全對齊 v3.16.86 的「補償依 GM 記錄的等級算」）。
  //   ⚠ 快照是派發當下的值：若學生在收到通知前又繼續練，退還會少算 → 老師可用既有
  //     「學生補償」工具補差額（刻意不改讀學生本機資料，那是可竄改來源）。
  //   payload：{ merit（獲得 UR 券的事蹟文字）, expected（應有幾隻）, surplus（要退幾隻）,
  //             autoRefund（預設 true＝學生送出當下自動退還培育資源）, heroes[]（培育快照） }
  // ════════════════════════════════════════════════════════════════════
  window._fbAdminQueueURSurplusReview = async function(uid, payload){
    if(!uid) throw new Error('缺少 uid');
    var _p = payload || {};
    var _heroes = [];
    var _src = Array.isArray(_p.heroes) ? _p.heroes : [];
    _src.forEach(function(x){
      if(!x || !x.name) return;
      var _c = (x.capsule && typeof x.capsule === 'object') ? x.capsule : {};
      _heroes.push({
        name:    String(x.name),
        lv:      Math.max(1, parseInt(x.lv, 10) || 1),
        exp:     Math.max(0, parseInt(x.exp, 10) || 0),
        capsule: {
          hp:  Math.max(0, parseInt(_c.hp,  10) || 0),
          atk: Math.max(0, parseInt(_c.atk, 10) || 0),
          sp:  Math.max(0, parseInt(_c.sp,  10) || 0),
          spd: Math.max(0, parseInt(_c.spd, 10) || 0)
        },
        s1:         Math.max(0, parseInt(x.s1, 10) || 0),
        s2:         Math.max(0, parseInt(x.s2, 10) || 0),
        burst:      Math.max(0, parseInt(x.burst, 10) || 0),
        invested:   Math.max(0, parseInt(x.invested, 10) || 0),
        byGmDirect: (x.byGmDirect ? 1 : 0),
        at:         (typeof x.at === 'number') ? x.at : 0
      });
    });
    if(!_heroes.length) return { ok:false, reason:'no_heroes' };
    var _expected = Math.max(0, parseInt(_p.expected, 10) || 0);
    var _actual   = _heroes.length;
    var _surplus  = parseInt(_p.surplus, 10);
    if(isNaN(_surplus) || _surplus <= 0) _surplus = _actual - _expected;
    if(_surplus <= 0) return { ok:false, reason:'no_surplus' };
    if(_surplus > _actual) _surplus = _actual;   // 保險：不可能要求退回比持有還多
    var _ts = Date.now();
    var _adminEmail = (_fbAuth && _fbAuth.currentUser && _fbAuth.currentUser.email) || 'admin';
    var _ref = doc(_fbDb, 'pendingAdminNotifications', uid, 'items', String(_ts));
    await setDoc(_ref, {
      type:       'urSurplusReview',
      title:      '👑 UR 角色數量確認',
      merit:      String(_p.merit || ''),
      expected:   _expected,
      actual:     _actual,
      surplus:    _surplus,
      autoRefund: (_p.autoRefund === false) ? false : true,
      heroes:     _heroes,
      gmNote:     String(_p.gmNote || ''),
      createdAt:  _ts,
      createdBy:  _adminEmail,
      read:       false
    });
    console.info('[UR 超額派發自審 v5.92.0] uid=' + String(uid).slice(0,8)
      + ' 應有' + _expected + '/實有' + _actual + ' → 請學生退回 ' + _surplus + ' 隻');
    return { ok:true, expected:_expected, actual:_actual, surplus:_surplus, heroCount:_actual };
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v5.99.0（老師 2026-08-21 需求）— 「老師發的自選召喚卷」學生自我確認通知
  // ────────────────────────────────────────────────────────────────────
  // 老師指示原文：「告訴學生：你曾經因為課堂表現優良獲得老師發送的 UR／至寶自選卷
  //   幾張，但是目前查到的持有數量不符，請到 GM 獎勵信箱或召喚星空做一次確認」
  //   並自陳五個選項（沒使用／用了沒拿到／用了券沒消失／沒拿到卷／其他說明）。
  //
  // 【與 v5.92.0 urSurplusReview 的分工】
  //   urSurplusReview ＝ 已確定超額 → 請學生**勾選退回**（會真的移除角色）。
  //   assetSelfCheck  ＝ 本函式 → **純問卷，不動任何資料**，只請學生回報現況。
  //   ⇒ 受影響人數多、且稽核數字對舊帳不一定準時，先問再處理才不會誤傷。
  //
  // 【為什麼只寄給「老師發過券」的人】
  //   老師裁定：「跟獎勵自選卷無關的不用通知」。呼叫端（admin_panel.js）以
  //   granted>0 || quota>0 篩選；本函式再擋一次（payload 內沒有任何一項有券 → 拒收），
  //   避免日後有人改了呼叫端就把整份名單寄出去。
  //
  // ★ 沿用既有 pendingAdminNotifications/{uid}/items/{ts} 集合與規則 ⇒ 零 rules 改動。
  // ════════════════════════════════════════════════════════════════════
  window._fbAdminQueueAssetSelfCheck = async function(uid, payload){
    if(!uid) throw new Error('缺少 uid');
    var _p = payload || {};
    var _assets = [];
    (Array.isArray(_p.assets) ? _p.assets : []).forEach(function(a){
      if(!a) return;
      var _kind = String(a.kind || '');
      if(_kind !== 'ur' && _kind !== 'treasure') return;
      var _granted = Math.max(0, parseInt(a.granted, 10) || 0);
      var _quota   = Math.max(0, parseInt(a.quota,   10) || 0);
      // ★ 守門：這項資產老師根本沒發過券 → 不寄（否則學生會收到看不懂的通知）
      if(_granted <= 0 && _quota <= 0) return;
      _assets.push({
        kind:    _kind,
        label:   String(a.label || (_kind === 'ur' ? '👑 UR 自選召喚卷' : '💠 自選至寶召喚卷')).slice(0, 40),
        granted: _granted,
        quota:   _quota,
        used:    Math.max(0, parseInt(a.used,   10) || 0),
        left:    Math.max(0, parseInt(a.left,   10) || 0),
        actual:  Math.max(0, parseInt(a.actual, 10) || 0),
        // ★ v5.99.0（老師修正①）— 稽核算出的短少數；學生選「用了沒拿到」時，
        //   學生端會即時據此告知「會補發」或「數量相符、本次不補發（可申訴）」。
        shortage: Math.max(0, parseInt(a.shortage, 10) || 0),
        flagText: String(a.flagText || '').slice(0, 120)
      });
    });
    if(!_assets.length) return { ok:false, reason:'no_ticket_asset' };
    // ★ v5.203.0（老師回報「填完還是一直跳」）— 寄送前兩道去重守門。
    //   後台那顆「📨 再寄一次（勾選者）」老師一定會整批再按一次（提示文字本來就這樣寫），
    //   而舊碼每按一次就寫一份新 id 的通知 ⇒ 已經填完的學生被重複問。
    //   ① 這位學生已經回答過 → 不再寄（reason:'already_answered'）
    //   ② 收件匣裡已經有一封還沒處理的同型通知 → 不再寄（reason:'already_queued'）
    //   ⚠ 讀取失敗一律放行照寄：寧可多寄一封，也不要因為網路問題漏掉真的該問的學生。
    //   ⚠ payload.force === true 可略過兩道守門（老師確定要再問同一位學生時用）。
    if(_p.force !== true){
      try{
        var _ansQ = await getDocs(query(collection(_fbDb, 'bugReports'), where('uid', '==', uid)));
        var _answered = false;
        _ansQ.forEach(function(d){ var v = d.data() || {}; if(String(v.page || '') === 'asset_self_check') _answered = true; });
        if(_answered) return { ok:false, reason:'already_answered' };
      }catch(_eAns){ console.warn('[召喚卷確認] 去重：查回覆失敗，照常寄出', _eAns); }
      try{
        var _pendQ = await getDocs(collection(_fbDb, 'pendingAdminNotifications', uid, 'items'));
        var _pending = false;
        _pendQ.forEach(function(d){
          var v = d.data() || {};
          if(v.type === 'assetSelfCheck' && v.read !== true) _pending = true;
        });
        if(_pending) return { ok:false, reason:'already_queued' };
      }catch(_ePend){ console.warn('[召喚卷確認] 去重：查收件匣失敗，照常寄出', _ePend); }
    }
    var _ts = Date.now();
    var _adminEmail = (_fbAuth && _fbAuth.currentUser && _fbAuth.currentUser.email) || 'admin';
    var _ref = doc(_fbDb, 'pendingAdminNotifications', uid, 'items', String(_ts));
    await setDoc(_ref, {
      type:      'assetSelfCheck',
      title:     '🎫 召喚卷確認',
      merit:     String(_p.merit || '').slice(0, 120),
      assets:    _assets,
      gmNote:    String(_p.gmNote || '').slice(0, 200),
      createdAt: _ts,
      createdBy: _adminEmail,
      read:      false
    });
    return { ok:true, assets:_assets.length };
  };

  // ════════════════════════════════════════════════════════════════════
  // ★ v5.93.0（老師 2026-08-20 裁定）— 回收「幽靈 UR 券」
  // ────────────────────────────────────────────────────────────────────
  // 幽靈券＝該扣沒扣、還留在背包裡的那幾張。它不是學生的合法資產，是 v5.91.0 之前
  //   「扣券沒上雲」留下的帳目錯誤 —— 不收回，同一張券還會被用第 N 次。
  //
  // ⚠⚠ 為什麼不能沿用既有的 _fbAdminClearAbnormalFruits（清除異常果實）⚠⚠
  //   那支只寫主檔的 playerBackpack，且**沒寫 playerBackpack_s** → 三槽合併與
  //   _applySafeData 都優先採信 _s 字串版 ⇒ 扣了等於沒扣；又不寫 live/safe ⇒ 殘槽撿回來；
  //   學生若在線，本機 _playerBackpack 還有券，下一次 autosave（v5.91.0 新加的
  //   _lxpsScheduleBackpackSync 反而讓它更快）就整包寫回三槽 ⇒ 當場復活。
  //
  // 【修法】比照 _fbStudentSurplusURRemove(v5.92.0) / _fbAdminGearDeleteForUid(v5.32.0)：
  //   ① 主檔 + saves/live + saves/safe **三槽各寫一次**
  //   ② 每槽 playerBackpack 與 playerBackpack_s **兩鍵同寫**（v3.15.57 洗錢口徑）
  //   ③ 每槽補一筆 ticketLedger 的 'revoke' 帳本（永久證據，可回溯）
  //   ④ 主檔蓋 _authoritativeRestoreAt → 在線學生下次存檔前會被導向乾淨重載，
  //      擋掉「本機殘留券整包寫回」那條路
  //   ⑤ 寄一封學生通知（老師指示：學生要收到通知）
  //
  // opts: { base:N, gm:N, reason:'', notify:true }
  //   base/gm 未指定時＝把該鍵目前的張數全部收回。
  // ════════════════════════════════════════════════════════════════════
  // ════════════════════════════════════════════════════════════════
  // ★★ v5.135.0 — GM 回收券時同步抽掉「券實例」（兩支回收工具共用）★★
  // ────────────────────────────────────────────────────────────────
  //   ⚠⚠ 不同步改 ticketData 的話，回收會形同沒做：
  //     學生下次登入 → _ticketReconcile 看到「背包 0 張、實例還有 N 張」
  //     → _ticketProjectBackpack 把背包數字又拉回 N 張（inst 是權威）→ 券從死裡復活。
  //   回收的 tid 一律 push 進 used 並 un +N：
  //     ① used → 殘槽 union 回來也會被 inst\used 剔除，永不復活
  //     ② un   → 抬高對帳分母，就算日後某個殘槽的背包數字仍是舊值，
  //       對帳器也不會把這幾張補成新實例（這是回收能不能持久的關鍵）
  //   ⚠ un 的語義是「歷來離開 inst 的張數」（對帳分母），不是「學生召喚過幾次」——
  //     後者請一律查 ticketLedger 的 ev:'use'。
  window._lxpsRevokeTicketInstInSlot = function(slotData, removedMap){
    try{
      if(!removedMap || typeof removedMap !== 'object') return null;
      var _td = null;
      try{ if(typeof slotData.ticketData_s === 'string' && slotData.ticketData_s){ var _p = JSON.parse(slotData.ticketData_s); if(_p && typeof _p === 'object' && !Array.isArray(_p)) _td = _p; } }catch(_e1){}
      if(!_td && slotData.ticketData && typeof slotData.ticketData === 'object' && !Array.isArray(slotData.ticketData)){
        try{ _td = JSON.parse(JSON.stringify(slotData.ticketData)); }catch(_e2){ _td = null; }
      }
      if(!_td) return null;                      // 該槽還沒有券實例資料（舊存檔）→ 不寫，下次登入自然對帳
      if(!_td.inst || typeof _td.inst !== 'object' || Array.isArray(_td.inst)) _td.inst = {};
      if(!Array.isArray(_td.used)) _td.used = [];
      if(!_td.un || typeof _td.un !== 'object' || Array.isArray(_td.un)) _td.un = {};
      Object.keys(removedMap).forEach(function(itemId){
        var _n = Math.max(0, parseInt(removedMap[itemId], 10) || 0);
        if(!_n) return;
        // 同券種的實例依取得時間由舊到新，先進先出（與玩家端消費同口徑）
        var _cand = [];
        Object.keys(_td.inst).forEach(function(tid){
          var r = _td.inst[tid];
          if(r && r.it === itemId) _cand.push({ tid: tid, at: (r.at || 0) });
        });
        _cand.sort(function(a, b){ return (a.at - b.at) || (a.tid < b.tid ? -1 : 1); });
        _cand.slice(0, _n).forEach(function(o){
          delete _td.inst[o.tid];
          if(_td.used.indexOf(o.tid) === -1) _td.used.push(o.tid);
        });
        _td.un[itemId] = (parseInt(_td.un[itemId], 10) || 0) + _n;   // ★ 分母只增不減
      });
      if(_td.used.length > 2000) _td.used = _td.used.slice(-2000);
      return _td;
    }catch(_e){ try{ console.warn('[_lxpsRevokeTicketInstInSlot]', _e); }catch(_){} return null; }
  };

  window._fbAdminRevokeURTickets = async function(uid, opts){
    if(!uid) throw new Error('缺少 uid');
    opts = opts || {};
    var _now = Date.now();
    var _uid12 = String(uid).slice(0, 12);
    var _adminEmail = (_fbAuth && _fbAuth.currentUser && _fbAuth.currentUser.email) || 'admin';
    var _KEYS = ['summon_ticket_ur_pick', 'gm_summon_ticket_ur_pick'];
    var _want = {
      'summon_ticket_ur_pick':    (opts.base == null) ? null : Math.max(0, parseInt(opts.base, 10) || 0),
      'gm_summon_ticket_ur_pick': (opts.gm   == null) ? null : Math.max(0, parseInt(opts.gm,   10) || 0)
    };
    var _gds = (typeof getDocFromServer === 'function') ? getDocFromServer : getDoc;
    var _targets = [
      { ref: doc(_fbDb, 'players', uid),                  label: '主檔', mustExist: true  },
      { ref: doc(_fbDb, 'players', uid, 'saves', 'live'), label: 'live', mustExist: false },
      { ref: doc(_fbDb, 'players', uid, 'saves', 'safe'), label: 'safe', mustExist: false }
    ];
    var _wrote = [], _removedTotal = { 'summon_ticket_ur_pick':0, 'gm_summon_ticket_ur_pick':0 };
    var _mainBefore = {}, _mainAfter = {};

    for(var i = 0; i < _targets.length; i++){
      var _t = _targets[i];
      var _snap = null;
      try{ _snap = await _gds(_t.ref); }catch(_e1){ try{ _snap = await getDoc(_t.ref); }catch(_e2){ _snap = null; } }
      var _ex = _snap && ((typeof _snap.exists === 'function') ? _snap.exists() : !!_snap.exists);
      if(!_ex){
        if(_t.mustExist) return { ok:false, reason:'player_not_found' };
        continue;   // live/safe 槽不存在是正常的
      }
      var _d = _snap.data() || {};
      // 讀該槽背包：_s 字串版優先（v3.15.57 洗錢口徑；map 欄位可能殘留 merge 幻影）
      var _bag = {};
      try{
        if(typeof _d.playerBackpack_s === 'string'){ _bag = JSON.parse(_d.playerBackpack_s) || {}; }
        else if(_d.playerBackpack && typeof _d.playerBackpack === 'object'){ _bag = Object.assign({}, _d.playerBackpack); }
      }catch(_){ _bag = (_d.playerBackpack && typeof _d.playerBackpack === 'object') ? Object.assign({}, _d.playerBackpack) : {}; }
      if(!_bag || typeof _bag !== 'object' || Array.isArray(_bag)) _bag = {};
      else _bag = Object.assign({}, _bag);

      var _thisRemoved = {}, _any = 0;
      _KEYS.forEach(function(k){
        var _cur = parseInt(_bag[k], 10) || 0;
        if(_cur <= 0){ _thisRemoved[k] = 0; return; }
        var _take = (_want[k] == null) ? _cur : Math.min(_want[k], _cur);
        if(_take <= 0){ _thisRemoved[k] = 0; return; }
        var _left = _cur - _take;
        if(_left <= 0) delete _bag[k]; else _bag[k] = _left;
        _thisRemoved[k] = _take; _any += _take;
        if(_t.label === '主檔'){ _mainBefore[k] = _cur; _mainAfter[k] = _left; }
      });
      if(!_any){ _wrote.push(_t.label + '(無券)'); continue; }
      if(_t.label === '主檔'){
        _removedTotal['summon_ticket_ur_pick']    = _thisRemoved['summon_ticket_ur_pick'] || 0;
        _removedTotal['gm_summon_ticket_ur_pick'] = _thisRemoved['gm_summon_ticket_ur_pick'] || 0;
      }
      // 券帳本：每槽補一筆 revoke（_recordTicketLedger 的 grant/use 之外的第三種事件，永久保留）
      var _led = Array.isArray(_d.ticketLedger) ? _d.ticketLedger.slice() : [];
      _KEYS.forEach(function(k){
        if(!_thisRemoved[k]) return;
        _led.push({
          ev: 'revoke', item: k, n: _thisRemoved[k], at: _now, uid: _uid12, slot: _t.label,
          by: _adminEmail,
          reason: String(opts.reason || '回收 v5.91.0 前「扣券未上雲」殘留的幽靈 UR 券(v5.93.0)')
        });
      });
      if(_led.length > 400) _led = _led.slice(-400);

      var _patch = {
        playerBackpack:   _bag,
        playerBackpack_s: JSON.stringify(_bag),
        ticketLedger:     _led
      };
      // ★ v5.135.0 — 同步抽掉券實例（不做的話學生下次登入會被對帳器把券拉回來）
      try{
        var _tdA = window._lxpsRevokeTicketInstInSlot(_d, _thisRemoved);
        if(_tdA){ _patch.ticketData = _tdA; _patch.ticketData_s = JSON.stringify(_tdA); }
      }catch(_eTdA){ console.warn('[UR 券回收] 券實例同步失敗(背包已扣・下次登入對帳會自行收斂)', _eTdA); }
      // ★ 主檔蓋 _authoritativeRestoreAt：在線學生會走乾淨重載，
      //   擋掉「本機殘留券被 autosave 整包寫回三槽」那條復活路徑。
      if(_t.label === '主檔') _patch._authoritativeRestoreAt = _now;
      await setDoc(_t.ref, _patch, { merge: true });
      _wrote.push(_t.label);
    }

    var _sum = (_removedTotal['summon_ticket_ur_pick'] || 0) + (_removedTotal['gm_summon_ticket_ur_pick'] || 0);
    if(!_sum) return { ok:true, removed:0, slots:_wrote, note:'no_ticket' };

    // ★ 學生通知（老師指示：學生要收到通知）
    if(opts.notify !== false){
      try{
        await setDoc(doc(_fbDb, 'pendingAdminNotifications', uid, 'items', String(_now + 1)), {
          type:      'urTicketRevoked',
          title:     '👑 UR 召喚卷的更正通知',
          n:         _sum,
          nBase:     _removedTotal['summon_ticket_ur_pick'] || 0,
          nGm:       _removedTotal['gm_summon_ticket_ur_pick'] || 0,
          gmNote:    String(opts.gmNote || ''),
          createdAt: _now + 1,
          createdBy: _adminEmail,
          read:      false
        });
      }catch(_eNote){ console.warn('[UR 券回收] 通知寫入失敗(券已回收·不影響結果)', _eNote); }
    }
    console.info('[UR 券回收 v5.93.0] uid=' + String(uid).slice(0,8) + ' 回收 ' + _sum
      + ' 張(base ' + (_removedTotal['summon_ticket_ur_pick']||0)
      + '/gm ' + (_removedTotal['gm_summon_ticket_ur_pick']||0) + ') 槽:' + _wrote.join('/'));
    return { ok:true, removed:_sum, detail:_removedTotal, before:_mainBefore, after:_mainAfter, slots:_wrote };
  };

  // ════════════════════════════════════════════════════════════════
  // ★ v5.132.0 — 泛化券回收 window._fbAdminRevokeBagItems(uid, keys, total, opts)
  // ────────────────────────────────────────────────────────────────
  //   供「🔎 單人獎勵查核」的直接處理區回收 自選至寶券／SSR 自選券／UR 券。
  //   與 _fbAdminRevokeURTickets(v5.93.0) 完全同一套三槽範式：
  //     三槽各寫一次 ＋ playerBackpack 與 _s 兩鍵同寫 ＋ 每槽補 ticketLedger
  //     的 ev:'revoke' 帳 ＋ 主檔蓋 _authoritativeRestoreAt（在線學生導向乾淨
  //     重載，擋掉本機殘留券被 autosave 整包寫回的復活路徑）。
  //   差異只有兩點：
  //     ① keys 參數化：依陣列順序「瀑布式」扣到 total 為止（呼叫端把最可疑的
  //        非 gm_ 鍵排前面＝v5.93.0 UI 既有口徑）；每槽獨立扣、槽內不足就扣光。
  //     ② 刻意不寄學生通知：登入通知迴圈只認 urTicketRevoked 等既有 type，
  //        亂寄新 type 學生端未必會渲染；本工具是老師當面手動處理用的，
  //        UI 已明示「不會另外通知學生」。
  // ════════════════════════════════════════════════════════════════
  window._fbAdminRevokeBagItems = async function(uid, keys, total, opts){
    if(!uid) throw new Error('缺少 uid');
    var _keys = (Array.isArray(keys) ? keys : [keys]).filter(function(k){ return !!k; });
    var _total = Math.max(0, parseInt(total, 10) || 0);
    if(!_keys.length || !_total) return { ok:false, reason:'bad_args' };
    opts = opts || {};
    var _now = Date.now();
    var _uid12 = String(uid).slice(0, 12);
    var _adminEmail = (_fbAuth && _fbAuth.currentUser && _fbAuth.currentUser.email) || 'admin';
    var _gds = (typeof getDocFromServer === 'function') ? getDocFromServer : getDoc;
    var _targets = [
      { ref: doc(_fbDb, 'players', uid),                  label: '主檔', mustExist: true  },
      { ref: doc(_fbDb, 'players', uid, 'saves', 'live'), label: 'live', mustExist: false },
      { ref: doc(_fbDb, 'players', uid, 'saves', 'safe'), label: 'safe', mustExist: false }
    ];
    var _wrote = [], _removedTotal = {}, _mainRemoved = 0;
    for(var i = 0; i < _targets.length; i++){
      var _t = _targets[i];
      var _snap = null;
      try{ _snap = await _gds(_t.ref); }catch(_e1){ try{ _snap = await getDoc(_t.ref); }catch(_e2){ _snap = null; } }
      var _ex = _snap && ((typeof _snap.exists === 'function') ? _snap.exists() : !!_snap.exists);
      if(!_ex){
        if(_t.mustExist) return { ok:false, reason:'player_not_found' };
        continue;   // live/safe 槽不存在是正常的
      }
      var _d = _snap.data() || {};
      // 讀該槽背包：_s 字串版優先（v3.15.57 洗錢口徑）
      var _bag = {};
      try{
        if(typeof _d.playerBackpack_s === 'string'){ _bag = JSON.parse(_d.playerBackpack_s) || {}; }
        else if(_d.playerBackpack && typeof _d.playerBackpack === 'object'){ _bag = Object.assign({}, _d.playerBackpack); }
      }catch(_){ _bag = (_d.playerBackpack && typeof _d.playerBackpack === 'object') ? Object.assign({}, _d.playerBackpack) : {}; }
      if(!_bag || typeof _bag !== 'object' || Array.isArray(_bag)) _bag = {};
      else _bag = Object.assign({}, _bag);

      var _remain = _total, _thisRemoved = {}, _any = 0;
      _keys.forEach(function(k){
        if(_remain <= 0) return;
        var _cur = parseInt(_bag[k], 10) || 0;
        if(_cur <= 0) return;
        var _take = Math.min(_remain, _cur);
        var _left = _cur - _take;
        if(_left <= 0) delete _bag[k]; else _bag[k] = _left;
        _thisRemoved[k] = _take; _any += _take; _remain -= _take;
      });
      if(!_any){ _wrote.push(_t.label + '(無券)'); continue; }
      if(_t.label === '主檔'){ _removedTotal = _thisRemoved; _mainRemoved = _any; }

      var _led = Array.isArray(_d.ticketLedger) ? _d.ticketLedger.slice() : [];
      Object.keys(_thisRemoved).forEach(function(k){
        _led.push({
          ev: 'revoke', item: k, n: _thisRemoved[k], at: _now, uid: _uid12, slot: _t.label,
          by: _adminEmail,
          reason: String(opts.reason || '單人獎勵查核 GM 回收多出來的券(v5.132.0)')
        });
      });
      if(_led.length > 400) _led = _led.slice(-400);

      var _patch = {
        playerBackpack:   _bag,
        playerBackpack_s: JSON.stringify(_bag),
        ticketLedger:     _led
      };
      // ★ v5.135.0 — 同步抽掉券實例（同 UR 回收口徑）
      try{
        var _tdB = window._lxpsRevokeTicketInstInSlot(_d, _thisRemoved);
        if(_tdB){ _patch.ticketData = _tdB; _patch.ticketData_s = JSON.stringify(_tdB); }
      }catch(_eTdB){ console.warn('[泛化券回收] 券實例同步失敗(背包已扣・下次登入對帳會自行收斂)', _eTdB); }
      if(_t.label === '主檔') _patch._authoritativeRestoreAt = _now;
      await setDoc(_t.ref, _patch, { merge: true });
      _wrote.push(_t.label);
    }
    console.info('[泛化券回收 v5.132.0] uid=' + String(uid).slice(0,8) + ' 回收 ' + _mainRemoved
      + ' 張(' + _keys.join('→') + ') 槽:' + _wrote.join('/'));
    return { ok:true, removed:_mainRemoved, detail:_removedTotal, slots:_wrote };
  };

  // ════════════════════════════════════════════════════════════════
  // ★ v5.95.0 — 稽核補發 UR 召喚卷（老師裁定乙：額度走獨立欄位 _urQuotaAdjust）
  // ────────────────────────────────────────────────────────────────
  //   用於兩種情況：
  //     ① 型態 B2  券已領到卻既沒用掉也不在背包（券憑空消失）
  //     ② 型態 C2  券已正確用掉、換來的 UR 卻不見了（老師裁定：補發召喚卷）
  //
  //   ⚠⚠ 為什麼一定要同步寫 _urQuotaAdjust ⚠⚠
  //     稽核的幽靈券判定式是 ghost = 背包實剩 − max(0, 額度 − 帳本實查用券次數)。
  //     情況②補發前：額度 1 / 用掉 1 / 背包 0 → 應剩 0、ghost 0（正常）。
  //     若只補券不加額度：背包變 1、應剩仍 0 ⇒ ghost = 1
  //     ⇒ 剛補的那張券當場被判成幽靈券，下次掃描反過來叫老師回收（補一張多一隻鬼）。
  //     額度 +1 之後：應剩 = 2 − 1 = 1、背包 1 ⇒ ghost = 0 ✅
  //
  //   ★ 老師裁定乙：刻意「不」寫進 gmGiftLog。
  //     gmGiftLog 是「老師實際發了幾張」的唯一權威，也正是額度夾制式的分母；
  //     拿補發去墊高分母＝讓分母被自己的下游動作推著跑，日後任何對帳都說不清
  //     哪一張是老師真的發的。獨立欄位保住分母乾淨，UI 另列一欄顯示。
  //
  //   ★ 券本體走既有 _fbCompensatePlayer（只增不減·三槽同寫·寄登入通知），
  //     本函式只負責「額度調整 + 稽核帳」，不自己碰背包（避免兩套寫入口徑）。
  // ════════════════════════════════════════════════════════════════
  window._fbAdminReissueURTicket = async function(uid, n, opts){
    if(!uid) throw new Error('缺少 uid');
    var _n = Math.max(1, parseInt(n, 10) || 1);
    opts = opts || {};
    var _now = Date.now();
    var _adminEmail = (_fbAuth && _fbAuth.currentUser && _fbAuth.currentUser.email) || 'admin';
    if(typeof window._fbCompensatePlayer !== 'function') throw new Error('_fbCompensatePlayer 未載入');
    // ① 先發券（只增不減·三槽同寫·寄通知）
    var _bp = {}; _bp['gm_summon_ticket_ur_pick'] = _n;
    await window._fbCompensatePlayer(uid, {
      backpack: _bp,
      reason:  String(opts.reason  || 'UR 券稽核補發'),
      summary: String(opts.summary || ('補發先前因程式問題而遺失的 UR 自選召喚卷 ×' + _n)),
      by: 'urTicketAudit:v5.95.0'
    });
    // ② 額度調整（★ 只有型態 C2 需要；型態 B2 一律不要 —— 見下方沙箱實證）
    //   ⚠⚠ 兩種補發的性質完全不同，混用會互相抵銷或製造幽靈券：
    //     型態 B2「券憑空消失」= 學生**已領到**的券不見了 → 那張券本來就已經算在額度裡，
    //       補回去只要背包 +1。若額度也 +1 ⇒ lostTickets = 額度 − 已用 − 背包 兩邊同加、
    //       差值不變 ⇒ 補完仍顯示「少 1 張」，老師會一直補下去。★ 故 B2 不調額度。
    //     型態 C2「券換的 UR 不見了」= 券已正確用掉（已計入 usedReal），補的是一次**額外**的
    //       重選機會 → 額度必須 +1，否則應剩 = 額度 − 已用 = 0 而背包多 1
    //       ⇒ 剛補的券當場被判成幽靈券。★ 故 C2 必須調額度。
    if(opts.adjustQuota !== true){
      console.info('[UR 券稽核補發 v5.95.0] uid=' + String(uid).slice(0,8) + ' 補 ' + _n + ' 張（型態 B2·不調額度）');
      return { ok:true, granted:_n, quotaAdjusted:false };
    }
    try{
      var _ref = doc(_fbDb, 'players', uid);
      var _gds = (typeof getDocFromServer === 'function') ? getDocFromServer : getDoc;
      var _snap = null;
      try{ _snap = await _gds(_ref); }catch(_e1){ try{ _snap = await getDoc(_ref); }catch(_e2){ _snap = null; } }
      var _d = (_snap && ((typeof _snap.exists === 'function') ? _snap.exists() : !!_snap.exists)) ? (_snap.data() || {}) : {};
      var _cur = Math.max(0, parseInt(_d._urQuotaAdjust, 10) || 0);
      var _log = Array.isArray(_d._urQuotaAdjustLog) ? _d._urQuotaAdjustLog.slice() : [];
      var _heroes = (Array.isArray(opts.heroes) ? opts.heroes : []).filter(function(x){ return !!x; });
      _log.push({
        at: _now, n: _n, by: _adminEmail,
        kind: String(opts.kind || 'reissue'),          // 'lostTicket'(B2) / 'lostTkHero'(C2)
        heroes: _heroes.slice(0, 20),
        note: String(opts.note || '').slice(0, 120)
      });
      if(_log.length > 100) _log = _log.slice(-100);
      var _patch = { _urQuotaAdjust: _cur + _n, _urQuotaAdjustLog: _log };
      // ★ C2 冪等：記下「已補償過哪幾隻」，稽核端不再重複列出（union，只增不減）
      if(_heroes.length){
        var _oldComp = Array.isArray(_d._urAuditCompensated) ? _d._urAuditCompensated : [];
        var _seen = {}, _merged = [];
        _oldComp.concat(_heroes).forEach(function(x){
          var _k = String(x); if(!_k || _seen[_k]) return; _seen[_k] = 1; _merged.push(_k);
        });
        _patch._urAuditCompensated = _merged.slice(-200);
      }
      await setDoc(_ref, _patch, { merge: true });
    }catch(_eAdj){
      // ⚠ 券已發出，額度沒寫成功 → 回報給 UI 明確提示，老師可重掃確認（不要靜默）
      console.error('[UR 券稽核補發] 額度調整寫入失敗（券已發出）', _eAdj);
      return { ok:true, granted:_n, quotaAdjusted:false,
               warn:'券已補發，但額度調整寫入失敗 → 重掃時這張券可能被誤判為幽靈券，請勿回收，並回報此訊息' };
    }
    console.info('[UR 券稽核補發 v5.95.0] uid=' + String(uid).slice(0,8) + ' 補 ' + _n + ' 張 + 額度同步 +' + _n);
    return { ok:true, granted:_n, quotaAdjusted:true };
  };

  // ════════════════════════════════════════════════════════════════
  // ★ v5.95.0 — 稽核補發「同名 UR 英雄」（老師狀況 F：老師直接發過、現在不在了）
  // ────────────────────────────────────────────────────────────────
  //   ⚠ 這與既有「🛟 英雄誤刪救回」卡分工不同：
  //     救回卡＝從存檔還原【原等級與培育】，但只救得回被 admin_delete 的；
  //     本函式＝針對「帳本有入帳、最後一筆卻不是移除類、人就是不在持有清單」
  //             這類雲端合併吃掉／存檔遺失的情況，直接補一隻 Lv1 回去。
  //   ★ 順序建議（UI 已標示）：先試救回卡（保留培育），無效再用本函式。
  //   ★ 走既有 _fbCompensatePlayer（只增不減·三槽同寫·寫 admin_grant 帳本·寄通知），
  //     刻意不自建第二條發英雄路徑。
  //   ★ 不動 _urQuotaAdjust：補的是英雄不是券，券帳（額度／已用／背包）完全不受影響。
  // ════════════════════════════════════════════════════════════════
  window._fbAdminReissueURHero = async function(uid, names, opts){
    if(!uid) throw new Error('缺少 uid');
    var _names = (Array.isArray(names) ? names : [names]).filter(function(x){ return !!x; });
    if(!_names.length) throw new Error('沒有指定英雄');
    opts = opts || {};
    if(typeof window._fbCompensatePlayer !== 'function') throw new Error('_fbCompensatePlayer 未載入');
    var _lv = {};
    _names.forEach(function(n){ _lv[n] = 1; });
    await window._fbCompensatePlayer(uid, {
      unlockedHeroes: _names.slice(),
      heroLevels: _lv,
      reason:  String(opts.reason  || 'UR 稽核補發同名英雄'),
      summary: String(opts.summary || ('補發先前因程式問題而遺失的 UR 角色：' + _names.join('、'))),
      by: 'urHeroAudit:v5.95.0'
    });
    console.info('[UR 英雄稽核補發 v5.95.0] uid=' + String(uid).slice(0,8) + ' 補 ' + _names.join('、'));
    return { ok:true, granted:_names.slice() };
  };

  // ════════════════════════════════════════════════════════════════
  // ★★ v5.97.0 — 至寶版補發（與 UR 兩支完全同構，只換資產與欄位）★★
  // ────────────────────────────────────────────────────────────────
  //   ⚠ 一律走既有 _fbCompensatePlayer（只增不減·三槽同寫·寫 admin_grant 帳本·寄登入通知），
  //     刻意不自建第二條發放路徑。
  //   ⚠ 額度調整用獨立欄位 _treQuotaAdjust（與 UR 的 _urQuotaAdjust 同理由）：
  //     gmGiftLog 是「老師實際發了幾張」的唯一權威，也是額度夾制式的分母，
  //     拿補發去墊高分母會讓分母被自己的下游動作推著跑。
  //   ★ 何時要調額度（與 UR 同一套裁定）：
  //     型態 B2「券憑空消失」→ **不調**（那張券本來就已算在額度裡，兩邊同加差值不變，
  //       會變成老師一直補下去）；
  //     型態 C2「券換的至寶不見了」→ **必須調**（券已計入 usedReal，補的是額外一次重選機會，
  //       不調的話應剩=額度−已用=0 而背包多 1 ⇒ 剛補的券當場被判成幽靈券）。
  // ════════════════════════════════════════════════════════════════
  window._fbAdminReissueTreasureTicket = async function(uid, n, opts){
    if(!uid) throw new Error('缺少 uid');
    var _n = Math.max(1, parseInt(n, 10) || 1);
    opts = opts || {};
    var _now = Date.now();
    var _adminEmail = (_fbAuth && _fbAuth.currentUser && _fbAuth.currentUser.email) || 'admin';
    if(typeof window._fbCompensatePlayer !== 'function') throw new Error('_fbCompensatePlayer 未載入');
    var _bp = {}; _bp['gm_summon_ticket_treasure_pick'] = _n;
    await window._fbCompensatePlayer(uid, {
      backpack: _bp,
      reason:  String(opts.reason  || '自選至寶券稽核補發'),
      summary: String(opts.summary || ('補發先前因程式問題而遺失的 自選至寶召喚卷 ×' + _n)),
      by: 'treTicketAudit:v5.97.0'
    });
    if(opts.adjustQuota !== true){
      console.info('[至寶券稽核補發 v5.97.0] uid=' + String(uid).slice(0,8) + ' 補 ' + _n + ' 張（型態 B2·不調額度）');
      return { ok:true, granted:_n, quotaAdjusted:false };
    }
    try{
      var _ref = doc(_fbDb, 'players', uid);
      var _gds = (typeof getDocFromServer === 'function') ? getDocFromServer : getDoc;
      var _snap = null;
      try{ _snap = await _gds(_ref); }catch(_e1){ try{ _snap = await getDoc(_ref); }catch(_e2){ _snap = null; } }
      var _d = (_snap && ((typeof _snap.exists === 'function') ? _snap.exists() : !!_snap.exists)) ? (_snap.data() || {}) : {};
      var _cur = Math.max(0, parseInt(_d._treQuotaAdjust, 10) || 0);
      var _log = Array.isArray(_d._treQuotaAdjustLog) ? _d._treQuotaAdjustLog.slice() : [];
      var _ids = (Array.isArray(opts.ids) ? opts.ids : []).filter(function(x){ return !!x; });
      _log.push({
        at: _now, n: _n, by: _adminEmail,
        kind: String(opts.kind || 'reissue'),
        ids: _ids.slice(0, 20),
        note: String(opts.note || '').slice(0, 120)
      });
      if(_log.length > 100) _log = _log.slice(-100);
      var _patch = { _treQuotaAdjust: _cur + _n, _treQuotaAdjustLog: _log };
      if(_ids.length){
        var _oldComp = Array.isArray(_d._treAuditCompensated) ? _d._treAuditCompensated : [];
        var _seen = {}, _merged = [];
        _oldComp.concat(_ids).forEach(function(x){
          var _k = String(x); if(!_k || _seen[_k]) return; _seen[_k] = 1; _merged.push(_k);
        });
        _patch._treAuditCompensated = _merged.slice(-200);
      }
      await setDoc(_ref, _patch, { merge: true });
    }catch(_eAdj){
      console.error('[至寶券稽核補發] 額度調整寫入失敗（券已發出）', _eAdj);
      return { ok:true, granted:_n, quotaAdjusted:false,
               warn:'券已補發，但額度調整寫入失敗 → 重掃時這張券可能被誤判為幽靈券，請勿回收，並回報此訊息' };
    }
    console.info('[至寶券稽核補發 v5.97.0] uid=' + String(uid).slice(0,8) + ' 補 ' + _n + ' 張 + 額度同步 +' + _n);
    return { ok:true, granted:_n, quotaAdjusted:true };
  };

  // ── 補發「同一件至寶」（型態 C：合法取得過、現在不在了）──
  //   ★ 冪等關鍵：至寶是 map 且不可重複持有 ⇒ 補之前先讀雲端，已擁有的直接跳過，
  //     只補真正缺的那幾件（_fbCompensatePlayer 內部對已擁有的至寶也不動，這裡再擋一層並回報）。
  //   ★ 老師若想連「培育過的等級」一起還原，請先試「🛟 帳號救援／汙染復原」那邊的還原工具；
  //     本函式補的是 Lv1 本體。
  window._fbAdminReissueTreasure = async function(uid, ids, opts){
    if(!uid) throw new Error('缺少 uid');
    var _ids = (Array.isArray(ids) ? ids : [ids]).filter(function(x){ return !!x; });
    if(!_ids.length) throw new Error('沒有指定至寶');
    opts = opts || {};
    if(typeof window._fbCompensatePlayer !== 'function') throw new Error('_fbCompensatePlayer 未載入');
    // ① 先讀雲端現況，濾掉已擁有的（冪等，避免重複補發與白發通知）
    var _have = {}, _skipped = [];
    try{
      var _ref2 = doc(_fbDb, 'players', uid);
      var _gds2 = (typeof getDocFromServer === 'function') ? getDocFromServer : getDoc;
      var _sn2 = null;
      try{ _sn2 = await _gds2(_ref2); }catch(_e3){ try{ _sn2 = await getDoc(_ref2); }catch(_e4){ _sn2 = null; } }
      var _d2 = (_sn2 && ((typeof _sn2.exists === 'function') ? _sn2.exists() : !!_sn2.exists)) ? (_sn2.data() || {}) : {};
      var _cur2 = {};
      if(_d2.taiwanTreasureData && typeof _d2.taiwanTreasureData === 'object' && Object.keys(_d2.taiwanTreasureData).length){
        _cur2 = _d2.taiwanTreasureData;
      } else if(typeof _d2.taiwanTreasureData_s === 'string'){
        try{ var _p2 = JSON.parse(_d2.taiwanTreasureData_s); if(_p2 && typeof _p2 === 'object') _cur2 = _p2; }catch(_){}
      }
      Object.keys(_cur2 || {}).forEach(function(k){ _have[k] = 1; });
    }catch(_eRd2){ /* 讀不到就照補，_fbCompensatePlayer 對已擁有的本來就不動 */ }
    var _todo = _ids.filter(function(id){
      if(_have[id]){ _skipped.push(id); return false; }
      return true;
    });
    if(!_todo.length){
      return { ok:true, granted:[], skipped:_skipped,
               note:'這幾件學生現在都已經有了（可能是重新掃描前已補過），本次沒有補發任何東西' };
    }
    var _nameOf = function(id){
      try{ return (window.TAIWAN_TREASURES && window.TAIWAN_TREASURES[id] && window.TAIWAN_TREASURES[id].name) || id; }catch(_){ return id; }
    };
    await window._fbCompensatePlayer(uid, {
      treasures: _todo.slice(),
      reason:  String(opts.reason  || '至寶稽核補發'),
      summary: String(opts.summary || ('補發先前因程式問題而遺失的至寶：' + _todo.map(_nameOf).join('、'))),
      by: 'treAudit:v5.97.0'
    });
    // ② 冪等名單（與 C2 共用同一份 _treAuditCompensated）
    try{
      var _ref3 = doc(_fbDb, 'players', uid);
      var _gds3 = (typeof getDocFromServer === 'function') ? getDocFromServer : getDoc;
      var _sn3 = null;
      try{ _sn3 = await _gds3(_ref3); }catch(_e5){ try{ _sn3 = await getDoc(_ref3); }catch(_e6){ _sn3 = null; } }
      var _d3 = (_sn3 && ((typeof _sn3.exists === 'function') ? _sn3.exists() : !!_sn3.exists)) ? (_sn3.data() || {}) : {};
      var _old3 = Array.isArray(_d3._treAuditCompensated) ? _d3._treAuditCompensated : [];
      var _seen3 = {}, _m3 = [];
      _old3.concat(_todo).forEach(function(x){
        var _k = String(x); if(!_k || _seen3[_k]) return; _seen3[_k] = 1; _m3.push(_k);
      });
      await setDoc(_ref3, { _treAuditCompensated: _m3.slice(-200) }, { merge: true });
    }catch(_eC){ console.warn('[至寶稽核補發] 冪等名單寫入失敗（至寶已補發）', _eC); }
    console.info('[至寶稽核補發 v5.97.0] uid=' + String(uid).slice(0,8) + ' 補 ' + _todo.join('、'));
    return { ok:true, granted:_todo.slice(), skipped:_skipped };
  };

  // 即時監聽統計：UI 元件用此更新顯示
  window._fbWatchStats = function(callback) {
    try {
      const statsRef = doc(_fbDb, 'stats', 'global');
      return onSnapshot(statsRef, (snap) => {
        const todayKey = _getTodayKeyTW();
        const weekKey  = _getThisWeekMondayKeyTW();
        if(!snap.exists()) {
          console.info('ℹ️ [人數統計] stats/global 文件尚未建立（首位玩家登入後會自動建立）');
          callback({ total: 0, todayActive: 0, weekNew: 0, totalAdventures: 0 });
          return;
        }
        const d = snap.data() || {};
        callback({
          total:            d.totalPlayers || 0,
          todayActive:      d['active_' + todayKey] || 0,
          weekNew:          d['newPlayers_' + weekKey] || 0,
          totalAdventures:  d.totalAdventures || 0,
        });
      }, (err) => {
        console.error('❌ [人數統計] 監聽錯誤 code=' + (err && err.code) + '（可能原因：Firestore 規則未允許讀取 stats 集合）', err);
        callback({ total: 0, todayActive: 0, weekNew: 0, totalAdventures: 0 });
      });
    } catch(e) {
      console.error('❌ [人數統計] 啟動監聽失敗', e);
      callback({ total: 0, todayActive: 0, weekNew: 0, totalAdventures: 0 });
      return () => {};
    }
  };

  window._fbResetAndRebuildPlayer = async function(uid, rebuild) {
    if(!uid || !rebuild) throw new Error('uid 與 rebuild 為必要');
    // 1) 先讀現有資料(只為了保留登入身份 + 記錄被覆寫的 snapshot)
    let existing = {};
    try {
      const snap = await getDocFromServer(doc(_fbDb, 'players', uid));
      if(snap.exists()) existing = snap.data() || {};
    } catch(e) {
      console.warn('[_fbResetAndRebuildPlayer] 讀取現有資料失敗,將以空白起點覆寫', e);
    }

    // 2) 保留的「登入身份」欄位(不清掉這些,否則玩家下次登入會建空帳號)
    const _keepEmail       = existing.email || rebuild.email || '';
    const _keepDisplayName = existing.displayName || '';
    const _keepFirstAt     = existing._firstLoginAt || existing.createdAt || Date.now();

    // 3) 處理 rebuild 指定的角色 + 等級
    const _newUnlocked = Array.isArray(rebuild.unlockedHeroes) ? rebuild.unlockedHeroes.slice() : [];
    const _newLevels = {};
    if(rebuild.heroLevels && typeof rebuild.heroLevels === 'object'){
      Object.keys(rebuild.heroLevels).forEach(name => {
        const lv = Math.max(1, Math.min(50, parseInt(rebuild.heroLevels[name], 10) || 1));
        _newLevels[name] = lv;
      });
    }
    // 確保 unlockedHeroes 跟 heroLevels 同步:有等級的角色必定在 unlockedHeroes
    const _unlockedSet = new Set(_newUnlocked);
    Object.keys(_newLevels).forEach(n => _unlockedSet.add(n));
    const _finalUnlocked = Array.from(_unlockedSet);

    // 4) 各角色 statPoints(自由分配點 = 等級 - 1,因為原始狀態還沒分配)
    const _newSP = {};
    Object.keys(_newLevels).forEach(name => {
      const lv = _newLevels[name];
      _newSP[name] = Math.max(0, lv - 1);
    });

    // 5) 知識幣(set 模式,直接覆寫)
    const _newCoins = Math.max(0, parseInt(rebuild.coins, 10) || 0);

    // 6) 背包(完全重建,不疊加)
    const _newBp = {};
    if(rebuild.backpack && typeof rebuild.backpack === 'object'){
      Object.keys(rebuild.backpack).forEach(itemId => {
        const qty = Math.max(0, Math.min(99, parseInt(rebuild.backpack[itemId], 10) || 0));
        if(qty > 0) _newBp[itemId] = qty;
      });
    }

    // 7) 寫入重置歷史(獨立於補償歷史)
    const _now = Date.now();
    const _resetEntry = {
      at: _now,
      by: rebuild.by || 'admin',
      reason: (rebuild.reason || '').slice(0, 500),
      summary: (rebuild.summary || '').slice(0, 1000),
      // 記錄被清掉前的「重要資料 snapshot」,萬一誤刪可手動參考
      cleared: {
        unlockedCount: (existing.unlockedHeroes || []).length,
        heroLevelCount: Object.keys(existing.heroLevels || {}).length,
        coins: existing.knowledgeCoins || 0,
        bpItemCount: Object.keys(existing.playerBackpack || {}).length,
        compensationCount: existing._compensationCount || 0,
        medalCount: Array.isArray(existing.medalsUnlocked) ? existing.medalsUnlocked.length : 0
      },
      // 重建的內容
      rebuilt: {
        unlockedHeroes: _finalUnlocked,
        heroLevels: _newLevels,
        coins: _newCoins,
        backpack: _newBp
      }
    };
    const _existResetHistory = Array.isArray(existing._resetHistory) ? existing._resetHistory : [];
    const _newResetHistory = [..._existResetHistory, _resetEntry].slice(-10);
    const _resetCount = (existing._resetCount || 0) + 1;

    // 8) 組 payload(用 setDoc 不帶 merge → 整個 doc 被覆寫,污染欄位徹底洗掉)
    const payload = {
      // === 登入身份(保留)===
      email: _keepEmail,
      displayName: _keepDisplayName,
      _firstLoginAt: _keepFirstAt,
      // === 重建的資料 ===
      savedAt: _now,
      unlockedHeroes: _finalUnlocked,
      heroLevels: _newLevels,
      heroLevels_s: JSON.stringify(_newLevels),
      heroStatPoints: _newSP,
      heroStatPoints_s: JSON.stringify(_newSP),
      heroStatInvested: {},      // 清空已分配的素質點
      heroStatInvested_s: '{}',  // ★ v3.16.4 — 補字串版(_applySafeData 已優先採信 _s,不寫則重置被舊 _s 復原)
      heroSkillLevels: {},        // 清空技能等級
      heroSkillLevels_s: '{}',    // ★ v3.16.4 — 同上,必須同步寫
      heroBurstLevels: {},        // 清空爆發等級
      heroBurstLevels_s: '{}',    // ★ v3.16.4
      heroExp: {}, heroExp_s: '{}',               // ★ v3.16.33 — 完全重置清空經驗(本版載入優先採信 _s,顯式寫空避免殘留)
      heroTraitLevel: {}, heroTraitLevel_s: '{}', // ★ v3.16.33 — 同上,清空天賦等級
      _authoritativeRestoreAt: Date.now(),   // ★ v3.16.5 — 過時 session 偵測後鎖存+重載,不覆蓋本次重置
      heroBurstResetCount: {},    // 清空爆發重置次數
      knowledgeCoins: _newCoins,
      playerBackpack: _newBp,
      playerBackpack_s: JSON.stringify(_newBp),
      representativeHero: null,   // 清代表英雄(玩家重設)
      // === 清空所有進度 / 紀錄 ===
      medalsUnlocked: [],
      _stageProgress: {},         // 關卡進度
      _wbBattleHistory: [],       // 世界 BOSS 戰歷史
      _wbCumulativeContrib: 0,    // 累計貢獻
      // === 重置歷史 ===
      _resetCount: _resetCount,
      _resetHistory: _newResetHistory,
      _resetHistory_s: JSON.stringify(_newResetHistory),
      _lastResetAt: _now,
      _lastResetBy: rebuild.by || 'admin',
      // === 清掉舊的補償紀錄(讓重置後是真正全新狀態)===
      _compensationCount: 0,
      _compensationHistory: [],
      _compensationHistory_s: '[]',
      _lastCompensatedAt: null,
      _lastCompensatedBy: null
    };

    // ★ 用 setDoc 不帶 merge 確保整個 doc 被取代(merge:true 會保留 doc 中沒提到的舊欄位)
    await setDoc(doc(_fbDb, 'players', uid), payload);
    console.info('✅ [完全重置] 已重建 ' + uid, payload);
    return {
      ok: true,
      uid,
      resetCount: _resetCount,
      cleared: _resetEntry.cleared,
      rebuilt: _resetEntry.rebuilt
    };
  };

  // ════════════════════════════════════════════════════════════════
  // ★ FIX 20260517(t) — 處理 redirect 登入回傳(iPad/iOS Safari 修復)
  //
  // 舊版用 localStorage 旗標(lxps_pending_redirect_signin)守門,
  // 沒旗標就跳過 getRedirectResult。但 iOS 17+ Safari 的 Storage
  // Partitioning 會在 cross-origin redirect 後清掉 localStorage,
  // 旗標消失 → 永遠不呼叫 getRedirectResult → 登入結果被丟掉,
  // 玩家陷入「點登入→選帳號→回首頁未登入」的無限循環。
  //
  // 修補:Firebase 官方建議「每次頁面載入都呼叫 getRedirectResult」,
  //      沒 redirect 過時呼叫也安全(result 為 null)。改成永遠呼叫,
  //      旗標只當輔助診斷用(知道是否真的有 redirect 流程)。
  //
  // 注意:這個 IIFE 是非同步的,onAuthStateChanged 仍可能在它完成前
  //      就以 user=null 觸發。但因為 getRedirectResult 一旦拿到 user,
  //      Firebase 會自動觸發 onAuthStateChanged 第二次以 user=非null
  //      呼叫,所以最終結果是正確的。
  // ════════════════════════════════════════════════════════════════
  // ★ v3.3.7 — 玩家回報「PC 版/iPad 版選了帳號跳回首頁仍未登入,無限循環」
  //   根因:FIX 20260517(ff) 改成「沒旗標就早退」,但 iOS 17+ Safari Storage
  //         Partitioning 會在 cross-origin redirect 後清掉 localStorage+sessionStorage,
  //         結果旗標消失 → 早退 → getRedirectResult 從未被呼叫 → 登入失敗。
  //   修法:回到「先看旗標,若沒旗標但 URL 看起來像 redirect 回來,仍嘗試呼叫」,
  //         並加 1.5 秒後重試機制(Firebase 內部 OAuth credential 處理可能慢)
  //   保留 _fbRedirectReady Promise(向後相容,讓 onAuthStateChanged 能 await)
  window._fbRedirectReady = (async function _handlePendingRedirect(){
    const _ua = (navigator.userAgent || '').toLowerCase();
    const _isIOS = /iphone|ipad|ipod/.test(_ua);
    const _isCriOS = /crios/.test(_ua);
    const _pending = (function(){ try{ return localStorage.getItem('lxps_pending_redirect_signin'); }catch(_){ return null; }})();
    const _pendingSession = (function(){ try{ return sessionStorage.getItem('lxps_pending_redirect_session'); }catch(_){ return null; }})();
    _fbDiag('redirect:啟動 pending(local)=' + (_pending?'Y':'N') + ' pending(session)=' + (_pendingSession?'Y':'N') + ' iOS=' + _isIOS + ' CrIOS=' + _isCriOS);

    // ★ v5.148.0 — 直連 Google OAuth 回程優先（URL hash 有 id_token 才會做事；否則立刻回 null 走原邏輯）
    try{
      const _du = await window._lxpsOAuthDirectResume();
      if(_du) return _du;
    }catch(_eDr){ _fbDiag('redirect:直連回程處理例外 ' + (_eDr && (_eDr.code || _eDr.message)), 'error'); }

    // ════════════════════════════════════════════════════════════════
    // ★★ v5.136.0 — redirect 登入「本次頁載」runtime 旗標 ★★
    //   為什麼要新增這個:v5.136.0 拆掉了 v3.13.9 的 _sameUidAsLastSession 放行
    //   (那道放行 = 共用平板「重啟就自動進上一位帳號」的主因,老師裁定甲移除)。
    //   但 v3.13.9 當初加它是為了修一個真實問題:iPad Safari 17+ 的 Storage Partitioning
    //   會在 cross-origin redirect 後清掉 sessionStorage(含 lxps_pending_redirect_session),
    //   於是**合法的 redirect 登入**回來時兩個旗標都沒了 → 被攔截器誤判成偷登 → 強制登出
    //   → 玩家陷入「登入後又被踢出」的迴圈。
    //   ⇒ 改用「從 localStorage 的 lxps_pending_redirect_signin 撈出來、轉存成 window runtime 變數」:
    //      ① localStorage 不會被 Partitioning 清掉 ⇒ 補上 sessionStorage 被清的缺口
    //      ② 轉成 runtime 變數後,reload / 關 App / 新分頁一律歸零 ⇒ 不會變成永久後門
    //      ③ 額外夾 5 分鐘時效 ⇒ 瀏覽器在 redirect 途中被殺掉留下的殘留旗標不會生效
    //   ★★ 與 _sameUidAsLastSession 的決定性差異:本旗標只在「這次頁載真的發起過 redirect 登入」
    //      時才成立;而舊的 uid 比對是「這台裝置上次是誰玩的」—— 後者在共用 iPad 上
    //      等於「前一位沒登出就直接放行下一位」,那正是老師要根除的行為。
    // ════════════════════════════════════════════════════════════════
    try{
      var _prTs = parseInt(_pending || '0', 10) || 0;
      if((_pending && (Date.now() - _prTs) < 300000) || _pendingSession){
        window._lxpsRedirectSignInThisPageload = true;
        _fbDiag('redirect:已設 runtime 旗標 _lxpsRedirectSignInThisPageload(供 onAuth 放行本次合法登入)');
      }
    }catch(_ePr){}

    let _shouldTryRedirect = !!(_pending || _pendingSession);
    if(!_shouldTryRedirect){
      try{
        // 檢查 Firebase 內部 sessionStorage key(redirect 流程的線索)
        for(let i = 0; i < sessionStorage.length; i++){
          const _k = sessionStorage.key(i);
          if(_k && _k.indexOf('firebase:pendingRedirect') === 0){
            _shouldTryRedirect = true;
            _fbDiag('redirect:沒玩家旗標但有 firebase:pendingRedirect key → 仍嘗試', 'warn');
            break;
          }
        }
      }catch(_){}
    }
    // ★ v3.3.7 — 進一步保險:URL 有 OAuth callback 線索(hash 含 access_token 或 state)
    if(!_shouldTryRedirect){
      try{
        const _h = window.location.hash || '';
        const _s = window.location.search || '';
        if((_h && (_h.indexOf('access_token') >= 0 || _h.indexOf('id_token') >= 0 || _h.indexOf('state=') >= 0))
           || (_s && _s.indexOf('state=') >= 0)){
          _shouldTryRedirect = true;
          _fbDiag('redirect:沒旗標但 URL 含 OAuth token/state → 仍嘗試', 'warn');
        }
      }catch(_){}
    }

    if(!_shouldTryRedirect){
      _fbDiag('redirect:本次頁面載入沒有 redirect 線索 → 不呼叫 getRedirectResult');
      return null;
    }
    if(!_pending && _pendingSession){
      _fbDiag('redirect:⚠ iOS storage partitioning(localStorage 被清/sessionStorage 保留),仍嘗試 getRedirectResult', 'warn');
    }
    // ★ v3.3.7 — getRedirectResult 加入 retry:如果第一次回 null 但有跡象,1.5s 後再試一次
    async function _tryGetRedirect(label){
      try {
        const t0 = Date.now();
        const result = await getRedirectResult(_fbAuth);
        _fbDiag('redirect:'+label+' getRedirectResult 完成 (耗時 ' + (Date.now()-t0) + 'ms)');
        if(result && result.user){
          _fbDiag('redirect:'+label+' ✅ 登入成功,user=' + result.user.email);
          // ★ v3.16.43 — redirect 成功也在最早一刻記錄最近帳號(與 popup 對稱;belt-and-suspenders)
          try{ if(typeof window._addRecentAccount === 'function') window._addRecentAccount(result.user); }catch(_){}
          return result.user;
        }
        // 沒拿到 user → 看 currentUser 是否已被 Firebase 自動填上
        try{
          if(_fbAuth.currentUser){
            _fbDiag('redirect:'+label+' getRedirectResult 回 null 但 currentUser=' + _fbAuth.currentUser.email);
            try{ if(typeof window._addRecentAccount === 'function') window._addRecentAccount(_fbAuth.currentUser); }catch(_){}
            return _fbAuth.currentUser;
          }
        }catch(_){}
        _fbDiag('redirect:'+label+' getRedirectResult 回 null 且 currentUser=null', 'warn');
        return null;
      } catch(e){
        const code = e && e.code;
        _fbDiag('redirect:'+label+' ❌ 失敗 code=' + (code || 'unknown'), 'error');
        if(code === 'auth/cancelled-by-user' || code === 'auth/user-cancelled'){
          return null;
        }
        throw e;
      }
    }
    try {
      let _user = await _tryGetRedirect('first');
      // 第一次沒拿到 user → 等 1.5s 再試一次(Firebase 內部 OAuth 可能還在跑)
      if(!_user){
        _fbDiag('redirect:第一次沒拿到 user,等 1500ms 後再試一次');
        await new Promise(r => setTimeout(r, 1500));
        _user = await _tryGetRedirect('retry');
      }
      try{ localStorage.removeItem('lxps_pending_redirect_signin'); }catch(_){}
      try{ sessionStorage.removeItem('lxps_pending_redirect_session'); }catch(_){}
      if(_user) return _user;
      _fbDiag('redirect:⚠ 兩次嘗試都沒拿到 user', 'warn');
      return null;
    } catch(e){
      try{ localStorage.removeItem('lxps_pending_redirect_signin'); }catch(_){}
      try{ sessionStorage.removeItem('lxps_pending_redirect_session'); }catch(_){}
      const code = e && e.code;
      _showLoginErrorTip(
        '登入未完成',
        '<b>錯誤代碼:</b> <code style="color:#ffaa66;">' + (code || 'unknown') + '</code><br><br>' +
        '可能是在 Google 那邊取消了,或網路不穩。<br>請重新點登入再試一次。<br><br>' +
        '<b>iPad 玩家提醒:</b><br>' +
        '・確認 設定 → Safari → <b>關閉</b>「阻擋所有 Cookie」<br>' +
        '・確認沒有開啟「私密瀏覽」<br>' +
        '・若用 Safari 失敗,改用 Chrome 試試'
      );
      return null;
    }
  })();

  // ★ FIX 20260517(gg) — 1.5 秒後也印一次 Firebase 內部狀態,看是否異步補上
  setTimeout(() => {
    try{
      _fbDiag('delayed[1.5s]:currentUser=' + (_fbAuth.currentUser ? _fbAuth.currentUser.email : 'null'));
      const _ssLater = Object.keys(sessionStorage || {}).filter(k => k.indexOf('firebase') >= 0);
      _fbDiag('delayed[1.5s]:sessionStorage firebase keys=' + (_ssLater.length ? _ssLater.join(',').slice(0, 160) : '(無)'));
      // 也看 IndexedDB(粗略)
      try{
        if(window.indexedDB && indexedDB.databases){
          indexedDB.databases().then(dbs => {
            const _fbDbs = (dbs || []).filter(d => (d.name || '').indexOf('firebase') >= 0);
            _fbDiag('delayed[1.5s]:IndexedDB firebase=' + (_fbDbs.length ? _fbDbs.map(d=>d.name).join(',') : '(無)'));
          }).catch(eDb => _fbDiag('delayed[1.5s]:indexedDB.databases() err ' + (eDb && eDb.message || ''), 'warn'));
        }
      }catch(_){}
    }catch(_){}
  }, 1500);

  // ════════════════════════════════════════════════════════════════════
  // ★★ v5.141.0（2026-09-04・老師回報「新電腦登入後首頁左上沒有帳號列／選關頁左上沒名字頭像，
  //     但雲端資料正常」）— 帳號列繪製「單一真相」 ★★
  // ─────────────────────────────────────────────────────────────────────
  //   根因：兩條登入流程賽跑。點「開始遊戲」→ _lgAfterSignIn 自己 gameCloudLoad + openAdventureOverlay
  //   （所以資料有載入）；但帳號列（頭像／暱稱／🔄切換／登出／📲加入主畫面／⬇️下載安裝）只由下方
  //   onAuthStateChanged 在跑完 **6～7 個連續 Firestore 讀寫**（維修→休息排程→停權→暱稱同步含 3 次重試
  //   各等 2 秒…）之後才畫。新裝置第一次連 Firestore 較慢 ⇒ 選關頁先開、#google-name 還是空的 ⇒
  //   openAdventureOverlay 的名字條判 textContent.trim() 為空就整條 display:none。這正是「偶發」的原因，
  //   而且會連鎖觸發 v3.15.45 污染看門狗（「本機記得暱稱、畫面卻沒顯示」）跳出重載視窗。
  //   修法：把「畫帳號列」抽成這一支，在四個時點呼叫：
  //     ① onAuth 一過閘門、window._fbUser 設好的當下（不等那 6 次讀取）
  //     ② _lgAfterSignIn 開頭（登入閘門路徑）
  //     ③ openAdventureOverlay 發現首頁名字空白時
  //     ④ 既有每秒輪詢的「自癒」（只在 _gUserId 存在且 _lxpsLoggedInThisSession===true 時才畫，
  //        ⇒ 自動還原被攔下時 _gUserId 已被清成 null，絕不會把被擋的帳號畫上去）
  //   ⚠ 純顯示層：不寫雲端、不動 _gUserId／session；暱稱優先序與原碼一致（本機暱稱＞Google 名稱＞email），
  //     onAuth 後續讀到雲端暱稱時仍會再呼叫原本那段覆蓋成最終值。
  // ════════════════════════════════════════════════════════════════════
  window._lxpsPaintAccountBar = function(user, opts){
    try{
      opts = opts || {};
      if(!user || !user.uid){
        user = (window._fbUser && window._fbUser.uid) ? window._fbUser
             : ((window._fbAuth && window._fbAuth.currentUser) ? window._fbAuth.currentUser : null);
      }
      if(!user || !user.uid) return false;
      // 自癒路徑加一道守門：必須是本 session 合法登入中的帳號（uid 對得上）
      if(opts.selfHeal && (!window._gUserId || window._gUserId !== user.uid)) return false;
      const av = document.getElementById('google-avatar');
      const nm = document.getElementById('google-name');
      const btnWrap = document.getElementById('google-login-btn-wrap');
      const userInfo = document.getElementById('google-user-info');
      let _nick = null;
      try{ _nick = localStorage.getItem('lxps_nickname_' + user.uid); }catch(_){}
      const _dispName = opts.displayName || _nick || user.displayName || user.email || '';
      if(_dispName) window._playerNickname = _dispName;
      const _shownName = (typeof window._formatPlayerDisplayName === 'function')
        ? window._formatPlayerDisplayName(_dispName, user.email) : _dispName;
      if(av && user.photoURL && av.getAttribute('src') !== user.photoURL) av.src = user.photoURL;
      if(nm){
        nm.textContent = _shownName;
        nm.title = '點擊修改暱稱';
        nm.style.cursor = 'pointer';
        nm.onclick = function(){ try{ openNicknameModal(); }catch(_){} };
      }
      if(btnWrap) btnWrap.style.display = 'none';   // v5.136.0 裁定：左上登入鈕永久隱藏
      if(userInfo) userInfo.style.display = 'flex';
      try{ if(typeof window._syncTrustEntryBtn === 'function') window._syncTrustEntryBtn(); }catch(_){}
      // 選關頁若已開著，一併補上名字條（否則要等下次 openAdventureOverlay）
      try{
        const _advUBar = document.getElementById('adv-username-bar');
        const _advUName = document.getElementById('adv-user-name');
        const _advUAv   = document.getElementById('adv-user-avatar');
        if(_advUBar && _advUName && _shownName){
          _advUName.textContent = _shownName;
          if(_advUAv && user.photoURL) _advUAv.src = user.photoURL;
          _advUBar.style.display = 'flex';
        }
      }catch(_){}
      if(opts.selfHeal){
        try{ _fbDiag('accountBar:自癒觸發（帳號列原本沒畫上）uid=' + String(user.uid).slice(0,8), 'warn'); }catch(_){}
      }
      return true;
    }catch(_e){ console.warn('[帳號列 v5.141.0] 繪製失敗', _e); return false; }
  };

  // 登入狀態變化
  onAuthStateChanged(_fbAuth, async (user) => {
    // ★ FIX 20260517(ee) — 等 redirect 處理完才推 UI,避免 user=null 先把 UI 推到未登入
    try{ await window._fbRedirectReady; }catch(_){}
    // 若 user 仍為 null 但 _fbAuth.currentUser 不為 null(race condition),改用 currentUser
    if(!user){
      try{
        if(_fbAuth.currentUser){
          _fbDiag('onAuth:user=null 但 currentUser=' + _fbAuth.currentUser.email + ' → 改用 currentUser', 'warn');
          user = _fbAuth.currentUser;
        }
      }catch(_){}
    }
    _fbDiag('onAuth:觸發 user=' + (user ? user.email : 'null'));
    // ★ v5.148.0 — 直連 OAuth 回程：signInWithCredential 讓「最初 user=null 的回呼（上面已改用 currentUser）」
    //   與「Firebase 隨後觸發的 onAuth(user)」都會跑到這裡；第二次直接略過，避免登入流程對同一 uid 跑兩遍。
    //   （只限直連回程；popup 路徑本來就只觸發一次，零影響。）
    try{
      if(user && window._lxpsOAuthDirectResumedUid && user.uid === window._lxpsOAuthDirectResumedUid){
        if(window._lxpsOnAuthDirectSeenUid === user.uid){
          _fbDiag('onAuth:直連回程同一 uid 重複觸發 → 略過');
          return;
        }
        window._lxpsOnAuthDirectSeenUid = user.uid;
      }
    }catch(_){}
    if(user) {
      // ════════════════════════════════════════════════════════════
      // ★ v3.10.1(2026-05-26) — 禁止自動登入(共用平板防護)
      // ─────────────────────────────────────────────────────────────
      // 老師需求(2026-05-26):學校 iPad 共用,前位學生沒登出 → 下位學生打開
      //   遊戲會自動進到別人帳號 → 行為被記到別人帳上、雲端資料污染。
      //   解法:每次打開遊戲都要重新確認登入,不允許自動還原 Firebase token。
      //
      // 實作邏輯(版本 2 — 完整防護):
      //   - 用 *JS runtime 變數* window._lxpsActiveSignInThisPageload 當旗標
      //   - 玩家主動點登入按鈕(_doSignInFlow)時設旗標 = true
      //   - 玩家從 redirect 回來時,sessionStorage 有 lxps_pending_redirect_session
      //     → 也視為主動登入(這是同一次連續操作)
      //   - onAuth 觸發時若兩個 flag 都沒有 → 自動還原 → 攔截 + 強制 signOut
      //   - 每次頁面載入(包含 F5 / location.reload / 關閉後重開)都是新的
      //     JS runtime,window._lxpsActiveSignInThisPageload 一定是 undefined →
      //     一定要主動登入才放行
      //
      // 邊界處理:
      //   - 主動登入流程:_doSignInFlow 設旗標 → popup/redirect → 完成 → onAuth 放行
      //   - redirect 慢迴路:sessionStorage flag 保證 redirect 回來那一次能通過
      //   - 老師除錯:URL ?autologin=1 可跳過
      //   - 維修模式 / 停權檢查:走原本流程,在攔截之後檢查(攔截更優先,
      //     省下不必要的雲端讀取;玩家下次主動登入時才會被檢查到維修/停權)
      // ════════════════════════════════════════════════════════════
      try {
        const _activeThisPage  = (window._lxpsActiveSignInThisPageload === true);
        // ★ v5.136.0 — 併入 runtime 旗標(見 _handlePendingRedirect 內註解):
        //   sessionStorage 這一半會被 iPad Safari 的 Storage Partitioning 清掉,
        //   runtime 那一半是從 localStorage 撈出來轉存的,兩者任一成立即視為「本次頁載的合法 redirect 登入」。
        const _pendingRedirect = sessionStorage.getItem('lxps_pending_redirect_session')
                              || (window._lxpsRedirectSignInThisPageload === true);   // ★ v5.136.0
        // ★ v3.11.15(2026-05-28) — 「自家發起的重新整理」放行旗標
        //   問題:v3.11.14 強制更新會 location.reload()/hard-refresh,既有的「重新整理套用新版」
        //   按鈕、F12 檢查更新硬刷也都會 reload。reload 後 Firebase 會自動還原 token,
        //   但 _activeThisPage(runtime 變數)被 reload 清掉、又非 redirect → 被誤判成
        //   「共用平板的自動還原」→ 強制登出 → 玩家「玩一玩突然跳回登入頁」。
        //   解法:任何「我們自己程式觸發的 reload」前先設這個短效旗標(30 秒內有效),
        //   onAuth 看到就視同合法登入、不踢人(用完即清,避免被當永久後門)。
        let _intentionalReload = false;
        try{
          const _ir = sessionStorage.getItem('lxps_intentional_reload');
          if(_ir){
            const _irTs = parseInt(_ir, 10) || 0;
            if(Date.now() - _irTs < 30000){ _intentionalReload = true; }
            sessionStorage.removeItem('lxps_intentional_reload'); // 一次性,用完即清
          }
        }catch(_){}
        // 後門:URL 加 ?autologin=1 可跳過(給老師除錯用)
        let _bypassAutoLoginBlock = false;
        try{
          const _url = new URL(window.location.href);
          if(_url.searchParams.get('autologin') === '1'){
            _bypassAutoLoginBlock = true;
            console.log('[禁止自動登入 v3.10.1] URL ?autologin=1 後門啟用,跳過攔截');
          }
        }catch(_){}

        // ════════════════════════════════════════════════════════════
        // ★★ v5.136.0(2026-09-03・老師裁定①丙)— 移除 v3.13.9 的「同一 uid 就放行」★★
        // ─────────────────────────────────────────────────────────────
        //   ⚠⚠ 這段是共用平板的核心防線,日後**不可以**因為「學生嫌每次都要登入很麻煩」
        //       而把 lxps_last_active_uid 的放行加回來。加回來 = 破口原地復活。⚠⚠
        //
        //   【被移除的是什麼】v3.13.9 曾經加了第四道放行:
        //       if(localStorage.lxps_last_active_uid === user.uid) → 視為「同一玩家重啟」,放行自動還原。
        //   【為什麼一定要拆】lxps_last_active_uid 是「每次登入成功就寫入、只有走完整登出才清除」。
        //       共用 iPad 上學生 A 玩完直接關 App(沒人會乖乖按登出)⇒ 這把鑰匙留在裝置上
        //       ⇒ 學生 B 開遊戲 → Firebase 從 IndexedDB 還原 A 的 token → uid 對得上 → **放行**
        //       ⇒ B 直接坐進 A 的帳號,行為記到 A 頭上、存檔被污染。
        //       這正是老師 2026-09-03 指出的「絕對不可以直接自動登入上一個使用者」。
        //   【當初為了修什麼 / 現在改用什麼取代】
        //       v3.13.9 的動機是 iPad Safari Storage Partitioning 會清掉 sessionStorage,
        //       害**合法的 redirect 登入**被誤攔成偷登。那個問題現在改由
        //       window._lxpsRedirectSignInThisPageload 處理(見 _handlePendingRedirect 內註解):
        //       它只在「這次頁載真的發起過 redirect 登入」時成立、reload 即歸零、還夾 5 分鐘時效
        //       ⇒ 精準修好誤攔,又不會像 uid 比對那樣變成「前一位沒登出就放行下一位」。
        //   ★ lxps_last_active_uid 這個 key 本身**保留不刪**:它另有用途
        //     (L≈17864 換帳號時的本機資料清理判定、_clearAccountLocalData 白名單),
        //     本輪只是不再拿它當「放行憑證」。
        // ════════════════════════════════════════════════════════════
        // 判定「是不是自動還原」:三個 flag 都不存在 → 一定是自動還原,必須攔下
        //   ① _activeThisPage    = 本次頁載玩家真的按過登入鈕
        //   ② _pendingRedirect   = 本次頁載發起過 redirect 登入(session 或 runtime 任一)
        //   ③ _intentionalReload = 自家程式觸發的 reload(版本更新套用、檢查更新硬刷…),
        //                          v3.11.15 加,不放行會讓玩家在更新後被誤踢出去
        //   ④ _appHandoff        = ★ v5.175.0 — 自家入口連結發起的「大對抗 ⇄ 小遊戲」跨程式導航
        //                          (一次性 sessionStorage 鑰匙 + uid 相符 + 120 秒時效,見檔頭 helper)
        //   ★ v5.136.0 移除第四道 _sameUidAsLastSession(理由見上;本輪的 ④ 與那把舊鑰匙性質完全不同)
        let _appHandoff = false;
        try{
          if(typeof window._lxpsConsumeAppHandoff === 'function'){
            _appHandoff = window._lxpsConsumeAppHandoff(user && user.uid);
          }
        }catch(_eHo){}
        const _isAutoRestore = !_activeThisPage && !_pendingRedirect && !_intentionalReload && !_appHandoff;   // ★ v5.175.0（舊:尾端沒有 && !_appHandoff）
        if(_appHandoff){
          console.log('[跨程式交接 v5.175.0] 偵測到小遊戲 ⇄ 大對抗 的自家導航鑰匙(uid 相符、120 秒內、已消費),放行本次登入(不視為共用裝置自動還原)');
          window._lxpsActiveSignInThisPageload = true;
        }
        if(_intentionalReload){
          console.log('[禁止自動登入 v3.11.15] 偵測到自家發起的重新整理旗標,放行本次登入(不視為共用裝置自動還原)');
          // 標記為本頁主動登入,讓後續流程一致
          window._lxpsActiveSignInThisPageload = true;
        }

        // ════════════════════════════════════════════════════════════
        // ★ v3.10.3(2026-05-26) — 「裝置信任」放行:PWA 安裝版專用
        //   流程:
        //     1. 確認在 PWA standalone 模式 + 自動還原情境
        //     2. 查雲端 players/{uid}.trustedDevices[deviceId] 有沒有信任本機
        //     3. 信任 → 顯示「即將自動登入 OOO... 不是我?」3 秒倒數確認卡
        //              玩家不取消 → 放行 + 寫 _lxpsActiveSignInThisPageload = true
        //              玩家取消 → 該裝置設不信任 + 強制登出 + 顯示重登卡
        //     4. 不信任 → 跑原本 v3.10.1 攔截邏輯
        //   風險點:雲端查詢失敗時(網路不穩),fail-safe 走攔截路徑(較安全)
        // ════════════════════════════════════════════════════════════
        // ════════════════════════════════════════════════════════════
        // ★★ v5.137.0(老師 2026-09-03 追加裁定,推翻 v5.136.0 的全面停用)★★
        //   老師原話:「信任裝置就不需要再重新登入。公用平板被誤設為信任裝置,可以取消信任裝置。」
        //   ⇒ v5.136.0 的 `_LXPS_DEVICE_TRUST_AUTOLOGIN = false`(整條路徑關掉)作廢,改回放行,
        //     但**新增一道裝置層封鎖**,補上原設計缺的那一塊。
        //
        //   【為什麼光靠原本的三個取消入口還不夠】
        //     v3.10.3 的信任是**逐帳號**寫在 players/{uid}.trustedDevices[deviceId]。
        //     一台公用 iPad 上,學生 A、B、C 可能各自都按過「信任此裝置」⇒ 雲端有三筆各自獨立的信任。
        //     原本三個取消入口(倒數卡「不是我」/ 首頁信任徽章 / 後台依 email 撤銷)**一次只能取消一位**,
        //     老師走到那台 iPad 前面,得讓三個人分別登入才清得完 —— 在共用平板情境等於清不掉。
        //
        //   【修法:裝置層封鎖旗標】新增 localStorage `lxps_device_shared_no_autologin`。
        //     只要這台 iPad 被標記為公用平板,**不管雲端有幾位學生信任過它,一律不自動登入**,
        //     老師在那台機器上按一次就永久生效,不必知道是誰按的、也不必逐一登入。
        //   ⚠ 這道守門必須排在 `isDeviceTrustedOnCloud` 雲端查詢**之前**:
        //     一來省下不必要的讀取,二來語意上「這台不准自動登入」本來就該壓過「某人信任過這台」。
        //   ⚠ 旗標必須是**裝置層**(已加入 _EXACT_GLOBAL 白名單),不可被 _clearAccountLocalData
        //     的換帳號清理帶走 —— 被清掉的話,下一位學生登入時封鎖就失效,等於沒設。
        // ════════════════════════════════════════════════════════════
        const _LXPS_DEVICE_TRUST_AUTOLOGIN = true;   // ★ v5.137.0 老師裁定恢復（v5.136.0 曾暫設 false）
        let _isDeviceTrusted = false;
        let _deviceTrustBlocked = false;
        // ★★ v5.137.0 — 「剛剛才按過登出」一次性守門(這道不能少)★★
        //   情境:這台是信任裝置 → 學生按登出 → _showLogoutPin 強制 reload →
        //   萬一 signOut 沒把 IndexedDB 的 token 清乾淨(deleteDatabase 常吃到 onblocked),
        //   reload 後 onAuth 會撈到殘留 token、_isAutoRestore 成立 →
        //   **走信任放行,又自動登回剛剛登出的那個帳號** ⇒ 老師看到的就是「登出沒有用」。
        //   ⇒ 登出前寫 sessionStorage 旗標,本次頁載一律不吃信任放行,交回攔截器強制登出。
        //   ⚠ 用 sessionStorage 而非 window 變數:必須撐過那一次 reload。
        //   ⚠ 一次性(讀完即清)+ 5 分鐘時效:只擋登出後緊接著的那一次,
        //     學生下次自己開遊戲時信任裝置照常自動登入,不影響老師要的免重登。
        let _justLoggedOut = false;
        try{
          const _jlo = sessionStorage.getItem('lxps_just_logged_out');
          if(_jlo){
            sessionStorage.removeItem('lxps_just_logged_out');   // 一次性,用完即清
            if(Date.now() - (parseInt(_jlo, 10) || 0) < 300000){
              _justLoggedOut = true;
              console.log('[裝置信任 v5.137.0] 偵測到「剛按過登出」→ 本次頁載不吃信任放行(確保登得掉)');
            }
          }
        }catch(_eJlo){}
        try{
          _deviceTrustBlocked = !!(window._lxpsDeviceTrust
                                && window._lxpsDeviceTrust.isAutoLoginBlockedOnDevice
                                && window._lxpsDeviceTrust.isAutoLoginBlockedOnDevice());
          if(_deviceTrustBlocked){
            console.log('[裝置信任 v5.137.0] 本機已標記為「共用裝置」→ 一律不自動登入(不論雲端有誰信任過)');
          }
        }catch(_eBlk){}
        if(_LXPS_DEVICE_TRUST_AUTOLOGIN && !_deviceTrustBlocked && !_justLoggedOut
           && _isAutoRestore && !_bypassAutoLoginBlock
           && window._lxpsDeviceTrust
           && ((typeof window._lxpsDeviceTrust.trustModeOk === 'function' && window._lxpsDeviceTrust.trustModeOk())   // ★ v5.148.0 不限安裝版
               || (window._lxpsDeviceTrust.isStandalone && window._lxpsDeviceTrust.isStandalone()))){   // 舊條件保留為備援
          try{
            const _deviceId = window._lxpsDeviceTrust.getDeviceId();
            _isDeviceTrusted = await window._lxpsDeviceTrust.isDeviceTrustedOnCloud(user.uid, _deviceId);
            if(_isDeviceTrusted){
              console.log('[裝置信任 v3.10.3] ✅ 此裝置已被信任,放行自動登入(會顯示 3 秒倒數確認卡)');
              // 顯示倒數確認卡 — 不阻塞 onAuth 後續流程,但給玩家機會在 3 秒內取消
              try{
                window._lxpsDeviceTrust.showAutoLoginConfirm(
                  user, _deviceId,
                  function(){ /* 倒數結束自動繼續 — 不做事 */ },
                  async function(){
                    // 玩家點「不是我」→ 強制登出 + 顯示重登卡
                    console.warn('[裝置信任 v3.10.3] 玩家在倒數內取消自動登入');
                    try { await _fbAuth.signOut(); } catch(_){}
                    window._fbUser = null;
                    window._gUserId = null;
                    // ★ v5.166.0 — 同 v3.10.1 攔截器的收尾：學生明確拒絕了這個殘留帳號，
                    //   不能把它留在共用登入戳記裡讓下一位在登入閘門看到快速繼續按鈕。
                    try{
                      localStorage.removeItem('lxps_shared_login_ts');
                      localStorage.removeItem('lxps_shared_login_uid');
                      localStorage.removeItem('lxps_shared_login_email');
                    }catch(_eClrShared2){}
                    // 提示玩家:已撤銷信任 + 請重新登入
                    try{
                      if(typeof window._showSimpleToast === 'function'){
                        window._showSimpleToast('✅ 已撤銷此裝置信任,請重新登入', 'ok');
                      }
                    }catch(_){}
                    // 重整頁面,回到乾淨未登入狀態
                    setTimeout(function(){
                      try{ window.location.reload(); }catch(_){}
                    }, 1500);
                  }
                );
              }catch(_eAlc){ console.warn('[裝置信任 v3.10.3] 倒數確認卡顯示失敗', _eAlc); }
              // 放行 onAuth 後續流程,標記為主動登入
              window._lxpsActiveSignInThisPageload = true;
              // 跳過 v3.10.1 攔截,直接進入後續維修/停權檢查
              // 不能 return — 要繼續往下執行
            }
          }catch(_eTrust){
            console.warn('[裝置信任 v3.10.3] 雲端查詢失敗,fail-safe 走攔截', _eTrust);
            _isDeviceTrusted = false;
          }
        }

        if(_isAutoRestore && !_bypassAutoLoginBlock && !_isDeviceTrusted){
          console.warn('[禁止自動登入 v3.10.1] 偵測到 Firebase 自動還原 token,' +
            'email=' + (user.email || '?') + ' → 強制登出,要求重新登入');
          _fbDiag('autoLoginBlock:偵測到自動還原 (email=' + (user.email || '?') + '),強制 signOut',  'warn');

          // 立刻登出(不清本機資料,讓玩家重新登入後 onAuth 自然走 _clearAccountLocalData 流程)
          try { await _fbAuth.signOut(); } catch(_eSO) { console.warn('[禁止自動登入] signOut 失敗', _eSO); }
          window._fbUser = null;
          window._gUserId = null;
          // ★ v5.166.0(2026-09-10・稽核發現：v5.165.0 新增的「30分鐘共用登入快速繼續」戳記
          //   沒有被這道共用裝置強制登出攔截器清除)— 這裡正是「偵測到殘留帳號、判定不可信、
          //   強制登出」的時刻，若不清掉 lxps_shared_login_ts/uid/email，下一位在 30 分鐘內
          //   打開遊戲(不論大對抗或小遊戲)的同學，仍會在登入閘門看到「✅ 繼續使用 A 的帳號」
          //   這顆快速按鈕，等於把剛剛才被判定為不可信、強制登出的帳號明晃晃留給下一位共用裝置
          //   的同學——違反 v5.136.0 起「共用裝置不留前一位痕跡」的鐵律。純安全性收尾，
          //   不影響按鈕本身在合法情境(同一位同學 30 分鐘內切換兩邊)下的正常運作。
          try{
            localStorage.removeItem('lxps_shared_login_ts');
            localStorage.removeItem('lxps_shared_login_uid');
            localStorage.removeItem('lxps_shared_login_email');
          }catch(_eClrShared){}

          // 顯示提示卡:告訴使用者「為保護你的帳號,請重新登入」
          try {
            const _old = document.getElementById('_auto-login-blocked');
            if(_old) _old.remove();
            const _ov = document.createElement('div');
            _ov.id = '_auto-login-blocked';
            _ov.style.cssText =
              'position:fixed;inset:0;z-index:99996;background:rgba(0,0,0,0.78);' +
              'display:flex;align-items:safe center;justify-content:center;padding:20px;' +
              'font-family:"M PLUS Rounded 1c","Nunito",sans-serif;';
            // ★ v5.156.0（老師截圖裁定「殘留帳號完整顯示，不要有 ***」）— 讓學生一眼確認是不是自己的帳號，
            //   遮掉反而讓人猶豫「是不是我」；完整顯示就是這張卡存在的目的（提示 → 手動重新登入）。
            const _hint = (function(em){
              if(!em || typeof em !== 'string') return '';
              return em;   // ★ v5.156.0（舊：em.slice(0,2)+'***'+em.slice(_at) 遮蔽版）
            })(user.email || '');
            _ov.innerHTML =
              '<div style="background:linear-gradient(135deg,#0e1a2e,#0a0e1c);' +
                'border:3px solid #66aaff;border-radius:22px;' +
                'padding:32px 36px;max-width:min(92vw,540px);max-height:min(92dvh,92vh);' +
                'overflow-y:auto;box-sizing:border-box;text-align:center;' +
                'box-shadow:0 0 40px rgba(80,140,255,0.4);color:#cce0ff;">' +
                '<div style="font-size:54px;margin-bottom:12px;">🔐</div>' +
                '<div style="font-size:26px;font-weight:900;color:#aaccff;margin-bottom:14px;letter-spacing:1.5px;">' +
                  '請重新登入確認身份' +
                '</div>' +
                '<div style="font-size:16px;color:#bbcce0;line-height:1.7;margin-bottom:14px;background:rgba(0,0,0,0.35);padding:14px 16px;border-radius:10px;text-align:left;">' +
                  '為了保護你的帳號安全(尤其是和同學共用裝置時),<br>' +
                  '本遊戲<b style="color:#ffcc66;">不會自動登入任何帳號</b>。<br><br>' +
                  (_hint ? ('剛剛偵測到平板殘留的帳號:<br>' +
                  '<code style="display:inline-block;margin-top:4px;padding:4px 12px;' +
                  'background:rgba(0,0,0,0.4);border:1px solid #556677;border-radius:6px;' +
                  'color:#ffcc66;font-size:15px;">' + _hint + '</code><br><br>') : '') +
                  '請點下方按鈕,<b style="color:#ffcc66;">重新登入你自己的帳號</b>。' +
                '</div>' +
                '<button id="_auto-login-blocked-btn" style="width:100%;padding:14px;font-size:18px;font-weight:900;' +
                  'background:linear-gradient(135deg,rgba(80,150,255,0.35),rgba(40,100,200,0.35));' +
                  'border:2px solid #66aaff;color:#cce4ff;border-radius:12px;cursor:pointer;' +
                  'font-family:inherit;letter-spacing:1.5px;position:sticky;bottom:-2px;' +
                  'box-shadow:0 -10px 14px -6px #0a0e1c;">' +
                  '🔑 我知道了,去登入' +
                '</button>' +
                '<div style="font-size:12px;color:#556677;margin-top:14px;opacity:0.7;">v3.10.4</div>' +
              '</div>';
            document.body.appendChild(_ov);
            const _btn = document.getElementById('_auto-login-blocked-btn');
            if(_btn) _btn.onclick = function(){
              _ov.remove();
              // 不主動觸發登入,讓玩家自己點首頁的登入按鈕
              // (避免老師擔心 popup 被擋的問題,讓他們自己選 popup/redirect)
            };
          } catch(_eUI) { console.warn('[禁止自動登入] 顯示提示卡失敗', _eUI); }

          return; // 停止後續登入流程(不讀雲端、不建立 session)
        }

        // 流程合法 → 在 runtime 變數標記「本次頁面載入已完成主動登入」
        // (這個變數會隨 reload / 關閉而清空,完全不需要清理)
        window._lxpsActiveSignInThisPageload = true;
      } catch(_eAR) {
        console.warn('[禁止自動登入] 例外,放行避免鎖死', _eAR);
      }
      // ════════════════════════════════════════════════════════════
      // ★ v3.10.1 攔截結束 — 以下沿用原本維修/停權檢查流程
      // ════════════════════════════════════════════════════════════
      const _DEV_EMAILS = ['clarebox123@gmail.com', 'clarebox123@lsps.tp.edu.tw', 'asemia@gmail.com'];
      const _isDev = _DEV_EMAILS.includes((user.email || '').toLowerCase());
      // ════════════════════════════════════════════════════════════
      // ★★ v5.152.0（2026-09-08・老師回報「25 台 iPad 同時登入仍卡登入／首頁左上沒帳號列」）★★
      //   【登入關鍵路徑瘦身・第一刀：帳號列先畫、三道閘門讀取並行】
      //   舊流程：閘門放行 → 維修(讀 stats/global) → 休息排程(讀 gameConfig/restSchedule) → 停權(讀 players/uid)
      //           三次 **序列** await 之後才 window._fbUser / _gUserId / 畫帳號列。
      //   校網 25 台同時開機時 Firestore 首次連線常常一等 10～30 秒，三道序列閘門就是 30～90 秒；
      //   這段期間 _fbUser 還是 null ⇒ v5.141.0 的自癒輪詢也畫不了（守門要 _gUserId）⇒ 老師看到的
      //   「看起來登入了，首頁左上卻沒帳號」就是卡在這三道閘門。
      //   修法：①帳號列**在閘門讀取之前**就先畫（純顯示層；維修/停權若踢人會 signOut → onAuth(null)
      //         分支本來就會把帳號列清掉，零回歸）②三道閘門讀取改 Promise.allSettled 一次併發，
      //         並各套 _fbRaceTimeout 10 秒（原本三支 helper 讀失敗本就「放行」，逾時視同讀失敗語意不變）
      //         ③players/uid 這一讀的結果快取到 window._lxpsLoginPlayerDoc，供下方 _fbTrackPlayerStats /
      //           暱稱同步重用 ⇒ 同一份文件從登入流程讀 4 次降為 1 次（第 4 次是 gameCloudLoad 的權威讀，保留）。
      // ════════════════════════════════════════════════════════════
      try{ window._lxpsPaintAccountBar(user); }catch(_){}
      let _gateM = { enabled:false }, _gateRest = null, _gatePlayerSnap = null;
      window._lxpsLoginPlayerDoc = null;
      if(!_isDev){
        try{
          const _gRace = function(p, label){
            return (typeof window._fbRaceTimeout === 'function') ? window._fbRaceTimeout(p, 10000, label) : p;
          };
          const _gr = await Promise.allSettled([
            _gRace(window._fbGetMaintenance(), 'gate-maint'),
            _gRace(window._fbGetRestSchedule(), 'gate-rest'),
            _gRace(getDoc(doc(_fbDb, 'players', user.uid)), 'gate-player')
          ]);
          if(_gr[0].status === 'fulfilled' && _gr[0].value) _gateM = _gr[0].value;
          else console.warn('[登入閘門 v5.152.0] 維修狀態讀取逾時/失敗,放行', _gr[0].reason);
          if(_gr[1].status === 'fulfilled') _gateRest = _gr[1].value;
          else console.warn('[登入閘門 v5.152.0] 休息排程讀取逾時/失敗,放行', _gr[1].reason);
          if(_gr[2].status === 'fulfilled'){
            _gatePlayerSnap = _gr[2].value;
            try{
              window._lxpsLoginPlayerDoc = {
                uid: user.uid,
                existed: !!(_gatePlayerSnap && _gatePlayerSnap.exists()),
                data: (_gatePlayerSnap && _gatePlayerSnap.exists()) ? (_gatePlayerSnap.data() || {}) : {},
                at: Date.now()
              };
            }catch(_){}
          } else console.warn('[登入閘門 v5.152.0] 玩家主檔讀取逾時/失敗,停權檢查放行', _gr[2].reason);
        }catch(_eGate){ console.warn('[登入閘門 v5.152.0] 併發讀取例外,全部放行', _eGate); }
      }
      if(!_isDev) {
        try {
          const m = _gateM;   // ★ v5.152.0（舊:await window._fbGetMaintenance()）
          if(m && m.enabled) {
            // ════════════════════════════════════════════════════════════════
            // ★ v3.11.4(2026-05-27) — 維修時允許「已授權下載學生」保留登入態(BUG #9)
            //   老師需求:讓學生可以在維修時登入帳號進行下載安裝
            //   邏輯:
            //     - 若學生已有「approved 且未過期」的下載權限 → 不登出,只顯示維修畫面 +
            //       「📥 我只是要下載安裝」按鈕,讓他可以走完整 PWA precache 流程
            //     - 沒授權的學生 → 維持原行為(登出 + 顯示維修畫面 + 按鈕按了也只是 sw-light)
            //   注意:這只允許「下載」這個唯一動作,其他遊戲功能仍鎖住(維修畫面蓋住整個介面)
            // ════════════════════════════════════════════════════════════════
            let _hasDownloadPerm = false;
            try{
              if(typeof window._fbGetDownloadPermission === 'function'){
                const _dp = await window._fbGetDownloadPermission(user.uid);
                _hasDownloadPerm = !!(_dp && _dp.status === 'approved'
                  && _dp.expiresAt && _dp.expiresAt > Date.now());
              }
            }catch(_eDp){ console.warn('[維修+下載授權檢查] 失敗,當作沒授權', _eDp); }
            if(_hasDownloadPerm){
              console.log('[維修模式 v3.11.4] 偵測到該玩家有有效下載授權,保留登入態僅顯示維修畫面 + 下載按鈕');
              // 保留 window._fbUser / window._gUserId,但顯示維修畫面
              window._fbUser = user;
              window._gUserId = user.uid;
              try { window._showMaintenanceOverlay && window._showMaintenanceOverlay(m); }
              catch(_e) { console.error(_e); }
              return;  // 停止後續登入流程(不讀雲端,不建 session)— 但保留 _fbUser 讓下載 path 能用
            }
            console.warn('[維修模式] 已啟動,非管理員(且無下載授權)被拒絕登入');
            // 立刻登出
            try { await _fbAuth.signOut(); } catch(_){}
            window._fbUser = null;
            window._gUserId = null;
            // 顯示維修畫面
            try { window._showMaintenanceOverlay && window._showMaintenanceOverlay(m); }
            catch(_e) { console.error(_e); }
            return;  // 停止後續登入流程
          }
        } catch(_e) { console.warn('[維修模式] 檢查失敗,放行以避免全體鎖死', _e); }
      }

      // ★★★ v3.15.9 — 休息排程登入閘 ★★★
      //   非管理員在「休息時段」不得登入(同維修模式:先存後擋)。讀失敗一律放行,避免誤鎖全體。
      if(!_isDev){
        try {
          const _restSch = (_gateRest !== null) ? _gateRest : { enabled:false };   // ★ v5.152.0（舊:await window._fbGetRestSchedule()）
          const _restEv = (typeof window._restEvalState === 'function')
            ? window._restEvalState(_restSch, new Date()) : { state:'open' };
          if(_restEv && _restEv.state === 'rest'){
            console.warn('[休息排程] 休息時段,非管理員登入封鎖');
            try { if(window._progressLoaded) gameCloudSave(); } catch(_){}
            try { if(window._fbSignOut) window._fbSignOut(); } catch(_){}
            try {
              window._restScheduleCache = _restSch;
              if(typeof window._showRestOverlay === 'function') window._showRestOverlay(_restSch, _restEv.bootAtMs);
            } catch(_e2) { console.error('[休息排程] 顯示休息畫面失敗', _e2); }
            return;  // 停止後續登入流程
          }
        } catch(_eRest) { console.warn('[休息排程] 登入檢查失敗,放行以避免誤鎖', _eRest); }
      }

      // ★★★ v3.5.0 — 停權檢查 ★★★
      //   若 players/{uid}._suspended === true,非管理員一律踢出,顯示停權說明
      if(!_isDev){
        try {
          const _snap = _gatePlayerSnap;   // ★ v5.152.0（舊:await getDoc(doc(_fbDb,'players',user.uid))）讀失敗＝null＝放行（與舊 catch 語意一致）
          if(_snap && _snap.exists() && _snap.data()._suspended === true){
            const _data = _snap.data();
            const _reason = _data._suspendReason || '帳號資料異常,已暫停存取';
            const _at = _data._suspendedAt ? new Date(_data._suspendedAt).toLocaleString() : '';
            console.warn('[停權] 帳號已被管理員停權,uid=' + user.uid);
            try { await _fbAuth.signOut(); } catch(_e2) {}
            window._fbUser = null;
            window._gUserId = null;
            // 顯示停權說明畫面
            try {
              const _ov = document.createElement('div');
              _ov.id = '_suspended-overlay';
              _ov.style.cssText = 'position:fixed;inset:0;z-index:99999;background:rgba(0,0,0,0.9);' +
                'display:flex;align-items:center;justify-content:center;padding:20px;' +
                'font-family:"M PLUS Rounded 1c","Nunito",sans-serif;';
              _ov.innerHTML =
                '<div style="background:linear-gradient(135deg,rgba(50,20,20,0.98),rgba(30,10,10,0.99));' +
                   'border:3px solid rgba(255,100,100,0.8);border-radius:18px;padding:32px 36px;' +
                   'max-width:520px;width:100%;color:#ffdddd;box-shadow:0 8px 36px rgba(255,80,80,0.3);">' +
                  '<div style="font-size:24px;font-weight:900;color:#ff8888;margin-bottom:14px;text-align:center;letter-spacing:1.5px;">' +
                    '🚫 帳號暫停存取' +
                  '</div>' +
                  '<div style="font-size:15px;line-height:1.7;margin-bottom:14px;background:rgba(0,0,0,0.4);padding:14px 16px;border-radius:10px;">' +
                    '<div style="margin-bottom:8px;"><b style="color:#ffaaaa;">原因:</b>' +
                      String(_reason).replace(/[<>]/g, '') +
                    '</div>' +
                    (_at ? ('<div style="font-size:13px;color:#ccaaaa;">停權時間:' + _at + '</div>') : '') +
                  '</div>' +
                  '<div style="font-size:14px;color:#ddaaaa;line-height:1.6;margin-bottom:18px;">' +
                    '若您認為這是誤判,請聯絡管理員老師處理。' +
                  '</div>' +
                  '<button onclick="location.reload()" style="width:100%;padding:11px;font-size:15px;font-weight:800;' +
                    'background:rgba(120,30,30,0.6);border:2px solid #ff6666;color:#ffcccc;border-radius:8px;cursor:pointer;font-family:inherit;">' +
                    '重新整理' +
                  '</button>' +
                '</div>';
              // 移除舊的
              const _old = document.getElementById('_suspended-overlay');
              if(_old) _old.remove();
              document.body.appendChild(_ov);
            } catch(_e3) { console.error(_e3); }
            return; // 停止後續登入流程
          }
        } catch(_e) { console.warn('[停權檢查] 讀取失敗,放行', _e); }
      }

      try {
        const _lastUid = localStorage.getItem('lxps_last_active_uid') || '';
        if(_lastUid && _lastUid !== user.uid){
          console.log('🔄 [換帳號清理] 偵測到不同帳號(前次=' + _lastUid.slice(0,8)
            + '..., 本次=' + user.uid.slice(0,8) + '...),清除本機殘留');
          // ★ v3.5.18 — 換帳號前先「保護前一位學生」的戰鬥快照(老師 2026/5/22 回報)
          //   情境:小明在 iPad-01 打 BOSS 沒登出 → 小華第 2 節拿到同台 iPad-01 登入自己帳號
          //   舊版:_clearAccountLocalData 直接清掉 adv_battle_snap → 小明回家進度全失
          //   新版:在清理前,把本機 adv_battle_snap 用「小明的 uid」搬到 pendingHandoff
          //         小明下次登入會被偵測到並補寫回他的雲端 advSnapshot
          //   雙路徑(任一成功即可救回):
          //     A. 雲端 pendingHandoffs/{小明 uid}:由小華這台寫入,小明下次讀
          //     B. 本機 lxps_pending_handoff_{小明 uid}:留在本機,小明下次回到同台時讀
          try{
            const _battleSnapRaw = localStorage.getItem('adv_battle_snap');
            if(_battleSnapRaw){
              const _snap = JSON.parse(_battleSnapRaw);
              // 只搶救「有效快照」:有 stage 且 < 24 小時
              const _isValid = _snap && _snap.stage
                && (_snap.ts && (Date.now() - _snap.ts) < 24*60*60*1000)
                && _snap.battleState && _snap.battleState.p1 && _snap.battleState.p1.length;
              if(_isValid){
                const _handoffPayload = {
                  fromUid: _lastUid,
                  rescuedByUid: user.uid,    // 救援者(用來除錯)
                  rescuedByEmail: (user.email || '').toLowerCase(),
                  snap: _snap,
                  savedAt: Date.now(),
                  reason: 'switch_account_on_shared_device',
                };
                // 路徑 A:寫雲端 pendingHandoffs/{小明 uid}
                try{
                  if(typeof window._fbSaveHandoff === 'function'){
                    // 不 await,避免阻塞 onAuth(若失敗下面 fallback 會接手)
                    window._fbSaveHandoff(_lastUid, _handoffPayload)
                      .then(function(ok){
                        if(ok) console.log('🤝 [換帳號保護] 已用雲端 handoff 為前位 (' + _lastUid.slice(0,8) + '...) 保存快照');
                      })
                      .catch(function(e){ console.warn('🤝 [換帳號保護] 雲端 handoff 寫入失敗', e); });
                  }
                }catch(_eH){ console.warn('🤝 [換帳號保護] _fbSaveHandoff 例外', _eH); }
                // 路徑 B:寫本機 lxps_pending_handoff_{小明 uid}
                //   雲端寫不寫得進去都先存本機(若小明下次回來這台就能直接讀)
                try{
                  localStorage.setItem('lxps_pending_handoff_' + _lastUid, JSON.stringify(_handoffPayload));
                  console.log('🤝 [換帳號保護] 已用本機 handoff 為前位 (' + _lastUid.slice(0,8) + '...) 保存快照(round=' + (_snap.round || '?') + ', stage=' + _snap.stage + ')');
                }catch(_eL){ console.warn('🤝 [換帳號保護] 本機 handoff 寫入失敗', _eL); }
              } else {
                console.log('🤝 [換帳號保護] 偵測到 adv_battle_snap 但內容無效或過期,跳過搶救');
              }
            }
          }catch(_eR){ console.warn('🤝 [換帳號保護] 搶救流程例外', _eR); }
          // ★ v3.5.72 — 換帳號保護升級:同時搬「前一位的本地存檔」到 handoff
          //   背景:學生 2026-05-24 案例 — 小明本地有 totalLv 141 / 11 隻英雄,雲端有別人的
          //         19 隻 / totalLv 122,健康度守門擋下,但學生今天的進度永遠寫不回雲端
          //   根因:只有 adv_battle_snap 被搬到 handoff,lxps_{uid}_progress 直接被清掉
          //         → 前一位學生的「日常進度」沒任何救援機制
          //   修法:同樣搬到 pendingHandoff,小明下次登入時 _fbConsumeHandoff 可以還原
          try{
            const _progressKey = 'lxps_' + _lastUid + '_progress';
            const _progressRaw = localStorage.getItem(_progressKey);
            if(_progressRaw && _progressRaw.length > 100){
              try{
                const _progressObj = JSON.parse(_progressRaw);
                // 驗證有效性:必須有 _dataSummary 或基本資料,且 24 小時內
                const _summaryTs = (_progressObj._dataSummary && _progressObj._dataSummary.ts) || _progressObj.savedAt || 0;
                const _isProgressValid = _progressObj
                  && _summaryTs
                  && (Date.now() - _summaryTs) < 7 * 24 * 60 * 60 * 1000  // 7 天內(比戰鬥快照寬鬆,日常進度更值得救)
                  && _progressObj._dataSummary
                  && ((_progressObj._dataSummary.unlockedCount || 0) > 0 || (_progressObj._dataSummary.totalHeroLv || 0) > 0);
                if(_isProgressValid){
                  const _progressHandoffKey = 'lxps_pending_progress_handoff_' + _lastUid;
                  const _progressHandoffPayload = {
                    fromUid: _lastUid,
                    rescuedByUid: user.uid,
                    rescuedByEmail: (user.email || '').toLowerCase(),
                    progressRaw: _progressRaw,  // 原始字串,避免序列化失真
                    summary: _progressObj._dataSummary,
                    savedAt: Date.now(),
                    reason: 'switch_account_local_progress_rescue',
                  };
                  // 路徑 A:本機 lxps_pending_progress_handoff_{uid}(主路徑,寫本機絕對不會失敗)
                  try{
                    localStorage.setItem(_progressHandoffKey, JSON.stringify(_progressHandoffPayload));
                    console.log('🤝 [本地存檔保護] 已搶救前位 (' + _lastUid.slice(0,8) + '...) 的日常進度,' +
                      'unlocked=' + (_progressObj._dataSummary.unlockedCount || 0) +
                      ', totalLv=' + (_progressObj._dataSummary.totalHeroLv || 0) +
                      ', coins=' + (_progressObj._dataSummary.coins || 0));
                  }catch(_eLp){ console.warn('🤝 [本地存檔保護] 本機寫入失敗', _eLp); }
                  // 路徑 B:雲端 pendingHandoffs/{lastUid} 補 progressSummary 欄位
                  //   (Rules 不見得開放跨用戶寫,失敗也無妨,有本機 fallback)
                  try{
                    if(typeof window._fbSaveHandoff === 'function'){
                      window._fbSaveHandoff(_lastUid, {
                        ..._progressHandoffPayload,
                        // progressRaw 字串可能很大,寫雲端只放摘要 + 簡化資料避免超 limit
                        progressRaw: undefined,
                        progressSummaryOnly: true,
                        summary: _progressObj._dataSummary,
                      })
                        .then(function(ok){
                          if(ok) console.log('🤝 [本地存檔保護] 雲端 handoff 摘要寫入成功');
                        })
                        .catch(function(e){ console.warn('🤝 [本地存檔保護] 雲端 handoff 寫入失敗(本機 fallback 仍有效)', e); });
                    }
                  }catch(_eHp){}
                } else {
                  console.log('🤝 [本地存檔保護] 偵測到前位本地存檔但無效或過期,跳過搶救');
                }
              }catch(_eParse){ console.warn('🤝 [本地存檔保護] 解析失敗', _eParse); }
            }
          }catch(_eL){ console.warn('🤝 [本地存檔保護] 搶救流程例外', _eL); }
          // 不需 await,本地清理是同步的
          if(typeof window._clearAccountLocalData === 'function'){
            window._clearAccountLocalData('switch_account');
          }
        }
        // 不論是否同帳號,都更新「本次活躍 uid」
        localStorage.setItem('lxps_last_active_uid', user.uid);
      } catch(_e) { console.warn('[換帳號清理] 失敗', _e); }

      window._fbUser = user;
      window._gUserId = user.uid; // ★ 確保 _gUserId 同步
      // ★ v3.11.14(2026-05-28) — 標記「本 session 已真正登入過」,
      //   給強制登入閘門 openAdventureOverlay 判斷用(避免遊戲中 race 誤擋)。
      window._lxpsLoggedInThisSession = true;
      // ★ v5.141.0 — 帳號列先畫再說（不等下面 6～7 次 Firestore 讀寫）；雲端暱稱到貨後原段落會再覆蓋成最終值
      try{ window._lxpsPaintAccountBar(user); }catch(_){}
      // ════════════════════════════════════════════════════════════
      // ★★ v5.152.0 — 【登入關鍵路徑瘦身・第二刀：統計與 presence 移出 await 鏈】★★
      //   ①_fbTrackPlayerStats 內含一筆 stats/global 寫入（active_今日 +1）—— 這是**全校共用的單一熱門文件**，
      //     25 台同時登入 = 25 筆寫入擠同一份文件（Firestore 單文件建議 ≤1 寫/秒），每台都 await 它
      //     ⇒ 排隊等待直接算進登入時間。改為：延後到 gameCloudLoad 之後、加 0～8 秒隨機抖動錯開、且不 await。
      //   ②_fbStartPresence（presences/uid 寫入）同樣改不 await（它自己有心跳補寫，登入時晚幾秒無差）。
      //   兩者一律包 catch，失敗只 console.warn，零影響登入。
      // ════════════════════════════════════════════════════════════
      try{
        const _statsJitter = 300 + Math.floor(Math.random() * 8000);
        setTimeout(function(){
          try{ if(window._gUserId === user.uid) window._fbTrackPlayerStats(user.uid).catch(function(e){ console.warn('[人數統計 v5.152.0 延後]', e); }); }catch(_e){}
        }, _statsJitter);
      }catch(_){}
      // ★ v1.1.0 — 啟動 Presence(目前在線追蹤)
      try {
        const _presenceName = (user.displayName || user.email || '匿名').slice(0, 30);
        if(typeof window._fbStartPresence === 'function'){
          window._fbStartPresence(user.uid, _presenceName).catch(function(_e){ console.warn('[Presence] 登入時啟動失敗', _e); });   // ★ v5.152.0 不 await
        }
      } catch(_e) { console.warn('[Presence] 登入時啟動失敗', _e); }
      // ★ v3.35.0 — 啟動好友「登入通知」常駐輕量輪詢(只讀有勾通知的好友·沒勾=零讀取)
      try{ if(typeof window._fbStartFriendNotifyPoll === 'function') window._fbStartFriendNotifyPoll(); }catch(_e){ console.warn('[好友登入通知] 啟動輪詢失敗', _e); }
      try {
        let _retry = 0;
        const _tryStartInviteSub = function(){
          if(typeof window._wbStartInviteNotifSub === 'function'){
            try{ window._wbStartInviteNotifSub(); }catch(_){}
            return true;
          }
          return false;
        };
        if(!_tryStartInviteSub()){
          const _h = setInterval(function(){
            _retry++;
            if(_retry > 30){ clearInterval(_h); return; }
            if(_tryStartInviteSub()) clearInterval(_h);
          }, 1000);
        }
      } catch(_e) { console.warn('[WB-Notif] 登入時啟動邀請訂閱失敗', _e); }

      // ════════════════════════════════════════════════════════════
      // ★ v3.12.0(2026-05-29)— 登入後 3 秒檢查並領取 pendingGifts
      //   延 3 秒讓:① 雲端資料載入完成(_applySafeData 套完 friendshipHeart)
      //              ② UI 渲染完成(避免登入畫面還在的時候彈通知)
      // ════════════════════════════════════════════════════════════
      try {
        setTimeout(function(){
          try{
            if(typeof window._giftClaimPendingGifts === 'function' && window._gUserId){
              window._giftClaimPendingGifts();
            }
          }catch(_eClaim){ console.warn('[v3.12.0 onAuth 領禮]', _eClaim); }
        }, 3000);
      } catch(_e) { console.warn('[v3.12.0 登入後領禮] setTimeout 失敗', _e); }
      // ★ 同步 email 到 Firestore（供好友搜尋）
      try {
        const _nick = localStorage.getItem('lxps_nickname_' + user.uid);
        // ★ 跨裝置修復：新裝置首次登入時，先讀雲端既有暱稱，避免被 Google Auth 名稱覆蓋
        let _remoteName = '';
        let _remoteEmail = '';
        // ★ v5.152.0 — 優先重用閘門那一次讀到的 players/uid（同一份文件登入流程原本讀 4 次）；
        //   沒有快取（GM 帳號跳過閘門讀取／閘門逾時）才退回原本的 getDoc，語意零改動。
        try {
          const _lc = window._lxpsLoginPlayerDoc;
          if(_lc && _lc.uid === user.uid && _lc.existed){
            _remoteName = _lc.data.displayName || '';
            _remoteEmail = _lc.data.email || '';
          } else {
            const _snap = await getDoc(doc(_fbDb, 'players', user.uid));
            if(_snap.exists()){ _remoteName = _snap.data().displayName || ''; _remoteEmail = _snap.data().email || ''; }
          }
        } catch(_){}
        // 優先序：本地暱稱 > 雲端暱稱 > Google Auth 名稱 > email
        const _finalName = _nick || _remoteName || user.displayName || user.email || '';
        const _finalEmail = (user.email || '').toLowerCase();
        // ★ v5.152.0 — 雲端已是同值 ⇒ 完全跳過這筆寫入（含最壞 3 次×2 秒的重試等待）。
        //   絕大多數登入都是「同一個人同一個暱稱」，這一筆每次都白寫，25 台同時開機時還佔頻寬。
        //   有任何不同（首登／改過暱稱／email 缺）才寫，寫入語意與舊碼完全相同。
        const _needNameWrite = !(_remoteName === _finalName && _remoteEmail === _finalEmail);
        // ★ v1.1.0 — 加重試 3 次,避免配額爆掉時暱稱靜默無法上雲
        let _writeOk = !_needNameWrite;
        for(let _r = 0; _needNameWrite && _r < 3; _r++){
          try {
            await setDoc(doc(_fbDb, 'players', user.uid), {
              email: _finalEmail,
              displayName: _finalName
            }, { merge: true });
            _writeOk = true;
            break;
          } catch(_writeE) {
            console.warn('[登入暱稱同步] 第 ' + (_r+1) + ' 次失敗', _writeE && _writeE.message);
            if(_r < 2) await new Promise(r => setTimeout(r, 2000));
          }
        }
        if(!_writeOk){
          console.warn('[登入暱稱同步] 3 次都失敗,將在背景定期重試');
          // 5 分鐘後再試一次
          setTimeout(async () => {
            try {
              await setDoc(doc(_fbDb, 'players', user.uid), {
                email: (user.email || '').toLowerCase(),
                displayName: _finalName
              }, { merge: true });
              console.info('[登入暱稱同步] 背景重試成功');
            } catch(_) { /* 放棄,等下次登入再試 */ }
          }, 5 * 60 * 1000);
        }
        // 雲端有自訂暱稱但本地沒有 → 補寫到本地，讓下面 UI 更新直接讀到正確值
        if(!_nick && _remoteName) {
          localStorage.setItem('lxps_nickname_' + user.uid, _remoteName);
        }
      } catch(e) { console.warn('[好友系統] email 同步失敗', e); }
      // 更新 UI
      const av = document.getElementById('google-avatar');
      const nm = document.getElementById('google-name');
      const btnWrap = document.getElementById('google-login-btn-wrap');
      const userInfo = document.getElementById('google-user-info');
      if(av) av.src = user.photoURL || '';
      // ★ 暱稱優先，次為 displayName
      const _savedNick = localStorage.getItem('lxps_nickname_' + user.uid);
      const _dispName = _savedNick || user.displayName || user.email || '';
      window._playerNickname = _dispName;
      // ★ v3.11.16 — 玩家可見處統一格式「快樂的小狐狸 (5324蔣同學)」
      const _shownName = (typeof window._formatPlayerDisplayName === 'function')
        ? window._formatPlayerDisplayName(_dispName, user.email) : _dispName;
      if(nm){ nm.textContent = _shownName; nm.title = '點擊修改暱稱'; nm.style.cursor='pointer'; nm.onclick = openNicknameModal; }
      if(btnWrap) btnWrap.style.display = 'none';
      if(userInfo) userInfo.style.display = 'flex';
      // ★ v5.139.0 — 帳號列顯示的同一刻就同步「🔐 信任裝置」鈕,不必等下一次輪詢(最長 1 秒)
      try{ if(typeof window._syncTrustEntryBtn === 'function') window._syncTrustEntryBtn(); }catch(_){}
      try {
        if(typeof window._addRecentAccount === 'function'){
          window._addRecentAccount({
            uid: user.uid,
            email: user.email,
            displayName: _dispName,
            photoURL: user.photoURL || '',
          });
        }
      } catch(_e) { console.warn('[最近帳號] 寫入失敗', _e); }
      // ★ 關閉登入選擇視窗（若還開著）
      const lgModal = document.getElementById('login-gate-modal');
      if(lgModal) lgModal.style.display = 'none';
      // ★ v3.15.93 — 登入流程一開始就蓋上全螢幕「課堂練習營」(背景照常 gameCloudLoad);
      //   純覆蓋層,任何失敗都不影響下方真正的登入/載入。雲端就緒後會通知可進入。
      try{ if(typeof window._campStart === 'function') window._campStart(user); }catch(_eCampS){ console.warn('[練習營] 啟動失敗', _eCampS); }
      window._kickedFromSession = false;
      // ★ 1. 先建立 session 並踢掉其他裝置(讓 A 電腦的下次心跳會發現自己被踢)
      const _sid = localStorage.getItem('lxps_session_' + user.uid) || (Math.random().toString(36).slice(2) + Date.now().toString(36));
      localStorage.setItem('lxps_session_' + user.uid, _sid);
      window._mySessionId = _sid;
      if(window._fbCreateSession) {
        try{ await window._fbCreateSession(user.uid, _sid); }catch(_){}
      }
      // ★ 2. 從雲端讀取最新進度(此時 A 電腦的存檔權已被搶走,雲端是 A 最後寫入版)
      if(typeof window.gameCloudLoad === 'function') await window.gameCloudLoad(user.uid);
      // ★ v3.15.93 — 雲端讀取完成 = 帳號資料就緒 → 通知練習營頂端「已經可以進入遊戲」
      try{ window._campGameReady = true; if(typeof window._campMarkReady === 'function') window._campMarkReady(); }catch(_eCampR){}
      // ★ v3.15.80 — 登入載入完成後同步召喚紀錄(雲端↔本地合併,換裝置/清快取也看得到完整紀錄)
      try{ if(typeof window._fbSyncSummonHistoryOnLogin === 'function') window._fbSyncSummonHistoryOnLogin(); }catch(_){}
      // ★ v3.17.6 — 登入同步召喚卷 UID 帳本(雲端↔本地合併·只增不減·換裝置也看得到完整領/用紀錄)
      try{ if(typeof window._fbSyncTicketLedgerOnLogin === 'function') window._fbSyncTicketLedgerOnLogin(); }catch(_){}
      // ★ v3.16.97 — 登入對帳:補上「已領課堂獎勵自選卷·實際沒拿到的」(安全不等式·永不重複發)
      try{ if(typeof window._fbReconcileGmClassRewardTickets === 'function') window._fbReconcileGmClassRewardTickets(); }catch(_){}
      // ★ v3.17.8 — 同款登入對帳,補齊序號兌換 / 全體獎勵漏拿到的自選卷(同一套安全不等式·永不重複發)
      try{ if(typeof window._fbReconcileRedeemCodeTickets === 'function') window._fbReconcileRedeemCodeTickets(); }catch(_){}
      try{ if(typeof window._fbReconcileGlobalRewardTickets === 'function') window._fbReconcileGlobalRewardTickets(); }catch(_){}
      console.log('✅ 已登入:', user.displayName);

      // ★ v3.10.3(2026-05-26) — PWA 主動登入完成後,5 秒提示是否「信任此裝置」
      //   只對 PWA standalone 模式 + 未信任的裝置出現,網頁版完全不影響
      //   提示卡 8 秒後自動消失(非阻斷式 toast,玩家可忽略)
      try{
        if(window._lxpsDeviceTrust
           && ((typeof window._lxpsDeviceTrust.trustModeOk === 'function' && window._lxpsDeviceTrust.trustModeOk())   // ★ v5.148.0 不限安裝版
               || (window._lxpsDeviceTrust.isStandalone && window._lxpsDeviceTrust.isStandalone()))){
          setTimeout(async function(){
            try{
              const _deviceId = window._lxpsDeviceTrust.getDeviceId();
              // 已信任就不再提示(badge 自然顯示「已信任」狀態)
              const _trusted = await window._lxpsDeviceTrust.isDeviceTrustedOnCloud(user.uid, _deviceId);
              if(_trusted) return;
              // 顯示輕量 toast(8 秒自動消失,可點開設定面板)
              _showTrustHintToast();
            }catch(_e){ console.warn('[裝置信任 hint] 失敗', _e); }
          }, 5000);
        }
      }catch(_){}
      // ★ v3.5.18 — 換帳號保護:接收 handoff(若上次有別台/別人幫我保存戰鬥快照)
      //   情境(配對 onAuth 內換帳號保護那一段):
      //     - 小明上次在 iPad-01 沒登出,小華在 iPad-01 登入時把小明的戰鬥快照搬到 handoff
      //     - 現在小明從家裡(或別台)登入 → 這段會檢查兩條 handoff 路徑(雲端 / 本機)
      //     - 找到就寫回 adv_battle_snap,讓下面的 _advCheckCrashRecovery 能彈續戰提示
      try{
        let _handoff = null;
        // 路徑 A:從雲端 pendingHandoffs 讀(要 Firestore Rules 設定才會成功)
        try{
          if(typeof window._fbConsumeHandoff === 'function'){
            _handoff = await window._fbConsumeHandoff(user.uid);
          }
        }catch(_eC){ console.warn('🤝 [接收 handoff] 雲端讀取例外', _eC); }
        // 路徑 B:從本機 fallback 讀(若小明回到原台,雲端 rules 沒設也救得回)
        if(!_handoff){
          try{
            const _localKey = 'lxps_pending_handoff_' + user.uid;
            const _localRaw = localStorage.getItem(_localKey);
            if(_localRaw){
              _handoff = JSON.parse(_localRaw);
              localStorage.removeItem(_localKey);
              console.log('🤝 [接收 handoff] 從本機 fallback 找到 handoff,已讀並清除');
            }
          }catch(_eL){ console.warn('🤝 [接收 handoff] 本機 fallback 讀取失敗', _eL); }
        }
        // 驗證 handoff 內容並寫回 adv_battle_snap
        if(_handoff && _handoff.snap){
          const _hSnap = _handoff.snap;
          const _isValid = _hSnap.stage
            && (_hSnap.ts && (Date.now() - _hSnap.ts) < 24*60*60*1000)
            && _hSnap.battleState && _hSnap.battleState.p1 && _hSnap.battleState.p1.length;
          if(_isValid){
            try{
              localStorage.setItem('adv_battle_snap', JSON.stringify(_hSnap));
              console.log('🤝 [接收 handoff] ✅ 已還原戰鬥快照(round=' + (_hSnap.round || '?')
                + ', stage=' + _hSnap.stage + ', 救援者=' + (_handoff.rescuedByEmail || '?') + ')');
              // 也順手寫入 advSnapshot 給 gameCloudSave 下次帶上雲端
              // (不立即呼叫 gameCloudSave,因為 _progressLoaded 可能還在路上;
              //  下面 _advCheckCrashRecovery 彈出續戰提示讓玩家選,玩家選續戰時自然會寫雲端)
            }catch(_eW){ console.warn('🤝 [接收 handoff] 寫入 adv_battle_snap 失敗', _eW); }
          } else {
            console.log('🤝 [接收 handoff] 取得 handoff 但內容無效或過期,丟棄');
          }
        }
      }catch(_eHo){ console.warn('🤝 [接收 handoff] 整體流程例外', _eHo); }
      // ★ v3.5.72 — 接收「本地存檔 progress handoff」(共用平板換帳號保護升級)
      //   情境:小明在 iPad-01 玩到 totalLv 141 → 小華登入時把小明的 progress 搬到 handoff
      //         現在小明回到 iPad-01(或任何裝置)登入 → 這段檢查 progress handoff
      //   策略:不直接覆蓋本地 lxps_{uid}_progress(可能小明已經在新裝置玩過),而是:
      //         1. 比對 handoff 摘要 vs 雲端剛載入的資料,選分數較高者
      //         2. 若 handoff 比雲端豐富 → 把 handoff 的 progressRaw 寫回 lxps_{uid}_progress
      //            → 觸發 _applySafeData 走「雲端少於本地」的保護路徑,本地資料勝出
      //         3. 標記 _adminForceRestore 繞過健康度守門,讓 autosave 把這份豐富版寫回雲端
      try{
        const _progressHandoffKey = 'lxps_pending_progress_handoff_' + user.uid;
        const _progressHandoffRaw = localStorage.getItem(_progressHandoffKey);
        if(_progressHandoffRaw){
          try{
            const _phPayload = JSON.parse(_progressHandoffRaw);
            // 必須在 7 天內,且有 progressRaw(本機 handoff 才有,雲端 handoff 只有摘要)
            const _phAgeOk = _phPayload && _phPayload.savedAt
              && (Date.now() - _phPayload.savedAt) < 7 * 24 * 60 * 60 * 1000;
            if(_phAgeOk && _phPayload.progressRaw){
              // 比對 handoff 摘要 vs 當前本地 progress
              const _curKey = 'lxps_' + user.uid + '_progress';
              const _curRaw = localStorage.getItem(_curKey);
              let _curScore = 0;
              if(_curRaw){
                try{
                  const _curObj = JSON.parse(_curRaw);
                  const _cs = _curObj && _curObj._dataSummary;
                  if(_cs){
                    // 分數 = unlocked*1000 + totalLv*10 + coins/100
                    _curScore = (_cs.unlockedCount || 0) * 1000
                              + (_cs.totalHeroLv || 0) * 10
                              + Math.min(1000, (_cs.coins || 0) / 100);
                  }
                }catch(_){}
              }
              const _phSummary = _phPayload.summary || {};
              const _phScore = (_phSummary.unlockedCount || 0) * 1000
                             + (_phSummary.totalHeroLv || 0) * 10
                             + Math.min(1000, (_phSummary.coins || 0) / 100);
              if(_phScore > _curScore + 100){
                // handoff 顯著豐富 → 用 handoff
                localStorage.setItem(_curKey, _phPayload.progressRaw);
                // 標記繞過下次健康度守門,讓豐富版寫回雲端
                window._adminForceRestore = true;
                setTimeout(function(){ window._adminForceRestore = false; }, 30 * 1000);
                console.log('🤝 [本地存檔 handoff] ✅ 已還原前次更豐富的本地存檔'
                  + '(unlocked=' + (_phSummary.unlockedCount || 0)
                  + ', totalLv=' + (_phSummary.totalHeroLv || 0)
                  + ', coins=' + (_phSummary.coins || 0) + ')'
                  + ',分數 ' + _phScore + ' > 本地 ' + _curScore);
                // UI 通知
                try{
                  const _t = document.createElement('div');
                  _t.style.cssText = 'position:fixed;top:60px;left:50%;transform:translateX(-50%);z-index:99999;' +
                    'background:rgba(30,80,40,0.96);border:3px solid #66dd99;color:#aaffcc;' +
                    'font-size:14px;padding:14px 22px;border-radius:12px;max-width:92vw;' +
                    'box-shadow:0 8px 32px rgba(0,0,0,0.6);text-align:center;line-height:1.6;';
                  _t.innerHTML =
                    '🤝 <b>已還原你之前的進度</b><br>' +
                    '<span style="font-size:13px;color:#ccffdd;">' +
                    '偵測到上次在共用裝置的本地存檔已搶救回來<br>' +
                    '<span style="font-size:11px;color:#aaaaaa;">解鎖英雄 ' + (_phSummary.unlockedCount || 0) + ' 隻 · ' +
                    '總等級 ' + (_phSummary.totalHeroLv || 0) + ' · ' +
                    '知識幣 ' + (_phSummary.coins || 0).toLocaleString() + '</span>' +
                    '</span>';
                  document.body.appendChild(_t);
                  setTimeout(()=>{ if(_t.parentNode){ _t.style.transition='opacity 1s'; _t.style.opacity='0'; setTimeout(()=>_t.remove(), 1100); } }, 8000);
                }catch(_){}
              } else {
                console.log('🤝 [本地存檔 handoff] 本地已有同樣或更豐富的資料,丟棄 handoff'
                  + '(handoff 分數 ' + _phScore + ' vs 本地 ' + _curScore + ')');
              }
            } else if(_phAgeOk){
              console.log('🤝 [本地存檔 handoff] handoff 只有摘要(雲端 handoff 路徑),無法還原 raw');
            } else {
              console.log('🤝 [本地存檔 handoff] handoff 已過期(>7 天),丟棄');
            }
            // 不論用沒用都清掉(避免重複觸發)
            localStorage.removeItem(_progressHandoffKey);
          }catch(_ePh){
            console.warn('🤝 [本地存檔 handoff] 解析失敗', _ePh);
            try{ localStorage.removeItem(_progressHandoffKey); }catch(_){}
          }
        }
      }catch(_eP){ console.warn('🤝 [本地存檔 handoff] 整體流程例外', _eP); }
      // ★ v3.4.21 — 老師需求 1:登入完成後立刻檢查未完成戰鬥快照,
      //   不必等玩家點冒險才彈,降低玩家錯過續戰提示的機率。
      try{
        if(typeof _advCheckCrashRecovery === 'function'){
          // 延 800ms 讓 UI 先穩定 + 雲端 load 寫入 localStorage 完成
          setTimeout(function(){
            try{ _advCheckCrashRecovery(); }
            catch(_eRec){ console.warn('[登入後續戰檢查] 例外', _eRec); }
          }, 800);
        }
      }catch(_eRcv){ console.warn('[登入後續戰檢查] 觸發失敗', _eRcv); }
      // ★ v3.18.0 — 老師需求 2:登入到關卡頁後,若從未主動選過年齡層風格,
      //   彈一次「風格比較選擇」畫面(簡單風/精緻風);延 1500ms:排在續戰提示(800ms)之後、
      //   會員資料首登(2200ms)/救援說明(2500ms)之前 → 風格選擇優先,且已內建防疊加判斷。
      try{
        setTimeout(function(){
          try{ if(typeof window._maybeShowStyleOnboarding === 'function') window._maybeShowStyleOnboarding(); }
          catch(_eSo){ console.warn('[風格選擇首登] 觸發例外', _eSo); }
        }, 1500);
      }catch(_eSoT){ console.warn('[風格選擇首登] 排程失敗', _eSoT); }
      // ★ v3.15.99 — 登入到關卡頁後,首次登入(未建會員資料)自動彈「會員資料建立」表單
      //   延 2200ms:排在 800ms 續戰提示之後、救援說明彈窗(2500ms)之前 → 首登表單優先;
      //   _maybeShowMemberProfileFirstLogin 內已對續戰/練習營/救援彈窗做防疊加守門。
      try{
        setTimeout(function(){
          try{ if(typeof window._maybeShowMemberProfileFirstLogin === 'function') window._maybeShowMemberProfileFirstLogin(); }
          catch(_eMp){ console.warn('[會員資料首登] 觸發例外', _eMp); }
        }, 2200);
      }catch(_eMpT){ console.warn('[會員資料首登] 排程失敗', _eMpT); }
      // ★ v3.15.91 — 登入到關卡頁後,自動彈一次「帳號救援申請」使用說明
      //   延 2500ms:讓關卡頁穩定 + 排在 800ms 續戰提示之後(若有續戰提示,彈窗會自動跳過不疊加)。
      //   per-uid localStorage 守門:學生勾「以後不要再顯示」後,該帳號不再自動彈。
      try{
        setTimeout(function(){
          try{ if(typeof window._showRescueGuidePopup === 'function') window._showRescueGuidePopup(false); }
          catch(_eRg){ console.warn('[帳號救援說明] 觸發例外', _eRg); }
        }, 2500);
      }catch(_eRgT){ console.warn('[帳號救援說明] 排程失敗', _eRgT); }
      // ★ v3.16.26 — 登入到關卡頁後,自動還原「被 v3.16.19 誤回收的練過英雄」(一次性·排在 orchestrator 之前)。
      //   延 1200ms:讓雲端 load 完成;內含雲端旗標 _auditLeveledRestoreV1 + 本機旗標防重跑。
      //   只把暫存等級>1(練過)的英雄補回 + 還原等級 + 補合法紀錄(不被出口過濾隱藏);不寫 _authoritativeRestoreAt(不重載·改記憶體/本機同步即時顯示)。
      try{
        setTimeout(function(){
          try{ if(typeof window._lxpsRestoreLeveledOnLogin === 'function') window._lxpsRestoreLeveledOnLogin(); }
          catch(_eLr){ console.warn('[練過英雄自動還原] 觸發例外', _eLr); }
        }, 1200);
      }catch(_eLrT){ console.warn('[練過英雄自動還原] 排程失敗', _eLrT); }
      // ★ v3.16.19→v3.16.26 — 登入到關卡頁後,執行「沒練過 Lv1 無解鎖證據 → 回收」(一次性)。
      //   ★ v3.16.26 判定已改:練過(lv>1)一律保留,本 orchestrator 現在只會回收「Lv1 且無任何解鎖證據」的純污染。
      //   延 1500ms:讓雲端 load 寫入 localStorage/記憶體完成(orchestrator 內有「擁有<8 自動重試」兜底)。
      try{
        setTimeout(function(){
          try{ if(typeof window._lxpsRecoverAuditErrorHeroes === 'function') window._lxpsRecoverAuditErrorHeroes(0); }
          catch(_eAr){ console.warn('[Lv1污染回收] 觸發例外', _eAr); }
        }, 1500);
      }catch(_eArT){ console.warn('[Lv1污染回收] 排程失敗', _eArT); }
      // ★ v3.16.14 — 登入到關卡頁後,彈一次「SSR 修復道歉公告」(per-uid 一次性)。
      //   原 v3.16.10 封存提醒已停用(封存機制本身是污染元兇,已全面停寫),改彈道歉公告。
      //   延 3200ms:排在會員首登(2200)、救援說明(2500)之後;_maybeShowApologyNotice 內有 per-uid 完成旗標 + 防疊加守門。
      try{
        setTimeout(function(){
          try{ if(typeof window._maybeShowApologyNotice === 'function') window._maybeShowApologyNotice(); }
          catch(_eSp){ console.warn('[道歉公告] 觸發例外', _eSp); }
        }, 3200);
      }catch(_eSpT){ console.warn('[道歉公告] 排程失敗', _eSpT); }
      // ★ v3.16.32 — 審查失誤「道歉 + 補償」(全體玩家·每帳號限領一次·UID 雲端旗標);等雲端載入完成再跑(發獎需正確餘額)
      try{
        setTimeout(function(){
          try{
            var _runAA = function(){ try{ if(typeof window._maybeShowAuditApologyCompensation === 'function') window._maybeShowAuditApologyCompensation(); }catch(_){} };
            if(window._waitForCloudReady){ window._waitForCloudReady(15000).then(_runAA).catch(_runAA); }
            else { _runAA(); }
          }catch(_eAA){ console.warn('[審查道歉補償] 排程失敗', _eAA); }
        }, 3600);
      }catch(_eAAT){}
      // ★ v3.16.99(2026-06-30·老師裁示)— 登入自動回收「GM 錯誤補發批次」(階段1):找出白名單日期(_GMCR_BAD_COMP_DATES,
      //   如 6/25)被 GM compensation 補進、且查無自己真實解鎖紀錄的英雄 → 彈「老師的更正通知」列清單 →
      //   玩家確認後回收(可逆 audit_error_recovered)+ 重載 → 重載後對本帳號做一次二次審查(階段2)。
      //   延 4000ms:排在 Lv1污染回收(1500)/道歉(3200)/審查補償(3600)之後、自我審查(4200)之前;
      //   內含「擁有<8/讀取失敗 自動重試」「總開關/白名單空/GM救援中/防重入」守門,只漏不誤刪。
      try{
        setTimeout(function(){
          try{
            var _runBCR = function(){ try{ if(typeof window._lxpsAutoReclaimBadCompHeroes === 'function') window._lxpsAutoReclaimBadCompHeroes(0); }catch(_){} };
            if(window._waitForCloudReady){ window._waitForCloudReady(15000).then(_runBCR).catch(_runBCR); }
            else { _runBCR(); }
          }catch(_eBCR){ console.warn('[自動回收錯誤補發] 排程失敗', _eBCR); }
        }, 4000);
      }catch(_eBCRT){ console.warn('[自動回收錯誤補發] 排程外失敗', _eBCRT); }
      // ★ v3.16.27 — 登入後自動彈「英雄來源自我審查」(乙):僅當帳號有「持有但查無硬證據」的英雄才彈,per-uid 一次性。
      //   延 4200ms:排在 還原(1200)/Lv1污染回收(1500)/會員首登(2200)/救援說明(2500)/道歉(3200)之後;
      //   內含「擁有<8 自動重試」+「有其他彈窗稍後重試」兜底,避免雲端未載完誤判或疊加。
      try{
        setTimeout(function(){
          try{ if(typeof window._maybeShowAccountAuditOnLogin === 'function') window._maybeShowAccountAuditOnLogin(0); }
          catch(_eAu){ console.warn('[自我審查登入提示] 觸發例外', _eAu); }
        }, 4200);
      }catch(_eAuT){ console.warn('[自我審查登入提示] 排程失敗', _eAuT); }
      // ★ v4.65.0 — 主線劇情首登自動導入(排最後 5000ms·防疊加;先 hydrate 雲端進度再判首登·僅 progress 空且測試期管理員)
      try{
        setTimeout(function(){
          try{
            var _runMs = function(){ try{ if(typeof window._msMaybeFirstLogin === 'function') window._msMaybeFirstLogin(); }catch(_){} };
            if(typeof window._msHydrateFromCloud === 'function'){ window._msHydrateFromCloud().then(_runMs).catch(_runMs); }
            else { _runMs(); }
          }catch(_eMs){ console.warn('[主線首登] 觸發例外', _eMs); }
        }, 5000);
      }catch(_eMsT){ console.warn('[主線首登] 排程失敗', _eMsT); }
      // ★ v3.16.32(2026-06-26)— 註解校正:此系統實際逾時 30 分鐘(非舊註解「5 分鐘」;IDLE_TIMEOUT_MS=30*60*1000)
      // ★ v3.14.7 — 傳入 user.uid 供跨 reload 過期檢查使用
      try { if(typeof window._idleAutoLogoutStart === 'function') window._idleAutoLogoutStart(user.uid); }
      catch(_e) { console.warn('[閒置自動登出] 啟動失敗', _e); }

      // ★ v3.16.32 — GM 登入後啟動「救援申請 / 錯誤回報」即時提醒徽章(含悅耳音效;非 GM 自動跳過)
      try { if(typeof window._lxpsRescueBadgeStart === 'function') window._lxpsRescueBadgeStart(); }
      catch(_eRb) { console.warn('[GM 即時提醒] 啟動失敗', _eRb); }

      // ★ v3.16.32 — GM 登入後自動解鎖全部至寶(台灣 + 8 龍王 + 未來新增;非 GM 自動跳過)
      //   延遲 + 等雲端載入完成再補,確保 _applySafeData 已把雲端至寶併入後才「只增不減」補缺,
      //   避免在載入前補(被守門/覆蓋)。此函式 idempotent,下次登入會自動補上次沒補到的。
      try {
        if(typeof window._isAdminUser === 'function' && window._isAdminUser()){
          setTimeout(function(){
            try{
              var _runGmTre = function(){ try{ if(typeof window._lxpsGmUnlockAllTreasures === 'function') window._lxpsGmUnlockAllTreasures(); }catch(_){} };
              if(window._waitForCloudReady){
                window._waitForCloudReady(15000).then(_runGmTre).catch(_runGmTre);
              } else { _runGmTre(); }
            }catch(_eGt){ console.warn('[GM 至寶全解鎖] 排程失敗', _eGt); }
          }, 4000);
        }
      } catch(_eGtT) {}

      try{
        const _autoEnter = sessionStorage.getItem('lxps_login_then_adventure');
        if(_autoEnter){
          sessionStorage.removeItem('lxps_login_then_adventure');
          _fbDiag('onAuth:偵測到 redirect 登入旗標,等雲端載完後自動進入冒險');
          (async function(){
            try{
              // 等雲端載完(最多 15 秒,超時就放行避免卡住)
              if(window._waitForCloudReady){
                const _ok = await window._waitForCloudReady(15000);
                _fbDiag('onAuth:雲端載入' + (_ok ? '完成' : '超時') + ',進入冒險');
              } else {
                // helper 未載入(理論上不會發生),退回 800ms 保險
                await new Promise(r => setTimeout(r, 800));
              }
              /* ★ v5.216.0 — redirect 模式(iOS 直連 OAuth)從荒島/小遊戲入口登入的，同樣導回原入口，不進冒險 */
              var _pe = null;
              try{ _pe = (typeof window._lxpsConsumePendingEntry === 'function') ? window._lxpsConsumePendingEntry() : null; }catch(_){ _pe = null; }
              if(_pe){
                var _went = false;
                try{ _went = !!window._lxpsGoPendingEntry(_pe); }catch(_){ _went = false; }
                if(_went) return;
              }
              if(typeof openAdventureOverlay === 'function'){
                try{ if(typeof window._lxpsShowProgressLoadedPopup === 'function') window._lxpsShowProgressLoadedPopup(); }catch(_){}
                openAdventureOverlay();
              }
            }catch(_e){ console.warn('[redirect 後自動進冒險] 失敗', _e); }
          })();
        }
      }catch(_){}
      // ★ v3.5.16 — 設計者英雄補發改成「等雲端就緒後立刻觸發 + 內建 retry」
      //   舊版固定 1.8 秒延遲,雲端載入慢時會在 _progressLoaded=false 狀態下執行 gameCloudSave
      //   → 雲端守門擋掉,設計者英雄只進到本機,雲端沒同步,下次清本機就遺失。
      //   新版 _grantStudentDesignerHero 已改 async,自己會 await _waitForCloudReady(15s)
      //   並做 5 次 retry(5/15/30/60/120 秒),不必固定延遲 1.8 秒。
      //   保留 setTimeout 800ms 是讓 onAuthStateChanged 內其他同步流程先跑完
      setTimeout(()=>{
        try{
          if(window._grantStudentDesignerHero){
            const _p = window._grantStudentDesignerHero(user.email);
            if(_p && typeof _p.catch === 'function'){
              _p.catch(e => console.warn('[設計者英雄補發] async 錯誤', e));
            }
          }
        }catch(e){ console.warn('[設計者英雄補發] 觸發失敗', e); }
      }, 800);
      // ★ v3.5.0 — 登入後啟動下載授權監聽
      setTimeout(()=>{
        try{ if(window._initDownloadAuth) window._initDownloadAuth(); }
        catch(e){ console.warn('[下載授權監聽] 啟動失敗', e); }
      }, 800);

      // ★ v3.11.21(2026-05-28) — 登入後「解鎖英雄補同步」(防英雄遺失最後保險)
      //   延遲 3000ms 確保 gameCloudLoad + _applySafeData union 都跑完,再比對本地∪雲端,
      //   把上次沒成功上傳的解鎖英雄補回雲端。詳見 _lxpsReconcileUnlockedHeroes。
      setTimeout(function(){
        try{
          if(typeof window._lxpsReconcileUnlockedHeroes === 'function'){
            const _pr = window._lxpsReconcileUnlockedHeroes();
            if(_pr && typeof _pr.catch === 'function') _pr.catch(e => console.warn('[英雄補同步] async 錯誤', e));
          }
        }catch(e){ console.warn('[英雄補同步] 觸發失敗', e); }
      }, 3000);

      // ★ v3.11.24 — 登入後「召喚卷保底補回」(防 SSR/SR 召喚卷遺失最後保險)
      //   情境:領卷當下 gameCloudSave 被守門擋下 → 重新整理 → 雲端覆蓋本地背包 → 卷消失。
      //   _kingConsumePendingTickets 會比對本地待領帳本,缺的卷補回背包再存一次。
      setTimeout(function(){
        try{
          if(typeof window._kingConsumePendingTickets === 'function'){
            const _pt = window._kingConsumePendingTickets();
            if(_pt && typeof _pt.catch === 'function') _pt.catch(e => console.warn('[召喚卷保底] async 錯誤', e));
          }
        }catch(e){ console.warn('[召喚卷保底] 觸發失敗', e); }
      }, 3500);

      // ★ v3.5.67(2026-05-23) — 補修上一輪沒做的:登入後自動處理小博士結算與領獎
      //   v3.5.63 寫了 trySettleLastWeek 跟 claimPendingAward 卻沒掛 onAuth → 自動結算永遠不會跑
      //   修法:登入完成 + 雲端載入完成後,依序執行:
      //     ① trySettleLastWeek():嘗試結算上週(第一個觸發鎖的玩家會發 pendingAward 給上榜玩家)
      //     ② claimPendingAward():檢查自己有沒有未領的 pending,有的話自動入帳 + 觸發排名獎章
      //     ③ 沒上榜也要呼叫 _checkMedalWqRank(0) 重置連霸 streak(否則跳一週後 streak 不會斷)
      //   delay 2500ms 確保雲端載入完成 + 訂閱 onSnapshot 已同步 _cachedGlobalStats
      setTimeout(async ()=>{
        try{
          if(!window._weeklyQuiz) return;
          // ① 嘗試結算上週(只有第一個觸發鎖的玩家會真的執行發獎)
          if(typeof window._weeklyQuiz.trySettleLastWeek === 'function'){
            try{
              await window._weeklyQuiz.trySettleLastWeek();
            }catch(_eSettle){ console.warn('[WeeklyQuiz 登入結算]', _eSettle); }
          }
          // ② 領取自己的 pendingAward(若上週有上榜)
          let _claimedThisWeek = false;
          if(typeof window._weeklyQuiz.claimPendingAward === 'function'){
            try{
              const _result = await window._weeklyQuiz.claimPendingAward();
              if(_result && _result.rank){
                _claimedThisWeek = true;
                // 顯示領獎通知
                try{
                  const _msg = '🎉 上週小博士第 ' + _result.rank + ' 名獎勵已自動入帳!\n'
                    + '💰 ' + (_result.coins || 0) + ' 知識幣 / '
                    + '💎 ' + (_result.crystals || 0) + ' 召喚水晶 / '
                    + '📖 ' + (_result.books || 0) + ' 本豪華典藏經驗書';
                  if(typeof _showInGameToast === 'function'){
                    _showInGameToast(_msg, '#ffe066', 5000);
                  } else {
                    console.log('[小博士領獎]', _msg);
                  }
                }catch(_eMsg){ console.warn('[小博士領獎 toast]', _eMsg); }
              }
            }catch(_eClaim){ console.warn('[WeeklyQuiz 登入領獎]', _eClaim); }
          }
          // ③ 沒上榜也要重置 streak(避免連霸 BUG)
          //   每週只跑一次 — 用本地 localStorage 旗標守門(避免一天多次登入重複跑)
          try{
            const _lastWk = window._weeklyQuiz.getLastWeekKey
              ? window._weeklyQuiz.getLastWeekKey() : '';
            if(_lastWk){
              const _streakKey = 'lxps_wq_streak_checked_' + _lastWk;
              if(!_claimedThisWeek && localStorage.getItem(_streakKey) !== '1'){
                // 沒領到獎勵 = 沒上榜 → 重置 streak
                if(typeof _checkMedalWqRank === 'function'){
                  _checkMedalWqRank(0);
                  console.log('[獎章 v3.5.67] 上週沒上榜,重置連霸 streak');
                }
                localStorage.setItem(_streakKey, '1');
              }
            }
          }catch(_eStreak){ console.warn('[小博士 streak 重置]', _eStreak); }
        }catch(_eMain){ console.warn('[WeeklyQuiz 登入觸發] 整體例外', _eMain); }
      }, 2500);

      // ★ v3.13.5(2026-05-31)— 登入後自動處理世界 BOSS 排名獎勵結算與領獎
      //   流程:
      //     ① _wbSettle.trySettle():若時間已到下個 8:00 → 嘗試結算(第一個搶到鎖的人會發 pending)
      //     ② _wbSettle.getMyPending():檢查自己有沒有未領 pending
      //     ③ 有 → 彈獎勵展示彈窗,玩家按確認後跑 claimPending 入帳
      //   delay 3500ms 確保雲端載入完成 + cachedGlobalStats 已訂閱同步
      setTimeout(async () => {
        try{
          if(!window._wbSettle) return;
          if(!window._gUserId) return;
          // ① 嘗試結算(只有時間到 + 第一個搶鎖的會真執行;其他人是 no-op)
          //   ★ v3.14.13(甲)— 改巡「所有曾倒下的 BOSS」(wbBossDownTimes keys),
          //     不再寫死維蘇威,翠綠森龍王等後續龍王倒下也會自動結算
          try{
            const _downTimes = (window._cachedGlobalStats && window._cachedGlobalStats.wbBossDownTimes) || {};
            const _bossIds = Object.keys(_downTimes).filter(Boolean);
            if(_bossIds.length === 0) _bossIds.push((typeof window._wbGetCurrentBossId === 'function') ? window._wbGetCurrentBossId() : 'vesuvius_fire_dragon');  // ★ v3.14.20
            for(const _bid of _bossIds){
              try{
                const _settleResult = await window._wbSettle.trySettle(_bid);
                if(_settleResult && _settleResult.settled){
                  console.log('[WbSettle 登入] ✅ 我是觸發者(' + _bid + '),共結算 ' + (_settleResult.totalCount||0) + ' 位玩家');
                }
              }catch(_eS1){ console.warn('[WbSettle 登入結算] ' + _bid, _eS1); }
            }
          }catch(_eS){ console.warn('[WbSettle 登入結算]', _eS); }
          // ② 檢查自己有沒有 pending
          try{
            const _pending = await window._wbSettle.getMyPending();
            if(_pending && !_pending.claimedAt){
              // 延遲一拍彈窗,讓其他登入通知先跑完
              setTimeout(function(){
                try{
                  if(typeof window._wbShowPendingAwardModal === 'function'){
                    window._wbShowPendingAwardModal(_pending);
                  } else {
                    console.warn('[WbSettle 登入] 有 pending 但 _wbShowPendingAwardModal 不存在');
                  }
                }catch(_eM){ console.warn('[WbSettle pending modal]', _eM); }
              }, 1800);
            } else {
              // ★ v3.14.17 — 沒有未領 pending(含已領過)→ 跑「至寶遺失自動補償」檢查
              //   (受害者=已領獎但必得至寶被舊 safe 槽覆蓋謀殺;判定與防重複見函式內註解)
              setTimeout(function(){
                try{
                  if(typeof window._wbCheckAndCompensateLostTreasure === 'function'){
                    window._wbCheckAndCompensateLostTreasure();
                  }
                }catch(_eCp){ console.warn('[WB至寶補償觸發]', _eCp); }
              }, 1800);
            }
          }catch(_eC){ console.warn('[WbSettle 登入領獎檢查]', _eC); }
        }catch(_eMain){ console.warn('[WbSettle 登入觸發] 整體例外', _eMain); }
      }, 3500);

      // ★ v3.12.10(2026-05-30) — 登入後檢查世界 BOSS 異常記錄補償通知
      //   情境:GM 從後台刪除排行榜異常紀錄時,會對隊伍中每位玩家寫 wbAbnormalRemoval,
      //         且自動 reset wbDailyCount(進場次數)。玩家下次登入要看到通知,
      //         告知「為什麼上次的紀錄不見了 + 已補償進場次數」。
      //
      //   delay 3000ms 確保雲端載入完成、_progressLoaded 已就緒,
      //   且不要跟小博士領獎通知同時跳(那條 delay 2500ms)。
      setTimeout(async () => {
        try{
          if(!window._gUserId) return;
          if(!window._fbDb) return;
          // 讀 players/{me}.wbAbnormalRemoval(直接走 firestore module API)
          let _ar = null;
          try{
            const _docRef = doc(_fbDb, 'players', window._gUserId);
            const _docSnap = await getDoc(_docRef);
            if(_docSnap.exists()){
              const _data = _docSnap.data() || {};
              _ar = _data.wbAbnormalRemoval || null;
            }
          }catch(_eRd){ console.warn('[wb 補償通知] 讀玩家文件失敗', _eRd); return; }
          if(!_ar) return;
          if(_ar.acknowledgedAt) return;  // 已讀過,略過

          // 組通知訊息
          const _reason = _ar.reason || '(GM 未填寫原因)';
          const _bossName = _ar.boss || '火山炎龍王';
          const _removedAt = _ar.removedAt ? new Date(_ar.removedAt) : null;
          const _timeStr = _removedAt
            ? (_removedAt.getMonth()+1) + '/' + _removedAt.getDate() + ' ' +
              String(_removedAt.getHours()).padStart(2,'0') + ':' + String(_removedAt.getMinutes()).padStart(2,'0')
            : '(未知)';

          // 彈出補償通知 modal
          try{
            if(typeof window._showWbAbnormalRemovalDialog === 'function'){
              window._showWbAbnormalRemovalDialog({
                bossName: _bossName,
                reason: _reason,
                timeStr: _timeStr,
              });
            } else {
              // fallback:沒有 helper 就用 alert(理論上不會走到)
              console.warn('[wb 補償通知] _showWbAbnormalRemovalDialog 未就緒,fallback 用 alert');
              alert('🐉 ' + _bossName + ' 排行榜紀錄補償通知\n\n' +
                    '原因:' + _reason + '\n' +
                    '時間:' + _timeStr + '\n\n' +
                    '✅ 今日進場次數已自動重置,可重新挑戰!');
              try{
                if(typeof window._wbConsumeAbnormalRemoval === 'function'){
                  await window._wbConsumeAbnormalRemoval();
                }
              }catch(_){}
            }
          }catch(_eDlg){ console.warn('[wb 補償通知] 彈窗失敗', _eDlg); }
        }catch(_eMain){ console.warn('[wb 補償通知] 整體例外', _eMain); }
      }, 3000);

      // ★ v3.13.61(2026-06-05)— 登入後自動處理鬥技場每週排名結算與領獎
      //   ① trySettleLastWeek():週一 08:00 後 + GM 開關開啟,第一個觸發者結算上週(寫結算包)
      //   ② claimMyAward():讀結算包找自己的名次獎勵 → 領到自己文件(零跨玩家寫)
      //   delay 4000ms:避開小博士(2500)、世界 BOSS(3000/3500),確保雲端載入 + onSnapshot 同步
      setTimeout(async () => {
        try{
          if(!window._arenaRank || !window._gUserId) return;
          // ★ v3.13.92 — 先把雲端本週鬥技之證「取 max」回補本機(修「更新後消失/本機被清」+ 讓 GM 補償可見)
          try{ if(typeof window._arenaRank.reconcileLocalFromCloud === 'function') await window._arenaRank.reconcileLocalFromCloud(); }catch(_eRec){ console.warn('[ArenaRank 登入回補]', _eRec); }
          try{ await window._arenaRank.trySettleLastWeek(); }catch(_eS){ console.warn('[ArenaRank 登入結算]', _eS); }
          try{
            const _r = await window._arenaRank.claimMyAward();
            if(_r && _r.rank){
              const _msg = '🏆 上週鬥技場排名第 ' + _r.rank + ' 名獎勵已自動入帳!\n'
                + '🔮 ' + (_r.crystals || 0) + ' 召喚水晶 / 🎖 ' + (_r.zheng || 0) + ' 鬥技之證 / 💰 ' + (_r.coins || 0) + ' 知識幣';
              if(typeof _showInGameToast === 'function') _showInGameToast(_msg, '#ffd54f', 5500);
              else console.log('[鬥技場排名領獎]', _msg);
            }
          }catch(_eC){ console.warn('[ArenaRank 登入領獎]', _eC); }
        }catch(_eMain){ console.warn('[ArenaRank 登入觸發] 例外', _eMain); }
      }, 4000);

      // ★★★ v3.13.49(2026-06-05)— 登入後檢查「汙染英雄刪除預告」(老師需求:改成預告制,不直接刪)★★★
      //   情境:GM 在後台對「來源不明(疑似跨帳號汙染)」的英雄發出「刪除預告」(只寫一個欄位, 不動英雄)。
      //         玩家下次登入要先看到:緣由 + 汙染證據 + 即將被刪英雄清單 + 各英雄刪除後的補償,
      //         並可勾選「最多保留 1 隻」(保留的那隻不補償)。玩家確認後 → 回寫保留選擇 + 已確認旗標
      //         (極小自寫, 不動英雄/幣);實際刪除由 GM 套用(admin, 刪前存快照可復原)。
      //   delay 4500ms:避開小博士領獎(2500)、世界 BOSS 補償(3000),確保雲端載入完成。
      setTimeout(async () => {
        try{
          if(!window._gUserId || !window._fbDb) return;
          let _pp = null;
          try{
            const _ref = doc(_fbDb, 'players', window._gUserId);
            const _snap = await getDoc(_ref);
            if(_snap.exists()){ _pp = (_snap.data() || {})._pendingPollutionPurge || null; }
          }catch(_eRd){ console.warn('[汙染刪除預告] 讀玩家文件失敗', _eRd); return; }
          if(!_pp) return;
          if(_pp.status !== 'pending') return;   // 只在「待玩家確認」時彈
          const _hasH = Array.isArray(_pp.heroes) && _pp.heroes.length;
          const _hasT = Array.isArray(_pp.treasures) && _pp.treasures.length;
          if(!_hasH && !_hasT){
            console.warn('[汙染刪除預告] 預告無英雄/至寶清單,略過'); return;
          }
          console.log('[汙染刪除預告 v3.13.49] 偵測到待確認預告(英雄 ' + (_hasH||0) + ' / 至寶 ' + (_hasT||0) + '),彈出玩家確認視窗');
          if(typeof window._showPollutionPurgeNotice === 'function'){
            window._showPollutionPurgeNotice(_pp);
          }
        }catch(_eMain){ console.warn('[汙染刪除預告] 整體例外', _eMain); }
      }, 4500);

      // ★ v3.15.49 — 登入後自動領取「全體獎勵」(GM 一鍵發給所有人,每人保證只領一次)
      //   delay 5500ms:避開小博士(2500)、世界 BOSS(3000)、鬥技場排名(4000)、汙染預告(4500)。
      //   _fbClaimGlobalRewards 內部:讀 enabled 的 globalRewards → 對未領者 transaction 認領(免疫存檔復活)
      //   → 標記成功才 _fbCompensatePlayer 發獎(寫三槽);回傳本次新領清單供彈窗通知。
      setTimeout(async () => {
        try{
          if(!window._gUserId || !window._fbDb) return;
          if(typeof window._fbClaimGlobalRewards !== 'function') return;
          const _res = await window._fbClaimGlobalRewards();
          const _list = (_res && Array.isArray(_res.claimed)) ? _res.claimed : [];
          // ★ v3.17.8 — 比照 v3.16.97:立刻把每筆新領獎勵的 backpack 內容套進記憶體背包,
          //   免疫玩家看到通知後點「稍後再看」繼續遊戲時,後續 autosave 用舊記憶體背包把剛領的獎勵沖掉。
          try{
            _list.forEach(function(_rw){
              if(_rw && _rw.reward && _rw.reward.backpack && typeof _rw.reward.backpack === 'object' && typeof backpackAdd === 'function'){
                Object.keys(_rw.reward.backpack).forEach(function(_bk){
                  var _bq = Math.max(0, parseInt(_rw.reward.backpack[_bk], 10) || 0);
                  if(_bq > 0) backpackAdd(_bk, _bq);
                });
              }
            });
          }catch(_eApplyMem){ console.warn('[全體獎勵] 套記憶體背包失敗(不影響·下次登入對帳會補)', _eApplyMem); }
          if(_list.length && typeof window._showGlobalRewardNotice === 'function'){
            window._showGlobalRewardNotice(_list);
          }
        }catch(_eMain){ console.warn('[全體獎勵 登入領取] 例外', _eMain); }
      }, 5500);

      // ★ v3.16.65 — 登入後刷新「🎁 GM獎勵」鈕上方「有可領取獎勵」標籤(老師指派的待領課堂獎勵)
      setTimeout(function(){
        try{ if(typeof window._refreshGmClassRewardBadge === 'function') window._refreshGmClassRewardBadge(); }catch(_eGmcr){}
      }, 6000);

      // ★ v5.147.0(老師回報)— 登入 8 秒後自動補送「上次沒存成功的會員資料」(本機暫存,綁 uid)
      setTimeout(function(){ try{ if(typeof window._memberProfileRetryPending === 'function') window._memberProfileRetryPending('login'); }catch(_eMp){} }, 8000);

      // ★ v5.146.0(老師需求 1)— 登入後自動處理「小遊戲每週排行榜」結算與領獎(仿鬥技場 v3.13.61)
      //   ① trySettleLastWeek():週一 08:00 後第一位登入者結算上週(transaction 鎖,寫結算包到 stats/minigameWeekly)
      //   ② claimMyAward():讀結算包找自己 → 認領文件 create-only + 自己主檔加幣/背包(零跨玩家寫、每人每週只領一次)
      //   delay 6500ms:排在小博士(2500)、世界 BOSS(3000)、鬥技場(4000)、汙染預告(4500)、全體獎勵(5500)、GM 獎勵標籤(6000)之後。
      setTimeout(async () => {
        try{
          if(!window._minigameRank || !window._gUserId) return;
          try{ await window._minigameRank.trySettleLastWeek(); }catch(_eS){ console.warn('[MinigameRank 登入結算]', _eS); }
          try{
            const _r = await window._minigameRank.claimMyAward();
            if(_r && _r.rank){
              const _msg = '🎮 上週小遊戲排行榜「' + (_r.gameName || '') + '」第 ' + _r.rank + ' 名(' + (_r.score || 0) + ' 分)獎勵已自動入帳!\n'
                + '💰 ' + (_r.coins || 0) + ' 知識幣 / 🔮 ' + (_r.crystals || 0) + ' 召喚水晶 / 📖 ' + (_r.books || 0) + ' 豪華經驗書';
              if(typeof _showInGameToast === 'function') _showInGameToast(_msg, '#ffd54f', 6500);
              else console.log('[小遊戲排行領獎]', _msg);
            }
          }catch(_eC){ console.warn('[MinigameRank 登入領獎]', _eC); }
        }catch(_eMain){ console.warn('[MinigameRank 登入觸發] 例外', _eMain); }
      }, 6500);

      // ★ v3.5.16 + v3.5.17 — 登入完成後重新觸發 bootPWA

      // ★ v3.5.16 + v3.5.17 — 登入完成後重新觸發 bootPWA
      //   原因:_initPWA 是 IIFE 在頁面載入時立即執行,當時 window._fbUser 尚未就緒,
      //   bootPWA 內守門(管理員 / 有效下載授權)永遠不通過 → SW 永遠不註冊
      //   修法:
      //     (a) 1.5 秒後:確認管理員身分 → 直接 boot(管理員不需要授權流程)
      //     (b) 3.5 秒後:確認一般學生的下載權限已從雲端載入 → 若有有效授權則 boot
      //   兩段時序分開:_initDownloadAuth 在 800ms 啟動 onSnapshot,通常 1-2 秒能收到雲端權限,
      //   3.5 秒留充裕緩衝(避免在 onSnapshot 還沒回來就誤判沒授權)
      setTimeout(()=>{
        try{
          const _isAdmin = (typeof window._isAdminUser === 'function' && window._isAdminUser());
          if(_isAdmin && !window._lxpsAdminPwaActive && window._lxpsPWA && typeof window._lxpsPWA.boot === 'function'){
            console.log('🛠️ [v3.5.16] 管理員登入確認,重新啟動 PWA bootPWA() 以走完整預載流程');
            window._lxpsPWA.boot();
          }
        }catch(e){ console.warn('[管理員 PWA 重啟] 失敗', e); }
      }, 1500);
      // ★ v3.5.17 — 學生端:等下載權限載入後,若有有效授權則啟動 PWA
      //   學生用平板:登入 → 等 _initDownloadAuth 啟動 onSnapshot → 雲端權限載入 →
      //   若是 approved 狀態 → 自動 boot 開始預載(不需要學生手動點任何按鈕)
      //   另一條路徑:首登入時還沒授權 → 學生申請 → 老師核准 → onSnapshot 回呼內主動 boot
      //   (見 _initDownloadAuth 的 onSnapshot callback 內 v3.5.17 區段)
      setTimeout(()=>{
        try{
          const _isAdmin = (typeof window._isAdminUser === 'function' && window._isAdminUser());
          if(_isAdmin) return; // 管理員已在 1.5 秒那段處理過
          const _perm = window._downloadAuth && window._downloadAuth.currentPermission;
          const _hasValid = _perm && _perm.status === 'approved'
            && _perm.expiresAt && _perm.expiresAt > Date.now();
          // ★ v5.143.0 — 免申請合成核准（_auto）不自動 boot：下載由玩家按鈕手動決定
          if(_hasValid && _perm._auto === true && window._LXPS_DL_MANUAL_ONLY === true) return;
          if(_hasValid && !window._lxpsStudentPwaActive
             && window._lxpsPWA && typeof window._lxpsPWA.boot === 'function'){
            const _expireIn = Math.round((_perm.expiresAt - Date.now()) / 60000);
            console.log('✅ [v3.5.17] 學生有效下載授權確認(剩餘 ' + _expireIn + ' 分鐘),'
              + '重新啟動 PWA bootPWA() 開始預載');
            window._lxpsPWA.boot();
          }
        }catch(e){ console.warn('[學生 PWA 啟動] 失敗', e); }
      }, 3500);
      // ★ 登入後延遲補發已達成但未記錄的成就
      // ★ v3.5.35 — 修補時序競態:1500ms 後雲端 merge 可能還沒完成,
      //   _checkMedalsOnLogin 內部一整批 _unlockMedal 會以「沒解鎖過」狀態錯誤重複發獎。
      //   改成:等 _cloudLoadDone === true 才跑(最多等 20 秒)
      setTimeout(()=>{
        let _waited = 0;
        const _MAX_WAIT = 20000;
        const _POLL = 250;
        function _runCheckMedalsOnLoginWhenReady(){
          if(window._cloudLoadDone === true){
            console.log('[獎章補檢查 v3.5.35] 雲端載入完成,執行 _checkMedalsOnLogin(等待 ' + _waited + 'ms)');
            try{ _checkMedalsOnLogin(); }catch(_){}
            return;
          }
          if(_waited >= _MAX_WAIT){
            console.warn('[獎章補檢查 v3.5.35] _checkMedalsOnLogin 等雲端超時,強制執行(_unlockMedal 第二層保險擋重複發獎)');
            try{ _checkMedalsOnLogin(); }catch(_){}
            return;
          }
          _waited += _POLL;
          setTimeout(_runCheckMedalsOnLoginWhenReady, _POLL);
        }
        _runCheckMedalsOnLoginWhenReady();
      }, 1500);
      setTimeout(()=>{
        try{ if(window._grantRetroactiveMedalRewards) window._grantRetroactiveMedalRewards(); }
        catch(e){ console.warn('[獎章補發] 觸發失敗', e); }
      }, 3000);
      // ★ v1.1.0 — 登入後檢查每日代表英雄經驗獎勵(同帳號同日只能領一次)
      // ★ v3.3.7 — 函式改 async(讀雲端),加 .catch 避免 unhandled rejection
      setTimeout(()=>{
        try{
          if(window._checkDailyRepHeroBonus){
            const _p = window._checkDailyRepHeroBonus();
            if(_p && typeof _p.catch === 'function'){
              _p.catch(e => console.warn('[每日代表英雄獎勵] async 錯誤', e));
            }
          }
        }catch(e){
          console.warn('[每日代表英雄獎勵]', e);
        }
      }, 2500);
      // ★ v1.0.20260424.1150 — 登入後確保有邀請碼(沒有則生成)
      setTimeout(async ()=>{
        try{
          if(window._fbEnsureInviteCode){
            const code = await window._fbEnsureInviteCode(user.uid);
            if(code) window._myInviteCode = code;
          }
        }catch(e){ console.warn('[InviteCode] 生成異常', e); }
      }, 800);
      // ★ v3.4.13 — 登入後 5 秒檢查「管理員回信未讀」
      //   有未讀 → 彈出短促置頂通知 + 關卡首頁 BUG 回報按鈕發亮
      //   並啟動每 60 秒輕量輪詢,讓老師在玩家還在線時回覆也能即時亮燈
      setTimeout(()=>{
        try{
          if(typeof window._checkUnreadBugReplies === 'function'){
            window._checkUnreadBugReplies();
          }
          if(typeof window._startUnreadBugReplyPolling === 'function'){
            window._startUnreadBugReplyPolling();
          }
        }catch(e){ console.warn('[未讀回信] 登入後啟動失敗', e); }
      }, 5000);
      // ★ v3.13.97 — 改用 onSnapshot 監聽 session(取代原 5 秒輪詢 _checkHeartbeat；偵測被踢更即時、近乎零讀寫)
      window._installSessionWatcher();

      window._trySaveSafeIfHealthier = async function(reason){
        try{
          if(!window._gUserId) return { written: false, reason: 'no_uid' };
          if(!window._progressLoaded) return { written: false, reason: 'progress_not_loaded' };
          if(window._kickedFromSession) return { written: false, reason: 'kicked' };
          if(typeof _buildSafeData !== 'function') return { written: false, reason: 'no_builder' };
          if(!window._fbWriteSafeIfHealthier) return { written: false, reason: 'no_api' };
          const _candidate = _buildSafeData();
          if(!_candidate || !_candidate._dataSummary){
            return { written: false, reason: 'candidate_no_summary' };
          }
          const _result = await window._fbWriteSafeIfHealthier(window._gUserId, _candidate);
          if(_result.written){
            console.info('🛡️ [safe 槽] 已備份 ('+reason+'):',
              'unlocked=' + _candidate._dataSummary.unlockedCount,
              'maxLv=' + _candidate._dataSummary.maxHeroLv);
          } else if(_result.reason === 'cloud_safe_richer'){
            console.warn('🛡️ [safe 槽] 跳過 ('+reason+') — 雲端 safe 更豐富(可能別裝置剛寫過)',
              { local: _result.newSummary, cloud: _result.prevSummary });
          } else {
            console.log('[safe 槽] 未寫入 ('+reason+'):', _result.reason);
          }
          return _result;
        }catch(e){
          console.error('[safe 槽] 觸發失敗 ('+reason+')', e);
          return { written: false, reason: 'exception', error: e && e.message };
        }
      };
      // 啟動 5 分鐘定時備份
      if(window._safeSlotInterval) clearInterval(window._safeSlotInterval);
      window._safeSlotInterval = setInterval(() => {
        window._trySaveSafeIfHealthier('5min_tick');
      }, 5 * 60 * 1000);
      // 首次延遲 90 秒做一次(讓登入後的補發流程跑完再開始備份),
      // 避免新玩家剛載入就被「補發未完成版本」當 safe 寫上去
      setTimeout(() => { window._trySaveSafeIfHealthier('initial'); }, 90 * 1000);
      // 頁面關閉 / 重整 / 跳離時觸發一次
      //   注意:beforeunload 與 pagehide 都用,因為 mobile Safari 在 background 不一定觸發 beforeunload
      //   但這兩個 handler 都不能 await(瀏覽器不會等),所以只能 fire-and-forget
      if(window._safeSlotUnloadHandler){
        window.removeEventListener('beforeunload', window._safeSlotUnloadHandler);
        window.removeEventListener('pagehide',     window._safeSlotUnloadHandler);
      }
      window._safeSlotUnloadHandler = () => {
        try{
          // 直接呼叫,不 await(瀏覽器不會等 Promise);失敗也無妨,反正每 5 分鐘還會跑
          window._trySaveSafeIfHealthier('unload');
        }catch(_){}
      };
      window.addEventListener('beforeunload', window._safeSlotUnloadHandler);
      window.addEventListener('pagehide',     window._safeSlotUnloadHandler);

      // ★ 啟動即時同步：監聽暱稱/代表英雄/好友名單變更
      if(window._fbWatchProfile){
        if(window._profileUnsubscribe) window._profileUnsubscribe();
        window._profileUnsubscribe = window._fbWatchProfile(user.uid, (data) => {
          // 暱稱同步
          if(data.displayName && data.displayName !== window._playerNickname){
            window._playerNickname = data.displayName;
            localStorage.setItem('lxps_nickname_' + user.uid, data.displayName);
            // ★ v3.11.16 — 套用玩家可見統一格式
            const _shown = (typeof window._formatPlayerDisplayName === 'function')
              ? window._formatPlayerDisplayName(data.displayName, user.email) : data.displayName;
            const nm = document.getElementById('google-name');
            if(nm) nm.textContent = _shown;
            const advNm = document.getElementById('adv-user-name');
            if(advNm) advNm.textContent = _shown;
          }
          // 代表英雄同步
          // ★ v3.16.43 — 改用 helper 同步 local + window(原本只設 window._myRepHero,且沒有 null 分支 →
          //   切到「沒有設代表英雄」的帳號時不會清掉前帳號殘留 → 跨帳號污染 + 畫面顯示別帳號代表英雄)。
          //   data.representativeHero 不存在時傳 null:清掉本機殘留並把欄位回復成「設定代表英雄」。
          if(typeof window._applyRepHeroFromCloud === 'function'){
            try{ window._applyRepHeroFromCloud(data.representativeHero || null); }catch(_){}
          } else {
            window._myRepHero = data.representativeHero || null;
            try{ _loadRepHeroBar(); }catch(_){}
          }
          // 好友名單同步
          if(data.friends && Array.isArray(data.friends)){
            window._friendList = data.friends;
            const panel = document.getElementById('_friend-panel');
            if(panel){
              try{
                if(typeof _refreshFriendListInner === 'function'){
                  _refreshFriendListInner();
                } else {
                  _renderFriendPanel();
                }
              }catch(_){}
            }
          }
        });
      }
    } else {
      // ★ v3.3.7 — 防誤觸發保護:頁面剛載入時的「user=null race condition」
      //   症狀:玩家點登入 → redirect → 回首頁 → onAuthStateChanged 先以 user=null 觸發
      //         → 進此 else 分支 → _clearAccountLocalData 清掉本機資料
      //         → 隨後 user=非null 又觸發,但已經晚了(資料已清)
      //         → 玩家看到「未登入狀態」,以為登入失敗,重新點 → 無限循環
      //   保護:如果頁面載入未滿 5 秒,且 _fbAuth.currentUser 仍有值 → 略過清理,
      //         等下一次 onAuthStateChanged 用 currentUser 補上來。
      //   注意:這只是「延後清理」,真實登出(_fbSignOut)時 currentUser=null,
      //         會正常走到清理流程。
      try{
        const _pageLoadedAt = window._pageLoadedAt || (window._pageLoadedAt = Date.now());
        const _sinceLoad = Date.now() - _pageLoadedAt;
        if(_sinceLoad < 5000 && _fbAuth && _fbAuth.currentUser){
          _fbDiag('onAuth:user=null 但頁面剛載入(' + _sinceLoad + 'ms) + currentUser='
            + _fbAuth.currentUser.email + ' → 略過清理,等下次觸發', 'warn');
          return;
        }
      }catch(_){}
      // ★ v1.1.0 — 停止 Presence(避免線上人數誤計)
      try {
        if(typeof window._fbStopPresence === 'function' && window._gUserId){
          await window._fbStopPresence(window._gUserId);
        }
        // ★ v3.35.0 — 一併停止好友登入通知輪詢
        try{ if(typeof window._fbStopFriendNotifyPoll === 'function') window._fbStopFriendNotifyPoll(); }catch(_){}
      } catch(_){}
      // ★ FIX 20260517(m) — 登出時停掉世界 BOSS 邀請通知訂閱(避免切帳號後仍收到舊帳號邀請)
      try {
        if(typeof window._wbStopInviteNotifSub === 'function'){
          window._wbStopInviteNotifSub();
        }
      } catch(_){}
      window._fbUser = null;
      // ★ v5.148.0 — 清直連回程去重旗標：登出後同一帳號再登入（popup 路徑）不可被誤判為重複觸發
      try{ window._lxpsOAuthDirectResumedUid = null; window._lxpsOnAuthDirectSeenUid = null; }catch(_){}
      // ★ 登出時清除 session + 取消監聽 + 停止心跳
      // ★ v3.13.97 — 改用 onSnapshot 後，登出統一走 teardown(取消監聽 + 清計時器)
      window._teardownSessionWatcher();
      if(window._gUserId && window._mySessionId && window._fbClearSession)
        window._fbClearSession(window._gUserId, window._mySessionId);
      window._mySessionId = null;
      if(window._profileUnsubscribe){ window._profileUnsubscribe(); window._profileUnsubscribe = null; }
      // ★ v3.5.0 — 登出時取消下載授權監聽 + 隱藏按鈕
      try {
        if(window._downloadAuth){
          if(window._downloadAuth.unsubscribe){
            try { window._downloadAuth.unsubscribe(); } catch(_){}
            window._downloadAuth.unsubscribe = null;
          }
          if(window._downloadAuth.expireWarnTimer){
            clearTimeout(window._downloadAuth.expireWarnTimer);
            window._downloadAuth.expireWarnTimer = null;
          }
          if(window._downloadAuth.expireRemoveTimer){
            clearTimeout(window._downloadAuth.expireRemoveTimer);
            window._downloadAuth.expireRemoveTimer = null;
          }
          window._downloadAuth.currentPermission = null;
        }
        // ★ v5.171.0（2026-09-10・老師回報「兩顆下載按鈕出現幾秒就消失了」）—
        //   根因：Firebase onAuthStateChanged 在頁面第一次載入時，即使是從未登入過的訪客，
        //   也必定會先以 user=null 觸發一次這個 else 分支（這是 Firebase SDK 的標準行為，
        //   不是 bug）。舊碼在這裡無條件把兩顆下載按鈕設回 display:none，蓋掉了頁面一開
        //   始 DOMContentLoaded 時 _initDownloadAuth() 已經為訪客免登入模式（_LXPS_DL_PERM_FREE）
        //   打開的顯示——訪客因此看到按鈕先閃現，幾秒後又被這裡的 onAuth(null) 回呼強制收掉，
        //   跟排版無關，是兩段程式碼各自為政、沒有共用同一套「訪客要不要顯示」的判斷。
        //   修法：訪客免登入模式開著且此刻確實沒有登入帳號時，改用與 _initDownloadAuth()
        //   guest 分支相同的邏輯（顯示按鈕 + 補寫 currentPermission，避免重蹈 v5.169.0 的
        //   下載逾時覆轍）；真正登出（_gUserId 此時仍是登出前的舊值，尚未於下方清空）或免登
        //   入模式關閉時，維持舊行為隱藏。
        const _dlw = document.getElementById('download-install-wrap');
        const _drb = document.getElementById('download-request-btn');
        if(window._LXPS_DL_PERM_FREE === true && !window._gUserId){
          if(typeof window._lxpsAutoDlPerm === 'function'){
            window._downloadAuth.currentPermission = window._lxpsAutoDlPerm(null);
          }
          if(_dlw) _dlw.style.display = '';
          if(_drb) _drb.style.display = 'none';
        } else {
          if(_dlw) _dlw.style.display = 'none';
          if(_drb) _drb.style.display = 'none';
        }
      } catch(_){}
      // ★ v1.0.20260514.6530 — 登出時清掉本機殘留 + 停閒置自動登出計時器
      try {
        if(typeof window._clearAccountLocalData === 'function'){
          window._clearAccountLocalData('sign_out');
        }
      } catch(_){}
      try { if(typeof window._idleAutoLogoutStop === 'function') window._idleAutoLogoutStop(); } catch(_){}
      // ★ v3.16.32 — 登出停止「救援/錯誤回報」即時提醒徽章監聽(避免殘留 onSnapshot 配額)
      try { if(typeof window._lxpsRescueBadgeStop === 'function') window._lxpsRescueBadgeStop(); } catch(_){}
      // 清掉「上次活躍 uid」(避免下次匿名訪客誤觸發換帳號清理 — 不過反正都清過了不影響)
      try { localStorage.removeItem('lxps_last_active_uid'); } catch(_){}
      window._gUserId = null;
      const btnWrap = document.getElementById('google-login-btn-wrap');
      const userInfo = document.getElementById('google-user-info');
      // ★ v5.195.0(老師「左上角恢復帳號登入的按鈕」)— 登出後還原顯示(舊 v5.136.0 裁定「一律隱藏」作廢)。
      //   ⚠ 這顆是休息時段唯一不經入口的登入路徑,老師要靠它登入管理員帳號解除休息閘門。
      if(btnWrap) btnWrap.style.display = '';   // 舊行備查:v5.136.0 曾改成 'none'(左上登入鈕永久隱藏),本輪作廢
      if(userInfo) userInfo.style.display = 'none';
      try{ if(typeof window._lxpsSyncAdventureBtnState === 'function') window._lxpsSyncAdventureBtnState(); }catch(_){}
      // ★ v5.165.0 — 手動登出清掉 30 分鐘共用登入戳記，不留給下一位共用裝置的同學撿到
      try{
        localStorage.removeItem('lxps_shared_login_ts');
        localStorage.removeItem('lxps_shared_login_uid');
        localStorage.removeItem('lxps_shared_login_email');
      }catch(_){}
      try {
        window._myRepHero = null;
        window._playerNickname = null;
        // 頂部玩家欄
        const _avEl = document.getElementById('google-avatar');
        if(_avEl) _avEl.src = '';
        const _nmEl = document.getElementById('google-name');
        if(_nmEl) _nmEl.textContent = '';
        // 冒險畫面玩家名字
        const _advUName = document.getElementById('adv-user-name');
        if(_advUName) _advUName.textContent = '';
        // 代表英雄欄(冒險選關頁頂端)
        const _rhName = document.getElementById('adv-rep-hero-name');
        if(_rhName) _rhName.textContent = '';
        const _rhLv   = document.getElementById('adv-rep-hero-lv');
        if(_rhLv)   _rhLv.textContent = '';
        const _rhWrap = document.getElementById('adv-rep-hero-img-wrap');
        if(_rhWrap) _rhWrap.innerHTML = '';
        const _rhBar  = document.getElementById('adv-rep-hero-bar');
        if(_rhBar)  _rhBar.style.display = 'none';
        // 好友面板若還開著就關掉(避免顯示前一位的好友)
        const _fp = document.getElementById('_friend-panel');
        if(_fp) _fp.remove();
      } catch(_e) { console.warn('[登出 UI 清理] 失敗', _e); }
    }
  });

  // ════════════════════════════════════════════════════════════════════
  // ★ v2.0 — 世界 BOSS 連線層 (此區塊由 world-boss.js 模組依賴,必須留在
  //         Firebase module 內以便使用 doc/setDoc/onSnapshot 等 SDK 函式)
  // ════════════════════════════════════════════════════════════════════
  // ★ v2.0 — 世界 BOSS 連線層 _wbNet (精簡版)
  // ─────────────────────────────────────────────────────────────────────
  // 功能:房間生命週期、選角、戰局同步、聊天表情
  // 架構:房主權威(Host-authoritative)
  //   - 房主跑本地戰鬥引擎,把 state 廣播給其他玩家
  //   - 其他玩家送 action intent,房主收到後執行
  // ═════════════════════════════════════════════════════════════════════
  (function setupWorldBossNet(){
    const WB = {
      roomId: null, isHost: false, mySlot: -1,
      roomUnsub: null, heartbeatTimer: null, lastSeenSnap: null,
      contributions: {},  // { uid: { dmgDealt, healDone, kos, dmgTaken } }
      battleStartTs: 0,
      _quotaCoolUntil: 0,  // ★ v5.11.0 — Firestore 配額熔斷:偵測 resource-exhausted 後暫停心跳/watchdog 到此時間戳
      listeners: {
        onRoomUpdate: null, onBattleUpdate: null, onMyTurn: null,
        onChatMessage: null, onError: null, onRoomClosed: null,
        onBattleEnded: null,
        // ★ v3.5.24(2026-05-22) — 補上漏的 key,讓 UI 端的 onPlayerOnlineChange listener
        //   能透過 .on() 註冊到。原本 .on() 內 `if(e in WB.listeners)` 守門會把這個事件
        //   的 listener 丟掉,導致 _subscribeRoom 內 _emit('onPlayerOnlineChange',...) 沒人接,
        //   進而讓 world-boss-ui.html line 7791 的代打接管邏輯永遠不會觸發。
        //   後果:玩家斷線時 watchdog 雖然會把 player.online=false 寫進 Firestore,
        //         但 hero._wbIsHostNpc=true 永遠不會被設,房主不接手代打、離線玩家
        //         也看不到「房主代打中」遮罩 — 整套設計空轉。
        onPlayerOnlineChange: null,
      },
      _codeChars: 'ABCDEFGHJKMNPQRSTUVWXYZ23456789',
    };

    function _genRoomCode(){
      let code = '';
      for(let i = 0; i < 6; i++) code += WB._codeChars.charAt(Math.floor(Math.random() * WB._codeChars.length));
      return code;
    }
    function _emit(eventName, payload){
      const fn = WB.listeners[eventName];
      if(typeof fn === 'function'){ try{ fn(payload); }catch(e){ console.error('[WBNet]', eventName, e); } }
    }
    function _requireLogin(){
      if(!window._gUserId){ _emit('onError', { msg: '請先登入 Google 才能連線' }); return false; }
      return true;
    }
    // ════════════════════════════════════════════════════════════════════
    // ★ v6 Phase 1 — 序列化 / 反序列化擴充
    // ────────────────────────────────────────────────────────────────────
    // 目的: 把主程式 G + hero 完整地寫入 Firestore,讓非房主端能還原本機 G
    //       走「深拷貝 + 黑名單」策略 — hero 上有 110+ 個自訂欄位,白名單法
    //       不可維護。安全規則: 跳過 function / DOM / 循環引用 / Set / Map
    //
    // ⚠ 舊版 _heroToWire 命名 bug: 主程式 hero 物件用 h.status (單數),
    //   舊版誤用 h.statuses (複數) → status 永遠 wire 出空陣列。新版修正。
    // ════════════════════════════════════════════════════════════════════

    // ── 深拷貝核心 — 跳過所有不可序列化的東西 ─────────────────────────
    // 黑名單欄位(hero / G 上絕對不 wire 的)
    const _WIRE_HERO_SKIP = new Set([
      '_mimicInProgress',  // 開著的 modal 引用,反序列化沒意義
      '_handledByOnCard',  // DOM 互動旗標,跨機器無意義
    ]);
    const _WIRE_G_SKIP = new Set([
      'activeChar', 'selMove', 'selItem', 'pendingTarget',
      'taunted', 'waitFor', 'interruptTarget',
      'gunnerTarget', 'guardFor',
      // 這些都是「目前選中的物件 ref」,跨機器要靠 {side,pos} 重指或 reset
      // 但戰鬥節奏由房主推進,非房主用不到這些,reset 為 null 即可
    ]);

    function _wireDeepClone(v, depth){
      depth = depth || 0;
      if(depth > 10) return null;  // 防爆 recursion
      if(v === null || v === undefined) return v;
      const t = typeof v;
      if(t === 'function') return undefined;  // 跳過 function
      if(t === 'number' || t === 'string' || t === 'boolean') return v;
      if(t !== 'object') return undefined;
      // DOM Element / Node → 跳過
      if(typeof window !== 'undefined' && window.Node && v instanceof window.Node) return undefined;
      // Set → array
      if(v instanceof Set) return Array.from(v).map(x => _wireDeepClone(x, depth+1)).filter(x => x !== undefined);
      // Map → object
      if(v instanceof Map){
        const o = {};
        v.forEach((val, key) => {
          if(typeof key === 'string' || typeof key === 'number'){
            const c = _wireDeepClone(val, depth+1);
            if(c !== undefined) o[String(key)] = c;
          }
        });
        return o;
      }
      // Array
      if(Array.isArray(v)){
        return v.map(x => _wireDeepClone(x, depth+1))
                .map(x => x === undefined ? null : x);  // 保留 array index
      }
      // 一般物件 — 列舉自有 enumerable property
      const out = {};
      for(const k in v){
        if(!Object.prototype.hasOwnProperty.call(v, k)) continue;
        if(_WIRE_HERO_SKIP.has(k)) continue;
        const c = _wireDeepClone(v[k], depth+1);
        if(c !== undefined) out[k] = c;
      }
      return out;
    }

    // ── hero → wire (v6) ──────────────────────────────────────────────
    function _heroToWireV6(h){
      if(!h) return null;
      // 走深拷貝,然後修正 status/buffs 確保是 array
      const w = _wireDeepClone(h, 0);
      if(!w) return null;
      // 主程式用 h.status (單數),保留並補 h.buffs
      if(!Array.isArray(w.status)) w.status = [];
      if(!Array.isArray(w.buffs))  w.buffs  = [];
      // 衍生欄位:isDead 方便接收端快速判斷
      w.isDead = !!(h.isDead) || (h.curHp <= 0);
      return w;
    }

    // ── wire → hero (v6) ──────────────────────────────────────────────
    // 用 newHero(name,side,pos) 起底建立完整骨架(含 av/_dualStarForm 等預設),
    // 然後把 wire 內的所有欄位覆蓋上去。這樣即使 wire 漏某欄位,也有預設值兜底。
    function _wireToHeroV6(w){
      if(!w || !w.name) return null;
      let h = null;
      try{
        if(typeof window.newHero === 'function'){
          h = window.newHero(w.name, w.side || 'p1', w.pos != null ? w.pos : 0);
        }
      }catch(e){ console.warn('[WB-Wire] newHero 失敗 for', w.name, e); }
      if(!h){
        // fallback:空殼 hero(防呆,正常不會走到)
        h = { name: w.name, side: w.side || 'p1', pos: w.pos || 0,
              hp: w.hp || 1, curHp: w.curHp || 1, atk: 0, sp: 0, spd: 0,
              status: [], buffs: [], s1: {}, s2: {} };
      }
      // 把 wire 內所有欄位覆蓋(包含 status/buffs/_wb* 等)
      for(const k in w){
        if(!Object.prototype.hasOwnProperty.call(w, k)) continue;
        h[k] = w[k];
      }
      // 確保 array 欄位至少是空 array(防呆)
      if(!Array.isArray(h.status)) h.status = [];
      if(!Array.isArray(h.buffs))  h.buffs  = [];
      return h;
    }

    // ── G → wire ──────────────────────────────────────────────────────
    function _GToWire(G){
      if(!G) return null;
      let _activeSidePos = null;
      try{
        const _ac = G.activeChar;
        if(_ac && _ac.side && typeof _ac.pos === 'number'){
          _activeSidePos = { side: _ac.side, pos: _ac.pos };
        }
      }catch(_){}
      const out = {
        // 戰鬥節奏
        // ★ v3.5.69 — 補上 round 序列化
        //   根因:主程式真正的「回合數」是 G.round(nextRound() line ~33621 G.round = (G.round||1) + 1),
        //         G.turn 在 _wbSetupAdvForBattle 只被設 0 一次,從不遞增 → 永遠 0
        //   舊版只傳 turn,client 收到後 G.round 永遠停在初始 1,造成:
        //     1. UI 顯示「回合 1」永不變
        //     2. BOSS 行動前題 _expectedSlot = (round-1) % 4 永遠 0 → 只有房主能答題
        //     3. 答題獎勵累積永遠到不了 5 / 10 / 15 / 20 → 5000 獎勵不出現
        //     4. 元素盾觸發回合(5/9/13/17)client 看不出來 → 「打光又補滿」感
        turn: G.turn || 1,
        round: G.round || 1,
        currentActorIdx: G.currentActorIdx || 0,
        turnOrder: Array.isArray(G.turnOrder)
          ? G.turnOrder.map(o => ({ side: o.side, pos: o.pos, spd: o.spd }))
          : [],
        // ★ FIX 20260517(dd) — 直接帶 activeChar 的 side+pos(主程式真實狀態)
        _activeSidePos: _activeSidePos,
        // 兩邊陣容
        p1: Array.isArray(G.p1) ? G.p1.map(_heroToWireV6) : [],
        p2: Array.isArray(G.p2) ? G.p2.map(_heroToWireV6) : [],
        // 能量
        energy: G.energy ? { p1: G.energy.p1 || 0, p2: G.energy.p2 || 0 } : { p1: 0, p2: 0 },
        // 戰場物品列(deck/inv/inv2 可能含 item card 物件)
        inv:  Array.isArray(G.inv)  ? _wireDeepClone(G.inv,  0) : [null,null,null],
        inv2: Array.isArray(G.inv2) ? _wireDeepClone(G.inv2, 0) : [null,null,null],
        deck: Array.isArray(G.deck) ? _wireDeepClone(G.deck, 0) : [],
        // 戰績統計
        battleStats: G.battleStats ? _wireDeepClone(G.battleStats, 0) : {},
        // 連招疲勞
        lastSkillName: G.lastSkillName || null,
        lastSkill: G.lastSkill || null,
        comboFatigue: G.comboFatigue || 0,
        lastSkillByHero: G.lastSkillByHero ? _wireDeepClone(G.lastSkillByHero, 0) : {},
        comboFatigueByHero: G.comboFatigueByHero ? _wireDeepClone(G.comboFatigueByHero, 0) : {},
        // 雜項戰場狀態旗標
        alchemist_s2_used: !!G.alchemist_s2_used,
        passiveInterruptUsed: !!G.passiveInterruptUsed,
        // Set → array
        sellUsed: G.sellUsed instanceof Set ? Array.from(G.sellUsed) : [],
        // dmgUp 是 array(回合內提傷標記)
        dmgUp: Array.isArray(G.dmgUp) ? _wireDeepClone(G.dmgUp, 0) : [],
        // 世界 BOSS 戰專屬
        _wbBurstUsed: G._wbBurstUsed ? _wireDeepClone(G._wbBurstUsed, 0) : null,
        _mimicActor: G._mimicActor || null,
        _mimicCost: G._mimicCost != null ? G._mimicCost : null,
        _mimicStype: G._mimicStype || null,
        _gunnerUsedThisTurn: !!G._gunnerUsedThisTurn,
        _tarotSellBonus: G._tarotSellBonus != null ? G._tarotSellBonus : null,
        _assetGuard: G._assetGuard != null ? G._assetGuard : null,
      };
      return out;
    }

    // ── wire → G(在已存在的 G 上覆蓋,而非 replace) ────────────────
    // 注意:非房主端的 window.G 一開始也是經由 _wbSetupAdvForBattle 建立,
    // 之後每次 onBattleUpdate 都呼叫這個函式覆蓋,不要直接 window.G = newG
    // (因為主程式可能持有 G 的 reference,直接覆蓋會脫鉤)
    function _applyWireToG(G, wire){
      if(!G || !wire) return false;
      G.turn = wire.turn || 1;
      // ★ v3.5.69 — 還原 round(舊版漏掉,造成 client 卡 round=1)
      //   fallback 到 wire.turn 是為了兼容房主推送舊格式 payload(沒帶 round)
      G.round = wire.round || wire.turn || 1;
      G.currentActorIdx = wire.currentActorIdx || 0;
      G.turnOrder = Array.isArray(wire.turnOrder) ? wire.turnOrder.slice() : [];
      // ★ FIX 20260517(dd) — 帶過來給 client 端重建 activeChar 用
      G._activeSidePos = wire._activeSidePos || null;
      // 英雄陣容 — 用 _wireToHeroV6 還原成完整 hero 物件
      G.p1 = Array.isArray(wire.p1) ? wire.p1.map(_wireToHeroV6) : [];
      G.p2 = Array.isArray(wire.p2) ? wire.p2.map(_wireToHeroV6) : [];
      G.energy = wire.energy ? { p1: wire.energy.p1 || 0, p2: wire.energy.p2 || 0 } : { p1: 0, p2: 0 };
      G.inv  = Array.isArray(wire.inv)  ? wire.inv.slice()  : [null,null,null];
      G.inv2 = Array.isArray(wire.inv2) ? wire.inv2.slice() : [null,null,null];
      G.deck = Array.isArray(wire.deck) ? wire.deck.slice() : [];
      G.battleStats = wire.battleStats || {};
      G.lastSkillName = wire.lastSkillName || null;
      G.lastSkill     = wire.lastSkill     || null;
      G.comboFatigue  = wire.comboFatigue  || 0;
      G.lastSkillByHero    = wire.lastSkillByHero    || {};
      G.comboFatigueByHero = wire.comboFatigueByHero || {};
      G.alchemist_s2_used    = !!wire.alchemist_s2_used;
      G.passiveInterruptUsed = !!wire.passiveInterruptUsed;
      G.sellUsed = new Set(Array.isArray(wire.sellUsed) ? wire.sellUsed : []);
      G.dmgUp    = Array.isArray(wire.dmgUp) ? wire.dmgUp : [];
      // 世界 BOSS 戰專屬
      G._wbBurstUsed = wire._wbBurstUsed || null;
      G._mimicActor  = wire._mimicActor  || null;
      G._mimicCost   = wire._mimicCost;
      G._mimicStype  = wire._mimicStype  || null;
      G._gunnerUsedThisTurn = !!wire._gunnerUsedThisTurn;
      G._tarotSellBonus = wire._tarotSellBonus;
      G._assetGuard     = wire._assetGuard;
      // 「目前選中」相關欄位 reset(由本機 UI 重建)
      G.activeChar = null;
      G.selMove = null;
      G.selItem = null;
      G.pendingTarget = null;
      G.taunted = null;
      G.waitFor = null;
      G.interruptTarget = null;
      G.gunnerTarget = null;
      G.guardFor = null;
      return true;
    }

    // ── 對外暴露(讓 world-boss.js / world-boss-ui.html 可用) ──────────
    try{
      window._wbWireUtils = {
        heroToWire:   _heroToWireV6,
        wireToHero:   _wireToHeroV6,
        GToWire:      _GToWire,
        applyWireToG: _applyWireToG,
        deepClone:    _wireDeepClone,
      };
    }catch(_){}

    function _heroToWire(h){ return _heroToWireV6(h); }
    function _heroesToWire(arr){ return Array.isArray(arr) ? arr.map(_heroToWireV6) : []; }

    async function createRoom(opts){
      if(!_requireLogin()) return null;
      const bossId = (opts && opts.bossId) || ((typeof window._wbGetCurrentBossId === 'function') ? window._wbGetCurrentBossId() : 'vesuvius_fire_dragon');  // ★ v3.14.20
      const visibility = (opts && opts.visibility === 'public') ? 'public' : 'private';
      let roomId = null;
      for(let attempt = 0; attempt < 5; attempt++){
        const c = _genRoomCode();
        try{
          const sn = await getDoc(doc(_fbDb, 'worldBossRooms', c));
          if(!sn.exists()){ roomId = c; break; }
        }catch(_eGen){
          // ★ v5.11.0 — 配額耗盡時立即停止嘗試(後續 getDoc 一樣會失敗,續試只是加重風暴)
          if(_wbQuotaTrip(_eGen)) break;
        }
      }
      if(!roomId){
        _emit('onError', { msg: _wbQuotaCooling() ? _wbQuotaMsg() : '房號產生失敗' });  // ★ v5.11.0 — 配額情境改友善雙版訊息
        return null;
      }

      const myName = (window._fbUser && (window._fbUser.displayName || window._fbUser.email)) ||
                     localStorage.getItem('lxps_nickname_' + window._gUserId) || '房主';
      // ★ v3.5.70 — 房主 email 也存進 player 物件,給排行榜跑 _getAdminPlayerLabel 用
      //   老師需求 2026/5/24:「現在全都是***同學沒有意義」— 排行榜要顯示班級座號(5324蔣同學)
      //   舊版 player 物件只有 uid/name/slotIdx 等,沒 email,排行榜跑保護策略時無法反查名冊
      const myEmail = ((window._fbUser && window._fbUser.email) || '').toLowerCase();

      const initial = {
        meta: {
          hostUid: window._gUserId,
          hostName: myName,           // ★ 新:公開房列表用
          bossId: bossId,
          status: 'waiting',
          visibility: visibility,     // ★ 新:'private' | 'public'
          createdAt: Date.now(),
          version: 1
        },
        players: [{
          uid: window._gUserId, name: myName, email: myEmail, slotIdx: 0,
          heroName: null, element: null, ready: false, online: true, lastSeen: Date.now()
        }],
        battle: null, pendingAction: null, chat: []
      };
      try{
        await setDoc(doc(_fbDb, 'worldBossRooms', roomId), initial);
        WB.roomId = roomId; WB.isHost = true; WB.mySlot = 0;
        _subscribeRoom(roomId); _startHeartbeat();
        // ★ FIX 20260517(x) — 房主啟動離線監測 watchdog
        _startOfflineWatchdog();
        // ★ v3.12.1(2026-05-30) — 進新房間時重置「onRoomClosed 已處理」守門旗標
        //   讓本場結束時 UI handler 能正常處理一次(只一次)
        try{ window._wbRoomClosedHandled = false; }catch(_){}
        console.log('[WBNet] 開房成功:', roomId);
        return roomId;
      }catch(e){
        console.error('[WBNet] 開房失敗', e);
        _emit('onError', { msg: '開房失敗:' + (e.code || e.message) });
        return null;
      }
    }

    async function joinRoom(roomId){
      if(!_requireLogin()) return false;
      const code = (roomId || '').toUpperCase().trim();
      if(code.length !== 6){ _emit('onError', { msg: '房號必須是 6 個字元' }); return false; }
      try{
        const result = await runTransaction(_fbDb, async (tx) => {
          const ref = doc(_fbDb, 'worldBossRooms', code);
          const sn = await tx.get(ref);
          if(!sn.exists()) throw new Error('ROOM_NOT_FOUND');
          const data = sn.data();
          const players = Array.isArray(data.players) ? data.players : [];
          const existIdx = players.findIndex(p => p.uid === window._gUserId);
          // ★ FIX 20260517(x) — 已在 players 內 → 視為重連,放行任何 status(含 fighting)
          if(existIdx >= 0){
            players[existIdx].online = true; players[existIdx].lastSeen = Date.now();
            tx.update(ref, { players: players });
            return { slotIdx: existIdx, rejoin: true };
          }
          // 新加入者:只能在 waiting/picking 階段
          if(data.meta.status !== 'waiting' && data.meta.status !== 'picking') throw new Error('ROOM_IN_BATTLE');
          if(players.length >= 4) throw new Error('ROOM_FULL');
          const myName = (window._fbUser && (window._fbUser.displayName || window._fbUser.email)) ||
                         localStorage.getItem('lxps_nickname_' + window._gUserId) || '玩家';
          // ★ v3.5.70 — 加入房間時也存 email,給排行榜班級座號顯示用
          const myEmail = ((window._fbUser && window._fbUser.email) || '').toLowerCase();
          const slot = players.length;
          players.push({
            uid: window._gUserId, name: myName, email: myEmail, slotIdx: slot,
            heroName: null, element: null, ready: false, online: true, lastSeen: Date.now()
          });
          tx.update(ref, { players: players });
          return { slotIdx: slot, rejoin: false };
        });
        WB.roomId = code; WB.isHost = false; WB.mySlot = result.slotIdx;
        _subscribeRoom(code); _startHeartbeat();
        // ★ v3.12.1(2026-05-30) — 進新房間時重置「onRoomClosed 已處理」守門旗標
        try{ window._wbRoomClosedHandled = false; }catch(_){}
        console.log('[WBNet] 加入房間:', code, '槽位', result.slotIdx);
        return true;
      }catch(e){
        if(_wbQuotaTrip(e)){ _emit('onError', { msg: _wbQuotaMsg() }); return false; }  // ★ v5.11.0 — 配額耗盡改友善雙版訊息
        const msgMap = {
          ROOM_NOT_FOUND: '找不到這個房號',
          ROOM_IN_BATTLE: '這個房間已經開戰',
          ROOM_FULL: '房間已滿 (4/4)'
        };
        _emit('onError', { msg: msgMap[e.message] || ('加入失敗:' + (e.code || e.message)) });
        return false;
      }
    }

    async function leaveRoom(){
      if(!WB.roomId) return;
      const roomId = WB.roomId;
      _stopHeartbeat(); _stopOfflineWatchdog(); _unsubscribeRoom();
      try{
        if(WB.isHost){
          await updateDoc(doc(_fbDb, 'worldBossRooms', roomId), {
            'meta.status': 'ended', 'meta.endedReason': 'host_left', 'meta.endedAt': Date.now()
          });
        }else{
          await runTransaction(_fbDb, async (tx) => {
            const ref = doc(_fbDb, 'worldBossRooms', roomId);
            const sn = await tx.get(ref);
            if(!sn.exists()) return;
            const data = sn.data();
            const status = data.meta && data.meta.status;
            const inBattle = (status === 'fighting');
            if(inBattle){
              // 戰鬥中:標 offline,不移除
              const players = data.players || [];
              const idx = players.findIndex(p => p && p.uid === window._gUserId);
              if(idx >= 0){
                players[idx].online = false;
                players[idx].lastSeen = 0;  // 設 0 確保 watchdog 不會立刻把他標回 online
                tx.update(ref, { players: players });
              }
            }else{
              // 戰鬥前:從 players 移除 + 重排 slotIdx
              const players = (data.players || []).filter(p => p.uid !== window._gUserId);
              players.forEach((p, i) => { p.slotIdx = i; });
              // ★ v3.5.6 — 修「公開房間沒人卻一直留在瀏覽列表」的 BUG
              //   觸發情境:房主直接關瀏覽器/斷網(沒走 leaveRoom),其他玩家依序退出後,
              //            房間只剩空 players 陣列,但 meta.status 還是 waiting/picking,
              //            導致 listPublicRooms 還是把它列出來,變成幽靈房。
              //   修法:非房主離開後若 players 變空 → 把 meta.status 標 'ended',
              //         listPublicRooms 的 where('meta.status', 'in', ['waiting','picking']) 就會自動濾掉。
              const _updatePayload = { players: players };
              if(players.length === 0){
                _updatePayload['meta.status'] = 'ended';
                _updatePayload['meta.endedReason'] = 'all_players_left';
                _updatePayload['meta.endedAt'] = Date.now();
                console.log('[WBNet v3.5.6] 最後一位非房主玩家離開,房間自動關閉');
              }
              tx.update(ref, _updatePayload);
            }
          });
        }
      }catch(_){}
      WB.roomId = null; WB.isHost = false; WB.mySlot = -1;
    }

    // ─── B. 選角 ────────────────────────────────────────────────
    // ★ v3.4.16 — 取得「我自己英雄的養成資料」(供 setMyHero 自動帶上)
    //   修復:房主開戰時建構真人玩家 hero 物件原本沒套技能等級/屬性點/爆發等級
    //   → 戰鬥中所有玩家技能威力都是 Lv.0
    //   解法:把這些資料塞進 element JSON 的 'g' 欄位(g = growth),
    //         房主開戰時讀出來套到 hero._skLv / _burstLvAdv 等
    function _wbBuildMyGrowth(heroName){
      if(!heroName) return null;
      try{
        const lv = (typeof _heroLevels    !== 'undefined' && _heroLevels    && _heroLevels[heroName])    || 1;
        const sk = (typeof _heroSkillLevels !== 'undefined' && _heroSkillLevels && _heroSkillLevels[heroName]) || {};
        const bu = (typeof _heroBurstLevels !== 'undefined' && _heroBurstLevels && _heroBurstLevels[heroName]) || 0;
        const iv = (typeof _heroStatInvested !== 'undefined' && _heroStatInvested && _heroStatInvested[heroName]) || {};
        return {
          lv: lv,
          skLv: { s1: (sk.s1 || 0), s2: (sk.s2 || 0) },
          burst: bu,
          inv: { hp: (iv.hp || 0), atk: (iv.atk || 0), sp: (iv.sp || 0), spd: (iv.spd || 0) }
        };
      }catch(e){
        console.warn('[WB-Growth] _wbBuildMyGrowth 失敗', e);
        return null;
      }
    }

    // 把 g (養成資料) 注入 element JSON 字串 (若 element 不是 JSON 字串,自動包裝)
    function _wbInjectGrowthIntoElement(elementStr, growth){
      if(!growth) return elementStr;
      let obj = null;
      if(typeof elementStr === 'string' && elementStr.startsWith('{')){
        try{ obj = JSON.parse(elementStr); }catch(_){ obj = null; }
      }
      if(!obj){
        // element 是純字串 (舊格式) 或 null → 包裝成 JSON
        const e = (typeof elementStr === 'string' && elementStr) ? elementStr : null;
        obj = { e: e, es: e ? [e] : [], t: [] };
      }
      obj.g = growth;
      return JSON.stringify(obj);
    }

    // ★ v3.4.18 — export 到 window 供 world-boss-ui.html 單人練習 mock setMyHero 使用
    //   原本這兩個函式是 IIFE scope 內,只在連線模式 setMyHero 用得到,單人練習 mock 拿不到。
    //   修 BUG:單人練習房主自己槽位的 element JSON 永遠沒有 g 欄位 → 進戰鬥技能全 Lv.0。
    try{
      window._wbBuildMyGrowth = _wbBuildMyGrowth;
      window._wbInjectGrowthIntoElement = _wbInjectGrowthIntoElement;
    }catch(_){}

    async function setMyHero(heroName, element){
      if(!WB.roomId) return false;
      try{
        // ★ v3.4.16 — 自動把自己的養成資料 (技能等級/屬性點/爆發等級/英雄等級) 塞進 element JSON
        //   讓房主開戰時建構此玩家的 hero 物件能套上正確的技能威力
        const _growth = _wbBuildMyGrowth(heroName);
        const _elementWithGrowth = _wbInjectGrowthIntoElement(element, _growth);
        await runTransaction(_fbDb, async (tx) => {
          const ref = doc(_fbDb, 'worldBossRooms', WB.roomId);
          const sn = await tx.get(ref);
          if(!sn.exists()) throw new Error('ROOM_GONE');
          const data = sn.data();
          const players = data.players || [];
          const me = players.find(p => p.uid === window._gUserId);
          if(!me) throw new Error('NOT_IN_ROOM');
          me.heroName = heroName;
          me.element = _elementWithGrowth || null;
          me.ready = false;  // 改角要重新按準備
          tx.update(ref, { players: players });
        });
        return true;
      }catch(e){
        if(_wbQuotaTrip(e)){ _emit('onError', { msg: _wbQuotaMsg() }); return false; }  // ★ v5.11.0 — 根治「選角失敗:resource-exhausted」英文錯誤碼直出
        const msg = { NOT_IN_ROOM: '你不在這個房間', ROOM_GONE: '房間已不存在' };
        _emit('onError', { msg: msg[e.message] || ('選角失敗:' + (e.code || e.message)) });
        return false;
      }
    }

    async function toggleReady(){
      if(!WB.roomId) return false;
      try{
        await runTransaction(_fbDb, async (tx) => {
          const ref = doc(_fbDb, 'worldBossRooms', WB.roomId);
          const sn = await tx.get(ref);
          if(!sn.exists()) throw new Error('ROOM_GONE');
          const players = sn.data().players || [];
          const me = players.find(p => p.uid === window._gUserId);
          if(!me) throw new Error('NOT_IN_ROOM');
          if(!me.heroName) throw new Error('NO_HERO');
          me.ready = !me.ready;
          tx.update(ref, { players: players });
        });
        return true;
      }catch(e){
        if(_wbQuotaTrip(e)){ _emit('onError', { msg: _wbQuotaMsg() }); return false; }  // ★ v5.11.0 — 配額耗盡改友善雙版訊息
        const msg = { NO_HERO: '請先選好英雄', NOT_IN_ROOM: '你不在這個房間', ROOM_GONE: '房間已不存在' };
        _emit('onError', { msg: msg[e.message] || '切換準備失敗' });
        return false;
      }
    }

    async function startBattle(initialBattle){
      if(!WB.isHost){ _emit('onError', { msg: '只有房主可以開戰' }); return false; }
      if(!WB.roomId) return false;
      WB.battleStartTs = Date.now();
      WB.contributions = {};
      // ★ FIX 20260517(y) — 設「開戰中」旗標,讓 heartbeat / watchdog 這次 skip,
      //   避免它們的 transaction 跟 startBattle 的 commit 撞 → failed-precondition。
      WB._startingBattle = true;
      try{
        await runTransaction(_fbDb, async (tx) => {
          const ref = doc(_fbDb, 'worldBossRooms', WB.roomId);
          const sn = await tx.get(ref);
          if(!sn.exists()) throw new Error('ROOM_GONE');
          const data = sn.data();
          // 把 meta.status 改 fighting,並寫入 battle 物件(保留現有 players 等其他欄位)
          const newMeta = Object.assign({}, data.meta || {}, {
            status: 'fighting',
            battleStartedAt: Date.now()
          });
          tx.update(ref, {
            meta: newMeta,
            battle: {
              turn: 1,
              currentActorIdx: initialBattle.currentActorIdx || 0,
              turnOrder: initialBattle.turnOrder || [],
              p1: _heroesToWire(initialBattle.p1),
              p2: _heroesToWire(initialBattle.p2),
              log: [], version: 1
            }
          });
        });
        console.log('[WBNet] ✅ startBattle commit 成功 (room=' + WB.roomId + ')');
        return true;
      }catch(e){
        // ★ FIX 20260517(y) — 失敗訊息同時打 console 讓 dev 看得到
        console.error('[WBNet] ❌ startBattle 失敗', e);
        _emit('onError', { msg: '開戰失敗:' + (e.code || e.message || e) });
        return false;
      }finally{
        WB._startingBattle = false;
      }
    }

    // ─── C. 戰鬥同步 ────────────────────────────────────────────
    async function sendAction(action){
      if(!WB.roomId) return false;
      if(WB.isHost){
        if(window._wbConnectedHostMode && typeof window._wbExecPlayerAction === 'function'){
          try{ window._wbExecPlayerAction(WB.mySlot, action); }catch(e){ console.error('[WBNet] _wbExecPlayerAction', e); }
        }else if(typeof window._wbHostExecuteAction === 'function'){
          // 舊版自製戰鬥引擎 fallback(單人練習等不會走進來,但保留)
          try{ window._wbHostExecuteAction(WB.mySlot, action); }catch(e){ console.error(e); }
        }
        return true;
      }
      // ★ v3.5.1 — 非房主端 updateDoc 加上 failed-precondition 重試
      //   根因:雙方同時寫 worldBossRooms/{roomId} 文件會撞樂觀鎖 → 後到者拿 failed-precondition
      //         原版只 try 一次 → 玩家點按鈕的 action 永久消失 → 房主以為玩家還沒操作 → 全場卡死
      //   修法:最多 3 次重試,指數退避(200ms → 400ms → 800ms)
      //         單次失敗的話 _emit('onError') 仍會發,但 toast 改在全部失敗才發
      const _maxAttempts = 3;
      let _lastErr = null;
      for(let _attempt = 0; _attempt < _maxAttempts; _attempt++){
        try{
          await updateDoc(doc(_fbDb, 'worldBossRooms', WB.roomId), {
            pendingAction: {
              actorUid: window._gUserId,
              actorSlot: WB.mySlot,
              action: action,
              ts: Date.now()
            }
          });
          if(_attempt > 0){
            console.info('[WBNet] sendAction 第 ' + (_attempt+1) + ' 次重試成功');
          }
          return true;
        }catch(e){
          _lastErr = e;
          const _isPrecond = e && (e.code === 'failed-precondition' ||
                                   (e.message || '').includes('failed-precondition'));
          console.warn('[WBNet] sendAction 第 ' + (_attempt+1) + ' 次失敗' +
            (_isPrecond ? ' (樂觀鎖衝突)' : ''), e && e.message);
          if(_attempt < _maxAttempts - 1){
            // 指數退避 + 加上小隨機抖動,避免多人同步重試又撞
            const _delay = 200 * Math.pow(2, _attempt) + Math.floor(Math.random() * 100);
            await new Promise(r => setTimeout(r, _delay));
          }
        }
      }
      // 全部失敗 → 才發 onError
      console.error('[WBNet] sendAction 重試 ' + _maxAttempts + ' 次都失敗', _lastErr);
      _emit('onError', { msg: '行動送出失敗(重試 3 次後仍無法送到房主)' });
      return false;
    }

    // ★ v3.3.6 — 深層清理 undefined(Firestore 不接受 undefined,只接受 null 或缺欄位)
    //   根因(老師圖中 console 大量 400 Bad Request + FirebaseError "Unsupported field value: undefined"):
    //     _GToWire 把 G.activeChar / hero 的 _xxx 私用欄位 / wire 內 nested array 元素
    //     等可能是 undefined 的東西塞進 wireG → updateDoc 時 Firestore 拒絕整個寫入
    //     → client 永遠收不到 state 更新 → 隊友卡死(看圖中 [卡死偵測] 訊息)。
    //   修法:在 hostPushBattleState 寫入前對整個 payload 做 deep clean,
    //         array 元素 undefined → null,object 屬性 undefined → 直接刪除。
    function _wbDeepCleanUndefined(v, depth){
      if(depth == null) depth = 0;
      if(depth > 20) return null;
      if(v === undefined) return null;
      if(v === null) return null;
      const t = typeof v;
      if(t === 'string' || t === 'number' || t === 'boolean') return v;
      if(t === 'function') return null;
      if(t === 'symbol') return null;
      if(Array.isArray(v)){
        const out = [];
        for(let i = 0; i < v.length; i++){
          const cleaned = _wbDeepCleanUndefined(v[i], depth + 1);
          out.push(cleaned === undefined ? null : cleaned);
        }
        return out;
      }
      if(t === 'object'){
        // 防 DOM/HTMLElement/Window/Document 等非 plain object 鑽進來
        if(v.nodeType !== undefined && v.nodeName !== undefined) return null;
        const out = {};
        for(const k in v){
          if(!Object.prototype.hasOwnProperty.call(v, k)) continue;
          const cleaned = _wbDeepCleanUndefined(v[k], depth + 1);
          if(cleaned !== undefined) out[k] = cleaned;
        }
        return out;
      }
      return null;
    }

    async function hostPushBattleState(newState){
      if(!WB.isHost || !WB.roomId) return false;
      try{
        const existing = WB.lastSeenSnap?.battle?.log || [];
        const newLog = existing.concat(newState.logEntries || []).slice(-60);
        let battle;
        if(newState.fullWireG){
          // 走新版:完整 G wire
          // ★ v3.3.3 — 取出 FX 佇列附在 battle 上,client 收到後重播
          let _fxEvents = [];
          try{
            if(typeof window._wbFxQueueFlush === 'function'){
              _fxEvents = window._wbFxQueueFlush();
            }
          }catch(_){}
          battle = Object.assign({}, newState.fullWireG, {
            log: newLog,
            version: ((WB.lastSeenSnap?.battle?.version) || 1) + 1,
            _syncReason: newState._syncReason || 'unknown',
            _syncTs: Date.now(),
            _schemaV6: true,
            // 也把整份 fullWireG 副本放在子欄位裡,讓 client applyWireToG(G, battle.fullWireG)
            //  能順利取到完整資料(不依賴 spread)。
            fullWireG: newState.fullWireG,
            // ★ v3.3.3 — FX 事件清單(client 收到後重播,讓全隊看到同步特效)
            _wbFxEvents: _fxEvents,
            // ★ v3.4.20 — 修 BUG:龍王戰玩家「輪到我回答但看不到題目」,
            //   根因:_wbHostBroadcastQuizShow 傳 _wbQuizPayload 進來,但這裡的 Object.assign
            //         沒把它複製進 battle 物件 → client 收到 _syncReason='quiz-show' 但
            //         battle._wbQuizPayload=undefined → world-boss-ui.html line 8047 條件 fail
            //         → 完全不 render quiz 視窗 → 玩家看不到題目。
            //   修法:明確帶上 _wbQuizPayload(若 newState 有的話),保持向下相容。
            //   同步帶上未來可能擴充的 _wb* 廣播欄位,避免相同 bug 再來一次。
            _wbQuizPayload: newState._wbQuizPayload || null,
          });
        }else{
          // 舊 schema 相容(舊 _wbHostExecuteAction 等地方還沒改完前用)
          battle = {
            turn: newState.turn,
            currentActorIdx: newState.currentActorIdx,
            turnOrder: newState.turnOrder || WB.lastSeenSnap?.battle?.turnOrder || [],
            p1: _heroesToWire(newState.p1),
            p2: _heroesToWire(newState.p2),
            log: newLog,
            version: ((WB.lastSeenSnap?.battle?.version) || 1) + 1
          };
        }
        const payload = { battle: battle, pendingAction: null };
        if(newState.ended){
          payload['meta.status'] = 'ended';
          payload['meta.endedReason'] = newState.endedReason || 'battle_complete';
          payload['meta.endedAt'] = Date.now();
          payload['meta.winner'] = newState.winner || null;
          payload['meta.elapsed'] = Date.now() - WB.battleStartTs;
          payload['meta.contributions'] = WB.contributions;
        }
        // ★ v3.3.6 — 寫入前 deep clean,把所有 undefined 換成 null / 刪除
        //   避免 FirebaseError: Unsupported field value: undefined → 400 Bad Request → client 卡死
        const _cleanPayload = _wbDeepCleanUndefined(payload);
        // ★ v3.5.8 — hostPushBattleState 加重試機制(老師 2026/5/21 回報 P2 client 回合無限循環)
        //   BUG 症狀:client (P2 玩家) 回合永遠卡住,endAction watchdog 不斷送 _client_stuck_resync,
        //             host 重發 sync 也卡住,console 看到 failed-precondition 400 Bad Request。
        //   根因:Firestore 多人同時寫 worldBossRooms/{roomId} 文件撞到樂觀鎖衝突
        //         (玩家送 pendingAction 寫 + host 自己寫 battle.version 同步 → 樂觀鎖),
        //         hostPushBattleState 沒有重試 → 一次失敗就丟下,client 永遠收不到新 sync。
        //   v3.5.1 修了 client 端 sendAction 重試,但 host 端的 hostPushBattleState 漏修。
        //   修法:跟 v3.5.1 sendAction 一樣 — 最多 3 次重試,指數退避 + 隨機抖動。
        const _maxAttempts = 3;
        let _lastErr = null;
        for(let _i = 0; _i < _maxAttempts; _i++){
          try{
            await updateDoc(doc(_fbDb, 'worldBossRooms', WB.roomId), _cleanPayload);
            if(_i > 0){
              console.info('[WBNet] hostPushBattleState 第 ' + (_i+1) + ' 次重試成功 reason=' + (newState._syncReason || 'unknown'));
            }
            return true;
          }catch(e){
            _lastErr = e;
            const _isPrecond = e && (e.code === 'failed-precondition' ||
                                     (e.message || '').includes('failed-precondition'));
            console.warn('[WBNet] hostPushBattleState 第 ' + (_i+1) + ' 次失敗' +
              (_isPrecond ? ' (樂觀鎖衝突, 將重試)' : ''), e && e.message);
            if(_i < _maxAttempts - 1){
              // 指數退避 + 隨機抖動:300/600/1200ms + 0~200ms 隨機
              const _delay = 300 * Math.pow(2, _i) + Math.floor(Math.random() * 200);
              await new Promise(r => setTimeout(r, _delay));
            }
          }
        }
        console.error('[WBNet] hostPushBattleState 重試 ' + _maxAttempts + ' 次都失敗,client 端可能卡住', _lastErr);
        return false;
      }catch(e){
        console.error('[WBNet] hostPushBattleState', e); return false;
      }
    }

    function addContribution(uid, key, val){
      if(!uid) return;
      if(!WB.contributions[uid]) WB.contributions[uid] = {
        dmgDealt:0, healDone:0, kos:0, dmgTaken:0, debuffsApplied:0, buffsGivenAlly:0
      };
      WB.contributions[uid][key] = (WB.contributions[uid][key] || 0) + val;
      try{
        // ★ v3.7.6 — 本場總傷改顯示「全隊累積總傷」(對齊 v3.6.9 設計)
        //   舊邏輯(v3.5.8):只顯示本機 uid 的累計 → host 看到自己的、client 看到 0
        //   新邏輯(v3.7.6):任何 uid 寫入時都把 WB.contributions 全部 uid 的 dmgDealt 加總顯示
        if(key === 'dmgDealt'){
          // 找新 ID 或舊 ID 兩種都更新
          const _dmgEl = document.getElementById('wb-cur-dmg-display')
                      || document.getElementById('wb-solo-cur-dmg-display');
          if(_dmgEl){
            let _teamDmg = 0;
            Object.keys(WB.contributions).forEach(function(_uid){
              _teamDmg += (WB.contributions[_uid] && WB.contributions[_uid].dmgDealt) || 0;
            });
            const _curShown = parseInt((_dmgEl.textContent || '0').replace(/,/g, ''), 10) || 0;
            if(_teamDmg >= _curShown){
              _dmgEl.textContent = _teamDmg.toLocaleString();
            }
          }
        }
      }catch(_){}
    }

    // ─── D. 表情貼圖 ────────────────────────────────────────────
    const QUICK_EMOJI = {
      // 既有 6 個快捷表情(右下角浮現)
      cheer: '💪 加油!', gg: '😭 GG', burst: '💥 用爆發!',
      heal: '🩹 補我血!', wait: '⏸ 等一下', strong: '🔥 太強了!',
      // ★ FIX 20260517(h) — 戰鬥中表情列重新規劃,6 個新 key 對應實戰需求
      //   (原本的 cheer/gg/heal/wait/strong 保留供舊版相容,但戰鬥中 emoji bar 改用下列 6 個)
      burst_now:    '💥 用爆發!',
      heal_first:   '🩹 先補血!',
      revive_first: '✨ 先復活!',
      ctrl_boss:    '🌀 先控住王!',
      awesome:      '🔥 太棒啦!',
      sorry_wait:   '😅 抱歉先等我一下',
      // ★ v3.0 — 新增 6 個答題討論選項(在角色卡頂端泡泡顯示)
      ans_a:    '🅰 我覺得答案是 A',
      ans_b:    '🅱 我覺得答案是 B',
      ans_c:    '🅲 我覺得答案是 C',
      ans_d:    '🅳 我覺得答案是 D',
      ans_idk:  '🤔 我也不確定答案...',
      cheerup:  '💪 加油你可以的!',
      role_dps:    '⚔ 我負責輸出',
      role_heal:   '✨ 我負責治療',
      role_tank:   '🛡 我負責當坦克保護大家',
      role_ctrl:   '🌀 我負責控場',
      role_quiz:   '📖 我負責回答問題',
      brb:         '⏸ 等我一下',
      back:        '🙋 我回來了',
      need_dps:    '⚠ 我們缺輸出',
      need_heal:   '⚠ 我們缺治療',
      need_tank:   '⚠ 我們缺坦克',
      need_ctrl:   '⚠ 我們缺控場',
      leaving:     '🙏 我有事要先退出了,謝謝邀請',
    };
    // ★ v3.0 — 標記哪些 key 是「答題討論」類(在角色卡頂端泡泡顯示而非右下浮現)
    const DISCUSSION_KEYS = new Set(['ans_a','ans_b','ans_c','ans_d','ans_idk','cheerup']);
    // ★ FIX 20260517(f) — 隊伍確認頁定型文 key 集合(用 isDiscussion 旗標讓泡泡顯示在卡上)
    const TEAM_SETUP_KEYS = new Set([
      'role_dps','role_heal','role_tank','role_ctrl','role_quiz',
      'brb','back','need_dps','need_heal','need_tank','need_ctrl','leaving'
    ]);
    const BATTLE_QUICK_KEYS = new Set([
      'burst_now','heal_first','revive_first','ctrl_boss','awesome','sorry_wait'
    ]);
    async function sendQuickEmoji(key){
      if(!WB.roomId || !QUICK_EMOJI[key]) return false;
      try{
        const myName = WB.lastSeenSnap?.players?.find(p => p.uid === window._gUserId)?.name || '玩家';
        const existing = WB.lastSeenSnap?.chat || [];
        const isDiscussion = DISCUSSION_KEYS.has(key) || TEAM_SETUP_KEYS.has(key) || BATTLE_QUICK_KEYS.has(key);
        const newChat = existing.concat([{
          uid: window._gUserId, name: myName, key: key,
          text: QUICK_EMOJI[key], ts: Date.now(),
          isDiscussion: isDiscussion,
          slotIdx: WB.mySlot,
        }]).slice(-20);
        await updateDoc(doc(_fbDb, 'worldBossRooms', WB.roomId), { chat: newChat });
        return true;
      }catch(_){ return false; }
    }

    async function broadcastCenterToast(text, color, dur, icon){
      if(!WB.roomId || !text) return false;
      try{
        const myName = WB.lastSeenSnap?.players?.find(p => p.uid === window._gUserId)?.name || '玩家';
        const existing = WB.lastSeenSnap?.chat || [];
        const newChat = existing.concat([{
          uid: window._gUserId, name: myName,
          _centerToast: true,
          text: String(text),
          color: color || '#ffe066',
          dur: typeof dur === 'number' ? dur : 3000,
          icon: icon || '',
          ts: Date.now(),
        }]).slice(-20);
        await updateDoc(doc(_fbDb, 'worldBossRooms', WB.roomId), { chat: newChat });
        return true;
      }catch(e){
        console.warn('[broadcastCenterToast] 失敗', e && e.message);
        return false;
      }
    }
    window._wbBroadcastCenterToast = broadcastCenterToast;

    // ★ v3.4.6 — Client 答完題後送答案給房主
    //   payload: { letter: 'A'~'D', isCorrect: bool, slotIdx, quizTs }
    //   房主收到後在自己機器上幫忙 advSubmitAnswer,讓全隊獎勵/懲罰正確觸發
    async function broadcastQuizAnswer(letter, isCorrect, quizTs){
      if(!WB.roomId) return false;
      try{
        const mySlot = WB.mySlot;
        const myName = WB.lastSeenSnap?.players?.find(p => p.uid === window._gUserId)?.name || '玩家';
        const existing = WB.lastSeenSnap?.chat || [];
        const newChat = existing.concat([{
          uid: window._gUserId, name: myName,
          _wbQuizAnswer: true,
          letter: String(letter || 'A'),
          isCorrect: !!isCorrect,
          slotIdx: mySlot,
          quizTs: quizTs || 0,
          ts: Date.now(),
        }]).slice(-20);
        await updateDoc(doc(_fbDb, 'worldBossRooms', WB.roomId), { chat: newChat });
        return true;
      }catch(e){
        console.warn('[broadcastQuizAnswer] 失敗', e && e.message);
        return false;
      }
    }
    window._wbBroadcastQuizAnswer = broadcastQuizAnswer;

    // ─── E. 訂閱 / 心跳 ─────────────────────────────────────────
    function _subscribeRoom(roomId){
      _unsubscribeRoom();
      WB.roomUnsub = onSnapshot(doc(_fbDb, 'worldBossRooms', roomId), (sn) => {
        // ════════════════════════════════════════════════════════════════
        // ★ v3.12.1(2026-05-30) — 修補學生回報:房主離開後其他人「房間已關閉」無限彈窗
        // ────────────────────────────────────────────────────────────────
        // 根因:onSnapshot 訂閱會持續監聽 firestore 房間文件變動。當 meta.status='ended'
        //       時,emit('onRoomClosed') 後只 return,沒 unsubscribe → 後續任何其他玩家
        //       離開、心跳更新、watchdog 標 offline 等變動,都會再次觸發 onSnapshot,
        //       而 status 永遠是 'ended',於是每次都 emit 一次 → 彈窗無限跳出,只能重開遊戲。
        // 修補:每個結束條件分支(doc 不存在、自己被踢、status=ended)都在 emit 前先
        //       _unsubscribeRoom(),確保訂閱關掉,事件絕對只觸發一次。
        // ════════════════════════════════════════════════════════════════
        if(!sn.exists()){
          _unsubscribeRoom();
          _emit('onRoomClosed', { reason: 'doc_deleted' });
          return;
        }
        const data = sn.data();
        const prev = WB.lastSeenSnap;
        WB.lastSeenSnap = data;
        const meIdx = (data.players || []).findIndex(p => p.uid === window._gUserId);
        if(meIdx < 0){
          _unsubscribeRoom();
          _emit('onRoomClosed', { reason: 'kicked_or_left' });
          return;
        }
        WB.mySlot = meIdx;
        WB.isHost = (data.meta && data.meta.hostUid === window._gUserId);
        if(data.meta && data.meta.status === 'ended'){
          _unsubscribeRoom();
          _emit('onRoomClosed', {
            reason: data.meta.endedReason || 'normal',
            winner: data.meta.winner || null,
            finalBattle: data.battle || null,
            elapsed: data.meta.elapsed || 0,
            contributions: data.meta.contributions || {}
          });
          return;
        }
        _emit('onRoomUpdate', data);
        // ★ FIX 20260517(x) — 偵測 player online 變化,emit 給 UI 處理代打接管
        try{
          const prevPlayers = prev?.players || [];
          const curPlayers = data.players || [];
          curPlayers.forEach(cp => {
            if(!cp || !cp.uid) return;
            // 房主自己的 online 變化不通知(自己永遠 online,通知只會打擾)
            if(cp.uid === window._gUserId) return;
            const pp = prevPlayers.find(p => p && p.uid === cp.uid);
            const prevOnline = pp ? (pp.online !== false) : true;  // 沒有 prev = 視為 online(避免初次進房誤判)
            const curOnline  = cp.online !== false;
            if(prevOnline !== curOnline){
              _emit('onPlayerOnlineChange', {
                uid: cp.uid,
                name: cp.name || '玩家',
                slotIdx: cp.slotIdx,
                online: curOnline,
              });
            }
          });
        }catch(e){
          console.warn('[WBNet] onPlayerOnlineChange 偵測例外', e && e.message);
        }
        if(data.battle){
          const battleChanged = !prev || !prev.battle || prev.battle.version !== data.battle.version;
          if(battleChanged){
            _emit('onBattleUpdate', data.battle);
            _checkMyTurn(data);
          }
        }
        // 聊天
        const newChat = data.chat || [];
        const prevLast = (prev?.chat?.length) ? prev.chat[prev.chat.length - 1] : null;
        const curLast  = newChat.length ? newChat[newChat.length - 1] : null;
        if(curLast && (!prevLast || prevLast.ts !== curLast.ts) && curLast.uid !== window._gUserId){
          _emit('onChatMessage', curLast);
        }
        // 房主處理 pendingAction
        if(WB.isHost && data.pendingAction){
          const pa = data.pendingAction;
          if(pa && pa.action && pa.action.type === '_client_stuck_resync'){
            console.log('[WBNet host] 收到 client 卡死求救,重發 sync',
              { from: pa.actorUid, reason: pa.action.reason, actor: pa.action.actor });
            // 清掉 pendingAction 避免重複觸發
            try{
              updateDoc(doc(_fbDb, 'worldBossRooms', WB.roomId), { pendingAction: null });
            }catch(_){}
            // ★ v3.13.57(2026-06-05)— BUG 1 根治:client 卡死求救時,若房主端 G.activeChar 卡在
            //   「已行動完(acted=true)的角色」,表示房主端 startTurn 漏跑/被擋住沒推進
            //   (這正是 client 卡死的根因 —— client 的 activeChar 推進完全靠房主廣播,
            //    房主沒推進就永遠等不到下一個 turn-start)。
            //   只重發 sync 沒用(重發的還是同一個卡住狀態,client 還是卡)→ 必須主動推進。
            //   安全守門:只在 activeChar.acted===true(已行動卻仍停著)才強制推進
            //   (沒行動的角色停著是正常等待玩家操作,絕不可亂推);先解 _gamePaused 再直接 startTurn。
            try{
              const _hG = (typeof window._wbGetG === 'function') ? window._wbGetG() : window.G;
              const _hAct = _hG && _hG.activeChar;
              if(window._wbConnectedHostMode && _hAct && _hAct.acted === true
                 && typeof window.startTurn === 'function'){
                console.warn('🆘 [client_stuck_resync] 房主端 activeChar(' + (_hAct.name||'?')
                  + ' ' + (_hAct.side||'?') + '-' + (_hAct.pos) + ') 已行動卻仍卡著,'
                  + '強制 startTurn 推進到下一個 actor(BUG 1 根治)');
                try{ window._gamePaused = false; }catch(_){}
                try{ if(typeof _resumeTimers === 'function') _resumeTimers(); }catch(_){}
                try{ window.startTurn(); }catch(_eStHost){ console.warn('[client_stuck_resync] 強制 startTurn 例外', _eStHost); }
              }
            }catch(_eForceAdv){ console.warn('[client_stuck_resync] 強制推進檢查例外', _eForceAdv); }
            // 立即重發 sync(若 host 已安裝 _wbHostSyncG)
            try{
              if(window._wbConnectedHostMode && typeof window._wbHostSyncG === 'function'){
                window._wbHostSyncG('client-stuck-resync');
              }
            }catch(_eRs){ console.warn('[client_stuck_resync] host sync 失敗', _eRs); }
            return;
          }
          const battle = data.battle;
          if(battle){
            // ★ v3.5.25(2026-05-22) — 「第二個玩家必卡死」根因修補
            //   ───────────────────────────────────────────────────────────────
            //   問題:房主原本用 `battle.turnOrder[battle.currentActorIdx]` 判定
            //         「當前該行動的玩家是誰」,但主程式 startTurn() / endAction()
            //         從不更新 G.currentActorIdx(grep 全檔案只有 3 處寫入,都是
            //         初始化:_GToWire 讀、_applyWireToG 寫、advStartBattle 設 0)。
            //         → battle.currentActorIdx 永遠是 0 → cur = turnOrder[0]
            //         = {side:'p2', pos:0}(BOSS) → 任何玩家送 pendingAction 都
            //         被判定為「BOSS 回合中卻收到玩家 action」→ 清掉重發 sync
            //         → 第二個玩家(slot 1+,走 firestore 路徑)永遠執行不到行動。
            //   為什麼第一個玩家(房主)不會卡:房主自己的 sendAction 走 line ~4033
            //         的 isHost 分支,直接呼叫 _wbExecPlayerAction(WB.mySlot, action),
            //         完全繞過 firestore → currentActorIdx 對房主沒影響。
            //   修法:改用「房主本機真實的 G.activeChar」判定誰該行動,
            //         pendingAction.actorSlot / actorUid 跟 G.activeChar 對齊就接受。
            //         判定路徑不依賴從未更新的 currentActorIdx 欄位。
            //   ───────────────────────────────────────────────────────────────
            const _G = (typeof window._wbGetG === 'function') ? window._wbGetG() : window.G;
            const _activeChar = _G && _G.activeChar;
            // 取本機 G.activeChar 的 side+pos 當權威來源
            const _activeIsP1 = !!(_activeChar && _activeChar.side === 'p1' && typeof _activeChar.pos === 'number');
            const _activeIsP2 = !!(_activeChar && _activeChar.side === 'p2');
            if(_activeIsP1){
              const _activeSlot = _activeChar.pos;
              const expectedPlayer = (data.players || []).find(p => p && p.slotIdx === _activeSlot);
              // 嚴格匹配:expectedPlayer 必須存在 + uid 匹配 + slot 匹配
              const _uidMatch  = !!(expectedPlayer && expectedPlayer.uid === pa.actorUid);
              const _slotMatch = (typeof pa.actorSlot !== 'number') || (pa.actorSlot === _activeSlot);
              if(_uidMatch && _slotMatch){
                if(expectedPlayer.online === false){
                  console.log('[WBNet v3.5.25] 槽 ' + _activeSlot + ' 玩家已離線,忽略遲到的 pendingAction(房主接管中)');
                  try{
                    updateDoc(doc(_fbDb, 'worldBossRooms', WB.roomId), { pendingAction: null });
                  }catch(_){}
                  return;
                }
                // ★ v6 Phase 5 — 連線房主用主程式 execSkill / execAtk / execBurst
                if(window._wbConnectedHostMode && typeof window._wbExecPlayerAction === 'function'){
                  try{
                    console.log('[WBNet v3.5.25] 執行 ' + (expectedPlayer.name||'?') + ' 的 action: ' + (pa.action && pa.action.type));
                    window._wbExecPlayerAction(_activeSlot, pa.action);
                    // 處理後清掉 pendingAction(避免下次 snapshot 再觸發)
                    try{
                      updateDoc(doc(_fbDb, 'worldBossRooms', WB.roomId), { pendingAction: null });
                    }catch(_){}
                  }catch(e){ console.error('[WBNet] _wbExecPlayerAction', e); }
                }else if(typeof window._wbHostExecuteAction === 'function'){
                  // 舊版自製戰鬥引擎 fallback
                  try{ window._wbHostExecuteAction(_activeSlot, pa.action); }catch(e){ console.error(e); }
                }
              } else {
                // 真正的 mismatch:uid/slot 不符當前 active actor
                //   可能原因:client 端基於舊的 sync 送出 action(時序競態),
                //            或玩家在「不是他的回合」誤點按鈕(理論上 UI 會擋,但保險檢查)
                // ★ v3.5.25 補強:特例 — G.activeChar.acted === true
                //   代表上一個 actor 剛動完、_pSetTimeout(startTurn,500) 還沒跑進去,
                //   這 500ms 內 client 若已基於「上一個 action 的 sync」算出下一輪是自己
                //   並送來合法的 action,房主這邊看到的 G.activeChar 還是「剛動完的舊 actor」
                //   → 會誤判 mismatch → 清掉合法的 pendingAction → 玩家永遠執行不到。
                //   保護:_activeChar.acted=true 時不要急著清 pendingAction,
                //         讓下一次 snapshot(startTurn 跑完後)再進來重判。
                if(_activeChar && _activeChar.acted === true){
                  console.log('[WBNet v3.5.25] mismatch 但上個 actor.acted=true(等 startTurn 推進中),'
                    + '保留 pendingAction 等下次 snapshot 重判 — actor=' + (_activeChar.name||'?')
                    + ' actorSlot=' + pa.actorSlot);
                  return;  // 不清 pendingAction、不重發 sync,讓自然推進
                }
                console.warn('[WBNet v3.5.25] pendingAction 不匹配 G.activeChar:',
                  '收到 actorUid=' + (pa.actorUid || '').slice(0, 8) +
                  ' slot=' + pa.actorSlot +
                  ',但本機 active slot=' + _activeSlot +
                  ' expectedUid=' + ((expectedPlayer && expectedPlayer.uid) || 'none').slice(0, 8));
                // 清掉 stale 的 pendingAction
                try{
                  updateDoc(doc(_fbDb, 'worldBossRooms', WB.roomId), { pendingAction: null });
                }catch(_){}
                // 重發 sync 給 client,讓他們看到正確的 active actor
                try{
                  if(window._wbConnectedHostMode && typeof window._wbHostSyncG === 'function'){
                    setTimeout(() => {
                      try{ window._wbHostSyncG('stale-action-resync'); }catch(_){}
                    }, 200);
                  }
                }catch(_){}
              }
            } else if(_activeIsP2){
              // 真正的 BOSS 回合卻收到玩家 action:可能是 client 端 sync 還沒同步到新回合
              console.warn('[WBNet v3.5.25] BOSS 回合中卻收到玩家 pendingAction(slot=' + pa.actorSlot + '),清掉並重發 sync');
              try{
                updateDoc(doc(_fbDb, 'worldBossRooms', WB.roomId), { pendingAction: null });
              }catch(_){}
              try{
                if(window._wbConnectedHostMode && typeof window._wbHostSyncG === 'function'){
                  setTimeout(() => {
                    try{ window._wbHostSyncG('boss-turn-stale-action'); }catch(_){}
                  }, 200);
                }
              }catch(_){}
            } else {
              // _activeChar 為 null / 既不是 p1 也不是 p2 → 異常狀態
              //   (例如戰鬥剛開始 G.activeChar 還沒設,或回合切換中的瞬間)
              //   不清 pendingAction(讓下個 snapshot 重試),避免合法 action 被誤清
              console.warn('[WBNet v3.5.25] 收到 pendingAction 但 G.activeChar 異常(null 或非 p1/p2),'
                + 'side=' + (_activeChar && _activeChar.side)
                + ' — 保留 pendingAction 等下次 snapshot 重試');
            }
          }
        }
      }, (err) => {
        console.error('[WBNet] 監聽錯誤', err);
        _emit('onError', { msg: '連線中斷:' + (err.code || err.message) });
      });
    }
    function _unsubscribeRoom(){
      if(WB.roomUnsub){ try{ WB.roomUnsub(); }catch(_){} WB.roomUnsub = null; }
      WB.lastSeenSnap = null;
    }
    function _checkMyTurn(data){
      if(!data || !data.battle) return;
      const order = data.battle.turnOrder || [];
      const idx = data.battle.currentActorIdx;
      if(idx < 0 || idx >= order.length) return;
      const actor = order[idx];
      if(actor.side !== 'p1') return;
      const player = data.players.find(p => p.slotIdx === actor.pos);
      if(player && player.uid === window._gUserId){
        // ★ FIX 20260517(x) — 自己被標離線時不觸發 onMyTurn(代打中,房主在操作)
        if(player.online === false){
          console.log('[WBNet] 自己被標離線中,跳過 onMyTurn 觸發(房主代打中)');
          return;
        }
        _emit('onMyTurn', { battle: data.battle, myActorPos: actor.pos, myHero: data.battle.p1[actor.pos] });
      }
    }
    // ★ v5.11.0(2026-08-03)— Firestore 配額熔斷 + 玩家友善錯誤訊息(玩家回報「打龍王一直選角失敗」根治)
    //   根因:免費方案配額耗盡(resource-exhausted)時,心跳(10s)/watchdog(5s) 的 transaction 內部重試
    //   形成玩家 console 可見的每秒 3 次 BatchGetDocuments 失敗風暴,且玩家看到英文錯誤碼「選角失敗:resource-exhausted」。
    //   修法三件事:①偵測到 resource-exhausted → 心跳/離線監測熔斷暫停 60 秒(停止風暴)
    //   ②玩家操作(開房/加入/選角/準備)失敗時改雙版友善訊息(鐵律1.232)
    //   ③心跳 10s→20s、watchdog 5s→15s、離線判定 20s→45s(常態讀寫量降約 60%,治本降載)
    function _wbQuotaTrip(e){
      try{
        const code = String((e && (e.code || e.message)) || '');
        if(code.indexOf('resource-exhausted') >= 0){
          WB._quotaCoolUntil = Date.now() + 60000;
          console.warn('[WB-Net v5.11.0] Firestore 流量受限(resource-exhausted)→ 心跳/離線監測暫停 60 秒,停止重試風暴');
          return true;
        }
      }catch(_){}
      return false;
    }
    function _wbQuotaCooling(){
      return !!(WB._quotaCoolUntil && Date.now() < WB._quotaCoolUntil);
    }
    function _wbQuotaMsg(){
      // 鐵律 1.232 — 玩家可見文字雙版(簡單風/精緻風)
      return (window._artStyle === 'premium')
        ? '⚠️ 伺服器目前流量繁忙,請稍候一分鐘再試一次。你的資料都很安全,不會遺失!'
        : '⚠️ 伺服器現在太忙了!休息一下,等一分鐘再來挑戰龍王吧!你的東西都不會不見!';
    }

    function _startHeartbeat(){
      _stopHeartbeat();
      // ★ FIX 20260517(x) — 心跳從 30 秒縮到 10 秒,讓 20 秒離線判定更準確
      //   (玩家斷網最多 10 秒內 lastSeen 不會更新,加上 10 秒緩衝就會被房主標離線)
      // ★ v5.11.0 — 心跳 10 秒 → 20 秒(降低 Firestore 讀寫量;離線判定同步放寬到 45 秒仍準確)
      WB.heartbeatTimer = setInterval(async () => {
        if(!WB.roomId || !window._gUserId) return;
        if(_wbQuotaCooling()) return;  // ★ v5.11.0 — 配額熔斷中:暫停心跳,不再加重風暴
        // ★ FIX 20260517(y) — startBattle 進行中時跳過這次心跳,
        //   避免 transaction 撞 startBattle 的 commit 噴 failed-precondition
        if(WB._startingBattle) return;
        try{
          await runTransaction(_fbDb, async (tx) => {
            const ref = doc(_fbDb, 'worldBossRooms', WB.roomId);
            const sn = await tx.get(ref);
            if(!sn.exists()) return;
            const players = sn.data().players || [];
            const me = players.find(p => p.uid === window._gUserId);
            if(!me) return;
            me.lastSeen = Date.now(); me.online = true;
            tx.update(ref, { players: players });
          });
        }catch(_eHb){
          // ★ v5.11.0 — 心跳失敗若為配額耗盡 → 觸發熔斷(其餘錯誤維持靜默)
          _wbQuotaTrip(_eHb);
        }
      }, 20000);  // ★ v5.11.0 — 10000 → 20000
    }
    function _stopHeartbeat(){
      if(WB.heartbeatTimer){ clearInterval(WB.heartbeatTimer); WB.heartbeatTimer = null; }
    }

    const OFFLINE_THRESHOLD_MS = 45000;  // ★ v5.11.0 — 45 秒沒心跳 → 離線(舊 20000;心跳改 20s 故放寬,仍有 25 秒緩衝)
    const OFFLINE_CHECK_INTERVAL_MS = 15000;  // ★ v5.11.0 — 每 15 秒檢查一次(舊 5000;降低 Firestore 讀取量)

    function _startOfflineWatchdog(){
      _stopOfflineWatchdog();
      WB.offlineWatchTimer = setInterval(async () => {
        if(!WB.roomId || !WB.isHost) return;
        if(_wbQuotaCooling()) return;  // ★ v5.11.0 — 配額熔斷中:暫停離線監測,不再加重風暴
        // ★ FIX 20260517(y) — startBattle 進行中時跳過這次 watchdog 掃描,
        //   避免 transaction 撞 startBattle 的 commit 噴 failed-precondition
        if(WB._startingBattle) return;
        try{
          await runTransaction(_fbDb, async (tx) => {
            const ref = doc(_fbDb, 'worldBossRooms', WB.roomId);
            const sn = await tx.get(ref);
            if(!sn.exists()) return;
            const data = sn.data();
            const status = data.meta && data.meta.status;
            // 結算後停止監測
            if(status === 'ended') return;
            const now = Date.now();
            const players = data.players || [];
            let changed = false;
            players.forEach(p => {
              if(!p || !p.uid) return;
              // 房主自己永遠 online
              if(p.uid === window._gUserId){
                if(!p.online){ p.online = true; changed = true; }
                return;
              }
              const last = p.lastSeen || 0;
              const elapsed = now - last;
              const shouldBeOffline = elapsed > OFFLINE_THRESHOLD_MS;
              if(shouldBeOffline && p.online !== false){
                p.online = false;
                changed = true;
                console.log('[WB-Watchdog] 玩家離線:' + (p.name || p.uid.slice(0,8)) + ' (超過 ' + Math.floor(elapsed/1000) + ' 秒)');
              }else if(!shouldBeOffline && p.online === false){
                p.online = true;
                changed = true;
                console.log('[WB-Watchdog] 玩家重連:' + (p.name || p.uid.slice(0,8)));
              }
            });
            if(changed){
              tx.update(ref, { players: players });
            }
          });
        }catch(e){
          console.warn('[WB-Watchdog] 例外', e && e.message);
          _wbQuotaTrip(e);  // ★ v5.11.0 — 配額耗盡 → 熔斷 60 秒
        }
      }, OFFLINE_CHECK_INTERVAL_MS);
    }
    function _stopOfflineWatchdog(){
      if(WB.offlineWatchTimer){ clearInterval(WB.offlineWatchTimer); WB.offlineWatchTimer = null; }
    }
    // 暴露給外部(讓 _wbNet 入口呼叫)
    WB._startOfflineWatchdog = _startOfflineWatchdog;
    WB._stopOfflineWatchdog  = _stopOfflineWatchdog;

    // ══════════════════════════════════════════════════════════════════
    // ★ v3.10.0(2026-05-26) — 每小時主動清除空房(機會式清掃)
    // ──────────────────────────────────────────────────────────────────
    // 原問題:v3.5.6 只在「列表時客戶端過濾」幽靈房,但 Firestore 上仍堆積
    //         status=waiting 但 players=[] 的死檔,長期下來影響查詢效能 +
    //         佔用儲存空間。老師需求:每小時固定清除一次空房間。
    //
    // 設計:
    //   1. 用 stats/global 文件加一個 wbRoomSweepAt 時間戳當「最近清掃」記錄
    //   2. 任何人呼叫 listPublicRooms 時順手檢查:距上次 ≥ 60 分鐘才執行
    //      → 不需要 Cloud Function,任何玩家進公開房列表都會觸發
    //   3. 清掃條件(同時滿足任一即關閉):
    //      (a) players 為空陣列 / 不存在
    //      (b) 所有玩家 online=false 且 lastSeen 超過 60 分鐘
    //      (c) meta.status === 'fighting' 但 createdAt 距今 > 3 小時(戰鬥不可能跑 3 小時)
    //   4. 一次最多掃 50 間(limit),避免單次清掃太久卡 UI
    //   5. 把符合的房間 meta.status 改 'ended' + endedReason='auto_sweep'
    //      → 下次 listPublicRooms 的 where 條件就會自動排除
    //   6. 並發保護:用 transaction 寫 wbRoomSweepAt,失敗就跳過
    //
    // 觸發點:listPublicRooms 進入時 fire-and-forget(不阻塞回傳)
    // ══════════════════════════════════════════════════════════════════
    const _WB_SWEEP_INTERVAL_MS = 60 * 60 * 1000;       // 60 分鐘清一次
    const _WB_SWEEP_GHOST_MS    = 60 * 60 * 1000;       // 玩家離線 > 60 分鐘 → 視為幽靈房
    const _WB_SWEEP_FIGHTING_MS = 3 * 60 * 60 * 1000;   // 戰鬥中 > 3 小時 → 強制關閉
    const _WB_SWEEP_BATCH       = 50;                   // 單次最多掃 50 間

    let _wbSweepInProgress = false;  // 同一 client 並發保護(transaction 已防多 client)

    async function _sweepEmptyRoomsIfDue(){
      if(_wbSweepInProgress) return { skipped:true, reason:'in_progress' };
      _wbSweepInProgress = true;
      try{
        // (a) 用 transaction 搶 sweep 鎖:檢查 stats/global.wbRoomSweepAt
        const _statsRef = doc(_fbDb, 'stats', 'global');
        let _shouldSweep = false;
        try{
          await runTransaction(_fbDb, async (tx) => {
            const _snap = await tx.get(_statsRef);
            const _data = _snap.exists() ? (_snap.data() || {}) : {};
            const _lastSweep = (typeof _data.wbRoomSweepAt === 'number') ? _data.wbRoomSweepAt : 0;
            const _now = Date.now();
            if((_now - _lastSweep) < _WB_SWEEP_INTERVAL_MS){
              _shouldSweep = false;
              return;
            }
            // 立刻寫入「我搶到鎖了」(其他 client 60 分鐘內看到這個會跳過)
            tx.set(_statsRef, { wbRoomSweepAt: _now }, { merge: true });
            _shouldSweep = true;
          });
        }catch(_eLock){
          console.warn('[WB-Sweep v3.10.0] 搶鎖 transaction 失敗,跳過本次', _eLock);
          return { skipped:true, reason:'lock_failed' };
        }

        if(!_shouldSweep){
          return { skipped:true, reason:'not_due' };
        }

        // (b) 拿 waiting/picking/fighting 的房間清單(limit 50)
        //     注意:用 query 過濾 status,避免拉全部 worldBossRooms
        const _qSweep = query(
          collection(_fbDb, 'worldBossRooms'),
          where('meta.status', 'in', ['waiting', 'picking', 'fighting']),
          orderBy('meta.createdAt', 'asc'),  // 從最舊的開始掃(最可能是幽靈房)
          limit(_WB_SWEEP_BATCH)
        );
        const _snap = await getDocs(_qSweep);
        const _now2 = Date.now();
        let _killedCount = 0;
        const _killTasks = [];

        _snap.forEach(s => {
          const d = s.data() || {};
          const _players = Array.isArray(d.players) ? d.players : [];
          const _meta = d.meta || {};
          const _status = _meta.status;
          let _shouldKill = false;
          let _reason = '';

          // (a) 空房 — 直接關
          if(_players.length === 0){
            _shouldKill = true;
            _reason = 'empty_players';
          }
          // (b) 全部離線 > 60 分鐘
          else if(_status === 'waiting' || _status === 'picking'){
            const _anyOnline = _players.some(p => p && (p.online !== false));
            if(!_anyOnline){
              const _maxLastSeen = _players.reduce((m, p) => {
                const _ls = (p && typeof p.lastSeen === 'number') ? p.lastSeen : 0;
                return _ls > m ? _ls : m;
              }, 0);
              if(_maxLastSeen > 0 && (_now2 - _maxLastSeen) > _WB_SWEEP_GHOST_MS){
                _shouldKill = true;
                _reason = 'all_offline_60min';
              }
            }
          }
          // (c) fighting 狀態但跑超過 3 小時 → 強制關閉(極端 corner case)
          else if(_status === 'fighting'){
            const _createdAt = _meta.createdAt || 0;
            if(_createdAt > 0 && (_now2 - _createdAt) > _WB_SWEEP_FIGHTING_MS){
              _shouldKill = true;
              _reason = 'fighting_3hr_timeout';
            }
          }

          if(_shouldKill){
            _killTasks.push(
              updateDoc(doc(_fbDb, 'worldBossRooms', s.id), {
                'meta.status': 'ended',
                'meta.endedReason': 'auto_sweep_' + _reason,
                'meta.endedAt': _now2,
              }).then(() => { _killedCount++; })
                .catch(_eK => { console.warn('[WB-Sweep] 關閉房間失敗', s.id, _eK && _eK.code); })
            );
          }
        });

        if(_killTasks.length > 0){
          await Promise.all(_killTasks);
        }

        console.log('[WB-Sweep v3.10.0] ✅ 清掃完成 — 掃描 '
          + _snap.size + ' 間, 關閉 ' + _killedCount + ' 間幽靈房');
        return { swept:true, scanned: _snap.size, killed: _killedCount };
      }catch(e){
        console.warn('[WB-Sweep v3.10.0] 清掃例外', e && e.message);
        return { skipped:true, reason:'exception', err: e && e.message };
      }finally{
        _wbSweepInProgress = false;
      }
    }
    // 暴露給後台呼叫(手動觸發清掃用)
    WB._sweepEmptyRoomsIfDue = _sweepEmptyRoomsIfDue;
    window._wbForceSweepNow = async function(){
      // 後台強制立即清掃:跳過 60 分鐘鎖
      try{
        // 把 wbRoomSweepAt 強制清成 0,讓下次 _sweepEmptyRoomsIfDue 一定會跑
        const _statsRef = doc(_fbDb, 'stats', 'global');
        await setDoc(_statsRef, { wbRoomSweepAt: 0 }, { merge: true });
      }catch(_){}
      return await _sweepEmptyRoomsIfDue();
    };

    // ★ v3.12.14(2026-05-31)— 一鍵清空所有公開房間(維修模式用)
    //   不管房間狀態(waiting/picking/fighting)、不管條件,
    //   只要還沒 'ended' 就強制標 'ended' + endedReason='admin_cleanup'
    //   用途:
    //     ① 維修模式啟動時自動呼叫,避免維修期間有殭屍房在列表上
    //     ② 老師後台有「立即清空所有公開房」按鈕,異常時手動觸發
    window._wbAdminCleanupAllPublicRooms = async function(){
      console.log('[WB-Cleanup v3.12.14] 開始清空所有公開房間...');
      try{
        // 查所有非 ended 的房間(無論 public/private 都清,維修就是要全清)
        const _q = query(
          collection(_fbDb, 'worldBossRooms'),
          where('meta.status', 'in', ['waiting', 'picking', 'fighting'])
        );
        const _snap = await getDocs(_q);
        const _now = Date.now();
        const _tasks = [];
        let _count = 0;
        _snap.forEach(s => {
          _count++;
          _tasks.push(
            updateDoc(doc(_fbDb, 'worldBossRooms', s.id), {
              'meta.status': 'ended',
              'meta.endedReason': 'admin_cleanup',
              'meta.endedAt': _now,
            }).catch(e => console.warn('[WB-Cleanup] 關閉房間失敗', s.id, e && e.code))
          );
        });
        if(_tasks.length > 0){
          await Promise.all(_tasks);
        }
        console.log('[WB-Cleanup v3.12.14] ✅ 已清空 ' + _count + ' 間公開房間');
        return { ok:true, count: _count };
      }catch(e){
        console.error('[WB-Cleanup v3.12.14] 失敗', e);
        return { ok:false, error: e && e.message };
      }
    };

    // ★ v3.12.14(2026-05-31)— 標記單一房間為 ended(給 world-boss.js 結算後呼叫)
    //   world-boss.js 是 IIFE,沒有 import 的 updateDoc/doc 可用,
    //   所以在 index.html 暴露這支 API 讓它呼叫。
    //   冪等:寫多次無害,失敗也不擋戰鬥流程。
    window._wbMarkRoomEnded = async function(roomId, reason){
      if(!roomId) return { ok:false, reason:'no_roomId' };
      try{
        await updateDoc(doc(_fbDb, 'worldBossRooms', roomId), {
          'meta.status': 'ended',
          'meta.endedReason': reason || 'manual_mark',
          'meta.endedAt': Date.now(),
        });
        console.log('[WB v3.12.14] 已標記房間 ended:', roomId, '(' + (reason || 'manual') + ')');
        return { ok:true };
      }catch(e){
        // 失敗常見原因:client 沒寫權限(房主才有),沒關係房主端會處理
        console.log('[WB v3.12.14] 標記房間 ended 失敗(常見:client 無權限,房主會處理)', roomId, e && e.code);
        return { ok:false, error: e && e.message };
      }
    };


    // ─── G-1. 列出目前公開且還在 waiting/picking 階段的房間 ─────────
    //   開戰後 status 變 fighting → 自動從列表消失(query 過濾)
    //   依 createdAt 降冪、最多 20 筆
    async function listPublicRooms(){
      // ★ v3.10.0 — 機會式清掃:fire-and-forget,不阻塞回傳
      //   每個進公開房列表的玩家都會檢查「距上次清掃是否 ≥ 60 分鐘」,
      //   若是則順手清一波;若否則 transaction 內直接 return,毫秒級開銷
      try{ _sweepEmptyRoomsIfDue().catch(()=>{}); }catch(_){}
      try{
        // ★ Firestore 上 meta.status 是巢狀欄位,可用點字串 query
        const q = query(
          collection(_fbDb, 'worldBossRooms'),
          where('meta.visibility', '==', 'public'),
          where('meta.status', 'in', ['waiting', 'picking']),
          orderBy('meta.createdAt', 'desc'),
          limit(20)
        );
        const snap = await getDocs(q);
        const rooms = [];
        // ★ v3.5.6 — 幽靈房防呆:對舊資料(沒走過 v3.5.6 修法的房間)做客戶端過濾
        //   觸發情境:房主直接關瀏覽器/斷網,leaveRoom 沒跑 → 房間 status 還是 waiting,
        //            但 players 已空 / 全部玩家斷線很久 → 顯示出來會誤導其他玩家加入空房。
        //   過濾規則:
        //     (a) players 為空 → 直接濾掉(任何狀況都不該顯示空房)
        //     (b) 所有玩家都 online=false 且最新 lastSeen 距今 > 10 分鐘 → 視為幽靈房
        const _GHOST_THRESHOLD_MS = 10 * 60 * 1000;  // 10 分鐘
        const _now = Date.now();
        snap.forEach(s => {
          const d = s.data() || {};
          const _players = Array.isArray(d.players) ? d.players : [];
          // (a) 空房直接濾
          if(_players.length === 0){
            console.log('[WBNet v3.5.6] 過濾掉空房:', s.id);
            return;
          }
          // (b) 全部離線且 lastSeen 過久 → 幽靈房濾掉
          //     未帶 online 欄位的舊資料當 true(向下相容,不誤殺)
          const _anyOnline = _players.some(p => p && (p.online !== false));
          if(!_anyOnline){
            const _maxLastSeen = _players.reduce((m, p) => {
              const _ls = (p && typeof p.lastSeen === 'number') ? p.lastSeen : 0;
              return _ls > m ? _ls : m;
            }, 0);
            if(_maxLastSeen > 0 && (_now - _maxLastSeen) > _GHOST_THRESHOLD_MS){
              console.log('[WBNet v3.5.6] 過濾掉幽靈房(全部離線>10分):', s.id);
              return;
            }
          }
          rooms.push({
            roomId: s.id,
            hostUid: d.meta?.hostUid,
            hostName: d.meta?.hostName || '房主',
            bossId: d.meta?.bossId,
            status: d.meta?.status,
            createdAt: d.meta?.createdAt || 0,
            players: _players.map(p => ({
              uid: p.uid, name: p.name, slotIdx: p.slotIdx,
              heroName: p.heroName, element: p.element,
              ready: !!p.ready, online: !!p.online,
            }))
          });
        });
        return rooms;
      }catch(e){
        console.error('[WBNet] listPublicRooms 失敗', e);
        // ★ 如果 query 報「需要建立 index」, 給友善訊息
        if(e && e.code === 'failed-precondition'){
          _emit('onError', { msg: '公開房列表需要建立 Firestore 索引,請聯絡管理員' });
        }else{
          _emit('onError', { msg: '讀取公開房失敗:' + (e.code || e.message) });
        }
        return [];
      }
    }

    async function sendInvite(toUid, toName){
      if(!_requireLogin()) return false;
      if(!WB.roomId || !WB.isHost){
        _emit('onError', { msg: '只有房主可以邀請好友' });
        return false;
      }
      if(!toUid || toUid === window._gUserId){
        _emit('onError', { msg: '邀請對象無效' });
        return false;
      }
      const myName = (window._fbUser && (window._fbUser.displayName || window._fbUser.email)) ||
                     localStorage.getItem('lxps_nickname_' + window._gUserId) || '玩家';
      const now = Date.now();
      const TTL_MS = 30 * 1000;  // ★ 30 秒過期
      try{
        // ★ 1. 把同一房主→同一好友的舊 pending 邀請全標成 cancelled
        //    (用 batched updates;失敗也不阻塞,後續可用 timestamp 比對)
        try{
          const dupQ = query(
            collection(_fbDb, 'worldBossInvites'),
            where('fromUid', '==', window._gUserId),
            where('toUid', '==', toUid),
            where('status', '==', 'pending')
          );
          const dupSnap = await getDocs(dupQ);
          const cancelTasks = [];
          dupSnap.forEach(s => {
            // 不論是否過期,一律標 cancelled(讓對方端訂閱即刻清除)
            cancelTasks.push(
              updateDoc(doc(_fbDb, 'worldBossInvites', s.id), {
                status: 'cancelled',
                respondedAt: now,
                cancelledBy: 'resend'
              }).catch(_ => {}) // 個別失敗忽略
            );
          });
          if(cancelTasks.length){
            await Promise.all(cancelTasks);
          }
        }catch(cancelErr){
          // 取消舊的失敗不算致命 — 繼續寫新的
          console.warn('[WBNet] sendInvite 取消舊邀請失敗(可忽略)', cancelErr);
        }

        try{
          const inRoomUids = new Set();
          const _curPlayers = (WB.lastSeenSnap && Array.isArray(WB.lastSeenSnap.players))
                              ? WB.lastSeenSnap.players : [];
          _curPlayers.forEach(p => { if(p && p.uid) inRoomUids.add(p.uid); });
          // 只在好友「不在房內」時清舊紀錄(避免取消正在進房中的 accepted)
          if(!inRoomUids.has(toUid)){
            const acceptedQ = query(
              collection(_fbDb, 'worldBossInvites'),
              where('fromUid', '==', window._gUserId),
              where('toUid', '==', toUid),
              where('status', '==', 'accepted')
            );
            const accSnap = await getDocs(acceptedQ);
            const staleTasks = [];
            accSnap.forEach(s => {
              staleTasks.push(
                updateDoc(doc(_fbDb, 'worldBossInvites', s.id), {
                  status: 'stale',  // 新狀態:歷史已接受但好友已不在房內
                  staledAt: now,
                  staledBy: 'resend'
                }).catch(_ => {})
              );
            });
            if(staleTasks.length){
              await Promise.all(staleTasks);
              console.log('[WBNet] sendInvite 已將 ' + staleTasks.length + ' 筆歷史 accepted 邀請標為 stale');
            }
          }
        }catch(staleErr){
          console.warn('[WBNet] sendInvite 清歷史 accepted 失敗(可忽略)', staleErr);
        }

        // ★ 2. 寫入新邀請
        const inviteId = 'inv_' + now + '_' + Math.random().toString(36).slice(2, 8);
        const payload = {
          fromUid: window._gUserId,
          fromName: myName,
          toUid: toUid,
          toName: toName || '',
          roomId: WB.roomId,
          bossId: WB.lastSeenSnap?.meta?.bossId || ((typeof window._wbGetCurrentBossId === 'function') ? window._wbGetCurrentBossId() : 'vesuvius_fire_dragon'),  // ★ v3.14.20
          createdAt: now,
          expiresAt: now + TTL_MS,
          status: 'pending'
        };
        await setDoc(doc(_fbDb, 'worldBossInvites', inviteId), payload);
        return inviteId;
      }catch(e){
        console.error('[WBNet] sendInvite 失敗', e);
        _emit('onError', { msg: '邀請失敗:' + (e.code || e.message) });
        return false;
      }
    }

    // ─── G-3. 訂閱「我」收到的待處理邀請 ────────────────────────
    //   組件:左側邊邀請通知 UI 用,onSnapshot 即時跟進
    //   回傳 unsubscribe fn
    function subscribeInvites(onUpdate){
      if(!window._gUserId) return null;
      try{
        const q = query(
          collection(_fbDb, 'worldBossInvites'),
          where('toUid', '==', window._gUserId),
          where('status', '==', 'pending')
        );
        const unsub = onSnapshot(q, (snap) => {
          const now = Date.now();
          const list = [];
          snap.forEach(s => {
            const d = s.data() || {};
            // 客戶端再過濾過期 (server 可能晚一步寫 'expired')
            if(d.expiresAt && d.expiresAt > now){
              list.push({
                inviteId: s.id,
                fromUid: d.fromUid,
                fromName: d.fromName || '玩家',
                roomId: d.roomId,
                bossId: d.bossId,
                createdAt: d.createdAt || 0,
                expiresAt: d.expiresAt
              });
            }
          });
          // 由舊到新排(舊的先過期、先顯示先處理)
          list.sort((a, b) => a.createdAt - b.createdAt);
          if(typeof onUpdate === 'function') onUpdate(list);
        }, (err) => {
          console.warn('[WBNet] subscribeInvites 訂閱失敗', err);
          if(typeof onUpdate === 'function') onUpdate([]);
        });
        return unsub;
      }catch(e){
        console.error('[WBNet] subscribeInvites 例外', e);
        return null;
      }
    }

    // ─── G-4. 回應邀請(接受/拒絕/忙碌) ─────────────────────────
    //   action: 'accepted' | 'declined' | 'busy' | 'expired'
    //   被邀請者按受/拒/沒按超時 → 寫對應狀態,邀請者端會收到 onSnapshot 更新
    async function respondInvite(inviteId, action){
      if(!inviteId) return false;
      const validActions = ['accepted', 'declined', 'busy', 'expired'];
      if(!validActions.includes(action)) return false;
      try{
        await updateDoc(doc(_fbDb, 'worldBossInvites', inviteId), {
          status: action,
          respondedAt: Date.now()
        });
        return true;
      }catch(e){
        console.warn('[WBNet] respondInvite 失敗', e);
        return false;
      }
    }

    // ─── G-5. 訂閱「我」發出的邀請(看對方回應) ──────────────
    //   房主用:被邀請者按受/拒/超時時即時得知
    function subscribeMyOutgoingInvites(onUpdate){
      if(!window._gUserId) return null;
      try{
        const q = query(
          collection(_fbDb, 'worldBossInvites'),
          where('fromUid', '==', window._gUserId)
        );
        const unsub = onSnapshot(q, (snap) => {
          const now = Date.now();
          const list = [];
          snap.forEach(s => {
            const d = s.data() || {};
            list.push({
              inviteId: s.id,
              toUid: d.toUid,
              toName: d.toName || '',
              roomId: d.roomId,
              status: d.status || 'pending',
              createdAt: d.createdAt || 0,
              expiresAt: d.expiresAt || 0,
              respondedAt: d.respondedAt || 0,
              expired: !!(d.expiresAt && d.expiresAt < now && d.status === 'pending')
            });
          });
          list.sort((a, b) => b.createdAt - a.createdAt);
          if(typeof onUpdate === 'function') onUpdate(list);
        }, (err) => {
          console.warn('[WBNet] subscribeMyOutgoingInvites 失敗', err);
        });
        return unsub;
      }catch(e){
        console.error('[WBNet] subscribeMyOutgoingInvites 例外', e);
        return null;
      }
    }

    async function _wbNetUpdateContribs(contribs){
      if(!WB.isHost || !WB.roomId) return false;
      // ★ v3.7.6 — 加 3 次重試(對齊 hostPushBattleState 的 v3.5.8 重試機制)
      //   老師 2026/5/25 回報 BUG:「本場總傷 2p~4p 看不到」
      //   根因:_wbNetUpdateContribs 寫雲端失敗(常見 failed-precondition)時不重試,
      //         contributions 永遠寫不進 meta → client onRoomUpdate 收不到 → 看不到傷害。
      //   修法:跟 hostPushBattleState 一樣 — 最多 3 次重試,失敗就放棄等下次 sync。
      const _maxAttempts = 3;
      for(let _i = 0; _i < _maxAttempts; _i++){
        try{
          // deep clean → Firestore 不接受 undefined
          const cleaned = _wbDeepCleanUndefined(contribs);
          await updateDoc(doc(_fbDb, 'worldBossRooms', WB.roomId), {
            'meta.contributions': cleaned
          });
          if(_i > 0){
            console.info('[WBNet] _wbNetUpdateContribs 第 ' + (_i+1) + ' 次重試成功');
          }
          return true;
        }catch(e){
          if(_i === _maxAttempts - 1){
            // 最後一次,放棄
            return false;
          }
          // 指數退避 + 隨機抖動
          const _delay = 100 * Math.pow(2, _i) + Math.floor(Math.random() * 100);
          await new Promise(function(r){ setTimeout(r, _delay); });
        }
      }
      return false;
    }
    window._wbNetUpdateContribs = _wbNetUpdateContribs;

    // ─── F. 對外 API ────────────────────────────────────────────
    window._wbNet = {
      createRoom, joinRoom, leaveRoom,
      setMyHero, toggleReady, startBattle,
      sendAction, hostPushBattleState, addContribution,
      sendQuickEmoji, QUICK_EMOJI,
      // ★ FIX 20260517(m) — 公開房 + 邀請好友
      listPublicRooms,
      sendInvite,
      subscribeInvites,
      subscribeMyOutgoingInvites,
      respondInvite,
      on: (e, cb) => { if(e in WB.listeners) WB.listeners[e] = cb; },
      off: (e) => { if(e in WB.listeners) WB.listeners[e] = null; },
      getRoomId:   () => WB.roomId,
      isHost:      () => WB.isHost,
      getMySlot:   () => WB.mySlot,
      getSnapshot: () => WB.lastSeenSnap,
      getContributions: () => WB.contributions,
    };
    window.addEventListener('beforeunload', () => { _stopHeartbeat(); });
    console.log('[WBNet] ✅ 世界 BOSS 連線層 v0.1 已載入');

    // ══════════════════════════════════════════════════════════════════
    // ★ v3.2 — 世界 BOSS 休戰期控制系統(管理員專用)
    // ──────────────────────────────────────────────────────────────────
    // 用 Firestore 文件 worldBossControl/main 同步全伺服器的休戰狀態:
    //   - ceasefire (bool): true = 休戰期(玩家無法開戰),false = 開放挑戰
    //   - nextOpenTime (str): 預告下次開戰時間 / 注記 (例: 今晚 22:00)
    //   - message (str): 額外公告訊息(可空)
    //   - updatedAt (ms): 最後更新時間戳
    //   - updatedBy (str): 最後操作管理員 email
    //
    // 即時訂閱:玩家進入遊戲後 onSnapshot 訂閱,狀態同步到 window._wbCeasefireState
    // 預設值:ceasefire=true (新部署預設休戰,避免玩家衝進去打半成品),nextOpenTime=''
    //
    // ⚠ 安全注意:這個文件不限制誰能寫(Firestore rules 自己處理),前端只用
    //    _isAdminUser() 隱藏 UI;真正權限保護要靠 Firestore 規則。
    // ══════════════════════════════════════════════════════════════════
    (function setupWorldBossControl(){
      let _ctrlUnsub = null;
      const CTRL_DOC = () => doc(_fbDb, 'worldBossControl', 'main');
      // 預設狀態
      window._wbCeasefireState = {
        ceasefire: true,            // 預設休戰
        nextOpenTime: '',
        message: '',
        updatedAt: 0,
        updatedBy: '',
        _loaded: false,             // 是否已從雲端載入過 (尚未載入時的 fallback)
      };
      // 即時訂閱(Firestore 在線/離線都會自動補上最新)
      function subscribeWbControl(){
        if(_ctrlUnsub) return;
        try{
          _ctrlUnsub = onSnapshot(CTRL_DOC(), (snap) => {
            if(snap.exists()){
              const d = snap.data() || {};
              window._wbCeasefireState = {
                ceasefire: (typeof d.ceasefire === 'boolean') ? d.ceasefire : true,
                nextOpenTime: d.nextOpenTime || '',
                message: d.message || '',
                updatedAt: d.updatedAt || 0,
                updatedBy: d.updatedBy || '',
                _loaded: true,
              };
            }else{
              // 文件不存在 → 維持預設休戰 + 標記已載入
              window._wbCeasefireState._loaded = true;
            }
            // 廣播事件給 UI 即時刷新
            try{ document.dispatchEvent(new CustomEvent('wbCeasefireStateChanged')); }catch(_){}
            console.log('[WBNet-Control] 狀態同步:', window._wbCeasefireState);
          }, (err) => {
            console.warn('[WBNet-Control] 訂閱失敗,使用預設休戰狀態:', err);
            window._wbCeasefireState._loaded = true;
            try{ document.dispatchEvent(new CustomEvent('wbCeasefireStateChanged')); }catch(_){}
          });
        }catch(e){
          console.warn('[WBNet-Control] subscribeWbControl 例外:', e);
          window._wbCeasefireState._loaded = true;
        }
      }
      // 管理員寫入新狀態
      async function setWbControl(newState){
        if(!(typeof window._isAdminUser === 'function' && window._isAdminUser())){
          throw new Error('非管理員,無權限修改世界 BOSS 控制狀態');
        }
        const payload = {
          ceasefire: !!newState.ceasefire,
          nextOpenTime: String(newState.nextOpenTime || ''),
          message: String(newState.message || ''),
          updatedAt: Date.now(),
          updatedBy: (window._fbUser && window._fbUser.email) || 'admin',
        };
        await setDoc(CTRL_DOC(), payload, { merge: true });
        return payload;
      }
      // 立即一次性讀取(管理員測試版/訪客模式時)
      async function fetchWbControl(){
        try{
          const snap = await getDoc(CTRL_DOC());
          if(snap.exists()){
            const d = snap.data() || {};
            window._wbCeasefireState = {
              ceasefire: (typeof d.ceasefire === 'boolean') ? d.ceasefire : true,
              nextOpenTime: d.nextOpenTime || '',
              message: d.message || '',
              updatedAt: d.updatedAt || 0,
              updatedBy: d.updatedBy || '',
              _loaded: true,
            };
          }else{
            window._wbCeasefireState._loaded = true;
          }
        }catch(e){
          console.warn('[WBNet-Control] fetchWbControl 失敗:', e);
          window._wbCeasefireState._loaded = true;
        }
      }

      // 暴露 API(world-boss.js / world-boss-ui.html 都能呼叫)
      window._wbControl = {
        subscribe: subscribeWbControl,
        set: setWbControl,
        fetch: fetchWbControl,
        get: () => window._wbCeasefireState,
      };
      // 啟動時自動訂閱(Firestore SDK 會自動處理離線/網路狀態)
      try{ subscribeWbControl(); }catch(_){}
      console.log('[WBNet-Control] ✅ 休戰期控制模組已掛載');
    })();

    // ══════════════════════════════════════════════════════════════════
    // ★ FIX 20260518 — 世界 BOSS 殘血同步模組(_wbHpSync)
    //
    // 設計:
    // - 儲存位置:stats/global.worldBossHp.<bossId> (number, 預設 = maxHp)
    // - 全伺服器共享:任何玩家造成的傷害都會即時扣到雲端 HP,所有人看到的進度一致
    // - 即時訂閱:onSnapshot 把雲端 HP 同步到 window._cachedGlobalStats.worldBossHp,
    //            world-boss.js 的 _wbSyncStageHpBar 會自動讀這份快取重繪 HP 條
    // - 自動休戰:若有人擊敗 BOSS(HP 扣到 0),寫入端會順帶把 worldBossControl/main
    //            的 ceasefire 設成 true(透過 _wbControl.set,需要管理員權限)
    //   ⚠ 受 Firestore rules 限制 — 一般玩家可能寫不進 worldBossControl,
    //     此情況下會留下 HP=0 但 ceasefire 仍 false,改由管理員手動切休戰即可
    // ══════════════════════════════════════════════════════════════════
    (function setupWorldBossHpSync(){
      let _hpUnsub = null;
      const STATS_DOC = () => doc(_fbDb, 'stats', 'global');

      // 即時訂閱:把雲端 worldBossHp 同步到 window._cachedGlobalStats
      function subscribeWbHp(){
        if(_hpUnsub) return;
        try{
          _hpUnsub = onSnapshot(STATS_DOC(), (snap) => {
            try{
              const d = snap.exists() ? (snap.data() || {}) : {};
              if(!window._cachedGlobalStats) window._cachedGlobalStats = {};
              window._cachedGlobalStats.worldBossHp = d.worldBossHp || {};
              // ★ FIX 20260519(v12) — 順手快取排行榜
              window._cachedGlobalStats.worldBossLeaderboard = d.worldBossLeaderboard || {};
              // 連同 wbBlessing 等其他欄位也順手快取(world-boss.js 第 201/933 行有讀)
              if(d.wbBlessing) window._cachedGlobalStats.wbBlessing = d.wbBlessing;
              // ★ v3.14.14 — 祝福快取一更新就刷新入口標籤(不等 30 秒輪詢)
              try{ if(typeof window._refreshAdvWbBlessingTag === 'function') window._refreshAdvWbBlessingTag(); }catch(_){}
              // ★ v3.13.5(2026-05-31)— 結算系統需要的雲端欄位順手快取
              window._cachedGlobalStats.wbBossDownTimes = d.wbBossDownTimes || {};
              window._cachedGlobalStats.wbCurrentRound = d.wbCurrentRound || {};
              window._cachedGlobalStats.wbSettlement = d.wbSettlement || {};
              // ★ v3.14.23(2026-06-13)— 補快取「當前龍王 ID」
              //   血淚教訓:本訂閱原本沒把 d.wbCurrentBossId 寫進快取 → _wbGetCurrentBossId()
              //   永遠拿不到雲端值 → 一律回退預設維蘇威。重整頁面後最明顯(GM 切換當下在本機
              //   暫存的值被這個漏快取的訂閱蓋掉),導致首頁/排行/HP/戰鬥生成的龍王全錯。
              if(d.wbCurrentBossId) window._cachedGlobalStats.wbCurrentBossId = d.wbCurrentBossId;
              // ★ v3.15.9 — 關卡頁龍王名稱即時校正(BUG 修正:world-boss-ui.html 是「點按鈕才 lazy load」,
              //   其 _wbApplyCurrentBossSkin 在「初次進關卡頁」時尚未注入 → 入口名稱停在寫死的「火山炎龍王」,
              //   要切換畫面回來才修正,容易誤導。這裡改用 eager 的 _wbGetCurrentBoss()(world-boss.js 啟動即定義)
              //   直接更新關卡頁入口 .wb-curboss-nm,首次 snapshot 一回來就立刻顯示「真正開放挑戰」的龍王名稱。)
              try{
                const _cbNow = (typeof window._wbGetCurrentBoss === 'function') ? window._wbGetCurrentBoss() : null;
                if(_cbNow && _cbNow.name){
                  document.querySelectorAll('.wb-curboss-nm').forEach(function(_el){ _el.textContent = _cbNow.name; });
                }
              }catch(_eNmFix){}
              // 當前龍王已知 → 重新套用入口/排行/戰鬥標頭外觀(名稱/立繪/背景/HP 條跟上)
              try{ if(typeof window._wbApplyCurrentBossSkin === 'function') window._wbApplyCurrentBossSkin(); }catch(_){}
              // ★ v3.15.61 — 當前龍王變動 → 下一隻龍王搶先看同步刷新(資料驅動)
              try{ if(typeof window._wbApplyNextBossPreview === 'function') window._wbApplyNextBossPreview(); }catch(_){}
              // 廣播事件給 UI 即時刷新 HP 條
              try{ document.dispatchEvent(new CustomEvent('wbHpSynced')); }catch(_){}
              // 主動觸發首頁 HP 條重繪(若 world-boss.js 已掛載)
              try{ if(typeof window._wbSyncStageHpBar === 'function') window._wbSyncStageHpBar(); }catch(_){}
              // ★ FIX 20260519(v12) — 廣播排行榜更新事件(UI 可即時重繪)
              try{ document.dispatchEvent(new CustomEvent('wbLeaderboardSynced')); }catch(_){}
            }catch(eIn){
              console.warn('[WB-HpSync] onSnapshot 處理失敗', eIn);
            }
          }, (err) => {
            console.warn('[WB-HpSync] 訂閱失敗', err);
          });
        }catch(e){
          console.warn('[WB-HpSync] subscribeWbHp 例外', e);
        }
      }

      // 讀取指定 BOSS 的當前 HP(回傳 null 表示尚未有殘血紀錄 = 滿血)
      function getCurrentHp(bossId){
        try{
          const gs = window._cachedGlobalStats;
          if(gs && gs.worldBossHp && typeof gs.worldBossHp[bossId] === 'number'){
            return gs.worldBossHp[bossId];
          }
        }catch(_){}
        return null;
      }

      // 扣血:把當前 HP 減 deltaDmg(無論之前是多少,以 max 為上限、0 為下限)
      // bossId: 字串,deltaDmg: 正數
      // maxHp: 用於初始化(若雲端尚無此 boss 紀錄,先以 maxHp 為基準扣)
      async function dealDamage(bossId, deltaDmg, maxHp){
        if(!bossId || typeof deltaDmg !== 'number' || deltaDmg <= 0) return null;
        try{
          // 用 transaction 確保「讀取-扣血-寫入」原子化(避免兩人同時扣血其中一人寫覆蓋)
          const result = await runTransaction(_fbDb, async (tx) => {
            const _ref = STATS_DOC();
            const _snap = await tx.get(_ref);
            const _data = _snap.exists() ? (_snap.data() || {}) : {};
            const _hpMap = _data.worldBossHp || {};
            const _cur = (typeof _hpMap[bossId] === 'number') ? _hpMap[bossId] : (maxHp || 0);
            const _next = Math.max(0, _cur - deltaDmg);
            const _newHpMap = Object.assign({}, _hpMap);
            _newHpMap[bossId] = _next;
            const _killed = (_next === 0 && _cur > 0);
            // ★ v3.13.5(2026-05-31)— BOSS 倒下時順手記錄倒下時間 + 起新一輪 round key
            //   用途:結算系統會用 bossDownAt 計算「下一個 8:00」做隔天結算
            //   roundKey 格式:'<bossId>-YYYYMMDD' (倒下日當下,台灣時區),
            //   讓同一隻 BOSS 多次倒下都能各自結算一輪
            const _patch = { worldBossHp: _newHpMap };
            if(_killed){
              const _bossDownTimes = Object.assign({}, _data.wbBossDownTimes || {});
              const _now = Date.now();
              _bossDownTimes[bossId] = _now;
              _patch.wbBossDownTimes = _bossDownTimes;
              // 同步寫 round key(台灣日期),供結算系統索引
              try{
                const _d = new Date(_now);
                const _tw = new Date(_d.getTime() + (8 * 3600 * 1000) - (_d.getTimezoneOffset() * 60 * 1000));
                const _pad = function(n){ return n < 10 ? '0' + n : '' + n; };
                const _ds = _tw.getUTCFullYear() + _pad(_tw.getUTCMonth() + 1) + _pad(_tw.getUTCDate());
                const _roundKey = bossId + '-' + _ds;
                const _currentRound = Object.assign({}, _data.wbCurrentRound || {});
                _currentRound[bossId] = { roundKey: _roundKey, bossDownAt: _now };
                _patch.wbCurrentRound = _currentRound;
              }catch(_eRk){ console.warn('[WB-HpSync v3.13.5] roundKey 計算失敗', _eRk); }
              // ★ v3.14.15 — 祝福寫入已從本 transaction 移出(改在 transaction 成功後獨立補寫):
              //   若 Firestore Rules 對 stats/global 玩家可寫欄位有白名單而未含 wbBlessing,
              //   同筆寫入會整批被拒 → 連 HP 歸零都寫不進(龍王打不死)。解耦後祝福寫入失敗只 warn,不影響擊殺。
            }
            tx.set(_ref, _patch, { merge: true });
            return { prev: _cur, next: _next, killed: _killed };
          });
          console.log('[WB-HpSync] dealDamage', bossId, deltaDmg, '→', result);
          // ★ v3.14.13/15(乙)— 龍王倒下 → 點亮「龍王的祝福」(全服 EXP/知識幣/掉寶 +25%,72 小時)
          //   v3.14.15 改為 transaction 成功「之後」獨立補寫:Rules 若未放行 wbBlessing 也只是祝福不亮,
          //   絕不影響擊殺/HP 寫入。讀取端(入口標籤/_gameBlessingMult/大廳橫幅)早已存在。
          if(result && result.killed){
            try{
              const _bnB = (function(){
                try{
                  const _L = window.WORLD_BOSS_LINEUP;
                  if(Array.isArray(_L)){ const _b = _L.find(function(x){ return x && x.id === bossId; }); if(_b && _b.name) return _b.name; }
                }catch(_){}
                return bossId;
              })();
              const _nowB = Date.now();
              await setDoc(STATS_DOC(), { wbBlessing: {
                active: true, bossId: bossId, bossName: _bnB, bonusPct: 25,
                startedAt: _nowB, expiresAt: _nowB + 72 * 3600 * 1000,
              } }, { merge: true });
              console.log('[WB-HpSync v3.14.15] 🌟 龍王的祝福已點亮(72 小時 +25%)');
            }catch(_eBl){
              console.warn('[WB-HpSync v3.14.15] 祝福寫入失敗(不影響擊殺;若是 permission-denied,'
                + '請在 Firestore Rules 放行 stats/global 的 wbBlessing 欄位,或由 GM 後台手動開啟)', _eBl);
            }
          }
          return result;
        }catch(e){
          console.error('[WB-HpSync] dealDamage 失敗', e);
          return null;
        }
      }

      // 重置 BOSS HP(管理員用:新一輪開始 / 被 BUG 秒殺後還血)
      async function resetHp(bossId, newHp){
        try{
          const _hpMap = (window._cachedGlobalStats && window._cachedGlobalStats.worldBossHp) || {};
          const _new = Object.assign({}, _hpMap);
          if(typeof newHp === 'number') _new[bossId] = newHp;
          else delete _new[bossId];  // 不指定 newHp → 直接刪除欄位,讓 UI 顯示滿血
          const _patch = { worldBossHp: _new };
          // ★ v3.13.28(2026-06-03) — 還血(復活)時一併清除「倒下時間戳 + 該輪 roundKey」(老師指定)
          //   背景:結算系統 trySettle 是以 wbBossDownTimes[bossId] 起算「隔天 8:00」結算,完全不看當前 HP。
          //         若被 BUG 打死後只還血、不清倒下時間戳 → 系統仍記得「牠在 BUG 那刻倒下過」,
          //         隔天 8:00 會用(GM 正在清理的)髒排行榜「提前結算」,把獎勵發給靠 BUG 刷排名的玩家。
          //   修法:還血到 HP>0(復活)時,把倒下時間戳 + roundKey 清掉,讓那一輪「當沒發生過」;
          //         結算改成等龍王「下次真正倒下」後的隔天 8:00 才跑(符合老師要求)。
          //   守門:只在「復活」(newHp 未指定=滿血,或 newHp>0)時清;若明確 newHp=0(維持倒下)則不清。
          const _isRevive = (typeof newHp !== 'number') || (newHp > 0);
          if(_isRevive && typeof deleteField === 'function'){
            _patch.wbBossDownTimes = { [bossId]: deleteField() };
            _patch.wbCurrentRound  = { [bossId]: deleteField() };
          }
          await setDoc(STATS_DOC(), _patch, { merge: true });
          // 本地快取同步(避免 getStatus/trySettle 在 onSnapshot 刷新前仍讀到舊的倒下時間)
          try{
            if(window._cachedGlobalStats){
              if(!window._cachedGlobalStats.worldBossHp) window._cachedGlobalStats.worldBossHp = {};
              if(typeof newHp === 'number') window._cachedGlobalStats.worldBossHp[bossId] = newHp;
              else delete window._cachedGlobalStats.worldBossHp[bossId];
              if(_isRevive){
                if(window._cachedGlobalStats.wbBossDownTimes) delete window._cachedGlobalStats.wbBossDownTimes[bossId];
                if(window._cachedGlobalStats.wbCurrentRound)  delete window._cachedGlobalStats.wbCurrentRound[bossId];
              }
            }
          }catch(_eCache){}
          console.log('[WB-HpSync] resetHp', bossId, newHp,
            _isRevive ? '(復活:已清除倒下時間戳+roundKey → 結算需等下次「真正倒下」後隔天 8:00)' : '(維持倒下狀態)');
          return true;
        }catch(e){
          console.error('[WB-HpSync] resetHp 失敗', e);
          return false;
        }
      }

      // ★ v3.5.43 — 簽章新增 championStats(四英雄個人冠軍) + dmgSources(傷害明細)
      //   ⚠ 釐清(老師 2026-05-23 確認):
      //     - addDmg 是「整隊累積傷害」,包含答題獎勵 5000 + 聯手爆發 5000(完整反映 BOSS 扣血)
      //     - championStats.topDmg/topHeal/topTank/topCtrl 是「個人評比」,已排除 fixedDmg
      //     - dmgSources 是「完整明細」,含 fixedDmg 條目(老師後台可以區分顯示)
      // ★ v3.5.70 — 函式簽名最後加 teamEmails 參數(排行榜班級座號顯示用)
      //   舊呼叫者沒傳會是 undefined,所有讀取處都有 fallback,完全向下相容
      // ★ v3.12.17(2026-05-31) — 函式簽名最後加 isBonus 參數(補償場次標記)
      //   舊呼叫者沒傳會是 undefined,所有讀取處有 fallback,完全向下相容
      //   isBonus=true 代表:該場是補償場次(超過正常每日 2 場上限)
      //   - 戰績歷史條目加 _isBonus:true → 玩家戰績卡片貼「🎫 補償場次」
      //   - 隊伍 entry 的 bonusBattleCount++ → 排行榜顯示「(含 N 場補償)」全校可見
      //   - 傷害一樣計入排行榜總傷(維持跨天累積公平)
      // ★ v3.13.7(2026-05-31) — 再加 bonusSource 參數(區分入場券 vs GM 補償)
      //   bonusSource='ticket' → 玩家入場券場次(綠色標籤:🎫 使用挑戰入場券)
      //   bonusSource='admin'  → GM 補償場次(粉色標籤:🎫 補償場次)
      //   bonusSource=null/undefined → 正常場次
      //   舊呼叫者沒傳 → 與 isBonus 連動:isBonus=true 視為 'admin'(向下相容)
      async function updateLeaderboard(bossId, teamKey, teamNames, addDmg, tiebreaker, teamHeroes, championStats, dmgSources, teamEmails, isBonus, bonusSource){
        if(!bossId || !teamKey || typeof addDmg !== 'number' || addDmg <= 0) return null;
        try{
          const result = await runTransaction(_fbDb, async (tx) => {
            const _ref = STATS_DOC();
            const _snap = await tx.get(_ref);
            const _data = _snap.exists() ? (_snap.data() || {}) : {};
            const _lbMap = _data.worldBossLeaderboard || {};
            const _list = Array.isArray(_lbMap[bossId]) ? _lbMap[bossId].slice() : [];
            // 找到該隊伍紀錄,沒有就新增
            const _idx = _list.findIndex(e => e && e.teamKey === teamKey);
            // ★ tiebreaker 累計:取本場最佳值(留下對隊伍最有利的數字)
            const _newTb = tiebreaker || {};
            // ★ v3.5.6 — teamHeroes 容錯:過濾掉空槽,每筆只留 {name, lv}
            const _sanitizeHeroes = function(arr){
              if(!Array.isArray(arr)) return null;
              const _out = arr.slice(0, 4).map(h => {
                if(!h || typeof h !== 'object') return { name: '?', lv: 1 };
                return {
                  name: (typeof h.name === 'string' && h.name) ? h.name : '?',
                  lv:   (typeof h.lv   === 'number' && h.lv > 0)  ? h.lv   : 1,
                };
              });
              return _out;
            };
            const _newHeroes = _sanitizeHeroes(teamHeroes);
            // ★ v3.5.43 — championStats 容錯
            const _sanitizeChampion = function(c){
              if(!c || typeof c !== 'object') return null;
              const _f = (obj) => {
                if(!obj || typeof obj !== 'object') return null;
                return {
                  name: (typeof obj.name === 'string' && obj.name) ? obj.name : '?',
                  lv: (typeof obj.lv === 'number' && obj.lv > 0) ? obj.lv : 1,
                  value: (typeof obj.value === 'number') ? Math.floor(obj.value) : 0,
                };
              };
              return {
                topDmg:  _f(c.topDmg),
                topHeal: _f(c.topHeal),
                topTank: _f(c.topTank),
                topCtrl: _f(c.topCtrl),
                topCtrl_detail: (c.topCtrl_detail && typeof c.topCtrl_detail === 'object') ? {
                  count: Math.floor(c.topCtrl_detail.count || 0),
                  turnSum: Math.floor(c.topCtrl_detail.turnSum || 0),
                } : null,
              };
            };
            const _newChampion = _sanitizeChampion(championStats);
            // ★ v3.5.43 — dmgSources 容錯 + 大小限制(40 筆 × 7 欄,約 5KB,在 Firestore 1MB 限制內)
            const _sanitizeSources = function(arr){
              if(!Array.isArray(arr)) return [];
              return arr.slice(0, 40).map(s => ({
                heroName: String(s.heroName || '?').slice(0, 30),
                heroLv:   Math.floor(s.heroLv || 1),
                skill:    String(s.skill || '?').slice(0, 30),
                skillLv:  Math.floor(s.skillLv || 0),
                totalDmg: Math.floor(s.totalDmg || 0),
                hits:     Math.floor(s.hits || 1),
                isFixed:  !!s.isFixed,
              }));
            };
            const _newSources = _sanitizeSources(dmgSources);

            // ★★ v3.11.11(2026-05-28) — 計算「本場單回合峰值」陣列(p1 玩家每位英雄一筆)
            //   給管理員後台「英雄表現排行榜」用 — 跨玩家比較單回合輸出/治療最強的技能
            //   來源:G.battleStats[name].peakRoundDmg / peakRoundHeal(只 p1 + 排除 isFixed)
            //   結構:[{name, lv, peakDmg, peakDmgRound, peakDmgSkill, peakHeal, peakHealRound, peakHealSkill}, ...]
            //   寫入大小:最多 4 隻英雄 × ~7 欄 ≈ 0.5KB,完全在 Firestore 1MB 內
            const _peakRoundSnaps = (function(){
              try{
                if(!G || !G.battleStats) return [];
                const _arr = [];
                Object.keys(G.battleStats).forEach(function(_name){
                  const _d = G.battleStats[_name];
                  if(!_d || _d.side !== 'p1') return;
                  // 結算可能還沒跑(若 statTrack 還沒切回合),先補一次峰值結算
                  if((_d._roundAccDmg || 0) > (_d.peakRoundDmg || 0)){
                    _d.peakRoundDmg = _d._roundAccDmg;
                    _d.peakRoundDmgRound = _d._roundAccDmgLastRound || 0;
                    _d.peakRoundDmgSkill = (_d._roundAccDmgTopHit && _d._roundAccDmgTopHit.skill) || _d.peakRoundDmgSkill || '';
                  }
                  if((_d._roundAccHeal || 0) > (_d.peakRoundHeal || 0)){
                    _d.peakRoundHeal = _d._roundAccHeal;
                    _d.peakRoundHealRound = _d._roundAccHealLastRound || 0;
                    _d.peakRoundHealSkill = (_d._roundAccHealTopHit && _d._roundAccHealTopHit.skill) || _d.peakRoundHealSkill || '';
                  }
                  if((_d.peakRoundDmg || 0) > 0 || (_d.peakRoundHeal || 0) > 0){
                    const _heroLv = (typeof getHeroLevel === 'function') ? getHeroLevel(_name) : 1;
                    _arr.push({
                      name: String(_name).slice(0, 30),
                      lv: Math.floor(_heroLv || 1),
                      peakDmg: Math.floor(_d.peakRoundDmg || 0),
                      peakDmgRound: Math.floor(_d.peakRoundDmgRound || 0),
                      peakDmgSkill: String(_d.peakRoundDmgSkill || '').slice(0, 30),
                      peakHeal: Math.floor(_d.peakRoundHeal || 0),
                      peakHealRound: Math.floor(_d.peakRoundHealRound || 0),
                      peakHealSkill: String(_d.peakRoundHealSkill || '').slice(0, 30),
                    });
                  }
                });
                return _arr.slice(0, 4);  // 4 位英雄就頂了
              }catch(_){ return []; }
            })();

            // ★★ v3.10.0(2026-05-26) — 計算「本場 MVP」(最高傷害英雄) ★★
            //   老師需求:排行榜要顯示「每場最高傷害的角色和等級」
            //   來源:championStats.topDmg(本場該隊伍最高 dmgReal 的英雄,已排除答題獎勵)
            //   注意:跟「歷史 PR」(_mergeChampion 的結果)不同,thisBattleMvp 只記本場
            const _thisBattleMvp = (_newChampion && _newChampion.topDmg) ? {
              name: _newChampion.topDmg.name || '?',
              lv:   _newChampion.topDmg.lv   || 1,
              dmg:  _newChampion.topDmg.value || 0,
            } : null;

            // ★★ v3.10.0 — 本場戰績條目(時間 / 傷害 / MVP) ★★
            //   設計:
            //     - at:  完成時間戳(Date.now())
            //     - dmg: 該場傷害(就是 addDmg)
            //     - mvp: 該場最高傷害英雄 { name, lv, dmg }
            //     - turns: 該場回合數(來自 tiebreaker)
            //     - qc:    該場答題正確數
            //     - tb:    ★ v3.11.8 — 該場聯手爆發觸發次數
            //     - _isBonus: ★ v3.12.17 — 是否為補償場次(超過正常每日 2 場上限)
            //   只保留最近 10 場,避免單筆紀錄無限膨脹(Firestore 1MB 限制)
            const _MAX_HISTORY = 10;
            // ★ v3.13.7 — bonusSource fallback:沒傳但 isBonus=true 視為 'admin'(舊資料相容)
            const _effectiveSource = isBonus
              ? (bonusSource === 'ticket' ? 'ticket' : 'admin')
              : null;
            const _thisBattleRecord = {
              at: Date.now(),
              dmg: Math.floor(addDmg),
              mvp: _thisBattleMvp,
              turns: Math.floor(_newTb.turns || 0),
              qc: Math.floor(_newTb.quizCorrect || 0),
              tb: Math.floor(_newTb.teamBurstCount || 0),
              // ★ v3.12.17 — 補償場次旗標(只在 isBonus=true 時寫,節省欄位空間)
              ...(isBonus ? { _isBonus: true } : {}),
              // ★ v3.13.7 — bonusSource('ticket' | 'admin'),只在 isBonus=true 時寫
              ...(_effectiveSource ? { _bonusSource: _effectiveSource } : {}),
            };

            if(_idx >= 0){
              const _cur = _list[_idx];
              const _curTb = _cur.tiebreaker || {};
              const _mergedTb = {
                turns: Math.min(_curTb.turns || 999, _newTb.turns || 999),       // 回合數越少越好
                quizCorrect: Math.max(_curTb.quizCorrect || 0, _newTb.quizCorrect || 0),  // 正確數越多越好
                aliveCount: Math.max(_curTb.aliveCount || 0, _newTb.aliveCount || 0),     // 存活越多越好
                // ★ v3.11.8(2026-05-27) — teamBurstCount 取「最高值」(該隊伍歷史 PR)
                //   不累加(累加會跨場無限膨脹失去意義),取最高代表「該隊伍最棒的一場觸發了 N 次聯手爆發」
                teamBurstCount: Math.max(_curTb.teamBurstCount || 0, _newTb.teamBurstCount || 0),
              };
              // ★ v3.5.43 — championStats merge:取本場 vs 既有紀錄的「最高值」當該隊伍 PR
              //   (代表這隊伍歷史上最強的一場該英雄表現)
              const _mergeChampion = function(cur, nw){
                if(!cur && !nw) return null;
                if(!cur) return nw;
                if(!nw) return cur;
                const _pick = (a, b) => {
                  if(!a) return b; if(!b) return a;
                  return (a.value || 0) >= (b.value || 0) ? a : b;
                };
                return {
                  topDmg:  _pick(cur.topDmg,  nw.topDmg),
                  topHeal: _pick(cur.topHeal, nw.topHeal),
                  topTank: _pick(cur.topTank, nw.topTank),
                  topCtrl: _pick(cur.topCtrl, nw.topCtrl),
                  topCtrl_detail: (nw.topCtrl && cur.topCtrl &&
                    (nw.topCtrl.value || 0) > (cur.topCtrl.value || 0))
                    ? nw.topCtrl_detail : cur.topCtrl_detail,
                };
              };
              // ★ v3.11.11(2026-05-28) — peakRoundSnaps merge:同英雄取較高的 peakDmg/peakHeal
              //   結構:[{name, lv, peakDmg, peakDmgRound, peakDmgSkill, peakHeal, peakHealRound, peakHealSkill}]
              const _mergePeakSnaps = function(curArr, nwArr){
                const _byName = {};
                (Array.isArray(curArr) ? curArr : []).forEach(function(s){
                  if(s && s.name) _byName[s.name] = s;
                });
                (Array.isArray(nwArr) ? nwArr : []).forEach(function(s){
                  if(!s || !s.name) return;
                  const _ex = _byName[s.name];
                  if(!_ex){
                    _byName[s.name] = s;
                  } else {
                    // 取較高的 peakDmg + peakHeal(各自獨立 merge,而非整筆)
                    const _merged = {
                      name: s.name,
                      lv: Math.max(_ex.lv || 1, s.lv || 1),  // 取較高等級(英雄會升級)
                    };
                    if((s.peakDmg || 0) > (_ex.peakDmg || 0)){
                      _merged.peakDmg = s.peakDmg;
                      _merged.peakDmgRound = s.peakDmgRound;
                      _merged.peakDmgSkill = s.peakDmgSkill;
                    } else {
                      _merged.peakDmg = _ex.peakDmg || 0;
                      _merged.peakDmgRound = _ex.peakDmgRound || 0;
                      _merged.peakDmgSkill = _ex.peakDmgSkill || '';
                    }
                    if((s.peakHeal || 0) > (_ex.peakHeal || 0)){
                      _merged.peakHeal = s.peakHeal;
                      _merged.peakHealRound = s.peakHealRound;
                      _merged.peakHealSkill = s.peakHealSkill;
                    } else {
                      _merged.peakHeal = _ex.peakHeal || 0;
                      _merged.peakHealRound = _ex.peakHealRound || 0;
                      _merged.peakHealSkill = _ex.peakHealSkill || '';
                    }
                    _byName[s.name] = _merged;
                  }
                });
                return Object.values(_byName).slice(0, 4);
              };
              // ★ v3.10.0 — battleHistory append:把本場加到歷史紀錄(最新放最前面),最多 10 場
              const _prevHistory = Array.isArray(_cur.battleHistory) ? _cur.battleHistory : [];
              const _newHistory = [_thisBattleRecord]
                .concat(_prevHistory)
                .slice(0, _MAX_HISTORY);

              _list[_idx] = {
                teamKey,
                teamNames: Array.isArray(teamNames) ? teamNames.slice(0, 4) : _cur.teamNames,
                // ★ v3.5.70 — teamEmails 也覆蓋(若沒傳就保留舊的,容錯)
                teamEmails: Array.isArray(teamEmails)
                  ? teamEmails.slice(0, 4).map(e => (typeof e === 'string') ? e.toLowerCase() : '')
                  : (_cur.teamEmails || null),
                // ★ v3.5.6 — 排行榜每場更新都覆蓋 teamHeroes(顯示最新一場用的英雄)
                teamHeroes: _newHeroes || _cur.teamHeroes || null,
                totalDmg: (_cur.totalDmg || 0) + addDmg,
                battles: (_cur.battles || 0) + 1,
                // ★ v3.12.17 — 補償場次累計(只算 GM 補償 'admin',不含入場券)
                // ★ v3.13.7 — 為了向下相容,bonusBattleCount 維持「只算 admin」語意
                bonusBattleCount: (_cur.bonusBattleCount || 0) + (_effectiveSource === 'admin' ? 1 : 0),
                // ★ v3.13.7 — 入場券場次累計(獨立於 bonusBattleCount)
                bonusTicketCount: (_cur.bonusTicketCount || 0) + (_effectiveSource === 'ticket' ? 1 : 0),
                lastUpdated: Date.now(),
                tiebreaker: _mergedTb,
                // ★ v3.5.43 — 個人四冠軍:merge 最高值
                championStats: _mergeChampion(_cur.championStats, _newChampion),
                // ★ v3.5.43 — 最近一場的 dmgSources(每場更新都覆蓋,給老師看「最近一場」明細)
                lastDmgSources: _newSources,
                // ★ v3.11.11(2026-05-28) — 本場單回合峰值快照,合併取每位英雄 PR(管理員後台技能平衡用)
                peakRoundSnaps: _mergePeakSnaps(_cur.peakRoundSnaps, _peakRoundSnaps),
                lastBattleAt: Date.now(),
                lastBattleDmg: addDmg,
                // ★ v3.10.0 — 每場戰績歷史(最新 10 場)
                battleHistory: _newHistory,
              };
            }else{
              _list.push({
                teamKey,
                teamNames: Array.isArray(teamNames) ? teamNames.slice(0, 4) : [],
                // ★ v3.5.70 — 新隊伍也帶上 teamEmails
                teamEmails: Array.isArray(teamEmails)
                  ? teamEmails.slice(0, 4).map(e => (typeof e === 'string') ? e.toLowerCase() : '')
                  : null,
                // ★ v3.5.6 — 新隊伍也帶上 teamHeroes
                teamHeroes: _newHeroes || null,
                totalDmg: addDmg,
                battles: 1,
                // ★ v3.12.17 — 第一場若是 GM 補償,直接 = 1
                bonusBattleCount: _effectiveSource === 'admin' ? 1 : 0,
                // ★ v3.13.7 — 第一場若是入場券,直接 = 1
                bonusTicketCount: _effectiveSource === 'ticket' ? 1 : 0,
                lastUpdated: Date.now(),
                tiebreaker: {
                  turns: _newTb.turns || 999,
                  quizCorrect: _newTb.quizCorrect || 0,
                  aliveCount: _newTb.aliveCount || 0,
                },
                // ★ v3.5.43 — 第一場直接帶上 championStats
                championStats: _newChampion,
                lastDmgSources: _newSources,
                // ★ v3.11.11(2026-05-28) — 本場單回合峰值快照
                peakRoundSnaps: _peakRoundSnaps,
                lastBattleAt: Date.now(),
                lastBattleDmg: addDmg,
                // ★ v3.10.0 — 第一場直接初始化 battleHistory
                battleHistory: [_thisBattleRecord],
              });
            }
            // ★ FIX 20260519(v13) — 多級排序(同總傷時依 tiebreaker 細排)
            _list.sort((a, b) => {
              const _da = b.totalDmg - a.totalDmg;
              if(_da !== 0) return _da;
              // 同總傷:tiebreaker 比較
              const ta = a.tiebreaker || {}, tb = b.tiebreaker || {};
              const _dt = (ta.turns || 999) - (tb.turns || 999);  // 回合數少優先
              if(_dt !== 0) return _dt;
              const _dq = (tb.quizCorrect || 0) - (ta.quizCorrect || 0);  // 正確多優先
              if(_dq !== 0) return _dq;
              return (tb.aliveCount || 0) - (ta.aliveCount || 0);  // 存活多優先
            });
            const _trimmed = _list.slice(0, 100);
            const _newLbMap = Object.assign({}, _lbMap);
            _newLbMap[bossId] = _trimmed;
            tx.set(_ref, { worldBossLeaderboard: _newLbMap }, { merge: true });
            // 找出自己排名
            const _myRank = _trimmed.findIndex(e => e.teamKey === teamKey) + 1;
            return { rank: _myRank, totalDmg: _trimmed[_myRank-1]?.totalDmg || addDmg };
          });
          console.log('[WB-HpSync] updateLeaderboard', bossId, teamKey, '→', result);
          // ★ v3.14.3 — 旁路寫入「逐回合 × 逐英雄」傷害明細到 wbDamageDetail/{uid}(GM 後台查看用)
          //   ❗失敗一律 try-catch 靜默,絕不影響排行榜結算與戰鬥流程。
          //   資料源:G._wbDmgSources(每筆帶 round,原本僅收集從未使用)。key 用 uid。
          //   甲2=保留這隻龍王的「每一場」(append,安全上限 80 場 ~200KB,Firestore 單 doc 1MB 內)
          //   乙1=含全部傷害(不排除 fixedDmg)。多人房記執行結算者(host)視角;solo 完整。
          try{
            const _wbDetailUid = window._gUserId;
            const _rawSrc = (typeof G !== 'undefined' && G && Array.isArray(G._wbDmgSources)) ? G._wbDmgSources : null;
            if(_wbDetailUid && _rawSrc && _rawSrc.length){
              const _byRound = {};
              let _grandTotal = 0;
              _rawSrc.forEach(function(s){
                if(!s) return;
                // ★ v3.14.4 — 老師需求:明細「不含聯手爆發 5000」。目前聯手爆發/答題獎勵直接改
                //   boss.curHp 不走 doDmg 收集,天然不在 _wbDmgSources;此過濾為保險,
                //   防未來該路徑改走 doDmg(skill 會標 '固定傷害(答題/聯手)')時混入明細。
                try{
                  const _sk = String(s.skill || '');
                  if(_sk.indexOf('答題') >= 0 || _sk.indexOf('聯手') >= 0) return;
                }catch(_){}
                const _r = Math.max(0, Math.floor(s.round || 0));
                const _nm = String(s.heroName || '?').slice(0, 24);
                const _amt = Math.max(0, Math.floor(s.amount || 0));
                if(!_byRound[_r]) _byRound[_r] = {};
                _byRound[_r][_nm] = (_byRound[_r][_nm] || 0) + _amt;
                _grandTotal += _amt;
              });
              const _rounds = Object.keys(_byRound)
                .map(function(k){ return parseInt(k, 10); })
                .sort(function(a, b){ return a - b; })
                .slice(0, 60)   // 一場最多保留 60 回合(防爆)
                .map(function(r){
                  const _hs = _byRound[r];
                  return { r: r, h: Object.keys(_hs).slice(0, 8).map(function(n){ return { n: n, d: _hs[n] }; }) };
                });
              const _thisBattle = { at: Date.now(), total: _grandTotal, rounds: _rounds };
              const _detailRef = doc(_fbDb, 'wbDamageDetail', _wbDetailUid);
              await runTransaction(_fbDb, async (tx) => {
                const _ds = await tx.get(_detailRef);
                const _dd = _ds.exists() ? (_ds.data() || {}) : {};
                let _battles = Array.isArray(_dd.battles) ? _dd.battles.slice() : [];
                _battles.push(_thisBattle);
                if(_battles.length > 80) _battles = _battles.slice(_battles.length - 80);
                tx.set(_detailRef, { bossId: bossId, uid: _wbDetailUid, battles: _battles, updatedAt: Date.now() }, { merge: false });
              });
              // 用完即清(雙保險防跨場殘留;正常情況 G 每場重建本就會清)
              try{ G._wbDmgSources = []; G._wbHealSources = []; }catch(_){}
            }
          }catch(_wbDetailErr){ console.warn('[WB逐回合明細] 寫入略過:', _wbDetailErr); }
          return result;
        }catch(e){
          console.error('[WB-HpSync] updateLeaderboard 失敗', e);
          return null;
        }
      }

      // 取得指定 BOSS 的排行榜(從快取讀,實時性靠 subscribeWbHp 訂閱)
      function getLeaderboard(bossId, topN){
        try{
          const gs = window._cachedGlobalStats;
          const _lbMap = (gs && gs.worldBossLeaderboard) || {};
          const _list = Array.isArray(_lbMap[bossId]) ? _lbMap[bossId] : [];
          // 確保已排序
          const _sorted = _list.slice().sort((a, b) => (b.totalDmg || 0) - (a.totalDmg || 0));
          if(typeof topN === 'number' && topN > 0) return _sorted.slice(0, topN);
          return _sorted;
        }catch(_){
          return [];
        }
      }

      // ★ v3.5.20 — 管理員清除世界 BOSS 排行榜(老師 2026-05-22 需求)
      // ★ v3.5.21 修補(老師回報「按了沒清掉」):
      //   原 BUG #1:用 setDoc(_newLbMap, {merge:true}),空物件不會覆蓋既有的 vesuvius_fire_dragon key
      //   原 BUG #2:BOSS_ID 寫成 'vesuvius',但實際是 'vesuvius_fire_dragon'(world-boss-ui.html line 3624)
      //   修法:
      //     - 改用 updateDoc + deleteField():真正從 Firestore 文件中移除指定欄位
      //     - 用「點記法路徑」如 'worldBossLeaderboard.vesuvius_fire_dragon' 直接刪子欄位
      //     - bossId 改為呼叫端傳入(預設 vesuvius_fire_dragon),清更乾淨
      async function clearLeaderboard(bossId){
        try{
          const _ref = STATS_DOC();
          // 先讀現況看看要清掉哪些
          const _snap = await getDoc(_ref);
          const _data = _snap.exists() ? (_snap.data() || {}) : {};
          const _lbMap = _data.worldBossLeaderboard || {};
          let _removed = 0;
          let _kept = 0;
          const _updates = {};

          if(bossId){
            // 指定 BOSS:用點記法刪該子欄位
            if(Array.isArray(_lbMap[bossId])){
              _removed = _lbMap[bossId].length;
            }
            _updates['worldBossLeaderboard.' + bossId] = deleteField();
            // 算保留的數量(統計用)
            Object.keys(_lbMap).forEach(function(k){
              if(k !== bossId && Array.isArray(_lbMap[k])){
                _kept += _lbMap[k].length;
              }
            });
          } else {
            // 沒指定 → 把整個 worldBossLeaderboard 刪掉
            Object.keys(_lbMap).forEach(function(k){
              _removed += Array.isArray(_lbMap[k]) ? _lbMap[k].length : 0;
            });
            _updates['worldBossLeaderboard'] = deleteField();
          }

          await updateDoc(_ref, _updates);

          // 本地快取也立刻清(避免 UI 還看到舊資料)
          try{
            if(window._cachedGlobalStats && window._cachedGlobalStats.worldBossLeaderboard){
              if(bossId){
                delete window._cachedGlobalStats.worldBossLeaderboard[bossId];
              } else {
                window._cachedGlobalStats.worldBossLeaderboard = {};
              }
            }
            // 廣播事件讓 UI 重繪
            document.dispatchEvent(new CustomEvent('wbLeaderboardSynced'));
          }catch(_){}

          const result = { removed: _removed, kept: _kept, bossId: bossId || 'ALL' };
          console.log('[WB-HpSync] clearLeaderboard', result);
          return result;
        }catch(e){
          console.error('[WB-HpSync] clearLeaderboard 失敗', e);
          return null;
        }
      }

      // ★ v3.5.22(2026-05-22) — 管理員指定刪除排行榜紀錄(老師需求:資料異常/BUG 造成的不均衡傷害)
      //   差別於 clearLeaderboard:
      //     - clearLeaderboard:整個 bossId 全清(或全 BOSS 全清)
      //     - clearLeaderboardEntries:只刪指定的 teamKey 列(可一次刪多筆),其他紀錄保留
      //   用 transaction 確保「讀-改-寫」一致(避免和玩家剛打完的 updateLeaderboard 衝突)
      async function clearLeaderboardEntries(bossId, teamKeys){
        if(!bossId || !Array.isArray(teamKeys) || teamKeys.length === 0){
          console.warn('[WB-HpSync] clearLeaderboardEntries 參數無效', bossId, teamKeys);
          return null;
        }
        const _keySet = new Set(teamKeys);
        try{
          const result = await runTransaction(_fbDb, async (tx) => {
            const _ref = STATS_DOC();
            const _snap = await tx.get(_ref);
            const _data = _snap.exists() ? (_snap.data() || {}) : {};
            const _lbMap = _data.worldBossLeaderboard || {};
            const _list = Array.isArray(_lbMap[bossId]) ? _lbMap[bossId] : [];
            let _removed = 0;
            const _filtered = _list.filter(function(e){
              if(e && _keySet.has(e.teamKey)){
                _removed++;
                return false;
              }
              return true;
            });
            const _newLbMap = Object.assign({}, _lbMap);
            _newLbMap[bossId] = _filtered;
            tx.set(_ref, { worldBossLeaderboard: _newLbMap }, { merge: true });
            return { removed: _removed, kept: _filtered.length, bossId: bossId };
          });
          // 本地快取也立刻同步(避免 UI 還看到舊資料)
          try{
            if(window._cachedGlobalStats && window._cachedGlobalStats.worldBossLeaderboard){
              const _localList = window._cachedGlobalStats.worldBossLeaderboard[bossId];
              if(Array.isArray(_localList)){
                window._cachedGlobalStats.worldBossLeaderboard[bossId] =
                  _localList.filter(function(e){ return !(e && _keySet.has(e.teamKey)); });
              }
            }
            document.dispatchEvent(new CustomEvent('wbLeaderboardSynced'));
          }catch(_){}
          console.log('[WB-HpSync] clearLeaderboardEntries', result);
          return result;
        }catch(e){
          console.error('[WB-HpSync] clearLeaderboardEntries 失敗', e);
          return null;
        }
      }

      // ════════════════════════════════════════════════════════════════
      // ★ v3.10.18(2026-05-26) — 刪除單筆 battleHistory 條目(老師需求)
      // ────────────────────────────────────────────────────────────────
      // 用途:某隊伍的「第 N 場」是異常傷害(BUG),只想砍那一場,
      //       不想砍整支隊伍歷史。
      //
      // 副作用:
      //   - entry.battleHistory[index] 砍掉
      //   - entry.totalDmg -= 那場 dmg
      //   - entry.battles -= 1
      //   - 若 battles 歸 0 → 整筆 entry 也砍掉(避免空殼留著)
      //
      // 自動連動:
      //   _wbDailyLimit.todayCount 是讀 battleHistory 計算的,
      //   所以刪掉一筆 → 該玩家的「今日場次」自動 -1,不用額外處理。
      //
      // 簽名:
      //   removeBattleFromHistory(bossId, teamKey, battleIndex)
      //   bossId / teamKey:string
      //   battleIndex:number(0-based,對應 battleHistory 陣列 index)
      // 回傳:
      //   { ok:true, removedDmg, newTotalDmg, newBattles, entryRemoved }
      //   失敗回 null
      // ════════════════════════════════════════════════════════════════
      async function removeBattleFromHistory(bossId, teamKey, battleIndex){
        if(!bossId || !teamKey || typeof battleIndex !== 'number' || battleIndex < 0){
          console.warn('[WB-HpSync] removeBattleFromHistory 參數無效', bossId, teamKey, battleIndex);
          return null;
        }
        try{
          const result = await runTransaction(_fbDb, async (tx) => {
            const _ref = STATS_DOC();
            const _snap = await tx.get(_ref);
            if(!_snap.exists()) return { ok:false, reason:'no_stats_doc' };
            const _data = _snap.data() || {};
            const _lbMap = _data.worldBossLeaderboard || {};
            const _list = Array.isArray(_lbMap[bossId]) ? _lbMap[bossId].slice() : [];
            const _idx = _list.findIndex(e => e && e.teamKey === teamKey);
            if(_idx < 0) return { ok:false, reason:'team_not_found' };

            const _cur = _list[_idx];
            const _hist = Array.isArray(_cur.battleHistory) ? _cur.battleHistory.slice() : [];
            if(battleIndex >= _hist.length) return { ok:false, reason:'battle_index_oob', historyLen:_hist.length };

            const _toRemove = _hist[battleIndex];
            const _removedDmg = (_toRemove && typeof _toRemove.dmg === 'number') ? _toRemove.dmg : 0;
            _hist.splice(battleIndex, 1);

            const _newTotalDmg = Math.max(0, (_cur.totalDmg || 0) - _removedDmg);
            const _newBattles = Math.max(0, (_cur.battles || 0) - 1);

            // 若該隊伍打完歸零 → 整筆 entry 也砍掉(避免空殼)
            if(_newBattles === 0 || _hist.length === 0){
              _list.splice(_idx, 1);
              const _updates = {};
              _updates['worldBossLeaderboard.' + bossId] = _list;
              tx.update(_ref, _updates);
              return {
                ok:true,
                removedDmg: _removedDmg,
                newTotalDmg: 0,
                newBattles: 0,
                entryRemoved: true,
              };
            }

            // 一般情況:更新該 entry
            _list[_idx] = Object.assign({}, _cur, {
              battleHistory: _hist,
              totalDmg: _newTotalDmg,
              battles: _newBattles,
              // 更新 lastBattleAt 為剩餘最新一場的時間
              lastBattleAt: (_hist[0] && _hist[0].at) || _cur.lastBattleAt || Date.now(),
              lastBattleDmg: (_hist[0] && _hist[0].dmg) || 0,
            });
            // 重新排序(總傷可能改變)
            _list.sort((a, b) => (b.totalDmg||0) - (a.totalDmg||0));
            const _updates = {};
            _updates['worldBossLeaderboard.' + bossId] = _list;
            tx.update(_ref, _updates);
            return {
              ok:true,
              removedDmg: _removedDmg,
              newTotalDmg: _newTotalDmg,
              newBattles: _newBattles,
              entryRemoved: false,
            };
          });

          // 本地快取也同步更新(避免 UI 看到舊資料)
          if(result && result.ok){
            try{
              if(window._cachedGlobalStats && window._cachedGlobalStats.worldBossLeaderboard){
                const _localList = window._cachedGlobalStats.worldBossLeaderboard[bossId];
                if(Array.isArray(_localList)){
                  const _li = _localList.findIndex(e => e && e.teamKey === teamKey);
                  if(_li >= 0){
                    if(result.entryRemoved){
                      _localList.splice(_li, 1);
                    } else {
                      const _ent = _localList[_li];
                      const _hist = Array.isArray(_ent.battleHistory) ? _ent.battleHistory.slice() : [];
                      _hist.splice(battleIndex, 1);
                      _ent.battleHistory = _hist;
                      _ent.totalDmg = result.newTotalDmg;
                      _ent.battles = result.newBattles;
                      _ent.lastBattleAt = (_hist[0] && _hist[0].at) || _ent.lastBattleAt;
                      _ent.lastBattleDmg = (_hist[0] && _hist[0].dmg) || 0;
                    }
                  }
                }
              }
              document.dispatchEvent(new CustomEvent('wbLeaderboardSynced'));
            }catch(_){}
          }
          console.log('[WB-HpSync] removeBattleFromHistory', result);
          return result;
        }catch(e){
          console.error('[WB-HpSync] removeBattleFromHistory 失敗', e);
          return null;
        }
      }

      // ════════════════════════════════════════════════════════════════
      // ★ v3.13.3(2026-05-31)— 墓碑模式刪除(老師需求 2)
      // ────────────────────────────────────────────────────────────────
      // 用途:GM 標記某場為 BUG/異常,「保留紀錄但傷害扣回 + 紅字墓碑」
      //       讓全員看到「這支隊伍有 N 場被判定為 BUG」,維護排行榜可信度
      //
      // 跟 removeBattleFromHistory 的差別:
      //   - removeBattleFromHistory:真刪、battleHistory 該 index 從陣列移除
      //   - markBattleAsDeleted:留著、加旗標(_deletedAt + _deletedReason + _origDmg)
      //
      // 副作用:
      //   - 該 battleHistory[idx] 增加 _deletedAt / _deletedReason / _origDmg 旗標
      //   - 該場 dmg 從 totalDmg 扣掉(玩家不該享受作弊收益)
      //   - battles 維持不變(那場仍算「進場過」紀錄,只是傷害不算)
      //   - bonusBattleCount 若該場是補償也跟著 -1(維持 _isBonus 對應)
      //
      // 簽名:
      //   markBattleAsDeleted(bossId, teamKey, battleIndex, reason)
      //   bossId / teamKey:string
      //   battleIndex:number(0-based,對應 battleHistory 陣列 index)
      //   reason:string(GM 填寫的刪除原因,玩家看不到原因細節,只看到「BUG數據已刪除」)
      // 回傳:
      //   { ok:true, removedDmg, newTotalDmg }
      //   失敗回 null
      // ════════════════════════════════════════════════════════════════
      async function markBattleAsDeleted(bossId, teamKey, battleIndex, reason){
        if(!bossId || !teamKey || typeof battleIndex !== 'number' || battleIndex < 0){
          console.warn('[WB-HpSync] markBattleAsDeleted 參數無效', bossId, teamKey, battleIndex);
          return null;
        }
        try{
          const result = await runTransaction(_fbDb, async (tx) => {
            const _ref = STATS_DOC();
            const _snap = await tx.get(_ref);
            if(!_snap.exists()) return { ok:false, reason:'no_stats_doc' };
            const _data = _snap.data() || {};
            const _lbMap = _data.worldBossLeaderboard || {};
            const _list = Array.isArray(_lbMap[bossId]) ? _lbMap[bossId].slice() : [];
            const _idx = _list.findIndex(e => e && e.teamKey === teamKey);
            if(_idx < 0) return { ok:false, reason:'team_not_found' };
            const _cur = _list[_idx];
            const _hist = Array.isArray(_cur.battleHistory) ? _cur.battleHistory.slice() : [];
            if(battleIndex >= _hist.length) return { ok:false, reason:'battle_index_oob', historyLen:_hist.length };
            const _toMark = _hist[battleIndex];
            // 已標記過 → 拒絕重複標記
            if(_toMark && _toMark._deletedAt){
              return { ok:false, reason:'already_marked_deleted' };
            }
            const _origDmg = (_toMark && typeof _toMark.dmg === 'number') ? _toMark.dmg : 0;
            // 標墓碑:留原資料但加旗標
            _hist[battleIndex] = Object.assign({}, _toMark, {
              _deletedAt: Date.now(),
              _deletedReason: String(reason || '').slice(0, 200) || '(GM 未填寫原因)',
              _origDmg: _origDmg,
              dmg: 0,  // 對外顯示傷害歸 0
            });
            // 扣 totalDmg(那場原本貢獻的傷害扣回)
            const _newTotalDmg = Math.max(0, (_cur.totalDmg || 0) - _origDmg);
            // ★ v3.13.7 — bonusBattleCount / bonusTicketCount 分流處理
            //   依照該場 _bonusSource 決定扣哪個欄位
            //   舊資料相容:若 _isBonus=true 但無 _bonusSource → 視為 admin
            const _delSource = _toMark._bonusSource
              || (_toMark._isBonus ? 'admin' : null);
            const _newBonus = _delSource === 'admin'
              ? Math.max(0, (_cur.bonusBattleCount || 0) - 1)
              : (_cur.bonusBattleCount || 0);
            const _newTicket = _delSource === 'ticket'
              ? Math.max(0, (_cur.bonusTicketCount || 0) - 1)
              : (_cur.bonusTicketCount || 0);
            _list[_idx] = Object.assign({}, _cur, {
              battleHistory: _hist,
              totalDmg: _newTotalDmg,
              // battles 維持不變(那場仍算「打過了」紀錄,只是傷害不算)
              bonusBattleCount: _newBonus,
              bonusTicketCount: _newTicket,
            });
            // 重新排序
            _list.sort((a, b) => (b.totalDmg||0) - (a.totalDmg||0));
            const _updates = {};
            _updates['worldBossLeaderboard.' + bossId] = _list;
            tx.update(_ref, _updates);
            return {
              ok:true,
              removedDmg: _origDmg,
              newTotalDmg: _newTotalDmg,
            };
          });
          // 本地快取同步更新
          if(result && result.ok){
            try{
              if(window._cachedGlobalStats && window._cachedGlobalStats.worldBossLeaderboard){
                const _localList = window._cachedGlobalStats.worldBossLeaderboard[bossId];
                if(Array.isArray(_localList)){
                  const _li = _localList.findIndex(e => e && e.teamKey === teamKey);
                  if(_li >= 0){
                    const _ent = _localList[_li];
                    const _hist = Array.isArray(_ent.battleHistory) ? _ent.battleHistory.slice() : [];
                    let _wasBonusBattle = false;
                    let _wasBonusTicket = false;
                    let _wasOrigDmg = 0;
                    if(_hist[battleIndex]){
                      const _orig = _hist[battleIndex];
                      // ★ v3.13.7 — 分流:依 _bonusSource 決定扣哪個本地計數
                      const _delSrc = _orig._bonusSource
                        || (_orig._isBonus ? 'admin' : null);
                      _wasBonusBattle = _delSrc === 'admin';
                      _wasBonusTicket = _delSrc === 'ticket';
                      _wasOrigDmg = (typeof _orig.dmg === 'number') ? _orig.dmg : 0;
                      _hist[battleIndex] = Object.assign({}, _orig, {
                        _deletedAt: Date.now(),
                        _deletedReason: String(reason || '').slice(0, 200) || '(GM 未填寫原因)',
                        _origDmg: _wasOrigDmg,
                        dmg: 0,
                      });
                    }
                    _ent.battleHistory = _hist;
                    _ent.totalDmg = result.newTotalDmg;
                    if(_wasBonusBattle){
                      _ent.bonusBattleCount = Math.max(0, (_ent.bonusBattleCount||0) - 1);
                    }
                    if(_wasBonusTicket){
                      _ent.bonusTicketCount = Math.max(0, (_ent.bonusTicketCount||0) - 1);
                    }
                  }
                }
              }
              document.dispatchEvent(new CustomEvent('wbLeaderboardSynced'));
            }catch(_){}
          }
          console.log('[WB-HpSync] markBattleAsDeleted', result);
          return result;
        }catch(e){
          console.error('[WB-HpSync] markBattleAsDeleted 失敗', e);
          return null;
        }
      }

      // 暴露 API
      window._wbHpSync = {
        subscribe: subscribeWbHp,
        getCurrentHp: getCurrentHp,
        dealDamage: dealDamage,
        resetHp: resetHp,
        // ★ FIX 20260519(v12) — 排行榜 API
        updateLeaderboard: updateLeaderboard,
        getLeaderboard: getLeaderboard,
        // ★ v3.5.20 — 管理員清除排行榜(整個 BOSS 或全清)
        clearLeaderboard: clearLeaderboard,
        // ★ v3.5.22 — 管理員指定刪除特定紀錄(處理異常/BUG 戰績)
        clearLeaderboardEntries: clearLeaderboardEntries,
        // ★ v3.10.18 — 刪單筆 battleHistory(精準砍異常傷害)
        removeBattleFromHistory: removeBattleFromHistory,
        // ★ v3.13.3 — 墓碑模式刪除(留紀錄、紅字標記)
        markBattleAsDeleted: markBattleAsDeleted,
      };
      try{ subscribeWbHp(); }catch(_){}
      console.log('[WB-HpSync] ✅ 世界 BOSS 殘血同步模組已掛載');
    })();

    // ════════════════════════════════════════════════════════════════════
    // ★ v3.5.64(2026-05-23) — 本週小博士排行榜 v2(完整版)
    //
    //   老師最終確認需求:
    //     ① 累計來源:冒險關卡 + 世界 BOSS + 知識王「全部不重複題目」答對數
    //                (玩家自己的不重複池,跟 _persistentCorrectQuestions 對齊)
    //     ② 結算時間:每週一 08:00(台灣時區),自動發送上週前 10 名獎勵
    //     ③ 入口位置:知識王挑戰 popup 右邊空白處
    //     ④ 排名範圍:不分班,顯示玩家暱稱 + 當週累積不重複答對題數
    //     ⑤ 雲端更新頻率:每小時 1 次(本地累計 + flush)— 避免爆 Firebase 配額
    //     ⑥ 獎勵分檔(老師指定):
    //        🥇 第 1 名     :50000 幣 + 召喚水晶 ×10 + 豪華典藏經驗書 ×10
    //        🥈 第 2-5 名   :35000 幣 + 召喚水晶 × 6 + 豪華典藏經驗書 × 6
    //        🥉 第 6-10 名  :20000 幣 + 召喚水晶 × 3 + 豪華典藏經驗書 × 3
    //     ⑦ 防重複機制(三重保險):
    //        - settlement[週key] transaction 鎖(第一個觸發的玩家寫入,後續略過)
    //        - 每位玩家 pendingAward.claimedAt 旗標(領過就不再領)
    //        - 本地 lxps_wq_claimed_[週key] localStorage 旗標(雙重保險)
    //
    //   ──────────────────────────────────────────────────────────────
    //   資料結構
    //   ──────────────────────────────────────────────────────────────
    //
    //   本地 localStorage(每位玩家獨立、用 uid 後綴):
    //     lxps_wq_local_<uid> = {
    //       weekKey: "2026-W21",
    //       correctCount: 45,         // 不重複累計
    //       lastFlushAt: 1716480000000,
    //       flushedBaseline: 40,      // 上次寫入雲端時的值(算 diff 用)
    //       lastAt: 1716480500000,    // 最後答對時間
    //     }
    //
    //   Firestore stats/global.weeklyQuiz:
    //     [週key]: {
    //       [uid]: { name, email, correct, lastAt }
    //     }
    //
    //   Firestore stats/global.weeklyQuizSettlement(防重複結算鎖):
    //     [週key]: {
    //       settledAt, settledBy: uid, awards: { [uid]: { rank, coins, crystals, books } }
    //     }
    //
    //   Firestore players/<uid>.weeklyQuizPendingAward(未領獎勵):
    //     { weekKey, rank, coins, crystals, books, createdAt, claimedAt: null }
    //
    //   ──────────────────────────────────────────────────────────────
    //   暴露 API
    //   ──────────────────────────────────────────────────────────────
    //
    //   window._weeklyQuiz = {
    //     getWeekKey:        () => string,
    //     getLastWeekKey:    () => string,
    //     getWeekRange:      () => { start, end },
    //     recordCorrect:     (subject, qid) => void,  // 答對 1 題,只更新本地
    //     flushNow:          () => Promise<void>,      // 立即 flush 本地累計到雲端
    //     getTopN:           (n=50) => [...],
    //     getMyRank:         () => { rank, correct, total, count },
    //     trySettleLastWeek: () => Promise<settled?>,  // 結算上週(會被 onAuth 自動呼叫)
    //     claimPendingAward: () => Promise<award?>,    // 領取我的 pending 獎勵
    //   };
    // ════════════════════════════════════════════════════════════════════
    (function _setupWeeklyQuizV2(){
      const STATS_DOC = () => doc(_fbDb, 'stats', 'global');
      const PLAYER_DOC = (uid) => doc(_fbDb, 'players', uid);

      // ── 取得「當前週 key」(YYYY-WW,以台灣時區週一 08:00 為跨週基準) ──
      function _calcWeekKey(date){
        const d = date || new Date();
        // 轉成「台灣時區的時間」用 UTC method 讀
        const tw = new Date(d.getTime() + (8 * 3600 * 1000) - (d.getTimezoneOffset() * 60 * 1000));
        const dow = tw.getUTCDay();     // 0=週日,1=週一
        const hour = tw.getUTCHours();
        let offsetDays;
        if(dow === 0) offsetDays = 6;             // 週日 → 屬上週
        else if(dow === 1 && hour < 8) offsetDays = 7;  // 週一 08:00 之前 → 屬上週
        else offsetDays = dow - 1;
        const monday = new Date(tw.getTime() - offsetDays * 86400 * 1000);
        monday.setUTCHours(8, 0, 0, 0);
        // year + ISO 週數
        const year = monday.getUTCFullYear();
        const yearStart = new Date(Date.UTC(year, 0, 1));
        const dayDiff = Math.floor((monday.getTime() - yearStart.getTime()) / 86400000);
        const weekNum = Math.floor(dayDiff / 7) + 1;
        return year + '-W' + (weekNum < 10 ? '0' : '') + weekNum;
      }

      function getWeekKey(){ return _calcWeekKey(); }
      function getLastWeekKey(){
        // 「上週」= 現在減 7 天的週 key
        return _calcWeekKey(new Date(Date.now() - 7 * 86400 * 1000));
      }
      function getWeekRange(date){
        const d = date || new Date();
        const tw = new Date(d.getTime() + (8 * 3600 * 1000) - (d.getTimezoneOffset() * 60 * 1000));
        const dow = tw.getUTCDay();
        const hour = tw.getUTCHours();
        let offsetDays;
        if(dow === 0) offsetDays = 6;
        else if(dow === 1 && hour < 8) offsetDays = 7;
        else offsetDays = dow - 1;
        const start = new Date(tw.getTime() - offsetDays * 86400 * 1000);
        start.setUTCHours(8, 0, 0, 0);
        const end = new Date(start.getTime() + 7 * 86400 * 1000);
        return {
          start: new Date(start.getTime() - (8 * 3600 * 1000) + (d.getTimezoneOffset() * 60 * 1000)),
          end:   new Date(end.getTime()   - (8 * 3600 * 1000) + (d.getTimezoneOffset() * 60 * 1000)),
        };
      }

      // ── 本地 storage ──
      function _localKey(){
        const uid = window._gUserId || 'guest';
        return 'lxps_wq_local_' + uid;
      }
      function _readLocal(){
        try{
          const raw = localStorage.getItem(_localKey());
          if(!raw) return null;
          const obj = JSON.parse(raw);
          if(!obj || typeof obj !== 'object') return null;
          return obj;
        }catch(_){ return null; }
      }
      function _writeLocal(obj){
        try{ localStorage.setItem(_localKey(), JSON.stringify(obj)); }catch(_){}
      }

      // ── 取得當前玩家本週的「本地紀錄」(自動切換週) ──
      function _getOrInitLocal(){
        const _wk = getWeekKey();
        let _obj = _readLocal();
        if(!_obj || _obj.weekKey !== _wk){
          // 新週開始 → 重置本地
          _obj = {
            weekKey: _wk,
            correctCount: 0,
            flushedBaseline: 0,
            lastFlushAt: 0,
            lastAt: 0,
          };
          _writeLocal(_obj);
        }
        return _obj;
      }

      // ── 答對 1 題:只更新本地,不立即寫雲端 ──
      //   注意:hook 點在 _persistentCorrectQuestions[科目][題id] = true 第一次設定時呼叫
      //         hook 那一側已保證「不重複」,所以這裡無條件 +1 即可
      function recordCorrect(subject, qid){
        try{
          if(!window._gUserId){
            // 未登入訪客也累計到本地(等登入時會被新的 uid 蓋掉,沒關係)
          }
          const _obj = _getOrInitLocal();
          const _wasZero = (_obj.correctCount || 0) === 0;
          _obj.correctCount += 1;
          _obj.lastAt = Date.now();
          _writeLocal(_obj);
          // ★ v3.5.66 — 本週小博士獎章檢查(每答對 1 題就檢查累計題數獎章)
          //   注意:_medalStats / _checkMedalWqXXX 在外部作用域,可直接呼叫
          try{
            if(typeof _checkMedalWqWeeklyCount === 'function'){
              _checkMedalWqWeeklyCount(_obj.correctCount);
            }
            if(typeof _checkMedalWqLifetimeAdd === 'function'){
              _checkMedalWqLifetimeAdd(1);  // 每答對 1 不重複題目 → 歷史累計 +1
            }
          }catch(_medalE){ console.warn('[WeeklyQuiz medal hook]', _medalE); }
          // ★ v3.5.65 — 本週第 1 題立刻 flush(讓玩家馬上看到上榜),其餘按 1 小時節流
          const _now = Date.now();
          if(_wasZero){
            // 本週開張第一題 → 立即 flush
            flushNow().catch(function(e){ console.warn('[WeeklyQuiz] first flush 失敗', e); });
          } else if(_now - (_obj.lastFlushAt || 0) >= 3600 * 1000){
            // 每答對都檢查是否該 flush(間隔 ≥ 1 小時)
            // 不 await,失敗不影響玩家體驗
            flushNow().catch(function(e){ console.warn('[WeeklyQuiz] hourly flush 失敗', e); });
          }
        }catch(e){
          console.warn('[WeeklyQuiz] recordCorrect 失敗', e);
        }
      }

      // ── flush 本地累計到雲端(用 increment 安全寫入差額) ──
      let _flushInFlight = false;
      async function flushNow(){
        if(_flushInFlight) return;       // 防重入
        if(!window._gUserId){
          console.log('[WeeklyQuiz] flush 跳過(未登入)');
          return;
        }
        // ★ v3.11.10(2026-05-28) — 管理員不計入小博士排行榜
        //   守門條件:當前登入帳號是管理員 → 不寫雲端,本地紀錄仍保留(避免破壞 _getOrInitLocal 邏輯)
        //   設計考量:管理員測試題庫、debug 時答對題目不該污染學生競賽榜
        try{
          if(typeof window._isAdminUser === 'function' && window._isAdminUser()){
            // 將本地進度標記為「已 flush」避免無限呼叫 flushNow,但實際雲端不寫
            try{
              const _obj = _getOrInitLocal();
              _obj.flushedBaseline = _obj.correctCount;
              _obj.lastFlushAt = Date.now();
              _writeLocal(_obj);
            }catch(_){}
            console.log('[WeeklyQuiz v3.11.10] 管理員身分,跳過雲端 flush(不列入小博士排行榜)');
            return;
          }
        }catch(_eAdmGate){ console.warn('[WeeklyQuiz v3.11.10] 管理員守門例外', _eAdmGate); }
        _flushInFlight = true;
        try{
          const _obj = _getOrInitLocal();
          const _diff = (_obj.correctCount || 0) - (_obj.flushedBaseline || 0);
          if(_diff <= 0){
            // 沒有新進度
            _obj.lastFlushAt = Date.now();
            _writeLocal(_obj);
            return;
          }
          const _uid = window._gUserId;
          const _email = (window._fbAuth && window._fbAuth.currentUser && window._fbAuth.currentUser.email) || '';
          // ★ v3.15.69 — 寫入排行榜前先套玩家可見遮罩格式(名冊「5408陳O彬」/「5408暱稱」、lsps 無名冊「137…」),避免真名外洩
          const _nameRaw = window._playerNickname || '玩家';
          const _name = (typeof window._formatPlayerDisplayName === 'function')
            ? (window._formatPlayerDisplayName(_nameRaw, _email) || _nameRaw) : _nameRaw;
          const _wk = _obj.weekKey;
          await runTransaction(_fbDb, async (tx) => {
            const _snap = await tx.get(STATS_DOC());
            const _data = _snap.exists() ? (_snap.data() || {}) : {};
            const _wq = _data.weeklyQuiz || {};
            const _weekMap = _wq[_wk] || {};
            const _prev = _weekMap[_uid] || { correct: 0, name: '', email: '' };
            // ★ v3.5.68 — email/name 不空覆蓋:如果本次 _email/_name 空,保留 _prev 既有的
            //   修補:有時 _fbAuth.currentUser 一瞬間不可用(reauth/refresh 中),寫進空字串
            //         反而會把雲端原本正確的 email 蓋掉,造成排行榜顯示「***同學」
            const _next = {
              name: _name || _prev.name || '玩家',
              email: (_email || '').toLowerCase() || _prev.email || '',
              correct: (_prev.correct || 0) + _diff,
              lastAt: Date.now(),
            };
            const _newWeekMap = Object.assign({}, _weekMap);
            _newWeekMap[_uid] = _next;
            const _newWq = Object.assign({}, _wq);
            _newWq[_wk] = _newWeekMap;
            tx.set(STATS_DOC(), { weeklyQuiz: _newWq }, { merge: true });
          });
          // 寫成功才更新 baseline
          _obj.flushedBaseline = _obj.correctCount;
          _obj.lastFlushAt = Date.now();
          _writeLocal(_obj);
          console.log('[WeeklyQuiz] flush ✅ +' + _diff + ' (週=' + _wk + ', 累計=' + _obj.correctCount + ')');
        }catch(e){
          console.warn('[WeeklyQuiz] flushNow 失敗', e);
        }finally{
          _flushInFlight = false;
        }
      }

      // ── 取得 Top N(從 _cachedGlobalStats 訂閱快取讀) ──
      function getTopN(n){
        n = n || 50;
        try{
          const _wk = getWeekKey();
          const _gs = window._cachedGlobalStats || {};
          const _weekMap = (_gs.weeklyQuiz && _gs.weeklyQuiz[_wk]) || {};
          // ★ v3.11.10(2026-05-28) — 排除管理員(顯示用,本週即時排行)
          const _adminEmails = new Set(['clarebox123@gmail.com', 'clarebox123@lsps.tp.edu.tw', 'asemia@gmail.com']);
          const _list = [];
          for(const _uid in _weekMap){
            const _e = _weekMap[_uid];
            if(!_e || (_e.correct || 0) <= 0) continue;
            // ★ v3.11.10 — 排除管理員
            const _eEmail = (_e.email || '').toLowerCase();
            if(_adminEmails.has(_eEmail)) continue;
            _list.push({
              uid: _uid,
              name: _e.name || '玩家',
              email: _e.email || '',
              correct: _e.correct || 0,
              lastAt: _e.lastAt || 0,
            });
          }
          _list.sort(function(a, b){
            if(a.correct !== b.correct) return b.correct - a.correct;
            return (a.lastAt || 0) - (b.lastAt || 0);
          });
          return _list.slice(0, n);
        }catch(e){
          console.warn('[WeeklyQuiz] getTopN 失敗', e);
          return [];
        }
      }

      // ── 我的排名 ──
      //   注意:雲端排名 + 本地 correctCount(本地比較新)
      function getMyRank(){
        try{
          const _all = getTopN(9999);
          const _myUid = window._gUserId;
          // 從雲端清單裡找我
          const _idx = _myUid ? _all.findIndex(function(e){ return e.uid === _myUid; }) : -1;
          // 用本地的最新 count(雲端可能還沒 flush)
          const _local = _readLocal();
          const _localCount = (_local && _local.weekKey === getWeekKey()) ? (_local.correctCount || 0) : 0;
          // 顯示用:本地 count > 雲端 count → 用本地(代表還沒 flush 上去)
          const _cloudCount = _idx >= 0 ? _all[_idx].correct : 0;
          const _displayCount = Math.max(_cloudCount, _localCount);
          return {
            rank: _idx >= 0 ? (_idx + 1) : 0,
            correct: _displayCount,   // 用本地較新值
            cloudCorrect: _cloudCount,
            total: _all.length,
          };
        }catch(e){
          console.warn('[WeeklyQuiz] getMyRank 失敗', e);
          return { rank: 0, correct: 0, cloudCorrect: 0, total: 0 };
        }
      }

      // ── 獎勵分檔(老師指定) ──
      function _calcAwardByRank(rank){
        if(rank === 1)            return { coins: 50000, crystals: 10, books: 10 };
        if(rank >= 2 && rank <= 5) return { coins: 35000, crystals:  6, books:  6 };
        if(rank >= 6 && rank <= 10) return { coins: 20000, crystals:  3, books:  3 };
        return null;
      }

      // ── 結算上週前 10 名(三重防重複) ──
      //   觸發時機:登入後 + 進入排行榜頁面時 + 每小時 flush 時順便檢查
      //   防重複機制:
      //     1) Transaction:若 settlement[上週key] 已存在 → 直接 return(別人已結算)
      //     2) Transaction 同時把獎勵寫入 stats/global.weeklyQuizSettlement[週key].awards
      //     3) 為每位上榜玩家寫 pendingAward 到 players/<uid>.weeklyQuizPendingAward
      async function trySettleLastWeek(){
        if(!window._gUserId) return false;
        const _lastWk = getLastWeekKey();
        try{
          // Phase 1:用 transaction 嘗試搶結算鎖
          const _result = await runTransaction(_fbDb, async (tx) => {
            const _snap = await tx.get(STATS_DOC());
            const _data = _snap.exists() ? (_snap.data() || {}) : {};
            const _settled = _data.weeklyQuizSettlement || {};
            if(_settled[_lastWk]){
              return { settled: false, reason: 'already_settled' };
            }
            // 從上週的 weeklyQuiz 資料抽出 top 10
            const _wq = _data.weeklyQuiz || {};
            const _weekMap = _wq[_lastWk] || {};
            // ★ v3.11.10(2026-05-28) — 排除管理員的舊資料(雙保險)
            //   背景:本週起 flushNow 已加管理員守門,但歷史資料可能已有管理員成績污染
            //   排除清單同 _isAdminUser:clarebox123@gmail.com、clarebox123@lsps.tp.edu.tw、asemia@gmail.com
            const _adminEmails = new Set(['clarebox123@gmail.com', 'clarebox123@lsps.tp.edu.tw', 'asemia@gmail.com']);
            const _list = [];
            for(const _uid in _weekMap){
              const _e = _weekMap[_uid];
              if(!_e || (_e.correct || 0) <= 0) continue;
              // ★ v3.11.10 — 排除管理員
              const _eEmail = (_e.email || '').toLowerCase();
              if(_adminEmails.has(_eEmail)){
                console.log('[WeeklyQuiz v3.11.10] 排名計算跳過管理員 ' + _eEmail);
                continue;
              }
              _list.push({ uid: _uid, name: _e.name||'玩家', email: _e.email||'', correct: _e.correct||0, lastAt: _e.lastAt||0 });
            }
            _list.sort(function(a, b){
              if(a.correct !== b.correct) return b.correct - a.correct;
              return (a.lastAt || 0) - (b.lastAt || 0);
            });
            const _top10 = _list.slice(0, 10);
            if(_top10.length === 0){
              // 沒人上榜,寫入空結算記錄避免重複觸發
              const _newSettled = Object.assign({}, _settled);
              _newSettled[_lastWk] = {
                settledAt: Date.now(),
                settledBy: window._gUserId,
                awards: {},
                empty: true,
              };
              tx.set(STATS_DOC(), { weeklyQuizSettlement: _newSettled }, { merge: true });
              return { settled: true, awards: {}, empty: true };
            }
            // 算每位的獎勵
            const _awards = {};
            _top10.forEach(function(e, i){
              const _rank = i + 1;
              const _award = _calcAwardByRank(_rank);
              if(_award){
                _awards[e.uid] = Object.assign({ rank: _rank, name: e.name, correct: e.correct }, _award);
              }
            });
            // 把結算結果寫到 stats/global
            const _newSettled = Object.assign({}, _settled);
            _newSettled[_lastWk] = {
              settledAt: Date.now(),
              settledBy: window._gUserId,
              awards: _awards,
            };
            tx.set(STATS_DOC(), { weeklyQuizSettlement: _newSettled }, { merge: true });
            return { settled: true, awards: _awards };
          });

          if(!_result.settled){
            // 別人已結算,但我可能還是上榜了 → 直接走 phase 2 看雲端有沒有給我發 pending
            return false;
          }

          // Phase 2:為每位上榜玩家寫 pendingAward
          //   ⚠ 這一步只有第一個觸發結算的玩家(_result.settled === true)會跑
          //   且為了避免 client 一次寫很多筆失敗,改用 batch 但分批
          if(_result.empty){
            console.log('[WeeklyQuiz] ✅ 上週(' + _lastWk + ') 無人上榜,跳過獎勵發放');
            return true;
          }
          const _awards = _result.awards || {};
          const _uids = Object.keys(_awards);
          console.log('[WeeklyQuiz] 🎁 開始發放上週(' + _lastWk + ')獎勵給 ' + _uids.length + ' 位玩家');
          let _failedUids = [];
          for(const _uid of _uids){
            try{
              const _award = _awards[_uid];
              await setDoc(PLAYER_DOC(_uid), {
                weeklyQuizPendingAward: {
                  weekKey: _lastWk,
                  rank: _award.rank,
                  coins: _award.coins,
                  crystals: _award.crystals,
                  books: _award.books,
                  correct: _award.correct,
                  createdAt: Date.now(),
                  claimedAt: null,
                }
              }, { merge: true });
              console.log('  ✅ ' + _uid + ' 第' + _award.rank + '名:' + _award.coins + '幣 / ' + _award.crystals + '水晶 / ' + _award.books + '書');
            }catch(_e){
              const _code = _e && _e.code;
              if(_code === 'permission-denied'){
                // ★ v3.5.67 — Firestore rules 沒開放寫別人的存檔
                //   需要的 rules(老師上 Firebase Console 設定):
                //   match /players/{playerUid} {
                //     allow read: if request.auth != null && request.auth.uid == playerUid;
                //     allow write: if request.auth != null && (
                //       request.auth.uid == playerUid
                //       || request.resource.data.diff(resource.data).affectedKeys()
                //          .hasOnly(['weeklyQuizPendingAward'])
                //     );
                //   }
                //   只允許「其他玩家」更新 weeklyQuizPendingAward 一個欄位,其他欄位禁
                console.warn('  ⚠ ' + _uid + ' pending 寫入被 rules 拒絕(預期內,需老師設定 Firestore Rules)');
              } else {
                console.warn('  ❌ ' + _uid + ' pending 寫入失敗', _e);
              }
              _failedUids.push(_uid);
              // 不停下,繼續發其他人
            }
          }
          if(_failedUids.length > 0){
            console.warn('[WeeklyQuiz] ⚠ ' + _failedUids.length + ' 位玩家發獎失敗,'
              + '請老師到管理員後台手動補發(或設定 Firestore Rules 開放 weeklyQuizPendingAward 跨玩家寫入)');
            // 把失敗清單暴露到 window 給老師後台補發用
            window._weeklyQuizSettlementFailed = {
              weekKey: _lastWk,
              failedUids: _failedUids,
              awards: _awards,
              at: Date.now(),
            };
          }
          return true;
        }catch(e){
          console.warn('[WeeklyQuiz] trySettleLastWeek 失敗', e);
          return false;
        }
      }

      // ── 玩家領取自己的 pending 獎勵 ──
      //   防重複:① pendingAward.claimedAt 不是 null → 已領 ② 本地 lxps_wq_claimed_<週key> 旗標
      async function claimPendingAward(){
        if(!window._gUserId) return null;
        try{
          const _snap = await getDoc(PLAYER_DOC(window._gUserId));
          if(!_snap.exists()) return null;
          const _data = _snap.data() || {};
          const _pending = _data.weeklyQuizPendingAward;
          if(!_pending) return null;
          if(_pending.claimedAt){
            // 雲端已標記領過
            return null;
          }
          // 檢查本地旗標
          const _localFlag = 'lxps_wq_claimed_' + _pending.weekKey;
          try{
            if(localStorage.getItem(_localFlag) === '1'){
              // 本地已標,可能上次寫雲端失敗,再寫一次雲端旗標
              await setDoc(PLAYER_DOC(window._gUserId), {
                weeklyQuizPendingAward: Object.assign({}, _pending, { claimedAt: Date.now() })
              }, { merge: true });
              return null;
            }
          }catch(_){}

          // 用 transaction 領獎(防多 tab 同時觸發)
          const _result = await runTransaction(_fbDb, async (tx) => {
            const _txSnap = await tx.get(PLAYER_DOC(window._gUserId));
            const _txData = _txSnap.exists() ? (_txSnap.data() || {}) : {};
            const _txPending = _txData.weeklyQuizPendingAward;
            if(!_txPending || _txPending.claimedAt){
              return null;  // 已被別的 tab 領了
            }
            // 計算新值
            const _curCoins = _txData.knowledgeCoins || 0;
            const _curBp = (_txData.playerBackpack && typeof _txData.playerBackpack === 'object') ? _txData.playerBackpack : {};
            const _newBp = Object.assign({}, _curBp);
            _newBp['summon_crystal'] = Math.min(99, (_newBp['summon_crystal'] || 0) + (_txPending.crystals || 0));
            _newBp['hero_exp_book_deluxe'] = Math.min(99, (_newBp['hero_exp_book_deluxe'] || 0) + (_txPending.books || 0));
            const _newCoins = _curCoins + (_txPending.coins || 0);
            tx.set(PLAYER_DOC(window._gUserId), {
              knowledgeCoins: _newCoins,
              playerBackpack: _newBp,
              playerBackpack_s: JSON.stringify(_newBp),
              weeklyQuizPendingAward: Object.assign({}, _txPending, { claimedAt: Date.now() }),
            }, { merge: true });
            return Object.assign({}, _txPending);
          });

          if(_result){
            // 寫本地旗標
            try{ localStorage.setItem(_localFlag, '1'); }catch(_){}
            // 同步本地遊戲狀態(玩家當下也要看到變化)
            try{
              if(typeof _knowledgeCoins !== 'undefined' && _result.coins){
                window._knowledgeCoins = (_knowledgeCoins || 0) + (_result.coins || 0);
              }
              if(typeof _playerBackpack === 'object' && _playerBackpack){
                if(_result.crystals > 0){
                  _playerBackpack['summon_crystal'] = Math.min(99, (_playerBackpack['summon_crystal']||0) + _result.crystals);
                }
                if(_result.books > 0){
                  _playerBackpack['hero_exp_book_deluxe'] = Math.min(99, (_playerBackpack['hero_exp_book_deluxe']||0) + _result.books);
                }
              }
              if(typeof _syncSummonCrystalDisplay === 'function') _syncSummonCrystalDisplay();
              if(typeof refreshAllCoinsDisplay === 'function') refreshAllCoinsDisplay();
            }catch(_syncE){ console.warn('[WeeklyQuiz claim] 本地同步失敗', _syncE); }
            // ★ v3.5.66 — 領到上週獎勵時觸發小博士排名獎章檢查
            //   注意:_result.rank 由前面 transaction 中讀的 _txPending.rank 帶出
            try{
              if(typeof _checkMedalWqRank === 'function' && _result.rank){
                _checkMedalWqRank(_result.rank);
                console.log('[獎章 v3.5.66] 小博士結算:我的排名 = 第', _result.rank, '名');
              }
            }catch(_eRkW){ console.warn('[獎章 wq rank check]', _eRkW); }
          }
          return _result;
        }catch(e){
          console.warn('[WeeklyQuiz] claimPendingAward 失敗', e);
          return null;
        }
      }

      // ── 訂閱 stats/global 把 weeklyQuiz 同步到 _cachedGlobalStats ──
      let _wqUnsub = null;
      function _subscribe(){
        if(_wqUnsub) return;
        try{
          _wqUnsub = onSnapshot(STATS_DOC(), function(snap){
            try{
              const d = snap.exists() ? (snap.data() || {}) : {};
              if(!window._cachedGlobalStats) window._cachedGlobalStats = {};
              window._cachedGlobalStats.weeklyQuiz = d.weeklyQuiz || {};
              window._cachedGlobalStats.weeklyQuizSettlement = d.weeklyQuizSettlement || {};
              // ★ v3.13.60 — 鬥技場 GM 開關旗標同步(商店預設開、排名發獎預設關)
              window._cachedGlobalStats.arenaShopOpen = (typeof d.arenaShopOpen === 'boolean') ? d.arenaShopOpen : true;
              window._cachedGlobalStats.arenaRankRewardEnabled = (typeof d.arenaRankRewardEnabled === 'boolean') ? d.arenaRankRewardEnabled : false;
              // ★ v3.13.61 — 鬥技場每週排名:週累計地圖 + 結算包(供排名模組結算/領獎/顯示)
              window._cachedGlobalStats.arenaWeekly = d.arenaWeekly || {};
              window._cachedGlobalStats.arenaRankSettlement = d.arenaRankSettlement || {};
              try{ document.dispatchEvent(new CustomEvent('weeklyQuizSynced')); }catch(_){}
            }catch(eIn){ console.warn('[WeeklyQuiz] onSnapshot 處理失敗', eIn); }
          }, function(err){
            console.warn('[WeeklyQuiz] 訂閱失敗', err);
          });
        }catch(e){
          console.warn('[WeeklyQuiz] _subscribe 例外', e);
        }
      }

      // ── 5 分鐘 setInterval 兜底:跨小時自動 flush ──
      setInterval(function(){
        try{
          if(!window._gUserId) return;
          const _obj = _readLocal();
          if(!_obj) return;
          const _now = Date.now();
          // 距離上次 flush 超過 1 小時 + 有新進度 → flush
          if(_now - (_obj.lastFlushAt || 0) >= 3600 * 1000
             && (_obj.correctCount || 0) > (_obj.flushedBaseline || 0)){
            flushNow().catch(function(e){ console.warn('[WeeklyQuiz] interval flush 失敗', e); });
          }
        }catch(_){}
      }, 5 * 60 * 1000);  // 5 分鐘檢查一次

      // ── beforeunload:玩家關閉前最後 flush ──
      // ★ v3.5.65(2026-05-23) — 老師需求:三層保險,涵蓋所有離場情境
      //   ① beforeunload — 標準瀏覽器關閉/重整(已有)
      //   ② visibilitychange→hidden — 切 tab、平板切 app(iPad Safari 主要靠這個)
      //   ③ pagehide — iOS Safari 點返回按鈕(beforeunload 在 iOS 不可靠)
      //   注意:三個事件可能會連續觸發,flushNow 內 _flushInFlight 旗標會防重入
      function _flushOnExit(reason){
        try{
          const _obj = _readLocal();
          if(_obj && (_obj.correctCount || 0) > (_obj.flushedBaseline || 0)){
            console.log('[WeeklyQuiz] 離場 flush 觸發:' + reason);
            // 不能 await(可能 1~2 秒就被瀏覽器中止)
            flushNow().catch(function(){});
          }
        }catch(_){}
      }
      try{
        window.addEventListener('beforeunload', function(){ _flushOnExit('beforeunload'); });
        window.addEventListener('pagehide',    function(){ _flushOnExit('pagehide');    });
        document.addEventListener('visibilitychange', function(){
          if(document.visibilityState === 'hidden'){
            _flushOnExit('visibilitychange→hidden');
          }
        });
      }catch(_){}

      // 暴露 API
      window._weeklyQuiz = {
        getWeekKey: getWeekKey,
        getLastWeekKey: getLastWeekKey,
        getWeekRange: getWeekRange,
        recordCorrect: recordCorrect,
        flushNow: flushNow,
        getTopN: getTopN,
        getMyRank: getMyRank,
        trySettleLastWeek: trySettleLastWeek,
        claimPendingAward: claimPendingAward,
      };

      // ── 啟動訂閱 ──
      try{ _subscribe(); }catch(_){}
      console.log('[WeeklyQuiz v2] ✅ 本週小博士排行榜模組已掛載');
    })();

    // ════════════════════════════════════════════════════════════════════
    // ★ v3.13.61(2026-06-05)— 鬥技場「每週排名獎勵」自動結算 + 自助領取
    //   仿 weeklyQuiz/wbSettle,但改用「結算包整包寫 stats/global、玩家自助領取」,
    //   避免對近 900 位玩家逐一跨文件寫(老師獎勵表 11 名以後也發 → 幾乎全員有獎)。
    //     ・每場戰鬥得到的鬥技之證 → 本地累計本週 → 第一次立刻、之後每小時 flush 到
    //       stats/global.arenaWeekly[週][uid] = { name, email, zheng, lastAt }
    //     ・每週一 08:00(台灣)結算上週:依本週鬥技之證排名 → 全部寫進
    //       stats/global.arenaRankSettlement[週] = { settledAt, count, awards{uid:{rank,crystals,zheng,coins}} }
    //     ・玩家登入讀自己 uid 的 award → 領到自己文件(零跨玩家寫、免 Firestore Rules)
    //     ・獎勵(老師指定):1名 水晶15/證15/幣3萬・2-5名 水晶10/證10/幣2萬・
    //       6-10名 水晶5/證5/幣1萬・11名以後 水晶2/證2/幣5000
    //     ・GM 開關:_arenaIsRankRewardEnabled() === false → 不結算(不發獎)
    // ════════════════════════════════════════════════════════════════════
    (function _setupArenaRankSettlement(){
      const STATS_DOC = () => doc(_fbDb, 'stats', 'global');
      const PLAYER_DOC = (uid) => doc(_fbDb, 'players', uid);
      const _ADMIN_EMAILS = new Set(['clarebox123@gmail.com', 'clarebox123@lsps.tp.edu.tw', 'asemia@gmail.com']);

      // 週鍵(台灣時區、週一 08:00 為界、ISO 週數)— 與 weeklyQuiz 同邏輯
      function _calcWeekKey(date){
        const d = date || new Date();
        const tw = new Date(d.getTime() + (8 * 3600 * 1000) - (d.getTimezoneOffset() * 60 * 1000));
        const dow = tw.getUTCDay(); const hour = tw.getUTCHours();
        let offsetDays;
        if(dow === 0) offsetDays = 6;
        else if(dow === 1 && hour < 8) offsetDays = 7;
        else offsetDays = dow - 1;
        const monday = new Date(tw.getTime() - offsetDays * 86400 * 1000);
        const year = monday.getUTCFullYear();
        const yearStart = new Date(Date.UTC(year, 0, 1));
        const dayDiff = Math.floor((monday.getTime() - yearStart.getTime()) / 86400000);
        const weekNum = Math.floor(dayDiff / 7) + 1;
        return year + '-W' + (weekNum < 10 ? '0' : '') + weekNum;
      }
      function getWeekKey(){ return _calcWeekKey(); }
      function getLastWeekKey(){ return _calcWeekKey(new Date(Date.now() - 7 * 86400 * 1000)); }

      // ── 本地週累計 ──
      function _localKey(){ return 'lxps_arena_wk_local_' + (window._gUserId || 'guest'); }
      function _readLocal(){ try{ const r = localStorage.getItem(_localKey()); return r ? JSON.parse(r) : null; }catch(_){ return null; } }
      function _writeLocal(o){ try{ localStorage.setItem(_localKey(), JSON.stringify(o)); }catch(_){} }
      function _getOrInitLocal(){
        const wk = getWeekKey(); let o = _readLocal();
        if(!o || o.weekKey !== wk){ o = { weekKey: wk, zheng: 0, flushedBaseline: 0, lastFlushAt: 0, lastAt: 0 }; _writeLocal(o); }
        return o;
      }

      // ── 得到鬥技之證:本地累計 + flush(本週第一次立刻、其餘每小時) ──
      function addZheng(amount){
        try{
          amount = Math.max(0, Math.round(Number(amount) || 0));
          if(amount <= 0) return;
          const o = _getOrInitLocal();
          const wasZero = (o.zheng || 0) === 0;
          o.zheng += amount; o.lastAt = Date.now(); _writeLocal(o);
          const now = Date.now();
          if(wasZero) flushNow().catch(function(){});
          else if(now - (o.lastFlushAt || 0) >= 3600 * 1000) flushNow().catch(function(){});
        }catch(e){ console.warn('[ArenaRank] addZheng 失敗', e); }
      }

      let _flushInFlight = false;
      async function flushNow(){
        if(_flushInFlight) return;
        if(!window._gUserId) return;
        // 管理員不列入排行榜(本地仍標記避免無限呼叫)
        try{
          if(typeof window._isAdminUser === 'function' && window._isAdminUser()){
            const o = _getOrInitLocal(); o.flushedBaseline = o.zheng; o.lastFlushAt = Date.now(); _writeLocal(o); return;
          }
        }catch(_){}
        _flushInFlight = true;
        try{
          const o = _getOrInitLocal();
          const diff = (o.zheng || 0) - (o.flushedBaseline || 0);
          if(diff <= 0){ o.lastFlushAt = Date.now(); _writeLocal(o); return; }
          const uid = window._gUserId;
          const email = (window._fbAuth && window._fbAuth.currentUser && window._fbAuth.currentUser.email) || '';
          // ★ v3.15.69 — 寫入排行榜前先套玩家可見遮罩格式,避免真名外洩
          const nameRaw = window._playerNickname || '玩家';
          const name = (typeof window._formatPlayerDisplayName === 'function')
            ? (window._formatPlayerDisplayName(nameRaw, email) || nameRaw) : nameRaw;
          const wk = o.weekKey;
          await runTransaction(_fbDb, async (tx) => {
            const snap = await tx.get(STATS_DOC());
            const data = snap.exists() ? (snap.data() || {}) : {};
            const aw = data.arenaWeekly || {};
            const weekMap = aw[wk] || {};
            const prev = weekMap[uid] || { zheng: 0, name: '', email: '' };
            const next = {
              name: name || prev.name || '玩家',
              email: (email || '').toLowerCase() || prev.email || '',
              zheng: (prev.zheng || 0) + diff,
              lastAt: Date.now(),
            };
            const newWeekMap = Object.assign({}, weekMap); newWeekMap[uid] = next;
            const newAw = Object.assign({}, aw); newAw[wk] = newWeekMap;
            tx.set(STATS_DOC(), { arenaWeekly: newAw }, { merge: true });
          });
          o.flushedBaseline = o.zheng; o.lastFlushAt = Date.now(); _writeLocal(o);
          console.log('[ArenaRank] flush ✅ +' + diff + ' (週=' + wk + ', 累計=' + o.zheng + ')');
        }catch(e){ console.warn('[ArenaRank] flushNow 失敗', e); }
        finally{ _flushInFlight = false; }
      }

      // ── 獎勵分檔(老師指定) ──
      function _calcAwardByRank(rank){
        if(rank === 1)               return { crystals: 15, zheng: 15, coins: 30000 };
        if(rank >= 2 && rank <= 5)   return { crystals: 10, zheng: 10, coins: 20000 };
        if(rank >= 6 && rank <= 10)  return { crystals: 5,  zheng: 5,  coins: 10000 };
        return { crystals: 2, zheng: 2, coins: 5000 };  // 11 名以後(本週有得證即發)
      }

      // ── 結算上週(週一 08:00 後 + GM 開關開啟才執行) ──
      async function trySettleLastWeek(){
        if(!window._gUserId) return false;
        try{ if(typeof window._arenaIsRankRewardEnabled === 'function' && !window._arenaIsRankRewardEnabled()) return false; }catch(_){}
        const lastWk = getLastWeekKey();
        try{
          const result = await runTransaction(_fbDb, async (tx) => {
            const snap = await tx.get(STATS_DOC());
            const data = snap.exists() ? (snap.data() || {}) : {};
            const settled = data.arenaRankSettlement || {};
            if(settled[lastWk]) return { settled: false };
            const aw = data.arenaWeekly || {};
            const weekMap = aw[lastWk] || {};
            const list = [];
            for(const uid in weekMap){
              const e = weekMap[uid];
              if(!e || (e.zheng || 0) <= 0) continue;
              if(_ADMIN_EMAILS.has((e.email || '').toLowerCase())) continue;
              list.push({ uid: uid, name: e.name || '玩家', zheng: e.zheng || 0, lastAt: e.lastAt || 0 });
            }
            list.sort(function(a, b){ if(a.zheng !== b.zheng) return b.zheng - a.zheng; return (a.lastAt || 0) - (b.lastAt || 0); });
            const awards = {};
            // ★ v3.15.49 — 並列同名次(競賽排名):rank = 1 + 「本週證數嚴格大於我」的人數。
            //   同證數者同名次(例:兩人並列第 1,下一位是第 3);獎勵依並列後的名次發放。
            list.forEach(function(e, i){
              var rank = 1;
              for(var _j = 0; _j < list.length; _j++){ if(list[_j].zheng > e.zheng) rank++; }
              const a = _calcAwardByRank(rank);
              awards[e.uid] = Object.assign({ rank: rank, name: e.name, zhengEarned: e.zheng }, a);
            });
            // ★ v3.15.49 — 結算 delta(用 deleteField 才能真的刪鍵:set merge:true 對 map 是深度合併,
            //   寫整份缺鍵 map 並不會刪掉缺的鍵)。新週寫入 + 過舊週(超過 KEEP)deleteField 縮減文件。
            const _KEEP = 6;   // 保留最近 6 週結算供大廳「歷史排名」往回查
            const _settleDelta = {};
            _settleDelta[lastWk] = { settledAt: Date.now(), settledBy: window._gUserId, count: list.length, awards: awards };
            try{
              const _allWeeks = Array.from(new Set(Object.keys(settled).concat([lastWk]))).sort();  // 舊→新
              while(_allWeeks.length > _KEEP){
                const _drop = _allWeeks.shift();
                if(_drop !== lastWk) _settleDelta[_drop] = deleteField();
              }
            }catch(_){}
            // ★ v3.15.49 — 結算完成後「清空上週排行榜」:deleteField 只刪 arenaWeekly[lastWk],
            //   本週 arenaWeekly[本週] 不動(玩家持續累積);歷史名次由 arenaRankSettlement 保存供回查。
            const _awDelta = {};
            _awDelta[lastWk] = deleteField();
            tx.set(STATS_DOC(), { arenaRankSettlement: _settleDelta, arenaWeekly: _awDelta }, { merge: true });
            return { settled: true, count: list.length };
          });
          if(result.settled){ console.log('[ArenaRank] ✅ 上週(' + lastWk + ')結算完成,共 ' + (result.count || 0) + ' 位上榜'); }
          return !!result.settled;
        }catch(e){ console.warn('[ArenaRank] trySettleLastWeek 失敗', e); return false; }
      }

      // ── 領取自己的排名獎勵(自助:讀 settlement 包 → 領到自己文件) ──
      function _claimedLocalKey(wk){ return 'lxps_arena_rank_claimed_' + wk; }
      async function claimMyAward(){
        if(!window._gUserId) return null;
        try{
          const gs = window._cachedGlobalStats || {};
          let settledMap = gs.arenaRankSettlement || null;
          if(!settledMap){
            try{ const s = await getDoc(STATS_DOC()); settledMap = s.exists() ? ((s.data() || {}).arenaRankSettlement || {}) : {}; }catch(_){ settledMap = {}; }
          }
          const uid = window._gUserId;
          const weeks = Object.keys(settledMap).sort().reverse();
          let targetWk = null, myAward = null;
          for(const wk of weeks){
            const aw = (settledMap[wk] && settledMap[wk].awards) || {};
            if(aw[uid]){
              if(localStorage.getItem(_claimedLocalKey(wk)) === '1') continue;
              targetWk = wk; myAward = aw[uid]; break;
            }
          }
          if(!targetWk || !myAward) return null;

          const result = await runTransaction(_fbDb, async (tx) => {
            const snap = await tx.get(PLAYER_DOC(uid));
            const data = snap.exists() ? (snap.data() || {}) : {};
            const claimedMap = (data.arenaRankClaimed && typeof data.arenaRankClaimed === 'object') ? data.arenaRankClaimed : {};
            if(claimedMap[targetWk]) return null;  // 已領
            const curCoins = data.knowledgeCoins || 0;
            const curBp = (data.playerBackpack && typeof data.playerBackpack === 'object') ? data.playerBackpack : {};
            const newBp = Object.assign({}, curBp);
            newBp['summon_crystal'] = Math.min(99, (newBp['summon_crystal'] || 0) + (myAward.crystals || 0));
            const newCoins = curCoins + (myAward.coins || 0);
            const newClaimed = Object.assign({}, claimedMap); newClaimed[targetWk] = Date.now();
            tx.set(PLAYER_DOC(uid), {
              knowledgeCoins: newCoins,
              playerBackpack: newBp,
              playerBackpack_s: JSON.stringify(newBp),
              arenaRankClaimed: newClaimed,
            }, { merge: true });
            return Object.assign({ weekKey: targetWk }, myAward);
          });

          if(result){
            try{ localStorage.setItem(_claimedLocalKey(targetWk), '1'); }catch(_){}
            try{
              if(typeof _knowledgeCoins !== 'undefined' && result.coins){ window._knowledgeCoins = (_knowledgeCoins || 0) + (result.coins || 0); }
              if(typeof _playerBackpack === 'object' && _playerBackpack && result.crystals > 0){
                _playerBackpack['summon_crystal'] = Math.min(99, (_playerBackpack['summon_crystal'] || 0) + result.crystals);
              }
              if(typeof _syncSummonCrystalDisplay === 'function') _syncSummonCrystalDisplay();
              if(typeof refreshAllCoinsDisplay === 'function') refreshAllCoinsDisplay();
              if(typeof _logCrystalTx === 'function' && result.crystals > 0) _logCrystalTx(result.crystals, '鬥技場排名獎勵(第' + result.rank + '名)');
              if(typeof _logCoinTx === 'function' && result.coins > 0) _logCoinTx(result.coins, '鬥技場排名獎勵(第' + result.rank + '名)');
            }catch(_se){ console.warn('[ArenaRank claim] 本地同步', _se); }
            try{ if(typeof window._arenaGrantZheng === 'function' && result.zheng > 0) window._arenaGrantZheng(result.zheng); }catch(_ze){}
          }
          return result;
        }catch(e){ console.warn('[ArenaRank] claimMyAward 失敗', e); return null; }
      }

      // 我本週即時排名(顯示用)
      function getMyWeeklyRank(){
        try{
          const wk = getWeekKey(); const gs = window._cachedGlobalStats || {};
          const weekMap = (gs.arenaWeekly && gs.arenaWeekly[wk]) || {};
          const list = [];
          for(const uid in weekMap){ const e = weekMap[uid]; if(!e || (e.zheng || 0) <= 0) continue; if(_ADMIN_EMAILS.has((e.email || '').toLowerCase())) continue; list.push({ uid: uid, zheng: e.zheng || 0, lastAt: e.lastAt || 0 }); }
          list.sort(function(a, b){ if(a.zheng !== b.zheng) return b.zheng - a.zheng; return (a.lastAt || 0) - (b.lastAt || 0); });
          const idx = list.findIndex(function(e){ return e.uid === window._gUserId; });
          const local = _readLocal();
          const localZ = (local && local.weekKey === wk) ? (local.zheng || 0) : 0;
          const cloudZ = idx >= 0 ? list[idx].zheng : 0;
          // ★ v3.15.49 — 並列同名次:rank = 1 + 「證數嚴格大於我」的人數(同數同名次)
          let _rank = 0;
          if(idx >= 0){
            const _myZ = list[idx].zheng;
            _rank = 1;
            for(let _j = 0; _j < list.length; _j++){ if(list[_j].zheng > _myZ) _rank++; }
          }
          return { rank: _rank, zheng: Math.max(cloudZ, localZ), total: list.length };
        }catch(e){ return { rank: 0, zheng: 0, total: 0 }; }
      }

      // ── 離場 / 間隔 flush(三層保險,沿用 weeklyQuiz 模式) ──
      setInterval(function(){
        try{ if(!window._gUserId) return; const o = _readLocal(); if(!o) return;
          if(Date.now() - (o.lastFlushAt || 0) >= 3600 * 1000 && (o.zheng || 0) > (o.flushedBaseline || 0)) flushNow().catch(function(){});
        }catch(_){}
      }, 5 * 60 * 1000);
      function _flushOnExit(){ try{ const o = _readLocal(); if(o && (o.zheng || 0) > (o.flushedBaseline || 0)) flushNow().catch(function(){}); }catch(_){} }
      try{
        window.addEventListener('beforeunload', _flushOnExit);
        window.addEventListener('pagehide', _flushOnExit);
        document.addEventListener('visibilitychange', function(){ if(document.visibilityState === 'hidden') _flushOnExit(); });
      }catch(_){}

      // ════════════════════════════════════════════════════════════════
      // ★ v3.13.92(2026-06-09)— 鬥技之證:GM 補償 + 登入回補(修「更新後鬥技之證消失」)
      // ────────────────────────────────────────────────────────────────
      //   reconcileLocalFromCloud():登入時把雲端本週 arenaWeekly[週][uid].zheng「取 max」回補本機。
      //     ・修復「本機週累計被清(共用 iPad 帳號重驗 / 更新)→ 顯示歸零」:雲端有 flush 過的值就回來。
      //     ・讓 GM 補償(寫雲端)在玩家端也看得到。
      //     ・安全:只「增」不「減」(max);週鍵一致才比對(不跨週污染);
      //            設 flushedBaseline=雲端值 → 下次 flush diff=0,不會重複上傳。
      //   _fbAdminGrantArenaZheng(uid, amount, reason):GM 補償 → 寫雲端 arenaWeekly[本週][uid].zheng,
      //     排名立即生效 + 寫 arenaZhengCompLog 供查證。玩家下次登入 reconcile 會把數字同步回本機。
      // ════════════════════════════════════════════════════════════════
      async function reconcileLocalFromCloud(){
        try{
          if(!window._gUserId) return;
          try{ if(typeof window._isAdminUser === 'function' && window._isAdminUser()) return; }catch(_){}  // 管理員不列排行
          const wk = getWeekKey();
          let weekMap = null;
          try{ const gs = window._cachedGlobalStats || {}; if(gs.arenaWeekly && gs.arenaWeekly[wk]) weekMap = gs.arenaWeekly[wk]; }catch(_){}
          if(!weekMap){ try{ const s = await getDoc(STATS_DOC()); const d = s.exists() ? (s.data() || {}) : {}; weekMap = (d.arenaWeekly && d.arenaWeekly[wk]) || {}; }catch(_){ return; } }
          const cloudZ = (weekMap && weekMap[window._gUserId] && weekMap[window._gUserId].zheng) || 0;
          const o = _getOrInitLocal();   // 確保是本週(跨週會自動重置)
          if(cloudZ > (o.zheng || 0)){
            console.warn('[ArenaRank reconcile v3.13.92] 雲端本週 zheng(' + cloudZ + ')> 本機(' + (o.zheng || 0)
              + ')→ 以雲端為準回補(修復本機被清/GM 補償),設 baseline 防重複上傳');
            o.zheng = cloudZ; o.flushedBaseline = cloudZ; o.lastAt = Date.now(); _writeLocal(o);
            try{ if(typeof window._refreshArenaRankDisplay === 'function') window._refreshArenaRankDisplay(); }catch(_){}
          }
        }catch(e){ console.warn('[ArenaRank] reconcileLocalFromCloud 失敗', e); }
      }

      async function _fbAdminGrantArenaZheng(targetUid, amount, reason){
        amount = Math.round(Number(amount) || 0);
        if(!targetUid || amount === 0) return { ok:false, reason:'uid 或數量無效' };
        const wk = getWeekKey();
        try{
          const _res = await runTransaction(_fbDb, async (tx) => {
            const snap = await tx.get(STATS_DOC());
            const data = snap.exists() ? (snap.data() || {}) : {};
            const aw = data.arenaWeekly || {};
            const weekMap = aw[wk] || {};
            const prev = weekMap[targetUid] || { zheng: 0, name: '', email: '' };
            const newZheng = Math.max(0, (prev.zheng || 0) + amount);
            const next = Object.assign({}, prev, { zheng: newZheng, lastAt: Date.now() });
            const newWeekMap = Object.assign({}, weekMap); newWeekMap[targetUid] = next;
            const newAw = Object.assign({}, aw); newAw[wk] = newWeekMap;
            const comp = Array.isArray(data.arenaZhengCompLog) ? data.arenaZhengCompLog.slice() : [];
            comp.push({ at: Date.now(), uid: targetUid, amount: amount, newZheng: newZheng, weekKey: wk,
                        reason: String(reason || '').slice(0, 64), by: (window._gUserId || '').slice(0, 12) });
            while(comp.length > 200) comp.shift();
            tx.set(STATS_DOC(), { arenaWeekly: newAw, arenaZhengCompLog: comp }, { merge: true });
            return { newZheng: newZheng, prevZheng: (prev.zheng || 0) };
          });
          console.log('[ArenaRank] ✅ GM 補償鬥技之證 uid=' + String(targetUid).slice(0,8) + ' +' + amount
            + ' (週=' + wk + ', ' + _res.prevZheng + '→' + _res.newZheng + ')');
          return { ok:true, weekKey: wk, amount: amount, prevZheng: _res.prevZheng, newZheng: _res.newZheng };
        }catch(e){ console.warn('[ArenaRank] GM 補償 zheng 失敗', e); return { ok:false, reason: (e && e.message) || '寫入失敗' }; }
      }
      try{ window._fbAdminGrantArenaZheng = _fbAdminGrantArenaZheng; }catch(_){}

      // ★ v3.15.49 — 大廳排行榜支援:本週即時排行(讀 arenaWeekly[本週])+ 歷史結算回查
      //   本週排行:回傳 [{uid,name,zheng,rank}](並列同名次),供大廳「本週排行榜」用權威週數據,
      //             不再聚合 arenaBattles(便宜、自動週重置、不限 500 筆)。
      async function getCurrentWeekRanking(){
        try{
          const wk = getWeekKey();
          let weekMap = null;
          try{ const gs = window._cachedGlobalStats || {}; if(gs.arenaWeekly && gs.arenaWeekly[wk]) weekMap = gs.arenaWeekly[wk]; }catch(_){}
          if(!weekMap){ try{ const s = await getDoc(STATS_DOC()); const d = s.exists() ? (s.data() || {}) : {}; weekMap = (d.arenaWeekly && d.arenaWeekly[wk]) || {}; }catch(_){ weekMap = {}; } }
          const list = [];
          for(const uid in weekMap){
            const e = weekMap[uid];
            if(!e || (e.zheng || 0) <= 0) continue;
            if(_ADMIN_EMAILS.has((e.email || '').toLowerCase())) continue;
            list.push({ uid: uid, name: e.name || '玩家', zheng: e.zheng || 0, lastAt: e.lastAt || 0 });
          }
          list.sort(function(a, b){ if(a.zheng !== b.zheng) return b.zheng - a.zheng; return (a.lastAt || 0) - (b.lastAt || 0); });
          // 並列同名次
          list.forEach(function(e){ var r = 1; for(var j = 0; j < list.length; j++){ if(list[j].zheng > e.zheng) r++; } e.rank = r; });
          return { weekKey: wk, list: list };
        }catch(e){ console.warn('[ArenaRank] getCurrentWeekRanking 失敗', e); return { weekKey: getWeekKey(), list: [] }; }
      }
      // 歷史結算:回傳 [{weekKey,settledAt,count,list:[{uid,name,rank,zhengEarned,crystals,zheng,coins}]}](新→舊)
      async function getSettlementHistory(){
        try{
          let settledMap = null;
          try{ const gs = window._cachedGlobalStats || {}; if(gs.arenaRankSettlement) settledMap = gs.arenaRankSettlement; }catch(_){}
          if(!settledMap){ try{ const s = await getDoc(STATS_DOC()); const d = s.exists() ? (s.data() || {}) : {}; settledMap = d.arenaRankSettlement || {}; }catch(_){ settledMap = {}; } }
          const weeks = Object.keys(settledMap).sort().reverse();
          const out = [];
          weeks.forEach(function(wk){
            const rec = settledMap[wk] || {};
            const aw = rec.awards || {};
            const list = [];
            for(const uid in aw){ const a = aw[uid] || {}; list.push({ uid: uid, name: a.name || '玩家', rank: a.rank || 0, zhengEarned: a.zhengEarned || 0, crystals: a.crystals || 0, zheng: a.zheng || 0, coins: a.coins || 0 }); }
            list.sort(function(a, b){ return (a.rank || 999) - (b.rank || 999) || (b.zhengEarned || 0) - (a.zhengEarned || 0); });
            out.push({ weekKey: wk, settledAt: rec.settledAt || 0, count: rec.count || list.length, list: list });
          });
          return out;
        }catch(e){ console.warn('[ArenaRank] getSettlementHistory 失敗', e); return []; }
      }

      window._arenaRank = {
        getWeekKey: getWeekKey,
        getLastWeekKey: getLastWeekKey,
        addZheng: addZheng,
        flushNow: flushNow,
        trySettleLastWeek: trySettleLastWeek,
        claimMyAward: claimMyAward,
        getMyWeeklyRank: getMyWeeklyRank,
        reconcileLocalFromCloud: reconcileLocalFromCloud,   // ★ v3.13.92
        grantZhengAdmin: _fbAdminGrantArenaZheng,           // ★ v3.13.92(GM 補償)
        getCurrentWeekRanking: getCurrentWeekRanking,       // ★ v3.15.49(大廳本週排行)
        getSettlementHistory: getSettlementHistory,         // ★ v3.15.49(大廳歷史排名)
      };
      console.log('[ArenaRank] ✅ 鬥技場每週排名結算模組已掛載 v3.15.49 (並列名次+結算清空+歷史回查)');
    })();

    // ════════════════════════════════════════════════════════════════════
    // ★ v5.146.0(2026-09-07・老師需求 1)— 小遊戲「每週排行榜獎勵」結算 + 自助領取
    //   資料(由小遊戲 minigame/index.html v1.10.0 寫入):stats/minigameWeekly = {
    //     weeks:      { <週>: { <uid>: { n:遮罩暱稱, g:{ <gid>:本週最高分 }, at } } }
    //     settlement: { <週>: { settledAt, settledBy, count, awards:{ <uid>:{rank,gid,score,name,coins,crystals,books} }, top:{ <gid>:[{uid,name,score}] } } }
    //   }
    //   規則(老師):各遊戲取前 10 名;每位玩家每週只領「一次」(拿名次最好的那一個;同名次取分數最高的);
    //           被讓出的名次順延給該遊戲的下一位;獎勵同知識王(小博士)週排行:1名 50000/🔮10/📖10、2~5名 35000/6/6、6~10名 20000/3/3。
    //   結算:每週一 08:00(台灣)後,第一位登入者 transaction 搶 settlement[上週] 鎖 → 逐名次 1→10 分配 → 寫結算包、刪 weeks[上週](週文件不膨脹)。
    //   領獎:讀結算包找自己 → transaction「minigameRankClaims/{uid}_{週} create-only(rules)+ players/{uid} 加幣/背包 + minigameRankClaimed[週]」
    //         ⇒ 跨裝置/多分頁/三槽合併都不會重複發放;認領文件寫不進去(rules 未部署)就不發、下次再試(寧漏不重)。
    //   仿 _arenaRank 模型(零跨玩家寫);週 key 演算法與 weeklyQuiz/arena 完全相同。
    // ════════════════════════════════════════════════════════════════════
    (function _setupMinigameRankSettlement(){
      const WEEK_DOC = () => doc(_fbDb, 'stats', 'minigameWeekly');
      const PLAYER_DOC = (uid) => doc(_fbDb, 'players', uid);
      const CLAIM_DOC = (uid, wk) => doc(_fbDb, 'minigameRankClaims', uid + '_' + wk);
      const _KEEP_SETTLE = 4;
      // 小遊戲名稱(顯示用;與 minigame_db.js name.s 對齊,找不到就顯示 gid)
      const _MG_GAME_NAMES = { weather:'天氣小偵探', rock:'石頭鑑定王', species:'特有種大冒險', heat:'保溫魔法師', map:'台灣飛飛飛', history:'時光小旅人', scale:'秤重小老闆', proverb:'俗諺解謎王', music:'音樂小精靈', recycle:'回收小英雄', teaorder:'排排站小達人', proverb_match:'俗諺連連看', coinrush:'算數大進擊', maze:'迷宮探險王', animal:'動物特徵二選一' };
      function _gname(gid){ return _MG_GAME_NAMES[gid] || gid || '小遊戲'; }
      function _calcWeekKey(date){
        const d = date || new Date();
        const tw = new Date(d.getTime() + (8 * 3600 * 1000) - (d.getTimezoneOffset() * 60 * 1000));
        const dow = tw.getUTCDay(); const hour = tw.getUTCHours();
        let offsetDays;
        if(dow === 0) offsetDays = 6;
        else if(dow === 1 && hour < 8) offsetDays = 7;
        else offsetDays = dow - 1;
        const monday = new Date(tw.getTime() - offsetDays * 86400 * 1000);
        const year = monday.getUTCFullYear();
        const yearStart = new Date(Date.UTC(year, 0, 1));
        const dayDiff = Math.floor((monday.getTime() - yearStart.getTime()) / 86400000);
        const weekNum = Math.floor(dayDiff / 7) + 1;
        return year + '-W' + (weekNum < 10 ? '0' : '') + weekNum;
      }
      function getWeekKey(){ return _calcWeekKey(); }
      function getLastWeekKey(){ return _calcWeekKey(new Date(Date.now() - 7 * 86400 * 1000)); }
      function _calcAwardByRank(rank){
        if(rank === 1)              return { coins: 50000, crystals: 10, books: 10 };
        if(rank >= 2 && rank <= 5)  return { coins: 35000, crystals: 6,  books: 6 };
        if(rank >= 6 && rank <= 10) return { coins: 20000, crystals: 3,  books: 3 };
        return null;
      }
      // ── 純函式:週資料 → { awards, top, count }(可單元測試) ──
      function computeAwards(weekMap){
        const lists = {};
        for(const uid in (weekMap || {})){
          const e = weekMap[uid];
          if(!e || !e.g || typeof e.g !== 'object') continue;
          for(const gid in e.g){
            const s = Math.round(Number(e.g[gid]) || 0);
            if(s <= 0) continue;
            (lists[gid] = lists[gid] || []).push({ uid: uid, name: e.n || '玩家', score: s, at: Number(e.at) || 0 });
          }
        }
        const gids = Object.keys(lists).sort();
        const top = {};
        gids.forEach(function(gid){
          lists[gid].sort(function(a, b){ if(a.score !== b.score) return b.score - a.score; return a.at - b.at; });
          top[gid] = lists[gid].slice(0, 10).map(function(x){ return { uid: x.uid, name: x.name, score: x.score }; });
        });
        const awarded = {};
        for(let r = 1; r <= 10; r++){
          const picks = {};
          let remaining = gids.slice();
          let guard = 0;
          while(remaining.length && guard++ < 200){
            const chosen = {};
            Object.keys(picks).forEach(function(g){ chosen[picks[g].uid] = true; });
            const proposals = {}, noneLeft = [];
            remaining.forEach(function(gid){
              const e = lists[gid].find(function(x){ return !awarded[x.uid] && !chosen[x.uid]; });
              if(!e){ noneLeft.push(gid); return; }
              (proposals[e.uid] = proposals[e.uid] || []).push({ gid: gid, e: e });
            });
            remaining = remaining.filter(function(g){ return noneLeft.indexOf(g) < 0; });
            if(!remaining.length) break;
            // 同一人同時是多個遊戲的候選 ⇒ 只領分數最高的那一個(同分取 gid 字典序),其餘遊戲下一輪順延
            Object.keys(proposals).forEach(function(uid){
              const arr = proposals[uid];
              arr.sort(function(a, b){ return (b.e.score - a.e.score) || (a.gid < b.gid ? -1 : 1); });
              picks[arr[0].gid] = arr[0].e;
            });
            remaining = remaining.filter(function(g){ return !picks[g]; });
          }
          Object.keys(picks).forEach(function(gid){
            const e = picks[gid], a = _calcAwardByRank(r);
            if(!a) return;
            awarded[e.uid] = Object.assign({ rank: r, gid: gid, score: e.score, name: e.name }, a);
          });
        }
        return { awards: awarded, top: top, count: Object.keys(awarded).length, games: gids.length };
      }

      // ── 結算上週(週一 08:00 後第一位登入者;transaction 鎖) ──
      async function trySettleLastWeek(){
        if(!window._gUserId) return false;
        const lastWk = getLastWeekKey(), curWk = getWeekKey();
        try{
          const result = await runTransaction(_fbDb, async (tx) => {
            const snap = await tx.get(WEEK_DOC());
            if(!snap.exists()) return { settled: false, reason: 'no_doc' };
            const data = snap.data() || {};
            const settled = data.settlement || {};
            if(settled[lastWk]) return { settled: false, reason: 'already' };
            const weeks = data.weeks || {};
            const res = computeAwards(weeks[lastWk] || {});
            const delta = { settlement: {}, weeks: {} };
            delta.settlement[lastWk] = { settledAt: Date.now(), settledBy: window._gUserId, count: res.count, games: res.games, awards: res.awards, top: res.top, empty: res.count === 0 };
            // 舊結算包只留最近 _KEEP_SETTLE 週;上週及更早的原始週資料一律 deleteField(文件不膨脹;歷史名次由結算包保存)
            try{
              const all = Array.from(new Set(Object.keys(settled).concat([lastWk]))).sort();
              while(all.length > _KEEP_SETTLE){ const drop = all.shift(); if(drop !== lastWk) delta.settlement[drop] = deleteField(); }
            }catch(_){}
            Object.keys(weeks).forEach(function(k){ if(k !== curWk) delta.weeks[k] = deleteField(); });
            tx.set(WEEK_DOC(), delta, { merge: true });
            return { settled: true, count: res.count, games: res.games };
          });
          if(result.settled) console.log('[MinigameRank] ✅ 上週(' + lastWk + ')結算完成:' + (result.games || 0) + ' 款遊戲、' + (result.count || 0) + ' 位得獎');
          return !!result.settled;
        }catch(e){ console.warn('[MinigameRank] trySettleLastWeek 失敗', e); return false; }
      }

      // ── 領取自己的獎勵(認領文件 create-only + 玩家主檔,同一 transaction) ──
      function _claimedLocalKey(uid, wk){ return 'lxps_mg_rank_claimed_' + uid + '_' + wk; }
      async function claimMyAward(){
        const uid = window._gUserId;
        if(!uid) return null;
        try{
          let settledMap = {};
          try{ const s = await getDoc(WEEK_DOC()); settledMap = s.exists() ? ((s.data() || {}).settlement || {}) : {}; }catch(_){ return null; }
          const weeks = Object.keys(settledMap).sort().reverse();
          let targetWk = null, myAward = null;
          for(const wk of weeks){
            const aw = (settledMap[wk] && settledMap[wk].awards) || {};
            if(!aw[uid]) continue;
            let done = false;
            try{ done = localStorage.getItem(_claimedLocalKey(uid, wk)) === '1'; }catch(_){}
            if(done) continue;
            targetWk = wk; myAward = aw[uid]; break;
          }
          if(!targetWk || !myAward) return null;
          const result = await runTransaction(_fbDb, async (tx) => {
            const cs = await tx.get(CLAIM_DOC(uid, targetWk));
            if(cs.exists()) return { already: true };
            const ps = await tx.get(PLAYER_DOC(uid));
            const data = ps.exists() ? (ps.data() || {}) : {};
            const claimedMap = (data.minigameRankClaimed && typeof data.minigameRankClaimed === 'object') ? data.minigameRankClaimed : {};
            if(claimedMap[targetWk]) return { already: true };
            const curCoins = data.knowledgeCoins || 0;
            const curBp = (data.playerBackpack && typeof data.playerBackpack === 'object') ? data.playerBackpack : {};
            const newBp = Object.assign({}, curBp);
            newBp['summon_crystal'] = Math.min(99, (newBp['summon_crystal'] || 0) + (myAward.crystals || 0));
            newBp['hero_exp_book_deluxe'] = Math.min(99, (newBp['hero_exp_book_deluxe'] || 0) + (myAward.books || 0));
            const newClaimed = Object.assign({}, claimedMap); newClaimed[targetWk] = Date.now();
            tx.set(CLAIM_DOC(uid, targetWk), { uid: uid, weekKey: targetWk, rank: myAward.rank || 0, gid: String(myAward.gid || ''), at: Date.now() });
            tx.set(PLAYER_DOC(uid), {
              knowledgeCoins: curCoins + (myAward.coins || 0),
              playerBackpack: newBp,
              playerBackpack_s: JSON.stringify(newBp),
              minigameRankClaimed: newClaimed,
            }, { merge: true });
            return { claimed: true };
          });
          if(result && result.already){ try{ localStorage.setItem(_claimedLocalKey(uid, targetWk), '1'); }catch(_){} return null; }
          if(!result || !result.claimed) return null;
          try{ localStorage.setItem(_claimedLocalKey(uid, targetWk), '1'); }catch(_){}
          // 本地即時同步(否則 autosave 用舊記憶體把剛領的沖掉)
          try{
            if(typeof _knowledgeCoins !== 'undefined' && myAward.coins){ window._knowledgeCoins = (_knowledgeCoins || 0) + (myAward.coins || 0); }
            if(typeof _playerBackpack === 'object' && _playerBackpack){
              if(myAward.crystals > 0) _playerBackpack['summon_crystal'] = Math.min(99, (_playerBackpack['summon_crystal'] || 0) + myAward.crystals);
              if(myAward.books > 0) _playerBackpack['hero_exp_book_deluxe'] = Math.min(99, (_playerBackpack['hero_exp_book_deluxe'] || 0) + myAward.books);
            }
            if(typeof _syncSummonCrystalDisplay === 'function') _syncSummonCrystalDisplay();
            if(typeof refreshAllCoinsDisplay === 'function') refreshAllCoinsDisplay();
            if(typeof _logCrystalTx === 'function' && myAward.crystals > 0) _logCrystalTx(myAward.crystals, '小遊戲週排行獎勵(第' + myAward.rank + '名)');
            if(typeof _logCoinTx === 'function' && myAward.coins > 0) _logCoinTx(myAward.coins, '小遊戲週排行獎勵(第' + myAward.rank + '名)');
          }catch(_se){ console.warn('[MinigameRank claim] 本地同步', _se); }
          return Object.assign({ weekKey: targetWk, gameName: _gname(myAward.gid) }, myAward);
        }catch(e){
          if(e && e.code === 'permission-denied') console.warn('[MinigameRank] 認領文件寫入被 rules 拒絕(minigameRankClaims 規則尚未部署?)→ 本次不發獎,下次登入再試');
          else console.warn('[MinigameRank] claimMyAward 失敗', e);
          return null;
        }
      }

      window._minigameRank = {
        getWeekKey: getWeekKey,
        getLastWeekKey: getLastWeekKey,
        computeAwards: computeAwards,
        trySettleLastWeek: trySettleLastWeek,
        claimMyAward: claimMyAward,
        gameName: _gname,
      };
      console.log('[MinigameRank] ✅ 小遊戲每週排行榜結算模組已掛載 v5.146.0');
    })();
    // ════════════════════════════════════════════════════════════════════
    // ★ v3.13.5(2026-05-31)— 世界 BOSS 排名獎勵正式結算系統
    // ────────────────────────────────────────────────────────────────────
    // 設計(對齊老師需求 2026-05-31):
    //   流程:
    //     1. BOSS 倒下 → dealDamage transaction 內順手寫 wbBossDownTimes[bossId]
    //        + wbCurrentRound[bossId] = { roundKey, bossDownAt }
    //     2. UI 公告「BOSS 已倒下,獎勵將於下次 8:00 自動發送」
    //     3. GM 清查期(倒下後 → 隔天 8:00)— 可用既有「場次標 BUG」修正排行榜
    //     4. 隔天 8:00 後,第一個觸發 trySettle 的玩家用 transaction 搶結算鎖
    //        - 讀當下 worldBossLeaderboard[bossId](已含 GM 修正後的最終排名)
    //        - 對每隊 teamUids 中每人寫 players/{uid}.wbPendingAward
    //     5. 玩家登入 → claim → transaction 領獎(加幣、加水晶、發書、擲火龍王之牙、發稱號)
    //
    // 設計理由:
    //   * 仿 weeklyQuiz 結算機制(已穩定運行 → 鐵律 1.54-1.59 整套適用)
    //   * 「機率部分在 claim 才擲」— 結算寫的是快照(隊伍/排名/分級獎勵 + 龍牙機率),
    //     每位玩家領獎時各自擲骰,避免「同隊 4 人都拿到 / 都沒拿到」造成不公感
    //   * roundKey 索引讓同隻 BOSS 多次倒下都能各自結算一輪
    //
    // 暴露 API:
    //   window._wbSettle.trySettle(bossId)            → 嘗試結算(自動判斷時間是否到)
    //   window._wbSettle.claimPending()               → 領取自己的 pending(玩家用)
    //   window._wbSettle.getMyPending()               → 讀自己有沒有 pending(玩家用,不領)
    //   window._wbSettle.getStatus(bossId)            → { bossDownAt, settleAt, hasSettled, roundKey }
    //   window._wbSettle.adminForceSettle(bossId, uid)→ GM 強制補發給單一玩家
    //
    // Firestore 資料:
    //   stats/global.wbBossDownTimes[bossId] = number  (BOSS 倒下的時間戳)
    //   stats/global.wbCurrentRound[bossId] = { roundKey, bossDownAt }
    //   stats/global.wbSettlement[bossId][roundKey] = {
    //     settledAt, settledBy, roundKey, bossDownAt, awards: { uid: {...} }
    //   }
    //   players/{uid}.wbPendingAward = { bossId, roundKey, rank, ..., claimedAt }
    //
    // ★ 鐵律 1.55 — players/{uid}.wbPendingAward 是跨玩家寫入欄位,
    //   Firestore Rules 需放行(hasOnly(['wbPendingAward']) 即允許其他玩家寫此欄位)
    // ════════════════════════════════════════════════════════════════════
    (function _setupWbSettlementV1(){
      const STATS_DOC = () => doc(_fbDb, 'stats', 'global');
      const PLAYER_DOC = (uid) => doc(_fbDb, 'players', uid);

      // ── 計算「BOSS 倒下後的下一個台灣時區 8:00」時間戳 ──
      //   bossDownAt = 5/31 23:30 → settleAt = 6/1 08:00
      //   bossDownAt = 5/31 07:00 → settleAt = 5/31 08:00(同一天 8:00 還沒到)
      //   bossDownAt = 5/31 09:00 → settleAt = 6/1 08:00
      //   原則:倒下後至少要再到「下一個 8:00」才開放結算,讓 GM 有清查時間
      function _calcSettleAt(bossDownAt){
        if(typeof bossDownAt !== 'number' || bossDownAt <= 0) return 0;
        // 轉成「台灣當地時間」處理:加 +8 小時偏移後用 UTC method 讀寫
        const _d = new Date(bossDownAt);
        const _tw = new Date(_d.getTime() + (8 * 3600 * 1000) - (_d.getTimezoneOffset() * 60 * 1000));
        const _hour = _tw.getUTCHours();
        const _twDayStart = new Date(_tw.getTime());
        _twDayStart.setUTCHours(8, 0, 0, 0);
        let _settleTw;
        if(_hour < 8){
          // 凌晨 0-7 點倒下 → 今天 08:00 結算
          _settleTw = _twDayStart;
        } else {
          // 早上 8 點後倒下 → 隔天 08:00 結算
          _settleTw = new Date(_twDayStart.getTime() + 86400000);
        }
        // 轉回實際 UTC 時間戳(就是上面 +8 偏移的反向)
        return _settleTw.getTime() - (8 * 3600 * 1000) + (_d.getTimezoneOffset() * 60 * 1000);
      }

      // ── 讀狀態(對外用) ──
      function getStatus(bossId){
        if(!bossId) bossId = (typeof window._wbGetCurrentBossId === 'function') ? window._wbGetCurrentBossId() : 'vesuvius_fire_dragon';  // ★ v3.14.20
        try{
          const _gs = window._cachedGlobalStats || {};
          const _bossDownTimes = _gs.wbBossDownTimes || {};
          const _currentRound = _gs.wbCurrentRound || {};
          const _settlement = _gs.wbSettlement || {};
          const _bossDownAt = _bossDownTimes[bossId] || 0;
          const _round = _currentRound[bossId] || null;
          const _roundKey = _round ? _round.roundKey : '';
          const _hasSettled = _roundKey
            && _settlement[bossId]
            && _settlement[bossId][_roundKey]
            && _settlement[bossId][_roundKey].settledAt > 0;
          const _settleAt = _bossDownAt > 0 ? _calcSettleAt(_bossDownAt) : 0;
          return {
            bossId,
            bossDownAt: _bossDownAt,
            settleAt: _settleAt,
            roundKey: _roundKey,
            hasSettled: !!_hasSettled,
            now: Date.now(),
            canSettle: _bossDownAt > 0 && _settleAt > 0 && Date.now() >= _settleAt && !_hasSettled,
          };
        }catch(e){
          console.warn('[WbSettle] getStatus 失敗', e);
          return { error: e.message };
        }
      }

      // ── 結算嘗試(任何玩家登入後跑;搶結算鎖) ──
      //   opts.force: true → 跳過時間守門(GM 後台「強制立即結算」用)
      async function trySettle(bossId, opts){
        if(!bossId) bossId = (typeof window._wbGetCurrentBossId === 'function') ? window._wbGetCurrentBossId() : 'vesuvius_fire_dragon';  // ★ v3.14.20
        if(!window._gUserId){
          return { settled:false, reason:'not_logged_in' };
        }
        const _force = !!(opts && opts.force);
        const _status = getStatus(bossId);
        // 守門:沒倒下 / 沒 roundKey / 已結算 → 一律擋(force 也擋,沒倒下就不該結算)
        if(_status.bossDownAt <= 0){
          return { settled:false, reason:'boss_not_down', status:_status };
        }
        if(!_status.roundKey){
          return { settled:false, reason:'no_round_key', status:_status };
        }
        if(_status.hasSettled){
          return { settled:false, reason:'already_settled', status:_status };
        }
        // 非 force 模式:還沒到 settleAt → 擋
        if(!_force && Date.now() < _status.settleAt){
          return { settled:false, reason:'not_yet', status:_status };
        }
        const _bossDownAt = _status.bossDownAt;
        const _roundKey = _status.roundKey;
        let _result_nextId = null, _result_nextName = null;   // ★ v3.14.21 接班結果(transaction 內賦值,Phase 2 後清榜用)

        try{
          // Phase 1:搶結算鎖 + 抓當下排行榜快照
          const _result = await runTransaction(_fbDb, async (tx) => {
            const _snap = await tx.get(STATS_DOC());
            const _data = _snap.exists() ? (_snap.data() || {}) : {};
            const _settlement = _data.wbSettlement || {};
            const _bossSettlement = _settlement[bossId] || {};
            if(_bossSettlement[_roundKey] && _bossSettlement[_roundKey].settledAt > 0){
              return { settled:false, reason:'already_settled' };
            }
            // 讀當下排行榜(GM 修正後的最終版本)
            const _lbMap = _data.worldBossLeaderboard || {};
            const _list = Array.isArray(_lbMap[bossId]) ? _lbMap[bossId].slice() : [];
            // 排序與正式排行榜一致:總傷由高到低 + tiebreaker
            _list.sort(function(a, b){
              const _da = (b.totalDmg||0) - (a.totalDmg||0);
              if(_da !== 0) return _da;
              const ta = a.tiebreaker||{}, tb = b.tiebreaker||{};
              const _dt = (ta.turns||999) - (tb.turns||999);
              if(_dt !== 0) return _dt;
              const _dq = (tb.quizCorrect||0) - (ta.quizCorrect||0);
              if(_dq !== 0) return _dq;
              return (tb.aliveCount||0) - (ta.aliveCount||0);
            });
            // BOSS 顯示名
            const _bossName = (function(){
              try{
                const _lineup = (typeof window.WORLD_BOSS_LINEUP !== 'undefined' ? window.WORLD_BOSS_LINEUP : null);
                if(Array.isArray(_lineup)){
                  const _b = _lineup.find(function(x){ return x && x.id === bossId; });
                  if(_b) return _b.name;
                }
              }catch(_){}
              return '火山炎龍王';
            })();
            // 為前 100 名隊伍計算獎勵快照
            const _getRewardTier = (typeof window._wbGetRewardTier === 'function') ? window._wbGetRewardTier : null;
            const _rewardsCfg = window._WORLD_BOSS_TEAM_REWARDS || {};
            const _awards = {};  // uid → award
            for(let _rank = 1; _rank <= _list.length && _rank <= 100; _rank++){
              const _entry = _list[_rank - 1];
              if(!_entry || !_entry.teamKey) continue;
              const _tier = _getRewardTier ? _getRewardTier(_rank) : 'memorial';
              const _cfg = _rewardsCfg[_tier] || {};
              // 從 teamKey 拆 uid(同隊伍可能有重複 uid,例如房主代打 4 槽)
              const _uids = String(_entry.teamKey).split('|').filter(Boolean);
              const _seen = new Set();
              for(const _uid of _uids){
                if(_seen.has(_uid)) continue;
                _seen.add(_uid);
                // bot 跳過(練習槽位 fallback uid)
                if(/^bot[-_]/i.test(_uid) || _uid === '__bot' || _uid === 'solo_user') continue;
                // 同位玩家如果在多隊伍上榜,只記第一名(最好的)
                if(_awards[_uid]) continue;
                // ★ v3.14.20 — 龍王至寶按 BOSS 映射:維蘇威→火龍王之牙、翠玉草→草龍王之鬚;
                //   ★ v3.15.17 — 加 山岳地龍王→地龍王之麟(須與 world-boss.js _WB_DRAGON_TREASURE_MAP 一致)
                //   ★ v4.32.0 — 補齊全 8 龍王(修:雷龍王/海龍王/暗/光/幻 原缺 → 查無 bossId → fallback 發錯至寶):
                //     每隻龍王對應其專屬至寶(id 見 _ARENA_DRAGON_TREASURE_IDS·TAIWAN_TREASURES 內定義)。
                //     單一真相=優先採用 world-boss.js 的 window._WB_DRAGON_TREASURE_MAP(登入時已載入·同步已補齊 8 筆),
                //     本地保留完整 8 筆為 base(Object.assign 先鋪本地再讓雲端 map 覆蓋/追加)→ 未來新增龍王:
                //     於 world-boss.js _WB_DRAGON_TREASURE_MAP 加一筆即自動對應(本地 base 亦建議同步補·雙保險防載入順序/離線)。
                //   ★ 單一真相:走 index.html 的 window._lxpsDragonTreasureMapFull()(完整 8 龍王·見 TAIWAN_TREASURES export 後定義);
                //     若該 helper 尚未就緒(理論上登入結算時早已載入)則用等價 inline 完整 8 筆 fallback,絕不落回 3 筆。
                const _WB_DRAGON_T_MAP = (typeof window._lxpsDragonTreasureMapFull === 'function')
                  ? window._lxpsDragonTreasureMapFull()
                  : Object.assign({
                      vesuvius_fire_dragon:   'dragon_fang_fire',      // 炎龍王之牙(火)
                      shenhai_water_dragon:   'dragon_claw_sea',       // 海龍王之爪(水)
                      taifeng_wind_dragon:    'dragon_wing_thunder',   // 雷龍王之翼(風暴雷龍王·當前)
                      shanyue_earth_dragon:   'dragon_scale_earth',    // 地龍王之麟(土)
                      bushi_dark_dragon:      'dragon_bone_dark',      // 暗龍王之骸(暗)
                      shensheng_light_dragon: 'dragon_feather_light',  // 光龍王之羽(光)
                      cuiyu_grass_dragon:     'dragon_whisker_grass',  // 森龍王之鬚(草)
                      xingchen_omni_dragon:   'dragon_horn_omni',      // 幻龍王之角(幻/omni)
                    }, (window._WB_DRAGON_TREASURE_MAP && typeof window._WB_DRAGON_TREASURE_MAP === 'object') ? window._WB_DRAGON_TREASURE_MAP : {});
                _awards[_uid] = {
                  bossId,
                  bossName: _bossName,
                  roundKey: _roundKey,
                  rank: _rank,
                  tier: _tier,
                  tierLabel: _cfg.tier || '',
                  coins: _cfg.coins || 0,
                  summonCrystals: _cfg.summonCrystals || 0,
                  expScrollTreasure: _cfg.expScrollTreasure || 0,
                  expBookDeluxe: _cfg.expBookDeluxe || 0,
                  dragonTreasureId: _WB_DRAGON_T_MAP[bossId] || _cfg.dragonTreasureId || '',
                  dragonTreasureChance: _cfg.dragonTreasureChance || 0,
                  treasureChance: _cfg.treasureChance || 0,
                  treasureRarity: _cfg.treasureRarity || '',
                  treasureLabel: _cfg.treasureLabel || '',
                  isChampion: _rank === 1,
                  championTitleTemplate: (_rank === 1 ? (_cfg.titleTemplate || '屠龍者・{bossName}') : null),
                  teamKey: _entry.teamKey,
                  teamNames: _entry.teamNames || [],
                };
              }
            }
            // 寫鎖 + 結算結果
            const _newSettlement = Object.assign({}, _settlement);
            const _newBossSettlement = Object.assign({}, _bossSettlement);
            _newBossSettlement[_roundKey] = {
              settledAt: Date.now(),
              settledBy: window._gUserId,
              roundKey: _roundKey,
              bossDownAt: _bossDownAt,
              awards: _awards,
              // ★ v3.14.22 — 歷戰記錄:存完整排行榜快照(每位上榜玩家 名次/隊名/總傷/層級/隊員uid)
              //   供 GM「歷戰記錄」分頁查詢過往戰績(補償 / BUG 檢查),不依賴日後可能被清空的即時排行榜。
              bossName: _bossName,
              spawnAt: (function(){ try{ const _sp = _data.wbBossSpawnTimes || {}; return _sp[bossId] || 0; }catch(_){ return 0; } })(),
              leaderboardSnapshot: _list.slice(0, 100).map(function(e, _i){
                return {
                  rank: _i + 1,
                  teamKey: e.teamKey || '',
                  teamLabel: e.teamLabel || e.displayLabel || '',
                  totalDmg: e.totalDmg || 0,
                  tier: (_getRewardTier ? _getRewardTier(_i + 1) : 'memorial'),
                };
              }),
            };
            _newSettlement[bossId] = _newBossSettlement;
            // ★ v3.14.21(老師裁示)— 結算當下「同一個 transaction」原子地接班下一隻龍王:
            //   隔天 8:00 結算成功 = 同時啟動下一隻(不再等祝福 72h 到期;祝福仍持續 72h 不受影響)。
            //   只有「正在被結算的這隻 == 當前龍王」時才推進,避免 GM 補結算舊輪誤切當前龍王。
            const _advancePatch = {};
            try{
              const _curId = _data.wbCurrentBossId || window._WB_DEFAULT_BOSS_ID || 'vesuvius_fire_dragon';
              if(bossId === _curId){
                const _nextId = (typeof window._wbGetNextBossId === 'function')
                                  ? window._wbGetNextBossId(bossId) : 'cuiyu_grass_dragon';
                const _nextBoss = (window.WORLD_BOSS_LINEUP || []).find(function(b){ return b && b.id === _nextId; }) || null;
                const _nextMaxHp = (_nextBoss && _nextBoss.maxHp) || 10000000;
                const _hpMap = Object.assign({}, _data.worldBossHp || {});
                _hpMap[_nextId] = _nextMaxHp;                 // 新龍王滿血
                _advancePatch.wbCurrentBossId = _nextId;
                _advancePatch.worldBossHp = _hpMap;
                // ★ v3.14.22 — 記錄新龍王「生出時間」(供歷戰記錄)
                _advancePatch.wbBossSpawnTimes = Object.assign({}, _data.wbBossSpawnTimes || {}, { [_nextId]: Date.now() });
                // 標記接班來源(稽核)
                _advancePatch.wbAdvanceLog = Object.assign({}, _data.wbAdvanceLog || {}, {
                  [_roundKey]: { from: bossId, to: _nextId, at: Date.now(), by: window._gUserId, via: 'settle' }
                });
                _result_nextId = _nextId;                     // 帶出供 Phase 2 後清榜
                _result_nextName = (_nextBoss && _nextBoss.name) || _nextId;
              }
            }catch(_eAdvTx){ console.warn('[WbSettle 接班] transaction 內計算下一隻失敗(不影響結算)', _eAdvTx); }
            tx.set(STATS_DOC(), Object.assign({ wbSettlement: _newSettlement }, _advancePatch), { merge: true });
            return { settled:true, awards:_awards, roundKey:_roundKey, bossName:_bossName, nextBossId:_result_nextId||null, nextBossName:_result_nextName||null };
          });

          if(!_result.settled){
            return _result;
          }

          // Phase 2:把獎勵寫到各玩家的 wbPendingAward(跨玩家寫入,鐵律 1.55)
          const _awards = _result.awards || {};
          const _uids = Object.keys(_awards);
          console.log('[WbSettle] 🎁 開始發放 ' + bossId + '/' + _roundKey + ' 給 ' + _uids.length + ' 位玩家');
          let _failedUids = [];
          let _skippedUids = [];  // ★ v3.13.5 — 已領過/已存在新 pending 的跳過清單
          for(const _uid of _uids){
            try{
              // ★ v3.13.5 — 防重複守門 — 寫前讀現況,雙層檢查:
              //   (A) 已有 pending 且 roundKey === 本次 roundKey 且 claimedAt 有值
              //       → 該玩家本輪已領 → 跳過(可能 GM 強制重結算誤觸)
              //   (B) 已有 pending 且 roundKey === 本次 roundKey 且 claimedAt=null
              //       → 該玩家本輪 pending 已存在但未領 → 跳過(adminForceSettle 寫過)
              //   (C) 已有 pending 但 roundKey !== 本次 → 舊輪殘留,先檢查是否已領
              //       → 已領才覆蓋(正常進入新輪);未領則寫到一個 backup 欄位再寫新的,
              //         避免玩家上輪沒登入直接被吃掉
              //   (D) 沒 pending → 正常寫入
              const _preSnap = await getDoc(PLAYER_DOC(_uid));
              const _preData = _preSnap.exists() ? (_preSnap.data() || {}) : {};
              const _prePending = _preData.wbPendingAward;
              if(_prePending && _prePending.bossId === bossId && _prePending.roundKey === _roundKey){
                // 同 boss + 同 round → A 或 B,直接跳過不覆蓋
                console.log('  ⏭ ' + _uid + ' 同輪 pending 已存在(' + (_prePending.claimedAt ? '已領' : '未領') + '),跳過');
                _skippedUids.push(_uid);
                continue;
              }
              if(_prePending && !_prePending.claimedAt && _prePending.roundKey !== _roundKey){
                // C — 舊輪 pending 未領:把它存進 wbPendingAwardBackup(陣列追加)
                //   不直接寫掉,讓玩家或 GM 有機會發現
                const _backup = Array.isArray(_preData.wbPendingAwardBackup) ? _preData.wbPendingAwardBackup.slice() : [];
                _backup.push(Object.assign({}, _prePending, { _backupAt: Date.now() }));
                // backup 限 5 筆避免膨脹
                const _trimmedBackup = _backup.slice(-5);
                await setDoc(PLAYER_DOC(_uid), {
                  wbPendingAwardBackup: _trimmedBackup,
                  wbPendingAward: Object.assign({}, _awards[_uid], {
                    createdAt: Date.now(),
                    claimedAt: null,
                  })
                }, { merge: true });
                console.log('  ⚠ ' + _uid + ' 舊輪未領 pending 移到 backup,寫新 pending (rank ' + _awards[_uid].rank + ')');
              } else {
                // D(或舊輪已領)— 正常寫
                await setDoc(PLAYER_DOC(_uid), {
                  wbPendingAward: Object.assign({}, _awards[_uid], {
                    createdAt: Date.now(),
                    claimedAt: null,
                  })
                }, { merge: true });
                console.log('  ✅ ' + _uid + ' 第' + _awards[_uid].rank + '名');
              }
            }catch(_e){
              const _code = _e && _e.code;
              if(_code === 'permission-denied'){
                console.warn('  ⚠ ' + _uid + ' pending 寫入被 rules 拒絕(需設 Firestore Rules 開放 wbPendingAward 跨玩家寫入)');
              } else {
                console.warn('  ❌ ' + _uid + ' pending 寫入失敗', _e);
              }
              _failedUids.push(_uid);
            }
          }
          if(_failedUids.length > 0){
            console.warn('[WbSettle] ⚠ ' + _failedUids.length + ' 位玩家發獎失敗,'
              + '請老師到管理員後台手動補發');
            // ★ v3.13.5 — 持久化失敗清單到雲端,讓 GM 後台/其他玩家後續登入都能補
            try{
              await setDoc(STATS_DOC(), {
                wbSettlementFailures: {
                  [bossId]: {
                    [_roundKey]: {
                      failedUids: _failedUids,
                      recordedAt: Date.now(),
                      recordedBy: window._gUserId,
                    }
                  }
                }
              }, { merge: true });
            }catch(_eW){ console.warn('[WbSettle] 持久化失敗清單失敗', _eW); }
            window._wbSettlementFailed = {
              bossId,
              roundKey: _roundKey,
              failedUids: _failedUids,
              awards: _awards,
            };
          }
          // ★ v3.14.21 — 結算 transaction 已原子接班下一隻;這裡做善後(清新榜/同步快取/通知)
          if(_result.nextBossId){
            console.log('[WbSettle 接班 v3.14.21] ✅ 結算 ' + bossId + ' 完成 → 下一隻龍王開戰:' + _result.nextBossId);
            try{
              if(window._cachedGlobalStats){
                window._cachedGlobalStats.wbCurrentBossId = _result.nextBossId;
                const _hm = Object.assign({}, window._cachedGlobalStats.worldBossHp || {});
                const _nb = (window.WORLD_BOSS_LINEUP || []).find(function(b){ return b && b.id === _result.nextBossId; });
                _hm[_result.nextBossId] = (_nb && _nb.maxHp) || 10000000;
                window._cachedGlobalStats.worldBossHp = _hm;
              }
            }catch(_){}
            // best-effort 清新 BOSS 排行榜(新一輪乾淨開局)
            try{
              if(window._wbHpSync && typeof window._wbHpSync.clearLeaderboard === 'function'){
                await window._wbHpSync.clearLeaderboard(_result.nextBossId);
                console.log('[WbSettle 接班] 新 BOSS 排行榜已清空:' + _result.nextBossId);
              }
            }catch(_eClr){ console.warn('[WbSettle 接班] 清榜失敗(不影響接班)', _eClr); }
            try{ if(typeof window._wbApplyCurrentBossSkin === 'function') setTimeout(window._wbApplyCurrentBossSkin, 100); }catch(_){}
            try{ if(typeof window._wbApplyNextBossPreview === 'function') setTimeout(window._wbApplyNextBossPreview, 140); }catch(_){}
            try{ if(typeof showToast === 'function') showToast('🐉 龍王戰結算完畢——下一隻「' + (_result.nextBossName || _result.nextBossId) + '」滿血參戰!'); }catch(_){}
          }
          return { settled:true, totalCount:_uids.length, failedCount:_failedUids.length, skippedCount:_skippedUids.length, advancedTo:_result.nextBossId||null };
        }catch(e){
          console.error('[WbSettle] trySettle 失敗', e);
          return { settled:false, error:e.message };
        }
      }

      // ── 讀自己的 pending(不領,只看) ──
      async function getMyPending(){
        if(!window._gUserId) return null;
        try{
          const _snap = await getDoc(PLAYER_DOC(window._gUserId));
          if(!_snap.exists()) return null;
          const _data = _snap.data() || {};
          const _p = _data.wbPendingAward;
          if(!_p || _p.claimedAt) return null;
          return _p;
        }catch(e){
          console.warn('[WbSettle] getMyPending 失敗', e);
          return null;
        }
      }

      // ── 玩家領取自己的 pending 獎勵(transaction) ──
      //   防重複:① pendingAward.claimedAt 非 null → 已領 ② 本地 lxps_wb_claimed_<bossId>_<roundKey>
      async function claimPending(){
        if(!window._gUserId) return null;
        try{
          // 先讀一次
          const _peekSnap = await getDoc(PLAYER_DOC(window._gUserId));
          if(!_peekSnap.exists()) return null;
          const _peek = (_peekSnap.data() || {}).wbPendingAward;
          if(!_peek) return null;
          if(_peek.claimedAt) return null;
          const _localFlag = 'lxps_wb_claimed_' + _peek.bossId + '_' + _peek.roundKey;
          try{
            if(localStorage.getItem(_localFlag) === '1'){
              // 本地已標 → 補寫雲端旗標,不重發
              await setDoc(PLAYER_DOC(window._gUserId), {
                wbPendingAward: Object.assign({}, _peek, { claimedAt: Date.now() })
              }, { merge: true });
              return null;
            }
          }catch(_){}

          // Transaction 領獎:寫入幣/水晶/書到 players/{uid},機率部分後面在 UI 端決定
          //   注意:火龍王之牙 + 至寶機率 + 稱號發放走 window._taiwanTreasureData 等本地狀態,
          //         不適合在 transaction 內處理 — transaction 只負責「金幣 / 召喚水晶 / 經驗書」,
          //         並把 pending 標記為 claimed
          const _result = await runTransaction(_fbDb, async (tx) => {
            const _txSnap = await tx.get(PLAYER_DOC(window._gUserId));
            const _txData = _txSnap.exists() ? (_txSnap.data() || {}) : {};
            const _txPending = _txData.wbPendingAward;
            if(!_txPending || _txPending.claimedAt){
              return null;  // 已被別的 tab 領了
            }
            // 加幣
            const _curCoins = _txData.knowledgeCoins || 0;
            const _newCoins = _curCoins + (_txPending.coins || 0);
            // 加水晶、經驗書、至寶 EXP 卷軸到背包
            const _curBp = (_txData.playerBackpack && typeof _txData.playerBackpack === 'object') ? _txData.playerBackpack : {};
            const _newBp = Object.assign({}, _curBp);
            if(_txPending.summonCrystals > 0){
              _newBp['summon_crystal'] = Math.min(99, (_newBp['summon_crystal']||0) + _txPending.summonCrystals);
            }
            if(_txPending.expBookDeluxe > 0){
              _newBp['hero_exp_book_deluxe'] = Math.min(99, (_newBp['hero_exp_book_deluxe']||0) + _txPending.expBookDeluxe);
            }
            if(_txPending.expScrollTreasure > 0){
              _newBp['treasure_exp_scroll'] = Math.min(99, (_newBp['treasure_exp_scroll']||0) + _txPending.expScrollTreasure);
            }
            tx.set(PLAYER_DOC(window._gUserId), {
              knowledgeCoins: _newCoins,
              playerBackpack: _newBp,
              playerBackpack_s: JSON.stringify(_newBp),
              wbPendingAward: Object.assign({}, _txPending, { claimedAt: Date.now() }),
            }, { merge: true });
            return Object.assign({}, _txPending);
          });

          if(_result){
            try{ localStorage.setItem(_localFlag, '1'); }catch(_){}
            // 同步本地遊戲狀態,讓玩家當下看到變化
            try{
              if(_result.coins > 0 && typeof _knowledgeCoins !== 'undefined'){
                window._knowledgeCoins = (_knowledgeCoins || 0) + _result.coins;
              }
              if(typeof _playerBackpack === 'object' && _playerBackpack){
                if(_result.summonCrystals > 0){
                  _playerBackpack['summon_crystal'] = Math.min(99, (_playerBackpack['summon_crystal']||0) + _result.summonCrystals);
                }
                if(_result.expBookDeluxe > 0){
                  _playerBackpack['hero_exp_book_deluxe'] = Math.min(99, (_playerBackpack['hero_exp_book_deluxe']||0) + _result.expBookDeluxe);
                }
                if(_result.expScrollTreasure > 0){
                  _playerBackpack['treasure_exp_scroll'] = Math.min(99, (_playerBackpack['treasure_exp_scroll']||0) + _result.expScrollTreasure);
                }
              }
              if(typeof _syncSummonCrystalDisplay === 'function') _syncSummonCrystalDisplay();
              if(typeof refreshAllCoinsDisplay === 'function') refreshAllCoinsDisplay();
            }catch(_eSync){ console.warn('[WbSettle claim 本地同步]', _eSync); }

            // ── 機率部分(在 transaction 外擲,雲端不參與) ──
            // (1) 火龍王之牙:依 rank tier 機率擲;若中且已擁有 → 補 3 張至寶 EXP 卷軸
            const _dragonResult = (function(){
              try{
                if(!_result.dragonTreasureId || !(_result.dragonTreasureChance > 0)){
                  return { granted:false, rolled:false };
                }
                if(typeof window._wbGrantDragonTreasure === 'function'){
                  // _wbGrantDragonTreasure 內部會做完整處理:擲骰 + 寫 _taiwanTreasureData / 卷軸 fallback
                  // ★ v4.56.0 — 改傳 _result.dragonTreasureId(結算當下依被擊敗龍王寫入的正確至寶 id):
                  //   原本只傳 rank → grant 內部用 _WORLD_BOSS_TEAM_REWARDS 寫死的 dragon_fang_fire →
                  //   非火龍王場次排名至寶一律發成炎龍王之牙;且領獎日下一隻龍王已接班,不能用「當前龍王」推算。
                  const _dr = window._wbGrantDragonTreasure(_result.rank, _result.dragonTreasureId);
                  // ★ v3.14.17 — 首次獲得時補寫至寶解鎖歷史(index 端補,不動 world-boss.js)
                  try{ if(_dr && _dr.granted && typeof window._advSaveTreasureUnlockHistory === 'function') window._advSaveTreasureUnlockHistory(_result.dragonTreasureId || 'dragon_fang_fire', 'wb_rank_reward_dragon'); }catch(_){}
                  return _dr;
                }
                return { granted:false, error:'_wbGrantDragonTreasure 不存在' };
              }catch(eD){ console.warn('[WbSettle claim 火龍王之牙]', eD); return { granted:false, error:eD.message }; }
            })();

            // (2) 未收錄至寶機率掉落(rare/normal tier 才有 treasureChance)
            const _treasureResult = (function(){
              try{
                // ★ v3.14.13 — 修復:第 1-5 名分級只有 treasureLabel(必得)沒有 treasureChance,
                //   舊判定 treasureChance>0 直接 return → 救世主級/勇者級的「未收錄至寶 ×1」從未發過。
                //   修正:有 label 無 chance → 視為 100% 必得,rarity 依 tier 對應(救世主→神話優先/勇者→傳說優先)。
                let _effChance = (_result.treasureChance > 0) ? _result.treasureChance : 0;
                let _effRarity = _result.treasureRarity || 'rare';
                if(_effChance <= 0 && _result.treasureLabel){
                  _effChance = 1.0;
                  _effRarity = (_result.tier === 'legendary') ? 'mythic' : 'legendary';
                }
                if(!(_effChance > 0)) return { won:false, rolled:false };
                const _won = Math.random() < _effChance;
                if(!_won) return { won:false, rolled:true, chance:_effChance };
                // 從 TAIWAN_TREASURES 找未獲得的(優先 rarity 對應 _effRarity 之上)
                if(typeof window.TAIWAN_TREASURES !== 'object') return { won:false, error:'TAIWAN_TREASURES 不存在' };
                const _td = (typeof window._taiwanTreasureData !== 'undefined') ? (window._taiwanTreasureData || {}) : {};
                const _RARITY_RANK = { 'mythic':5, 'legendary':4, 'epic':3, 'rare':2, 'normal':1 };
                const _targetRank = _RARITY_RANK[_effRarity || 'rare'] || 2;  // ★ v3.14.13 改用 _effRarity
                const _candidates = [];
                Object.keys(window.TAIWAN_TREASURES).forEach(function(_tid){
                  const _t = window.TAIWAN_TREASURES[_tid];
                  if(!_t) return;  // ★ v3.15.86(老師甲是)— 不再排除 noSummon → 8 龍王至寶(mythic)納入「未收錄至寶」隨機池,上榜可隨機獲得任一未擁有龍王至寶
                  // ★★ v5.101.0 — 【中國三至寶外洩根治】★★
                  //   根因:v3.15.86 為了放行 8 龍王至寶而拿掉 noSummon 排除,改成掃**整張**
                  //   TAIWAN_TREASURES;v5.43.0 又把 打神鞭/金箍棒/青龍偃月刀(rarity:'mythic')
                  //   加進同一張表 ⇒ 三件「路線首通專屬」至寶落入龍王排名獎勵候選池。
                  //   而救世主級 _targetRank=mythic(5)、_atTier 只留 rank>=5 ⇒ 該階池
                  //   = 8 龍王 + 3 中國至寶,學生只要世界龍王上榜就可能在中國關還沒開放時
                  //   直接拿到三至寶之一,「路線首通必得」的設計語意整個消失。
                  //   ★ 刻意只擋 isChinaTreasure 一種旗標(與既有 isDragonTreasure 白名單同層),
                  //     不改回 noSummon 全擋 —— 那會把龍王 8 件一起擋掉,推翻 v3.15.86 老師裁定。
                  //   ★ 日後新增「關卡首通專屬至寶」只要在定義上掛 isChinaTreasure 同類旗標
                  //     並在此加一行,即自動涵蓋。
                  if(_t.isChinaTreasure === true) return;
                  const _owned = _td[_tid] && _td[_tid].lv >= 1;
                  if(_owned) return;
                  const _r = _RARITY_RANK[_t.rarity || 'normal'] || 1;
                  _candidates.push({ id:_tid, rarity:_t.rarity, rRank:_r, name:_t.name });
                });
                if(_candidates.length === 0){
                  // ★ v3.15.86(老師乙統一5)— 含 8 龍王至寶在內全部收齊 → 重複轉至寶經驗卷軸 ×5(對齊自選券/該場龍王至寶)
                  try{ if(typeof window.backpackAdd === 'function') window.backpackAdd('treasure_exp_scroll', 5); }catch(_){}
                  try{ if(typeof window.gameCloudSave === 'function') window.gameCloudSave(); }catch(_){}
                  return { won:true, granted:false, reason:'all_owned', fallbackScrolls:5 };
                }
                // 排序:rarity 高的優先(若 _targetRank=4,先抓 rarity>=4 的;沒有再降級)
                _candidates.sort(function(a,b){ return b.rRank - a.rRank; });
                // 在達到 _targetRank 的候選裡隨機取一個
                const _atTier = _candidates.filter(function(c){ return c.rRank >= _targetRank; });
                const _pick = _atTier.length > 0
                  ? _atTier[Math.floor(Math.random() * _atTier.length)]
                  : _candidates[Math.floor(Math.random() * _candidates.length)];
                // 寫入 _taiwanTreasureData
                if(typeof window._taiwanTreasureData === 'undefined' || !window._taiwanTreasureData){
                  window._taiwanTreasureData = {};
                }
                window._taiwanTreasureData[_pick.id] = { lv:1, exp:0, equippedTo:null, invested:{hp:0,atk:0,sp:0,spd:0} };
                // ★ v3.14.17 — 補寫至寶解鎖歷史(來源 wb_rank_reward,稽核/未來補發判定用)
                try{ if(typeof window._advSaveTreasureUnlockHistory === 'function') window._advSaveTreasureUnlockHistory(_pick.id, 'wb_rank_reward'); }catch(_){}
                try{
                  if(typeof window._saveTaiwanTreasureData === 'function') window._saveTaiwanTreasureData();
                  else localStorage.setItem('lxps_taiwan_treasures', JSON.stringify(window._taiwanTreasureData));
                }catch(_){}
                try{ if(typeof window.gameCloudSave === 'function') window.gameCloudSave(); }catch(_){}
                return { won:true, granted:true, id:_pick.id, name:_pick.name, rarity:_pick.rarity };
              }catch(eT){ console.warn('[WbSettle claim 未收錄至寶]', eT); return { won:false, error:eT.message }; }
            })();

            // (3) 「屠龍者」稱號(僅第 1 名)
            const _titleResult = (function(){
              try{
                if(!_result.isChampion) return { granted:false };
                if(!_result.championTitleTemplate) return { granted:false };
                const _titleStr = String(_result.championTitleTemplate).replace('{bossName}', _result.bossName || '');
                // 沿用既有稱號系統:若有 grantTitle 函式 → 用它;否則寫到背包某欄
                try{
                  if(typeof window._grantPlayerTitle === 'function'){
                    window._grantPlayerTitle(_titleStr);
                    return { granted:true, title:_titleStr };
                  }
                }catch(_eT){}
                // fallback:寫到 unlockedTitles 陣列(後續由「我的稱號」介面讀)
                try{
                  if(!Array.isArray(window._unlockedTitles)) window._unlockedTitles = [];
                  if(window._unlockedTitles.indexOf(_titleStr) < 0){
                    window._unlockedTitles.push(_titleStr);
                    try{ if(typeof window.gameCloudSave === 'function') window.gameCloudSave(); }catch(_){}
                  }
                  return { granted:true, title:_titleStr, viaFallback:true };
                }catch(_eF){ return { granted:false, error:_eF.message }; }
              }catch(eT){ console.warn('[WbSettle claim 稱號]', eT); return { granted:false, error:eT.message }; }
            })();

            // 把機率結果 patch 回 _result 給 UI 顯示
            _result._dragonResult = _dragonResult;
            _result._treasureResult = _treasureResult;
            _result._titleResult = _titleResult;

            // ★ v3.14.19 — 把「實際擲骰/發放結果」寫回 wbPendingAward.grantedManifest(雲端永久留底)
            //   背景:5408 第 2 名學生災情 — claim 當下龍牙擲中+必得至寶都有發,之後被覆蓋吃掉;
            //         舊補償只能靠機率分類「猜」,對「有中卻被吃」的學生顯示錯誤的「沒抽中」說明。
            //   有了 manifest,補償可逐件精準比對「確實發過 vs 現在還在不在」,缺了就原樣補回。
            try{
              await setDoc(PLAYER_DOC(window._gUserId), {
                wbPendingAward: {
                  grantedManifest: {
                    at: Date.now(),
                    dragonId: _result.dragonTreasureId || 'dragon_fang_fire',
                    dragonGranted: !!(_dragonResult && (_dragonResult.granted || _dragonResult.alreadyOwned)),
                    treasureGranted: !!(_treasureResult && _treasureResult.granted),
                    treasureId: (_treasureResult && _treasureResult.id) || null,
                    treasureName: (_treasureResult && _treasureResult.name) || null,
                  }
                }
              }, { merge: true });
              console.log('[WbSettle claim] ✅ grantedManifest 已寫回雲端(補償精準比對用)');
            }catch(_eMf){ console.warn('[WbSettle claim] grantedManifest 寫回失敗(不影響領獎)', _eMf); }
          }
          return _result;
        }catch(e){
          console.error('[WbSettle] claimPending 失敗', e);
          return null;
        }
      }

      // ── GM 強制補發給單一玩家(後台用) ──
      //   opts.force: true → 即使該玩家已領過/已有 pending 仍強制重寫(慎用,可造成重複發)
      //   預設 false:檢查到該玩家本輪已有 pending → 拒絕,並回報狀態給 GM 判斷
      async function adminForceSettle(bossId, targetUid, opts){
        if(!bossId || !targetUid) return { ok:false, reason:'missing_args' };
        const _force = !!(opts && opts.force);
        try{
          const _gs = window._cachedGlobalStats || {};
          const _settlement = _gs.wbSettlement || {};
          const _currentRound = _gs.wbCurrentRound || {};
          const _round = _currentRound[bossId];
          if(!_round) return { ok:false, reason:'no_current_round' };
          const _roundKey = _round.roundKey;
          const _entry = _settlement[bossId] && _settlement[bossId][_roundKey];
          if(!_entry) return { ok:false, reason:'not_settled_yet', tip:'請先讓系統自動結算或手動觸發 trySettle' };
          const _award = (_entry.awards || {})[targetUid];
          if(!_award) return { ok:false, reason:'uid_not_in_awards', tip:'此玩家不在結算榜上(可能未上榜或 uid 錯誤)' };
          // ★ v3.13.5 — 防重複守門:讀目標玩家現況,雙層檢查
          const _tarSnap = await getDoc(PLAYER_DOC(targetUid));
          const _tarData = _tarSnap.exists() ? (_tarSnap.data() || {}) : {};
          const _tarPending = _tarData.wbPendingAward;
          if(_tarPending && _tarPending.bossId === bossId && _tarPending.roundKey === _roundKey){
            if(_tarPending.claimedAt && !_force){
              return {
                ok:false,
                reason:'already_claimed',
                tip:'⚠️ 此玩家本輪獎勵已領取(claimedAt: ' + new Date(_tarPending.claimedAt).toLocaleString('zh-TW') + ')。'
                  + '若確認要重發(可能造成重複發放),請傳 opts.force=true。',
                claimedAt: _tarPending.claimedAt,
              };
            }
            if(!_tarPending.claimedAt && !_force){
              return {
                ok:false,
                reason:'already_pending',
                tip:'✅ 此玩家本輪 pending 已存在(尚未領取),不需要補發。'
                  + '請玩家自己登入領取,或請玩家檢查 console 是否有錯誤。',
                pendingCreatedAt: _tarPending.createdAt,
              };
            }
          }
          await setDoc(PLAYER_DOC(targetUid), {
            wbPendingAward: Object.assign({}, _award, {
              createdAt: Date.now(),
              claimedAt: null,
              _adminForced: true,
              _adminForcedAt: Date.now(),
              _adminForcedBy: window._gUserId,
            })
          }, { merge: true });
          return { ok:true, award:_award, forcedOverwrite: _force && !!_tarPending };
        }catch(e){
          console.error('[WbSettle] adminForceSettle 失敗', e);
          return { ok:false, error:e.message };
        }
      }

      window._wbSettle = {
        trySettle: trySettle,
        getMyPending: getMyPending,
        claimPending: claimPending,
        getStatus: getStatus,
        adminForceSettle: adminForceSettle,
        _calcSettleAt: _calcSettleAt,  // 給除錯用
      };
      console.log('[WbSettle v1] ✅ 世界 BOSS 結算模組已掛載');
    })();

    // ════════════════════════════════════════════════════════════════════
    // ★ v3.14.13(丙)— 龍王祝福倍率 helper(index 本體版,不依賴延遲載入的 world-boss.js)
    //   讀 stats/global.wbBlessing(經 _cachedGlobalStats 快取,onSnapshot 同步)。
    //   消費點:戰鬥 EXP(grantBattleExp / 小怪 baseExp)、戰鬥知識幣(BOSS 結算 / 答題即時)、
    //           物品掉落(日本BOSS/八岐果實/杏花/黑暗球5項/稀有怪)。
    //   ⚠ 鐵律:英雄「解鎖機率」(_dropMultUnlock / SSR 稀有角色 / 巫女解救)一律【不】吃祝福加成。
    // ════════════════════════════════════════════════════════════════════
    window._gameBlessingMult = function(){
      var m = 1.0;
      // 龍王的祝福(全服·不分科目)
      try{
        const b = window._cachedGlobalStats && window._cachedGlobalStats.wbBlessing;
        if(b && b.active && !(b.expiresAt && b.expiresAt < Date.now())) m *= (1 + ((b.bonusPct || 0) / 100));
      }catch(_){}
      // ★ v3.29.0 課堂獎勵加成:僅當「本場冒險科目 _advPlayerSubject」== GM 指定的一般科目時,
      //   結算 EXP / 知識幣 / 掉寶率 +pct%(與龍王祝福乘算並存;英雄解鎖機率不吃此加成,同祝福)。
      try{
        const c = window._classBoost;
        if(c && c.enabled && (!c.expiresAt || c.expiresAt > Date.now())
           && typeof _advPlayerSubject !== 'undefined' && _advPlayerSubject
           && c.subject === _advPlayerSubject){
          m *= (1 + ((typeof c.pct === 'number' ? c.pct : 25) / 100));
        }
      }catch(_){}
      // ★ v3.32.0 本日獎勵題庫:本場冒險科目 == 當日自動輪替科目時 +25%。
      //   GM 手動加成(classRewardBoost)若對「同一科目」已生效 → 本段跳過(防雙重疊乘);
      //   GM 加成其他科目則兩者各自獨立生效。
      try{
        const dz = (typeof window._dailyBonusInfo === 'function') ? window._dailyBonusInfo() : null;
        if(dz && dz.subject && typeof _advPlayerSubject !== 'undefined' && _advPlayerSubject
           && dz.subject === _advPlayerSubject){
          const c2 = window._classBoost;
          const _gmSame = !!(c2 && c2.enabled && (!c2.expiresAt || c2.expiresAt > Date.now()) && c2.subject === _advPlayerSubject);
          if(!_gmSame) m *= (1 + ((dz.pct || 25) / 100));
        }
      }catch(_){}
      return m;
    };

    // ★ v3.14.14 — 世界 BOSS 入口卡片「龍王的祝福」標籤刷新(老師需求 2026-06-12)
    //   祝福生效中:顯示金色膠囊標籤 + 剩餘時間倒數(>=1 小時顯示小時,<1 小時顯示分鐘);
    //   未生效/過期:隱藏。每 30 秒自動刷新;祝福快取一更新也會即刻刷新。
    // ═══════════════════════════════════════════════════════════════
    // ★ v3.14.21(老師裁示修正)— 接班時機改為「龍王倒下隔天 8:00 結算當下」
    //   (不再等祝福 72h 到期)。接班已原子併入 _wbSettle.trySettle 的結算 transaction:
    //   結算成功 = 同時寫 wbCurrentBossId=下一隻+新龍王滿血。祝福仍獨立持續 72h 不受影響。
    //   本函式保留為相容殼:呼叫者(登入/輪詢)→ 改為觸發 trySettle 巡迴(沒人線上時靠首位登入者推進)。
    //   ⚠ Firestore Rules:stats/global 玩家可寫欄位需含 wbCurrentBossId / worldBossHp / wbSettlement / wbAdvanceLog。
    // ═══════════════════════════════════════════════════════════════
    window._wbTryAutoAdvanceBoss = async function(){
      try{
        if(window._wbAutoAdvanceBusy) return;
        if(!window._wbSettle || typeof window._wbSettle.trySettle !== 'function') return;
        window._wbAutoAdvanceBusy = true;
        const _downTimes = (window._cachedGlobalStats && window._cachedGlobalStats.wbBossDownTimes) || {};
        const _ids = Object.keys(_downTimes);
        if(_ids.length === 0){ window._wbAutoAdvanceBusy = false; return; }
        for(const _bid of _ids){
          try{
            const _r = await window._wbSettle.trySettle(_bid);
            if(_r && _r.settled && _r.advancedTo){
              console.log('[WB 接班 v3.14.21] 結算 ' + _bid + ' 並接班 → ' + _r.advancedTo);
            }
          }catch(_eOne){ console.warn('[WB 接班] trySettle(' + _bid + ') 失敗', _eOne); }
        }
      }catch(_eAdv){
        console.warn('[WB 接班 v3.14.21] 失敗(可能 Rules 未開放玩家寫 wbCurrentBossId/wbSettlement)', _eAdv);
      }finally{
        window._wbAutoAdvanceBusy = false;
      }
    };

    window._refreshAdvWbBlessingTag = function(){
      try{
        const _tag = document.getElementById('adv-worldboss-blessing-tag');
        if(!_tag) return;  // 還沒進到關卡選擇頁
        const b = window._cachedGlobalStats && window._cachedGlobalStats.wbBlessing;
        const _now = Date.now();
        if(!b || !b.active || !(b.expiresAt > _now)){
          _tag.style.display = 'none';
          return;
        }
        const _remainEl = document.getElementById('adv-wb-blessing-remain');
        const _pctEl = document.getElementById('adv-wb-blessing-pct');
        const _remainMs = b.expiresAt - _now;
        const _hours = Math.floor(_remainMs / 3600000);
        if(_remainEl){
          _remainEl.textContent = _hours >= 1
            ? ('還有 ' + _hours + ' 小時')
            : ('還有 ' + Math.max(1, Math.ceil(_remainMs / 60000)) + ' 分鐘');
        }
        if(_pctEl) _pctEl.textContent = String(b.bonusPct || 25);
        _tag.style.display = '';
      }catch(_e){ /* 靜默 */ }
    };
    try{
      window._refreshAdvWbBlessingTag();
      setInterval(function(){ try{ window._refreshAdvWbBlessingTag(); }catch(_){} }, 30000);
    }catch(_){}

    // ════════════════════════════════════════════════════════════════════
    // ★ v3.14.13(甲)— 世界 BOSS 排名獎勵「領獎彈窗」_wbShowPendingAwardModal
    //   背景:結算系統 v1(v3.13.5)的登入流程會呼叫本函式,但函式一直沒實作
    //         → 玩家有 pending 也只在 console 默默 warn,claimPending 永遠不會被觸發
    //         → 排名獎勵全部躺在雲端沒人領得到。本版補上這「最後一哩」。
    //   流程:第一幕展示排名+獎勵預告 → 玩家按「領取」→ claimPending()(transaction 入帳+擲骰)
    //         → 第二幕展示實際入帳與擲骰結果(火龍王之牙/未收錄至寶/冠軍稱號)。
    //   「稍後領取」:pending 不動,下次登入會再跳(claimPending 冪等,跨裝置安全)。
    // ════════════════════════════════════════════════════════════════════
    window._wbShowPendingAwardModal = function(_pending){
      try{
        if(!_pending) return;
        if(document.getElementById('wb-award-modal-overlay')) return; // 防重複開啟
        const _esc = function(s){ return String(s == null ? '' : s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;'); };
        const _rank = _pending.rank || 0;
        const _rankIcon = _rank === 1 ? '🏆' : _rank <= 5 ? '🥈' : _rank <= 10 ? '🥉' : _rank <= 20 ? '📦' : '🎁';
        const _bossName = _esc(_pending.bossName || '世界 BOSS');
        const _tierLabel = _esc(_pending.tierLabel || '');
        // 獎勵預告列
        const _rows = [];
        if(_pending.coins > 0)             _rows.push('💰 知識幣 ×' + Number(_pending.coins).toLocaleString());
        if(_pending.summonCrystals > 0)    _rows.push('🔮 召喚水晶 ×' + _pending.summonCrystals);
        if(_pending.expBookDeluxe > 0)     _rows.push('📕 豪華典藏版EXP書 ×' + _pending.expBookDeluxe);
        if(_pending.expScrollTreasure > 0) _rows.push('📜 至寶EXP卷軸 ×' + _pending.expScrollTreasure);
        if(_pending.dragonTreasureChance > 0){
          // ★ v3.14.20 — 龍王至寶名動態(維蘇威=火龍王之牙/翠玉草=草龍王之鬚,依 pending.dragonTreasureId)
          const _dtNmP = (function(){ try{ const _t = window.TAIWAN_TREASURES && window.TAIWAN_TREASURES[_pending.dragonTreasureId]; return (_t && _t.name) || '火龍王之牙'; }catch(_){ return '火龍王之牙'; } })();
          _rows.push(_pending.dragonTreasureChance >= 1
            ? ('🐉 <b style="color:#ff8866;">' + _esc(_dtNmP) + ' ×1 必得!</b>')
            : ('🐉 ' + _esc(_dtNmP) + '(' + Math.round(_pending.dragonTreasureChance * 100) + '% 機率)'));
        }
        if(_pending.treasureChance > 0 && _pending.treasureLabel) _rows.push('✨ ' + _esc(_pending.treasureLabel));
        else if(_pending.treasureLabel && _pending.tier !== 'rare' && _pending.tier !== 'normal') _rows.push('✨ ' + _esc(_pending.treasureLabel));
        if(_pending.isChampion) _rows.push('👑 專屬稱號【' + _esc(String(_pending.championTitleTemplate || '屠龍者・{bossName}').replace('{bossName}', _pending.bossName || '')) + '】');
        const _ov = document.createElement('div');
        _ov.id = 'wb-award-modal-overlay';
        _ov.style.cssText = 'position:fixed;inset:0;z-index:999999;display:flex;align-items:center;justify-content:center;'
          + 'background:rgba(5,8,20,0.88);backdrop-filter:blur(4px);padding:16px;';
        _ov.innerHTML =
          '<div id="wb-award-modal-card" style="width:min(92vw,560px);max-height:88vh;overflow-y:auto;-webkit-overflow-scrolling:touch;'
          +   'background:linear-gradient(165deg,#241a08,#0e0a18 60%,#1a1028);border:2.5px solid rgba(255,200,80,0.75);'
          +   'border-radius:20px;box-shadow:0 0 50px rgba(255,180,60,0.4);padding:26px 22px;text-align:center;font-family:inherit;">'
          + '<div style="font-size:clamp(34px,5vw,48px);line-height:1;">' + _rankIcon + '</div>'
          + '<div style="font-size:clamp(22px,3vw,30px);font-weight:900;color:#ffe066;margin-top:8px;text-shadow:0 0 14px rgba(255,200,80,0.6);">世界 BOSS 討伐獎勵</div>'
          + '<div style="font-size:clamp(15px,2vw,19px);color:#ffd9a0;margin-top:6px;line-height:1.5;">' + _bossName + ' 已被全伺服器擊敗!<br>你的隊伍拿下 <b style="color:#ffee88;font-size:1.2em;">全伺服器第 ' + _rank + ' 名</b>' + (_tierLabel ? '(' + _tierLabel + ')' : '') + '</div>'
          + '<div id="wb-award-modal-body" style="margin:16px 0 6px;padding:14px 16px;background:rgba(255,200,80,0.08);border:1.5px solid rgba(255,200,80,0.3);border-radius:14px;text-align:left;">'
          +   _rows.map(function(r){ return '<div style="font-size:clamp(16px,2vw,20px);color:#ffeecc;line-height:1.9;">' + r + '</div>'; }).join('')
          + '</div>'
          + '<button id="wb-award-claim-btn" style="margin-top:14px;width:100%;padding:16px;font-size:clamp(18px,2.4vw,24px);font-weight:900;'
          +   'background:linear-gradient(135deg,#ffb347,#cc7722);color:#fff;border:none;border-radius:14px;cursor:pointer;'
          +   'box-shadow:0 4px 18px rgba(255,160,50,0.45);letter-spacing:2px;font-family:inherit;">🎁 領取獎勵</button>'
          + '<div style="margin-top:10px;"><button id="wb-award-later-btn" style="background:none;border:none;color:#998866;'
          +   'font-size:clamp(13px,1.6vw,16px);cursor:pointer;text-decoration:underline;font-family:inherit;">稍後領取(下次登入會再提醒)</button></div>'
          + '</div>';
        document.body.appendChild(_ov);
        try{ if(typeof playSfx === 'function') playSfx('sfx-gentle', 0.5); }catch(_){}
        const _close = function(){ try{ _ov.remove(); }catch(_){ if(_ov && _ov.parentNode) _ov.parentNode.removeChild(_ov); } };
        const _laterBtn = document.getElementById('wb-award-later-btn');
        if(_laterBtn) _laterBtn.onclick = _close;
        const _btn = document.getElementById('wb-award-claim-btn');
        if(_btn) _btn.onclick = async function(){
          _btn.disabled = true;
          _btn.style.opacity = '0.6';
          _btn.textContent = '⏳ 領取中…';
          let _r = null;
          try{ _r = await window._wbSettle.claimPending(); }catch(_eC){ console.warn('[WB領獎彈窗] claim 例外', _eC); }
          const _body = document.getElementById('wb-award-modal-body');
          if(!_r){
            if(_body) _body.innerHTML = '<div style="font-size:clamp(16px,2vw,20px);color:#ffcc99;line-height:1.7;text-align:center;">'
              + 'ℹ️ 獎勵可能已在其他裝置領取,或暫時無法連線。<br>若尚未入帳,下次登入會自動再提醒。</div>';
            _btn.textContent = '關閉';
            _btn.disabled = false; _btn.style.opacity = '1';
            _btn.onclick = _close;
            return;
          }
          // 第二幕:實際入帳 + 擲骰結果
          const _out = [];
          if(_r.coins > 0)             _out.push('💰 知識幣 +' + Number(_r.coins).toLocaleString() + ' <span style="color:#7df0a8;">✔ 已入帳</span>');
          if(_r.summonCrystals > 0)    _out.push('🔮 召喚水晶 +' + _r.summonCrystals + ' <span style="color:#7df0a8;">✔ 已入帳</span>');
          if(_r.expBookDeluxe > 0)     _out.push('📕 豪華典藏版EXP書 +' + _r.expBookDeluxe + ' <span style="color:#7df0a8;">✔ 已入帳</span>');
          if(_r.expScrollTreasure > 0) _out.push('📜 至寶EXP卷軸 +' + _r.expScrollTreasure + ' <span style="color:#7df0a8;">✔ 已入帳</span>');
          const _d = _r._dragonResult || {};
          const _dtNmR = (function(){ try{ const _t = window.TAIWAN_TREASURES && window.TAIWAN_TREASURES[_r.dragonTreasureId]; return (_t && _t.name) || '火龍王之牙'; }catch(_){ return '火龍王之牙'; } })();  // ★ v3.14.20
          if(_d.granted)            _out.push('🐉 <b style="color:#ff8866;">' + _esc(_dtNmR) + ' 獲得!</b>(至寶圖鑑查看)');
          else if(_d.alreadyOwned)  _out.push('🐉 ' + _esc(_dtNmR) + '已擁有 → 補發 📜 至寶EXP卷軸 ×3');
          else if(_d.rolled !== false && _r.dragonTreasureChance > 0 && _r.dragonTreasureChance < 1)
            _out.push('🐉 ' + _esc(_dtNmR) + '…這次沒掉落(' + Math.round(_r.dragonTreasureChance * 100) + '% 機率),下次再挑戰!');
          const _t = _r._treasureResult || {};
          if(_t.granted)                 _out.push('✨ <b style="color:#cc99ff;">獲得未收錄至寶【' + _esc(_t.name || _t.id) + '】!</b>');
          else if(_t.won && _t.reason === 'all_owned') _out.push('✨ 至寶機率中獎,但你已收服所有至寶 → 改贈 至寶經驗卷軸 ×5!');
          else if(_t.rolled)             _out.push('✨ 未收錄至寶…這次沒掉落,下次再挑戰!');
          const _ti = _r._titleResult || {};
          if(_ti.granted) _out.push('👑 <b style="color:#ffe066;">獲得稱號【' + _esc(_ti.title) + '】!</b>');
          if(_body) _body.innerHTML = _out.map(function(r){ return '<div style="font-size:clamp(16px,2vw,20px);color:#ffeecc;line-height:1.9;">' + r + '</div>'; }).join('')
            || '<div style="text-align:center;color:#ffeecc;">獎勵已入帳!</div>';
          _btn.textContent = '🎉 太棒了!';
          _btn.disabled = false; _btn.style.opacity = '1';
          _btn.onclick = _close;
          try{ if(typeof playSfx === 'function') playSfx('sfx-confirm', 0.5); }catch(_){}
          try{ if(typeof refreshAllCoinsDisplay === 'function') refreshAllCoinsDisplay(); }catch(_){}
        };
      }catch(_eM){ console.warn('[WB領獎彈窗] 建立失敗', _eM); }
    };

    // ════════════════════════════════════════════════════════════════════
    // ★ v3.14.17 — 世界 BOSS 排名獎勵「至寶遺失自動補償」(零 console,學生點視窗領取)
    // ★ v3.14.19 — 補償 v2「證據導向逐件精準比對」(5408 第 2 名學生災情根治)
    //   災情:claim 當下龍牙擲中+必得至寶(台北101槍)都有發、歷史也有寫,之後仍被覆蓋吃掉;
    //         舊判定「歷史有 wb_rank 就 return」→ 歷史在、東西沒了 → 補償永不觸發;
    //         且 2-5 名機率龍牙一律顯示「沒抽中」,對確實擲中的學生是錯誤訊息。
    //   v2 判定:
    //     ① wbPendingAward.claimedAt 已標(確實領過)
    //     ② 證據優先序:grantedManifest(v3.14.19 claim 寫回)> 歷史 wb_rank* 紀錄 id > 必得推定
    //     ③ 逐件對 _taiwanTreasureData 檢查:「確實發過但現在缺」→ 照原 id 精準補回(冪等)
    //     ④ 無證據的機率項才顯示「沒抽中」說明;無證據的必得項隨機補(歷史 wb_rank_compensate 防重抽)
    // ════════════════════════════════════════════════════════════════════
    window._wbCheckAndCompensateLostTreasure = async function(){
      try{
        if(!window._gUserId || !window._fbDb) return;
        // ★ v3.15.41 — 修 ReferenceError:PLAYER_DOC 是別的函式作用域的區域 const,本函式拿不到
        //   → 每次第一行 getDoc 就 throw、被 try/catch 吞成 WARN,世界BOSS排名至寶自動補償整支從未跑成功。
        const PLAYER_DOC = (uid) => doc(window._fbDb, 'players', uid);
        if(document.getElementById('wb-award-modal-overlay')) return; // 領獎窗開著就不疊
        // ① 讀自己的 pending(claim 過的也讀得到)
        const _snap = await getDoc(PLAYER_DOC(window._gUserId));
        if(!_snap.exists()) return;
        const _p = (_snap.data() || {}).wbPendingAward;
        if(!_p || !_p.claimedAt) return;  // 沒領過 → 走正常領獎窗,不歸補償管
        // ★ v4.13.6 修(老師回報:排名獎勵至寶補發通知每次登入重複跳,圖鑑其實已解鎖):
        //   根因—原本補償領取後「從未寫任何已補償旗標」,只靠下次登入 _owned() 判斷是否缺件;
        //   但登入早期 _taiwanTreasureData 尚未載入,或補回 id 與實際擁有 id 對不上 → 誤判仍缺 → 每次重跳。
        //   修法—補償領取完成即寫持久旗標(雲端 wbPendingAward.treasureCompClaimedAt + 本地),此處開頭先檢查,已補償過就不再跳。
        var _compDoneKey = 'lxps_wb_treas_comp_done_' + (_p.bossId || 'wb') + '_' + (_p.roundKey || 'r');
        if(_p.treasureCompClaimedAt) return;                                     // 雲端權威:已補償過 → 不再跳
        try{ if(localStorage.getItem(_compDoneKey) === '1') return; }catch(_){}   // 本地保險(同裝置)
        // ════ ★ v3.14.19 補償 v2:證據導向「逐件精準比對」════
        //   舊版兩大盲點(5408 第 2 名學生災情):
        //   ① 「歷史有 wb_rank 紀錄就提前 return」— 歷史只證明「發過」,不證明「還在」;
        //      v3.14.17 claim 會寫歷史,之後至寶再被覆蓋吃掉 → 歷史在、東西沒了 → 補償永不觸發。
        //   ② 機率項(2-5 名龍牙 75%)一律顯示「沒抽中」— 對 claim 當下確實擲中的學生是錯誤訊息。
        //   v2 作法:收集「確實發過」的證據(grantedManifest > 歷史紀錄 id > 必得推定),
        //   逐件對 _taiwanTreasureData 檢查 — 缺了就照原 id 補回(冪等:已擁有自動跳過)。
        const _isMustTreasure = (_p.tier === 'legendary' || _p.tier === 'epic') && !!_p.treasureLabel;
        const _isMustDragon = (_p.dragonTreasureChance >= 1) && !!_p.dragonTreasureId;
        const _hasAnyProb = (_p.dragonTreasureChance > 0 && _p.dragonTreasureChance < 1) || (_p.treasureChance > 0);
        if(!_isMustTreasure && !_isMustDragon && !_hasAnyProb && !_p.grantedManifest) return;
        // ── 歷史紀錄(本地 + 雲端 player doc 聯集,雙保險)──
        let _hist = [];
        try{ const _raw = localStorage.getItem('adv_treasure_unlock_history'); _hist = _raw ? JSON.parse(_raw) : []; if(!Array.isArray(_hist)) _hist = []; }catch(_){}
        try{
          const _cloudHist = (_snap.data() || {})._treasureUnlockHistory;
          if(Array.isArray(_cloudHist)) _hist = _hist.concat(_cloudHist);
        }catch(_){}
        const _td = (typeof window._taiwanTreasureData === 'object' && window._taiwanTreasureData) || {};
        const _owned = function(_tid){ return !!(_tid && _td[_tid] && _td[_tid].lv >= 1); };
        const _dragonId = _p.dragonTreasureId || 'dragon_fang_fire';
        // ── 收集「確實發過」證據 ──
        const _mf = (_p.grantedManifest && typeof _p.grantedManifest === 'object') ? _p.grantedManifest : null;
        const _histWb = _hist.filter(function(r){ return r && r.id && typeof r.source === 'string' && r.source.indexOf('wb_rank') === 0; });
        let _evDragon = false;        // 龍牙確實發過
        let _evTreasureId = null;     // 未收錄至寶確實發過的精準 id
        if(_mf){
          if(_mf.dragonGranted) _evDragon = true;
          if(_mf.treasureGranted && _mf.treasureId) _evTreasureId = _mf.treasureId;
        }
        // 歷史補強(legacy claim 無 manifest,但 v3.14.17 claim 有寫歷史 id)
        _histWb.forEach(function(r){
          if(r.id === _dragonId || String(r.id).indexOf('dragon_fang') === 0 || String(r.id).indexOf('dragon_whisker') === 0){ _evDragon = true; }
          else if(r.source === 'wb_rank_reward' || r.source === 'wb_rank_compensate'){ if(!_evTreasureId) _evTreasureId = r.id; }
        });
        // ── 組「補發清單」與「機率說明清單」──
        const _items = [];
        const _notices = [];
        const _histHasCompRandom = _histWb.some(function(r){ return r.source === 'wb_rank_compensate'; });
        // (1) 龍牙:有證據(manifest/歷史)或第 1 名必得 → 缺了就精準補;
        //     僅機率且無任何證據 → 說明「沒抽中」
        const _dtNmC = (function(){ try{ const _t = window.TAIWAN_TREASURES && window.TAIWAN_TREASURES[_dragonId]; return (_t && _t.name) || '火龍王之牙'; }catch(_){ return '火龍王之牙'; } })();  // ★ v3.14.20 龍王至寶名動態
        if((_evDragon || _isMustDragon)){
          if(!_owned(_dragonId)) _items.push({ kind:'dragon', id:_dragonId, label:'🐉 ' + _dtNmC });
        } else if(_p.dragonTreasureChance > 0 && !_owned(_dragonId)){
          _notices.push('🐉 ' + _dtNmC + '(' + Math.round(_p.dragonTreasureChance * 100) + '% 機率):'
            + '這次擲骰<b style="color:#ffaa88;">沒有抽中</b>,並不是系統漏發喔!下次擊敗龍王再挑戰!');
        }
        // (2) 未收錄至寶:
        //     a. 有精準 id 證據 → 缺了就照原 id 補回(冪等,還在就跳過)
        //     b. 無證據但必得 tier → 隨機補一件未擁有(維持舊邏輯,以歷史 wb_rank_compensate 防重複抽)
        //     c. 機率型且無證據 → 說明擲骰規則
        if(_evTreasureId){
          if(!_owned(_evTreasureId)){
            const _tInfo = (typeof window.TAIWAN_TREASURES === 'object' && window.TAIWAN_TREASURES[_evTreasureId]) || {};
            _items.push({ kind:'exact', id:_evTreasureId, label:'✨ ' + ((_tInfo.icon || '💎') + ' ' + (_tInfo.name || _evTreasureId)) });
          }
        } else if(_isMustTreasure){
          if(!_histHasCompRandom){
            _items.push({ kind:'treasure', rarity:(_p.tier === 'legendary' ? 'mythic' : 'legendary'),
              label:'✨ 未收錄至寶 ×1(' + (_p.tier === 'legendary' ? '優先神話級' : '優先傳說級') + ')' });
          }
        } else if(_p.treasureChance > 0){
          _notices.push('✨ 未收錄至寶(' + Math.round(_p.treasureChance * 100) + '% 機率):'
            + '以領獎當下的擲骰為準 — 領獎視窗有顯示「獲得」才算抽中;沒顯示就是<b style="color:#ffaa88;">沒抽中</b>,並非漏發。');
        }
        if(_items.length === 0 && _notices.length === 0) return;
        // 純說明(無補發)→ 一次性,本地旗標守門(換裝置最多多看一次,無發放故無風險)
        const _noticeFlag = 'lxps_wb_prob_notice_' + (_p.bossId || 'wb') + '_' + (_p.roundKey || 'r');
        if(_items.length === 0){
          try{ if(localStorage.getItem(_noticeFlag) === '1') return; }catch(_){}
        }
        // ⑤ 補償/說明視窗
        const _esc = function(s){ return String(s == null ? '' : s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;'); };
        const _isCompMode = _items.length > 0;
        const _ov = document.createElement('div');
        _ov.id = 'wb-award-modal-overlay';  // 沿用同 id 防與領獎窗並開
        _ov.style.cssText = 'position:fixed;inset:0;z-index:999999;display:flex;align-items:center;justify-content:center;'
          + 'background:rgba(5,8,20,0.88);backdrop-filter:blur(4px);padding:16px;';
        const _noticeHtml = _notices.length
          ? '<div style="margin:12px 0 4px;padding:12px 14px;background:rgba(120,140,200,0.10);border:1.5px dashed rgba(150,170,255,0.35);border-radius:12px;text-align:left;">'
            + '<div style="font-size:clamp(13px,1.7vw,16px);color:#aab8e8;font-weight:800;margin-bottom:4px;">ℹ️ 機率獎勵擲骰結果說明</div>'
            + _notices.map(function(n){ return '<div style="font-size:clamp(13px,1.7vw,16px);color:#c8d2f0;line-height:1.7;">' + n + '</div>'; }).join('')
            + '</div>'
          : '';
        _ov.innerHTML =
          '<div style="width:min(92vw,540px);max-height:88vh;overflow-y:auto;-webkit-overflow-scrolling:touch;'
          +   'background:linear-gradient(165deg,#241a08,#0e0a18 60%,#1a1028);border:2.5px solid rgba(255,200,80,0.75);'
          +   'border-radius:20px;box-shadow:0 0 50px rgba(255,180,60,0.4);padding:26px 22px;text-align:center;font-family:inherit;">'
          + '<div style="font-size:clamp(34px,5vw,46px);line-height:1;">' + (_isCompMode ? '🎁' : 'ℹ️') + '</div>'
          + '<div style="font-size:clamp(21px,3vw,28px);font-weight:900;color:#ffe066;margin-top:8px;text-shadow:0 0 14px rgba(255,200,80,0.6);">'
          +   (_isCompMode ? '排名獎勵補發' : '排名獎勵小提醒') + '</div>'
          + '<div style="font-size:clamp(14px,1.9vw,18px);color:#ffd9a0;margin-top:8px;line-height:1.6;">'
          +   (_isCompMode
              ? ('你之前領取的 ' + _esc(_p.bossName || '世界 BOSS') + ' 排名獎勵中,<br>有至寶因為一個「已修復的問題」不小心遺失了。<br><b style="color:#ffee88;">老師現在補發給你!</b>')
              : ('關於你領取的 ' + _esc(_p.bossName || '世界 BOSS') + ' 排名獎勵(第 ' + (_p.rank || '?') + ' 名),<br>機率型獎勵的擲骰結果說明如下:'))
          + '</div>'
          + (_isCompMode
              ? ('<div id="wb-comp-modal-body" style="margin:16px 0 6px;padding:14px 16px;background:rgba(255,200,80,0.08);border:1.5px solid rgba(255,200,80,0.3);border-radius:14px;text-align:left;">'
                + _items.map(function(it){ return '<div style="font-size:clamp(16px,2vw,20px);color:#ffeecc;line-height:1.9;">' + it.label + '</div>'; }).join('')
                + '</div>')
              : '')
          + _noticeHtml
          + '<button id="wb-comp-claim-btn" style="margin-top:14px;width:100%;padding:16px;font-size:clamp(18px,2.4vw,23px);font-weight:900;'
          +   'background:linear-gradient(135deg,#ffb347,#cc7722);color:#fff;border:none;border-radius:14px;cursor:pointer;'
          +   'box-shadow:0 4px 18px rgba(255,160,50,0.45);letter-spacing:2px;font-family:inherit;">'
          +   (_isCompMode ? '🎁 領取補償' : '👌 我知道了') + '</button>'
          + '</div>';
        document.body.appendChild(_ov);
        try{ if(typeof playSfx === 'function') playSfx('sfx-gentle', 0.5); }catch(_){}
        const _close = function(){ try{ _ov.remove(); }catch(_){ if(_ov && _ov.parentNode) _ov.parentNode.removeChild(_ov); } };
        const _btn = document.getElementById('wb-comp-claim-btn');
        if(_btn && !_isCompMode){
          // 純說明模式:點「我知道了」記旗標關閉
          _btn.onclick = function(){
            try{ localStorage.setItem(_noticeFlag, '1'); }catch(_){}
            _close();
          };
          return;
        }
        if(_btn) _btn.onclick = function(){
          try{
            _btn.disabled = true; _btn.style.opacity = '0.6'; _btn.textContent = '⏳ 補發中…';
            if(typeof window._taiwanTreasureData !== 'object' || !window._taiwanTreasureData) window._taiwanTreasureData = {};
            const _out = [];
            _items.forEach(function(it){
              if(it.kind === 'dragon'){
                if(!(window._taiwanTreasureData[it.id] && window._taiwanTreasureData[it.id].lv >= 1)){
                  window._taiwanTreasureData[it.id] = { lv:1, exp:0, equippedTo:null, invested:{hp:0,atk:0,sp:0,spd:0} };
                  try{ if(typeof window._advSaveTreasureUnlockHistory === 'function') window._advSaveTreasureUnlockHistory(it.id, 'wb_rank_compensate_dragon'); }catch(_){}
                  _out.push('🐉 <b style="color:#ff8866;">' + _esc((function(){ try{ const _t = window.TAIWAN_TREASURES && window.TAIWAN_TREASURES[it.id]; return (_t && _t.name) || '火龍王之牙'; }catch(_){ return '火龍王之牙'; } })()) + ' 已補發!</b>(至寶圖鑑查看)');
                }
              } else if(it.kind === 'exact'){
                // ★ v3.14.19 — 精準補發:依 manifest/歷史證據照原 id 補回(冪等)
                if(!(window._taiwanTreasureData[it.id] && window._taiwanTreasureData[it.id].lv >= 1)){
                  window._taiwanTreasureData[it.id] = { lv:1, exp:0, equippedTo:null, invested:{hp:0,atk:0,sp:0,spd:0} };
                  try{ if(typeof window._advSaveTreasureUnlockHistory === 'function') window._advSaveTreasureUnlockHistory(it.id, 'wb_rank_compensate_exact'); }catch(_){}
                  const _tNm = (typeof window.TAIWAN_TREASURES === 'object' && window.TAIWAN_TREASURES[it.id] && window.TAIWAN_TREASURES[it.id].name) || it.id;
                  _out.push('✨ <b style="color:#cc99ff;">【' + _esc(_tNm) + '】已補發!</b>(至寶圖鑑查看)');
                }
              } else if(it.kind === 'treasure'){
                // 抽一件未擁有(優先指定稀有度以上)
                if(typeof window.TAIWAN_TREASURES !== 'object'){ _out.push('✨ 至寶資料載入中,請稍後重新登入再領'); return; }
                const _R = { 'mythic':5, 'legendary':4, 'epic':3, 'rare':2, 'normal':1 };
                const _target = _R[it.rarity] || 4;
                const _cand = [];
                Object.keys(window.TAIWAN_TREASURES).forEach(function(_tid){
                  const _t = window.TAIWAN_TREASURES[_tid];
                  if(!_t) return;  // ★ v3.15.86 — 補償重發隨機未收錄至寶亦納入 8 龍王至寶(與主獎勵池一致)
                  if(window._taiwanTreasureData[_tid] && window._taiwanTreasureData[_tid].lv >= 1) return;
                  _cand.push({ id:_tid, name:_t.name, rarity:_t.rarity, rRank:(_R[_t.rarity||'normal']||1) });
                });
                if(_cand.length === 0){ _out.push('✨ 你已收服所有至寶,太強了!(無可補發)'); return; }
                _cand.sort(function(a,b){ return b.rRank - a.rRank; });
                const _at = _cand.filter(function(c){ return c.rRank >= _target; });
                const _pick = (_at.length ? _at : _cand)[Math.floor(Math.random() * (_at.length ? _at.length : _cand.length))];
                window._taiwanTreasureData[_pick.id] = { lv:1, exp:0, equippedTo:null, invested:{hp:0,atk:0,sp:0,spd:0} };
                try{ if(typeof window._advSaveTreasureUnlockHistory === 'function') window._advSaveTreasureUnlockHistory(_pick.id, 'wb_rank_compensate'); }catch(_){}
                _out.push('✨ <b style="color:#cc99ff;">獲得未收錄至寶【' + _esc(_pick.name) + '】!</b>');
              }
            });
            try{ if(typeof window._saveTaiwanTreasureData === 'function') window._saveTaiwanTreasureData(); else localStorage.setItem('lxps_taiwan_treasures', JSON.stringify(window._taiwanTreasureData)); }catch(_){}
            try{ if(typeof window.gameCloudSave === 'function') window.gameCloudSave(); }catch(_){}
            // ★ v4.13.6 — 補償完成即寫「已補償」持久旗標(雲端權威 + 本地保險),下次登入不再重跳
            try{ localStorage.setItem(_compDoneKey, '1'); }catch(_){}
            try{ if(window._fbDb && window._gUserId) updateDoc(PLAYER_DOC(window._gUserId), { 'wbPendingAward.treasureCompClaimedAt': Date.now() }); }catch(_){}
            const _body = document.getElementById('wb-comp-modal-body');
            if(_body) _body.innerHTML = _out.map(function(r){ return '<div style="font-size:clamp(16px,2vw,20px);color:#ffeecc;line-height:1.9;">' + r + '</div>'; }).join('')
              || '<div style="text-align:center;color:#ffeecc;">補發完成!</div>';
            _btn.textContent = '🎉 太棒了!';
            _btn.disabled = false; _btn.style.opacity = '1';
            _btn.onclick = _close;
            try{ if(typeof playSfx === 'function') playSfx('sfx-confirm', 0.5); }catch(_){}
            console.log('[WB至寶補償 v3.14.17] ✅ 已補發', _items.map(function(i){ return i.kind; }).join('+'));
          }catch(_eGo){
            console.warn('[WB至寶補償] 補發失敗', _eGo);
            _btn.textContent = '❌ 失敗,請重新登入再試';
            _btn.disabled = false; _btn.style.opacity = '1';
            _btn.onclick = _close;
          }
        };
      }catch(_eComp){ console.warn('[WB至寶補償] 檢查失敗', _eComp); }
    };
    // ════════════════════════════════════════════════════════════════════
    // ★ v3.12.9(2026-05-30)— 世界 BOSS 每日場次限制【第三版】
    // ────────────────────────────────────────────────────────────────────
    // 老師需求(2026-05-30 拍板):
    //   「能打幾次跟排行榜無關,就類似每日知識王一天只能打一次那樣設計就好了」
    //
    // 設計鐵則:
    //   ✅ 每位玩家有自己的計數器(players/{uid}.wbDailyCount = {count, dateStr})
    //   ✅ 進場前 +1(不論最後有沒有上榜、有沒有打完都算)
    //   ✅ 跨日(早上 08:00)自動重置
    //   ✅ 單人練習不算次數(原本就不算)
    //   ✅ 管理員也受限(對齊鐵律 1.32 一視同仁)
    //
    // 為什麼前兩版錯了:
    //   v3.10.13 改用「讀排行榜 battleHistory」計次 → 排行榜 slice(0,100) 會擠資料,
    //   且「進場後到上榜前」有空窗,玩家可在窗口內三連進場 → 一天打 3+ 場。
    //
    // 為什麼這版穩:
    //   - 計數器直接寫在 players/{uid},是玩家本人權威文件,Rules 容易設,單一來源
    //   - 跟知識王 _kingGetTodayStr / _kingCheckReset 同套模式(已穩定運行)
    //   - 進場 = 計數,結算什麼的都不關它的事
    //
    // 暴露 API:
    //   window._wbDailyLimit.canEnter()       → { allowed, used, limit, isAdmin }
    //   window._wbDailyLimit.bumpDailyCount() → 進場前呼叫,把計數 +1
    //   window._wbDailyLimit.getTodayCount()  → 老師查看用
    //   window._wbDailyLimit.resetDailyCount()→ 老師後台重置用
    //   window._wbDailyLimit.getLimit()       → 2
    //
    // Firestore 資料:
    //   players/{uid}.wbDailyCount = { count: 0~2, dateStr: 'YYYY-MM-DD' }
    //
    // ⚠ 注意:舊版的 todayCount(uid) 仍保留(讀排行榜版本),只給老師後台 _adminPeekWbDailyByEmail
    //         查「實際上榜次數」用,跟計次解耦,各司其職。
    // ════════════════════════════════════════════════════════════════════
    (function _setupWbDailyLimitV3(){
      const WB_DAILY_LIMIT = 2;

      // 「今日日期字串」(早上 08:00 為跨日基準)
      // ★ v3.13.12(2026-06-01) 鐵律 1.142 — 改用統一 _lxpsGetGameDayKey()
      //   原本是 getHours() < 8 用裝置本地時區,跨時區同學可能對不上。
      //   現改用強制 Asia/Taipei 時區,確保所有同學都對齊「台灣 8:00」。
      function _todayStr(){
        try{
          if(typeof window._lxpsGetGameDayKey === 'function'){
            return window._lxpsGetGameDayKey();
          }
        }catch(_){}
        // fallback (舊邏輯,保留以防全域函式還沒載入)
        const _now = new Date();
        if(_now.getHours() < 8){
          _now.setDate(_now.getDate() - 1);
        }
        const _y = _now.getFullYear();
        const _m = String(_now.getMonth()+1).padStart(2,'0');
        const _d = String(_now.getDate()).padStart(2,'0');
        return _y + '-' + _m + '-' + _d;
      }

      // 內部 cache(避免每次進場都讀雲端;會在 bumpDailyCount 時主動更新)
      let _cache = { uid: null, count: 0, dateStr: '' };

      // 從 Firestore 讀計數(跨日會自動重置回傳 0,並嘗試清掉舊資料)
      // forceFresh=true 直接讀雲端;否則先用 cache
      async function _readCount(opts){
        opts = opts || {};
        const _uid = window._gUserId;
        if(!_uid) return 0;
        const _today = _todayStr();
        // cache 命中且不強制 → 直接回傳
        if(!opts.forceFresh && _cache.uid === _uid && _cache.dateStr === _today){
          return _cache.count;
        }
        try{
          const _ref = doc(_fbDb, 'players', _uid);
          const _snap = await getDoc(_ref);
          if(!_snap.exists()){
            _cache = { uid: _uid, count: 0, dateStr: _today };
            return 0;
          }
          const _data = _snap.data() || {};
          const _wb = _data.wbDailyCount;
          if(!_wb || typeof _wb !== 'object'){
            _cache = { uid: _uid, count: 0, dateStr: _today };
            return 0;
          }
          // 跨日 → 視為 0(實際資料下次 bump 時會覆寫)
          if(_wb.dateStr !== _today){
            console.log('[WB-DailyLimit v3] 跨日(' + _wb.dateStr + ' → ' + _today + '),重置為 0');
            _cache = { uid: _uid, count: 0, dateStr: _today };
            return 0;
          }
          const _count = Math.max(0, Math.min(99, Number(_wb.count) || 0));
          _cache = { uid: _uid, count: _count, dateStr: _today };
          return _count;
        }catch(e){
          console.warn('[WB-DailyLimit v3] 讀計數失敗,保守回 0', e);
          return 0;
        }
      }

      // ════════════════════════════════════════════════════════════════
      // ★ v3.12.17(2026-05-31)— 補償券系統 helper
      // ────────────────────────────────────────────────────────────────
      // 老師需求:某玩家因 BUG 多被算 1 場,刪掉戰績 + 重置場次後,
      //          但玩家當天可能沒上線錯過機會 → 隔天 GM 補 1 場給他
      //          所有玩家每天可打 = 2 場 + 未使用補償券數
      //          補償場次計入排行榜總傷(公平,因為他本來就少打一場)
      //          排行榜顯示「(含 N 場補償)」全校透明可見
      //
      // 資料結構(永久,不跨日 reset):
      //   players/{uid}.wbBonusGrants = [
      //     {
      //       grantedAt: timestamp,         // 老師發放時間
      //       grantedDateStr: 'YYYY-MM-DD',  // 老師希望玩家「從哪一天起」可以用
      //       reason: '...',                  // 發放原因(學生看不到)
      //       grantedBy: 'admin_uid',         // 哪個 GM 發的
      //       usedAt: null | timestamp,       // 玩家何時用掉(null = 還沒用)
      //       usedDateStr: null | 'YYYY-MM-DD', // 玩家用掉時是哪一天
      //     },
      //     ...
      //   ]
      //
      // 規則:
      //   - 補償券永久有效,不會自動過期
      //   - canEnter() limit = 2 + 已發但未用的補償券數
      //   - bumpDailyCount() 在 count >= 2 時消耗一張補償券,並回傳 isBonus=true
      //   - GM 後台「補 1 次進場」按鈕 push 一筆進 wbBonusGrants(grantedDateStr = 隔天)
      // ════════════════════════════════════════════════════════════════
      async function _readBonusGrants(uid){
        if(!uid){ uid = window._gUserId; }
        if(!uid) return [];
        try{
          const _ref = doc(_fbDb, 'players', uid);
          const _snap = await getDoc(_ref);
          if(!_snap.exists()) return [];
          const _data = _snap.data() || {};
          return Array.isArray(_data.wbBonusGrants) ? _data.wbBonusGrants : [];
        }catch(e){
          console.warn('[WB-Bonus] 讀補償券失敗', e);
          return [];
        }
      }

      // 計算「目前可用」的補償券數(已發 + 到達生效日 + 未用)
      function _countUnusedBonus(grants, todayStr){
        if(!Array.isArray(grants)) return 0;
        return grants.filter(g =>
          g && !g.usedAt
          && typeof g.grantedDateStr === 'string'
          && g.grantedDateStr <= todayStr  // 字串比對:'2026-06-01' <= '2026-06-02' 正確
        ).length;
      }

      // 公開:檢查還能不能進場
      //   回 { allowed:bool, used:n, limit:N, baseLimit:N, bonusCount:n, ticketCount:n, isAdmin:bool }
      //   ★ v3.13.7 — 加 ticketCount(玩家持有入場券數,供 UI 顯示「使用入場券」按鈕)
      async function canEnter(){
        const _isAdmin = (typeof window._isAdminUser === 'function' && window._isAdminUser());
        const _uid = window._gUserId;
        if(!_uid){
          return { allowed: false, used: 0, limit: WB_DAILY_LIMIT, baseLimit: WB_DAILY_LIMIT, bonusCount: 0, ticketCount: 0, isAdmin: _isAdmin, reason: 'no_uid' };
        }
        // forceFresh:true → 進場守門直接讀雲端,避免本機 cache 慢半拍
        const _used = await _readCount({ forceFresh: true });
        // ★ v3.12.17 — 讀補償券,加進有效 limit
        const _grants = await _readBonusGrants(_uid);
        const _today = _todayStr();
        const _bonusCount = _countUnusedBonus(_grants, _today);
        const _effectiveLimit = WB_DAILY_LIMIT + _bonusCount;
        // ★ v3.15.31 — 管理員自動繞過每日次數上限(登入後自動生效)
        const _allowed = _isAdmin ? true : (_used < _effectiveLimit);
        // ★ v3.13.7 — 讀入場券持有數
        const _ticketCount = await _readTicketCount(_uid);
        console.log('[WB-DailyLimit v3] canEnter: used=' + _used + '/' + _effectiveLimit
          + ' (base=' + WB_DAILY_LIMIT + '+bonus=' + _bonusCount + ') allowed=' + _allowed
          + ' ticket=' + _ticketCount);
        return {
          allowed: _allowed,
          used: _used,
          limit: _effectiveLimit,
          baseLimit: WB_DAILY_LIMIT,
          bonusCount: _bonusCount,
          ticketCount: _ticketCount,
          isAdmin: _isAdmin
        };
      }

      // 進場前 +1
      //   ⚠ 用「讀-改-寫」+ runTransaction,避免兩個分頁同時進場時雙寫只算一次
      //   reason:debug 用,記在 log
      //   回 { ok:bool, count:n, isBonus:bool, bonusSource:'ticket'|'admin'|null }
      //   ★ v3.12.17 — 加 isBonus 旗標:若 count 超過 WB_DAILY_LIMIT 就消耗 1 張補償券
      //   ★ v3.13.7 — 加 bonusSource:區分入場券(ticket) vs GM 補償券(admin)
      //                規則:
      //                  (a) 若 wbNextBumpFromTicket=true(剛用 useTicket 退過場次)→ bonusSource='ticket'
      //                  (b) 若 _next > WB_DAILY_LIMIT 且消耗 wbBonusGrants → bonusSource='admin'
      //                  (c) 正常場次 → bonusSource=null
      async function bumpDailyCount(reason){
        const _uid = window._gUserId;
        if(!_uid){
          console.warn('[WB-DailyLimit v3] bumpDailyCount: 未登入,跳過');
          return { ok: false, count: 0, isBonus: false, bonusSource: null, reason: 'no_uid' };
        }
        const _today = _todayStr();
        try{
          const _ref = doc(_fbDb, 'players', _uid);
          // 用 transaction 避免並行寫入造成計次失準
          const _result = await runTransaction(_fbDb, async (tx) => {
            const _snap = await tx.get(_ref);
            let _cur = 0;
            let _grants = [];
            let _nextFromTicket = false;
            if(_snap.exists()){
              const _data = _snap.data() || {};
              const _wb = _data.wbDailyCount;
              if(_wb && _wb.dateStr === _today){
                _cur = Math.max(0, Number(_wb.count) || 0);
              }
              if(Array.isArray(_data.wbBonusGrants)) _grants = _data.wbBonusGrants.slice();
              // ★ v3.13.7 — 讀入場券旗標(useTicket 剛立的)
              _nextFromTicket = _data.wbNextBumpFromTicket === true;
              // 跨日就從 0 開始累計(自然重置)
            }
            const _next = _cur + 1;
            // ★ v3.13.7 — 判定 isBonus + bonusSource
            //   優先順序:ticket > admin > 正常
            let _isBonus = false;
            let _bonusSource = null;
            if(_nextFromTicket){
              // 路徑 (a):入場券退場次後的第一場
              _isBonus = true;
              _bonusSource = 'ticket';
            } else if(_next > WB_DAILY_LIMIT){
              // 路徑 (b):超過正常 limit → 消耗 wbBonusGrants
              const _bonusIdx = _grants.findIndex(g =>
                g && !g.usedAt
                && typeof g.grantedDateStr === 'string'
                && g.grantedDateStr <= _today
              );
              if(_bonusIdx >= 0){
                _grants[_bonusIdx] = Object.assign({}, _grants[_bonusIdx], {
                  usedAt: Date.now(),
                  usedDateStr: _today,
                });
                _isBonus = true;
                _bonusSource = 'admin';
              } else {
                // 不應該發生(canEnter 應該已擋下),但保險:不允許超過 limit 但無券
                console.error('[WB-DailyLimit v3.12.17] ⚠ count 超過 limit 但找不到可用補償券!_next='
                  + _next + ', limit=' + WB_DAILY_LIMIT + ', grants=' + _grants.length);
                throw new Error('No bonus grant available for over-limit bump');
              }
            }
            const _patch = {
              wbDailyCount: { count: _next, dateStr: _today, lastReason: String(reason || 'unknown'), lastAt: Date.now() }
            };
            if(_bonusSource === 'admin'){
              _patch.wbBonusGrants = _grants;  // 把更新的 usedAt 寫回
            }
            if(_nextFromTicket){
              // ★ v3.13.7 — 用掉旗標
              _patch.wbNextBumpFromTicket = false;
            }
            tx.set(_ref, _patch, { merge: true });
            return { count: _next, isBonus: _isBonus, bonusSource: _bonusSource };
          });
          _cache = { uid: _uid, count: _result.count, dateStr: _today };
          console.log('[WB-DailyLimit v3] bumpDailyCount (' + reason + '): ' + _result.count + '/' + WB_DAILY_LIMIT
            + (_result.isBonus
                ? (_result.bonusSource === 'ticket' ? ' 🎫 入場券場次!' : ' 🎫 GM 補償場次!')
                : ''));
          return { ok: true, count: _result.count, isBonus: _result.isBonus, bonusSource: _result.bonusSource };
        }catch(e){
          console.error('[WB-DailyLimit v3] bumpDailyCount 失敗', e);
          return { ok: false, count: 0, isBonus: false, bonusSource: null, reason: e && e.message };
        }
      }

      // 重置(老師後台用)
      async function resetDailyCount(){
        const _uid = window._gUserId;
        if(!_uid) return { ok: false, reason: 'no_uid' };
        try{
          await setDoc(doc(_fbDb, 'players', _uid),
            { wbDailyCount: deleteField() }, { merge: true });
          _cache = { uid: null, count: 0, dateStr: '' };
          console.log('[WB-DailyLimit v3] resetDailyCount: 已清除');
          return { ok: true };
        }catch(e){
          console.error('[WB-DailyLimit v3] resetDailyCount 失敗', e);
          return { ok: false, reason: e && e.message };
        }
      }

      // 給後台查指定 uid 的計次(讀雲端不走 cache)
      async function getCountByUid(uid){
        if(!uid) return null;
        try{
          const _ref = doc(_fbDb, 'players', uid);
          const _snap = await getDoc(_ref);
          if(!_snap.exists()) return { uid, count: 0, dateStr: _todayStr() };
          const _wb = (_snap.data() || {}).wbDailyCount;
          if(!_wb) return { uid, count: 0, dateStr: _todayStr() };
          return {
            uid,
            count: Number(_wb.count) || 0,
            dateStr: _wb.dateStr || '',
            lastReason: _wb.lastReason || '',
            lastAt: _wb.lastAt || 0,
          };
        }catch(e){
          return { uid, error: e && e.message };
        }
      }

      // 重置指定 uid 計次(老師後台救援用)
      async function resetCountByUid(uid){
        if(!uid) return { ok: false, reason: 'no_uid' };
        try{
          await setDoc(doc(_fbDb, 'players', uid),
            { wbDailyCount: deleteField() }, { merge: true });
          console.log('[WB-DailyLimit v3] resetCountByUid: uid=' + uid + ' 已清除');
          return { ok: true };
        }catch(e){
          return { ok: false, reason: e && e.message };
        }
      }

      // ════════════════════════════════════════════════════════════════
      // ★ v3.12.17 — 補償券系統 GM 後台 API
      // ────────────────────────────────────────────────────────────────
      // 場景:
      //   - 玩家因 BUG 多算 1 場 → GM 「刪錯誤場次 + 恢復進場 1 次」(_decreaseCountByUid)
      //   - 玩家當天沒上線錯過機會 → GM 「補 1 次進場」(grantBonusByUid)
      //   - 玩家用後 → 戰績卡片貼「🎫 補償場次」標籤
      //   - 排行榜該隊伍顯示「(含 N 場補償)」
      // ════════════════════════════════════════════════════════════════

      // 把指定玩家的 wbDailyCount 減 1(下限 0)
      //   ⚠ 用 transaction 安全;不要直接寫死值,玩家可能正在打第 2 場時被減
      //   reason:GM 填寫的原因,記在 lastReason 給未來查
      //   回 { ok:bool, newCount:n, oldCount:n }
      async function _decreaseCountByUid(uid, reason){
        if(!uid) return { ok: false, reason: 'no_uid' };
        const _today = _todayStr();
        try{
          const _ref = doc(_fbDb, 'players', uid);
          const _result = await runTransaction(_fbDb, async (tx) => {
            const _snap = await tx.get(_ref);
            if(!_snap.exists()) return { oldCount: 0, newCount: 0 };
            const _data = _snap.data() || {};
            const _wb = _data.wbDailyCount;
            if(!_wb || _wb.dateStr !== _today){
              // 跨日或無資料 → 視為 0,不能再減
              return { oldCount: 0, newCount: 0 };
            }
            const _old = Math.max(0, Number(_wb.count) || 0);
            const _new = Math.max(0, _old - 1);
            tx.set(_ref, {
              wbDailyCount: {
                count: _new,
                dateStr: _today,
                lastReason: 'GM 修正:' + String(reason || '(未填)'),
                lastAt: Date.now(),
              }
            }, { merge: true });
            return { oldCount: _old, newCount: _new };
          });
          console.log('[WB-DailyLimit v3.12.17] _decreaseCountByUid: uid=' + uid
            + ' ' + _result.oldCount + ' → ' + _result.newCount);
          return { ok: true, oldCount: _result.oldCount, newCount: _result.newCount };
        }catch(e){
          console.error('[WB-DailyLimit v3.12.17] _decreaseCountByUid 失敗', e);
          return { ok: false, reason: e && e.message };
        }
      }

      // GM 發補償券給某玩家(隔天可多打 1 場)
      //   opts.uid:目標玩家 uid
      //   opts.reason:GM 填寫原因(學生看不到,給未來查)
      //   opts.grantedDateStr:可選,預設「明天」(老師意圖:玩家錯過今天,明天補)
      //                       老師也可以傳「今天」(=立即可用)
      //   opts.grantedBy:GM uid(可選,預設讀 window._gUserId)
      //   回 { ok:bool, grantsCount:n }
      async function grantBonusByUid(opts){
        opts = opts || {};
        const _uid = opts.uid;
        if(!_uid) return { ok: false, reason: 'no_uid' };
        const _grantedBy = opts.grantedBy || window._gUserId || 'unknown';
        const _reason = String(opts.reason || '').slice(0, 200) || '(未填寫原因)';
        // 預設:給玩家「明天起可用」(老師意圖)
        // 計算「明天」: _todayStr 已處理 08:00 跨日,直接加 1 天
        let _grantedDateStr = opts.grantedDateStr;
        if(!_grantedDateStr){
          const _now = new Date();
          // 對齊 _todayStr 邏輯(08:00 前算昨天)
          if(_now.getHours() < 8){
            _now.setDate(_now.getDate() - 1);
          }
          // 給「明天」
          _now.setDate(_now.getDate() + 1);
          const _y = _now.getFullYear();
          const _m = String(_now.getMonth()+1).padStart(2,'0');
          const _d = String(_now.getDate()).padStart(2,'0');
          _grantedDateStr = _y + '-' + _m + '-' + _d;
        }
        const _newGrant = {
          grantedAt: Date.now(),
          grantedDateStr: _grantedDateStr,
          reason: _reason,
          grantedBy: _grantedBy,
          usedAt: null,
          usedDateStr: null,
        };
        try{
          const _ref = doc(_fbDb, 'players', _uid);
          const _count = await runTransaction(_fbDb, async (tx) => {
            const _snap = await tx.get(_ref);
            let _grants = [];
            if(_snap.exists()){
              const _d = _snap.data() || {};
              if(Array.isArray(_d.wbBonusGrants)) _grants = _d.wbBonusGrants.slice();
            }
            // 安全上限:單一玩家最多累積 20 張未使用券(避免無限灌)
            const _unused = _grants.filter(g => g && !g.usedAt).length;
            if(_unused >= 20){
              throw new Error('該玩家未使用補償券已達上限 20 張');
            }
            _grants.push(_newGrant);
            tx.set(_ref, { wbBonusGrants: _grants }, { merge: true });
            return _grants.length;
          });
          console.log('[WB-Bonus v3.12.17] grantBonusByUid: uid=' + _uid
            + ' 已發 1 張(生效日 ' + _grantedDateStr + '),總共 ' + _count + ' 張');
          return { ok: true, grantsCount: _count, grantedDateStr: _grantedDateStr };
        }catch(e){
          console.error('[WB-Bonus v3.12.17] grantBonusByUid 失敗', e);
          return { ok: false, reason: e && e.message };
        }
      }

      // 給後台查指定 uid 的補償券清單
      async function getBonusByUid(uid){
        if(!uid) return [];
        return await _readBonusGrants(uid);
      }

      // ════════════════════════════════════════════════════════════════
      // ★ v3.13.7(2026-05-31)— 世界 BOSS 挑戰入場券系統(玩家版)
      // ────────────────────────────────────────────────────────────────
      // 老師需求(2026-05-31):
      //   玩家昨天沒用完的場次,會轉換成「世界 BOSS 挑戰入場券」物品,
      //   最多持有 5 張。在換日登入時自動發到背包。
      //   使用 1 張可恢復 1 次入場次數,只能在沒有入場次數時使用。
      //   用入場券多打的場次上榜要註記「使用挑戰入場券」。
      //
      // 設計鐵則:
      //   - 入場券跟 GM 補償券(wbBonusGrants)是完全不同的東西,意義/性質/UI 分開
      //   - 入場券存在 playerBackpack.wb_entry_ticket(背包物品,上限 5)
      //   - 一天最多發 +2 張(對應每日上限 2 場)
      //   - 跨日(早上 08:00)登入時觸發 _grantDailyTickets,
      //     wbTicketLastGrantDateStr 守門防同日重複發
      //   - useTicket() 把 wbDailyCount.count 從 N → N-1(直接退還次數)
      //     ★ 不走 bumpDailyCount 路徑,語意完全獨立
      //
      // Firestore 資料:
      //   players/{uid}.playerBackpack.wb_entry_ticket = 0~5
      //   players/{uid}.wbTicketLastGrantDateStr = 'YYYY-MM-DD'
      //
      // API:
      //   _wbDailyLimit.getTicketCount() → async, 回 0~5
      //   _wbDailyLimit.grantDailyTickets() → 換日登入時自動呼叫
      //   _wbDailyLimit.useTicket() → 玩家主動消耗一張券
      //   _wbDailyLimit.grantTicketByUid(uid, n) → GM 後台補發(獨立於 wbBonusGrants)
      //   _wbDailyLimit.getTicketByUid(uid) → GM 後台查詢
      // ════════════════════════════════════════════════════════════════
      const WB_TICKET_MAX = 10;   // ★ v3.15.68 — 每日儲備入場卷上限 5→10(老師需求:1 天沒打可累積 2 張,最多持有 10 張)
      const WB_TICKET_ITEM_ID = 'wb_entry_ticket';

      // 讀指定 uid 的入場券持有數
      async function _readTicketCount(uid){
        if(!uid){ uid = window._gUserId; }
        if(!uid) return 0;
        try{
          const _ref = doc(_fbDb, 'players', uid);
          const _snap = await getDoc(_ref);
          if(!_snap.exists()) return 0;
          const _data = _snap.data() || {};
          const _bp = _data.playerBackpack;
          if(!_bp || typeof _bp !== 'object') return 0;
          return Math.max(0, Math.min(WB_TICKET_MAX, Number(_bp[WB_TICKET_ITEM_ID]) || 0));
        }catch(e){
          console.warn('[WB-Ticket v3.13.7] 讀入場券失敗', e);
          return 0;
        }
      }

      // 公開:查自己今日入場券持有數
      async function getTicketCount(){
        return await _readTicketCount(window._gUserId);
      }

      // 公開:GM 後台查指定 uid 入場券持有數
      async function getTicketByUid(uid){
        return await _readTicketCount(uid);
      }

      // 換日登入時觸發:把昨天沒用完的場次轉成入場券
      //   ⚠ 一天最多發 +2 張(對應每日上限 2 場),上限 5 張
      //   ⚠ wbTicketLastGrantDateStr 守門:同一天只發一次
      //   回 { granted: n, total: m, skipped: bool, reason: string }
      async function grantDailyTickets(){
        const _uid = window._gUserId;
        if(!_uid){
          return { granted: 0, total: 0, skipped: true, reason: 'no_uid' };
        }
        const _today = _todayStr();
        try{
          const _ref = doc(_fbDb, 'players', _uid);
          const _result = await runTransaction(_fbDb, async (tx) => {
            const _snap = await tx.get(_ref);
            if(!_snap.exists()){
              // 新玩家:寫入 lastGrantDateStr 標記但不發券(沒打過 BOSS 不能拿券)
              tx.set(_ref, { wbTicketLastGrantDateStr: _today }, { merge: true });
              return { granted: 0, total: 0, skipped: true, reason: 'new_player' };
            }
            const _data = _snap.data() || {};
            // ★ 守門 1:今天已發過
            if(_data.wbTicketLastGrantDateStr === _today){
              const _curHave = Math.max(0, Math.min(WB_TICKET_MAX,
                Number((_data.playerBackpack || {})[WB_TICKET_ITEM_ID]) || 0));
              return { granted: 0, total: _curHave, skipped: true, reason: 'already_granted_today' };
            }
            // ★ 計算昨天未用場次
            //   wbDailyCount.dateStr 不是今天 → 是昨天(或更早)的資料,
            //   unused = 2 - count(0/1/2)
            const _wb = _data.wbDailyCount;
            let _unused = WB_DAILY_LIMIT;  // 預設:沒有 wbDailyCount 視為昨天 0 場
            if(_wb && typeof _wb === 'object'){
              if(_wb.dateStr === _today){
                // 今天已經打過(罕見:玩家跨日後又打了一場才看到這個邏輯)→ 不發
                return { granted: 0, total: 0, skipped: true, reason: 'today_already_played' };
              }
              const _yesterdayCount = Math.max(0, Math.min(WB_DAILY_LIMIT, Number(_wb.count) || 0));
              _unused = Math.max(0, WB_DAILY_LIMIT - _yesterdayCount);
            }
            // ★ 守門 2:從未進場過(沒有 wbDailyCount 欄位)→ 視為新玩家保護
            //   只有「曾經有 wbDailyCount 資料」的玩家才能拿券(避免一開始就送 2 張給新玩家)
            if(!_wb){
              tx.set(_ref, { wbTicketLastGrantDateStr: _today }, { merge: true });
              return { granted: 0, total: 0, skipped: true, reason: 'never_played' };
            }
            // 計算實發
            const _curBp = (_data.playerBackpack && typeof _data.playerBackpack === 'object')
              ? _data.playerBackpack : {};
            const _curTickets = Math.max(0, Math.min(WB_TICKET_MAX,
              Number(_curBp[WB_TICKET_ITEM_ID]) || 0));
            const _canAdd = Math.max(0, Math.min(_unused, WB_TICKET_MAX - _curTickets));
            const _newTickets = _curTickets + _canAdd;
            // 寫入(即使 _canAdd=0 也要寫 wbTicketLastGrantDateStr 避免空轉)
            const _newBp = Object.assign({}, _curBp);
            if(_newTickets > 0){
              _newBp[WB_TICKET_ITEM_ID] = _newTickets;
            } else {
              delete _newBp[WB_TICKET_ITEM_ID];
            }
            tx.set(_ref, {
              playerBackpack: _newBp,
              wbTicketLastGrantDateStr: _today,
            }, { merge: true });
            return { granted: _canAdd, total: _newTickets, skipped: false, reason: 'ok',
                     unused: _unused, atMax: (_newTickets >= WB_TICKET_MAX) };
          });
          console.log('[WB-Ticket v3.13.7] grantDailyTickets:'
            + ' granted=' + _result.granted
            + ' total=' + _result.total
            + ' skipped=' + _result.skipped
            + ' reason=' + _result.reason);
          return _result;
        }catch(e){
          console.error('[WB-Ticket v3.13.7] grantDailyTickets 失敗', e);
          return { granted: 0, total: 0, skipped: true, reason: e && e.message };
        }
      }

      // 玩家主動消耗 1 張入場券,把 wbDailyCount.count 退 1
      //   ⚠ 守門:只能在「今日入場次數已用完」時使用
      //   ⚠ 守門:必須持有至少 1 張入場券
      //   ⚠ 守門:本場退還後本日仍會記為「來自入場券」(供 bumpDailyCount 下次呼叫帶 bonusSource='ticket')
      //   回 { ok: bool, newCount: n, ticketsLeft: m, reason: string }
      async function useTicket(){
        const _uid = window._gUserId;
        if(!_uid){
          return { ok: false, newCount: 0, ticketsLeft: 0, reason: 'no_uid' };
        }
        const _today = _todayStr();
        try{
          const _ref = doc(_fbDb, 'players', _uid);
          const _result = await runTransaction(_fbDb, async (tx) => {
            const _snap = await tx.get(_ref);
            if(!_snap.exists()){
              return { ok: false, newCount: 0, ticketsLeft: 0, reason: 'no_player_doc' };
            }
            const _data = _snap.data() || {};
            // ★ 守門 1:必須當前場次已用完
            //   讀 wbDailyCount;若 dateStr 不是今天 → 視為 0 場(理論上不需要券)
            const _wb = _data.wbDailyCount;
            let _curCount = 0;
            if(_wb && _wb.dateStr === _today){
              _curCount = Math.max(0, Math.min(WB_DAILY_LIMIT, Number(_wb.count) || 0));
            }
            if(_curCount < WB_DAILY_LIMIT){
              return { ok: false, newCount: _curCount, ticketsLeft: 0, reason: 'still_have_count' };
            }
            // ★ 守門 2:必須持有至少 1 張券
            const _curBp = (_data.playerBackpack && typeof _data.playerBackpack === 'object')
              ? _data.playerBackpack : {};
            const _curTickets = Math.max(0, Math.min(WB_TICKET_MAX,
              Number(_curBp[WB_TICKET_ITEM_ID]) || 0));
            if(_curTickets < 1){
              return { ok: false, newCount: _curCount, ticketsLeft: 0, reason: 'no_tickets' };
            }
            // 消耗 1 張,count -1
            const _newCount = _curCount - 1;
            const _newTickets = _curTickets - 1;
            const _newBp = Object.assign({}, _curBp);
            if(_newTickets > 0){
              _newBp[WB_TICKET_ITEM_ID] = _newTickets;
            } else {
              delete _newBp[WB_TICKET_ITEM_ID];
            }
            tx.set(_ref, {
              wbDailyCount: { count: _newCount, dateStr: _today,
                lastReason: 'ticket_use', lastAt: Date.now() },
              playerBackpack: _newBp,
              // ★ 旗標:下一場 bumpDailyCount 要帶 bonusSource='ticket'
              //   設計理由:玩家用券退場次後,接著進場時 bumpDailyCount 會把 count 從 N-1 → N,
              //   這個 N 場就要被標 _isBonus + _bonusSource='ticket'
              wbNextBumpFromTicket: true,
            }, { merge: true });
            return { ok: true, newCount: _newCount, ticketsLeft: _newTickets, reason: 'ok' };
          });
          // 更新本機 cache
          if(_result.ok){
            _cache = { uid: _uid, count: _result.newCount, dateStr: _today };
          }
          console.log('[WB-Ticket v3.13.7] useTicket:'
            + ' ok=' + _result.ok
            + ' newCount=' + _result.newCount
            + ' ticketsLeft=' + _result.ticketsLeft
            + ' reason=' + _result.reason);
          return _result;
        }catch(e){
          console.error('[WB-Ticket v3.13.7] useTicket 失敗', e);
          return { ok: false, newCount: 0, ticketsLeft: 0, reason: e && e.message };
        }
      }

      // GM 後台補發入場券(獨立於 wbBonusGrants 補償券)
      //   n: 要補幾張(會夾在 1~5;最終持有不超過 5)
      //   回 { ok, granted, total, reason }
      async function grantTicketByUid(uid, n){
        if(!uid){ return { ok: false, granted: 0, total: 0, reason: 'no_uid' }; }
        const _addRaw = Math.max(1, Math.min(WB_TICKET_MAX, Number(n) || 1));
        try{
          const _ref = doc(_fbDb, 'players', uid);
          const _result = await runTransaction(_fbDb, async (tx) => {
            const _snap = await tx.get(_ref);
            const _data = _snap.exists() ? (_snap.data() || {}) : {};
            const _curBp = (_data.playerBackpack && typeof _data.playerBackpack === 'object')
              ? _data.playerBackpack : {};
            const _curTickets = Math.max(0, Math.min(WB_TICKET_MAX,
              Number(_curBp[WB_TICKET_ITEM_ID]) || 0));
            const _newTickets = Math.min(WB_TICKET_MAX, _curTickets + _addRaw);
            const _actuallyAdded = _newTickets - _curTickets;
            const _newBp = Object.assign({}, _curBp);
            if(_newTickets > 0){
              _newBp[WB_TICKET_ITEM_ID] = _newTickets;
            } else {
              delete _newBp[WB_TICKET_ITEM_ID];
            }
            tx.set(_ref, { playerBackpack: _newBp }, { merge: true });
            return { ok: true, granted: _actuallyAdded, total: _newTickets, reason: 'ok' };
          });
          console.log('[WB-Ticket v3.13.7] grantTicketByUid(' + uid + ', ' + _addRaw
            + '): granted=' + _result.granted + ' total=' + _result.total);
          return _result;
        }catch(e){
          console.error('[WB-Ticket v3.13.7] grantTicketByUid 失敗', e);
          return { ok: false, granted: 0, total: 0, reason: e && e.message };
        }
      }

      // GM 後台清除指定 uid 的入場券
      async function clearTicketByUid(uid){
        if(!uid){ return { ok: false, reason: 'no_uid' }; }
        try{
          const _ref = doc(_fbDb, 'players', uid);
          await runTransaction(_fbDb, async (tx) => {
            const _snap = await tx.get(_ref);
            if(!_snap.exists()) return;
            const _data = _snap.data() || {};
            const _curBp = (_data.playerBackpack && typeof _data.playerBackpack === 'object')
              ? Object.assign({}, _data.playerBackpack) : {};
            delete _curBp[WB_TICKET_ITEM_ID];
            tx.set(_ref, { playerBackpack: _curBp }, { merge: true });
          });
          console.log('[WB-Ticket v3.13.7] clearTicketByUid(' + uid + '): cleared');
          return { ok: true };
        }catch(e){
          console.error('[WB-Ticket v3.13.7] clearTicketByUid 失敗', e);
          return { ok: false, reason: e && e.message };
        }
      }

      window._wbDailyLimit = {
        canEnter: canEnter,
        bumpDailyCount: bumpDailyCount,
        getTodayCount: function(){ return _readCount({ forceFresh: true }); },
        resetDailyCount: resetDailyCount,
        getCountByUid: getCountByUid,
        resetCountByUid: resetCountByUid,
        getLimit: function(){ return WB_DAILY_LIMIT; },
        // ★ v3.12.17 — 補償券系統 GM 後台 API
        _decreaseCountByUid: _decreaseCountByUid,  // GM 「刪錯誤場次 + 恢復進場」用
        grantBonusByUid: grantBonusByUid,           // GM 「補 1 次進場」用(隔天)
        getBonusByUid: getBonusByUid,                // GM 查補償券清單
        // ★ v3.13.7 — 入場券系統 API(跟補償券完全不同)
        getTicketCount: getTicketCount,              // 玩家自己查持有數
        grantDailyTickets: grantDailyTickets,        // 換日登入時自動呼叫
        useTicket: useTicket,                        // 玩家主動消耗
        grantTicketByUid: grantTicketByUid,          // GM 後台補發
        getTicketByUid: getTicketByUid,              // GM 後台查詢
        clearTicketByUid: clearTicketByUid,          // GM 後台清除
        getTicketMax: function(){ return WB_TICKET_MAX; },
        // ★ 舊版相容:helper for admin tools(這些舊 API 已不用於計次,只給後台查上榜次數用)
        _dateStrOf: function(ts){
          if(!ts || typeof ts !== 'number') return '';
          const _d = new Date(ts);
          if(_d.getHours() < 8){
            _d.setDate(_d.getDate() - 1);
          }
          const _y = _d.getFullYear();
          const _m = String(_d.getMonth()+1).padStart(2,'0');
          const _day = String(_d.getDate()).padStart(2,'0');
          return _y + '-' + _m + '-' + _day;
        },
        _todayStr: _todayStr,
      };
      console.log('[WB-DailyLimit v3] ✅ 模組已掛載(上限 ' + WB_DAILY_LIMIT + ' 場/日,玩家自己的計數器,排行榜無關)');
      console.log('[WB-Ticket v3.13.7] ✅ 入場券系統已掛載(上限 ' + WB_TICKET_MAX + ' 張,跨日自動發放)');
    })();
  })();


;(window._LXPS_MOD_OK || (window._LXPS_MOD_OK = {}))['game_cloud.js'] = 1;   /* ★ v5.235.0 載入完成記號(必須是本檔最後一行) */
